[package]
name = "bmo-net"
version = "0.1.0"
edition = "2021"
authors = ["BMO Project"]
description = "BMO Red -- tramas Ethernet como capability. Vacio: ver src/lib.rs"

# Sin dependencias, y eso es parte del borrado.
#
# Habia tres --`smoltcp`, `bitflags`, `volatile`-- y la primera es una pila
# TCP/IP entera que se descargaba y compilaba en cada build de un crate que no
# llamaba nadie. Cuando vuelva, si vuelve, sera en un programa de **Ring 3**:
# aqui abajo no pinta nada. Ver la cabecera de `src/lib.rs`.
[dependencies]
