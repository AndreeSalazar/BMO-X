[package]
name = "bmo-ahci"
version = "0.1.0"
edition = "2021"
description = "BMO AHCI/SATA Storage Driver — HAL-based controller driver"

[lib]

[dependencies]
# Sin dependencias: el driver solo necesita su StorageHal. Tenia bmo-abi,
# que no usaba para nada y arrastra `alloc` — que en Ring 0 no existe.
