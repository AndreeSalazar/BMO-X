[package]
name = "bmo-mmio-juicio"
version = "0.1.0"
edition = "2021"
description = "El juez de la cesion: se puede dar este rango fisico a un proceso de Ring 3?"
license = "Simbiosis"

[lib]

# ** CERO DEPENDENCIAS, por el mismo motivo que `bmo-bex-gate`.
#
# Lo consume Ring 0 --sin asignador, en el metal-- y `cargo test` en el
# anfitrion. Una sola dependencia que arrastrara `alloc` lo dejaria fuera del
# kernel, que es el consumidor por el que existe.
[dependencies]
