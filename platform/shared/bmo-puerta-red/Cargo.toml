[package]
name = "bmo-puerta-red"
version = "0.1.0"
edition = "2021"
description = "El GATE RED: la puerta que se paga una vez, el buzon sin syscalls y el radar de 4 ms. Politica pura, probada en el anfitrion."
license = "Apache-2.0"

# Sin dependencias, a proposito. Lo enlazan el kernel (que lo JUZGA) y Ring 3
# (que lo USA), y L8 no deja que Ring 3 enlace `bmo-net`, que es un driver.
[dependencies]
