[package]
name = "bmo-orquesta"
version = "0.1.0"
edition = "2021"
description = "La PARTITURA: que partes existen, y como se reparte una entre n atriles"
license = "Apache-2.0"

[lib]

# ** CERO DEPENDENCIAS, por el mismo motivo que `bmo-mmio-juicio`.
#
# Lo consume Ring 0 --sin asignador, sobre obreros que giran en `pause` y no
# pueden tomar un cerrojo-- y `cargo test` en el anfitrion. Una sola dependencia
# que arrastrara `alloc` lo dejaria fuera del kernel, que es el consumidor por el
# que existe.
[dependencies]
