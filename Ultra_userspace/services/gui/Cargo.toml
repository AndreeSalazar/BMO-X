[package]
name = "bmo-service-gui"
version = "0.1.0"
edition = "2021"
description = "El compositor de BMO — el proceso Ring 3 que es dueno de la pantalla"

[[bin]]
name = "compositor"
path = "src/main.rs"

[dependencies]
bmo-rtc = { path = "../../../platform/drivers/rtc" }
bmo-userland = { path = "../../userland" }
# La politica de FOCO (quien recibe las teclas) vive en el subsistema de
# entrada, no aqui: es el oficio de ese crate y ademas alli se puede PROBAR --
# este binario es `no_main` para un target sin sistema operativo y no corre un
# test. Aqui solo se pinta lo que la politica ya decidio.
bmo-input = { path = "../../../platform/drivers/usb/input", default-features = false }
