# ADead-BIB — C upper layers
# Not yet active — waiting for kernel base

CC = gcc
CFLAGS = -Wall -Wextra -ffreestanding -nostdlib -m64

.PHONY: all clean

all:
	@echo "[ADead-BIB] Not yet active — kernel base needed first"

clean:
	rm -f build/*.o

# Future targets will go here
