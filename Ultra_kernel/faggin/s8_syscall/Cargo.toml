[package]
name = "s8-syscall"
version = "2.0.0"
edition = "2021"
description = "Faggin stage 8 — see Ultra_kernel/fagging/README.md"

[dependencies]
serial-shared = { path = "../serial_shared" }
boot-context = { path = "../../boot_context" }
