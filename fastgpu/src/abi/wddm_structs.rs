#![allow(non_camel_case_types, non_snake_case, dead_code, non_upper_case_globals)]
use core::ffi::c_void;

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NODE_ADDRESS {
    pub NA_Node_Number: u16,
    pub NA_Bus_Number: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDRESS_OFFSET {
    pub Off_High: u16,
    pub Off_Low: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_ADDRESS {
    pub IA_Destination_ID: u32,
    pub IA_Destination_Offset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDRESS_RANGE {
    pub AR_Off_High: u16,
    pub AR_Length: u16,
    pub AR_Off_Low: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SELF_ID {
    pub SID_Phys_ID: u32,
    pub SID_Packet_ID: u32,
    pub SID_Gap_Count: u32,
    pub SID_Link_Active: u32,
    pub SID_Zero: u32,
    pub SID_Power_Class: u32,
    pub SID_Contender: u32,
    pub SID_Delay: u32,
    pub SID_Speed: u32,
    pub SID_More_Packets: u32,
    pub SID_Initiated_Rst: u32,
    pub SID_Port3: u32,
    pub SID_Port2: u32,
    pub SID_Port1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SELF_ID_MORE {
    pub SID_Phys_ID: u32,
    pub SID_Packet_ID: u32,
    pub SID_PortA: u32,
    pub SID_Reserved2: u32,
    pub SID_Sequence: u32,
    pub SID_One: u32,
    pub SID_PortE: u32,
    pub SID_PortD: u32,
    pub SID_PortC: u32,
    pub SID_PortB: u32,
    pub SID_More_Packets: u32,
    pub SID_Reserved3: u32,
    pub SID_PortH: u32,
    pub SID_PortG: u32,
    pub SID_PortF: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHY_CONFIGURATION_PACKET {
    pub PCP_Phys_ID: u32,
    pub PCP_Packet_ID: u32,
    pub PCP_Gap_Count: u32,
    pub PCP_Set_Gap_Count: u32,
    pub PCP_Force_Root: u32,
    pub PCP_Reserved1: u32,
    pub PCP_Reserved2: u32,
    pub PCP_Inverse: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASYNC_PACKET {
    pub AP_Priority: u16,
    pub AP_tCode: u16,
    pub AP_rt: u16,
    pub AP_tLabel: u16,
    pub AP_Destination_ID: u32,
    pub AP_Reserved: u16,
    pub AP_Rcode: u16,
    pub AP_Offset_High: u16,
    pub AP_Source_ID: u32,
    pub AP_Offset_Low: u32,
    pub AP_Extended_tCode: u16,
    pub AP_Data_Length: u16,
    pub AP_Quadlet_Data: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISOCH_HEADER {
    pub IH_Sy: u32,
    pub IH_tCode: u32,
    pub IH_Channel: u32,
    pub IH_Tag: u32,
    pub IH_Data_Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPEED_MAP {
    pub SPD_Length: u16,
    pub SPD_CRC: u16,
    pub SPD_Generation: u32,
    pub SPD_Speed_Code: [u8; 4032],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOPOLOGY_MAP {
    pub TOP_Length: u16,
    pub TOP_CRC: u16,
    pub TOP_Generation: u32,
    pub TOP_Node_Count: u16,
    pub TOP_Self_ID_Count: u16,
    pub TOP_Self_ID_Array: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONFIG_ROM {
    pub CR_Info: u32,
    pub CR_Signiture: u32,
    pub CR_BusInfoBlockCaps: u32,
    pub CR_Node_UniqueID: [u32; 2],
    pub CR_Root_Info: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEXTUAL_LEAF {
    pub TL_CRC: u16,
    pub TL_Length: u16,
    pub TL_Spec_Id: u32,
    pub TL_Language_Id: u32,
    pub TL_Data: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CYCLE_TIME {
    pub CL_CycleOffset: u32,
    pub CL_CycleCount: u32,
    pub CL_SecondCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDRESS_FIFO {
    pub FifoList: u32,
    pub FifoMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_INFO_W2K {
    pub Mdl: usize,
    pub ulOffset: u32,
    pub nLength: u32,
    pub fulNotificationOptions: u32,
    pub Context: usize,
    pub Fifo: usize,
    pub RequestPacket: usize,
    pub ResponseMdl: usize,
    pub ResponsePacket: usize,
    pub ResponseLength: *mut u32,
    pub ResponseEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_INFO_WXP {
    pub Mdl: usize,
    pub ulOffset: u32,
    pub nLength: u32,
    pub fulNotificationOptions: u32,
    pub Context: usize,
    pub Fifo: usize,
    pub RequestPacket: usize,
    pub ResponseMdl: usize,
    pub ResponsePacket: usize,
    pub ResponseLength: *mut u32,
    pub ResponseEvent: usize,
    pub ResponseCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONFIG_ROM_HEADER {
    pub Crc: u32,
    pub CrcLength: u32,
    pub InfoLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_INFO_CAPABILITIES {
    pub LinkSpeed: u32,
    pub Reserved0: u32,
    pub Generation: u32,
    pub MaxRom: u32,
    pub Reserved1: u32,
    pub MaxRec: u32,
    pub CycleClockAccuracy: u32,
    pub Reserved2: u32,
    pub PowerManagementCapable: u32,
    pub BusManagerCapable: u32,
    pub IsochCapable: u32,
    pub CycleMasterCapable: u32,
    pub IsochResourceManagerCapable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_INFO_IDS {
    pub ChipIdHigh: u32,
    pub NodeVendorId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIRECTORY_HEADER {
    pub Crc: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIRECTORY_ENTRY {
    pub Value: u32,
    pub KeyId: u32,
    pub KeyType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LEAF_FORMAT {
    pub SpecifierId: u32,
    pub DescriptorType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEXTUAL_LEAF_FORMAT {
    pub Language: u32,
    pub CharacterSet: u32,
    pub Width: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CROM {
    pub ConfigRomHeader: u32,
    pub Signature: u32,
    pub BusInfoCaps: u32,
    pub BusInfoIds: u32,
    pub ChipIdLow: u32,
    pub RootDirectoryHeader: u32,
    pub Entries: [u32; 250],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_RESET_DATA {
    pub ResetContext: usize,
    pub GenerationCount: u32,
    pub DeviceNodeId: u32,
    pub LocalNodeId: u32,
    pub SpeedToNode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISOCH_DESCRIPTOR {
    pub fulFlags: u32,
    pub Mdl: usize,
    pub ulLength: u32,
    pub nMaxBytesPerFrame: u32,
    pub ulSynch: u32,
    pub ulTag: u32,
    pub CycleTime: u32,
    pub Callback: usize,
    pub Context1: usize,
    pub Context2: usize,
    pub status: u32,
    pub DeviceReserved: [usize; 8],
    pub BusReserved: [usize; 8],
    pub PortReserved: [usize; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IEEE1394_SCATTER_GATHER_HEADER {
    pub HeaderLength: u16,
    pub DataLength: u16,
    pub HeaderData: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ASYNC_READ {
    pub DestinationAddress: u32,
    pub nNumberOfBytesToRead: u32,
    pub nBlockSize: u32,
    pub fulFlags: u32,
    pub Mdl: usize,
    pub ulGeneration: u32,
    pub chPriority: u8,
    pub nSpeed: u8,
    pub tCode: u8,
    pub Reserved: u8,
    pub ElapsedTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ASYNC_WRITE {
    pub DestinationAddress: u32,
    pub nNumberOfBytesToWrite: u32,
    pub nBlockSize: u32,
    pub fulFlags: u32,
    pub Mdl: usize,
    pub ulGeneration: u32,
    pub chPriority: u8,
    pub nSpeed: u8,
    pub tCode: u8,
    pub Reserved: u8,
    pub ElapsedTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ASYNC_LOCK {
    pub DestinationAddress: u32,
    pub nNumberOfArgBytes: u32,
    pub nNumberOfDataBytes: u32,
    pub fulTransactionType: u32,
    pub fulFlags: u32,
    pub Arguments: [u32; 2],
    pub DataValues: [u32; 2],
    pub pBuffer: usize,
    pub ulGeneration: u32,
    pub chPriority: u8,
    pub nSpeed: u8,
    pub tCode: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_ALLOCATE_BANDWIDTH {
    pub nMaxBytesPerFrameRequested: u32,
    pub fulSpeed: u32,
    pub hBandwidth: usize,
    pub BytesPerFrameAvailable: u32,
    pub SpeedSelected: u32,
    pub nBandwidthUnitsRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_ALLOCATE_CHANNEL {
    pub nRequestedChannel: u32,
    pub Channel: u32,
    pub ChannelsAvailable: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_ALLOCATE_RESOURCES_W2K {
    pub fulSpeed: u32,
    pub fulFlags: u32,
    pub nChannel: u32,
    pub nMaxBytesPerFrame: u32,
    pub nNumberOfBuffers: u32,
    pub nMaxBufferSize: u32,
    pub nQuadletsToStrip: u32,
    pub hResource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_ALLOCATE_RESOURCES_WXP {
    pub fulSpeed: u32,
    pub fulFlags: u32,
    pub nChannel: u32,
    pub nMaxBytesPerFrame: u32,
    pub nNumberOfBuffers: u32,
    pub nMaxBufferSize: u32,
    pub nQuadletsToStrip: u32,
    pub hResource: usize,
    pub ChannelMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_ATTACH_BUFFERS {
    pub hResource: usize,
    pub nNumberOfDescriptors: u32,
    pub pIsochDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_DETACH_BUFFERS {
    pub hResource: usize,
    pub nNumberOfDescriptors: u32,
    pub pIsochDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_FREE_BANDWIDTH {
    pub hBandwidth: usize,
    pub nMaxBytesPerFrameRequested: u32,
    pub fulSpeed: u32,
    pub BytesPerFrameAvailable: u32,
    pub SpeedSelected: u32,
    pub nBandwidthUnitsRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_FREE_CHANNEL {
    pub nChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_FREE_RESOURCES {
    pub hResource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_LISTEN {
    pub hResource: usize,
    pub fulFlags: u32,
    pub StartTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_QUERY_CURRENT_CYCLE_TIME {
    pub CycleTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_QUERY_RESOURCES {
    pub fulSpeed: u32,
    pub BytesPerFrameAvailable: u32,
    pub ChannelsAvailable: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_SET_CHANNEL_BANDWIDTH {
    pub hBandwidth: usize,
    pub nMaxBytesPerFrame: u32,
    pub nBandwidthUnitsRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_STOP {
    pub hResource: usize,
    pub fulFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_TALK {
    pub hResource: usize,
    pub fulFlags: u32,
    pub StartTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ISOCH_MODIFY_STREAM_PROPERTIES {
    pub hResource: usize,
    pub ChannelMask: u64,
    pub fulSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ALLOCATE_ADDRESS_RANGE {
    pub Mdl: usize,
    pub fulFlags: u32,
    pub nLength: u32,
    pub MaxSegmentSize: u32,
    pub fulAccessType: u32,
    pub fulNotificationOptions: u32,
    pub Callback: usize,
    pub Context: usize,
    pub Required1394Offset: u32,
    pub FifoSListHead: usize,
    pub FifoSpinLock: usize,
    pub AddressesReturned: u32,
    pub p1394AddressRange: usize,
    pub hAddressRange: usize,
    pub DeviceExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_FREE_ADDRESS_RANGE {
    pub nAddressesToFree: u32,
    pub p1394AddressRange: usize,
    pub pAddressRange: usize,
    pub DeviceExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_LOCAL_HOST_INFORMATION {
    pub nLevel: u32,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_1394_ADDRESS_FROM_DEVICE_OBJECT {
    pub fulFlags: u32,
    pub NodeAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_CONTROL {
    pub ulIoControlCode: u32,
    pub pInBuffer: usize,
    pub ulInBufferLength: u32,
    pub pOutBuffer: usize,
    pub ulOutBufferLength: u32,
    pub BytesReturned: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_MAX_SPEED_BETWEEN_DEVICES {
    pub fulFlags: u32,
    pub ulNumberOfDestinations: u32,
    pub hDestinationDeviceObjects: [usize; 64],
    pub fulSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_SET_DEVICE_XMIT_PROPERTIES {
    pub fulSpeed: u32,
    pub fulPriority: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_SET_LOCAL_HOST_PROPERTIES {
    pub nLevel: u32,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_CONFIGURATION_INFORMATION {
    pub ConfigRom: usize,
    pub UnitDirectoryBufferSize: u32,
    pub UnitDirectory: usize,
    pub UnitDirectoryLocation: u32,
    pub UnitDependentDirectoryBufferSize: u32,
    pub UnitDependentDirectory: usize,
    pub UnitDependentDirectoryLocation: u32,
    pub VendorLeafBufferSize: u32,
    pub VendorLeaf: usize,
    pub ModelLeafBufferSize: u32,
    pub ModelLeaf: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_CONFIG_ROM {
    pub GenerationCount: u32,
    pub ConfigRom: usize,
    pub UnitDirectoryIndex: u32,
    pub UnitDependentDirectoryIndex: u32,
    pub VendorLeafIndex: u32,
    pub ModelLeafIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_BUS_RESET {
    pub fulFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_GENERATION_COUNT {
    pub GenerationCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_SEND_PHY_CONFIGURATION_PACKET {
    pub PhyConfigurationPacket: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_SEND_PHY_PACKET {
    pub Flags: u32,
    pub GenerationCount: u32,
    pub PhyPacket: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_RECEIVE_PHY_PACKETS {
    pub Flags: u32,
    pub PhyPacketRoutine: usize,
    pub PhyPacketContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_GET_SPEED_TOPOLOGY_MAPS {
    pub SpeedMap: usize,
    pub TopologyMap: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_BUS_RESET_NOTIFICATION {
    pub fulFlags: u32,
    pub ResetRoutine: usize,
    pub ResetContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB_REQ_ASYNC_STREAM {
    pub nNumberOfBytesToStream: u32,
    pub fulFlags: u32,
    pub Mdl: usize,
    pub ulTag: u32,
    pub nChannel: u32,
    pub ulSynch: u32,
    pub Reserved: u32,
    pub nSpeed: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IRB {
    pub FunctionNumber: u32,
    pub Flags: u32,
    pub BusReserved: [usize; 1],
    pub PortReserved: [usize; 1],
    pub AsyncRead: u32,
    pub AsyncWrite: u32,
    pub AsyncLock: u32,
    pub IsochAllocateBandwidth: u32,
    pub IsochAllocateChannel: u32,
    pub IsochAllocateResources: u32,
    pub IsochAttachBuffers: u32,
    pub IsochDetachBuffers: u32,
    pub IsochFreeBandwidth: u32,
    pub IsochFreeChannel: u32,
    pub IsochFreeResources: u32,
    pub IsochListen: u32,
    pub IsochQueryCurrentCycleTime: u32,
    pub IsochQueryResources: u32,
    pub IsochSetChannelBandwidth: u32,
    pub IsochStop: u32,
    pub IsochTalk: u32,
    pub IsochModifyStreamProperties: u32,
    pub AllocateAddressRange: u32,
    pub FreeAddressRange: u32,
    pub GetLocalHostInformation: u32,
    pub Get1394AddressFromDeviceObject: u32,
    pub Control: u32,
    pub GetMaxSpeedBetweenDevices: u32,
    pub SetDeviceXmitProperties: u32,
    pub SetLocalHostProperties: u32,
    pub GetConfigurationInformation: u32,
    pub GetConfigRom: u32,
    pub BusReset: u32,
    pub GetGenerationCount: u32,
    pub SendPhyConfigurationPacket: u32,
    pub SendPhyPacket: u32,
    pub ReceivePhyPackets: u32,
    pub GetSpeedTopologyMaps: u32,
    pub BusResetNotification: u32,
    pub AsyncStream: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_LOCAL_HOST_PROPS1 {
    pub fulFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_LOCAL_HOST_PROPS2 {
    pub GapCountLowerBound: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_LOCAL_HOST_PROPS3 {
    pub fulFlags: u32,
    pub hCromData: usize,
    pub nLength: u32,
    pub Mdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_LOCAL_HOST_PROPS4 {
    pub MaxAsyncPayloadRequested: u32,
    pub MaxAsyncPayloadResult: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_LOCAL_HOST_PROPS5 {
    pub DebugAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO1 {
    pub UniqueId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO2 {
    pub HostCapabilities: u32,
    pub MaxAsyncReadRequest: u32,
    pub MaxAsyncWriteRequest: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO3 {
    pub deciWattsSupplied: u32,
    pub Voltage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO4 {
    pub PhysAddrMappingRoutine: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO5 {
    pub ConfigRom: usize,
    pub ConfigRomLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO6 {
    pub CsrBaseAddress: u32,
    pub CsrDataLength: u32,
    pub CsrDataBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO7_W2K {
    pub HostDmaCapabilities: u32,
    pub MaxDmaBufferSize: u64,
    pub MaxOutstandingXmitRequests: u32,
    pub MaxOutstandingXmitResponses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO7_WXP {
    pub HostDmaCapabilities: u32,
    pub MaxDmaBufferSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LOCAL_HOST_INFO8 {
    pub MajorVersion: u16,
    pub MinorVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_CHANNELSR {
    pub NC_Channel: u32,
    pub NC_Reserved: u32,
    pub NC_Npm_ID: u32,
    pub NC_Valid: u32,
    pub NC_One: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NODE_DEVICE_EXTENSION {
    pub Tag: u32,
    pub bConfigurationInformationValid: u8,
    pub ConfigRom: usize,
    pub UnitDirectoryLength: u32,
    pub UnitDirectory: usize,
    pub UnitDirectoryLocation: u32,
    pub UnitDependentDirectoryLength: u32,
    pub UnitDependentDirectory: usize,
    pub UnitDependentDirectoryLocation: u32,
    pub VendorLeafLength: u32,
    pub VendorLeaf: usize,
    pub ModelLeafLength: u32,
    pub ModelLeaf: usize,
    pub NodeAddress: u32,
    pub Speed: u8,
    pub Priority: u8,
    pub Irp: usize,
    pub DeviceObject: usize,
    pub PortDeviceObject: usize,
    pub DeviceInformation: usize,
    pub ResetRoutine: usize,
    pub ResetContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BANDWIDTH_ALLOCATION {
    pub AllocationList: u32,
    pub Tag: u32,
    pub OwnedUnits: u32,
    pub fulSpeed: u32,
    pub bRemoteAllocation: u8,
    pub Generation: u32,
    pub DeviceExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPCR {
    pub Payload: u32,
    pub OverheadID: u32,
    pub DataRate: u32,
    pub Channel: u32,
    pub Reserved: u32,
    pub PPCCounter: u32,
    pub BCCCounter: u32,
    pub OnLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPCR {
    pub Reserved0: u32,
    pub Channel: u32,
    pub Reserved1: u32,
    pub PPCCounter: u32,
    pub BCCCounter: u32,
    pub OnLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AV_PCR {
    pub oPCR: u32,
    pub iPCR: u32,
    pub ulongData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_UNIT_IDS {
    pub UniqueID: i64,
    pub VendorID: u32,
    pub ModelID: u32,
    pub ulVendorLength: u32,
    pub VendorText: usize,
    pub ulModelLength: u32,
    pub ModelText: usize,
    pub UnitModelID: u32,
    pub ulUnitModelLength: u32,
    pub UnitModelText: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_UNIT_CAPABILITIES {
    pub NumOutputPlugs: u32,
    pub NumInputPlugs: u32,
    pub MaxDataRate: u32,
    pub CTSFlags: u32,
    pub HardwareFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNIT_ISOCH_PARAMS {
    pub RX_NumPackets: u32,
    pub RX_NumDescriptors: u32,
    pub TX_NumPackets: u32,
    pub TX_NumDescriptors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_GENERATION_NODE {
    pub GenerationCount: u32,
    pub LocalNodeAddress: u32,
    pub DeviceNodeAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNIT_DDI_VERSION {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNIT_DIAG_LEVEL {
    pub DiagLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_UNIT_INFO {
    pub nLevel: u32,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_CMP_ADDRESS_TYPE {
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_UNIT_INFO {
    pub nLevel: u32,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_GET_PLUG_HANDLE {
    pub PlugNum: u32,
    pub Type: u32,
    pub hPlug: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_GET_PLUG_STATE {
    pub hPlug: usize,
    pub State: u32,
    pub DataRate: u32,
    pub Payload: u32,
    pub BC_Connections: u32,
    pub PP_Connections: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_DATA_FORMAT_VER3 {
    pub FMT: u8,
    pub FDF_hi: u8,
    pub FDF_mid: u8,
    pub FDF_lo: u8,
    pub bHeader: u8,
    pub Padding: u8,
    pub BlockSize: u8,
    pub Fraction: u8,
    pub BlockPeriod: u32,
    pub BlockPeriodRemainder: u32,
    pub BlocksPerPacket: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_DATA_FORMAT_VER2 {
    pub FMT: u8,
    pub FDF_hi: u8,
    pub FDF_mid: u8,
    pub FDF_lo: u8,
    pub bHeader: u8,
    pub Padding: u8,
    pub BlockSize: u8,
    pub Fraction: u8,
    pub BlockPeriod: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_CONNECT_VER3 {
    pub hOutputPlug: usize,
    pub hInputPlug: usize,
    pub Type: u32,
    pub Format: u32,
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_CONNECT_VER2 {
    pub hOutputPlug: usize,
    pub hInputPlug: usize,
    pub Type: u32,
    pub Format: u32,
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_DISCONNECT {
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_VALIDATE_INFO {
    pub hConnect: usize,
    pub Context: usize,
    pub TimeStamp: u32,
    pub Packet: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_NOTIFY_INFO {
    pub hConnect: usize,
    pub Context: usize,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_FRAME {
    pub Reserved: usize,
    pub pNext: usize,
    pub Flags: u32,
    pub pfnValidate: usize,
    pub ValidateContext: usize,
    pub pfnNotify: usize,
    pub NotifyContext: usize,
    pub Timestamp: u32,
    pub Status: u32,
    pub Packet: *mut u8,
    pub CompletedBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_ATTACH_FRAME {
    pub hConnect: usize,
    pub FrameLength: u32,
    pub SourceLength: u32,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_CANCEL_FRAME {
    pub hConnect: usize,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_TALK {
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_LISTEN {
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_STOP {
    pub hConnect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCP_FRAME {
    pub ctype: u8,
    pub cts: u8,
    pub payload: [u8; 511],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCP_SEND_REQUEST {
    pub NodeAddress: u32,
    pub Length: u32,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCP_GET_RESPONSE {
    pub NodeAddress: u32,
    pub Length: u32,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCP_GET_REQUEST {
    pub NodeAddress: u32,
    pub Length: u32,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCP_SEND_RESPONSE {
    pub NodeAddress: u32,
    pub Length: u32,
    pub Frame: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_FCP_NOTIFY {
    pub Flags: u32,
    pub NodeAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_NOTIFY_INFO {
    pub hPlug: usize,
    pub Pcr: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_CREATE_PLUG {
    pub PlugType: u32,
    pub Pcr: u32,
    pub pfnNotify: usize,
    pub Context: usize,
    pub PlugNum: u32,
    pub hPlug: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_DELETE_PLUG {
    pub hPlug: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_SET_PLUG {
    pub hPlug: usize,
    pub Pcr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_RESET_NOTIFY {
    pub Flags: u32,
    pub pfnNotify: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_UNIT_DIRECTORY {
    pub Flags: u32,
    pub UnitSpecId: u32,
    pub UnitSwVersion: u32,
    pub hCromEntry: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_MONITOR_INFO {
    pub State: u32,
    pub PlugNum: u32,
    pub PlugType: u32,
    pub Pcr: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMP_MONITOR_PLUGS {
    pub Flags: u32,
    pub pfnNotify: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AV_61883_REQUEST {
    pub Function: u32,
    pub Version: u32,
    pub Flags: u32,
    pub GetUnitInfo: u32,
    pub SetUnitInfo: u32,
    pub GetPlugHandle: u32,
    pub GetPlugState: u32,
    pub Connect: u32,
    pub Disconnect: u32,
    pub AttachFrame: u32,
    pub CancelFrame: u32,
    pub Talk: u32,
    pub Listen: u32,
    pub Stop: u32,
    pub Request: u32,
    pub Response: u32,
    pub SendRequest: u32,
    pub GetResponse: u32,
    pub GetRequest: u32,
    pub SendResponse: u32,
    pub SetFcpNotify: u32,
    pub CreatePlug: u32,
    pub DeletePlug: u32,
    pub SetPlug: u32,
    pub BusResetNotify: u32,
    pub SetUnitDirectory: u32,
    pub MonitorPlugs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GEN_ADDR {
    pub AddressSpaceID: u8,
    pub BitWidth: u8,
    pub BitOffset: u8,
    pub AccessSize: u8,
    pub Address: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSDP {
    pub Signature: u64,
    pub Checksum: u8,
    pub OEMID: [u8; 6],
    pub Revision: u8,
    pub RsdtAddress: u32,
    pub Length: u32,
    pub XsdtAddress: u64,
    pub XChecksum: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DESCRIPTION_HEADER {
    pub Signature: u32,
    pub Length: u32,
    pub Revision: u8,
    pub Checksum: u8,
    pub OEMID: [i8; 1],
    pub OEMTableID: [i8; 1],
    pub OEMRevision: u32,
    pub CreatorID: [i8; 1],
    pub CreatorRev: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FACS {
    pub Signature: u32,
    pub Length: u32,
    pub HardwareSignature: u32,
    pub pFirmwareWakingVector: u32,
    pub GlobalLock: u32,
    pub Flags: u32,
    pub x_FirmwareWakingVector: u64,
    pub version: u8,
    pub Reserved: [u8; 31],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FADT {
    pub Header: u32,
    pub facs: u32,
    pub dsdt: u32,
    pub int_model: u8,
    pub pm_profile: u8,
    pub sci_int_vector: u16,
    pub smi_cmd_io_port: u32,
    pub acpi_on_value: u8,
    pub acpi_off_value: u8,
    pub s4bios_req: u8,
    pub pstate_control: u8,
    pub pm1a_evt_blk_io_port: u32,
    pub pm1b_evt_blk_io_port: u32,
    pub pm1a_ctrl_blk_io_port: u32,
    pub pm1b_ctrl_blk_io_port: u32,
    pub pm2_ctrl_blk_io_port: u32,
    pub pm_tmr_blk_io_port: u32,
    pub gp0_blk_io_port: u32,
    pub gp1_blk_io_port: u32,
    pub pm1_evt_len: u8,
    pub pm1_ctrl_len: u8,
    pub pm2_ctrl_len: u8,
    pub pm_tmr_len: u8,
    pub gp0_blk_len: u8,
    pub gp1_blk_len: u8,
    pub gp1_base: u8,
    pub cstate_control: u8,
    pub lvl2_latency: u16,
    pub lvl3_latency: u16,
    pub flush_size: u16,
    pub flush_stride: u16,
    pub duty_offset: u8,
    pub duty_width: u8,
    pub day_alarm_index: u8,
    pub month_alarm_index: u8,
    pub century_alarm_index: u8,
    pub boot_arch: u16,
    pub reserved3: [u8; 1],
    pub flags: u32,
    pub reset_reg: u32,
    pub reset_val: u8,
    pub arm_boot_arch: u16,
    pub minor_version_number: u8,
    pub x_firmware_ctrl: u64,
    pub x_dsdt: u64,
    pub x_pm1a_evt_blk: u32,
    pub x_pm1b_evt_blk: u32,
    pub x_pm1a_ctrl_blk: u32,
    pub x_pm1b_ctrl_blk: u32,
    pub x_pm2_ctrl_blk: u32,
    pub x_pm_tmr_blk: u32,
    pub x_gp0_blk: u32,
    pub x_gp1_blk: u32,
    pub sleep_control_reg: u32,
    pub sleep_status_reg: u32,
    pub hypervisor_vendor_identity: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_SRAT {
    pub Header: u32,
    pub TableRevision: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_SRAT_ENTRY {
    pub Type: u8,
    pub Length: u8,
    pub ProximityDomainLow: u8,
    pub ApicId: u8,
    pub Enabled: u32,
    pub Reserved: u32,
    pub SApicEid: u8,
    pub ProximityDomainHigh: [u8; 3],
    pub ClockDomain: u32,
    pub ProximityDomain: u32,
    pub Reserved_1: [u8; 2],
    pub Base: u64,
    pub Length_1: u64,
    pub Reserved2: u32,
    pub Enabled_1: u32,
    pub HotPlug: u32,
    pub NonVolatile: u32,
    pub SpecificPurpose: u32,
    pub Reserved_2: u32,
    pub Reserved3: [u8; 8],
    pub Reserved_3: [u8; 2],
    pub ProximityDomain_1: u32,
    pub ApicId_1: u32,
    pub Enabled_2: u32,
    pub Reserved_4: u32,
    pub ClockDomain_1: u32,
    pub Reserved2_1: u32,
    pub ProximityDomain_2: u32,
    pub ProcessorUid: u32,
    pub Enabled_3: u32,
    pub Reserved_5: u32,
    pub ClockDomain_2: u32,
    pub ProximityDomain_3: u32,
    pub Reserved_6: [u8; 2],
    pub ITSID: u32,
    pub Reserved_7: u8,
    pub DeviceHandleType: u8,
    pub ProximityDomain_4: u32,
    pub ACPI_HID: [u8; 8],
    pub ACPI_UID: u32,
    pub Reserved_8: u32,
    pub PCISegment: u16,
    pub PCIBDFNumber: u16,
    pub Reserved_9: [u8; 12],
    pub Enabled_4: u32,
    pub ArchitecturalTransactions: u32,
    pub Reserved_10: u32,
    pub Reserved2_2: u32,
    pub Reserved_11: u8,
    pub DeviceHandleType_1: u8,
    pub ProximityDomain_5: u32,
    pub ACPI_HID_1: [u8; 8],
    pub ACPI_UID_1: u32,
    pub Reserved_12: u32,
    pub PCISegment_1: u16,
    pub PCIBDFNumber_1: u16,
    pub Reserved_13: [u8; 12],
    pub Enabled_5: u32,
    pub ArchitecturalTransactions_1: u32,
    pub Reserved_14: u32,
    pub Reserved2_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_POWER_STATE {
    pub PowerStateValue: u8,
    pub PowerStateInformationIndex: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_POWER_NODE {
    pub Flag: u8,
    pub Reserved1: u8,
    pub MpnId: u16,
    pub Length: u32,
    pub BaseAddressLow: u32,
    pub BaseAddressHigh: u32,
    pub LengthLow: u32,
    pub LengthHigh: u32,
    pub PowerStateCount: u32,
    pub PhysicalComponentCount: u32,
    pub MpState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_MPST {
    pub Header: u32,
    pub SubspaceId: u8,
    pub Reserved2: [u8; 3],
    pub MpnCount: u16,
    pub Reserved: [u8; 2],
    pub Mpn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_STATE_CHARACTERISTICS {
    pub AsUINT8: u32,
    pub Value: u32,
    pub Revision: u32,
    pub Flag: u32,
    pub Reserved1: u16,
    pub PowerInMPS0: u32,
    pub PowerSavingToMPS0: u32,
    pub ExitLatencyNs: u64,
    pub Reserved2: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_COOLING_REGION {
    pub PowerCommandRegister: u32,
    pub PowerStatusRegister: u32,
    pub PowerStateId: u32,
    pub MpnId: u32,
    pub EnergyConsumed: u64,
    pub ExpectedPowerConsumed: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_MSCT {
    pub Header: u32,
    pub DomainInfoOffset: u32,
    pub ProximityDomainCount: u32,
    pub ClockDomainCount: u32,
    pub MaximumPhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_MSCT_ENTRY {
    pub Revision: u8,
    pub Length: u8,
    pub DomainIdRangeLow: u32,
    pub DomainIdRangeHigh: u32,
    pub ProcessorCapacity: u32,
    pub MemoryCapacity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MAPIC {
    pub Header: u32,
    pub LocalAPICAddress: u32,
    pub Flags: u32,
    pub APICTables: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct APICTABLE {
    pub Type: u8,
    pub Length: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCLOCALAPIC {
    pub Type: u8,
    pub Length: u8,
    pub ACPIProcessorID: u8,
    pub APICID: u8,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOAPIC {
    pub Type: u8,
    pub Length: u8,
    pub IOAPICID: u8,
    pub Reserved: u8,
    pub IOAPICAddress: u32,
    pub SystemVectorBase: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISA_VECTOR {
    pub Type: u8,
    pub Length: u8,
    pub Bus: u8,
    pub Source: u8,
    pub GlobalSystemInterruptVector: u32,
    pub Flags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_NMISOURCE {
    pub Type: u8,
    pub Length: u8,
    pub Flags: u16,
    pub GlobalSystemInterruptVector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCAL_NMISOURCE {
    pub Type: u8,
    pub Length: u8,
    pub ProcessorID: u8,
    pub Flags: u16,
    pub LINTIN: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCLOCALSAPIC {
    pub Type: u8,
    pub Length: u8,
    pub ACPIProcessorID: u8,
    pub APICID: u8,
    pub APICEID: u8,
    pub Reserved: [u8; 3],
    pub Flags: u32,
    pub ACPIProcessorUIDInteger: u32,
    pub ACPIProcessorUIDString: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOSAPIC {
    pub Type: u8,
    pub Length: u8,
    pub IOSAPICID: u8,
    pub Reserved: u8,
    pub SystemVectorBase: u32,
    pub IOSAPICAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLATFORM_INTERRUPT {
    pub Type: u8,
    pub Length: u8,
    pub Flags: u16,
    pub InterruptType: u8,
    pub APICID: u8,
    pub ACPIEID: u8,
    pub IOSAPICVector: u8,
    pub GlobalVector: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCLOCALX2APIC {
    pub Type: u8,
    pub Length: u8,
    pub Reserved: u16,
    pub APICID: u32,
    pub Flags: u32,
    pub ACPIProcessorID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCAL_X2APIC_NMISOURCE {
    pub Type: u8,
    pub Length: u8,
    pub Flags: u16,
    pub ProcessorID: u32,
    pub LINTIN: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCLOCALGIC {
    pub Type: u8,
    pub Length: u8,
    pub Reserved: u16,
    pub Identifier: u32,
    pub AcpiProcessorId: u32,
    pub Flags: u32,
    pub ParkingProtocolVersion: u32,
    pub PerformanceInterruptGsi: u32,
    pub ParkedPhysicalAddress: u64,
    pub ControllerPhysicalAddress: u64,
    pub Gicv: u64,
    pub Gich: u64,
    pub VgicMaintenanceInterrupt: u32,
    pub GicrBaseAddress: u64,
    pub Mpidr: u64,
    pub ProcessorPowerEfficiencyClass: u8,
    pub Reserved77: u8,
    pub SpeInterruptGsi: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GIC_DISTRIBUTOR {
    pub Type: u8,
    pub Length: u8,
    pub Reserved1: u16,
    pub Identifier: u32,
    pub ControllerPhysicalAddress: u64,
    pub GsivBase: u32,
    pub Version: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GIC_MSIFRAME_ENTRY {
    pub Type: u8,
    pub Length: u8,
    pub Reserved1: u16,
    pub Identifier: u32,
    pub PhysicalAddress: u64,
    pub Flags: u32,
    pub SpiCount: u16,
    pub SpiBase: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GIC_REDISTRIBUTOR {
    pub Type: u8,
    pub Length: u8,
    pub Reserved: u16,
    pub DiscoveryRangeBaseAddress: u64,
    pub DiscoveryRangeLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GIC_ITS {
    pub Type: u8,
    pub Length: u8,
    pub Reserved1: u16,
    pub Identifier: u32,
    pub PhysicalAddress: u64,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMARTBATTTABLE {
    pub Header: u32,
    pub WarningEnergyLevel: u32,
    pub LowEnergyLevel: u32,
    pub CriticalEnergyLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSDT_32 {
    pub Header: u32,
    pub Tables: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSDT_64 {
    pub Header: u32,
    pub Reserved: u32,
    pub Tables: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSDT {
    pub Header: u32,
    pub Tables: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSDT {
    pub Header: u32,
    pub DiffDefBlock: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_PORT_TABLE {
    pub Header: u32,
    pub InterfaceType: u8,
    pub Reserved0: [u8; 3],
    pub BaseAddress: u32,
    pub OffsetDbgDevInfo: u32,
    pub NumberDbgDevInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_DEVICE_INFORMATION {
    pub Revision: u8,
    pub Length: u8,
    pub InterfaceType: u16,
    pub InterfaceSubType: u32,
    pub DeviceAddress: u32,
    pub AddressSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_DEVICE_INFORMATION_V2 {
    pub Revision: u8,
    pub Length: u16,
    pub BaseAddressRegisterCount: u8,
    pub NameSpaceStringLength: u16,
    pub NameSpaceStringOffset: u16,
    pub OemDataLength: u16,
    pub OemDataOffset: u16,
    pub PortType: u16,
    pub PortSubtype: u16,
    pub Reserved: u16,
    pub BaseAddressRegisterOffset: u16,
    pub AddressSizeOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_PORT_TABLE_V2 {
    pub Header: u32,
    pub OffsetDbgDevInfo: u32,
    pub NumberDbgDevInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_PORT_REDIRECTION_TABLE {
    pub Header: u32,
    pub InterfaceType: u8,
    pub Reserved0: [u8; 3],
    pub BaseAddress: u32,
    pub InterruptType: u8,
    pub Irq: u8,
    pub GlobalSystemInterruptVector: u32,
    pub BaudRate: u8,
    pub Parity: u8,
    pub StopBits: u8,
    pub FlowControl: u8,
    pub TerminalType: u8,
    pub Language: u8,
    pub PciDeviceId: u16,
    pub PciVendorId: u16,
    pub PciBusNumber: u8,
    pub PciSlotNumber: u8,
    pub PciFunctionNumber: u8,
    pub PciFlags: u32,
    pub PciSegment: u8,
    pub Reserved1: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WATCHDOG_TIMER_RESOURCE_TABLE {
    pub Header: u32,
    pub ControlRegisterAddress: u32,
    pub CountRegisterAddress: u32,
    pub PciDeviceId: u16,
    pub PciVendorId: u16,
    pub PciBusNumber: u8,
    pub PciSlotNumber: u8,
    pub PciFunctionNumber: u8,
    pub PciSegment: u8,
    pub MaxCount: u16,
    pub Units: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WATCHDOG_TIMER_INSTRUCTION_ENTRY {
    pub WatchdogAction: u8,
    pub InstructionFlags: u8,
    pub Reserved: [u8; 2],
    pub RegisterRegion: u32,
    pub Value: u32,
    pub Mask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WATCHDOG_TIMER_ACTION_TABLE {
    pub Header: u32,
    pub WatchdogHeaderLength: u32,
    pub PciSegment: u16,
    pub PciBusNumber: u8,
    pub PciDeviceNumber: u8,
    pub PciFunctionNumber: u8,
    pub MaximumRetries: u8,
    pub RetryInterval: u8,
    pub Reserved1: u8,
    pub TimerPeriod: u32,
    pub MaximumCount: u32,
    pub MinimumCount: u32,
    pub WatchdogFlags: u8,
    pub Reserved2: [u8; 3],
    pub InstructionCount: u32,
    pub InstructionEntry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BOOT_TABLE {
    pub Header: u32,
    pub CMOSIndex: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPPT_TABLE {
    pub Header: u32,
    pub Flags: u32,
    pub Reserved: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCFG_TABLE_ENTRY {
    pub BaseAddress: u64,
    pub SegmentNumber: u16,
    pub StartBusNumber: u8,
    pub EndBusNumber: u8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCFG_TABLE {
    pub Header: u32,
    pub Reserved: [u32; 2],
    pub TableEntry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HPET_DESCRIPTION_TABLE {
    pub Header: u32,
    pub EventTimerBlockId: u32,
    pub Address: u32,
    pub HpetNumber: u8,
    pub MinimumPeriodicTickCount: u16,
    pub PageProtection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_DESCRIPTION_TABLE_CLIENT {
    pub Header: u32,
    pub PlatformClass: u16,
    pub LAML: u32,
    pub LASA: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_DESCRIPTION_TABLE_SERVER {
    pub Header: u32,
    pub PlatformClass: u16,
    pub Reserved1: u16,
    pub LAML: u64,
    pub LASA: u64,
    pub SpecificationRevision: u16,
    pub DeviceFlags: u8,
    pub InterruptFlags: u8,
    pub GPE: u8,
    pub Reserved2: [u8; 3],
    pub GlobalSystemInterupt: u32,
    pub BaseAddress: u32,
    pub Reserved3: u32,
    pub ConfigurationAddress: u32,
    pub PCISegmentGroupNumber: u8,
    pub PCIBusNumber: u8,
    pub PCIDeviceNumber: u8,
    pub PCIFunctionNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_DESCRIPTION_TABLE {
    pub ClientTable: u32,
    pub ServerTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAE_DESCRIPTION_TABLE {
    pub Header: u32,
    pub EmulatedDeviceFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT {
    pub Header: u32,
    pub NodeCount: u32,
    pub NodeArrayOffset: u32,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_NODE_HEADER {
    pub Type: u8,
    pub Length: u16,
    pub Revision: u8,
    pub Identifier: u32,
    pub IdMappingCount: u32,
    pub IdMappingArrayOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_IDMAPPING {
    pub InputBase: u32,
    pub IdCount: u32,
    pub OutputBase: u32,
    pub OutputNodeOffset: u32,
    pub AsULONG: u32,
    pub SingleMapping: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_SMMUV2_NODE {
    pub Header: u32,
    pub BaseAddress: u64,
    pub Span: u64,
    pub Model: u32,
    pub AsULONG: u32,
    pub DvmSupported: u32,
    pub CoherentPageTableWalk: u32,
    pub Reserved: u32,
    pub GlobalInterruptArrayOffset: u32,
    pub ContextInterruptCount: u32,
    pub ContextInterruptArrayOffset: u32,
    pub PmuInterruptCount: u32,
    pub PmuInterruptArrayOffset: u32,
    pub NSgIrpt: u32,
    pub NSgCfgIrpt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_SMMUV3_NODE {
    pub Header: u32,
    pub BaseAddress: u64,
    pub AsULONG: u32,
    pub CohaccOverride: u32,
    pub HttuOverride: u32,
    pub ProximityDomainValid: u32,
    pub DeviceIDMappingIndexValid: u32,
    pub Reserved: u32,
    pub Reserved_1: [u8; 4],
    pub VatosAddress: u64,
    pub Model: u32,
    pub EventGsiv: u32,
    pub PriGsiv: u32,
    pub GerrGsiv: u32,
    pub SyncGsiv: u32,
    pub ProximityDomain: u32,
    pub DeviceIdMappingIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_ITS_GROUP_NODE {
    pub Header: u32,
    pub ItsCount: u32,
    pub ItsArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_NAMED_COMPONENT_NODE {
    pub Header: u32,
    pub Reserved: [u8; 4],
    pub MemoryProperties: u32,
    pub MemoryAccessWidth: u8,
    pub DeviceName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_ROOT_COMPLEX_NODE_V2 {
    pub Header: u32,
    pub MemoryProperties: u32,
    pub AtsAttribute: u32,
    pub PciSegmentNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_ROOT_COMPLEX_NODE_V4 {
    pub Header: u32,
    pub MemoryProperties: u32,
    pub AtsAttribute: u32,
    pub PciSegmentNumber: u32,
    pub MemoryAddressWidthLimit: u8,
    pub PasidCapabilities: u16,
    pub Reserved: u8,
    pub AsULONG: u32,
    pub PasidSupported: u32,
    pub Reserved_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_MEMORY_RANGE_DESCRIPTOR {
    pub PhysicalRangeOffset: u32,
    pub PhysicalRangeLength: u32,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IORT_RMR_NODE {
    pub Header: u32,
    pub AsULONG: u32,
    pub RemappingPermited: u32,
    pub AccessPrivileged: u32,
    pub AsUCHAR: u8,
    pub DevicenGnRnE: u8,
    pub DevicenGnRE: u8,
    pub DevicenGRE: u8,
    pub DeviceGRE: u8,
    pub InnerCacheOuterNoncache: u8,
    pub InnerWriteBackOuterWriteBack: u8,
    pub Reserved: u8,
    pub Reserved_1: u32,
    pub MemoryRangeDescriptorsCount: u32,
    pub MemoryRangeArrayOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICESCOPE {
    pub Type: u8,
    pub Length: u8,
    pub Reserved: [u8; 2],
    pub EnumerationId: u8,
    pub StartBusNumber: u8,
    pub Device: u8,
    pub Function: u8,
    pub PCIPath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRHD {
    pub Flags: u8,
    pub Reserved: u8,
    pub SegmentNumber: u16,
    pub BaseAddress: u64,
    pub DeviceScope: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RMRR {
    pub Reserved: [u8; 2],
    pub SegmentNumber: u16,
    pub RegionBaseAddress: u64,
    pub RegionLimitAddress: u64,
    pub DeviceScope: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATSR {
    pub Flags: u8,
    pub Reserved: u8,
    pub SegmentNumber: u16,
    pub DeviceScope: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RHSA {
    pub Type: u16,
    pub Length: u16,
    pub ReservedZ: u32,
    pub RegisterBaseAddress: u64,
    pub ProximityDomain: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SATC {
    pub Flags: u8,
    pub Reserved: u8,
    pub SegmentNumber: u16,
    pub DeviceScope: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMARTABLE {
    pub Type: u16,
    pub Length: u16,
    pub Drhd: u32,
    pub Rmrr: u32,
    pub Atsr: u32,
    pub Rhsa: u32,
    pub Satc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMAR {
    pub Header: u32,
    pub HostAddressWidth: u8,
    pub Flags: u8,
    pub Reserved: [u8; 10],
    pub DMARTables: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVRS {
    pub Header: u32,
    pub IVInfo: u32,
    pub Reserved: u64,
    pub DefinitionBlocks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVRS_BLOCK_HEADER {
    pub Type: u32,
    pub Flags: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVHD_BLOCK {
    pub Type: u32,
    pub AsUINT8: u32,
    pub HtTunEn: u32,
    pub PassPW: u32,
    pub ResPassPW: u32,
    pub Isoc: u32,
    pub IotlbSup: u32,
    pub ReservedZ0: u32,
    pub Length: u32,
    pub DeviceId: u32,
    pub CapabilityOffset: u32,
    pub IommuBaseAddress: u64,
    pub PciSegment: u32,
    pub AsUINT16: u32,
    pub MsiNum: u32,
    pub Reserved1: u32,
    pub UnitId: u32,
    pub Reserved2: u32,
    pub ReservedZ0_1: u32,
    pub DeviceEntries: [u32; 1],
    pub EfrRegisterImage: u64,
    pub ReservedZ1: u64,
    pub Type11DeviceEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVHD_DEVICE_ENTRY {
    pub Type: u32,
    pub DeviceId: u32,
    pub AsUINT8: u32,
    pub INITPass: u32,
    pub ExtIntPass: u32,
    pub NMIPass: u32,
    pub ReservedZ0: u32,
    pub SysMgt: u32,
    pub LINT0Pass: u32,
    pub LINT1Pass: u32,
    pub AsUINT32: u32,
    pub Handle: u32,
    pub AliasDeviceId: u32,
    pub Variety: u32,
    pub Reserved: u32,
    pub AtsDisabled: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVHD_ACPI_DEVICE_ENTRY {
    pub Type: u32,
    pub DeviceId: u32,
    pub AsUINT8: u32,
    pub INITPass: u32,
    pub ExtIntPass: u32,
    pub NMIPass: u32,
    pub ReservedZ0: u32,
    pub SysMgt: u32,
    pub LINT0Pass: u32,
    pub LINT1Pass: u32,
    pub HardwareId: u64,
    pub CompatibleId: u64,
    pub UniqueIdFormat: u32,
    pub UniqueIdLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IVMD_BLOCK {
    pub Type: u32,
    pub AsUINT8: u32,
    pub Unity: u32,
    pub IR: u32,
    pub IW: u32,
    pub ExclusionRange: u32,
    pub ReservedZ0: u32,
    pub Length: u32,
    pub DeviceId: u32,
    pub StartDeviceId: u32,
    pub AuxiliaryData: u32,
    pub EndDeviceId: u32,
    pub ReservedZ0_1: u64,
    pub StartAddress: u64,
    pub MemoryBlockLength: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_ENTRY_HEADER {
    pub Type: u8,
    pub Flags: u8,
    pub Length: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_SECURE_PCI_INFO_ENTRY {
    pub Header: u32,
    pub PciSegmentNumber: u16,
    pub StartBusNumber: u16,
    pub PciPathOffset: u16,
    pub PciPathLength: u16,
    pub VendorInfoOffset: u16,
    pub VendorInfoLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_SECURE_ACPI_INFO_ENTRY {
    pub Header: u32,
    pub IdentifierOffset: u16,
    pub IdentifierLength: u16,
    pub VendorInfoOffset: u16,
    pub VendorInfoLength: u16,
    pub SecureResourcesOffset: u16,
    pub SecureResourcesLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_SECURE_RESOURCE_ID_ENTRY {
    pub Header: u32,
    pub HardwareIdentifierOffset: u16,
    pub HardwareIdentifierLength: u16,
    pub SubsystemIdentifierOffset: u16,
    pub SubsystemIdentifierLength: u16,
    pub HardwareRevision: u16,
    pub HardwareRevisionPresent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_SECURE_RESOURCE_MEMORY_ENTRY {
    pub Header: u32,
    pub Reserved: u32,
    pub MemoryAddressBase: u64,
    pub MemoryAddressLength: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV {
    pub Header: u32,
    pub SDEVTables: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRM_HANDLER_INFORMATION {
    pub Revision: u16,
    pub Length: u16,
    pub Identifier: u32,
    pub PhysicalAddress: u32,
    pub StaticBufferPhysicalAddress: u32,
    pub AcpiParameterPhysicalAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRM_MODULE_INFORMATION {
    pub Revision: u16,
    pub Length: u16,
    pub Identifier: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub HandlerCount: u16,
    pub HandlerOffset: u32,
    pub MmioRangesPhysicalAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRMT_TABLE {
    pub Header: u32,
    pub PlatformGuid: u32,
    pub ModuleInfoOffset: u32,
    pub ModuleInfoCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSRT_TABLE_HEADER {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSRT_RESOURCE_GROUP_HEADER {
    pub Length: u32,
    pub VendorId: u32,
    pub SubvendorId: u32,
    pub DeviceId: u32,
    pub SubdeviceId: u32,
    pub Revision: u32,
    pub Instance: u32,
    pub SharedInfoLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSRT_RESOURCE_DESCRIPTOR_HEADER {
    pub Length: u32,
    pub Type: u32,
    pub Subtype: u32,
    pub Uid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_SUBSPACE_HEADER {
    pub Type: u8,
    pub Length: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_GENERIC_SUBSPACE {
    pub Header: usize,
    pub Reserved1: u16,
    pub Reserved2: u32,
    pub BaseAddress: u64,
    pub Length: u64,
    pub DoorbellRegister: u32,
    pub DoorbellPreserve: u64,
    pub DoorbellWrite: u64,
    pub NominalLatency: u32,
    pub MaximumPeriodicAccessRate: u32,
    pub MinimumRequestTurnaroundTime: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_REDUCED_1_SUBSPACE {
    pub Header: usize,
    pub PlatformInterruptGsiv: u32,
    pub PlatformInterruptPolarity: u8,
    pub PlatformInterruptMode: u8,
    pub Reserved1: u8,
    pub PlatformInterruptFlags: u8,
    pub Reserved2: u8,
    pub BaseAddress: u64,
    pub Length: u64,
    pub DoorbellRegister: u32,
    pub DoorbellPreserve: u64,
    pub DoorbellWrite: u64,
    pub NominalLatency: u32,
    pub MaximumPeriodicAccessRate: u32,
    pub MinimumRequestTurnaroundTime: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_REDUCED_2_SUBSPACE {
    pub Header: usize,
    pub PlatformInterruptGsiv: u32,
    pub PlatformInterruptPolarity: u8,
    pub PlatformInterruptMode: u8,
    pub Reserved1: u8,
    pub PlatformInterruptFlags: u8,
    pub Reserved2: u8,
    pub BaseAddress: u64,
    pub Length: u64,
    pub DoorbellRegister: u32,
    pub DoorbellPreserve: u64,
    pub DoorbellWrite: u64,
    pub NominalLatency: u32,
    pub MaximumPeriodicAccessRate: u32,
    pub MinimumRequestTurnaroundTime: u16,
    pub PlatformInterruptAckRegister: u32,
    pub PlatformInterruptAckPreserve: u64,
    pub PlatformInterruptAckWrite: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_GENERIC_SHARED_REGION {
    pub Signature: u32,
    pub CommandCode: u16,
    pub ReservedZ: u16,
    pub SciDoorbell: u16,
    pub AsUShort: u16,
    pub CommandComplete: u16,
    pub SciReceived: u16,
    pub Error: u16,
    pub PlatformNotification: u16,
    pub Reserved: u16,
    pub AsUShort_1: u16,
    pub CommunicationSpace: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_EXTENDED_3_4_SUBSPACE {
    pub Header: usize,
    pub PlatformInterruptGsiv: u32,
    pub PlatformInterruptPolarity: u8,
    pub PlatformInterruptMode: u8,
    pub Reserved1: u8,
    pub PlatformInterruptFlags: u8,
    pub Reserved2: u8,
    pub BaseAddress: u64,
    pub Length: u32,
    pub DoorbellRegister: u32,
    pub DoorbellPreserve: u32,
    pub DoorbellWrite: u32,
    pub NominalLatency: u32,
    pub MaximumPeriodicAccessRate: u32,
    pub MinimumRequestTurnaroundTime: u32,
    pub PlatformInterruptAckRegister: u32,
    pub PlatformInterruptAckPreserve: u32,
    pub PlatformInterruptAckWrite: u32,
    pub Reserved3: u32,
    pub CommandCompleteCheckRegister: u32,
    pub CommandCompleteCheckMask: u32,
    pub CommandCompleteUpdateRegister: u32,
    pub CommandCompleteUpdatePreserve: u32,
    pub CommandCompleteUpdateWrite: u32,
    pub ErrorStatusRegister: u32,
    pub ErrorStatusMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_EXTENDED_SHARED_REGION {
    pub Signature: u32,
    pub NotifyOnComplete: u32,
    pub Reserved: u32,
    pub AsUlong: u32,
    pub Length: u32,
    pub CommandCode: u32,
    pub CommunicationSpace: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCC_TABLE {
    pub Header: u32,
    pub AsULong: u32,
    pub SciSupported: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Subspaces: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BGRT_TABLE {
    pub Header: u32,
    pub Version: u16,
    pub Status: u8,
    pub ImageType: u8,
    pub LogoAddress: u64,
    pub OffsetX: u32,
    pub OffsetY: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BBRT_TABLE {
    pub Header: u32,
    pub Background: u32,
    pub Foreground: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GTDT_TABLE {
    pub Header: u32,
    pub CntControlBasePhysicalAddress: u64,
    pub Reserved: u32,
    pub SecurePhysicalTimerGsiv: u32,
    pub SecurePhysicalTimerFlags: u32,
    pub NonSecurePhysicalTimerGsiv: u32,
    pub NonSecurePhysicalTimerFlags: u32,
    pub VirtualTimerEventGsiv: u32,
    pub VirtualTimerEventFlags: u32,
    pub NonSecurePhysicalTimer2Gsiv: u32,
    pub NonSecurePhysicalTimer2Flags: u32,
    pub CntReadBasePhysicalAddress: u64,
    pub TimerBlockCount: u32,
    pub TimerBlockOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GTDT_PLATFORM_TIMER_HEADER {
    pub Type: u8,
    pub Length: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GTDT_PLATFORM_GT_BLOCK {
    pub Header: u32,
    pub Reserved: u8,
    pub CntCtlBasePhysicalAddress: u64,
    pub BlockTimerCount: u32,
    pub BlockTimerOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GTDT_PLATFORM_GT_INSTANCE {
    pub FrameNumber: u8,
    pub Reserved: [u8; 3],
    pub CntBasePhysicalAddress: u64,
    pub CntEL0BasePhysicalAddress: u64,
    pub PhysicalTimerGsiv: u32,
    pub PhysicalTimerFlags: u32,
    pub VirtualTimerGsiv: u32,
    pub VirtualTimerFlags: u32,
    pub CommonFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GTDT_PLATFORM_WATCHDOG_BLOCK {
    pub Header: u32,
    pub Reserved: u8,
    pub RefreshFramePhysicalAddress: u64,
    pub ControlFramePhysicalAddress: u64,
    pub WatchdogTimerGsiv: u32,
    pub WatchdogTimerFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OEM0_TABLE {
    pub Header: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FIRMWARE_S3_PERFORMANCE_TABLE {
    pub Signature: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct S3PT_RECORD_HEADER {
    pub RecordSubtype: u16,
    pub RecordLength: i8,
    pub Revision: i8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct S3PT_PERFORMANCE_RECORD {
    pub RecordHeader: u32,
    pub ResumeCount: u32,
    pub FullResume: u64,
    pub AverageResume: u64,
    pub SuspendStart: u64,
    pub SuspendEnd: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FIRMWARE_BOOT_PERFORMANCE_TABLE {
    pub Signature: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FBPT_RECORD_HEADER {
    pub RecordSubtype: u16,
    pub RecordLength: i8,
    pub Revision: i8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FBPT_RECORD {
    pub RecordHeader: u32,
    pub Reserved: u32,
    pub ResetEnd: u64,
    pub LoadImageStart: u64,
    pub StartImageStart: u64,
    pub ExitBootServicesEntry: u64,
    pub ExitBootServicesExit: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FPDT_TABLE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FPDT_RECORD_HEADER {
    pub RecordType: u16,
    pub RecordLength: i8,
    pub Revision: i8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FPDT_RECORD {
    pub RecordHeader: u32,
    pub PhysicalAddress: u64,
    pub PhysicalAddress_1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_WPBT_TABLE {
    pub Header: u32,
    pub HandoffMemorySize: u32,
    pub HandoffMemoryLocation: u32,
    pub ContentLayout: u8,
    pub ContentType: u8,
    pub CommandLineArgumentsLength: u16,
    pub CommandLineArguments: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPM20_TABLE {
    pub Header: u32,
    pub UseMemoryDescriptors: u32,
    pub CmdListCapable: u32,
    pub NoDeviceIO: u32,
    pub DeviceMemory: u32,
    pub DevMemOnly: u32,
    pub Reserved: u32,
    pub FlagBits: u32,
    pub ControlAreaPA: u64,
    pub StartMethod: u32,
    pub PlatformParameters: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MHSP_CHANNEL {
    pub ChannelBaseAddress: u64,
    pub RequestDoorbellAddress: u64,
    pub ReplyDoorbellAddress: u64,
    pub ChannelSize: u32,
    pub IrqResource: u32,
    pub ChannelParameters: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MHSP_TABLE {
    pub Header: u32,
    pub ProtocolId: u32,
    pub Channels: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_BUFFER {
    pub Revision: u32,
    pub IgnoreColor: u32,
    pub Color: u32,
    pub Width: u32,
    pub Height: u32,
    pub UserVisible: u32,
    pub Dock: u32,
    pub Lid: u32,
    pub Panel: u32,
    pub VerticalPosition: u32,
    pub HorizontalPosition: u32,
    pub Shape: u32,
    pub GroupOrientation: u32,
    pub GroupToken: u32,
    pub GroupPosition: u32,
    pub Bay: u32,
    pub Ejectable: u32,
    pub EjectionRequired: u32,
    pub CabinetNumber: u32,
    pub CardCageNumber: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_V2_BUFFER {
    pub Revision: u32,
    pub IgnoreColor: u32,
    pub Color: u32,
    pub Width: u32,
    pub Height: u32,
    pub UserVisible: u32,
    pub Dock: u32,
    pub Lid: u32,
    pub Panel: u32,
    pub VerticalPosition: u32,
    pub HorizontalPosition: u32,
    pub Shape: u32,
    pub GroupOrientation: u32,
    pub GroupToken: u32,
    pub GroupPosition: u32,
    pub Bay: u32,
    pub Ejectable: u32,
    pub EjectionRequired: u32,
    pub CabinetNumber: u32,
    pub CardCageNumber: u32,
    pub Reference: u32,
    pub Rotation: u32,
    pub Order: u32,
    pub Reserved: u32,
    pub VerticalOffset: u32,
    pub HorizontalOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_JOINT_BUFFER {
    pub Revision: u32,
    pub JointType: u32,
    pub SourceEdge: u32,
    pub TargetCabinetNumber: u32,
    pub TargetPanel: u32,
    pub TargetEdge: u32,
    pub Reserved1: u32,
    pub MovementOrientation: u32,
    pub ForwardMovementRange: u32,
    pub BackwardMovementRange: u32,
    pub HorizontalOffset: u32,
    pub VerticalOffset: u32,
    pub GapOffset: u32,
    pub Rotation: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_SPATIAL_BUFFER {
    pub Revision: u32,
    pub RollRotation: u32,
    pub PitchRotation: u32,
    pub YawRotation: u32,
    pub Width: u32,
    pub Height: u32,
    pub Length: u32,
    pub HorizontalOffset: u32,
    pub VerticalOffset: u32,
    pub DepthOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_INTERFACE_CLASS_BUFFER {
    pub ClassGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_INTERFACE_INSTANCE_ANSI_BUFFER {
    pub ReferenceString: [i8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_INTERFACE_INSTANCE_UNICODE_BUFFER {
    pub ReferenceString: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_INTERFACE_INSTANCE_GUID_BUFFER {
    pub ReferenceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PLD_CONTAINER_BUFFER {
    pub ContainerId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_TABLE {
    pub Header: u32,
    pub Reserved: u32,
    pub Structures: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_STRUCT_HEADER {
    pub Type: u16,
    pub Length: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_SYSTEM_PHYSICAL_ADDRESS_RANGE {
    pub Header: u32,
    pub SPARangeIndex: u16,
    pub Flags: u16,
    pub Reserved: [u8; 4],
    pub ProximityDomain: u32,
    pub AddressRangeType: u32,
    pub SPARangeBase: u32,
    pub SPARangeLength: u32,
    pub AddressRangeMappingAttribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_DEVICE_HANDLE {
    pub DIMMNumber: u32,
    pub MemoryChannelNumber: u32,
    pub MemoryControllerId: u32,
    pub SocketId: u32,
    pub NodeControllerId: u32,
    pub Reserved: u32,
    pub AsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_NVDIMM_REGION_MAPPING {
    pub Header: u32,
    pub NfitHandle: u32,
    pub NVDIMMPhysicalId: u16,
    pub NVDIMMRegionId: u16,
    pub SpaRangeIndex: u16,
    pub NVDIMMControlRegionIndex: u16,
    pub NVDIMMRegionSize: u32,
    pub RegionOffset: u32,
    pub NVDIMMPhysicalAddrRegionBase: u32,
    pub InterleaveIndex: u16,
    pub InterleaveWays: u16,
    pub NVDIMMStateFlags: u16,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_INTERLEAVE {
    pub Header: u32,
    pub InterleaveIndex: u16,
    pub Reserved: [u8; 2],
    pub LinesDescribed: u32,
    pub LineSize: u32,
    pub LineOffsets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_SMBIOS_MANAGEMENT_INFO {
    pub Header: u32,
    pub Reserved: [u8; 4],
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_NVDIMM_CONTROL_REGION {
    pub Header: u32,
    pub NvdimmControlRegionIndex: u16,
    pub VendorId: u16,
    pub DeviceId: u16,
    pub RevisionId: u16,
    pub SubsystemVendorId: u16,
    pub SubsystemDeviceId: u16,
    pub SubsystemRevisionId: u16,
    pub AsUchar: u8,
    pub ManufacturingFields: u8,
    pub Reserved: u8,
    pub ManufacturingLocation: u8,
    pub ManufacturingYear: u8,
    pub ManufacturingWeek: u8,
    pub Reserved_1: [u8; 2],
    pub SerialNumber: u32,
    pub RegionFormatInterfaceCode: u16,
    pub BCWCount: u16,
    pub BCWSize: u32,
    pub BCWCommandRegisterOffset: u32,
    pub BCWCommandRegisterSize: u32,
    pub BCWStatusRegisterOffset: u32,
    pub BCWStatusRegisterSize: u32,
    pub NvdimmControlRegionFlag: u16,
    pub Reserved1: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_BLOCK_DATA_WINDOW_REGION {
    pub Header: u32,
    pub NvdimmControlRegionIndex: u16,
    pub BlockDataWindowCount: u16,
    pub BlockDataWindowOffset: u32,
    pub BlockDataWindowSize: u32,
    pub BlockAccessibleCapacity: u32,
    pub FirstAccessibleBlockAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_FLUSH_HINT_ADDRESS {
    pub Header: u32,
    pub NfitHandle: u32,
    pub FlushHintAddressCount: u16,
    pub Reserved: [u8; 6],
    pub FlushHintAddress: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFIT_PLATFORM_CAPABILITIES {
    pub Header: u32,
    pub HighestValidCapability: u8,
    pub Reserved: [u8; 3],
    pub AsUlong: u32,
    pub CpuCacheFlushOnPowerLoss: u32,
    pub MemCtrlrFlushOnPowerLoss: u32,
    pub PmemHardwareMirroring: u32,
    pub Reserved1: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSMT {
    pub Header: u32,
    pub ProtectionFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LPI_STATE_DESCRIPTOR {
    pub Type: u32,
    pub Length: u32,
    pub UniqueId: u16,
    pub Reserved: [u8; 2],
    pub Flags: usize,
    pub EntryTrigger: u32,
    pub Residency: u32,
    pub Latency: u32,
    pub ResidencyCounter: u32,
    pub ResidencyCounterFrequency: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LPIT {
    pub Header: u32,
    pub LpiStates: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROC_TOPOLOGY_NODE {
    pub Type: u8,
    pub Length: u8,
    pub Reserved: [u8; 2],
    pub Flags: usize,
    pub Parent: u32,
    pub ACPIProcessorId: u32,
    pub NumberPrivateResources: u32,
    pub PrivateResources: [u32; 1],
    pub Flags_1: usize,
    pub NextLevelCacheOffset: u32,
    pub Size: u32,
    pub Sets: u32,
    pub Associativity: u8,
    pub Attributes: usize,
    pub LineSize: u16,
    pub CacheId: u32,
    pub Vendor: u32,
    pub Level1: u32,
    pub Level2: u32,
    pub Major: u16,
    pub Minor: u16,
    pub Spin: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PPTT {
    pub Header: u32,
    pub HierarchyNodes: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PDTT_PCC_SUBCHANNEL_INDENTIFIER {
    pub SubChannelId: u8,
    pub Runtime: u8,
    pub WaitCompletion: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_PDTT {
    pub Header: u32,
    pub TriggerCount: u8,
    pub Reserved: [u8; 3],
    pub TriggerOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HMAT_ENTRY {
    pub Type: u16,
    pub Reserved: u16,
    pub Length: u32,
    pub ProcessorProximityDomainValid: u16,
    pub Reserved0: u16,
    pub Reserved1: u16,
    pub Reserved_1: u16,
    pub AsUSHort: u16,
    pub Reserved1_1: u16,
    pub ProcessorProximityDomain: u32,
    pub MemoryProximityDomain: u32,
    pub Reserved2: u32,
    pub Reserved3: u64,
    pub Reserved4: u64,
    pub MemoryHierarchy: u8,
    pub MinTransferSizeToAchieveValues: u8,
    pub NonSequentialTransfers: u8,
    pub Reserved_2: u8,
    pub AsUChar: u8,
    pub DataType: u8,
    pub MinTransferSize: u8,
    pub Reserved1_2: u8,
    pub NumberOfInitiatorProximityDomains: u32,
    pub NumberOfTargetProximityDomains: u32,
    pub Reserved2_1: u32,
    pub EntryBaseUnit: u64,
    pub MemoryProximityDomain_1: u32,
    pub Reserved1_3: u32,
    pub MemorySideCacheSize: u64,
    pub TotalCacheLevels: u32,
    pub CacheLevel: u32,
    pub CacheAssociativity: u32,
    pub WritePolicy: u32,
    pub CacheLineSize: u32,
    pub AsULong: u32,
    pub Reserved2_2: u16,
    pub NumberOfSmBiosHandles: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HMAT_TABLE {
    pub Header: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPMI_DESCRIPTION_TABLE {
    pub Header: u32,
    pub InterfaceType: u32,
    pub Reserved1: u32,
    pub SpecificationRevision: u32,
    pub GpeSupported: u32,
    pub ApicInterrupt: u32,
    pub Reserved: u32,
    pub Gpe: u32,
    pub Reserved2: u32,
    pub PciDeviceFlag: u32,
    pub GlobalSystemInterrupt: u32,
    pub BaseAddress: u32,
    pub PciSegmentGroupNumber: u32,
    pub PciBusNumber: u32,
    pub PciDeviceNumber: u32,
    pub PciFunctionNumber: u32,
    pub Reserved3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_HEADER {
    pub Type: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_ASP_GLOBAL_REGISTERS_V1 {
    pub Header: u32,
    pub Reserved: u32,
    pub FeatureRegisterAddress: u64,
    pub InterruptEnableRegisterAddress: u64,
    pub InterruptStatusRegisterAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_SEV_MAILBOX_REGISTERS_V1 {
    pub Header: u32,
    pub MailboxInterruptId: u32,
    pub Reserved: [u32; 3],
    pub CmdRespRegisterAddress: u64,
    pub CmdBufAddrLoRegisterAddress: u64,
    pub CmdBufAddrHiRegisterAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_ACPI_MAILBOX_REGISTERS_V1 {
    pub Header: u32,
    pub Reserved1: u32,
    pub CmdRespRegisterAddress: u64,
    pub Reserved2: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_ASP_GLOBAL_REGISTERS_V2 {
    pub Header: u32,
    pub Reserved: u32,
    pub FeatureRegisterOffset: u32,
    pub InterruptEnableRegisterOffset: u32,
    pub InterruptStatusRegisterOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_SEV_MAILBOX_REGISTERS_V2 {
    pub Header: u32,
    pub MailboxInterruptId: u32,
    pub Reserved: [u32; 3],
    pub CmdRespRegisterOffset: u32,
    pub CmdBufAddrLoRegisterOffset: u32,
    pub CmdBufAddrHiRegisterOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_ENTRY_ACPI_MAILBOX_REGISTERS_V2 {
    pub Header: u32,
    pub Reserved1: u32,
    pub CmdRespRegisterOffset: u32,
    pub Reserved2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_TABLE_V1 {
    pub Header: u32,
    pub NumberOfAsptEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASPT_TABLE_V2 {
    pub Header: u32,
    pub AspRegisterBaseAddress: u64,
    pub AspRegisterSpacePages: u32,
    pub NumberOfAsptEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_FUNCTIONAL_DEPENDENCY_DESCRIPTOR {
    pub Producer: u32,
    pub Reserved0: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_RESOURCE_LOCATOR {
    pub Type: u32,
    pub CacheReference: u64,
    pub Reserved0: u32,
    pub ProximityDomain: u64,
    pub Reserved0_1: u32,
    pub SmmuInterface: u64,
    pub Reserved0_2: u32,
    pub Reserved0_3: u64,
    pub Reference: u32,
    pub AcpiHardwareId: u64,
    pub AcpiUniqueId: u32,
    pub DescriptorTableOffset: u64,
    pub Locator: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_RESOURCE_NODE {
    pub Identifier: u32,
    pub RisIndex: u32,
    pub Reserved0: [u32; 2],
    pub Locator: u32,
    pub FunctionalDependencyCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_INTERCONNECT_DESCRIPTOR {
    pub SourceId: u32,
    pub DestinationId: u32,
    pub LinkType: u32,
    pub Reserved: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_INTERCONNECT_DESCRIPTOR_TABLE {
    pub Signature: [u32; 16],
    pub DescriptorCount: u32,
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_MSC_NODE {
    pub Length: u32,
    pub Reserved0: u32,
    pub Identifier: u32,
    pub BaseAddress: u64,
    pub MmioSize: u32,
    pub OverflowInterrupt: u32,
    pub OverflowInterruptFlags: u32,
    pub Reserved1: u32,
    pub OverflowInterruptAffinity: u32,
    pub ErrorInterrupt: u32,
    pub ErrorInterruptFlags: u32,
    pub Reserved2: u32,
    pub ErrorInterruptAffinity: u32,
    pub MaxNrdyUsec: u32,
    pub LinkedHardwareId: u64,
    pub LinkedInstanceId: u32,
    pub ResourceNodeCount: u32,
    pub ResourceNodes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPAM_TABLE {
    pub Header: u32,
    pub Nodes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPR_REGISTERS {
    pub BaseAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPR_INSTANCE {
    pub Flags: u32,
    pub TprCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DTPR {
    pub Signature: u32,
    pub Length: u32,
    pub Revision: u32,
    pub Checksum: u32,
    pub OEMID: [u32; 6],
    pub OEMTableID: [u32; 8],
    pub OEMRevision: u32,
    pub CreatorID: u32,
    pub CreatorRevision: u32,
    pub Flags: u32,
    pub TprInstanceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFY_DEVICE_DATA {
    pub Reserved1: u16,
    pub Retired3: u16,
    pub ResponseIncomplete: u16,
    pub Retired2: u16,
    pub FixedDevice: u16,
    pub RemovableMedia: u16,
    pub Retired1: u16,
    pub DeviceType: u16,
    pub NumCylinders: u16,
    pub SpecificConfiguration: u16,
    pub NumHeads: u16,
    pub Retired1_1: [u16; 2],
    pub NumSectorsPerTrack: u16,
    pub VendorUnique1: [u16; 3],
    pub SerialNumber: [u8; 20],
    pub Retired2_1: [u16; 2],
    pub Obsolete1: u16,
    pub FirmwareRevision: [u8; 8],
    pub ModelNumber: [u8; 40],
    pub MaximumBlockTransfer: u8,
    pub VendorUnique2: u8,
    pub FeatureSupported: u16,
    pub Reserved: u16,
    pub CurrentLongPhysicalSectorAlignment: u8,
    pub ReservedByte49: u8,
    pub DmaSupported: u8,
    pub LbaSupported: u8,
    pub IordyDisable: u8,
    pub IordySupported: u8,
    pub Reserved1_1: u8,
    pub StandybyTimerSupport: u8,
    pub Reserved2: u8,
    pub ReservedWord50: u16,
    pub ObsoleteWords51: [u16; 2],
    pub TranslationFieldsValid: u16,
    pub Reserved3: u16,
    pub FreeFallControlSensitivity: u16,
    pub NumberOfCurrentCylinders: u16,
    pub NumberOfCurrentHeads: u16,
    pub CurrentSectorsPerTrack: u16,
    pub CurrentSectorCapacity: u32,
    pub CurrentMultiSectorSetting: u8,
    pub MultiSectorSettingValid: u8,
    pub ReservedByte59: u8,
    pub SanitizeFeatureSupported: u8,
    pub CryptoScrambleExtCommandSupported: u8,
    pub OverwriteExtCommandSupported: u8,
    pub BlockEraseExtCommandSupported: u8,
    pub UserAddressableSectors: u32,
    pub ObsoleteWord62: u16,
    pub MultiWordDMASupport: u16,
    pub MultiWordDMAActive: u16,
    pub AdvancedPIOModes: u16,
    pub ReservedByte64: u16,
    pub MinimumMWXferCycleTime: u16,
    pub RecommendedMWXferCycleTime: u16,
    pub MinimumPIOCycleTime: u16,
    pub MinimumPIOCycleTimeIORDY: u16,
    pub ZonedCapabilities: u16,
    pub NonVolatileWriteCache: u16,
    pub ExtendedUserAddressableSectorsSupported: u16,
    pub DeviceEncryptsAllUserData: u16,
    pub ReadZeroAfterTrimSupported: u16,
    pub Optional28BitCommandsSupported: u16,
    pub IEEE1667: u16,
    pub DownloadMicrocodeDmaSupported: u16,
    pub SetMaxSetPasswordUnlockDmaSupported: u16,
    pub WriteBufferDmaSupported: u16,
    pub ReadBufferDmaSupported: u16,
    pub DeviceConfigIdentifySetDmaSupported: u16,
    pub LPSAERCSupported: u16,
    pub DeterministicReadAfterTrimSupported: u16,
    pub CFastSpecSupported: u16,
    pub ReservedWords70: [u16; 5],
    pub QueueDepth: u16,
    pub ReservedWord75: u16,
    pub Reserved0: u16,
    pub SataGen1: u16,
    pub SataGen2: u16,
    pub SataGen3: u16,
    pub Reserved1_2: u16,
    pub NCQ: u16,
    pub HIPM: u16,
    pub PhyEvents: u16,
    pub NcqUnload: u16,
    pub NcqPriority: u16,
    pub HostAutoPS: u16,
    pub DeviceAutoPS: u16,
    pub ReadLogDMA: u16,
    pub Reserved2_1: u16,
    pub CurrentSpeed: u16,
    pub NcqStreaming: u16,
    pub NcqQueueMgmt: u16,
    pub NcqReceiveSend: u16,
    pub DEVSLPtoReducedPwrState: u16,
    pub Reserved3_1: u16,
    pub Reserved0_1: u16,
    pub NonZeroOffsets: u16,
    pub DmaSetupAutoActivate: u16,
    pub DIPM: u16,
    pub InOrderData: u16,
    pub HardwareFeatureControl: u16,
    pub SoftwareSettingsPreservation: u16,
    pub NCQAutosense: u16,
    pub DEVSLP: u16,
    pub HybridInformation: u16,
    pub Reserved1_3: u16,
    pub Reserved0_2: u16,
    pub NonZeroOffsets_1: u16,
    pub DmaSetupAutoActivate_1: u16,
    pub DIPM_1: u16,
    pub InOrderData_1: u16,
    pub HardwareFeatureControl_1: u16,
    pub SoftwareSettingsPreservation_1: u16,
    pub DeviceAutoPS_1: u16,
    pub DEVSLP_1: u16,
    pub HybridInformation_1: u16,
    pub Reserved1_4: u16,
    pub MajorRevision: u16,
    pub MinorRevision: u16,
    pub SmartCommands: u16,
    pub SecurityMode: u16,
    pub RemovableMediaFeature: u16,
    pub PowerManagement: u16,
    pub Reserved1_5: u16,
    pub WriteCache: u16,
    pub LookAhead: u16,
    pub ReleaseInterrupt: u16,
    pub ServiceInterrupt: u16,
    pub DeviceReset: u16,
    pub HostProtectedArea: u16,
    pub Obsolete1_1: u16,
    pub WriteBuffer: u16,
    pub ReadBuffer: u16,
    pub Nop: u16,
    pub Obsolete2: u16,
    pub DownloadMicrocode: u16,
    pub DmaQueued: u16,
    pub Cfa: u16,
    pub AdvancedPm: u16,
    pub Msn: u16,
    pub PowerUpInStandby: u16,
    pub ManualPowerUp: u16,
    pub Reserved2_2: u16,
    pub SetMax: u16,
    pub Acoustics: u16,
    pub BigLba: u16,
    pub DeviceConfigOverlay: u16,
    pub FlushCache: u16,
    pub FlushCacheExt: u16,
    pub WordValid83: u16,
    pub SmartErrorLog: u16,
    pub SmartSelfTest: u16,
    pub MediaSerialNumber: u16,
    pub MediaCardPassThrough: u16,
    pub StreamingFeature: u16,
    pub GpLogging: u16,
    pub WriteFua: u16,
    pub WriteQueuedFua: u16,
    pub WWN64Bit: u16,
    pub URGReadStream: u16,
    pub URGWriteStream: u16,
    pub ReservedForTechReport: u16,
    pub IdleWithUnloadFeature: u16,
    pub WordValid: u16,
    pub SmartCommands_1: u16,
    pub SecurityMode_1: u16,
    pub RemovableMediaFeature_1: u16,
    pub PowerManagement_1: u16,
    pub Reserved1_6: u16,
    pub WriteCache_1: u16,
    pub LookAhead_1: u16,
    pub ReleaseInterrupt_1: u16,
    pub ServiceInterrupt_1: u16,
    pub DeviceReset_1: u16,
    pub HostProtectedArea_1: u16,
    pub Obsolete1_2: u16,
    pub WriteBuffer_1: u16,
    pub ReadBuffer_1: u16,
    pub Nop_1: u16,
    pub Obsolete2_1: u16,
    pub DownloadMicrocode_1: u16,
    pub DmaQueued_1: u16,
    pub Cfa_1: u16,
    pub AdvancedPm_1: u16,
    pub Msn_1: u16,
    pub PowerUpInStandby_1: u16,
    pub ManualPowerUp_1: u16,
    pub Reserved2_3: u16,
    pub SetMax_1: u16,
    pub Acoustics_1: u16,
    pub BigLba_1: u16,
    pub DeviceConfigOverlay_1: u16,
    pub FlushCache_1: u16,
    pub FlushCacheExt_1: u16,
    pub Resrved3: u16,
    pub Words119_120Valid: u16,
    pub SmartErrorLog_1: u16,
    pub SmartSelfTest_1: u16,
    pub MediaSerialNumber_1: u16,
    pub MediaCardPassThrough_1: u16,
    pub StreamingFeature_1: u16,
    pub GpLogging_1: u16,
    pub WriteFua_1: u16,
    pub WriteQueuedFua_1: u16,
    pub WWN64Bit_1: u16,
    pub URGReadStream_1: u16,
    pub URGWriteStream_1: u16,
    pub ReservedForTechReport_1: u16,
    pub IdleWithUnloadFeature_1: u16,
    pub Reserved4: u16,
    pub UltraDMASupport: u16,
    pub UltraDMAActive: u16,
    pub TimeRequired: u16,
    pub ExtendedTimeReported: u16,
    pub TimeRequired_1: u16,
    pub ExtendedTimeReported_1: u16,
    pub CurrentAPMLevel: u16,
    pub ReservedWord91: u16,
    pub MasterPasswordID: u16,
    pub HardwareResetResult: u16,
    pub CurrentAcousticValue: u16,
    pub RecommendedAcousticValue: u16,
    pub StreamMinRequestSize: u16,
    pub StreamingTransferTimeDMA: u16,
    pub StreamingAccessLatencyDMAPIO: u16,
    pub StreamingPerfGranularity: u32,
    pub Max48BitLBA: [u32; 2],
    pub StreamingTransferTime: u16,
    pub DsmCap: u16,
    pub LogicalSectorsPerPhysicalSector: u16,
    pub Reserved0_3: u16,
    pub LogicalSectorLongerThan256Words: u16,
    pub MultipleLogicalSectorsPerPhysicalSector: u16,
    pub Reserved1_7: u16,
    pub InterSeekDelay: u16,
    pub WorldWideName: [u16; 4],
    pub ReservedForWorldWideName128: [u16; 4],
    pub ReservedForTlcTechnicalReport: u16,
    pub WordsPerLogicalSector: [u16; 2],
    pub ReservedForDrqTechnicalReport: u16,
    pub WriteReadVerify: u16,
    pub WriteUncorrectableExt: u16,
    pub ReadWriteLogDmaExt: u16,
    pub DownloadMicrocodeMode3: u16,
    pub FreefallControl: u16,
    pub SenseDataReporting: u16,
    pub ExtendedPowerConditions: u16,
    pub Reserved0_4: u16,
    pub WordValid_1: u16,
    pub ReservedForDrqTechnicalReport_1: u16,
    pub WriteReadVerify_1: u16,
    pub WriteUncorrectableExt_1: u16,
    pub ReadWriteLogDmaExt_1: u16,
    pub DownloadMicrocodeMode3_1: u16,
    pub FreefallControl_1: u16,
    pub SenseDataReporting_1: u16,
    pub ExtendedPowerConditions_1: u16,
    pub Reserved0_5: u16,
    pub Reserved1_8: u16,
    pub ReservedForExpandedSupportandActive: [u16; 6],
    pub MsnSupport: u16,
    pub ReservedWord127: u16,
    pub SecuritySupported: u16,
    pub SecurityEnabled: u16,
    pub SecurityLocked: u16,
    pub SecurityFrozen: u16,
    pub SecurityCountExpired: u16,
    pub EnhancedSecurityEraseSupported: u16,
    pub Reserved0_6: u16,
    pub SecurityLevel: u16,
    pub Reserved1_9: u16,
    pub ReservedWord129: [u16; 31],
    pub MaximumCurrentInMA: u16,
    pub CfaPowerMode1Disabled: u16,
    pub CfaPowerMode1Required: u16,
    pub Reserved0_7: u16,
    pub Word160Supported: u16,
    pub ReservedForCfaWord161: [u16; 7],
    pub NominalFormFactor: u16,
    pub ReservedWord168: u16,
    pub SupportsTrim: u16,
    pub Reserved0_8: u16,
    pub AdditionalProductID: [u16; 4],
    pub ReservedForCfaWord174: [u16; 2],
    pub CurrentMediaSerialNumber: [u16; 30],
    pub Supported: u16,
    pub Reserved0_9: u16,
    pub WriteSameSuported: u16,
    pub ErrorRecoveryControlSupported: u16,
    pub FeatureControlSuported: u16,
    pub DataTablesSuported: u16,
    pub Reserved1_10: u16,
    pub VendorSpecific: u16,
    pub ReservedWord207: [u16; 2],
    pub AlignmentOfLogicalWithinPhysical: u16,
    pub Word209Supported: u16,
    pub Reserved0_10: u16,
    pub WriteReadVerifySectorCountMode3Only: [u16; 2],
    pub WriteReadVerifySectorCountMode2Only: [u16; 2],
    pub NVCachePowerModeEnabled: u16,
    pub Reserved0_11: u16,
    pub NVCacheFeatureSetEnabled: u16,
    pub Reserved1_11: u16,
    pub NVCachePowerModeVersion: u16,
    pub NVCacheFeatureSetVersion: u16,
    pub NVCacheSizeLSW: u16,
    pub NVCacheSizeMSW: u16,
    pub NominalMediaRotationRate: u16,
    pub ReservedWord218: u16,
    pub NVCacheEstimatedTimeToSpinUpInSeconds: u8,
    pub Reserved_1: u8,
    pub WriteReadVerifySectorCountMode: u16,
    pub ReservedWord220: u16,
    pub ReservedWord221: u16,
    pub MajorVersion: u16,
    pub TransportType: u16,
    pub TransportMinorVersion: u16,
    pub ReservedWord224: [u16; 6],
    pub ExtendedNumberOfUserAddressableSectors: [u32; 2],
    pub MinBlocksPerDownloadMicrocodeMode03: u16,
    pub MaxBlocksPerDownloadMicrocodeMode03: u16,
    pub ReservedWord236: [u16; 19],
    pub Signature: u16,
    pub CheckSum: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFY_PACKET_DATA {
    pub PacketType: u16,
    pub IncompleteResponse: u16,
    pub Reserved1: u16,
    pub DrqDelay: u16,
    pub RemovableMedia: u16,
    pub CommandPacketType: u16,
    pub Reserved2: u16,
    pub DeviceType: u16,
    pub ResevedWord1: u16,
    pub UniqueConfiguration: u16,
    pub ReservedWords3: [u16; 7],
    pub SerialNumber: [u8; 20],
    pub ReservedWords20: [u16; 3],
    pub FirmwareRevision: [u8; 8],
    pub ModelNumber: [u8; 40],
    pub ReservedWords47: [u16; 2],
    pub VendorSpecific: u16,
    pub DmaSupported: u16,
    pub LbaSupported: u16,
    pub IordyDisabled: u16,
    pub IordySupported: u16,
    pub Obsolete: u16,
    pub OverlapSupported: u16,
    pub QueuedCommandsSupported: u16,
    pub InterleavedDmaSupported: u16,
    pub DeviceSpecificStandbyTimerValueMin: u16,
    pub Obsolete1: u16,
    pub ReservedWord50: u16,
    pub WordValid: u16,
    pub ObsoleteWords51: [u16; 2],
    pub TranslationFieldsValid: u16,
    pub Reserved3: u16,
    pub ReservedWords54: [u16; 8],
    pub UDMA0Supported: u16,
    pub UDMA1Supported: u16,
    pub UDMA2Supported: u16,
    pub UDMA3Supported: u16,
    pub UDMA4Supported: u16,
    pub UDMA5Supported: u16,
    pub UDMA6Supported: u16,
    pub MDMA0Supported: u16,
    pub MDMA1Supported: u16,
    pub MDMA2Supported: u16,
    pub DMASupported: u16,
    pub ReservedWord62: u16,
    pub DMADIRBitRequired: u16,
    pub MultiWordDMASupport: u16,
    pub MultiWordDMAActive: u16,
    pub AdvancedPIOModes: u16,
    pub ReservedByte64: u16,
    pub MinimumMWXferCycleTime: u16,
    pub RecommendedMWXferCycleTime: u16,
    pub MinimumPIOCycleTime: u16,
    pub MinimumPIOCycleTimeIORDY: u16,
    pub ReservedWords69: [u16; 2],
    pub BusReleaseDelay: u16,
    pub ServiceCommandDelay: u16,
    pub ReservedWords73: [u16; 2],
    pub QueueDepth: u16,
    pub ReservedWord75: u16,
    pub Reserved0: u16,
    pub SataGen1: u16,
    pub SataGen2: u16,
    pub SataGen3: u16,
    pub Reserved1_1: u16,
    pub HIPM: u16,
    pub PhyEvents: u16,
    pub Reserved3_1: u16,
    pub HostAutoPS: u16,
    pub DeviceAutoPS: u16,
    pub Reserved4: u16,
    pub Reserved5: u16,
    pub CurrentSpeed: u16,
    pub SlimlineDeviceAttention: u16,
    pub HostEnvironmentDetect: u16,
    pub Reserved: u16,
    pub Reserved0_1: u16,
    pub Reserved1_2: u16,
    pub DIPM: u16,
    pub Reserved2_1: u16,
    pub AsynchronousNotification: u16,
    pub SoftwareSettingsPreservation: u16,
    pub Reserved3_2: u16,
    pub Reserved0_2: u16,
    pub Reserved1_3: u16,
    pub DIPM_1: u16,
    pub Reserved2_2: u16,
    pub AsynchronousNotification_1: u16,
    pub SoftwareSettingsPreservation_1: u16,
    pub DeviceAutoPS_1: u16,
    pub Reserved3_3: u16,
    pub MajorRevision: u16,
    pub MinorRevision: u16,
    pub SmartCommands: u16,
    pub SecurityMode: u16,
    pub RemovableMedia_1: u16,
    pub PowerManagement: u16,
    pub PacketCommands: u16,
    pub WriteCache: u16,
    pub LookAhead: u16,
    pub ReleaseInterrupt: u16,
    pub ServiceInterrupt: u16,
    pub DeviceReset: u16,
    pub HostProtectedArea: u16,
    pub Obsolete1_1: u16,
    pub WriteBuffer: u16,
    pub ReadBuffer: u16,
    pub Nop: u16,
    pub Obsolete2: u16,
    pub DownloadMicrocode: u16,
    pub Reserved1_4: u16,
    pub AdvancedPm: u16,
    pub Msn: u16,
    pub PowerUpInStandby: u16,
    pub ManualPowerUp: u16,
    pub Reserved2_3: u16,
    pub SetMax: u16,
    pub Reserved3_4: u16,
    pub FlushCache: u16,
    pub Reserved4_1: u16,
    pub WordValid_1: u16,
    pub Reserved0_3: u16,
    pub GpLogging: u16,
    pub Reserved1_5: u16,
    pub WWN64Bit: u16,
    pub Reserved2_4: u16,
    pub WordValid_2: u16,
    pub SmartCommands_1: u16,
    pub SecurityMode_1: u16,
    pub RemovableMedia_2: u16,
    pub PowerManagement_1: u16,
    pub PacketCommands_1: u16,
    pub WriteCache_1: u16,
    pub LookAhead_1: u16,
    pub ReleaseInterrupt_1: u16,
    pub ServiceInterrupt_1: u16,
    pub DeviceReset_1: u16,
    pub HostProtectedArea_1: u16,
    pub Obsolete1_2: u16,
    pub WriteBuffer_1: u16,
    pub ReadBuffer_1: u16,
    pub Nop_1: u16,
    pub Obsolete2_1: u16,
    pub DownloadMicrocode_1: u16,
    pub Reserved1_6: u16,
    pub AdvancedPm_1: u16,
    pub Msn_1: u16,
    pub PowerUpInStandby_1: u16,
    pub ManualPowerUp_1: u16,
    pub Reserved2_5: u16,
    pub SetMax_1: u16,
    pub Reserved3_5: u16,
    pub FlushCache_1: u16,
    pub Reserved_1: u16,
    pub Reserved0_4: u16,
    pub GpLogging_1: u16,
    pub Reserved1_7: u16,
    pub WWN64Bit_1: u16,
    pub Reserved2_6: u16,
    pub WordValid_3: u16,
    pub UltraDMASupport: u16,
    pub UltraDMAActive: u16,
    pub TimeRequiredForNormalEraseModeSecurityEraseUnit: u16,
    pub TimeRequiredForEnhancedEraseModeSecurityEraseUnit: u16,
    pub CurrentAPMLevel: u16,
    pub MasterPasswordID: u16,
    pub HardwareResetResult: u16,
    pub ReservedWords94: [u16; 14],
    pub WorldWideName: [u16; 4],
    pub ReservedWords112: [u16; 13],
    pub AtapiZeroByteCount: u16,
    pub ReservedWord126: u16,
    pub MsnSupport: u16,
    pub ReservedWord127: u16,
    pub SecurityStatus: u16,
    pub VendorSpecific_1: [u16; 31],
    pub ReservedWord160: [u16; 16],
    pub ReservedWord176: [u16; 46],
    pub MajorVersion: u16,
    pub TransportType: u16,
    pub TransportMinorVersion: u16,
    pub ReservedWord224: [u16; 31],
    pub Signature: u16,
    pub CheckSum: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REGISTER_FIS {
    pub FisType: u8,
    pub Reserved0: u8,
    pub CmdReg: u8,
    pub Command: u8,
    pub Features: u8,
    pub SectorNumber: u8,
    pub CylinderLow: u8,
    pub CylinderHigh: u8,
    pub DeviceHead: u8,
    pub SectorNumberExp: u8,
    pub CylinderLowExp: u8,
    pub CylinderHighExp: u8,
    pub FeaturesExp: u8,
    pub SectorCount: u8,
    pub SectorCountExp: u8,
    pub Reserved2: u8,
    pub Control: u8,
    pub Reserved3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_SET_PASSWORD {
    pub PasswordIdentifier: u16,
    pub Reserved1: u16,
    pub MasterPasswordCapability: u16,
    pub Reserved2: u16,
    pub Password: [u16; 16],
    pub MasterPasswordIdentifier: u16,
    pub Reserved: [u16; 238],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFY_DEVICE_DATA_LOG_PAGE_HEADER {
    pub RevisionNumber: u64,
    pub PageNumber: u64,
    pub Reserved: u64,
    pub Valid: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFY_DEVICE_DATA_LOG_PAGE_SUPPORTED_CAPABILITIES {
    pub Header: u32,
    pub WRV: u64,
    pub WriteUncorrectable: u64,
    pub GplDma: u64,
    pub DmMode3: u64,
    pub FreeFall: u64,
    pub SenseData: u64,
    pub EPC: u64,
    pub SmartErrorLogging: u64,
    pub SmartSelfTest: u64,
    pub Reserved9: u64,
    pub Streaming: u64,
    pub GPL: u64,
    pub WriteFuaExt: u64,
    pub Unload: u64,
    pub DownloadMicrocode: u64,
    pub Reserved15ForCFA: u64,
    pub APM: u64,
    pub PUIS: u64,
    pub SpinUp: u64,
    pub Reserved19: u64,
    pub Cmd48Bit: u64,
    pub Reserved21: u64,
    pub FlushCacheExt: u64,
    pub Smart: u64,
    pub VolatileWriteCache: u64,
    pub ReadLookahead: u64,
    pub Reserved26: u64,
    pub WriteBuffer: u64,
    pub ReadBuffer: u64,
    pub NOP: u64,
    pub Reserved30: u64,
    pub RZAT: u64,
    pub Cmd28bit: u64,
    pub DownloadMicrocodeDma: u64,
    pub Reserved34: u64,
    pub WriteBufferDma: u64,
    pub ReadBufferDma: u64,
    pub Reserved37: u64,
    pub LpsMisalignmentReporting: u64,
    pub DRAT: u64,
    pub Reserved40ForCFA: u64,
    pub AmaxAddr: u64,
    pub SetEpcPowerSource: u64,
    pub LowPowerStandby: u64,
    pub DSN: u64,
    pub RequestSenseDeviceFault: u64,
    pub Reserved: u64,
    pub Valid: u64,
    pub DmMinTransferSize: u64,
    pub DmMaxTransferSize: u64,
    pub DmOffsetsImmediateSupported: u64,
    pub DmImmediateSupported: u64,
    pub DmOffsetsDeferredSupported: u64,
    pub Reserved_1: u64,
    pub Valid_1: u64,
    pub Rate: u64,
    pub Reserved_2: u64,
    pub Valid_2: u64,
    pub Factor: u64,
    pub Reserved_3: u64,
    pub Valid_3: u64,
    pub Count: u64,
    pub Reserved_4: u64,
    pub Valid_4: u64,
    pub Count_1: u64,
    pub Reserved_5: u64,
    pub Valid_5: u64,
    pub Name: u64,
    pub Reserved_6: u64,
    pub Valid_6: u64,
    pub TrimSupported: u64,
    pub Reserved_7: u64,
    pub Valid_7: u64,
    pub UtilizationA: u64,
    pub UtilizationB: u64,
    pub Reserved0: u64,
    pub UtilizationInterval: u64,
    pub UtilizationUnit: u64,
    pub UtilizationType: u64,
    pub Reserved1: u64,
    pub Valid_8: u64,
    pub DateTimeRateBasisSupported: u64,
    pub Reserved0_1: u64,
    pub PowerOnHoursRateBasisSupported: u64,
    pub Reserved1_1: u64,
    pub SincePowerOnRateBasisSupported: u64,
    pub Reserved2: u64,
    pub SettingRateBasisSupported: u64,
    pub Reserved3: u64,
    pub Valid_9: u64,
    pub Zoned: u64,
    pub Reserved_8: u64,
    pub Valid_10: u64,
    pub ReportZonesExtSupported: u64,
    pub NonDataOpenZoneExtSupported: u64,
    pub NonDataCloseZoneExtSupported: u64,
    pub NonDataFinishZoneExtSupported: u64,
    pub NonDataResetWritePointersExtSupported: u64,
    pub Reserved_9: u64,
    pub Valid_11: u64,
    pub Reserved_10: [u8; 392],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFY_DEVICE_DATA_LOG_PAGE_ZONED_DEVICE_INFO {
    pub Header: u32,
    pub URSWRZ: u64,
    pub Reserved: u64,
    pub Valid: u64,
    pub Reserved_1: u64,
    pub Valid_1: u64,
    pub Number: u64,
    pub Reserved_2: u64,
    pub Valid_2: u64,
    pub Number_1: u64,
    pub Reserved_3: u64,
    pub Valid_3: u64,
    pub Number_2: u64,
    pub Reserved_4: u64,
    pub Valid_4: u64,
    pub ZacMinorVersion: u64,
    pub Reserved0: u64,
    pub Valid_5: u64,
    pub Reserved_5: [u8; 456],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CURRENT_DEVICE_INTERNAL_STATUS_LOG {
    pub LogAddress: u8,
    pub Reserved0: [u8; 3],
    pub OrganizationID: u32,
    pub Area1LastLogPage: u16,
    pub Area2LastLogPage: u16,
    pub Area3LastLogPage: u16,
    pub Reserved2: [u8; 368],
    pub SavedDataAvailable: u8,
    pub SavedDataGenerationNumber: u8,
    pub ReasonIdentifier: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SAVED_DEVICE_INTERNAL_STATUS_LOG {
    pub LogAddress: u8,
    pub Reserved0: [u8; 3],
    pub OrganizationID: u32,
    pub Area1LastLogPage: u16,
    pub Area2LastLogPage: u16,
    pub Area3LastLogPage: u16,
    pub Reserved2: [u8; 368],
    pub SavedDataAvailable: u8,
    pub GenerationNumber: u8,
    pub ReasonIdentifier: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_STATISTICS_LOG_PAGE_HEADER {
    pub RevisionNumber: u64,
    pub PageNumber: u64,
    pub Reserved: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_SUPPORTED_DEVICE_STATISTICS {
    pub Header: u32,
    pub NumberOfEntries: u8,
    pub LogPageNumbers: [u8; 503],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_GENERAL_STATISTICS {
    pub Header: u32,
    pub Count: u64,
    pub Reserved: u64,
    pub Count_1: u64,
    pub Reserved_1: u64,
    pub Count_2: u64,
    pub Reserved_2: u64,
    pub Count_3: u64,
    pub Reserved_3: u64,
    pub Count_4: u64,
    pub Reserved_4: u64,
    pub Count_5: u64,
    pub Reserved_5: u64,
    pub TimeStamp: u64,
    pub Reserved_6: u64,
    pub Count_6: u64,
    pub Reserved_7: u64,
    pub Value: u64,
    pub Reserved_8: u64,
    pub Value_1: u64,
    pub Reserved0: u64,
    pub RateBasis: u64,
    pub RateValidity: u64,
    pub Reserved1: u64,
    pub Reserved_9: [u8; 424],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_FREE_FALL_STATISTICS {
    pub Header: u32,
    pub Count: u64,
    pub Reserved: u64,
    pub Count_1: u64,
    pub Reserved_1: u64,
    pub Reserved_2: [u8; 488],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_ROTATING_MEDIA_STATISTICS {
    pub Header: u32,
    pub Count: u64,
    pub Reserved: u64,
    pub Count_1: u64,
    pub Reserved_1: u64,
    pub Count_2: u64,
    pub Reserved_2: u64,
    pub Count_3: u64,
    pub Reserved_3: u64,
    pub Count_4: u64,
    pub Reserved_4: u64,
    pub Count_5: u64,
    pub Reserved_5: u64,
    pub Count_6: u64,
    pub Reserved_6: u64,
    pub Count_7: u64,
    pub Reserved_7: u64,
    pub Reserved_8: [u8; 440],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_GENERAL_ERROR_STATISTICS {
    pub Header: u32,
    pub Count: u64,
    pub Reserved: u64,
    pub Count_1: u64,
    pub Reserved_1: u64,
    pub Reserved_2: [u8; 488],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_TEMPERATURE_STATISTICS {
    pub Header: u32,
    pub Value: u64,
    pub Reserved: u64,
    pub Value_1: u64,
    pub Reserved_1: u64,
    pub Value_2: u64,
    pub Reserved_2: u64,
    pub Value_3: u64,
    pub Reserved_3: u64,
    pub Value_4: u64,
    pub Reserved_4: u64,
    pub Value_5: u64,
    pub Reserved_5: u64,
    pub Value_6: u64,
    pub Reserved_6: u64,
    pub Value_7: u64,
    pub Reserved_7: u64,
    pub Value_8: u64,
    pub Reserved_8: u64,
    pub Value_9: u64,
    pub Reserved_9: u64,
    pub Value_10: u64,
    pub Reserved_10: u64,
    pub Value_11: u64,
    pub Reserved_11: u64,
    pub Value_12: u64,
    pub Reserved_12: u64,
    pub Reserved_13: [u8; 400],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_TRANSPORT_STATISTICS {
    pub Header: u32,
    pub Count: u64,
    pub Reserved: u64,
    pub Count_1: u64,
    pub Reserved_1: u64,
    pub Count_2: u64,
    pub Reserved_2: u64,
    pub Reserved_3: [u8; 480],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_SOLID_STATE_DEVICE_STATISTICS {
    pub Header: u32,
    pub Value: u64,
    pub Reserved: u64,
    pub Reserved_1: [u8; 496],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_NCQ_COMMAND_ERROR {
    pub NcqTag: u8,
    pub Reserved0: u8,
    pub UNL: u8,
    pub NonQueuedCmd: u8,
    pub Reserved1: u8,
    pub Status: u8,
    pub Error: u8,
    pub LBA7_0: u8,
    pub LBA15_8: u8,
    pub LBA23_16: u8,
    pub Device: u8,
    pub LBA31_24: u8,
    pub LBA39_32: u8,
    pub LBA47_40: u8,
    pub Reserved2: u8,
    pub Count7_0: u8,
    pub Count15_8: u8,
    pub SenseKey: u8,
    pub ASC: u8,
    pub ASCQ: u8,
    pub Reserved3: [u8; 239],
    pub Vendor: [u8; 255],
    pub Checksum: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_NCQ_NON_DATA {
    pub AbortNcq: u32,
    pub AbortAll: u32,
    pub AbortStreaming: u32,
    pub AbortNonStreaming: u32,
    pub AbortSelectedTTag: u32,
    pub Reserved: u32,
    pub DeadlineHandling: u32,
    pub WriteDataNotContinue: u32,
    pub ReadDataNotContinue: u32,
    pub Reserved_1: u32,
    pub HybridDemoteBySize: u32,
    pub Reserved_2: u32,
    pub HybridChangeByLbaRange: u32,
    pub Reserved_3: u32,
    pub HybridControl: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub Reserved_6: u32,
    pub Reserved_7: u32,
    pub Reserved_8: u32,
    pub Reserved_9: u32,
    pub Reserved_10: u32,
    pub Reserved_11: u32,
    pub Reserved_12: u32,
    pub Reserved_13: u32,
    pub Reserved_14: u32,
    pub Reserved_15: u32,
    pub Reserved_16: [u32; 112],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_NCQ_SEND_RECEIVE {
    pub DataSetManagement: u32,
    pub HybridEvict: u32,
    pub Reserved: u32,
    pub Trim: u32,
    pub Reserved_1: u32,
    pub Reserved_2: [u32; 126],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_HYBRID_INFORMATION_HEADER {
    pub HybridInfoDescrCount: u16,
    pub Reserved0: u16,
    pub Enabled: u8,
    pub HybridHealth: u8,
    pub DirtyLowThreshold: u8,
    pub DirtyHighThreshold: u8,
    pub OptimalWriteGranularity: u8,
    pub MaximumHybridPriorityLevel: u8,
    pub Reserved1: u8,
    pub PowerCondidtion: u8,
    pub CachingMediumEnabled: u8,
    pub MaximumPriorityBehavior: u8,
    pub SupportCacheBehavior: u8,
    pub Reserved: u8,
    pub Reserved2: u8,
    pub TimeSinceEnabled: u32,
    pub NVMSize: u64,
    pub EnableCount: u64,
    pub MaximumEvictionCommands: u16,
    pub Reserved3: u16,
    pub MaximumEvictionDataBlocks: u16,
    pub Reserved_1: [u8; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_HYBRID_INFORMATION_DESCRIPTOR {
    pub HybridPriority: u8,
    pub ConsumedNVMSizeFraction: u8,
    pub ConsumedMappingResourcesFraction: u8,
    pub ConsumedNVMSizeForDirtyDataFraction: u8,
    pub ConsumedMappingResourcesForDirtyDataFraction: u8,
    pub Reserved: [u8; 11],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_HYBRID_INFORMATION {
    pub Header: u32,
    pub Descriptor: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_ZONES_EXT_DATA {
    pub ZoneListLength: u32,
    pub SAME: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 3],
    pub MaxLBA: u64,
    pub Reserved2: u64,
    pub Reserved3: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_ZONE_DESCRIPTOR {
    pub ZoneType: u8,
    pub Reserved0: u8,
    pub Reset: u8,
    pub NonSeq: u8,
    pub Reserved1: u8,
    pub ZoneCondition: u8,
    pub Reserved2: [u8; 6],
    pub ZoneLength: u64,
    pub Reserved3: u64,
    pub ZoneStartLBA: u64,
    pub Reserved4: u64,
    pub WritePointerLBA: u64,
    pub Reserved5: u64,
    pub Reserved6: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PHYSICAL_ELEMENT_STATUS_DESCRIPTOR {
    pub Reserved1: [u8; 4],
    pub ElementIdentifier: u32,
    pub Reserved2: [u8; 6],
    pub PhysicalElementType: u8,
    pub PhysicalElementHealth: u8,
    pub AssociatedCapacity: u64,
    pub Reserved3: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_GET_PHYSICAL_ELEMENT_STATUS_PARAMETER_DATA {
    pub NumberOfDescriptors: u32,
    pub NumberOfDescriptorsReturned: u32,
    pub ElementIdentifierBeingDepoped: u32,
    pub Reserved: [u8; 20],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_ADDRESS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Q2931_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AAL1_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AAL34_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AAL5_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AALUSER_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AAL_PARAMETERS_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_TRAFFIC_DESCRIPTOR_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_BROADBAND_BEARER_CAPABILITY_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_BHLI_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_BLLI_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_CALLING_PARTY_NUMBER_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_QOS_CLASS_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_BROADBAND_SENDING_COMPLETE_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_TRANSIT_NETWORK_SELECTION_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_CAUSE_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_LIJ_CALLID_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_RAW_IE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Q2931_CALLMGR_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_VPIVCI {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_FLOW_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_MEDIA_PARAMETERS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_SAP {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_PVC_SAP {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Q2931_ADD_PVC {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Q2931_DELETE_PVC {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_GET_CALL_INFORMATION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_VC_RATES_SUPPORTED {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_AAL_OOB_INFO {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_AAL5_INFO {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_AAL0_INFO {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATM_SERVICE_ADDRESS_LIST {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_FUNCTION_INFORMATION {
    pub Size: u32,
    pub Flags: u32,
    pub SubSystemId: u32,
    pub ControllerId: u32,
    pub LinkId: u32,
    pub PeripheralId: u32,
    pub FunctionId: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGGREGATION_COMMIT_GROUP {
    pub Size: u32,
    pub Technology: u32,
    pub FunctionCount: u32,
    pub FunctionInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGGREGATION_END_COMMIT_GROUP {
    pub Size: u32,
    pub CommitGroupHandle: u32,
    pub CommitOperation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGGREGATION_DATAPORT_CONNECTION {
    pub Size: u32,
    pub SrcFunctionInfo: u32,
    pub SrcDataPortCount: u32,
    pub SrcDataPortNumbers: [u32; 1],
    pub DstFunctionInfo: u32,
    pub DstDataPortCount: u32,
    pub DstDataPortNumbers: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGGREGATION_CONNECTION_MAP {
    pub Size: u32,
    pub ConnectionCount: u32,
    pub Connection: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AGGREGATION_DEVICE {
    pub FunctionIndex: u32,
    pub FunctionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AGGREGATION_DEVICES {
    pub Size: u32,
    pub FunctionCount: u32,
    pub FunctionIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AGGREGATION_ASSOCIATION {
    pub AggregationId: u32,
    pub FunctionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AGGREGATION_ASSOCIATIONS {
    pub Size: u32,
    pub Count: u32,
    pub Association: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUDIO_MODULE_ID {
    pub ClassId: u32,
    pub InstanceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_FUNCTION_INFORMATION {
    pub Size: u32,
    pub Version: u32,
    pub FunctionNumber: u32,
    pub FunctionType: u32,
    pub FunctionManufacturerId: u32,
    pub FunctionId: u32,
    pub ControllerId: u32,
    pub LinkId: u32,
    pub UniqueId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_ENDPOINT_CONFIG {
    pub Size: u32,
    pub Version: u32,
    pub SdcaFunctionInformationCount: u32,
    pub SdcaFunctionInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUDIO_ENDPOINT_CONFIG {
    pub Size: u32,
    pub Version: u32,
    pub AudioInterfaceType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUDIO_SENSORS_INTERFACE_V0100 {
    pub InterfaceHeader: u32,
    pub EvtAudioSensorsStartSession: usize,
    pub EvtAudioSensorsStopSession: usize,
    pub EvtAudioSensorsGetBuffer: usize,
    pub EvtAudioSensorsSetBuffer: usize,
    pub EvtAudioSensorsSubmitReadReport: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUX_MODULE_BASIC_INFO {
    pub ImageBase: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUX_MODULE_EXTENDED_INFO {
    pub BasicInfo: u32,
    pub ImageSize: u32,
    pub FileNameOffset: u16,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_DATA {
    pub BugCheckDataSize: u32,
    pub BugCheckCode: u32,
    pub Parameter1: usize,
    pub Parameter2: usize,
    pub Parameter3: usize,
    pub Parameter4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_IRB {
    pub Function: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_COMMAND_IRB {
    pub Common: u32,
    pub SubunitAddrFlag: u8,
    pub AlternateOpcodesFlag: u8,
    pub TimeoutFlag: u8,
    pub RetryFlag: u8,
    pub CommandType: u8,
    pub ResponseCode: u8,
    pub SubunitAddr: *mut u8,
    pub AlternateOpcodes: *mut u8,
    pub Timeout: i64,
    pub Retries: u8,
    pub Opcode: u8,
    pub OperandLength: u32,
    pub Operands: [u8; 1],
    pub NodeAddress: u32,
    pub Generation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PIN_COUNT {
    pub PinCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PIN_DESCRIPTOR {
    pub PinId: u32,
    pub PinDescriptor: u32,
    pub IntersectHandler: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVCPRECONNECTINFO {
    pub DeviceID: u32,
    pub SubunitAddress: [u8; 1],
    pub SubunitPlugNumber: u32,
    pub DataFlow: u32,
    pub Flags: u32,
    pub UnitPlugNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PRECONNECT_INFO {
    pub PinId: u32,
    pub ConnectInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVCCONNECTINFO {
    pub DeviceID: u32,
    pub SubunitAddress: [u8; 1],
    pub SubunitPlugNumber: u32,
    pub DataFlow: u32,
    pub hPlug: usize,
    pub UnitPlugNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_SETCONNECT_INFO {
    pub PinId: u32,
    pub ConnectInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PIN_ID {
    pub PinId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_EXT_PLUG_COUNTS {
    pub ExtInputs: u32,
    pub ExtOutputs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_UNIQUE_ID {
    pub DeviceID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PEER_DO_LOCATOR {
    pub NodeAddress: u32,
    pub Generation: u32,
    pub DeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_PEER_DO_LIST {
    pub Count: u32,
    pub field_Objects: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_SUBUNIT_INFO_BLOCK {
    pub Info: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_MULTIFUNC_IRB {
    pub Common: u32,
    pub PinCount: u32,
    pub PinDescriptor: u32,
    pub PreConnectInfo: u32,
    pub SetConnectInfo: u32,
    pub PinId: u32,
    pub ExtPlugCounts: u32,
    pub UniqueID: u32,
    pub PeerLocator: u32,
    pub PeerList: u32,
    pub Subunits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_SUBUNIT_ADDR_SPEC {
    pub Flags: u32,
    pub SubunitAddress: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVINFO {
    pub dwDVAAuxSrc: u32,
    pub dwDVAAuxCtl: u32,
    pub dwDVAAuxSrc1: u32,
    pub dwDVAAuxCtl1: u32,
    pub dwDVVAuxSrc: u32,
    pub dwDVVAuxCtl: u32,
    pub dwDVReserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_HDR1 {
    pub DBC: u32,
    pub Rsv00: u32,
    pub SPH: u32,
    pub QPC: u32,
    pub FN: u32,
    pub DBS: u32,
    pub SID: u32,
    pub Bit00: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_HDR2_SYT {
    pub SYT: u32,
    pub RSV: u32,
    pub STYPE: u32,
    pub F5060_OR_TSF: u32,
    pub FMT: u32,
    pub Bit10: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_HDR2_FDF {
    pub FDF: u32,
    pub FMT: u32,
    pub Bit10: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CIP_HDR2_MPEGTS {
    pub TSF: u32,
    pub RSV23bit: u32,
    pub FMT: u32,
    pub Bit10: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVCSTRM_FORMAT_INFO {
    pub SizeOfThisBlock: u32,
    pub AVCStrmFormat: u32,
    pub cipHdr1: u32,
    pub cipHdr2: u32,
    pub SrcPacketsPerFrame: u32,
    pub FrameSize: u32,
    pub NumOfRcvBuffers: u32,
    pub NumOfXmtBuffers: u32,
    pub OptionFlags: u32,
    pub AvgTimePerFrame: u32,
    pub BlockPeriod: u32,
    pub Reserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVCSTRM_OPEN_STRUCT {
    pub DataFlow: u32,
    pub AVCFormatInfo: usize,
    pub AVCStreamContext: usize,
    pub hPlugLocal: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVCSTRM_BUFFER_STRUCT {
    pub ClockProvider: i32,
    pub ClockHandle: usize,
    pub StreamHeader: usize,
    pub FrameBuffer: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AVC_STREAM_REQUEST_BLOCK {
    pub SizeOfThisBlock: u32,
    pub Version: u32,
    pub Function: u32,
    pub Flags: u32,
    pub Status: u32,
    pub AVCStreamContext: usize,
    pub Context1: usize,
    pub Context2: usize,
    pub Context3: usize,
    pub Context4: usize,
    pub Reserved: [u32; 4],
    pub StreamState: u32,
    pub OpenStruct: u32,
    pub BufferStruct: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THROTTLING_STATE {
    pub NextTime: i64,
    pub CurrentIncrement: u32,
    pub MaximumDelay: u32,
    pub Increment: i64,
    pub NumberOfQueries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_PIN_PAIR {
    pub Method: u32,
    pub InputPinId: u32,
    pub OutputPinId: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_PIN {
    pub Method: u32,
    pub PinId: u32,
    pub PinType: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_PIN_PAIRING {
    pub ulInputPin: u32,
    pub ulOutputPin: u32,
    pub ulcMaxInputsPerOutput: u32,
    pub ulcMinInputsPerOutput: u32,
    pub ulcMaxOutputsPerInput: u32,
    pub ulcMinOutputsPerInput: u32,
    pub ulcTopologyJoints: u32,
    pub pTopologyJoints: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_FILTER_TEMPLATE {
    pub pFilterDescriptor: usize,
    pub ulcPinPairs: u32,
    pub pPinPairs: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_LE_AUDIO_CODEC_ID {
    pub CodingFormat: u32,
    pub CompanyId: u32,
    pub VendorCodecId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_LE_AUDIO_BIDIRECTIONAL_MULTICHANNEL_STREAMING_CAPABILITY {
    pub CodecId: u32,
    pub IsCodecPresent: i32,
    pub RenderChannelCount: u32,
    pub CaptureChannelCount: u32,
    pub RenderSamplingFrequencies: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_HEADER {
    pub ListEntry: u32,
    pub Length: u32,
    pub Version: u16,
    pub Type: u16,
    pub BthportFlags: u32,
    pub Status: u32,
    pub BtStatus: u32,
    pub Context: [usize; 1],
    pub ClientContext: [usize; 1],
    pub Reserved: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_CONFIG_RANGE {
    pub Min: u16,
    pub Max: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_CONFIG_VALUE_RANGE {
    pub Min: u16,
    pub Preferred: u16,
    pub Max: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_FLOWSPEC {
    pub Flags: u8,
    pub ServiceType: u8,
    pub TokenRate: u32,
    pub TokenBucketSize: u32,
    pub PeakBandwidth: u32,
    pub Latency: u32,
    pub DelayVariation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_RETRANSMISSION_AND_FLOW_CONTROL {
    pub Mode: u8,
    pub TxWindowSize: u8,
    pub MaxTransmit: u8,
    pub RetransmissionTO: u16,
    pub MonitorTO: u16,
    pub MaxPDUSize: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_EXTENDED_FLOW_SPEC {
    pub Identifier: u8,
    pub ServiceType: u8,
    pub MaxSDUSize: u16,
    pub SDUInterArrivalTime: u32,
    pub AccessLatency: u32,
    pub FlushTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_HEADER {
    pub Type: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCO_INDICATION_PARAMETERS {
    pub ConnectionHandle: u32,
    pub BtAddress: u32,
    pub LinkType: u32,
    pub Reason: u32,
    pub CloseNow: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_REGISTER_SERVER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub Reserved: u32,
    pub IndicationFlags: u32,
    pub IndicationCallback: usize,
    pub IndicationCallbackContext: usize,
    pub ReferenceObject: usize,
    pub ServerHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_UNREGISTER_SERVER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ServerHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_OPEN_CHANNEL {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub TransmitBandwidth: u32,
    pub ReceiveBandwidth: u32,
    pub MaxLatency: u16,
    pub PacketType: u16,
    pub ContentFormat: u16,
    pub Reserved: u16,
    pub RetransmissionEffort: u32,
    pub ChannelFlags: u32,
    pub CallbackFlags: u32,
    pub Callback: usize,
    pub CallbackContext: usize,
    pub ReferenceObject: usize,
    pub ChannelHandle: u32,
    pub Response: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_CLOSE_CHANNEL {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_FLUSH_CHANNEL {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
    pub FlushFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BASEBAND_CHANNEL_INFO {
    pub Transmission_Interval: u8,
    pub Retransmission_Window: u8,
    pub AirMode: u8,
    pub Rx_Packet_Length: u16,
    pub Tx_Packet_Length: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_GET_CHANNEL_INFO {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
    pub InfoFlags: u32,
    pub TransmitBandwidth: u32,
    pub ReceiveBandwidth: u32,
    pub MaxLatency: u16,
    pub PacketType: u16,
    pub ContentFormat: u16,
    pub Reserved: u16,
    pub RetransmissionEffort: u32,
    pub ChannelFlags: u32,
    pub HciConnectionHandle: u32,
    pub LinkType: u32,
    pub BasebandInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_TRANSFER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
    pub TransferFlags: u32,
    pub BufferSize: u32,
    pub Buffer: usize,
    pub BufferMDL: usize,
    pub DataTag: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_SCO_GET_SYSTEM_INFO {
    pub Hdr: u32,
    pub Features: u32,
    pub MaxChannels: u32,
    pub TransferUnit: u32,
    pub PacketTypes: u16,
    pub DataFormats: u16,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct L2CAP_CONFIG_OPTION {
    pub Header: u32,
    pub field_DynamicBuffer: u32,
    pub FixedBuffer: [u8; 4],
    pub Flags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANNEL_CONFIG_PARAMETERS {
    pub Flags: u32,
    pub Mtu: u32,
    pub FlushTO: u32,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub Flow: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANNEL_CONFIG_PARAMETERS_ENHANCED {
    pub Flags: u32,
    pub Mtu: u32,
    pub FlushTO: u32,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub Flow: u32,
    pub RetransmissionAndFlow: u32,
    pub Fcs: u32,
    pub ExtendedFlowSpec: u32,
    pub ExtendedWindowSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANNEL_CONFIG_RESULTS_ENHANCED {
    pub Params: u32,
    pub ExtraOptionsBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANNEL_CONFIG_RESULTS {
    pub Params: u32,
    pub ExtraOptionsBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INDICATION_PARAMETERS {
    pub ConnectionHandle: u32,
    pub BtAddress: u32,
    pub PSM: u16,
    pub CurrentParams: u32,
    pub RequestedParams: u32,
    pub ResponseParams: u32,
    pub Response: u16,
    pub CurrentParams_1: u32,
    pub RequestedParams_1: u32,
    pub RejectedParams: u32,
    pub UnknownTypes: usize,
    pub NumUnknownTypes: u32,
    pub NewRequestParams: u32,
    pub Response_1: u16,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub Reason: u32,
    pub CloseNow: u8,
    pub PacketLength: u32,
    pub TotalQueueLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INDICATION_PARAMETERS_ENHANCED {
    pub ConnectionHandle: u32,
    pub BtAddress: u32,
    pub PSM: u16,
    pub CurrentParams: u32,
    pub RequestedParams: u32,
    pub ResponseParams: u32,
    pub Response: u16,
    pub CurrentParams_1: u32,
    pub RequestedParams_1: u32,
    pub RejectedParams: u32,
    pub UnknownTypes: usize,
    pub NumUnknownTypes: u32,
    pub NewRequestParams: u32,
    pub Response_1: u16,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub Reason: u32,
    pub CloseNow: u8,
    pub PacketLength: u32,
    pub TotalQueueLength: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_REGISTER_SERVER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub PSM: u16,
    pub IndicationFlags: u32,
    pub IndicationCallback: usize,
    pub IndicationCallbackContext: usize,
    pub ReferenceObject: usize,
    pub ServerHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_UNREGISTER_SERVER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ServerHandle: usize,
    pub Psm: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_OPEN_ENHANCED_CHANNEL {
    pub Hdr: u32,
    pub ChannelHandle: u32,
    pub Response: u16,
    pub ResponseStatus: u16,
    pub Psm: u16,
    pub ChannelFlags: u32,
    pub BtAddress: u32,
    pub Flags: u32,
    pub Mtu: u32,
    pub FlushTO: u32,
    pub Flow: u32,
    pub LinkTO: u16,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub ServiceType: u8,
    pub Latency: u32,
    pub Flags_1: u32,
    pub RetransmissionAndFlow: u32,
    pub Fcs: u16,
    pub ExtendedFlowSpec: u32,
    pub ExtendedWindowSize: u16,
    pub Flags_2: u32,
    pub Mtu_1: u32,
    pub FlushTO_1: u32,
    pub CallbackFlags: u32,
    pub Callback: usize,
    pub CallbackContext: usize,
    pub ReferenceObject: usize,
    pub OutResults: u32,
    pub InResults: u32,
    pub IncomingQueueDepth: u8,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_OPEN_CHANNEL {
    pub Hdr: u32,
    pub ChannelHandle: u32,
    pub Response: u16,
    pub ResponseStatus: u16,
    pub Psm: u16,
    pub ChannelFlags: u32,
    pub BtAddress: u32,
    pub Flags: u32,
    pub Mtu: u32,
    pub FlushTO: u32,
    pub Flow: u32,
    pub LinkTO: u16,
    pub NumExtraOptions: u32,
    pub ExtraOptions: usize,
    pub ServiceType: u8,
    pub Latency: u32,
    pub Flags_1: u32,
    pub Mtu_1: u32,
    pub FlushTO_1: u32,
    pub CallbackFlags: u32,
    pub Callback: usize,
    pub CallbackContext: usize,
    pub ReferenceObject: usize,
    pub OutResults: u32,
    pub InResults: u32,
    pub IncomingQueueDepth: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_CLOSE_CHANNEL {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_ACL_TRANSFER {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
    pub TransferFlags: u32,
    pub BufferSize: u32,
    pub Buffer: usize,
    pub BufferMDL: usize,
    pub Timeout: i64,
    pub RemainingBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_GET_LOCAL_BD_ADDR {
    pub Hdr: u32,
    pub BtAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_GET_DEVICE_INTERFACE_STRING {
    pub Hdr: u32,
    pub DeviceInterfaceString: usize,
    pub DeviceInterfaceStringCbLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_PING {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub PingRequestLength: u8,
    pub PingRequestData: [u8; 1],
    pub PingResponseLength: u8,
    pub PingResponseData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_L2CA_UPDATE_CHANNEL {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub ChannelHandle: u32,
    pub NewChannelFlags: u32,
    pub FailedChannelFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_PSM {
    pub Hdr: u32,
    pub Psm: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_ACL_GET_MODE {
    pub Hdr: u32,
    pub BtAddress: u32,
    pub AclMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB_ACL_ENTER_ACTIVE_MODE {
    pub Hdr: u32,
    pub BtAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRB {
    pub BrbHeader: u32,
    pub BrbGetDeviceInterfaceString: u32,
    pub BrbGetLocalBdAddress: u32,
    pub BrbAclGetMode: u32,
    pub BrbAclEnterActiveMode: u32,
    pub BrbPsm: u32,
    pub BrbL2caRegisterServer: u32,
    pub BrbL2caUnregisterServer: u32,
    pub BrbL2caOpenChannel: u32,
    pub BrbL2caCloseChannel: u32,
    pub BrbL2caPing: u32,
    pub BrbL2caAclTransfer: u32,
    pub BrbL2caUpdateChannel: u32,
    pub BrbL2caOpenEnhancedChannel: u32,
    pub BrbScoRegisterServer: u32,
    pub BrbScoUnregisterServer: u32,
    pub BrbScoOpenChannel: u32,
    pub BrbScoCloseChannel: u32,
    pub BrbScoFlushChannel: u32,
    pub BrbScoTransfer: u32,
    pub BrbScoGetChannelInfo: u32,
    pub BrbScoGetSystemInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_PROFILE_DRIVER_INTERFACE {
    pub Interface: u32,
    pub BthAllocateBrb: usize,
    pub BthFreeBrb: usize,
    pub BthInitializeBrb: usize,
    pub BthReuseBrb: usize,
    pub IsBluetoothVersionAvailable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_ENUMERATOR_INFO {
    pub EnumeratorType: u32,
    pub Action: u32,
    pub Port: u32,
    pub Flags: u32,
    pub Guid: u32,
    pub InstanceId: u32,
    pub InstanceIdStr: [u32; 1],
    pub Vid: u16,
    pub Pid: u16,
    pub Mfg: u16,
    pub LocalMfg: u16,
    pub VidType: u16,
    pub ServiceName: [u32; 1],
    pub SdpPriLangServiceName: [i8; 1],
    pub DeviceString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHHFP_DESCRIPTOR {
    pub InputPinCategory: u32,
    pub OutputPinCategory: u32,
    pub ContainerId: u32,
    pub SupportsVolume: i32,
    pub VolumePropertyValuesSize: u32,
    pub FriendlyName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHHFP_DESCRIPTOR2 {
    pub InputPinCategory: u32,
    pub OutputPinCategory: u32,
    pub ContainerId: u32,
    pub SupportsVolume: i32,
    pub VolumePropertyValuesSize: u32,
    pub FriendlyName: u32,
    pub SupportsNREC: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHHFP_AUDIO_DEVICE_CAPABILTIES {
    pub Version: u32,
    pub Supports16kHzSampling: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HFP_BYPASS_CODEC_ID_V1 {
    pub CodecId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHDDI_SDP_NODE_INTERFACE {
    pub Interface: u32,
    pub SdpCreateNodeTree: usize,
    pub SdpFreeTree: usize,
    pub SdpCreateNodeNil: usize,
    pub SdpCreateNodeBoolean: usize,
    pub SdpCreateNodeUint8: usize,
    pub SdpCreateNodeUint16: usize,
    pub SdpCreateNodeUint32: usize,
    pub SdpCreateNodeUint64: usize,
    pub SdpCreateNodeUint128: usize,
    pub SdpCreateNodeInt8: usize,
    pub SdpCreateNodeInt16: usize,
    pub SdpCreateNodeInt32: usize,
    pub SdpCreateNodeInt64: usize,
    pub SdpCreateNodeInt128: usize,
    pub SdpCreateNodeUuid16: usize,
    pub SdpCreateNodeUuid32: usize,
    pub SdpCreateNodeUuid128: usize,
    pub SdpCreateNodeString: usize,
    pub SdpCreateNodeUrl: usize,
    pub SdpCreateNodeAlternative: usize,
    pub SdpCreateNodeSequence: usize,
    pub SdpAddAttributeToTree: usize,
    pub SdpAppendNodeToContainerNode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHDDI_SDP_PARSE_INTERFACE {
    pub Interface: u32,
    pub SdpValidateStream: usize,
    pub SdpConvertStreamToTree: usize,
    pub SdpConvertTreeToStream: usize,
    pub SdpFreeTree: usize,
    pub SdpByteSwapUuid128: usize,
    pub SdpByteSwapUint128: usize,
    pub SdpByteSwapUint64: usize,
    pub SdpRetrieveUuid128: usize,
    pub SdpRetrieveUint128: usize,
    pub SdpRetrieveUint64: usize,
    pub SdpFindAttributeInTree: usize,
    pub SdpGetNextElement: usize,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub Reserved3: u32,
    pub Reserved4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHX_VERSION {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHX_CAPABILITIES {
    pub MaxAclTransferInSize: u32,
    pub ScoSupport: u32,
    pub MaxScoChannels: u32,
    pub IsDeviceIdleCapable: u8,
    pub IsDeviceWakeCapable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTHX_HCI_READ_WRITE_CONTEXT {
    pub DataLen: u32,
    pub Type: u8,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGE_BUFFERING_STATE_REQUEST {
    pub ListEntry: u32,
    pub Flags: u32,
    pub SrvOpen: usize,
    pub SrvOpenKey: usize,
    pub MRxContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_BUFFERING_MANAGER {
    pub DispatcherActive: u8,
    pub HandlerInactive: u8,
    pub LastChanceHandlerActive: u8,
    pub Pad: u8,
    pub SpinLock: u32,
    pub CumulativeNumberOfBufferingChangeRequests: u32,
    pub NumberOfUnhandledRequests: i32,
    pub NumberOfUndispatchedRequests: i32,
    pub NumberOfOutstandingOpens: u32,
    pub DispatcherList: u32,
    pub HandlerList: u32,
    pub LastChanceHandlerList: u32,
    pub DispatcherWorkItem: u32,
    pub HandlerWorkItem: u32,
    pub LastChanceHandlerWorkItem: u32,
    pub Mutex: u32,
    pub SrvOpenLists: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWERSOURCESTATUS {
    pub PowerSourceId: usize,
    pub MaxChargeCurrent: u32,
    pub PowerSourceInformation: u32,
    pub PowerSourceStatus: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHARGINGSTATUSCOMPLETE {
    pub UsbFnStatus: usize,
    pub UsbTypeCStatus: usize,
    pub KDStatus: usize,
    pub WirelessStatus: usize,
    pub ForceDisableChargingRefCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWERSOURCEUPDATE {
    pub PowerSourceId: usize,
    pub MaxChargeCurrent: u32,
    pub PowerSourceAvailableStatus: u8,
    pub PowerSourceInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWERSOURCEUPDATEEX {
    pub Source: usize,
    pub ChargerId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CAD_POWER_SOURCE_INFO {
    pub SourceId: usize,
    pub Version: u16,
    pub VaData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CAD_POWER_SOURCE_INFO_USB {
    pub SourceId: usize,
    pub Version: u16,
    pub Flags: u32,
    pub MaxCurrent: u32,
    pub Voltage: u32,
    pub PortType: i32,
    pub PortId: u64,
    pub PowerSourceInformation: u32,
    pub OemCharger: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERYPROVISIONINGSTATUS {
    pub IsDeviceProvisionedForBatteryBlank: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONFIGURABLE_CHARGER_PROPERTY_HEADER {
    pub Size: u32,
    pub ChargerId: u32,
    pub PropertyId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_FIND_BUFFER {
    pub CreationDate: u32,
    pub CreationTime: u32,
    pub LastAccessDate: u32,
    pub LastAccessTime: u32,
    pub LastWriteDate: u32,
    pub LastWriteTime: u32,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub FileNameLength: u8,
    pub FileName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_FIND_BUFFER2 {
    pub CreationDate: u32,
    pub CreationTime: u32,
    pub LastAccessDate: u32,
    pub LastAccessTime: u32,
    pub LastWriteDate: u32,
    pub LastWriteTime: u32,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub FileNameLength: u8,
    pub FileName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEA {
    pub fEA: u8,
    pub cbName: u8,
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEALIST {
    pub reserved_padding: u32,
    pub list: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GEA {
    pub cbName: u8,
    pub szName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GEALIST {
    pub reserved_padding: u32,
    pub list: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EAOP {
    pub fpGEAList: usize,
    pub fpFEAList: usize,
    pub oError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSALLOCATE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUMELABEL {
    pub cch: u8,
    pub szVolLabel: [i8; 12],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSINFO {
    pub reserved_padding: u32,
    pub vol: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_HEADER {
    pub Protocol: [u8; 4],
    pub Command: u8,
    pub ErrorClass: u8,
    pub Reserved: u8,
    pub reserved_padding: u32,
    pub Flags: u8,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: [u32; 6],
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub SecuritySignature: [u8; 1],
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NT_SMB_HEADER {
    pub Protocol: [u8; 4],
    pub Command: u8,
    pub ErrorClass: u8,
    pub Reserved: u8,
    pub reserved_padding: u32,
    pub NtStatus: u32,
    pub Flags: u8,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: [u32; 6],
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub SecuritySignature: [u8; 1],
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_PARAMS {
    pub WordCount: u8,
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERIC_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CANCEL_FORWARD {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CANCEL_FORWARD {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CHECK_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CHECK_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CLOSE_AND_TREE_DISC {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CLOSE_AND_TREE_DISC {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CLOSE_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CLOSE_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_COPY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_COPY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CREATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CREATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CREATE_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CREATE_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CREATE_TEMPORARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CREATE_TEMPORARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_DELETE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_DELETE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_DELETE_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_DELETE_DIRECTORY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_ECHO {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_ECHO {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FIND_CLOSE2 {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FIND_CLOSE2 {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FIND_NOTIFY_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FIND_NOTIFY_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FLUSH {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FLUSH {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FORWARD_USER_NAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FORWARD_USER_NAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_GET_MACHINE_NAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_GET_MACHINE_NAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_GET_PRINT_QUEUE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_GET_PRINT_QUEUE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_IOCTL {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
    pub reserved_padding_13: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_IOCTL_INTERIM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_IOCTL_SECONDARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_IOCTL {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_LOCK_BYTE_RANGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_LOCK_BYTE_RANGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_LOCKING_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCKING_ANDX_RANGE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NTLOCKING_ANDX_RANGE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_LOCKING_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_LOGOFF_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_LOGOFF_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_MOVE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_MOVE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NEGOTIATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NEGOTIATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub ServerTime: u32,
    pub ServerDate: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_OLD_NEGOTIATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NT_NEGOTIATE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_OPEN {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_OPEN {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_OPEN_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_OPEN_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_CREATE_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub Reserved: u8,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub DesiredAccess: u32,
    pub AllocationSize: i64,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub SecurityFlags: u8,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NT_CREATE_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub OplockLevel: u8,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub CreationTime: u32,
    pub LastAccessTime: u32,
    pub LastWriteTime: u32,
    pub ChangeTime: u32,
    pub reserved_padding_3: u32,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub Directory: u8,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_OPEN_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_OPEN_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_PROCESS_EXIT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_PROCESS_EXIT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_INFORMATION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_INFORMATION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: [u32; 5],
    pub reserved_padding_4: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_INFORMATION2 {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_INFORMATION2 {
    pub WordCount: u8,
    pub CreationDate: u32,
    pub CreationTime: u32,
    pub LastAccessDate: u32,
    pub LastAccessTime: u32,
    pub LastWriteDate: u32,
    pub LastWriteTime: u32,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_INFORMATION_DISK {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_INFORMATION_DISK {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_INFORMATION_SRV {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_INFORMATION_SRV {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub smb_vldate: u32,
    pub smb_vltime: u32,
    pub smb_vllen: u8,
    pub Reserved: u8,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub ServerTime: u32,
    pub ServerDate: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
    pub reserved_padding_13: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_READ {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_READ {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: [u32; 4],
    pub reserved_padding_2: u32,
    pub BufferFormat: u8,
    pub reserved_padding_3: u32,
    pub Buffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_READ_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_READ_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_READ_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: [u32; 2],
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_READ_MPX {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_READ_MPX {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_READ_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_READ_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_RENAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NTRENAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_RENAME {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEARCH {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEARCH {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_RESUME_KEY {
    pub Reserved: u8,
    pub FileName: [u8; 11],
    pub Sid: u8,
    pub reserved_padding: u32,
    pub Consumer: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_DIRECTORY_INFORMATION {
    pub ResumeKey: u32,
    pub FileAttributes: u8,
    pub LastWriteTime: u32,
    pub LastWriteDate: u32,
    pub reserved_padding: u32,
    pub FileName: [u8; 13],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEEK {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEEK {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEND_BROADCAST_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEND_END_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEND_END_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEND_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEND_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEND_START_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEND_START_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SEND_TEXT_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SEND_TEXT_MB_MESSAGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SESSION_SETUP_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_SESSION_SETUP_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub Reserved_: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SESSION_SETUP_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_INFORMATION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: [u32; 5],
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SET_INFORMATION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_INFORMATION2 {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub CreationDate: u32,
    pub CreationTime: u32,
    pub LastAccessDate: u32,
    pub LastAccessTime: u32,
    pub LastWriteDate: u32,
    pub LastWriteTime: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SET_INFORMATION2 {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_TRANSACTION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub MaxSetupCount: u8,
    pub Reserved: u8,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub SetupCount: u8,
    pub Reserved3: u8,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_TRANSACTION_INTERIM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_TRANSACTION_SECONDARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_TRANSACTION {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub SetupCount: u8,
    pub Reserved2: u8,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_TRANSACTION {
    pub WordCount: u8,
    pub MaxSetupCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub SetupCount: u8,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NT_TRANSACTION_INTERIM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_TRANSACTION_SECONDARY {
    pub WordCount: u8,
    pub Reserved1: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub Reserved3: u8,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NT_TRANSACTION {
    pub WordCount: u8,
    pub Reserved1: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub SetupCount: u8,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_TREE_CONNECT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_TREE_CONNECT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_TREE_CONNECT_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_TREE_CONNECT_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_21_TREE_CONNECT_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_TREE_DISCONNECT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_TREE_DISCONNECT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_UNLOCK_BYTE_RANGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_UNLOCK_BYTE_RANGE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub BufferFormat: u8,
    pub reserved_padding_5: u32,
    pub Buffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_AND_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub Pad: u8,
    pub Buffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_AND_CLOSE_LONG {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: [u32; 3],
    pub reserved_padding_5: u32,
    pub Pad: u8,
    pub Buffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_AND_CLOSE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_WRITE_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_ANDX {
    pub WordCount: u8,
    pub AndXCommand: u8,
    pub AndXReserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_MPX {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_MPX_INTERIM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_MPX_DATAGRAM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_MPX_SECONDARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_COMPLETE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_PRINT_FILE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_WRITE_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_WRITE_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_RAW_INTERIM {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_WRITE_RAW_SECONDARY {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_103_WRITE_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_103_WRITE_RAW {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_CANCEL {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_NT_CANCEL {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_FS_INFORMATION_FID {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CREATE_DIRECTORY2 {
    pub reserved_padding: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CREATE_DIRECTORY2 {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FIND_FIRST2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FIND_FIRST2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_FIND_NEXT2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_FIND_NEXT2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_OPEN2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: [u32; 5],
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_OPEN2 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_FS_INFORMATION {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_FS_INFORMATION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_FS_INFORMATION {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SET_FS_INFORMATION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_PATH_INFORMATION {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_PATH_INFORMATION {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_PATH_INFORMATION {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SET_PATH_INFORMATION {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_FILE_INFORMATION {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_FILE_INFORMATION {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_FILE_INFORMATION {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_SET_FILE_INFORMATION {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_TRANSACT_MAILSLOT {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub MaxSetupCount: u8,
    pub Reserved: u8,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub SetupWordCount: u8,
    pub Reserved2: u8,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
    pub reserved_padding_13: u32,
    pub reserved_padding_14: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_TRANSACT_NAMED_PIPE {
    pub WordCount: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub MaxSetupCount: u8,
    pub Reserved: u8,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NAMED_PIPE_INFORMATION_1 {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub MaximumInstances: u8,
    pub CurrentInstances: u8,
    pub PipeNameLength: u8,
    pub PipeName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_PEEK_NMPIPE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_CREATE_WITH_SD_OR_EA {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub DesiredAccess: u32,
    pub AllocationSize: i64,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub SecurityFlags: u8,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_CREATE_WITH_SD_OR_EA {
    pub OplockLevel: u8,
    pub Reserved: u8,
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub CreationTime: u32,
    pub LastAccessTime: u32,
    pub LastWriteTime: u32,
    pub ChangeTime: u32,
    pub reserved_padding_3: u32,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub Directory: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_IO_CONTROL {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub IsFsctl: u8,
    pub IsFlags: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NOTIFY_CHANGE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub WatchTree: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_SET_SECURITY_DESCRIPTOR {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_QUERY_SECURITY_DESCRIPTOR {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESP_QUERY_SECURITY_DESCRIPTOR {
    pub reserved_padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQ_NT_RENAME {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub NewName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DICTIONARY {
    pub Signature: u64,
    pub List: usize,
    pub SpinLock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASSPNP_SCAN_FOR_SPECIAL_INFO {
    pub VendorId: *mut i8,
    pub ProductId: *mut i8,
    pub ProductRevision: *mut i8,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRB_HISTORY_ITEM {
    pub TickCountSent: i64,
    pub TickCountCompleted: i64,
    pub MillisecondsDelayOnRetry: u32,
    pub NormalizedSenseData: u32,
    pub SrbStatus: u8,
    pub ClassDriverUse: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRB_HISTORY {
    pub ClassDriverUse: [usize; 4],
    pub TotalHistoryCount: u32,
    pub UsedHistoryCount: u32,
    pub History: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_WMI_INFO {
    pub GuidCount: u32,
    pub GuidRegInfo: usize,
    pub ClassQueryWmiRegInfo: usize,
    pub ClassQueryWmiDataBlock: usize,
    pub ClassSetWmiDataBlock: usize,
    pub ClassSetWmiDataItem: usize,
    pub ClassExecuteWmiMethod: usize,
    pub ClassWmiFunctionControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_DEV_INFO {
    pub DeviceExtensionSize: u32,
    pub DeviceType: u32,
    pub StackSize: u8,
    pub DeviceCharacteristics: u32,
    pub ClassError: usize,
    pub ClassReadWriteVerification: usize,
    pub ClassDeviceControl: usize,
    pub ClassShutdownFlush: usize,
    pub ClassCreateClose: usize,
    pub ClassInitDevice: usize,
    pub ClassStartDevice: usize,
    pub ClassPowerDevice: usize,
    pub ClassStopDevice: usize,
    pub ClassRemoveDevice: usize,
    pub ClassQueryPnpCapabilities: usize,
    pub ClassWmiInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_INIT_DATA {
    pub InitializationDataSize: u32,
    pub FdoData: u32,
    pub PdoData: u32,
    pub ClassAddDevice: usize,
    pub ClassEnumerateDevice: usize,
    pub ClassQueryId: usize,
    pub ClassStartIo: usize,
    pub ClassUnload: usize,
    pub ClassTick: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_OBJECT_EXTENSION {
    pub FileObject: usize,
    pub DeviceObject: usize,
    pub LockCount: u32,
    pub McnDisableCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_WORKING_SET {
    pub Size: u32,
    pub XferPacketsWorkingSetMaximum: u32,
    pub XferPacketsWorkingSetMinimum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_INTERPRET_SENSE_INFO2 {
    pub Size: u32,
    pub HistoryCount: u32,
    pub Compress: u32,
    pub Interpret: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_DRIVER_EXTENSION {
    pub RegistryPath: u32,
    pub InitData: u32,
    pub DeviceCount: u32,
    pub ClassFdoQueryWmiRegInfoEx: usize,
    pub ClassPdoQueryWmiRegInfoEx: usize,
    pub EtwHandle: u32,
    pub DeviceMajorFunctionTable: [usize; 1],
    pub MpDeviceMajorFunctionTable: [usize; 1],
    pub InterpretSenseInfo: usize,
    pub WorkingSet: usize,
    pub SrbSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMMON_DEVICE_EXTENSION {
    pub Version: u32,
    pub DeviceObject: usize,
    pub LowerDeviceObject: usize,
    pub PartitionZeroExtension: usize,
    pub DriverExtension: usize,
    pub RemoveLock: i32,
    pub RemoveEvent: u32,
    pub RemoveTrackingSpinlock: u32,
    pub RemoveTrackingList: usize,
    pub RemoveTrackingUntrackedCount: i32,
    pub DriverData: usize,
    pub IsFdo: u8,
    pub IsInitialized: u8,
    pub IsSrbLookasideListInitialized: u8,
    pub PreviousState: u8,
    pub CurrentState: u8,
    pub IsRemoved: u32,
    pub DeviceName: u32,
    pub ChildList: usize,
    pub PartitionNumber: u32,
    pub PartitionLength: i64,
    pub StartingOffset: i64,
    pub DevInfo: usize,
    pub PagingPathCount: u32,
    pub DumpPathCount: u32,
    pub HibernationPathCount: u32,
    pub PathCountEvent: u32,
    pub SrbLookasideList: u32,
    pub MountedDeviceInterfaceName: u32,
    pub GuidCount: u32,
    pub GuidRegInfo: usize,
    pub FileObjectDictionary: u32,
    pub PrivateCommonData: usize,
    pub Reserved1: usize,
    pub field_DispatchTable: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_POWER_OPTIONS {
    pub PowerDown: u32,
    pub LockQueue: u32,
    pub HandleSpinDown: u32,
    pub HandleSpinUp: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_POWER_CONTEXT {
    pub PowerDown: u32,
    pub PowerDown2: u32,
    pub PowerDown3: u32,
    pub PowerUp: u32,
    pub Options: u32,
    pub InUse: u8,
    pub QueueLocked: u8,
    pub FinalStatus: u32,
    pub RetryCount: u32,
    pub RetryInterval: u32,
    pub CompletionRoutine: usize,
    pub DeviceObject: usize,
    pub Irp: usize,
    pub Srb: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_VPD_B1_DATA {
    pub CommandStatus: u32,
    pub MediumRotationRate: u16,
    pub NominalFormFactor: u8,
    pub Zoned: u8,
    pub MediumProductType: u32,
    pub DepopulationTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_VPD_B0_DATA {
    pub CommandStatus: u32,
    pub MaxUnmapLbaCount: u32,
    pub MaxUnmapBlockDescrCount: u32,
    pub OptimalUnmapGranularity: u32,
    pub UnmapGranularityAlignment: u32,
    pub UGAVALID: u8,
    pub Reserved0: u8,
    pub OptimalTransferLengthGranularity: u16,
    pub MaximumTransferLength: u32,
    pub OptimalTransferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_VPD_B2_DATA {
    pub CommandStatus: u32,
    pub ThresholdExponent: u8,
    pub DP: u8,
    pub ANC_SUP: u8,
    pub Reserved0: u8,
    pub LBPRZ: u8,
    pub LBPWS10: u8,
    pub LBPWS: u8,
    pub LBPU: u8,
    pub ProvisioningType: u8,
    pub Reserved1: u8,
    pub SoftThresholdEventPending: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_READ_CAPACITY16_DATA {
    pub CommandStatus: u32,
    pub BytesPerLogicalSector: u32,
    pub BytesPerPhysicalSector: u32,
    pub BytesOffsetForSectorAlignment: u32,
    pub LBProvisioningEnabled: u8,
    pub LBProvisioningReadZeros: u8,
    pub Reserved0: [u8; 2],
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_VPD_ECOP_BLOCK_DEVICE_ROD_LIMITS {
    pub CommandStatus: u32,
    pub MaximumRangeDescriptors: u16,
    pub Restricted: u8,
    pub Reserved: u8,
    pub MaximumInactivityTimer: u32,
    pub DefaultInactivityTimer: u32,
    pub MaximumTokenTransferSize: u64,
    pub OptimalTransferCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_FUNCTION_SUPPORT_INFO {
    pub SyncLock: u32,
    pub GenerationCount: u32,
    pub ChangeRequestCount: u32,
    pub BlockLimits: u32,
    pub BlockDeviceCharacteristics: u32,
    pub LBProvisioning: u32,
    pub BlockDeviceRODLimits: u32,
    pub ZonedBlockDeviceCharacteristics: u32,
    pub Reserved: u32,
    pub DeviceType: u32,
    pub SeekPenaltyProperty: u32,
    pub AccessAlignmentProperty: u32,
    pub TrimProperty: u32,
    pub TrimProcess: u32,
    pub RegAccessAlignmentQueryNotSupported: u8,
    pub AsynchronousNotificationSupported: u8,
    pub UseModeSense10: u8,
    pub Reserved_1: u8,
    pub Reserved_2: [u8; 2],
    pub BlockLimitsData: u32,
    pub DeviceCharacteristicsData: u32,
    pub LBProvisioningData: u32,
    pub ReadCapacity16Data: u32,
    pub BlockDeviceRODLimitsData: u32,
    pub D3ColdSupported: u32,
    pub DeviceWakeable: u32,
    pub IdlePowerEnabled: u32,
    pub D3IdleTimeoutOverridden: u32,
    pub NoVerifyDuringIdlePower: u32,
    pub Reserved2: u32,
    pub D3IdleTimeout: u32,
    pub HwFirmwareGetInfoSupport: u32,
    pub HwFirmwareInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDITIONAL_FDO_DATA {
    pub DeviceGuidFlags: u32,
    pub DeviceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FUNCTIONAL_DEVICE_EXTENSION {
    pub Version: u32,
    pub DeviceObject: usize,
    pub CommonExtension: u32,
    pub LowerPdo: usize,
    pub DeviceDescriptor: usize,
    pub AdapterDescriptor: usize,
    pub DevicePowerState: u32,
    pub DMByteSkew: u32,
    pub DMSkew: u32,
    pub DMActive: u8,
    pub SenseDataLength: u8,
    pub Reserved: u8,
    pub Reserved0: [u8; 2],
    pub DiskGeometry: u32,
    pub SenseData: usize,
    pub TimeOutValue: u32,
    pub DeviceNumber: u32,
    pub SrbFlags: u32,
    pub ErrorCount: u32,
    pub LockCount: i32,
    pub ProtectedLockCount: i32,
    pub InternalLockCount: i32,
    pub EjectSynchronizationEvent: u32,
    pub DeviceFlags: u16,
    pub SectorShift: u8,
    pub CdbForceUnitAccess: u8,
    pub ReservedByte: u8,
    pub MediaChangeDetectionInfo: usize,
    pub Unused1: usize,
    pub Unused2: usize,
    pub KernelModeMcnContext: u32,
    pub MediaChangeCount: u32,
    pub DeviceDirectory: usize,
    pub ReleaseQueueSpinLock: u32,
    pub ReleaseQueueIrp: usize,
    pub ReleaseQueueSrb: u32,
    pub ReleaseQueueNeeded: u8,
    pub ReleaseQueueInProgress: u8,
    pub ReleaseQueueIrpFromPool: u8,
    pub FailurePredicted: u8,
    pub FailureReason: u32,
    pub FailurePredictionInfo: usize,
    pub PowerDownInProgress: u8,
    pub EnumerationInterlock: u32,
    pub ChildLock: u32,
    pub ChildLockOwner: usize,
    pub ChildLockAcquisitionCount: u32,
    pub ScanForSpecialFlags: u32,
    pub PowerRetryDpc: u32,
    pub PowerRetryTimer: u32,
    pub PowerContext: u32,
    pub Reserved1: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
    pub CompletionSuccessCount: u32,
    pub SavedSrbFlags: u32,
    pub SavedErrorCount: u32,
    pub Reserved1_1: usize,
    pub PrivateFdoData: usize,
    pub FunctionSupportInfo: usize,
    pub MiniportDescriptor: usize,
    pub Reserved2_1: usize,
    pub Reserved3_1: usize,
    pub AdditionalFdoData: usize,
    pub Reserved4_1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_DEVICE_EXTENSION {
    pub Version: u32,
    pub DeviceObject: usize,
    pub CommonExtension: u32,
    pub IsMissing: u8,
    pub IsEnumerated: u8,
    pub PrivatePdoData: usize,
    pub Reserved1: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMPLETION_CONTEXT {
    pub DeviceObject: usize,
    pub Srb: u32,
    pub SrbEx: u32,
    pub SrbExBuffer: [u8; 1],
    pub Srb_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_QUERY_WMI_REGINFO_EX_LIST {
    pub Size: u32,
    pub ClassFdoQueryWmiRegInfoEx: u32,
    pub ClassPdoQueryWmiRegInfoEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT4_DRIVER_CMD {
    pub hChannelHandle: u32,
    pub ulSize: u32,
    pub ulOffset: u32,
    pub ulTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT4_DC_OPEN_DATA {
    pub bHsid: u32,
    pub fAddActivity: u32,
    pub hChannelHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT4_DC_CREATE_DATA {
    pub bPsid: u32,
    pub pServiceName: [i8; 1],
    pub bType: u32,
    pub ulBufferSize: u32,
    pub usMaxHtoPPacketSize: u16,
    pub usMaxPtoHPacketSize: u16,
    pub bHsid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT4_DC_DESTROY_DATA {
    pub bHsid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT4_ACTIVITY {
    pub ulMessage: u32,
    pub ulByteCount: u32,
    pub hChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EMULATOR_ACCESS_ENTRY {
    pub BasePort: u32,
    pub NumConsecutivePorts: u32,
    pub AccessType: u32,
    pub AccessMode: u8,
    pub StringSupport: u8,
    pub Routine: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINKED_DEVICE {
    pub ChainUid: u32,
    pub NumberOfLinksInChain: u32,
    pub LeadLink: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDEO_OUTPUT_CAPABILITIES {
    pub InterfaceTechnology: u32,
    pub MonitorOrientationAwareness: u32,
    pub SupportsSdtvModes: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_INTEGRATED_DISPLAY_CHILD {
    pub InterfaceTechnology: u32,
    pub DescriptorLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CHILD_CAPABILITIES {
    pub VideoOutput: u32,
    pub MustBeZero: u32,
    pub IntegratedDisplayChild: u32,
    pub HpdAwareness: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CHILD_DESCRIPTOR {
    pub ChildDeviceType: u32,
    pub ChildCapabilities: u32,
    pub AcpiUid: u32,
    pub ChildUid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEVICE_DESCRIPTOR {
    pub DescriptorOffset: u32,
    pub DescriptorLength: u32,
    pub DescriptorBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GENERIC_DESCRIPTOR {
    pub HardwareId: [u32; 1],
    pub InstanceId: [u32; 1],
    pub CompatibleId: [u32; 1],
    pub DeviceText: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CHILD_STATUS {
    pub Type: u32,
    pub ChildUid: u32,
    pub Connected: u8,
    pub Angle: u8,
    pub Connected_1: u8,
    pub MiracastMonitorType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_I2C_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiI2CTransmitDataToDisplay: usize,
    pub DxgkDdiI2CReceiveDataFromDisplay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_I2C_INTERFACE_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiI2CTransmitDataToDisplay: usize,
    pub DxgkDdiI2CReceiveDataFromDisplay: usize,
    pub DxgkDdiI2CTransmitAndReceiveDataFromDisplay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPM_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiOPMGetCertificateSize: usize,
    pub DxgkDdiOPMGetCertificate: usize,
    pub DxgkDdiOPMCreateProtectedOutput: usize,
    pub DxgkDdiOPMGetRandomNumber: usize,
    pub DxgkDdiOPMSetSigningKeyAndSequenceNumbers: usize,
    pub DxgkDdiOPMGetInformation: usize,
    pub DxgkDdiOPMGetCOPPCompatibleInformation: usize,
    pub DxgkDdiOPMConfigureProtectedOutput: usize,
    pub DxgkDdiOPMDestroyProtectedOutput: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPM_INTERFACE_2_JTP {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiOPMGetCertificateSize: usize,
    pub DxgkDdiOPMGetCertificate: usize,
    pub DxgkDdiOPMCreateProtectedOutput: usize,
    pub DxgkDdiOPMGetRandomNumber: usize,
    pub DxgkDdiOPMSetSigningKeyAndSequenceNumbers: usize,
    pub DxgkDdiOPMGetInformation: usize,
    pub DxgkDdiOPMGetCOPPCompatibleInformation: usize,
    pub DxgkDdiOPMConfigureProtectedOutput: usize,
    pub DxgkDdiOPMDestroyProtectedOutput: usize,
    pub DxgkDdiOPMCreateProtectedOutputVirtualMode: usize,
    pub DxgkDdiOPMCreateProtectedOutputNonLocalDisplay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPM_INTERFACE_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiOPMGetCertificateSize: usize,
    pub DxgkDdiOPMGetCertificate: usize,
    pub DxgkDdiOPMCreateProtectedOutput: usize,
    pub DxgkDdiOPMGetRandomNumber: usize,
    pub DxgkDdiOPMSetSigningKeyAndSequenceNumbers: usize,
    pub DxgkDdiOPMGetInformation: usize,
    pub DxgkDdiOPMGetCOPPCompatibleInformation: usize,
    pub DxgkDdiOPMConfigureProtectedOutput: usize,
    pub DxgkDdiOPMDestroyProtectedOutput: usize,
    pub DxgkDdiOPMCreateProtectedOutputNonLocalDisplay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPM_INTERFACE_3 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiOPMGetCertificateSize: usize,
    pub DxgkDdiOPMGetCertificate: usize,
    pub DxgkDdiOPMCreateProtectedOutput: usize,
    pub DxgkDdiOPMGetRandomNumber: usize,
    pub DxgkDdiOPMSetSigningKeyAndSequenceNumbers: usize,
    pub DxgkDdiOPMGetInformation: usize,
    pub DxgkDdiOPMGetCOPPCompatibleInformation: usize,
    pub DxgkDdiOPMConfigureProtectedOutput: usize,
    pub DxgkDdiOPMDestroyProtectedOutput: usize,
    pub DxgkDdiOPMCreateProtectedOutputNonLocalDisplay: usize,
    pub DxgkDdiOPMSetSrmList: usize,
    pub DxgkDdiOPMGetSrmListVersion: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MIRACAST_CAPS {
    pub MaxChunkPrivateDriverDataSize: u32,
    pub HdcpSupport: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MIRACAST_DISPLAY_CALLBACKS {
    pub MiracastHandle: usize,
    pub DxgkCbMiracastSendMessage: u32,
    pub DxgkCbReportChunkInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MIRACAST_DISPLAY_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiMiracastQueryCaps: usize,
    pub DxgkDdiMiracastCreateContext: usize,
    pub DxgkDdiMiracastIoControl: usize,
    pub DxgkDdiMiracastDestroyContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_AGP_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub AgpAllocatePool: u32,
    pub AgpFreePool: u32,
    pub AgpSetCommand: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEBUG_REPORT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TIMED_OPERATION {
    pub Size: u16,
    pub OwnerTag: usize,
    pub OsHandled: u8,
    pub TimeoutTriggered: u8,
    pub Timeout: i64,
    pub StartTick: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TIMED_OPERATION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SPB_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FIRMWARE_TABLE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAYMUX_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiDisplayMuxGetDriverSupportLevel: usize,
    pub DxgkDdiDisplayMuxGetRuntimeStatus: usize,
    pub DxgkDdiDisplayMuxPreSwitchAway: usize,
    pub DxgkDdiDisplayMuxPreSwitchAwayGetPrivateData: usize,
    pub DxgkDdiDisplayMuxPreSwitchTo: usize,
    pub DxgkDdiDisplayMuxSwitchCanceled: usize,
    pub DxgkDdiDisplayMuxPostSwitchAway: usize,
    pub DxgkDdiDisplayMuxPostSwitchToPhase1: usize,
    pub DxgkDdiDisplayMuxPostSwitchToPhase2: usize,
    pub DxgkDdiDisplayMuxUpdateState: usize,
    pub DxgkDdiDisplayMuxReportPresence: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAYMUX_INTERFACE_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiDisplayMuxGetDriverSupportLevel: usize,
    pub DxgkDdiDisplayMuxGetRuntimeStatus: usize,
    pub DxgkDdiDisplayMuxPreSwitchAway: usize,
    pub DxgkDdiDisplayMuxPreSwitchAwayGetPrivateData: usize,
    pub DxgkDdiDisplayMuxPreSwitchTo: usize,
    pub DxgkDdiDisplayMuxSwitchCanceled: usize,
    pub DxgkDdiDisplayMuxPostSwitchAway: usize,
    pub DxgkDdiDisplayMuxPostSwitchToPhase1: usize,
    pub DxgkDdiDisplayMuxPostSwitchToPhase2: usize,
    pub DxgkDdiDisplayMuxUpdateState: usize,
    pub DxgkDdiDisplayMuxReportPresence: usize,
    pub DxgkDdiDisplayMuxSetInternalPanelInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FEATURE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub IsFeatureEnabled: u32,
    pub QueryFeatureInterface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_FEATURE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub QueryFeatureSupport: usize,
    pub QueryFeatureInterface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAY_CORE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiQueryMonitorDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_WAITWAKE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiWaitWakeArming: usize,
    pub DxgkDdiWaitWakeDisarming: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEVICE_INFO {
    pub MiniportDeviceContext: usize,
    pub PhysicalDeviceObject: usize,
    pub DeviceRegistryPath: u32,
    pub TranslatedResourceList: usize,
    pub SystemMemorySize: i64,
    pub HighestPhysicalAddress: u64,
    pub AgpApertureBase: u64,
    pub AgpApertureSize: usize,
    pub DockingState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_START_INFO {
    pub RequiredDmaQueueEntry: u32,
    pub AdapterGuid: u32,
    pub AdapterLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAY_OWNERSHIP_FLAGS {
    pub FrameBufferState: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGNOSTIC_CATEGORIES {
    pub Notifications: u32,
    pub Progressions: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGTYPE_NOTIFICATIONS {
    pub PanelSelfRefreshSoftware: u32,
    pub PanelSelfRefreshHardware: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGTYPE_PROGRESSIONS {
    pub SyncLockEnableSync: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGNOSTIC_TYPES {
    pub Notifications: u32,
    pub Progressions: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGNOSTIC_HEADER {
    pub Category: u32,
    pub Type: u32,
    pub Size: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub SequenceNumber: u32,
    pub TargetId: u32,
    pub SourceId: u32,
    pub Id: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGNOSTIC_PSR {
    pub Header: u32,
    pub RefreshReason: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAGNOSTIC_SYNCLOCK_ENABLESYNC {
    pub Header: u32,
    pub DuringSetTiming: u32,
    pub EnableSyncStart: u32,
    pub EnableSyncEnd: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKRNL_INTERFACE {
    pub Size: u32,
    pub Version: u32,
    pub DeviceHandle: usize,
    pub DxgkCbEvalAcpiMethod: u32,
    pub DxgkCbGetDeviceInformation: u32,
    pub DxgkCbIndicateChildStatus: u32,
    pub DxgkCbMapMemory: u32,
    pub DxgkCbQueueDpc: u32,
    pub DxgkCbQueryServices: u32,
    pub DxgkCbReadDeviceSpace: u32,
    pub DxgkCbSynchronizeExecution: u32,
    pub DxgkCbUnmapMemory: u32,
    pub DxgkCbWriteDeviceSpace: u32,
    pub DxgkCbIsDevicePresent: u32,
    pub DxgkCbGetHandleData: u32,
    pub DxgkCbGetHandleParent: u32,
    pub DxgkCbEnumHandleChildren: u32,
    pub DxgkCbNotifyInterrupt: u32,
    pub DxgkCbNotifyDpc: u32,
    pub DxgkCbQueryVidPnInterface: u32,
    pub DxgkCbQueryMonitorInterface: u32,
    pub DxgkCbGetCaptureAddress: u32,
    pub DxgkCbLogEtwEvent: u32,
    pub DxgkCbExcludeAdapterAccess: u32,
    pub DxgkCbCreateContextAllocation: u32,
    pub DxgkCbDestroyContextAllocation: u32,
    pub DxgkCbSetPowerComponentActive: u32,
    pub DxgkCbSetPowerComponentIdle: u32,
    pub DxgkCbAcquirePostDisplayOwnership: u32,
    pub DxgkCbPowerRuntimeControlRequest: u32,
    pub DxgkCbSetPowerComponentLatency: u32,
    pub DxgkCbSetPowerComponentResidency: u32,
    pub DxgkCbCompleteFStateTransition: u32,
    pub DxgkCbCompletePStateTransition: u32,
    pub DxgkCbMapContextAllocation: u32,
    pub DxgkCbUpdateContextAllocation: u32,
    pub DxgkCbReserveGpuVirtualAddressRange: u32,
    pub DxgkCbAcquireHandleData: u32,
    pub DxgkCbReleaseHandleData: u32,
    pub DxgkCbHardwareContentProtectionTeardown: u32,
    pub DxgkCbMultiPlaneOverlayDisabled: u32,
    pub DxgkCbMitigatedRangeUpdate: u32,
    pub DxgkCbInvalidateHwContext: u32,
    pub DxgkCbIndicateConnectorChange: u32,
    pub DxgkCbUnblockUEFIFrameBufferRanges: u32,
    pub DxgkCbAcquirePostDisplayOwnership2: u32,
    pub DxgkCbSetProtectedSessionStatus: u32,
    pub DxgkCbAllocateContiguousMemory: u32,
    pub DxgkCbFreeContiguousMemory: u32,
    pub DxgkCbAllocatePagesForMdl: u32,
    pub DxgkCbFreePagesFromMdl: u32,
    pub DxgkCbPinFrameBufferForSave: u32,
    pub DxgkCbUnpinFrameBufferForSave: u32,
    pub DxgkCbMapFrameBufferPointer: u32,
    pub DxgkCbUnmapFrameBufferPointer: u32,
    pub DxgkCbMapMdlToIoMmu: u32,
    pub DxgkCbUnmapMdlFromIoMmu: u32,
    pub DxgkCbReportDiagnostic: u32,
    pub DxgkCbSignalEvent: u32,
    pub DxgkCbIsFeatureEnabled: u32,
    pub DxgkCbSaveMemoryForHotUpdate: u32,
    pub DxgkCbNotifyCursorSupportChange: u32,
    pub DxgkCbQueryFeatureSupport: u32,
    pub DxgkCbCreatePhysicalMemoryObject: u32,
    pub DxgkCbDestroyPhysicalMemoryObject: u32,
    pub DxgkCbMapPhysicalMemory: u32,
    pub DxgkCbUnmapPhysicalMemory: u32,
    pub DxgkCbAllocateAdl: u32,
    pub DxgkCbFreeAdl: u32,
    pub DxgkCbOpenPhysicalMemoryObject: u32,
    pub DxgkCbClosePhysicalMemoryObject: u32,
    pub DxgkCbPinFrameBufferForSave2: u32,
    pub DxgkCbDisconnectDoorbell: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRE_START_INFO {
    pub ReservedIn: u32,
    pub Input: u32,
    pub SupportPreserveBootDisplay: u32,
    pub IsUEFIFrameBufferCpuAccessibleDuringStartup: u32,
    pub ReservedOut: u32,
    pub Output: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYDIAGNOSTICTYPESSUPPORT {
    pub DiagnosticCategory: u32,
    pub NoninvasiveTypes: u32,
    pub InvasiveTypes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONTROLDIAGNOSTICREPORTING {
    pub DiagnosticCategory: u32,
    pub RequestedDiagnostics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTARGETADJUSTEDCOLORIMETRY2 {
    pub TargetId: u32,
    pub AdjustedColorimetry: u32,
    pub SdrWhiteLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SYSTEM_DISPLAY_ENABLE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CHILD_CONTAINER_ID {
    pub ContainerId: u32,
    pub PortId: u32,
    pub ManufacturerName: u16,
    pub ProductCode: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COLLECTDIAGNOSTICINFO {
    pub hAdapter: usize,
    pub Type: u32,
    pub BucketingString: [i8; 1],
    pub DescriptionString: [i8; 1],
    pub pReserved: usize,
    pub BufferSizeIn: u32,
    pub BufferSizeOut: u32,
    pub pBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_INITIALIZATION_DATA {
    pub Version: u32,
    pub DxgkDdiAddDevice: usize,
    pub DxgkDdiStartDevice: usize,
    pub DxgkDdiStopDevice: usize,
    pub DxgkDdiRemoveDevice: usize,
    pub DxgkDdiDispatchIoRequest: usize,
    pub DxgkDdiInterruptRoutine: usize,
    pub DxgkDdiDpcRoutine: usize,
    pub DxgkDdiQueryChildRelations: usize,
    pub DxgkDdiQueryChildStatus: usize,
    pub DxgkDdiQueryDeviceDescriptor: usize,
    pub DxgkDdiSetPowerState: usize,
    pub DxgkDdiNotifyAcpiEvent: usize,
    pub DxgkDdiResetDevice: usize,
    pub DxgkDdiUnload: usize,
    pub DxgkDdiQueryInterface: usize,
    pub DxgkDdiControlEtwLogging: usize,
    pub DxgkDdiQueryAdapterInfo: usize,
    pub DxgkDdiCreateDevice: usize,
    pub DxgkDdiCreateAllocation: usize,
    pub DxgkDdiDestroyAllocation: usize,
    pub DxgkDdiDescribeAllocation: usize,
    pub DxgkDdiGetStandardAllocationDriverData: usize,
    pub DxgkDdiAcquireSwizzlingRange: usize,
    pub DxgkDdiReleaseSwizzlingRange: usize,
    pub DxgkDdiPatch: usize,
    pub DxgkDdiSubmitCommand: usize,
    pub DxgkDdiPreemptCommand: usize,
    pub DxgkDdiBuildPagingBuffer: usize,
    pub DxgkDdiSetPalette: usize,
    pub DxgkDdiSetPointerPosition: usize,
    pub DxgkDdiSetPointerShape: usize,
    pub DxgkDdiResetFromTimeout: usize,
    pub DxgkDdiRestartFromTimeout: usize,
    pub DxgkDdiEscape: usize,
    pub DxgkDdiCollectDbgInfo: usize,
    pub DxgkDdiQueryCurrentFence: usize,
    pub DxgkDdiIsSupportedVidPn: usize,
    pub DxgkDdiRecommendFunctionalVidPn: usize,
    pub DxgkDdiEnumVidPnCofuncModality: usize,
    pub DxgkDdiSetVidPnSourceAddress: usize,
    pub DxgkDdiSetVidPnSourceVisibility: usize,
    pub DxgkDdiCommitVidPn: usize,
    pub DxgkDdiUpdateActiveVidPnPresentPath: usize,
    pub DxgkDdiRecommendMonitorModes: usize,
    pub DxgkDdiRecommendVidPnTopology: usize,
    pub DxgkDdiGetScanLine: usize,
    pub DxgkDdiStopCapture: usize,
    pub DxgkDdiControlInterrupt: usize,
    pub DxgkDdiCreateOverlay: usize,
    pub DxgkDdiDestroyDevice: usize,
    pub DxgkDdiOpenAllocation: usize,
    pub DxgkDdiCloseAllocation: usize,
    pub DxgkDdiRender: usize,
    pub DxgkDdiPresent: usize,
    pub DxgkDdiUpdateOverlay: usize,
    pub DxgkDdiFlipOverlay: usize,
    pub DxgkDdiDestroyOverlay: usize,
    pub DxgkDdiCreateContext: usize,
    pub DxgkDdiDestroyContext: usize,
    pub DxgkDdiLinkDevice: usize,
    pub DxgkDdiSetDisplayPrivateDriverFormat: usize,
    pub DxgkDdiDescribePageTable: usize,
    pub DxgkDdiUpdatePageTable: usize,
    pub DxgkDdiUpdatePageDirectory: usize,
    pub DxgkDdiMovePageDirectory: usize,
    pub DxgkDdiSubmitRender: usize,
    pub DxgkDdiCreateAllocation2: usize,
    pub DxgkDdiRenderKm: usize,
    pub Reserved: usize,
    pub DxgkDdiQueryVidPnHWCapability: usize,
    pub DxgkDdiSetPowerComponentFState: usize,
    pub DxgkDdiQueryDependentEngineGroup: usize,
    pub DxgkDdiQueryEngineStatus: usize,
    pub DxgkDdiResetEngine: usize,
    pub DxgkDdiStopDeviceAndReleasePostDisplayOwnership: usize,
    pub DxgkDdiSystemDisplayEnable: usize,
    pub DxgkDdiSystemDisplayWrite: usize,
    pub DxgkDdiCancelCommand: usize,
    pub DxgkDdiGetChildContainerId: usize,
    pub DxgkDdiPowerRuntimeControlRequest: usize,
    pub DxgkDdiSetVidPnSourceAddressWithMultiPlaneOverlay: usize,
    pub DxgkDdiNotifySurpriseRemoval: usize,
    pub DxgkDdiGetNodeMetadata: usize,
    pub DxgkDdiSetPowerPState: usize,
    pub DxgkDdiControlInterrupt2: usize,
    pub DxgkDdiCheckMultiPlaneOverlaySupport: usize,
    pub DxgkDdiCalibrateGpuClock: usize,
    pub DxgkDdiFormatHistoryBuffer: usize,
    pub DxgkDdiRenderGdi: usize,
    pub DxgkDdiSubmitCommandVirtual: usize,
    pub DxgkDdiSetRootPageTable: usize,
    pub DxgkDdiGetRootPageTableSize: usize,
    pub DxgkDdiMapCpuHostAperture: usize,
    pub DxgkDdiUnmapCpuHostAperture: usize,
    pub DxgkDdiCheckMultiPlaneOverlaySupport2: usize,
    pub DxgkDdiCreateProcess: usize,
    pub DxgkDdiDestroyProcess: usize,
    pub DxgkDdiSetVidPnSourceAddressWithMultiPlaneOverlay2: usize,
    pub Reserved1: usize,
    pub Reserved2: usize,
    pub DxgkDdiPowerRuntimeSetDeviceHandle: usize,
    pub DxgkDdiSetStablePowerState: usize,
    pub DxgkDdiSetVideoProtectedRegion: usize,
    pub DxgkDdiCheckMultiPlaneOverlaySupport3: usize,
    pub DxgkDdiSetVidPnSourceAddressWithMultiPlaneOverlay3: usize,
    pub DxgkDdiPostMultiPlaneOverlayPresent: usize,
    pub DxgkDdiValidateUpdateAllocationProperty: usize,
    pub DxgkDdiControlModeBehavior: usize,
    pub DxgkDdiUpdateMonitorLinkInfo: usize,
    pub DxgkDdiCreateHwContext: usize,
    pub DxgkDdiDestroyHwContext: usize,
    pub DxgkDdiCreateHwQueue: usize,
    pub DxgkDdiDestroyHwQueue: usize,
    pub DxgkDdiSubmitCommandToHwQueue: usize,
    pub DxgkDdiSwitchToHwContextList: usize,
    pub DxgkDdiResetHwEngine: usize,
    pub DxgkDdiCreatePeriodicFrameNotification: usize,
    pub DxgkDdiDestroyPeriodicFrameNotification: usize,
    pub DxgkDdiSetTimingsFromVidPn: usize,
    pub DxgkDdiSetTargetGamma: usize,
    pub DxgkDdiSetTargetContentType: usize,
    pub DxgkDdiSetTargetAnalogCopyProtection: usize,
    pub DxgkDdiSetTargetAdjustedColorimetry: usize,
    pub DxgkDdiDisplayDetectControl: usize,
    pub DxgkDdiQueryConnectionChange: usize,
    pub DxgkDdiExchangePreStartInfo: usize,
    pub DxgkDdiGetMultiPlaneOverlayCaps: usize,
    pub DxgkDdiGetPostCompositionCaps: usize,
    pub DxgkDdiUpdateHwContextState: usize,
    pub DxgkDdiCreateProtectedSession: usize,
    pub DxgkDdiDestroyProtectedSession: usize,
    pub DxgkDdiSetSchedulingLogBuffer: usize,
    pub DxgkDdiSetupPriorityBands: usize,
    pub DxgkDdiNotifyFocusPresent: usize,
    pub DxgkDdiSetContextSchedulingProperties: usize,
    pub DxgkDdiSuspendContext: usize,
    pub DxgkDdiResumeContext: usize,
    pub DxgkDdiSetVirtualMachineData: usize,
    pub DxgkDdiBeginExclusiveAccess: usize,
    pub DxgkDdiEndExclusiveAccess: usize,
    pub DxgkDdiQueryDiagnosticTypesSupport: usize,
    pub DxgkDdiControlDiagnosticReporting: usize,
    pub DxgkDdiResumeHwEngine: usize,
    pub DxgkDdiSignalMonitoredFence: usize,
    pub DxgkDdiPresentToHwQueue: usize,
    pub DxgkDdiValidateSubmitCommand: usize,
    pub DxgkDdiSetTargetAdjustedColorimetry2: usize,
    pub DxgkDdiSetTrackedWorkloadPowerLevel: usize,
    pub DxgkDdiSaveMemoryForHotUpdate: usize,
    pub DxgkDdiRestoreMemoryForHotUpdate: usize,
    pub DxgkDdiCollectDiagnosticInfo: usize,
    pub Reserved3: usize,
    pub DxgkDdiControlInterrupt3: usize,
    pub DxgkDdiSetFlipQueueLogBuffer: usize,
    pub DxgkDdiUpdateFlipQueueLog: usize,
    pub DxgkDdiCancelQueuedFlips: usize,
    pub DxgkDdiSetInterruptTargetPresentId: usize,
    pub DxgkDdiSetAllocationBackingStore: usize,
    pub DxgkDdiCreateCpuEvent: usize,
    pub DxgkDdiDestroyCpuEvent: usize,
    pub DxgkDdiCancelFlips: usize,
    pub DxgkDdiCreateNativeFence: usize,
    pub DxgkDdiDestroyNativeFence: usize,
    pub DxgkDdiUpdateMonitoredValues: usize,
    pub DxgkDdiUpdateCurrentValuesFromCpu: usize,
    pub DxgkDdiCreateDoorbell: usize,
    pub DxgkDdiConnectDoorbell: usize,
    pub DxgkDdiDisconnectDoorbell: usize,
    pub DxgkDdiDestroyDoorbell: usize,
    pub DxgkDdiNotifyWorkSubmission: usize,
    pub Reserved4: usize,
    pub DxgkDdiCreateMemoryBasis: usize,
    pub DxgkDdiDestroyMemoryBasis: usize,
    pub DxgkDdiStartDirtyTracking: usize,
    pub DxgkDdiStopDirtyTracking: usize,
    pub DxgkDdiQueryDirtyBitData: usize,
    pub DxgkDdiPrepareLiveMigration: usize,
    pub DxgkDdiSaveImmutableMigrationData: usize,
    pub DxgkDdiSaveMutableMigrationData: usize,
    pub DxgkDdiEndLiveMigration: usize,
    pub DxgkDdiRestoreImmutableMigrationData: usize,
    pub DxgkDdiRestoreMutableMigrationData: usize,
    pub DxgkDdiWriteVirtualizedInterrupt: usize,
    pub DxgkDdiSetVirtualGpuResources2: usize,
    pub DxgkDdiSetVirtualFunctionPauseState: usize,
    pub DxgkDdiOpenNativeFence: usize,
    pub DxgkDdiCloseNativeFence: usize,
    pub DxgkDdiSetNativeFenceLogBuffer: usize,
    pub DxgkDdiUpdateNativeFenceLogs: usize,
    pub DxgkDdiCollectDbgInfo2: usize,
    pub DxgkDdiNotifyContextPriorityChange: usize,
    pub DxgkDdiResetDisplayEngine: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMDDOD_INITIALIZATION_DATA {
    pub Version: u32,
    pub DxgkDdiAddDevice: usize,
    pub DxgkDdiStartDevice: usize,
    pub DxgkDdiStopDevice: usize,
    pub DxgkDdiRemoveDevice: usize,
    pub DxgkDdiDispatchIoRequest: usize,
    pub DxgkDdiInterruptRoutine: usize,
    pub DxgkDdiDpcRoutine: usize,
    pub DxgkDdiQueryChildRelations: usize,
    pub DxgkDdiQueryChildStatus: usize,
    pub DxgkDdiQueryDeviceDescriptor: usize,
    pub DxgkDdiSetPowerState: usize,
    pub DxgkDdiNotifyAcpiEvent: usize,
    pub DxgkDdiResetDevice: usize,
    pub DxgkDdiUnload: usize,
    pub DxgkDdiQueryInterface: usize,
    pub DxgkDdiControlEtwLogging: usize,
    pub DxgkDdiQueryAdapterInfo: usize,
    pub DxgkDdiSetPalette: usize,
    pub DxgkDdiSetPointerPosition: usize,
    pub DxgkDdiSetPointerShape: usize,
    pub DxgkDdiEscape: usize,
    pub DxgkDdiCollectDbgInfo: usize,
    pub DxgkDdiIsSupportedVidPn: usize,
    pub DxgkDdiRecommendFunctionalVidPn: usize,
    pub DxgkDdiEnumVidPnCofuncModality: usize,
    pub DxgkDdiSetVidPnSourceVisibility: usize,
    pub DxgkDdiCommitVidPn: usize,
    pub DxgkDdiUpdateActiveVidPnPresentPath: usize,
    pub DxgkDdiRecommendMonitorModes: usize,
    pub DxgkDdiGetScanLine: usize,
    pub DxgkDdiQueryVidPnHWCapability: usize,
    pub DxgkDdiPresentDisplayOnly: usize,
    pub DxgkDdiStopDeviceAndReleasePostDisplayOwnership: usize,
    pub DxgkDdiSystemDisplayEnable: usize,
    pub DxgkDdiSystemDisplayWrite: usize,
    pub DxgkDdiGetChildContainerId: usize,
    pub DxgkDdiControlInterrupt: usize,
    pub DxgkDdiSetPowerComponentFState: usize,
    pub DxgkDdiPowerRuntimeControlRequest: usize,
    pub DxgkDdiNotifySurpriseRemoval: usize,
    pub DxgkDdiPowerRuntimeSetDeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETGPUPARTITIONINFO {
    pub NumGpuPartitionOptions: u32,
    pub pGpuPartitionOptions: usize,
    pub CurrentGpuPartitionCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VGPU_PROFILE_CAPABILITY {
    pub TotalValue: u64,
    pub AvailableValue: u64,
    pub MinPartitionValue: u64,
    pub MaxPartitionValue: u64,
    pub OptimalPartitionValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETVIRTUALGPUPROFILE {
    pub PartitionCount: u32,
    pub VirtualizationFlags: u32,
    pub ProfileCapability: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUENGINE {
    pub EngineType: u32,
    pub Name: [u32; 1],
    pub NumPartitionUnits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETGPUENGINES {
    pub NumEngines: u32,
    pub EngineInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETGPUPARTITIONCOUNT {
    pub PartitionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUCAPABILITY {
    pub MinValue: u64,
    pub MaxValue: u64,
    pub OptimalValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUPROFILE {
    pub Capability: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUENGINEINFO {
    pub MinPartitionUnits: u32,
    pub MaxPartitionUnits: u32,
    pub EngineId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUSEGMENTINFO {
    pub DriverSegmentId: u32,
    pub Size: u64,
    pub Alignment: u32,
    pub MinSegmentOffset: u64,
    pub MaxSegmentOffset: u64,
    pub PrivateDriverData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEVIRTUALGPU {
    pub PartitionId: u32,
    pub Profile: u32,
    pub UserModeVirtualDeviceProvider: u32,
    pub VirtualGpuLuid: u32,
    pub NumMemorySegments: u32,
    pub SegmentInfo: [u32; 1],
    pub NumEngines: u32,
    pub EngineInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VGPU_CAPABILITY {
    pub MinValue: u64,
    pub MaxValue: u64,
    pub CurrentValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETVIRTUALGPUINFO {
    pub PartitionId: u32,
    pub Capability: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALGPUVMBUS {
    pub VirtualGpuIndex: u32,
    pub VmBusHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPU_PHYSICAL_ADDRESS {
    pub MemorySegmentId: u32,
    pub MemorySegmentOffset: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUMEMORYRESOURCE {
    pub DriverAllocationHandle: usize,
    pub AllocationAddress: u32,
    pub AllocationSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALGPURESOURCES {
    pub PartitionId: u32,
    pub NumMemoryAllocations: u32,
    pub MemoryInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESTROYVIRTUALGPU {
    pub PartitionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SUSPENDVIRTUALGPU {
    pub PartitionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESUMEVIRTUALGPU {
    pub PartitionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALGPUDRIVERESCAPE {
    pub PartitionId: u32,
    pub InputBufferSize: u32,
    pub OutputBufferSize: u32,
    pub pInputBuffer: u32,
    pub pOutputBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_GPU_PARTITION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiGetGpuPartitionInfo: usize,
    pub DxgkDdiSetGpuPartitionCount: usize,
    pub DxgkDdiGetGpuEngines: usize,
    pub DxgkDdiGetVirtualGpuProfile: usize,
    pub DxgkDdiCreateVirtualGpu: usize,
    pub DxgkDdiGetVirtualGpuInfo: usize,
    pub DxgkDdiSetVirtualGpuResources: usize,
    pub DxgkDdiDestroyVirtualGpu: usize,
    pub DxgkDdiSuspendVirtualGpu: usize,
    pub DxgkDdiResumeVirtualGpu: usize,
    pub DxgkDdiVirtualGpuDriverEscape: usize,
    pub DxgkDdiSetVirtualGpuVmBus: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_READVIRTUALFUNCTIONCONFIG {
    pub Data: u32,
    pub VirtualFunctionIndex: u32,
    pub Offset: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_WRITEVIRTUALFUNCTIONCONFIG {
    pub Data: u32,
    pub VirtualFunctionIndex: u32,
    pub Offset: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_READVIRTUALFUNCTIONCONFIGBLOCK {
    pub Data: u32,
    pub VirtualFunctionIndex: u32,
    pub BlockId: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_WRITEVIRTUALFUNCTIONCONFIGBLOCK {
    pub Data: u32,
    pub VirtualFunctionIndex: u32,
    pub BlockId: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYPROBEDBARS {
    pub VirtualFunctionIndex: u32,
    pub BaseRegisterValues: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYVIRTUALFUNCTIONLUID {
    pub VirtualFunctionIndex: u32,
    pub pLuid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETVENDORANDDEVICE {
    pub VirtualFunctionIndex: u32,
    pub VendorId: u16,
    pub DeviceId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETDEVICELOCATION {
    pub VirtualFunctionIndex: u32,
    pub SegmentNumber: u32,
    pub BusNumber: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESETVIRTUALFUNCTION {
    pub VirtualFunctionIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALFUNCTIONPOWERSTATE {
    pub VirtualFunctionIndex: u32,
    pub PowerState: u32,
    pub Wake: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETRESOURCEFORBAR {
    pub VirtualFunctionIndex: u32,
    pub BarIndex: u32,
    pub pResource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_SRIOV_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiReadVirtualFunctionConfig: usize,
    pub DxgkDdiWriteVirtualFunctionConfig: usize,
    pub DxgkDdiReadVirtualFunctionConfigBlock: usize,
    pub DxgkDdiWriteVirtualFunctionConfigBlock: usize,
    pub DxgkDdiQueryProbedBars: usize,
    pub DxgkDdiGetVendorAndDevice: usize,
    pub DxgkDdiGetDeviceLocation: usize,
    pub DxgkDdiResetVirtualFunction: usize,
    pub DxgkDdiSetVirtualFunctionPowerState: usize,
    pub DxgkDdiGetResourceForBar: usize,
    pub DxgkDdiQueryVirtualFunctionLuid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYMITIGATEDRANGECOUNT {
    pub VirtualFunctionIndex: u32,
    pub RangeCount: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MITIGATEDRANGEINFO {
    pub BasePageNumber: u32,
    pub PageCount: u32,
    pub InterceptReads: u8,
    pub InterceptWrites: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYMITIGATEDRANGES {
    pub VirtualFunctionIndex: u32,
    pub BarIndex: u32,
    pub NumRanges: u32,
    pub pMitigatedRange: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_MITIGABLE_DEVICE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiQueryMitigatedRangeCount: usize,
    pub DxgkDdiQueryMitigatedRanges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETBACKINGRESOURCE {
    pub VirtualFunctionIndex: u32,
    pub ResourceIndex: usize,
    pub Resource: usize,
    pub pMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETMMIORANGECOUNT {
    pub VirtualFunctionIndex: u32,
    pub RangeCount: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MMIORANGEINFO {
    pub BasePageNumber: u32,
    pub BasePhysicalPageNumber: u32,
    pub BasePhysicalResourceNumber: u8,
    pub InterceptReads: u8,
    pub InterceptWrites: u8,
    pub PageCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETMMIORANGES {
    pub VirtualFunctionIndex: u32,
    pub BarIndex: u32,
    pub NumRanges: u32,
    pub pMmioRanges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_FLEXIOV_DEVICE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiGetBackingResource: usize,
    pub DxgkDdiGetMmioRangeCount: usize,
    pub DxgkDdiGetMmioRanges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DSI_CAPS {
    pub DSITypeMajor: u8,
    pub DSITypeMinor: u8,
    pub SpecVersionMajor: u8,
    pub SpecVersionMinor: u8,
    pub SpecVersionPatch: u8,
    pub TargetMaximumReturnPacketSize: u16,
    pub ResultCodeFlags: u8,
    pub ResultCodeStatus: u8,
    pub Revision: u8,
    pub Level: u8,
    pub DeviceClassHi: u8,
    pub DeviceClassLo: u8,
    pub ManufacturerHi: u8,
    pub ManufacturerLo: u8,
    pub ProductHi: u8,
    pub ProductLo: u8,
    pub LengthHi: u8,
    pub LengthLo: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DSI_PACKET {
    pub DataId: u8,
    pub DataType: u8,
    pub VirtualChannel: u8,
    pub Data0: u8,
    pub Data1: u8,
    pub LongWriteWordCount: u16,
    pub EccFiller: u8,
    pub Payload: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DSI_TRANSMISSION {
    pub TotalBufferSize: u32,
    pub PacketCount: u8,
    pub FailedPacket: u8,
    pub TransmissionMode: u16,
    pub ReportMipiErrors: u16,
    pub ClearMipiErrors: u16,
    pub SecondaryPort: u16,
    pub ManufacturingMode: u16,
    pub Reserved: u16,
    pub ReadWordCount: u16,
    pub FinalCommandExtraPayload: u16,
    pub MipiErrors: u16,
    pub HostErrors: u16,
    pub Packets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DSI_RESET {
    pub Flags: u32,
    pub MipiErrors: u32,
    pub ResetFailed: u32,
    pub NeedModeSet: u32,
    pub Results: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MIPI_DSI_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiDsiCaps: usize,
    pub DxgkDdiDsiTransmission: usize,
    pub DxgkDdiDsiReset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAYSTATE_NONINTRUSIVE {
    pub VidPnTargetId: u32,
    pub DisplayConnectivity: u32,
    pub DisplayLidState: u32,
    pub DisplayTopology: u32,
    pub DisplayLinkState: u32,
    pub DisplayModeSet: u32,
    pub ReturnSubStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETDISPLAYSTATENONINTRUSIVE {
    pub NumOfTargets: u32,
    pub SizeOfDisplayStateNonIntrusiveElement: u32,
    pub ppDisplayStateNonIntrusive: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAG_DISPLAY_SAMPLED_GAMMA {
    pub Red: [u32; 1],
    pub Green: [u32; 1],
    pub Blue: [u32; 1],
    pub ColorMatrix: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAG_DISPLAY_SCANOUT_BUFFER_HISTOGRAM {
    pub MinPixelValue: i32,
    pub MaxPixelValue: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIAG_SCANOUT_BUFFER_CONTENT {
    pub ScanoutBufferCrc: u32,
    pub ScanoutBufferHistogram: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAYSTATE_INTRUSIVE {
    pub VidPnTargetId: u32,
    pub MonitorState: u32,
    pub DisplayScanoutState: u32,
    pub DisplaySampledGamma: u32,
    pub DisplayBufferContent: u32,
    pub DisplayErrorState: u32,
    pub DisplayBandwidth: u32,
    pub ReturnSubStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETDISPLAYSTATEINTRUSIVE {
    pub NumOfTargets: u32,
    pub SizeOfDisplayStateIntrusiveElement: u32,
    pub ppDisplayStateIntrusive: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAY_DIAGNOSTICS_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiGetDisplayStateNonIntrusive: usize,
    pub DxgkDdiGetDisplayStateIntrusive: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYDPCAPS {
    pub NumRootPorts: u32,
    pub DPVersionMajor: u8,
    pub DPVersionMinor: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETDPADDRESS {
    pub TargetId: u32,
    pub DPNativeError: u32,
    pub RootPortIndex: u32,
    pub NumLinks: u8,
    pub RelAddress: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DPAUXIOTRANSMISSION {
    pub Write: u32,
    pub CanUseCachedData: u32,
    pub Reserved: u32,
    pub RootPortIndex: u32,
    pub DPCDAddress: u32,
    pub NumBytesRequested: u8,
    pub DPNativeError: u32,
    pub NumBytesDone: u8,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DPI2CIOTRANSMISSION {
    pub Read: u32,
    pub Write: u32,
    pub EDDCMode: u32,
    pub OffsetSizeInBytes: u32,
    pub CanUseCachedData: u32,
    pub Reserved: u32,
    pub RootPortIndex: u32,
    pub I2CAddress: u32,
    pub WordOffset: u32,
    pub SegmentPointer: u32,
    pub Reserved1: u32,
    pub Offset: u32,
    pub BufferSizeSupplied: u32,
    pub BytesToWrite: u32,
    pub BytesToRead: u32,
    pub DPNativeError: u32,
    pub BytesWritten: u32,
    pub BytesRead: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DPSBMTRANSMISSION {
    pub CanUseCachedData: u32,
    pub Reserved: u32,
    pub RootPortIndex: u32,
    pub BufferSizeSupplied: u32,
    pub RequestLength: u32,
    pub MaxReplyLength: u32,
    pub DPNativeError: u32,
    pub ActualReplyLength: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DP_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiQueryDPCaps: usize,
    pub DxgkDdiGetDPAddress: usize,
    pub DxgkDdiDPAuxIoTransmission: usize,
    pub DxgkDdiDPI2CIoTransmission: usize,
    pub DxgkDdiDPSBMTransmission: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_KERNEL_EVENT {
    pub bReserved: u8,
    pub cbStruct: u8,
    pub cbEvent: u16,
    pub usChannelGroup: u16,
    pub usFlags: u16,
    pub ullPresTime100ns: u32,
    pub ullBytePosition: u64,
    pub field_pNextEvt: u32,
    pub abData: [u8; 1],
    pub pbData: *mut u8,
    pub field_pPackageEvt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTH_BUFFER {
    pub BufferSize: u32,
    pub BufferAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTHDOWNLOAD {
    pub DownloadHandle: usize,
    pub Free: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTHCAPS {
    pub Guid: u32,
    pub Flags: u32,
    pub MemorySize: u32,
    pub MaxChannelGroups: u32,
    pub MaxVoices: u32,
    pub MaxAudioChannels: u32,
    pub EffectFlags: u32,
    pub Description: [u32; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTH_PORTPARAMS {
    pub ValidParams: u32,
    pub Voices: u32,
    pub ChannelGroups: u32,
    pub AudioChannels: u32,
    pub SampleRate: u32,
    pub EffectsFlags: u32,
    pub Share: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTHVOICEPRIORITY_INSTANCE {
    pub ChannelGroup: u32,
    pub Channel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTH_STATS {
    pub ValidStats: u32,
    pub Voices: u32,
    pub TotalCPU: u32,
    pub CPUPerVoice: u32,
    pub LostNotes: u32,
    pub FreeMemory: u32,
    pub PeakVolume: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNTH_REVERB_PARAMS {
    pub fInGain: u32,
    pub fReverbMix: u32,
    pub fReverbTime: u32,
    pub fHighFreqRTRatio: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_MAC_ADDRESS {
    pub Address: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_MESSAGE_HEADER {
    pub PortId: u32,
    pub Reserved: u32,
    pub Status: u32,
    pub TransactionId: u32,
    pub IhvSpecificId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_P2P_SERVICE_NAME_HASH {
    pub Hash: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TXRX_PEER_CFG {
    pub PeerQoSConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TX_COMPLETE_DATA {
    pub SeqCtl: u32,
    pub PnLength: u32,
    pub RetryCount: u32,
    pub wPad: u32,
    pub MpduPN: u32,
    pub ReplayIHVReserved0: u64,
    pub ReplayIHVReserved1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TXRX_CAPABILITIES {
    pub InterconnectType: u32,
    pub TargetPriorityQueueing: u8,
    pub MaxScatterGatherElementsPerFrame: u32,
    pub ExplicitSendCompleteFlagRequired: u8,
    pub bPad: u32,
    pub MinEffectiveSize: u32,
    pub FrameSizeGranularity: u32,
    pub RxTxForwarding: u8,
    pub MaxThroughput: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TXRX_PARAMETERS {
    pub TxRxCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TXRX_TARGET_CONFIGURATION {
    pub TxRxParams: u32,
    pub MaxNumPorts: u32,
    pub MaxNumPeers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAL_TXRX_PARAMETERS {
    pub MaxOutstandingTransfers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_TX_METADATA {
    pub PortID: u32,
    pub PeerID: u32,
    pub ExTID: u32,
    pub IsUnicast: u8,
    pub bAllowLegacyRates: u8,
    pub Ethertype: u32,
    pub bTxCompleteRequired: u8,
    pub PnLength: u32,
    pub TxCost: u32,
    pub ExemptionAction: u32,
    pub MpduPn: u32,
    pub ReplayIHVReserved0: u64,
    pub ReplayIHVReserved1: u64,
    pub SeqCtl: u32,
    pub wPad: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_RX_METADATA {
    pub PayloadType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WDI_FRAME_METADATA {
    pub Linkage: u32,
    pub pNBL: usize,
    pub FrameID: u32,
    pub wPad: u32,
    pub dPad: u32,
    pub txMetaData: u32,
    pub rxMetaData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WDI_DATA_API {
    pub Header: u32,
    pub TxDequeueIndication: u32,
    pub TxTransferCompleteIndication: u32,
    pub TxSendCompleteIndication: u32,
    pub TxQueryRATIDState: u32,
    pub TxSendPauseIndication: u32,
    pub TxSendRestartIndication: u32,
    pub TxReleaseFrameIndication: u32,
    pub TxInjectFrameIndication: u32,
    pub TxAbortConfirm: u32,
    pub RxInorderDataIndication: u32,
    pub RxStopConfirm: u32,
    pub RxFlushConfirm: u32,
    pub PeerCreateIndication: u32,
    pub PeerDeleteIndication: u32,
    pub AllocateWiFiFrameMetaData: u32,
    pub FreeWiFiFrameMetaData: u32,
    pub TxQuerySuspectFrameCompleteStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_WDI_DATA_HANDLERS {
    pub Header: u32,
    pub TxAbortHandler: u32,
    pub TxTargetDescInitHandler: u32,
    pub TxTargetDescDeInitHandler: u32,
    pub TxDataSendHandler: u32,
    pub TxTalSendHandler: u32,
    pub TxTalSendCompleteHandler: u32,
    pub TxTalQueueInOrderHandler: u32,
    pub TxPeerBacklogHandler: u32,
    pub RxStopHandler: u32,
    pub RxFlushHandler: u32,
    pub RxRestartHandler: u32,
    pub RxGetMpdusHandler: u32,
    pub RxReturnFramesHandler: u32,
    pub RxResumeHandler: u32,
    pub RxThrottleHandler: u32,
    pub RxPpduRssiHandler: u32,
    pub TalTxRxStartHandler: u32,
    pub TalTxRxStopHandler: u32,
    pub TalTxRxAddPortHandler: u32,
    pub TalTxRxDeletePortHandler: u32,
    pub TalTxRxSetPortOpModeHandler: u32,
    pub TalTxRxResetPortHandler: u32,
    pub TalTxRxPeerConfigHandler: u32,
    pub TalTxRxPeerDeleteConfirmHandler: u32,
    pub TxSuspectFrameAbortHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WDI_INIT_PARAMETERS {
    pub Header: u32,
    pub WdiVersion: u32,
    pub OpenAdapterCompleteHandler: u32,
    pub CloseAdapterCompleteHandler: u32,
    pub UeIdleNotificationConfirm: u32,
    pub UeIdleNotificationComplete: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_DRIVER_WDI_CHARACTERISTICS {
    pub Header: u32,
    pub WdiVersion: u32,
    pub AllocateAdapterHandler: u32,
    pub FreeAdapterHandler: u32,
    pub OpenAdapterHandler: u32,
    pub CloseAdapterHandler: u32,
    pub StartOperationHandler: u32,
    pub StopOperationHandler: u32,
    pub PostPauseHandler: u32,
    pub PostRestartHandler: u32,
    pub HangDiagnoseHandler: u32,
    pub TalTxRxInitializeHandler: u32,
    pub TalTxRxDeinitializeHandler: u32,
    pub LeIdleNotificationHandler: u32,
    pub LeCancelIdleNotificationHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MLO_LINK_INFO {
    pub LinkID: u32,
    pub StaLinkAddress: u32,
    pub APLinkAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_IDS {
    pub Count: u32,
    pub IdList: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_WITH_BUFFERS {
    pub ScsiPassThrough: u32,
    pub SenseInfoBuffer: [usize; 1],
    pub DataBuffer: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_DIRECT_WITH_BUFFER {
    pub ScsiPassThroughDirect: u32,
    pub SenseInfoBuffer: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTROLLER_IDS {
    pub Type: u32,
    pub Length: u32,
    pub SerialNumber: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTROLLER_INFO {
    pub DeviceObject: usize,
    pub Identifier: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_COMPLETION_INFO {
    pub DsmCompletionRoutine: u32,
    pub DsmContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_WMILIB_CONTEXT {
    pub GuidCount: u32,
    pub GuidList: usize,
    pub RegistryPath: u32,
    pub MofResourceName: u32,
    pub QueryWmiDataBlock: u32,
    pub QueryWmiDataBlockEx: u32,
    pub SetWmiDataBlock: u32,
    pub SetWmiDataBlockEx: u32,
    pub ExecuteWmiMethod: u32,
    pub ExecuteWmiMethodEx: u32,
    pub WmiFunctionControl: u32,
    pub WmiFunctionControlEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_VERSION_INFO {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub ProductBuild: u32,
    pub QfeNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_INIT_DATA {
    pub InitDataSize: u32,
    pub DsmInquireDriver: u32,
    pub DsmCompareDevices: u32,
    pub DsmDeviceSerialNumber: u32,
    pub DsmGetControllerInfo: u32,
    pub DsmSetDeviceInfo: u32,
    pub DsmIsPathActive: u32,
    pub DsmPathVerify: u32,
    pub DsmInvalidatePath: u32,
    pub DsmMoveDevice: u32,
    pub DsmRemovePending: u32,
    pub DsmRemoveDevice: u32,
    pub DsmRemovePath: u32,
    pub DsmSrbDeviceControl: u32,
    pub DsmLBGetPath: u32,
    pub DsmInterpretError: u32,
    pub DsmInterpretErrorEx: u32,
    pub DsmUnload: u32,
    pub DsmSetCompletion: u32,
    pub DsmCategorizeRequest: u32,
    pub DsmBroadcastSrb: u32,
    pub DsmWmiInfo: u32,
    pub DsmContext: usize,
    pub DriverObject: usize,
    pub DisplayName: u32,
    pub Reserved: u32,
    pub DsmType: u32,
    pub DsmVersion: u32,
    pub DsmWmiGlobalInfo: u32,
    pub DsmIsAddressTypeSupported: u32,
    pub DsmDeviceNotUsed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_VERSION_INFO {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub ProductBuild: u32,
    pub QfeNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_MPIO_CONTEXT {
    pub MPIOContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_DEREGISTER_DATA {
    pub DeregisterDataSize: u32,
    pub DsmContext: usize,
    pub DriverObject: usize,
    pub MpioContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDDETECTOR_PATTERNHEADER {
    pub Size: u32,
    pub PatternType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DETECTIONEVENT {
    pub EventId: u32,
    pub EventFeat: u32,
    pub ProviderId: u32,
    pub DisplayName: [u32; 100],
    pub UserModelValid: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DETECTIONEVENTSELECTOR {
    pub Event: u32,
    pub LangId: u32,
    pub UserId: u32,
    pub Armed: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENTACTION {
    pub EventdActionType: u32,
    pub EventActionContextType: u32,
    pub Confidence: u32,
    pub Scale: u32,
    pub ErrorCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRV_CALL {
    pub spacer: u32,
    pub UpperFinalizationDone: u8,
    pub PrefixEntry: u32,
    pub Condition: u32,
    pub SerialNumberForEnum: u32,
    pub NumberOfCloseDelayedFiles: u32,
    pub TransitionWaitList: u32,
    pub ScavengerFinalizationList: u32,
    pub PurgeSyncronizationContext: usize,
    pub BufferingManager: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ROOT {
    pub spacer: u32,
    pub SrvCall: usize,
    pub UpperFinalizationDone: u8,
    pub Condition: u32,
    pub TransitionWaitList: u32,
    pub ScavengerFinalizationList: u32,
    pub PurgeSyncronizationContext: usize,
    pub DefaultVNetRoot: usize,
    pub VirtualNetRoots: u32,
    pub NumberOfVirtualNetRoots: u32,
    pub SerialNumberForEnum: u32,
    pub PrefixEntry: u32,
    pub FcbTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct V_NET_ROOT {
    pub spacer: u32,
    pub NetRoot: usize,
    pub UpperFinalizationDone: u8,
    pub ConnectionFinalizationDone: u8,
    pub Condition: u32,
    pub AdditionalReferenceForDeleteFsctlTaken: u32,
    pub PrefixEntry: u32,
    pub NamePrefix: u32,
    pub PrefixOffsetInBytes: u32,
    pub NetRootListEntry: u32,
    pub SerialNumberForEnum: u32,
    pub TransitionWaitList: u32,
    pub ScavengerFinalizationList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NON_PAGED_FCB {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub SectionObjectPointers: u32,
    pub HeaderResource: u32,
    pub PagingIoResource: u32,
    pub FileSizeLock: u32,
    pub TransitionWaitList: u32,
    pub OutstandingAsyncWrites: u32,
    pub OutstandingAsyncEvent: usize,
    pub TheActualEvent: u32,
    pub MiniRdrContext: [usize; 2],
    pub AdvancedFcbHeaderMutex: u32,
    pub BufferedLocksResource: u32,
    pub FcbBackPointer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCB_LOCK {
    pub field_Next: u32,
    pub Length: i64,
    pub BytesOffset: i64,
    pub Key: u32,
    pub ExclusiveLock: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCB_BUFFERED_LOCKS {
    pub field_List: u32,
    pub PendingLockOps: u32,
    pub Resource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCB {
    pub spacer: u32,
    pub NetRoot: usize,
    pub VNetRoot: usize,
    pub NonPaged: usize,
    pub ScavengerFinalizationList: u32,
    pub pBufferingStateChangeCompletedEvent: usize,
    pub NumberOfBufferingStateChangeWaiters: i32,
    pub FcbTableEntry: u32,
    pub PrivateAlreadyPrefixedName: u32,
    pub UpperFinalizationDone: u8,
    pub Condition: u32,
    pub PrivateDispatchVector: usize,
    pub RxDeviceObject: usize,
    pub MRxDispatch: usize,
    pub MRxFastIoDispatch: usize,
    pub InternalSrvOpen: usize,
    pub InternalFobx: usize,
    pub ShareAccess: u32,
    pub ShareAccessPerSrvOpens: u32,
    pub NumberOfLinks: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub LastChangeTime: i64,
    pub ulFileSizeVersion: u32,
    pub FileLock: u32,
    pub LowIoPerFcbInfo: u32,
    pub FileSizeLock: usize,
    pub EaModificationCount: u32,
    pub BufferedLocks: u32,
    pub CopyOfNonPaged: usize,
    pub FcbAcquires: [u32; 1],
    pub FcbReleases: [u32; 1],
    pub PagingIoResourceFile: *mut i8,
    pub PagingIoResourceLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FCB_INIT_PACKET {
    pub pAttributes: *mut u32,
    pub pNumLinks: *mut u32,
    pub pCreationTime: *mut i64,
    pub pLastAccessTime: *mut i64,
    pub pLastWriteTime: *mut i64,
    pub pLastChangeTime: *mut i64,
    pub pAllocationSize: *mut i64,
    pub pFileSize: *mut i64,
    pub pValidDataLength: *mut i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRV_OPEN {
    pub spacer: u32,
    pub Fcb: usize,
    pub VNetRoot: usize,
    pub UpperFinalizationDone: u8,
    pub Condition: u32,
    pub BufferingToken: u32,
    pub ScavengerFinalizationList: u32,
    pub TransitionWaitList: u32,
    pub FobxList: u32,
    pub InternalFobx: usize,
    pub SrvOpenKeyList: u32,
    pub SequenceNumber: u32,
    pub OpenStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FOBX {
    pub spacer: u32,
    pub SrvOpen: usize,
    pub FobxSerialNumber: u32,
    pub FobxQLinks: u32,
    pub ScavengerFinalizationList: u32,
    pub ClosePendingList: u32,
    pub CloseTime: i64,
    pub UpperFinalizationDone: u8,
    pub ContainsWildCards: u8,
    pub fOpenCountDecremented: u8,
    pub PipeHandleInformation: u32,
    pub CollectDataTime: i64,
    pub CollectDataSize: u32,
    pub ThrottlingState: u32,
    pub ReadSerializationQueue: u32,
    pub WriteSerializationQueue: u32,
    pub PredictedReadOffset: u32,
    pub PredictedWriteOffset: u32,
    pub LockThrottlingState: u32,
    pub LastLockOffset: i64,
    pub LastLockRange: i64,
    pub RxDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_FCB_TABLE_ENTRY {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub HashValue: u32,
    pub Path: u32,
    pub HashLinks: u32,
    pub Lookups: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_FCB_TABLE {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub Version: u32,
    pub CaseInsensitiveMatch: u8,
    pub NumberOfBuckets: u16,
    pub Lookups: u32,
    pub FailedLookups: u32,
    pub Compares: u32,
    pub TableLock: u32,
    pub TableEntryForNull: usize,
    pub HashBuckets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_IO_PARAMETER_BLOCK {
    pub IrpFlags: u32,
    pub MajorFunction: u8,
    pub MinorFunction: u8,
    pub OperationFlags: u8,
    pub Reserved: u8,
    pub TargetFileObject: usize,
    pub TargetInstance: usize,
    pub Parameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_CALLBACK_DATA {
    pub Flags: usize,
    pub Thread: usize,
    pub Iopb: usize,
    pub IoStatus: u32,
    pub field_TagData: u32,
    pub QueueLinks: u32,
    pub QueueContext: [usize; 2],
    pub FilterContext: [usize; 4],
    pub RequestorMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_RELATED_OBJECTS {
    pub Size: u32,
    pub TransactionContext: u32,
    pub Filter: usize,
    pub Volume: usize,
    pub Instance: usize,
    pub FileObject: usize,
    pub Transaction: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_RELATED_CONTEXTS {
    pub VolumeContext: usize,
    pub InstanceContext: usize,
    pub FileContext: usize,
    pub StreamContext: usize,
    pub StreamHandleContext: usize,
    pub TransactionContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_RELATED_CONTEXTS_EX {
    pub VolumeContext: usize,
    pub InstanceContext: usize,
    pub FileContext: usize,
    pub StreamContext: usize,
    pub StreamHandleContext: usize,
    pub TransactionContext: usize,
    pub SectionContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_CONTEXT_REGISTRATION {
    pub ContextType: u32,
    pub Flags: u32,
    pub ContextCleanupCallback: usize,
    pub Size: usize,
    pub PoolTag: u32,
    pub ContextAllocateCallback: usize,
    pub ContextFreeCallback: usize,
    pub Reserved1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_OPERATION_REGISTRATION {
    pub MajorFunction: u8,
    pub Flags: u32,
    pub PreOperation: usize,
    pub PostOperation: usize,
    pub Reserved1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_TAG_DATA_BUFFER {
    pub FileTag: u32,
    pub TagDataLength: u16,
    pub UnparsedNameLength: u16,
    pub SubstituteNameOffset: u16,
    pub SubstituteNameLength: u16,
    pub PrintNameOffset: u16,
    pub PrintNameLength: u16,
    pub Flags: u32,
    pub PathBuffer: [u32; 1],
    pub SubstituteNameOffset_1: u16,
    pub SubstituteNameLength_1: u16,
    pub PrintNameOffset_1: u16,
    pub PrintNameLength_1: u16,
    pub PathBuffer_1: [u32; 1],
    pub DataBuffer: [u8; 1],
    pub TagGuid: u32,
    pub DataBuffer_1: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_NAME_CONTROL {
    pub Name: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_REGISTRATION {
    pub Size: u16,
    pub Version: u16,
    pub Flags: u32,
    pub field_ContextRegistration: u32,
    pub field_OperationRegistration: u32,
    pub FilterUnloadCallback: usize,
    pub InstanceSetupCallback: usize,
    pub InstanceQueryTeardownCallback: usize,
    pub InstanceTeardownStartCallback: usize,
    pub InstanceTeardownCompleteCallback: usize,
    pub GenerateFileNameCallback: usize,
    pub NormalizeNameComponentCallback: usize,
    pub NormalizeContextCleanupCallback: usize,
    pub TransactionNotificationCallback: usize,
    pub NormalizeNameComponentExCallback: usize,
    pub SectionNotificationCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_FILE_NAME_INFORMATION {
    pub Size: u16,
    pub NamesParsed: u32,
    pub Format: u32,
    pub Name: u32,
    pub Volume: u32,
    pub Share: u32,
    pub Extension: u32,
    pub Stream: u32,
    pub FinalComponent: u32,
    pub ParentDir: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_CREATEFILE_TARGET_ECP_CONTEXT {
    pub Instance: usize,
    pub Volume: usize,
    pub FileNameInformation: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_VOLUME_PROPERTIES {
    pub DeviceType: u32,
    pub DeviceCharacteristics: u32,
    pub DeviceObjectFlags: u32,
    pub AlignmentRequirement: u32,
    pub SectorSize: u16,
    pub Flags: u16,
    pub FileSystemDriverName: u32,
    pub FileSystemDeviceName: u32,
    pub RealDeviceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLT_CALLBACK_DATA_QUEUE {
    pub Csq: u32,
    pub Flags: usize,
    pub Instance: usize,
    pub InsertIo: usize,
    pub RemoveIo: usize,
    pub PeekNextIo: usize,
    pub Acquire: usize,
    pub Release: usize,
    pub CompleteCanceledIo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FWPS_CONNECT_REQUEST0 {
    pub localAddressAndPort: u32,
    pub remoteAddressAndPort: u32,
    pub portReservationToken: u32,
    pub localRedirectTargetPID: u32,
    pub previousVersion: usize,
    pub modifierFilterId: u64,
    pub localRedirectHandle: u32,
    pub localRedirectContext: usize,
    pub localRedirectContextSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FWPS_BIND_REQUEST0 {
    pub localAddressAndPort: u32,
    pub portReservationToken: u32,
    pub previousVersion: usize,
    pub modifierFilterId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FWPS_CONNECTION_POLICY0 {
    pub localAddress: u32,
    pub nextHopAddress: u32,
    pub interfaceLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_CONTROLLER_BASIC_INFORMATION {
    pub Version: u16,
    pub Size: u16,
    pub TotalPins: u16,
    pub NumberOfPinsPerBank: u8,
    pub DeviceIdleTimeout: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_CONTROLLER_QUERY_SET_INFORMATION_INPUT {
    pub RequestType: u32,
    pub Size: u16,
    pub Flags: u32,
    pub BankId: u32,
    pub ResourcesTranslated: u32,
    pub ResourcesRaw: u32,
    pub TotalBanks: u16,
    pub InputBuffer: usize,
    pub InputBufferSize: usize,
    pub OutputBufferSize: usize,
    pub TotalBanks_1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_QUERY_BANK_POWER_INFORMATION_OUTPUT {
    pub F1StateSupported: u16,
    pub Reserved: u16,
    pub F1IdleStateParameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_CONTROLLER_QUERY_SET_INFORMATION_OUTPUT {
    pub Version: u16,
    pub Size: u16,
    pub BankPowerInformation: u32,
    pub ResourceMapping: [u32; 1],
    pub Mapping: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_ENABLE_INTERRUPT_PARAMETERS {
    pub BankId: u32,
    pub PinNumber: usize,
    pub Flags: u32,
    pub InterruptMode: u32,
    pub Polarity: u32,
    pub PullConfiguration: u8,
    pub DebounceTimeout: u16,
    pub VendorData: usize,
    pub VendorDataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_DISABLE_INTERRUPT_PARAMETERS {
    pub BankId: u32,
    pub PinNumber: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_QUERY_ENABLED_INTERRUPTS_PARAMETERS {
    pub BankId: u32,
    pub EnabledMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_MASK_INTERRUPT_PARAMETERS {
    pub BankId: u32,
    pub PinMask: u32,
    pub FailedMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_QUERY_ACTIVE_INTERRUPTS_PARAMETERS {
    pub BankId: u32,
    pub EnabledMask: u32,
    pub ActiveMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_CLEAR_ACTIVE_INTERRUPTS_PARAMETERS {
    pub BankId: u32,
    pub ClearActiveMask: u32,
    pub FailedClearMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_RECONFIGURE_INTERRUPTS_PARAMETERS {
    pub BankId: u32,
    pub PinNumber: usize,
    pub InterruptMode: u32,
    pub Polarity: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_CONNECT_FUNCTION_CONFIG_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u16,
    pub FunctionNumber: u32,
    pub PullConfiguration: u8,
    pub VendorData: usize,
    pub VendorDataLength: u32,
    pub ConnectFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_DISCONNECT_FUNCTION_CONFIG_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u32,
    pub DisconnectFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_CONNECT_IO_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u16,
    pub ConnectMode: u32,
    pub PullConfiguration: u8,
    pub DebounceTimeout: u16,
    pub DriveStrength: u16,
    pub VendorData: usize,
    pub VendorDataLength: u32,
    pub ConnectFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_DISCONNECT_IO_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u32,
    pub DisconnectMode: u32,
    pub DisconnectFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_READ_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u32,
    pub Buffer: usize,
    pub Flags: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_READ_PINS_MASK_PARAMETERS {
    pub BankId: u32,
    pub PinValues: usize,
    pub Flags: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_WRITE_PINS_PARAMETERS {
    pub BankId: u32,
    pub PinNumberTable: u32,
    pub PinCount: u32,
    pub Buffer: usize,
    pub Flags: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_WRITE_PINS_MASK_PARAMETERS {
    pub BankId: u32,
    pub SetMask: u32,
    pub ClearMask: u32,
    pub Flags: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_SAVE_RESTORE_BANK_HARDWARE_CONTEXT_PARAMETERS {
    pub BankId: u32,
    pub State: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_CLIENT_CONTROLLER_SPECIFIC_FUNCTION_PARAMETERS {
    pub InputBuffer: usize,
    pub InputBufferLength: usize,
    pub OutputBuffer: usize,
    pub OutputBufferLength: usize,
    pub BytesWritten: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPIO_CLIENT_REGISTRATION_PACKET {
    pub Version: u16,
    pub Size: u16,
    pub Flags: u32,
    pub ControllerContextSize: u32,
    pub Reserved: u32,
    pub CLIENT_PrepareController: usize,
    pub CLIENT_ReleaseController: usize,
    pub CLIENT_StartController: usize,
    pub CLIENT_StopController: usize,
    pub CLIENT_QueryControllerBasicInformation: usize,
    pub CLIENT_QuerySetControllerInformation: usize,
    pub CLIENT_EnableInterrupt: usize,
    pub CLIENT_DisableInterrupt: usize,
    pub CLIENT_UnmaskInterrupt: usize,
    pub CLIENT_MaskInterrupts: usize,
    pub CLIENT_QueryActiveInterrupts: usize,
    pub CLIENT_ClearActiveInterrupts: usize,
    pub CLIENT_ConnectIoPins: usize,
    pub CLIENT_DisconnectIoPins: usize,
    pub CLIENT_ReadGpioPins: usize,
    pub CLIENT_ReadGpioPinsUsingMask: usize,
    pub CLIENT_WriteGpioPins: usize,
    pub CLIENT_WriteGpioPinsUsingMask: usize,
    pub CLIENT_SaveBankHardwareContext: usize,
    pub CLIENT_RestoreBankHardwareContext: usize,
    pub CLIENT_PreProcessControllerInterrupt: usize,
    pub CLIENT_ControllerSpecificFunction: usize,
    pub CLIENT_ReconfigureInterrupt: usize,
    pub CLIENT_QueryEnabledInterrupts: usize,
    pub CLIENT_ConnectFunctionConfigPins: usize,
    pub CLIENT_DisconnectFunctionConfigPins: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_HBAPortStatistics {
    pub SecondsSinceLastReset: i64,
    pub TxFrames: i64,
    pub TxWords: i64,
    pub RxFrames: i64,
    pub RxWords: i64,
    pub LIPCount: i64,
    pub NOSCount: i64,
    pub ErrorFrames: i64,
    pub DumpedFrames: i64,
    pub LinkFailureCount: i64,
    pub LossOfSyncCount: i64,
    pub LossOfSignalCount: i64,
    pub PrimitiveSeqProtocolErrCount: i64,
    pub InvalidTxWordCount: i64,
    pub InvalidCRCCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAFC3MgmtInfo {
    pub UniqueAdapterId: u64,
    pub wwn: [u8; 8],
    pub unittype: u32,
    pub PortId: u32,
    pub NumberOfAttachedNodes: u32,
    pub IPVersion: u16,
    pub UDPPort: u16,
    pub IPAddress: [u8; 16],
    pub reserved: u16,
    pub TopologyDiscoveryFlags: u16,
    pub reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAScsiID {
    pub ScsiBusNumber: u32,
    pub ScsiTargetNumber: u32,
    pub ScsiOSLun: u32,
    pub OSDeviceName: [u16; 257],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_LinkEvent {
    pub EventType: u32,
    pub AdapterWWN: [u8; 8],
    pub RLIRBufferSize: u32,
    pub RLIRBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_FCAdapterHBAAttributes {
    pub UniqueAdapterId: u64,
    pub HBAStatus: u32,
    pub NodeWWN: [u8; 8],
    pub VendorSpecificID: u32,
    pub NumberOfPorts: u32,
    pub Manufacturer: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub Model: [u32; 1],
    pub ModelDescription: [u32; 1],
    pub NodeSymbolicName: [u32; 1],
    pub HardwareVersion: [u32; 1],
    pub DriverVersion: [u32; 1],
    pub OptionROMVersion: [u32; 1],
    pub FirmwareVersion: [u32; 1],
    pub DriverName: [u32; 1],
    pub MfgDomain: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_HBAPortAttributesResults {
    pub NodeWWN: [u8; 8],
    pub PortWWN: [u8; 8],
    pub PortFcId: u32,
    pub PortType: u32,
    pub PortState: u32,
    pub PortSupportedClassofService: u32,
    pub PortSupportedFc4Types: [u8; 32],
    pub PortActiveFc4Types: [u8; 32],
    pub PortSupportedSpeed: u32,
    pub PortSpeed: u32,
    pub PortMaxFrameSize: u32,
    pub FabricName: [u8; 8],
    pub NumberofDiscoveredPorts: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_FibrePortHBAAttributes {
    pub UniquePortId: u64,
    pub HBAStatus: u32,
    pub Attributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_FibrePortHBAStatistics {
    pub UniquePortId: u64,
    pub HBAStatus: u32,
    pub Statistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_FC4STATISTICS {
    pub InputRequests: u64,
    pub OutputRequests: u64,
    pub ControlRequests: u64,
    pub InputMegabytes: u64,
    pub OutputMegabytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_EventBuffer {
    pub EventType: u32,
    pub EventInfo: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetDiscoveredPortAttributes_IN {
    pub PortIndex: u32,
    pub DiscoveredPortIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetDiscoveredPortAttributes_OUT {
    pub HBAStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPortAttributesByWWN_IN {
    pub wwn: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPortAttributesByWWN_OUT {
    pub HBAStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendCTPassThru_IN {
    pub PortWWN: [u8; 8],
    pub RequestBufferCount: u32,
    pub RequestBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendCTPassThru_OUT {
    pub HBAStatus: u32,
    pub TotalResponseBufferCount: u32,
    pub ActualResponseBufferCount: u32,
    pub ResponseBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRNID_IN {
    pub wwn: [u8; 8],
    pub wwntype: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRNID_OUT {
    pub HBAStatus: u32,
    pub ResponseBufferCount: u32,
    pub ResponseBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRNIDV2_IN {
    pub PortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub DestFCID: u32,
    pub NodeIdDataFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRNIDV2_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFC3MgmtInfo_OUT {
    pub HBAStatus: u32,
    pub MgmtInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetFC3MgmtInfo_IN {
    pub MgmtInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetFC3MgmtInfo_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRPL_IN {
    pub PortWWN: [u8; 8],
    pub AgentWWN: [u8; 8],
    pub agent_domain: u32,
    pub portIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRPL_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRPS_IN {
    pub PortWWN: [u8; 8],
    pub AgentWWN: [u8; 8],
    pub ObjectWWN: [u8; 8],
    pub AgentDomain: u32,
    pub ObjectPortNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRPS_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendSRL_IN {
    pub PortWWN: [u8; 8],
    pub WWN: [u8; 8],
    pub Domain: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendSRL_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendLIRR_IN {
    pub SourceWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub Function: u8,
    pub Type: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendLIRR_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFC4Statistics_IN {
    pub PortWWN: [u8; 8],
    pub FC4Type: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFC4Statistics_OUT {
    pub HBAStatus: u32,
    pub FC4Statistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFCPStatistics_IN {
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFCPStatistics_OUT {
    pub HBAStatus: u32,
    pub FC4Statistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiInquiry_IN {
    pub UniqueSessionId: u64,
    pub Lun: u64,
    pub InquiryFlags: u8,
    pub PageCode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiInquiry_OUT {
    pub Status: u32,
    pub ResponseBufferSize: u32,
    pub ScsiStatus: u8,
    pub SenseBuffer: [u8; 18],
    pub ResponseBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiReadCapacity_IN {
    pub UniqueSessionId: u64,
    pub Lun: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiReadCapacity_OUT {
    pub Status: u32,
    pub ResponseBufferSize: u32,
    pub ScsiStatus: u8,
    pub SenseBuffer: [u8; 18],
    pub ResponseBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiReportLuns_IN {
    pub UniqueSessionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ScsiReportLuns_OUT {
    pub Status: u32,
    pub ResponseBufferSize: u32,
    pub ScsiStatus: u8,
    pub SenseBuffer: [u8; 18],
    pub ResponseBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetEventBuffer_OUT {
    pub HBAStatus: u32,
    pub EventCount: u32,
    pub Events: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRLS_IN {
    pub PortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendRLS_OUT {
    pub HBAStatus: u32,
    pub TotalRspBufferSize: u32,
    pub ActualRspBufferSize: u32,
    pub RspBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAFCPID {
    pub Fcid: u32,
    pub NodeWWN: [u8; 8],
    pub PortWWN: [u8; 8],
    pub FcpLun: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAFCPScsiEntry {
    pub FCPId: u32,
    pub Luid: [u8; 256],
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAFCPBindingEntry {
    pub Type: u32,
    pub FCPId: u32,
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HBAFCPBindingEntry2 {
    pub Type: u32,
    pub FCPId: u32,
    pub Luid: [u8; 256],
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFcpTargetMapping_IN {
    pub HbaPortWWN: [u8; 8],
    pub InEntryCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFcpTargetMapping_OUT {
    pub HBAStatus: u32,
    pub TotalEntryCount: u32,
    pub OutEntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFcpPersistentBinding_IN {
    pub InEntryCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetFcpPersistentBinding_OUT {
    pub HBAStatus: u32,
    pub TotalEntryCount: u32,
    pub OutEntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetBindingCapability_IN {
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetBindingCapability_OUT {
    pub HBAStatus: u32,
    pub BindType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetBindingSupport_IN {
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetBindingSupport_OUT {
    pub HBAStatus: u32,
    pub BindType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetBindingSupport_IN {
    pub PortWWN: [u8; 8],
    pub BindType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetBindingSupport_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPersistentBinding2_IN {
    pub PortWWN: [u8; 8],
    pub InEntryCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPersistentBinding2_OUT {
    pub HBAStatus: u32,
    pub TotalEntryCount: u32,
    pub OutEntryCount: u32,
    pub Bindings: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetPersistentEntry_IN {
    pub PortWWN: [u8; 8],
    pub Binding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetPersistentEntry_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePersistentEntry_IN {
    pub PortWWN: [u8; 8],
    pub Binding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePersistentEntry_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_AdapterEvent {
    pub EventType: u32,
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_PortEvent {
    pub EventType: u32,
    pub FabricPortId: u32,
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_TargetEvent {
    pub EventType: u32,
    pub PortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddTarget_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub AllTargets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddTarget_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveTarget_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub AllTargets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveTarget_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddPort_IN {
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddPort_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePort_IN {
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePort_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddLink_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveLink_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SM_AdapterInformationQuery {
    pub UniqueAdapterId: u64,
    pub HBAStatus: u32,
    pub NumberOfPorts: u32,
    pub VendorSpecificID: u32,
    pub Manufacturer: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub Model: [u32; 1],
    pub ModelDescription: [u32; 1],
    pub HardwareVersion: [u32; 1],
    pub DriverVersion: [u32; 1],
    pub OptionROMVersion: [u32; 1],
    pub FirmwareVersion: [u32; 1],
    pub DriverName: [u32; 1],
    pub HBASymbolicName: [u32; 1],
    pub RedundantOptionROMVersion: [u32; 1],
    pub RedundantFirmwareVersion: [u32; 1],
    pub MfgDomain: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_FC_Port {
    pub NodeWWN: [u8; 8],
    pub PortWWN: [u8; 8],
    pub FcId: u32,
    pub PortSupportedClassofService: u32,
    pub PortSupportedFc4Types: [u8; 32],
    pub PortActiveFc4Types: [u8; 32],
    pub FabricName: [u8; 8],
    pub NumberofDiscoveredPorts: u32,
    pub NumberofPhys: u8,
    pub PortSymbolicName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_SAS_Port {
    pub PortProtocol: u32,
    pub LocalSASAddress: [u8; 8],
    pub AttachedSASAddress: [u8; 8],
    pub NumberofDiscoveredPorts: u32,
    pub NumberofPhys: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_PORTATTRIBUTES {
    pub PortType: u32,
    pub PortState: u32,
    pub PortSpecificAttributesSize: u32,
    pub OSDeviceName: [u32; 1],
    pub Reserved: u64,
    pub PortSpecificAttributes: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_PROTOCOLSTATISTICS {
    pub SecondsSinceLastReset: i64,
    pub InputRequests: i64,
    pub OutputRequests: i64,
    pub ControlRequests: i64,
    pub InputMegabytes: i64,
    pub OutputMegabytes: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_SASPHYSTATISTICS {
    pub SecondsSinceLastReset: i64,
    pub TxFrames: i64,
    pub TxWords: i64,
    pub RxFrames: i64,
    pub RxWords: i64,
    pub InvalidDwordCount: i64,
    pub RunningDisparityErrorCount: i64,
    pub LossofDwordSyncCount: i64,
    pub PhyResetProblemCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_FC_PHY {
    pub PhySupportSpeed: u32,
    pub PhySpeed: u32,
    pub PhyType: u8,
    pub MaxFrameSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_SAS_PHY {
    pub PhyIdentifier: u8,
    pub NegotiatedLinkRate: u32,
    pub ProgrammedMinLinkRate: u32,
    pub HardwareMinLinkRate: u32,
    pub ProgrammedMaxLinkRate: u32,
    pub HardwareMaxLinkRate: u32,
    pub domainPortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPortType_IN {
    pub PortIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPortType_OUT {
    pub HBAStatus: u32,
    pub PortType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetAdapterPortAttributes_IN {
    pub PortIndex: u32,
    pub PortSpecificAttributesMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetAdapterPortAttributes_OUT {
    pub HBAStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetDiscoveredPortAttributes_IN {
    pub PortIndex: u32,
    pub DiscoveredPortIndex: u32,
    pub PortSpecificAttributesMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetDiscoveredPortAttributes_OUT {
    pub HBAStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPortAttributesByWWN_IN {
    pub PortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub PortSpecificAttributesMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPortAttributesByWWN_OUT {
    pub HBAStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetProtocolStatistics_IN {
    pub PortIndex: u32,
    pub ProtocolType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetProtocolStatistics_OUT {
    pub HBAStatus: u32,
    pub ProtocolStatistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPhyStatistics_IN {
    pub PortIndex: u32,
    pub PhyIndex: u32,
    pub InNumOfPhyCounters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPhyStatistics_OUT {
    pub HBAStatus: u32,
    pub TotalNumOfPhyCounters: u32,
    pub OutNumOfPhyCounters: u32,
    pub PhyCounter: [i64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetFCPhyAttributes_IN {
    pub PortIndex: u32,
    pub PhyIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetFCPhyAttributes_OUT {
    pub HBAStatus: u32,
    pub PhyType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetSASPhyAttributes_IN {
    pub PortIndex: u32,
    pub PhyIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetSASPhyAttributes_OUT {
    pub HBAStatus: u32,
    pub PhyType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_PORTLUN {
    pub PortWWN: [u8; 8],
    pub domainPortWWN: [u8; 8],
    pub TargetLun: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_SCSIENTRY {
    pub PortLun: u32,
    pub LUID: [u8; 256],
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SMHBA_BINDINGENTRY {
    pub field_type: u32,
    pub PortLun: u32,
    pub LUID: [u8; 256],
    pub Status: u32,
    pub ScsiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetTargetMapping_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub InEntryCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetTargetMapping_OUT {
    pub HBAStatus: u32,
    pub TotalEntryCount: u32,
    pub OutEntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetBindingCapability_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetBindingCapability_OUT {
    pub HBAStatus: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetBindingSupport_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetBindingSupport_OUT {
    pub HBAStatus: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetBindingSupport_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetBindingSupport_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPersistentBinding_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub InEntryCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetPersistentBinding_OUT {
    pub HBAStatus: u32,
    pub TotalEntryCount: u32,
    pub OutEntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetPersistentBinding_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub InEntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetPersistentBinding_OUT {
    pub HBAStatus: u32,
    pub OutStatusCount: u32,
    pub EntryStatus: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemovePersistentBinding_IN {
    pub HbaPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub EntryCount: u32,
    pub Entry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemovePersistentBinding_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetLUNStatistics_IN {
    pub Lunit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetLUNStatistics_OUT {
    pub HBAStatus: u32,
    pub ProtocolStatistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiInquiry_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub SmhbaLUN: u64,
    pub Cdb: [u8; 6],
    pub InRespBufferMaxSize: u32,
    pub InSenseBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiInquiry_OUT {
    pub HBAStatus: u32,
    pub ScsiStatus: u8,
    pub OutRespBufferSize: u32,
    pub OutSenseBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiReportLuns_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub Cdb: [u8; 12],
    pub InRespBufferMaxSize: u32,
    pub InSenseBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiReportLuns_OUT {
    pub HBAStatus: u32,
    pub ScsiStatus: u8,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub OutSenseBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiReadCapacity_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub SmhbaLUN: u64,
    pub Cdb: [u8; 16],
    pub InRespBufferMaxSize: u32,
    pub InSenseBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_ScsiReadCapacity_OUT {
    pub HBAStatus: u32,
    pub ScsiStatus: u8,
    pub OutRespBufferSize: u32,
    pub OutSenseBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendTEST_IN {
    pub HbaPortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub DestFCID: u32,
    pub ReqBufferSize: u32,
    pub ReqBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendTEST_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendECHO_IN {
    pub HbaPortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub DestFCID: u32,
    pub InRespBufferMaxSize: u32,
    pub ReqBufferSize: u32,
    pub ReqBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendECHO_OUT {
    pub HBAStatus: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendSMPPassThru_IN {
    pub HbaPortWWN: [u8; 8],
    pub DestPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub InRespBufferMaxSize: u32,
    pub ReqBufferSize: u32,
    pub ReqBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendSMPPassThru_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendCTPassThru_IN {
    pub HbaPortWWN: [u8; 8],
    pub InRespBufferMaxSize: u32,
    pub ReqBufferSize: u32,
    pub ReqBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendCTPassThru_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_GetRNIDMgmtInfo_OUT {
    pub HBAStatus: u32,
    pub MgmtInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetRNIDMgmtInfo_IN {
    pub MgmtInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SetRNIDMgmtInfo_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRNID_IN {
    pub HbaPortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub DestFCID: u32,
    pub NodeIdDataFormat: u32,
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRNID_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRPL_IN {
    pub HbaPortWWN: [u8; 8],
    pub AgentWWN: [u8; 8],
    pub AgentDomain: u32,
    pub PortIndex: u32,
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRPL_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRPS_IN {
    pub HbaPortWWN: [u8; 8],
    pub AgentWWN: [u8; 8],
    pub ObjectWWN: [u8; 8],
    pub AgentDomain: u32,
    pub ObjectPortNumber: u32,
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRPS_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendSRL_IN {
    pub HbaPortWWN: [u8; 8],
    pub WWN: [u8; 8],
    pub Domain: u32,
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendSRL_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendLIRR_IN {
    pub SourceWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub Function: u8,
    pub Type: u8,
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendLIRR_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRLS_IN {
    pub HbaPortWWN: [u8; 8],
    pub DestWWN: [u8; 8],
    pub InRespBufferMaxSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_SendRLS_OUT {
    pub HBAStatus: u32,
    pub TotalRespBufferSize: u32,
    pub OutRespBufferSize: u32,
    pub RespBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SM_AdapterEvent {
    pub EventType: u32,
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SM_PortEvent {
    pub EventType: u32,
    pub FabricPortId: u32,
    pub PortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MS_SM_TargetEvent {
    pub EventType: u32,
    pub PortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_AddTarget_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub AllTargets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_AddTarget_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemoveTarget_IN {
    pub HbaPortWWN: [u8; 8],
    pub DiscoveredPortWWN: [u8; 8],
    pub DomainPortWWN: [u8; 8],
    pub AllTargets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemoveTarget_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_AddPort_IN {
    pub PortWWN: [u8; 8],
    pub EventType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_AddPort_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemovePort_IN {
    pub PortWWN: [u8; 8],
    pub EventType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemovePort_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_AddLink_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SM_RemoveLink_OUT {
    pub HBAStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_TM {
    pub tm_sec: u32,
    pub tm_min: u32,
    pub tm_hour: u32,
    pub tm_mday: u32,
    pub tm_mon: u32,
    pub tm_year: u32,
    pub tm_wday: u32,
    pub tm_yday: u32,
    pub tm_isdst: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_CODEC_COMMAND {
    pub Data: u32,
    pub VerbId: u32,
    pub Node: u32,
    pub CodecAddress: u32,
    pub Data_1: u32,
    pub VerbId_1: u32,
    pub Node_1: u32,
    pub CodecAddress_1: u32,
    pub Command: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_CODEC_RESPONSE {
    pub Response: u32,
    pub SubTag: u32,
    pub Tag: u32,
    pub Response_1: u32,
    pub SDataIn: u32,
    pub IsUnsolicitedResponse: u32,
    pub HasFifoOverrun: u32,
    pub IsValid: u32,
    pub CompleteResponse: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_CODEC_TRANSFER {
    pub Output: u32,
    pub Input: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_STREAM_FORMAT {
    pub SampleRate: u32,
    pub ValidBitsPerSample: u16,
    pub ContainerSize: u16,
    pub NumberOfChannels: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_CONVERTER_FORMAT {
    pub NumberOfChannels: u16,
    pub BitsPerSample: u16,
    pub SampleRate: u16,
    pub StreamType: u16,
    pub ConverterFormat: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_DEVICE_INFORMATION {
    pub Size: u16,
    pub DeviceVersion: u16,
    pub DriverVersion: u16,
    pub CodecsDetected: u16,
    pub IsStripingSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_DEVICE_INFORMATION_V2 {
    pub Size: u16,
    pub DeviceVersion: u16,
    pub DriverVersion: u16,
    pub CodecsDetected: u16,
    pub IsStripingSupported: u8,
    pub CtrlRevision: u8,
    pub CtrlVendorId: u16,
    pub CtrlDeviceId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_BUFFER_DESCRIPTOR {
    pub Address: u64,
    pub Length: u32,
    pub InterruptOnCompletion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_BUS_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TransferCodecVerbs: usize,
    pub AllocateCaptureDmaEngine: usize,
    pub AllocateRenderDmaEngine: usize,
    pub ChangeBandwidthAllocation: usize,
    pub AllocateDmaBuffer: usize,
    pub FreeDmaBuffer: usize,
    pub FreeDmaEngine: usize,
    pub SetDmaEngineState: usize,
    pub GetWallClockRegister: usize,
    pub GetLinkPositionRegister: usize,
    pub RegisterEventCallback: usize,
    pub UnregisterEventCallback: usize,
    pub GetDeviceInformation: usize,
    pub GetResourceInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_BUS_INTERFACE_BDL {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TransferCodecVerbs: usize,
    pub AllocateCaptureDmaEngine: usize,
    pub AllocateRenderDmaEngine: usize,
    pub ChangeBandwidthAllocation: usize,
    pub AllocateContiguousDmaBuffer: usize,
    pub SetupDmaEngineWithBdl: usize,
    pub FreeContiguousDmaBuffer: usize,
    pub FreeDmaEngine: usize,
    pub SetDmaEngineState: usize,
    pub GetWallClockRegister: usize,
    pub GetLinkPositionRegister: usize,
    pub RegisterEventCallback: usize,
    pub UnregisterEventCallback: usize,
    pub GetDeviceInformation: usize,
    pub GetResourceInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_BUS_INTERFACE_V2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TransferCodecVerbs: usize,
    pub AllocateCaptureDmaEngine: usize,
    pub AllocateRenderDmaEngine: usize,
    pub ChangeBandwidthAllocation: usize,
    pub AllocateDmaBuffer: usize,
    pub FreeDmaBuffer: usize,
    pub FreeDmaEngine: usize,
    pub SetDmaEngineState: usize,
    pub GetWallClockRegister: usize,
    pub GetLinkPositionRegister: usize,
    pub RegisterEventCallback: usize,
    pub UnregisterEventCallback: usize,
    pub GetDeviceInformation: usize,
    pub GetResourceInformation: usize,
    pub AllocateDmaBufferWithNotification: usize,
    pub FreeDmaBufferWithNotification: usize,
    pub RegisterNotificationEvent: usize,
    pub UnregisterNotificationEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HDAUDIO_BUS_INTERFACE_V3 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TransferCodecVerbs: usize,
    pub AllocateCaptureDmaEngine: usize,
    pub AllocateRenderDmaEngine: usize,
    pub ChangeBandwidthAllocation: usize,
    pub AllocateDmaBuffer: usize,
    pub FreeDmaBuffer: usize,
    pub FreeDmaEngine: usize,
    pub SetDmaEngineState: usize,
    pub GetWallClockRegister: usize,
    pub GetLinkPositionRegister: usize,
    pub RegisterEventCallback: usize,
    pub UnregisterEventCallback: usize,
    pub GetDeviceInformation: usize,
    pub GetResourceInformation: usize,
    pub AllocateDmaBufferWithNotification: usize,
    pub FreeDmaBufferWithNotification: usize,
    pub RegisterNotificationEvent: usize,
    pub UnregisterNotificationEvent: usize,
    pub RegisterNotificationCallback: usize,
    pub UnregisterNotificationCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_COLLECTION_DESC {
    pub UsagePage: u32,
    pub Usage: u32,
    pub CollectionNumber: u8,
    pub reserved_padding: [u32; 15],
    pub InputLength: u16,
    pub OutputLength: u16,
    pub FeatureLength: u16,
    pub PreparsedDataLength: u16,
    pub PreparsedData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_REPORT_IDS {
    pub ReportID: u8,
    pub CollectionNumber: u8,
    pub InputLength: u16,
    pub OutputLength: u16,
    pub FeatureLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_GETCOLDESC_DBG {
    pub BreakOffset: u32,
    pub ErrorCode: u32,
    pub Args: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_DEVICE_DESC {
    pub CollectionDesc: usize,
    pub CollectionDescLength: u32,
    pub ReportIDs: usize,
    pub ReportIDsLength: u32,
    pub Dbg: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_MINIDRIVER_REGISTRATION {
    pub Revision: u32,
    pub DriverObject: usize,
    pub RegistryPath: usize,
    pub DeviceExtensionSize: u32,
    pub DevicesArePolled: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_DEVICE_EXTENSION {
    pub PhysicalDeviceObject: usize,
    pub NextDeviceObject: usize,
    pub MiniDeviceExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_DEVICE_ATTRIBUTES {
    pub Size: u32,
    pub VendorID: u16,
    pub ProductID: u16,
    pub VersionNumber: u16,
    pub Reserved: [u16; 11],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bcdHID: u16,
    pub bCountry: u8,
    pub bNumDescriptors: u8,
    pub bReportType: u8,
    pub wReportLength: u16,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_DESCRIPTOR_DESC_LIST {
    pub bReportType: u8,
    pub wReportLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_SUBMIT_IDLE_NOTIFICATION_CALLBACK_INFO {
    pub IdleCallback: usize,
    pub IdleContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HPMI_QUERY_CAPABILITIES {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HPMI_QUERY_CAPABILITIES_RESPONSE {
    pub Version: u32,
    pub RequestService: u32,
    pub SdbCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HPMI_BATTERY_UTILIZATION_HINT {
    pub Version: u32,
    pub PreserveNonHotSwappableBatteries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_PDO_EXTENSION {
    pub Signature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CD_ERROR_INFORMATION {
    pub Version: u32,
    pub PathError: u32,
    pub UlongArg1: u32,
    pub UlongArg2: u32,
    pub NtStatus: u32,
    pub XtraInfo: [u8; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_PORT_PATH {
    pub PortPathDepth: u32,
    pub PortPath: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_ID_ERROR_INFORMATION {
    pub Version: u32,
    pub PathError: u32,
    pub Arg1: u32,
    pub UsbAddress: u32,
    pub NtStatus: u32,
    pub UsbdStatus: u32,
    pub XtraInfo: [u8; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V0 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V1 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V2 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V3 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V4 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V5 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
    pub SetDeviceHandleData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V6 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
    pub SetDeviceHandleData: usize,
    pub HubIsRoot: usize,
    pub AcquireBusSemaphore: usize,
    pub ReleaseBusSemaphore: usize,
    pub CaculatePipeBandwidth: usize,
    pub SetBusSystemWakeMode: usize,
    pub SetDeviceFlag: usize,
    pub HubTestPoint: usize,
    pub GetDevicePerformanceInfo: usize,
    pub WaitAsyncPowerUp: usize,
    pub GetDeviceAddress: usize,
    pub RefDeviceHandle: usize,
    pub DerefDeviceHandle: usize,
    pub SetDeviceHandleIdleReadyState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V7 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
    pub SetDeviceHandleData: usize,
    pub HubIsRoot: usize,
    pub AcquireBusSemaphore: usize,
    pub ReleaseBusSemaphore: usize,
    pub CaculatePipeBandwidth: usize,
    pub SetBusSystemWakeMode: usize,
    pub SetDeviceFlag: usize,
    pub HubTestPoint: usize,
    pub GetDevicePerformanceInfo: usize,
    pub WaitAsyncPowerUp: usize,
    pub GetDeviceAddress: usize,
    pub RefDeviceHandle: usize,
    pub DerefDeviceHandle: usize,
    pub SetDeviceHandleIdleReadyState: usize,
    pub CreateUsbDeviceV7: usize,
    pub GetContainerIdForPort: usize,
    pub SetContainerIdForPort: usize,
    pub AbortAllDevicePipes: usize,
    pub SetDeviceErrataFlag: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V8 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
    pub SetDeviceHandleData: usize,
    pub HubIsRoot: usize,
    pub AcquireBusSemaphore: usize,
    pub ReleaseBusSemaphore: usize,
    pub CaculatePipeBandwidth: usize,
    pub SetBusSystemWakeMode: usize,
    pub SetDeviceFlag: usize,
    pub HubTestPoint: usize,
    pub GetDevicePerformanceInfo: usize,
    pub WaitAsyncPowerUp: usize,
    pub GetDeviceAddress: usize,
    pub RefDeviceHandle: usize,
    pub DerefDeviceHandle: usize,
    pub SetDeviceHandleIdleReadyState: usize,
    pub CreateUsbDeviceV7: usize,
    pub GetContainerIdForPort: usize,
    pub SetContainerIdForPort: usize,
    pub AbortAllDevicePipes: usize,
    pub SetDeviceErrataFlag: usize,
    pub GetDebugPortNumber: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_V9 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub CreateUsbDevice: usize,
    pub InitializeUsbDevice: usize,
    pub GetUsbDescriptors: usize,
    pub RemoveUsbDevice: usize,
    pub RestoreUsbDevice: usize,
    pub GetPortHackFlags: usize,
    pub QueryDeviceInformation: usize,
    pub GetControllerInformation: usize,
    pub ControllerSelectiveSuspend: usize,
    pub GetExtendedHubInformation: usize,
    pub GetRootHubSymbolicName: usize,
    pub GetDeviceBusContext: usize,
    pub Initialize20Hub: usize,
    pub RootHubInitNotification: usize,
    pub FlushTransfers: usize,
    pub SetDeviceHandleData: usize,
    pub HubIsRoot: usize,
    pub AcquireBusSemaphore: usize,
    pub ReleaseBusSemaphore: usize,
    pub CaculatePipeBandwidth: usize,
    pub SetBusSystemWakeMode: usize,
    pub SetDeviceFlag: usize,
    pub HubTestPoint: usize,
    pub GetDevicePerformanceInfo: usize,
    pub WaitAsyncPowerUp: usize,
    pub GetDeviceAddress: usize,
    pub RefDeviceHandle: usize,
    pub DerefDeviceHandle: usize,
    pub SetDeviceHandleIdleReadyState: usize,
    pub CreateUsbDeviceV7: usize,
    pub GetContainerIdForPort: usize,
    pub SetContainerIdForPort: usize,
    pub AbortAllDevicePipes: usize,
    pub SetDeviceErrataFlag: usize,
    pub GetDebugPortNumber: usize,
    pub SetMsOs20Support: usize,
    pub SetClearRootPortResumeTime: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_MINIDUMP {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetUsbPortMiniDumpFlags: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_HUB_SELECTIVE_SUSPEND {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SuspendHub: usize,
    pub ResumeHub: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_PIPE_INFORMATION_0 {
    pub EndpointDescriptor: u32,
    pub ED_Pad: [u8; 1],
    pub ScheduleOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_LEVEL_INFORMATION {
    pub InformationLevel: u32,
    pub ActualLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_INFORMATION_0 {
    pub InformationLevel: u32,
    pub ActualLength: u32,
    pub PortNumber: u32,
    pub DeviceDescriptor: u32,
    pub DD_pad: [u8; 2],
    pub CurrentConfigurationValue: u8,
    pub ReservedMBZ: u8,
    pub DeviceAddress: u16,
    pub HubAddress: u32,
    pub DeviceSpeed: u32,
    pub DeviceType: u32,
    pub NumberOfOpenPipes: u32,
    pub PipeList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONTROLLER_INFORMATION_0 {
    pub InformationLevel: u32,
    pub ActualLength: u32,
    pub SelectiveSuspendEnabled: u8,
    pub IsHighSpeedController: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONTROLLER_INFORMATION_1 {
    pub InformationLevel: u32,
    pub ActualLength: u32,
    pub SelectiveSuspendEnabled: u8,
    pub IsHighSpeedController: u8,
    pub HcBusNumber: u32,
    pub HcBusDevice: u32,
    pub HcBusFunction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_EXTPORT_INFORMATION_0 {
    pub PhysicalPortNumber: u32,
    pub PortLabelNumber: u32,
    pub VidOverride: u16,
    pub PidOverride: u16,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_EXTHUB_INFORMATION_0 {
    pub InformationLevel: u32,
    pub NumberOfPorts: u32,
    pub Port: [u32; 255],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_PERFORMANCE_INFO_0 {
    pub InformationLevel: u32,
    pub ActualLength: u32,
    pub BulkBytes: u32,
    pub BulkUrbCount: u32,
    pub ControlDataBytes: u32,
    pub ControlUrbCount: u32,
    pub IsoBytes: u32,
    pub IsoUrbCount: u32,
    pub InterruptBytes: u32,
    pub InterruptUrbCount: u32,
    pub AllocedInterrupt: [u32; 6],
    pub AllocedIso: u32,
    pub Total32secBandwidth: u32,
    pub TotalTtBandwidth: u32,
    pub TotalIsoLatency: u32,
    pub DroppedIsoPackets: u32,
    pub TransferErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_DEVICE_INFORMATION {
    pub Version: u16,
    pub Size: u16,
    pub TotalHwNs: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HWN_CLIENT_REGISTRATION_PACKET {
    pub Version: u16,
    pub Size: u16,
    pub DeviceContextSize: u32,
    pub Reserved: u32,
    pub ClientInitializeDevice: usize,
    pub ClientUnInitializeDevice: usize,
    pub ClientQueryDeviceInformation: usize,
    pub ClientStartDevice: usize,
    pub ClientStopDevice: usize,
    pub ClientSetHwNState: usize,
    pub ClientGetHwNState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FIREWALL_CONTEXT_T {
    pub Direction: u32,
    pub field_NTE: u32,
    pub field_LinkCtxt: u32,
    pub LContext1: u32,
    pub LContext2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IP_SET_FIREWALL_HOOK_INFO {
    pub FirewallPtr: u32,
    pub Priority: u32,
    pub Add: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_IO_CONTROL {
    pub HeaderLength: u32,
    pub Signature: [u8; 8],
    pub Timeout: u32,
    pub ControlCode: u32,
    pub ReturnStatus: u32,
    pub DataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_POWER_INFO {
    pub CurrentPowerState: u32,
    pub DesiredPowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_VENDOR_DEFINED_POWER_INFO {
    pub TargetId: u8,
    pub SettingGuid: u32,
    pub Value: usize,
    pub ValueLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDEREGISTERS {
    pub bFeaturesReg: u8,
    pub bSectorCountReg: u8,
    pub bSectorNumberReg: u8,
    pub bCylLowReg: u8,
    pub bCylHighReg: u8,
    pub bDriveHeadReg: u8,
    pub bCommandReg: u8,
    pub bReserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_TASK_FILE {
    pub Current: u32,
    pub Previous: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_REQUEST_BLOCK {
    pub Function: u16,
    pub IrbStatus: u8,
    pub AtaStatus: u8,
    pub AtaError: u8,
    pub Channel: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub CdbLength: u8,
    pub SenseInfoBufferLength: u8,
    pub SenseInfoBufferType: u8,
    pub QueueTag: u8,
    pub ReservedAsUlong: u32,
    pub IrbFlags: u32,
    pub TimeOutValue: u32,
    pub DataTransferLength: u32,
    pub IrbExtension: usize,
    pub DataBuffer: usize,
    pub SenseInfoBuffer: usize,
    pub NextIrb: usize,
    pub Reserved: usize,
    pub IdeTaskFile: u32,
    pub Cdb: [u8; 16],
    pub PowerChange: u32,
    pub AsUChar: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_SCATTER_GATHER_ELEMENT {
    pub Address: u32,
    pub Length: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_SCATTER_GATHER_LIST {
    pub NumberOfElements: u32,
    pub Reserved: usize,
    pub Elements: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_REGISTERS_1 {
    pub RegistersBaseAddress: *mut u8,
    pub Data: *mut u16,
    pub Error: *mut u8,
    pub Features: *mut u8,
    pub BlockCount: *mut u8,
    pub InterruptReason: *mut u8,
    pub BlockNumber: *mut u8,
    pub CylinderLow: *mut u8,
    pub ByteCountLow: *mut u8,
    pub CylinderHigh: *mut u8,
    pub ByteCountHigh: *mut u8,
    pub DriveSelect: *mut u8,
    pub Command: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_REGISTERS_2 {
    pub RegistersBaseAddress: *mut u8,
    pub DeviceControl: *mut u8,
    pub DriveAddress: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_ACCESS_RANGE {
    pub RangeStart: u32,
    pub PhysicalRangeStart: u32,
    pub RangeLength: u32,
    pub InMemory: u8,
    pub Bar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_ERROR_LOG_ENTRY {
    pub IrbStatus: u8,
    pub Channel: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub UniqueId: u8,
    pub DumpDataSize: u8,
    pub DumpData: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_DEVICE_PARAMETERS {
    pub Version: u16,
    pub IdeDeviceType: u32,
    pub TargetId: u8,
    pub MaximumLun: u8,
    pub NumberOfOverlappedRequests: u8,
    pub MaxBlockXfer: u8,
    pub DeviceCharacteristics: u16,
    pub AddressTranslation: u32,
    pub MaxLba: i64,
    pub NumCylinders: u16,
    pub NumHeads: u16,
    pub NumSectorsPerTrack: u16,
    pub Reserved: u16,
    pub BytesPerLogicalSector: u32,
    pub BytesPerPhysicalSector: u32,
    pub BytesOffsetForSectorAlignment: u32,
    pub TransferModeSupported: u32,
    pub TransferModeSelected: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_MINIPORT_RESOURCES {
    pub NumberOfAccessRanges: u32,
    pub IdeAccessRange: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_VENDOR_DEFINED_POWER {
    pub ValidGuids: u8,
    pub Guid: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_ADVANCED_CHANNEL_CONFIGURATION {
    pub Length: u16,
    pub Present: u32,
    pub VendorDefinedPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_CHANNEL_CONFIGURATION {
    pub Version: u16,
    pub ChannelNumber: u8,
    pub SupportedAdvances: u32,
    pub ChannelMode: u32,
    pub ChannelResources: usize,
    pub NumberOfOverlappedRequests: u8,
    pub MaxTargetId: u8,
    pub SyncWithIsr: u8,
    pub SupportsWmi: u8,
    pub AdvancedChannelConfiguration: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_CHANNEL_INTERFACE {
    pub Version: u16,
    pub ChannelNumber: u8,
    pub Reserved: u8,
    pub ReservedUlong: u32,
    pub IdeHwInitialize: u32,
    pub IdeHwBuildIo: u32,
    pub IdeHwStartIo: u32,
    pub IdeHwInterrupt: u32,
    pub IdeHwReset: u32,
    pub IdeHwControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_TRANSFER_MODE_PARAMETERS {
    pub ChannelNumber: u8,
    pub DeviceType: [u32; 1],
    pub IoReadySupported: [u8; 1],
    pub DeviceTransferModeSupported: [u32; 1],
    pub DeviceTransferModeCurrent: [u32; 1],
    pub DeviceTransferModeSelected: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_CONTROLLER_CONFIGURATION {
    pub Version: u16,
    pub NumberOfChannels: u8,
    pub ControllerMode: u32,
    pub NumberOfPhysicalBreaks: u8,
    pub MaximumTransferLength: u32,
    pub Reserved: u8,
    pub NativeModeEnabled: u8,
    pub Dma64BitAddress: u8,
    pub BusMaster: u8,
    pub AtaBusType: u32,
    pub ControllerResources: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_CONTROLLER_INTERFACE {
    pub Version: u16,
    pub Reserved: u16,
    pub ControllerExtensionSize: u32,
    pub ChannelExtensionSize: u32,
    pub AlignmentMask: u32,
    pub AtaChannelInitRoutine: u32,
    pub AtaControllerChannelEnabled: u32,
    pub AtaControllerTransferModeSelect: u32,
    pub AtaAdapterControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDE_LBA_RANGE {
    pub StartSector: u64,
    pub SectorCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_DEV_CAPS {
    pub ProtocolVersion: u32,
    pub NumTransmitPorts: u32,
    pub NumReceivePorts: u32,
    pub LearningReceiverMask: u32,
    pub DevCapsFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_DEV_CAPS_V2 {
    pub WakeProtocols: u32,
    pub TunerPnpId: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_TRANSMIT_PARAMS {
    pub TransmitPortMask: u32,
    pub CarrierPeriod: u32,
    pub Flags: u32,
    pub PulseSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_TRANSMIT_CHUNK {
    pub OffsetToNextChunk: usize,
    pub RepeatCount: usize,
    pub ByteCount: usize,
    pub Data: [i32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_RECEIVE_PARAMS {
    pub DataEnd: u32,
    pub ByteCount: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_PRIORITY_RECEIVE_PARAMS {
    pub DataEnd: u32,
    pub ByteCount: u32,
    pub CarrierFrequency: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOCTL_IR_ENTER_PRIORITY_RECEIVE_PARAMS {
    pub Receiver: u32,
    pub TimeOut: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IR_SET_WAKE_PATTERN_PARAMS {
    pub Protocol: u32,
    pub Payload: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_TCPIPConfig {
    pub UseLinkLocalAddress: u8,
    pub EnableDHCP: u8,
    pub UseDHCPForDNS: u8,
    pub IPVersions: u32,
    pub IpAddress: u32,
    pub DefaultGateway: u32,
    pub SubnetMask: u32,
    pub PreferredDNSServer: u32,
    pub AlternateDNSServer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_NICConfig {
    pub LinkSpeed: u32,
    pub MaxLinkSpeed: u32,
    pub LinkState: u32,
    pub MaxFrameSize: u32,
    pub MacAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_BootConfiguration {
    pub LUN: u64,
    pub SecurityFlags: u64,
    pub UsernameSize: u32,
    pub PasswordSize: u32,
    pub DiscoverBootDevice: u8,
    pub InitiatorNode: [u32; 1],
    pub TargetName: [u32; 1],
    pub TargetPortal: u32,
    pub LoginOptions: u32,
    pub Username: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_SecurityCapabilities {
    pub ProtectiScsiTraffic: u8,
    pub ProtectiSNSTraffic: u8,
    pub CertificatesSupported: u8,
    pub EncryptionAvailableCount: u32,
    pub EncryptionAvailable: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_DiscoveryConfig {
    pub PerformiSNSDiscovery: u8,
    pub PerformSLPDiscovery: u8,
    pub AutomaticiSNSDiscovery: u8,
    pub InitiatorName: [u32; 1],
    pub iSNSServer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_RADIUSConfig {
    pub UseRADIUSForCHAP: u8,
    pub SharedSecretSizeInBytes: u32,
    pub RADIUSServer: u32,
    pub BackupRADIUSServer: u32,
    pub Reserved: u32,
    pub SharedSecret: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_IP_Address {
    pub Type: u32,
    pub IpV4Address: u32,
    pub IpV6Address: [u8; 16],
    pub IpV6FlowInfo: u32,
    pub IpV6ScopeId: u32,
    pub TextAddress: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_TargetPortal {
    pub Address: u32,
    pub Reserved: u32,
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_TargetPortalGroup {
    pub PortalCount: u32,
    pub Portals: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_LoginOptions {
    pub InformationSpecified: u32,
    pub HeaderDigest: u32,
    pub DataDigest: u32,
    pub MaximumConnections: u32,
    pub DefaultTime2Wait: u32,
    pub DefaultTime2Retain: u32,
    pub LoginFlags: u32,
    pub AuthType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_LUNList {
    pub TargetLUN: u64,
    pub OSLUN: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_TargetMapping {
    pub OSBus: u32,
    pub OSTarget: u32,
    pub UniqueSessionId: u64,
    pub LUNCount: u32,
    pub TargetName: [u32; 1],
    pub FromPersistentLogin: u8,
    pub Reserved: u64,
    pub LUNList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTargetPortal {
    pub Socket: u16,
    pub Address: u32,
    pub SymbolicName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTargetPortalGroup {
    pub PortalCount: u32,
    pub Portals: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTarget {
    pub TargetPortalGroupCount: u32,
    pub TargetName: [u32; 1],
    pub TargetAlias: [u32; 1],
    pub TargetDiscoveredPortalGroups: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTargetPortal2 {
    pub Socket: u16,
    pub Address: u32,
    pub SecurityBitmap: u32,
    pub KeySize: u32,
    pub Key: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTargetPortalGroup2 {
    pub PortalCount: u32,
    pub Tag: u16,
    pub Portals: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_DiscoveredTarget2 {
    pub TargetPortalGroupCount: u32,
    pub TargetName: [u32; 1],
    pub TargetAlias: [u32; 1],
    pub TargetDiscoveredPortalGroups: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ReportDiscoveredTargets_OUT {
    pub Status: u32,
    pub TargetCount: u32,
    pub Targets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ReportDiscoveredTargets2_OUT {
    pub Status: u32,
    pub TargetCount: u32,
    pub Targets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_HBAInformation {
    pub UniqueAdapterId: u64,
    pub IntegratedTCPIP: u8,
    pub RequiresBinaryIpAddresses: u8,
    pub VersionMin: u8,
    pub VersionMax: u8,
    pub MultifunctionDevice: u8,
    pub CacheValid: u8,
    pub NumberOfPorts: u32,
    pub Status: u32,
    pub FunctionalitySupported: u32,
    pub GenerationalGuid: [u8; 16],
    pub MaxCDBLength: u32,
    pub BiDiScsiCommands: u8,
    pub VendorID: [u32; 1],
    pub VendorModel: [u32; 1],
    pub VendorVersion: [u32; 1],
    pub FirmwareVersion: [u32; 1],
    pub AsicVersion: [u32; 1],
    pub OptionRomVersion: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub DriverName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_HBASessionConfig {
    pub InitialR2T: u8,
    pub ImmediateData: u8,
    pub MaxRecvDataSegmentLength: u32,
    pub MaxBurstLength: u32,
    pub FirstBurstLength: u32,
    pub MaxOutstandingR2T: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_ConnectionStaticInfo {
    pub UniqueConnectionId: u64,
    pub CID: u16,
    pub State: u8,
    pub Protocol: u8,
    pub HeaderIntegrity: u8,
    pub DataIntegrity: u8,
    pub Reserved: u16,
    pub MaxRecvDataSegmentLength: u32,
    pub AuthType: u32,
    pub LocalAddr: u32,
    pub LocalPort: u32,
    pub RemoteAddr: u32,
    pub RemotePort: u32,
    pub EstimatedThroughput: u64,
    pub MaxDatagramSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_SessionStaticInfo {
    pub UniqueSessionId: u64,
    pub InitiatoriSCSIName: [u32; 1],
    pub TargetiSCSIName: [u32; 1],
    pub TSID: u16,
    pub ISID: [u8; 6],
    pub InitialR2t: u8,
    pub ImmediateData: u8,
    pub Type: u8,
    pub DataSequenceInOrder: u8,
    pub DataPduInOrder: u8,
    pub ErrorRecoveryLevel: u8,
    pub MaxOutstandingR2t: u32,
    pub FirstBurstLength: u32,
    pub MaxBurstLength: u32,
    pub MaxConnections: u32,
    pub ConnectionCount: u16,
    pub ConnectionsList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_PortalInfo {
    pub Index: u32,
    pub PortalType: u8,
    pub Protocol: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub IPAddr: u32,
    pub Port: u32,
    pub PortalTag: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_PortalInfoClass {
    pub PortalInfoCount: u32,
    pub PortalInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_InitiatorSessionInfo {
    pub UniqueAdapterId: u64,
    pub SessionCount: u32,
    pub SessionsList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_InitiatorNodeFailureEvent {
    pub FailureTime: u64,
    pub FailureType: u8,
    pub TargetFailureName: [u32; 1],
    pub TargetFailureAddr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_InitiatorInstanceFailureEvent {
    pub FailureType: u8,
    pub RemoteNodeName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_Path {
    pub UniqueConnectionId: u64,
    pub EstimatedLinkSpeed: u64,
    pub PathWeight: u32,
    pub PrimaryPath: u32,
    pub ConnectionStatus: u32,
    pub TCPOffLoadAvailable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_Supported_LB_Policies {
    pub UniqueSessionId: u64,
    pub LoadBalancePolicy: u32,
    pub iSCSI_PathCount: u32,
    pub iSCSI_Paths: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetLoadBalancePolicy_IN {
    pub LoadBalancePolicies: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetLoadBalancePolicy_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_QueryLBPolicy {
    pub UniqueAdapterId: u64,
    pub Reserved: u32,
    pub SessionCount: u32,
    pub LoadBalancePolicies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_Eventlog {
    pub Type: u32,
    pub LogToEventlog: u32,
    pub Size: u32,
    pub AdditionalData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_RedirectPortalInfo {
    pub UniqueConnectionId: u64,
    pub OriginalIPAddr: u32,
    pub OriginalPort: u32,
    pub RedirectedIPAddr: u32,
    pub RedirectedPort: u32,
    pub Redirected: u8,
    pub TemporaryRedirect: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_RedirectSessionInfo {
    pub UniqueSessionId: u64,
    pub TargetPortalGroupTag: u32,
    pub ConnectionCount: u32,
    pub RedirectPortalList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_RedirectPortalInfoClass {
    pub UniqueAdapterId: u64,
    pub SessionCount: u32,
    pub RedirectSessionList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PingIPAddress_IN {
    pub RequestCount: u32,
    pub RequestSize: u32,
    pub Timeout: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PingIPAddress_OUT {
    pub Status: u32,
    pub ResponsesReceived: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendTargets_IN {
    pub UniqueSessionId: u64,
    pub SendTargetsText: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SendTargets_OUT {
    pub Status: u32,
    pub ResponseSize: u32,
    pub Response: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LoginToTarget_IN {
    pub PortNumber: u32,
    pub LoginOptions: u32,
    pub SessionType: u32,
    pub SecurityFlags: u64,
    pub TargetPortal: u32,
    pub UsernameSize: u32,
    pub PasswordSize: u32,
    pub KeySize: u32,
    pub UniqueIdForISID: u16,
    pub PersistentLogin: u8,
    pub InitiatorNode: [u32; 1],
    pub InitiatorAlias: [u32; 1],
    pub TargetName: [u32; 1],
    pub Mappings: u32,
    pub Key: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LoginToTarget_OUT {
    pub Status: u32,
    pub UniqueSessionId: u64,
    pub UniqueConnectionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LogoutFromTarget_IN {
    pub UniqueSessionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LogoutFromTarget_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddConnectionToSession_IN {
    pub UniqueAdapterId: u64,
    pub UniqueSessionId: u64,
    pub SecurityFlags: u64,
    pub PortNumber: u32,
    pub LoginOptions: u32,
    pub TargetPortal: u32,
    pub UsernameSize: u32,
    pub PasswordSize: u32,
    pub KeySize: u32,
    pub Key: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddConnectionToSession_OUT {
    pub Status: u32,
    pub UniqueConnectionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePersistentLogin_IN {
    pub PortNumber: u32,
    pub TargetName: [u32; 1],
    pub TargetPortal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemovePersistentLogin_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveConnectionFromSession_IN {
    pub UniqueSessionId: u64,
    pub UniqueConnectionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveConnectionFromSession_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetCHAPSharedSecret_IN {
    pub SharedSecretSize: u32,
    pub SharedSecret: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetCHAPSharedSecret_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetRADIUSSharedSecret_IN {
    pub SharedSecretSize: u32,
    pub SharedSecret: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetRADIUSSharedSecret_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DeleteInitiatorNodeName_IN {
    pub DeletedInitiatorName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DeleteInitiatorNodeName_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetInitiatorNodeName_IN {
    pub CreatedInitiatorName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetInitiatorNodeName_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddiSNSServer_IN {
    pub iSNSServerName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddiSNSServer_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveiSNSServer_IN {
    pub iSNSServerName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveiSNSServer_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddRADIUSServer_IN {
    pub RADIUSIPAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddRADIUSServer_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveRADIUSServer_IN {
    pub RADIUSIPAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveRADIUSServer_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ISCSI_Persistent_Login {
    pub TargetName: [u32; 1],
    pub SecurityFlags: u64,
    pub InitiatorPortNumber: u32,
    pub UsernameSize: u32,
    pub IsInformationalSession: u8,
    pub UniqueIdForISID: u16,
    pub TargetPortal: u32,
    pub LoginOptions: u32,
    pub TargetMapping: u32,
    pub Username: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_PersistentLogins {
    pub PersistentLoginCount: u32,
    pub Reserved: u32,
    pub PersistentLogins: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_TargetMappings {
    pub UniqueAdapterId: u64,
    pub TargetMappingCount: u32,
    pub Reserved: u32,
    pub TargetMappings: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_LUNMappingInformation {
    pub UniqueAdapterId: u64,
    pub UniqueSessionId: u64,
    pub OSBus: u32,
    pub OSTarget: u32,
    pub OSLUN: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetPresharedKeyForId_IN {
    pub PortNumber: u32,
    pub SecurityFlags: u64,
    pub IdType: u8,
    pub IdSize: u32,
    pub KeySize: u32,
    pub Id: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetPresharedKeyForId_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPresharedKeyForId_IN {
    pub PortNumber: u32,
    pub IdType: u8,
    pub IdSize: u32,
    pub Id: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPresharedKeyForId_OUT {
    pub Status: u32,
    pub SecurityFlags: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetGroupPresharedKey_IN {
    pub KeySize: u32,
    pub Key: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetGroupPresharedKey_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetTunnelModeOuterAddress_IN {
    pub PortNumber: u32,
    pub DestinationAddress: u32,
    pub TunnelModeOuterAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetTunnelModeOuterAddress_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ClearCache_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetGenerationalGuid_IN {
    pub GenerationalGuid: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetGenerationalGuid_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_BootInformation {
    pub NodeName: [u8; 223],
    pub SharedSecretLength: u32,
    pub SharedSecret: [u8; 255],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_AdapterEvent {
    pub UniqueAdapterId: u64,
    pub EventCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_MMIPSECStats {
    pub ActiveAcquire: u64,
    pub ActiveReceive: u64,
    pub AcquireFailures: u64,
    pub ReceiveFailures: u64,
    pub SendFailures: u64,
    pub AcquireHeapSize: u64,
    pub ReceiveHeapSize: u64,
    pub NegotiationFailures: u64,
    pub AuthenticationFailures: u64,
    pub InvalidCookiesReceived: u64,
    pub TotalGetSPI: u64,
    pub KeyAdditions: u64,
    pub KeyUpdates: u64,
    pub GetSPIFailures: u64,
    pub KeyAdditionFailures: u64,
    pub KeyUpdateFailures: u64,
    pub ConnectionListSize: u64,
    pub OakleyMainMode: u64,
    pub OakleyQuickMode: u64,
    pub InvalidPackets: u64,
    pub SoftAssociations: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_QMIPSECStats {
    pub ActiveSA: u64,
    pub PendingKeyOperations: u64,
    pub KeyAdditions: u64,
    pub KeyDeletions: u64,
    pub ReKeys: u64,
    pub ActiveTunnels: u64,
    pub BadSPIPackets: u64,
    pub PacketsNotDecrypted: u64,
    pub PacketsNotAuthenticated: u64,
    pub PacketsWithReplayDetection: u64,
    pub ConfidentialBytesSent: u64,
    pub ConfidentialBytesReceived: u64,
    pub AuthenticatedBytesSent: u64,
    pub AuthenticatedBytesReceived: u64,
    pub TransportBytesSent: u64,
    pub TransportBytesReceived: u64,
    pub TunnelBytesSent: u64,
    pub TunnelBytesReceived: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_ConnectionStatistics {
    pub iSCSIName: [u32; 1],
    pub CID: u16,
    pub USID: u64,
    pub UniqueAdapterId: u64,
    pub BytesSent: u64,
    pub BytesReceived: u64,
    pub PDUCommandsSent: u64,
    pub PDUResponsesReceived: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_SessionStatistics {
    pub iSCSIName: [u32; 1],
    pub USID: u64,
    pub UniqueAdapterId: u64,
    pub BytesSent: u64,
    pub BytesReceived: u64,
    pub PDUCommandsSent: u64,
    pub PDUResponsesReceived: u64,
    pub DigestErrors: u64,
    pub ConnectionTimeoutErrors: u64,
    pub FormatErrors: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_InitiatorLoginStatistics {
    pub UniqueAdapterId: u64,
    pub LoginAcceptRsps: u32,
    pub LoginOtherFailRsps: u32,
    pub LoginRedirectRsps: u32,
    pub LoginAuthFailRsps: u32,
    pub LoginAuthenticateFails: u32,
    pub LoginNegotiateFails: u32,
    pub LogoutNormals: u32,
    pub LogoutOtherCodes: u32,
    pub LoginFailures: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_InitiatorInstanceStatistics {
    pub UniqueAdapterId: u64,
    pub SessionDigestErrorCount: u32,
    pub SessionConnectionTimeoutErrorCount: u32,
    pub SessionFormatErrorCount: u32,
    pub SessionFailureCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_NICPerformance {
    pub BytesTransmitted: u32,
    pub BytesReceived: u32,
    pub PDUTransmitted: u32,
    pub PDUReceived: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSiSCSI_RequestTimeStatistics {
    pub iSCSIName: [u32; 1],
    pub CID: u16,
    pub USID: u64,
    pub UniqueAdapterId: u64,
    pub MaximumProcessingTime: u32,
    pub AverageProcessingTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONNECT_DATA {
    pub ClassDeviceObject: usize,
    pub ClassService: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYWORDSELECTOR {
    pub KeywordId: u32,
    pub LangId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFNKMCLASSLIB_POWER_MANAGEMENT_PARAMETERS {
    pub Size: u32,
    pub LibraryPowerManaged: u8,
    pub PowerIdleTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KUSBFNCLASSLIB_PIPE_MEMORY {
    pub PipeMemory: u32,
    pub PipeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOWIO_PER_FCB_INFO {
    pub PagingIoReadsOutstanding: u32,
    pub PagingIoWritesOutstanding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCD_INIT_DATA {
    pub InitDataSize: u32,
    pub ChangerAdditionalExtensionSize: u32,
    pub ChangerInitialize: u32,
    pub ChangerError: u32,
    pub ChangerPerformDiagnostics: u32,
    pub ChangerGetParameters: u32,
    pub ChangerGetStatus: u32,
    pub ChangerGetProductData: u32,
    pub ChangerSetAccess: u32,
    pub ChangerGetElementStatus: u32,
    pub ChangerInitializeElementStatus: u32,
    pub ChangerSetPosition: u32,
    pub ChangerExchangeMedium: u32,
    pub ChangerMoveMedium: u32,
    pub ChangerReinitializeUnit: u32,
    pub ChangerQueryVolumeTags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_ELEMENT_ADDRESS_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PS: u8,
    pub PageLength: u8,
    pub MediumTransportElementAddress: [u8; 2],
    pub NumberTransportElements: [u8; 2],
    pub FirstStorageElementAddress: [u8; 2],
    pub NumberStorageElements: [u8; 2],
    pub FirstIEPortElementAddress: [u8; 2],
    pub NumberIEPortElements: [u8; 2],
    pub FirstDataXFerElementAddress: [u8; 2],
    pub NumberDataXFerElements: [u8; 2],
    pub Reserved2: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_TRANSPORT_GEOMETRY_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PS: u8,
    pub PageLength: u8,
    pub Flip: u8,
    pub Reserved2: u8,
    pub TransportElementNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DEVICE_CAPABILITIES_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PS: u8,
    pub PageLength: u8,
    pub MediumTransport: u8,
    pub StorageLocation: u8,
    pub IEPort: u8,
    pub DataXFer: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub MTtoMT: u8,
    pub MTtoST: u8,
    pub MTtoIE: u8,
    pub MTtoDT: u8,
    pub Reserved4: u8,
    pub STtoMT: u8,
    pub STtoST: u8,
    pub STtoIE: u8,
    pub STtoDT: u8,
    pub Reserved5: u8,
    pub IEtoMT: u8,
    pub IEtoST: u8,
    pub IEtoIE: u8,
    pub IEtoDT: u8,
    pub Reserved6: u8,
    pub DTtoMT: u8,
    pub DTtoST: u8,
    pub DTtoIE: u8,
    pub DTtoDT: u8,
    pub Reserved7: u8,
    pub Reserved8: [u8; 4],
    pub XMTtoMT: u8,
    pub XMTtoST: u8,
    pub XMTtoIE: u8,
    pub XMTtoDT: u8,
    pub Reserved9: u8,
    pub XSTtoMT: u8,
    pub XSTtoST: u8,
    pub XSTtoIE: u8,
    pub XSTtoDT: u8,
    pub Reserved10: u8,
    pub XIEtoMT: u8,
    pub XIEtoST: u8,
    pub XIEtoIE: u8,
    pub XIEtoDT: u8,
    pub Reserved11: u8,
    pub XDTtoMT: u8,
    pub XDTtoST: u8,
    pub XDTtoIE: u8,
    pub XDTtoDT: u8,
    pub Reserved12: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ELEMENT_STATUS_HEADER {
    pub FirstElementAddress: [u8; 2],
    pub NumberOfElements: [u8; 2],
    pub Reserved1: u8,
    pub ReportByteCount: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ELEMENT_STATUS_PAGE {
    pub ElementType: u8,
    pub Reserved1: u8,
    pub AVolTag: u8,
    pub PVolTag: u8,
    pub ElementDescriptorLength: [u8; 2],
    pub Reserved2: u8,
    pub DescriptorByteCount: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ELEMENT_DESCRIPTOR {
    pub ElementAddress: [u8; 2],
    pub Full: u8,
    pub Reserved1: u8,
    pub Exception: u8,
    pub Accessible: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub AdditionalSenseCode: u8,
    pub AddSenseCodeQualifier: u8,
    pub Lun: u8,
    pub Reserved4: u8,
    pub LunValid: u8,
    pub IdValid: u8,
    pub Reserved5: u8,
    pub NotThisBus: u8,
    pub BusAddress: u8,
    pub Reserved6: u8,
    pub Reserved7: u8,
    pub Invert: u8,
    pub SValid: u8,
    pub SourceStorageElementAddress: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCA_EXCEPTION {
    pub Dummy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_GUID {
    pub Data1: u32,
    pub Data2: u16,
    pub Data3: u16,
    pub Data4: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_RECORD_HEADER {
    pub Id: u64,
    pub Revision: u32,
    pub ErrorSeverity: u32,
    pub Valid: u32,
    pub Length: u32,
    pub TimeStamp: u32,
    pub OemPlatformId: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_SECTION_HEADER {
    pub Guid: u32,
    pub Revision: u32,
    pub RecoveryInfo: u32,
    pub Reserved: u8,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_MODINFO {
    pub Valid: u32,
    pub CheckInfo: u32,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub PreciseIP: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PROCESSOR_MS {
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PROCESSOR_CPUID_INFO {
    pub CpuId0: u64,
    pub CpuId1: u64,
    pub CpuId2: u64,
    pub CpuId3: u64,
    pub CpuId4: u64,
    pub Reserved: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PROCESSOR_STATIC_INFO {
    pub Valid: u32,
    pub MinState: [u8; 1],
    pub July_2000_and_Jan_2001_state_approximatively_____1024_: u8,
    pub reserved_padding: [u32; 8],
    pub reserved_padding_1: [u32; 1],
    pub July_2000_states_that_it_is_processor_dependent____128__: u32,
    pub reserved_padding_2: [u32; 1],
    pub July_2000_states_that_it_is_processor_dependent____128___1: u32,
    pub reserved_padding_3: [u32; 8],
    pub reserved_padding_4: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PROCESSOR {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorMap: u32,
    pub StateParameter: u32,
    pub CRLid: usize,
    pub reserved_padding: [u32; 1],
    pub reserved_padding_1: [u32; 1],
    pub reserved_padding_2: [u32; 1],
    pub reserved_padding_3: [u32; 1],
    pub reserved_padding_4: [u32; 1],
    pub CpuIdInfo: u32,
    pub StaticInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_OEM_DATA {
    pub Length: u16,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_MEMORY {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub PhysicalAddress: u64,
    pub PhysicalAddressMask: u64,
    pub Node: u16,
    pub Card: u16,
    pub Module: u16,
    pub Bank: u16,
    pub Device: u16,
    pub Row: u16,
    pub Column: u16,
    pub BitPosition: u16,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub BusSpecificData: u64,
    pub OemId: [u8; 16],
    pub OemData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PCI_BUS_TYPE {
    pub Type: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PCI_BUS_ID {
    pub BusNumber: u8,
    pub SegmentNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PCI_BUS {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub Type: u32,
    pub Id: u32,
    pub Reserved: [u8; 4],
    pub Address: u64,
    pub Data: u64,
    pub CmdType: u64,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub OemId: [u8; 16],
    pub OemData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PCI_COMPONENT_INFO {
    pub VendorId: u16,
    pub DeviceId: u16,
    pub ClassCodeInterface: u8,
    pub ClassCodeSubClass: u8,
    pub ClassCodeBaseClass: u8,
    pub FunctionNumber: u8,
    pub DeviceNumber: u8,
    pub BusNumber: u8,
    pub SegmentNumber: u8,
    pub Reserved0: u8,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PCI_COMPONENT {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub Info: u32,
    pub MemoryMappedRegistersPairs: u32,
    pub ProgrammedIORegistersPairs: u32,
    pub RegistersPairs: [u64; 1],
    pub OemData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_SYSTEM_EVENT_LOG {
    pub Header: u32,
    pub Valid: u32,
    pub RecordId: u16,
    pub RecordType: u8,
    pub TimeStamp: u32,
    pub GeneratorId: u16,
    pub EVMRevision: u8,
    pub SensorType: u8,
    pub SensorNumber: u8,
    pub EventDir: u8,
    pub Data1: u8,
    pub Data2: u8,
    pub Data3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_SMBIOS {
    pub Header: u32,
    pub Valid: u32,
    pub EventType: u32,
    pub Length: u8,
    pub TimeStamp: u32,
    pub OemData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PLATFORM_SPECIFIC {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub BusSpecificData: u32,
    pub OemId: [u8; 16],
    pub OemData: u32,
    pub OemDevicePath: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PLATFORM_BUS {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub BusSpecificData: u32,
    pub OemId: [u8; 16],
    pub OemData: u32,
    pub OemDevicePath: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_PLATFORM_HOST_CONTROLLER {
    pub Header: u32,
    pub Valid: u32,
    pub ErrorStatus: u32,
    pub RequestorId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub BusSpecificData: u32,
    pub OemId: [u8; 16],
    pub OemData: u32,
    pub OemDevicePath: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MF_RESOURCE_MAP {
    pub Count: u32,
    pub Resources: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MF_VARYING_RESOURCE_ENTRY {
    pub ResourceIndex: u8,
    pub Reserved: [u8; 3],
    pub Offset: u32,
    pub Size: u32,
    pub MaxCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MF_VARYING_RESOURCE_MAP {
    pub Count: u32,
    pub Resources: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MF_DEVICE_INFO {
    pub Name: u32,
    pub HardwareID: u32,
    pub CompatibleID: u32,
    pub ResourceMap: usize,
    pub VaryingResourceMap: usize,
    pub MfFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MF_ENUMERATION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub EnumerateChild: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_MID_ATLAS {
    pub MaximumNumberOfMids: u16,
    pub MidsAllocated: u16,
    pub NumberOfMidsInUse: u16,
    pub NumberOfMidsDiscarded: u16,
    pub MaximumMidFieldWidth: u16,
    pub Reserved: u16,
    pub MidQuantum: u16,
    pub MidQuantumFieldWidth: u8,
    pub NumberOfLevels: u8,
    pub MidMapFreeList: u32,
    pub MidMapExpansionList: u32,
    pub pRootMidMap: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUAD {
    pub UseThisFieldToCopy: i64,
    pub DoNotUseThisField: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_NUMBER {
    pub Group: u16,
    pub Number: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GROUP_AFFINITY {
    pub Mask: u32,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GROUP_AFFINITY32 {
    pub Mask: u32,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GROUP_AFFINITY64 {
    pub Mask: i64,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LARGE_INTEGER {
    pub QuadPart: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ULARGE_INTEGER {
    pub QuadPart: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FOO {
    pub Bar: [u8; 123],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENUM_FLAG_SIZED_INTEGER {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_DESCRIPTOR {
    pub Level: u8,
    pub Associativity: u8,
    pub LineSize: u16,
    pub Size: u32,
    pub Type: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_LOGICAL_PROCESSOR_INFORMATION {
    pub ProcessorMask: usize,
    pub Relationship: u32,
    pub Flags: u8,
    pub NodeNumber: u32,
    pub Cache: u32,
    pub Reserved: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_RELATIONSHIP {
    pub Flags: u8,
    pub EfficiencyClass: u8,
    pub Reserved: [u8; 20],
    pub GroupCount: u16,
    pub GroupMask: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NUMA_NODE_RELATIONSHIP {
    pub NodeNumber: u32,
    pub Reserved: [u8; 18],
    pub GroupCount: u16,
    pub GroupMask: u32,
    pub GroupMasks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_RELATIONSHIP {
    pub Level: u8,
    pub Associativity: u8,
    pub LineSize: u16,
    pub CacheSize: u32,
    pub Type: usize,
    pub Reserved: [u8; 18],
    pub GroupCount: u16,
    pub GroupMask: u32,
    pub GroupMasks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_GROUP_INFO {
    pub MaximumProcessorCount: u8,
    pub ActiveProcessorCount: u8,
    pub Reserved: [u8; 38],
    pub ActiveProcessorMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GROUP_RELATIONSHIP {
    pub MaximumGroupCount: u16,
    pub ActiveGroupCount: u16,
    pub Reserved: [u8; 20],
    pub GroupInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_LOGICAL_PROCESSOR_INFORMATION_EX {
    pub Relationship: u32,
    pub Size: u32,
    pub Processor: usize,
    pub NumaNode: u32,
    pub Cache: u32,
    pub Group: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_CPU_SET_INFORMATION {
    pub Size: u32,
    pub Type: u32,
    pub Id: u32,
    pub Group: u16,
    pub LogicalProcessorIndex: u8,
    pub CoreIndex: u8,
    pub LastLevelCacheIndex: u8,
    pub NumaNodeIndex: u8,
    pub EfficiencyClass: u8,
    pub AllFlags: u8,
    pub Parked: u8,
    pub Allocated: u8,
    pub AllocatedToTargetProcess: u8,
    pub RealTime: u8,
    pub ReservedFlags: u8,
    pub Reserved: u32,
    pub SchedulingClass: u8,
    pub AllocationTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_SEGMENT_BUS_NUMBER {
    pub BusNumber: u32,
    pub SegmentNumber: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_SLOT_NUMBER {
    pub DeviceNumber: u32,
    pub FunctionNumber: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_COMMON_HEADER {
    pub VendorID: u16,
    pub DeviceID: u16,
    pub Command: u16,
    pub Status: u16,
    pub RevisionID: u8,
    pub ProgIf: u8,
    pub SubClass: u8,
    pub BaseClass: u8,
    pub CacheLineSize: u8,
    pub LatencyTimer: u8,
    pub HeaderType: u8,
    pub BIST: u8,
    pub BaseAddresses: [u32; 1],
    pub CIS: u32,
    pub SubVendorID: u16,
    pub SubSystemID: u16,
    pub ROMBaseAddress: u32,
    pub CapabilitiesPtr: u8,
    pub Reserved1: [u8; 3],
    pub Reserved2: u32,
    pub InterruptLine: u8,
    pub InterruptPin: u8,
    pub MinimumGrant: u8,
    pub MaximumLatency: u8,
    pub BaseAddresses_1: [u32; 1],
    pub PrimaryBus: u8,
    pub SecondaryBus: u8,
    pub SubordinateBus: u8,
    pub SecondaryLatency: u8,
    pub IOBase: u8,
    pub IOLimit: u8,
    pub SecondaryStatus: u16,
    pub MemoryBase: u16,
    pub MemoryLimit: u16,
    pub PrefetchBase: u16,
    pub PrefetchLimit: u16,
    pub PrefetchBaseUpper32: u32,
    pub PrefetchLimitUpper32: u32,
    pub IOBaseUpper16: u16,
    pub IOLimitUpper16: u16,
    pub CapabilitiesPtr_1: u8,
    pub Reserved1_1: [u8; 3],
    pub ROMBaseAddress_1: u32,
    pub InterruptLine_1: u8,
    pub InterruptPin_1: u8,
    pub BridgeControl: u16,
    pub SocketRegistersBaseAddress: u32,
    pub CapabilitiesPtr_2: u8,
    pub Reserved: u8,
    pub SecondaryStatus_1: u16,
    pub PrimaryBus_1: u8,
    pub SecondaryBus_1: u8,
    pub SubordinateBus_1: u8,
    pub SecondaryLatency_1: u8,
    pub Base: u32,
    pub Limit: u32,
    pub Range: [u32; 1],
    pub InterruptLine_2: u8,
    pub InterruptPin_2: u8,
    pub BridgeControl_1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct type0 {
    pub BaseAddresses: [u32; 1],
    pub CIS: u32,
    pub SubVendorID: u16,
    pub SubSystemID: u16,
    pub ROMBaseAddress: u32,
    pub CapabilitiesPtr: u8,
    pub Reserved1: [u8; 3],
    pub Reserved2: u32,
    pub InterruptLine: u8,
    pub InterruptPin: u8,
    pub MinimumGrant: u8,
    pub MaximumLatency: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_COMMON_CONFIG {
    pub DUMMYSTRUCTNAME: usize,
    pub DeviceSpecific: [u8; 192],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_CAPABILITIES_HEADER {
    pub CapabilityID: u8,
    pub Next: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_PMC {
    pub Version: u8,
    pub PMEClock: u8,
    pub Rsvd1: u8,
    pub DeviceSpecificInitialization: u8,
    pub Rsvd2: u8,
    pub Rsvd2_1: u8,
    pub D1: u8,
    pub D2: u8,
    pub PMED0: u8,
    pub PMED1: u8,
    pub PMED2: u8,
    pub PMED3Hot: u8,
    pub PMED3Cold: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Support {
    pub Rsvd2: u8,
    pub D1: u8,
    pub D2: u8,
    pub PMED0: u8,
    pub PMED1: u8,
    pub PMED2: u8,
    pub PMED3Hot: u8,
    pub PMED3Cold: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_PMCSR {
    pub PowerState: u16,
    pub Rsvd1: u16,
    pub NoSoftReset: u16,
    pub Rsvd2: u16,
    pub PMEEnable: u16,
    pub DataSelect: u16,
    pub DataScale: u16,
    pub PMEStatus: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_PMCSR_BSE {
    pub Rsvd1: u8,
    pub D3HotSupportsStopClock: u8,
    pub BusPowerClockControlEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_PM_CAPABILITY {
    pub Header: usize,
    pub Capabilities: usize,
    pub AsUSHORT: u16,
    pub ControlStatus: usize,
    pub AsUSHORT_1: u16,
    pub BridgeSupport: usize,
    pub AsUCHAR: u8,
    pub Data: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_AGP_CAPABILITY {
    pub Header: usize,
    pub Minor: u16,
    pub Major: u16,
    pub Rsvd1: u16,
    pub Rate: u32,
    pub Agp3Mode: u32,
    pub FastWrite: u32,
    pub FourGB: u32,
    pub HostTransDisable: u32,
    pub Gart64: u32,
    pub ITA_Coherent: u32,
    pub SideBandAddressing: u32,
    pub CalibrationCycle: u32,
    pub AsyncRequestSize: u32,
    pub Rsvd1_1: u32,
    pub Isoch: u32,
    pub Rsvd2: u32,
    pub RequestQueueDepthMaximum: u32,
    pub Rate_1: u32,
    pub Rsvd1_2: u32,
    pub FastWriteEnable: u32,
    pub FourGBEnable: u32,
    pub Rsvd2_1: u32,
    pub Gart64_1: u32,
    pub AGPEnable: u32,
    pub SBAEnable: u32,
    pub CalibrationCycle_1: u32,
    pub AsyncReqSize: u32,
    pub Rsvd3: u32,
    pub RequestQueueDepth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGPStatus {
    pub Rate: u32,
    pub Agp3Mode: u32,
    pub FastWrite: u32,
    pub FourGB: u32,
    pub HostTransDisable: u32,
    pub Gart64: u32,
    pub ITA_Coherent: u32,
    pub SideBandAddressing: u32,
    pub CalibrationCycle: u32,
    pub AsyncRequestSize: u32,
    pub Rsvd1: u32,
    pub Isoch: u32,
    pub Rsvd2: u32,
    pub RequestQueueDepthMaximum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGPCommand {
    pub Rate: u32,
    pub Rsvd1: u32,
    pub FastWriteEnable: u32,
    pub FourGBEnable: u32,
    pub Rsvd2: u32,
    pub Gart64: u32,
    pub AGPEnable: u32,
    pub SBAEnable: u32,
    pub CalibrationCycle: u32,
    pub AsyncReqSize: u32,
    pub Rsvd3: u32,
    pub RequestQueueDepth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_AGP_ISOCH_STATUS {
    pub ErrorCode: u32,
    pub Rsvd1: u32,
    pub Isoch_L: u32,
    pub Isoch_Y: u32,
    pub Isoch_N: u32,
    pub Rsvd2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_AGP_CONTROL {
    pub Rsvd1: u32,
    pub GTLB_Enable: u32,
    pub AP_Enable: u32,
    pub CAL_Disable: u32,
    pub Rsvd2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_AGP_APERTURE_PAGE_SIZE {
    pub PageSizeMask: u16,
    pub Rsvd1: u16,
    pub PageSizeSelect: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_AGP_ISOCH_COMMAND {
    pub Rsvd1: u16,
    pub Isoch_Y: u16,
    pub Isoch_N: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCIX_BRIDGE_CAPABILITY {
    pub Header: usize,
    pub Bus64Bit: u16,
    pub Bus133MHzCapable: u16,
    pub SplitCompletionDiscarded: u16,
    pub UnexpectedSplitCompletion: u16,
    pub SplitCompletionOverrun: u16,
    pub SplitRequestDelayed: u16,
    pub BusModeFrequency: u16,
    pub Rsvd: u16,
    pub Version: u16,
    pub Bus266MHzCapable: u16,
    pub Bus533MHzCapable: u16,
    pub AsUSHORT: u16,
    pub FunctionNumber: u32,
    pub DeviceNumber: u32,
    pub BusNumber: u32,
    pub Device64Bit: u32,
    pub Device133MHzCapable: u32,
    pub SplitCompletionDiscarded_1: u32,
    pub UnexpectedSplitCompletion_1: u32,
    pub SplitCompletionOverrun_1: u32,
    pub SplitRequestDelayed_1: u32,
    pub Rsvd_1: u32,
    pub DIMCapable: u32,
    pub Device266MHzCapable: u32,
    pub Device533MHzCapable: u32,
    pub AsULONG: u32,
    pub UpstreamSplitTransactionCapacity: u16,
    pub UpstreamSplitTransactionLimit: u16,
    pub DownstreamSplitTransactionCapacity: u16,
    pub DownstreamSplitTransactionLimit: u16,
    pub SelectSecondaryRegisters: u32,
    pub ErrorPresentInOtherBank: u32,
    pub AdditionalCorrectableError: u32,
    pub AdditionalUncorrectableError: u32,
    pub ErrorPhase: u32,
    pub ErrorCorrected: u32,
    pub Syndrome: u32,
    pub ErrorFirstCommand: u32,
    pub ErrorSecondCommand: u32,
    pub ErrorUpperAttributes: u32,
    pub ControlUpdateEnable: u32,
    pub Rsvd_2: u32,
    pub DisableSingleBitCorrection: u32,
    pub EccMode: u32,
    pub AsULONG_1: u32,
    pub EccFirstAddress: u32,
    pub EccSecondAddress: u32,
    pub EccAttribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_SUBSYSTEM_IDS_CAPABILITY {
    pub Header: usize,
    pub Reserved: u16,
    pub SubVendorID: u16,
    pub SubSystemID: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_ADVANCED_FEATURES_CAPABILITY {
    pub Header: usize,
    pub Length: u8,
    pub FunctionLevelResetSupported: u8,
    pub TransactionsPendingSupported: u8,
    pub Rsvd: u8,
    pub AsUCHAR: u8,
    pub InitiateFunctionLevelReset: u8,
    pub Rsvd_1: u8,
    pub AsUCHAR_1: u8,
    pub TransactionsPending: u8,
    pub Rsvd_2: u8,
    pub AsUCHAR_2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_ROOT_BUS_OSC_SUPPORT_FIELD {
    pub ExtendedConfigOpRegions: u32,
    pub ActiveStatePowerManagement: u32,
    pub ClockPowerManagement: u32,
    pub SegmentGroups: u32,
    pub MessageSignaledInterrupts: u32,
    pub OptimizedBufferFlushAndFill: u32,
    pub AspmOptionality: u32,
    pub ErrorDisconnectRecovery: u32,
    pub HpxDescriptorRecord: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_ROOT_BUS_OSC_CONTROL_FIELD {
    pub ExpressNativeHotPlug: u32,
    pub ShpcNativeHotPlug: u32,
    pub ExpressNativePME: u32,
    pub ExpressAdvancedErrorReporting: u32,
    pub ExpressCapabilityStructure: u32,
    pub LatencyToleranceReporting: u32,
    pub AllOnesMmioInvalidToDrivers: u32,
    pub DownstreamPortContainment: u32,
    pub CompletionTimeout: u32,
    pub FirmwareIntermediaryConfig: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FIRMWARE_BUS_CAPS {
    pub Type: u16,
    pub Length: u16,
    pub SixtyFourBitDevice: u8,
    pub PciXMode1EccCapable: u8,
    pub DeviceIdMessagingCapable: u8,
    pub ObffWakeSignalCapable: u8,
    pub Reserved1: u8,
    pub CurrentSpeedAndMode: u8,
    pub SupportedSpeedsAndModesLowByte: u8,
    pub SupportedSpeedsAndModesHighByte: u8,
    pub Voltage: u8,
    pub Reserved2: [u8; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FIRMWARE_BUS_CAPS_RETURN_BUFFER {
    pub Version: u16,
    pub Status: u16,
    pub Length: u32,
    pub Caps: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_ROOT_BUS_HARDWARE_CAPABILITY {
    pub SecondaryInterface: usize,
    pub BusCapabilitiesFound: u8,
    pub CurrentSpeedAndMode: u32,
    pub SupportedSpeedsAndModes: u32,
    pub DeviceIDMessagingCapable: u8,
    pub SecondaryBusWidth: usize,
    pub OscFeatureSupport: usize,
    pub OscControlRequest: usize,
    pub OscControlGranted: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FPB_CAPABILITY_HEADER {
    pub Header: usize,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FPB_MEM_HIGH_VECTOR_CONTROL2_REGISTER {
    pub MemHighVectorStartUpper: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FPB_VECTOR_ACCESS_DATA_REGISTER {
    pub VectorAccessData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_FPB_CAPABILITY {
    pub Header: usize,
    pub CapabilitiesRegister: usize,
    pub RidVectorControl1Register: usize,
    pub RidVectorControl2Register: usize,
    pub MemLowVectorControlRegister: usize,
    pub MemHighVectorControl1Register: usize,
    pub MemHighVectorControl2Register: usize,
    pub VectorAccessControlRegister: usize,
    pub VectorAccessDataRegister: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_VENDOR_SPECIFIC_CAPABILITY {
    pub Header: usize,
    pub VscLength: u8,
    pub VendorSpecific: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_CAPABILITY {
    pub Header: usize,
    pub ExpressCapabilities: usize,
    pub DeviceCapabilities: usize,
    pub DeviceControl: usize,
    pub DeviceStatus: usize,
    pub LinkCapabilities: usize,
    pub LinkControl: usize,
    pub LinkStatus: usize,
    pub SlotCapabilities: usize,
    pub SlotControl: usize,
    pub SlotStatus: usize,
    pub RootControl: usize,
    pub RootCapabilities: usize,
    pub RootStatus: usize,
    pub DeviceCapabilities2: usize,
    pub DeviceControl2: usize,
    pub DeviceStatus2: usize,
    pub LinkCapabilities2: usize,
    pub LinkControl2: usize,
    pub LinkStatus2: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ENHANCED_CAPABILITY_HEADER {
    pub CapabilityID: u16,
    pub Version: u16,
    pub Next: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_VENDOR_SPECIFIC_CAPABILITY {
    pub Header: usize,
    pub VsecId: u16,
    pub VsecRev: u16,
    pub VsecLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_SERIAL_NUMBER_CAPABILITY {
    pub Header: usize,
    pub LowSerialNumber: u32,
    pub HighSerialNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ARI_CAPABILITY_REGISTER {
    pub MfvcFunctionGroupsCapability: u16,
    pub AcsFunctionGroupsCapability: u16,
    pub Reserved: u16,
    pub NextFunctionNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ARI_CONTROL_REGISTER {
    pub MfvcFunctionGroupsEnable: u16,
    pub AcsFunctionGroupsEnable: u16,
    pub Reserved1: u16,
    pub FunctionGroup: u16,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ARI_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUAL_RESOURCE {
    pub Capability: u32,
    pub Control: u32,
    pub RsvdP: u16,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_VIRTUAL_CHANNEL_CAPABILITY {
    pub Header: usize,
    pub Capabilities1: u32,
    pub Capabilities2: u32,
    pub Control: u32,
    pub Status: u32,
    pub Resource: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ATS_CAPABILITY_REGISTER {
    pub InvalidateQueueDepth: u16,
    pub PageAlignedRequest: u16,
    pub GlobalInvalidateSupported: u16,
    pub RelaxedOrderingSupported: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ATS_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_PASID_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_PRI_CAPABILITY {
    pub Header: usize,
    pub Control: usize,
    pub Status: usize,
    pub PRCapacity: u32,
    pub PRAllocation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_DEVICE_3_EXTENDED_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_PTM_CAPABILITY {
    pub Header: usize,
    pub PtmCapability: usize,
    pub PtmControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_AER_CAPABILITY {
    pub Header: usize,
    pub UncorrectableErrorStatus: usize,
    pub UncorrectableErrorMask: usize,
    pub UncorrectableErrorSeverity: usize,
    pub CorrectableErrorStatus: usize,
    pub CorrectableErrorMask: usize,
    pub CapabilitiesAndControl: usize,
    pub HeaderLog: [u32; 4],
    pub SecUncorrectableErrorStatus: usize,
    pub SecUncorrectableErrorMask: usize,
    pub SecUncorrectableErrorSeverity: usize,
    pub SecCapabilitiesAndControl: usize,
    pub SecHeaderLog: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ROOTPORT_AER_CAPABILITY {
    pub Header: usize,
    pub UncorrectableErrorStatus: usize,
    pub UncorrectableErrorMask: usize,
    pub UncorrectableErrorSeverity: usize,
    pub CorrectableErrorStatus: usize,
    pub CorrectableErrorMask: usize,
    pub CapabilitiesAndControl: usize,
    pub HeaderLog: [u32; 4],
    pub RootErrorCommand: usize,
    pub RootErrorStatus: usize,
    pub ErrorSourceId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_BRIDGE_AER_CAPABILITY {
    pub Header: usize,
    pub UncorrectableErrorStatus: usize,
    pub UncorrectableErrorMask: usize,
    pub UncorrectableErrorSeverity: usize,
    pub CorrectableErrorStatus: usize,
    pub CorrectableErrorMask: usize,
    pub CapabilitiesAndControl: usize,
    pub HeaderLog: [u32; 4],
    pub SecUncorrectableErrorStatus: usize,
    pub SecUncorrectableErrorMask: usize,
    pub SecUncorrectableErrorSeverity: usize,
    pub SecCapabilitiesAndControl: usize,
    pub SecHeaderLog: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ACS_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
    pub EgressControl: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_SRIOV_CAPABILITY {
    pub Header: usize,
    pub SRIOVCapabilities: usize,
    pub SRIOVControl: usize,
    pub SRIOVStatus: usize,
    pub InitialVFs: u16,
    pub TotalVFs: u16,
    pub NumVFs: u16,
    pub FunctionDependencyLink: u8,
    pub RsvdP1: u8,
    pub FirstVFOffset: u16,
    pub VFStride: u16,
    pub RsvdP2: u16,
    pub VFDeviceId: u16,
    pub SupportedPageSizes: u32,
    pub SystemPageSize: u32,
    pub BaseAddresses: [u32; 1],
    pub VFMigrationStateArrayOffset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_MCA_POS_DATA {
    pub AdapterId: u16,
    pub PosData1: u8,
    pub PosData2: u8,
    pub PosData3: u8,
    pub PosData4: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_MEMORY_TYPE {
    pub ReadWrite: u8,
    pub Cached: u8,
    pub Reserved0: u8,
    pub Type: u8,
    pub Shared: u8,
    pub Reserved1: u8,
    pub MoreEntries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_MEMORY_CONFIGURATION {
    pub ConfigurationByte: u32,
    pub DataSize: u8,
    pub AddressLowWord: u16,
    pub AddressHighByte: u8,
    pub MemorySize: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_IRQ_DESCRIPTOR {
    pub Interrupt: u8,
    pub Reserved: u8,
    pub LevelTriggered: u8,
    pub Shared: u8,
    pub MoreEntries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_IRQ_CONFIGURATION {
    pub ConfigurationByte: u32,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_CONFIGURATION_BYTE0 {
    pub Channel: u8,
    pub Reserved: u8,
    pub Shared: u8,
    pub MoreEntries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_CONFIGURATION_BYTE1 {
    pub Reserved0: u8,
    pub TransferSize: u8,
    pub Timing: u8,
    pub Reserved1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_DMA_CONFIGURATION {
    pub ConfigurationByte0: u32,
    pub ConfigurationByte1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_PORT_DESCRIPTOR {
    pub NumberPorts: u8,
    pub Reserved: u8,
    pub Shared: u8,
    pub MoreEntries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EISA_PORT_CONFIGURATION {
    pub Configuration: u32,
    pub PortAddress: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_EISA_SLOT_INFORMATION {
    pub ReturnCode: u8,
    pub ReturnFlags: u8,
    pub MajorRevision: u8,
    pub MinorRevision: u8,
    pub Checksum: u16,
    pub NumberFunctions: u8,
    pub FunctionInformation: u8,
    pub CompressedId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_EISA_FUNCTION_INFORMATION {
    pub CompressedId: u32,
    pub IdSlotFlags1: u8,
    pub IdSlotFlags2: u8,
    pub MinorRevision: u8,
    pub MajorRevision: u8,
    pub Selections: [u8; 26],
    pub FunctionFlags: u8,
    pub TypeString: [u8; 80],
    pub EisaMemory: [u32; 9],
    pub EisaIrq: [u32; 7],
    pub EisaDma: [u32; 4],
    pub EisaPort: [u32; 20],
    pub InitializationData: [u8; 60],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_PNP_BIOS_DEVICE_NODE {
    pub Size: u16,
    pub Node: u8,
    pub ProductId: u32,
    pub DeviceType: [u8; 3],
    pub DeviceAttributes: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_PNP_BIOS_INSTALLATION_CHECK {
    pub Signature: [u8; 4],
    pub Revision: u8,
    pub Length: u8,
    pub ControlField: u16,
    pub Checksum: u8,
    pub EventFlagAddress: u32,
    pub RealModeEntryOffset: u16,
    pub RealModeEntrySegment: u16,
    pub ProtectedModeEntryOffset: u16,
    pub ProtectedModeCodeBaseAddress: u32,
    pub OemDeviceId: u32,
    pub RealModeDataBaseAddress: u16,
    pub ProtectedModeDataBaseAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_RESOURCE_DESCRIPTOR {
    pub Option: u8,
    pub Type: u8,
    pub ShareDisposition: u8,
    pub Spare1: u8,
    pub Flags: u16,
    pub Spare2: u16,
    pub Length: u32,
    pub Alignment: u32,
    pub MinimumAddress: u64,
    pub MaximumAddress: u64,
    pub Length_1: u32,
    pub Alignment_1: u32,
    pub MinimumAddress_1: u64,
    pub MaximumAddress_1: u64,
    pub MinimumVector: u32,
    pub MaximumVector: u32,
    pub AffinityPolicy: u32,
    pub Group: u16,
    pub AffinityPolicy_1: u32,
    pub PriorityPolicy: u32,
    pub TargetedProcessors: u32,
    pub MinimumChannel: u32,
    pub MaximumChannel: u32,
    pub RequestLine: u32,
    pub Reserved: u32,
    pub Channel: u32,
    pub TransferWidth: u32,
    pub Length_2: u32,
    pub Alignment_2: u32,
    pub MinimumAddress_2: u64,
    pub MaximumAddress_2: u64,
    pub Data: [u32; 3],
    pub Length_3: u32,
    pub MinBusNumber: u32,
    pub MaxBusNumber: u32,
    pub Reserved_1: u32,
    pub Priority: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub Length40: u32,
    pub Alignment40: u32,
    pub MinimumAddress_3: u64,
    pub MaximumAddress_3: u64,
    pub Length48: u32,
    pub Alignment48: u32,
    pub MinimumAddress_4: u64,
    pub MaximumAddress_4: u64,
    pub Length64: u32,
    pub Alignment64: u32,
    pub MinimumAddress_5: u64,
    pub MaximumAddress_5: u64,
    pub Class: u8,
    pub Type_1: u8,
    pub Reserved1_1: u8,
    pub Reserved2_1: u8,
    pub IdLowPart: u32,
    pub IdHighPart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_RESOURCE_LIST {
    pub Version: u16,
    pub Revision: u16,
    pub Count: u32,
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_RESOURCE_REQUIREMENTS_LIST {
    pub ListSize: u32,
    pub InterfaceType: u32,
    pub BusNumber: u32,
    pub SlotNumber: u32,
    pub Reserved: [u32; 3],
    pub AlternativeLists: u32,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_ERASE {
    pub Type: u32,
    pub Immediate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_PREPARE {
    pub Operation: u32,
    pub Immediate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_WRITE_MARKS {
    pub Type: u32,
    pub Count: u32,
    pub Immediate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_GET_POSITION {
    pub Type: u32,
    pub Partition: u32,
    pub Offset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_SET_POSITION {
    pub Method: u32,
    pub Partition: u32,
    pub Offset: i64,
    pub Immediate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_GET_DRIVE_PARAMETERS {
    pub ECC: u8,
    pub Compression: u8,
    pub DataPadding: u8,
    pub ReportSetmarks: u8,
    pub DefaultBlockSize: u32,
    pub MaximumBlockSize: u32,
    pub MinimumBlockSize: u32,
    pub MaximumPartitionCount: u32,
    pub FeaturesLow: u32,
    pub FeaturesHigh: u32,
    pub EOTWarningZoneSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_SET_DRIVE_PARAMETERS {
    pub ECC: u8,
    pub Compression: u8,
    pub DataPadding: u8,
    pub ReportSetmarks: u8,
    pub EOTWarningZoneSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_GET_MEDIA_PARAMETERS {
    pub Capacity: i64,
    pub Remaining: i64,
    pub BlockSize: u32,
    pub PartitionCount: u32,
    pub WriteProtected: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_SET_MEDIA_PARAMETERS {
    pub BlockSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_CREATE_PARTITION {
    pub Method: u32,
    pub Count: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_WMI_OPERATIONS {
    pub Method: u32,
    pub DataBufferSize: u32,
    pub DataBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_STATISTICS {
    pub Version: u32,
    pub Flags: u32,
    pub RecoveredWrites: i64,
    pub UnrecoveredWrites: i64,
    pub RecoveredReads: i64,
    pub UnrecoveredReads: i64,
    pub CompressionRatioReads: u8,
    pub CompressionRatioWrites: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_GET_STATISTICS {
    pub Operation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_MEDIA_INFO {
    pub Cylinders: i64,
    pub MediaType: u32,
    pub TracksPerCylinder: u32,
    pub SectorsPerTrack: u32,
    pub BytesPerSector: u32,
    pub NumberMediaSides: u32,
    pub MediaCharacteristics: u32,
    pub Cylinders_1: i64,
    pub MediaType_1: u32,
    pub TracksPerCylinder_1: u32,
    pub SectorsPerTrack_1: u32,
    pub BytesPerSector_1: u32,
    pub NumberMediaSides_1: u32,
    pub MediaCharacteristics_1: u32,
    pub MediaType_2: u32,
    pub MediaCharacteristics_2: u32,
    pub CurrentBlockSize: u32,
    pub BusType: u32,
    pub MediumType: u8,
    pub DensityCode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_MEDIA_TYPES {
    pub DeviceType: u32,
    pub MediaInfoCount: u32,
    pub MediaInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PREDICT_FAILURE {
    pub PredictFailure: u32,
    pub VendorSpecific: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FAILURE_PREDICTION_CONFIG {
    pub Version: u32,
    pub Size: u32,
    pub Set: u8,
    pub Enabled: u8,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_REQUEST_BLOCK {
    pub Length: u16,
    pub Function: u8,
    pub SrbStatus: u8,
    pub ScsiStatus: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub QueueTag: u8,
    pub QueueAction: u8,
    pub CdbLength: u8,
    pub SenseInfoBufferLength: u8,
    pub SrbFlags: u32,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: usize,
    pub SenseInfoBuffer: usize,
    pub field_NextSrb: u32,
    pub OriginalRequest: usize,
    pub SrbExtension: usize,
    pub InternalStatus: u32,
    pub QueueSortKey: u32,
    pub LinkTimeoutValue: u32,
    pub Reserved: u32,
    pub Cdb: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_WMI_REQUEST_BLOCK {
    pub Length: u16,
    pub Function: u8,
    pub SrbStatus: u8,
    pub WMISubFunction: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub Reserved1: u8,
    pub WMIFlags: u8,
    pub Reserved2: [u8; 2],
    pub SrbFlags: u32,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: usize,
    pub DataPath: usize,
    pub Reserved3: usize,
    pub OriginalRequest: usize,
    pub SrbExtension: usize,
    pub Reserved4: u32,
    pub Reserved6: u32,
    pub Reserved5: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_POWER_REQUEST_BLOCK {
    pub Length: u16,
    pub Function: u8,
    pub SrbStatus: u8,
    pub SrbPowerFlags: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub DevicePowerState: u32,
    pub SrbFlags: u32,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: usize,
    pub SenseInfoBuffer: usize,
    pub field_NextSrb: u32,
    pub OriginalRequest: usize,
    pub SrbExtension: usize,
    pub PowerAction: u32,
    pub Reserved: u32,
    pub Reserved5: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DEVICE_CAPABILITIES {
    pub Version: u16,
    pub DeviceD1: u32,
    pub DeviceD2: u32,
    pub LockSupported: u32,
    pub EjectSupported: u32,
    pub Removable: u32,
    pub DockDevice: u32,
    pub UniqueID: u32,
    pub SilentInstall: u32,
    pub SurpriseRemovalOK: u32,
    pub NoDisplayInUI: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DEVICE_CAPABILITIES_EX {
    pub Version: u16,
    pub Size: u16,
    pub DeviceD1: u32,
    pub DeviceD2: u32,
    pub LockSupported: u32,
    pub EjectSupported: u32,
    pub Removable: u32,
    pub DockDevice: u32,
    pub UniqueID: u32,
    pub SilentInstall: u32,
    pub RawDeviceOK: u32,
    pub SurpriseRemovalOK: u32,
    pub NoDisplayInUI: u32,
    pub DefaultWriteCacheEnabled: u32,
    pub Reserved0: u32,
    pub Address: u32,
    pub UINumber: u32,
    pub Reserved1: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PNP_REQUEST_BLOCK {
    pub Length: u16,
    pub Function: u8,
    pub SrbStatus: u8,
    pub PnPSubFunction: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub PnPAction: u32,
    pub SrbFlags: u32,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: usize,
    pub SenseInfoBuffer: usize,
    pub field_NextSrb: u32,
    pub OriginalRequest: usize,
    pub SrbExtension: usize,
    pub SrbPnPFlags: u32,
    pub Reserved: u32,
    pub Reserved4: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6GENERIC {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub CommandUniqueBits: u8,
    pub LogicalUnitNumber: u8,
    pub CommandUniqueBytes: [u8; 3],
    pub Link: u8,
    pub Flag: u8,
    pub Reserved: u8,
    pub VendorUnique: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6READWRITE {
    pub OperationCode: u8,
    pub LogicalBlockMsb1: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlockMsb0: u8,
    pub LogicalBlockLsb: u8,
    pub TransferBlocks: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6INQUIRY {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub PageCode: u8,
    pub IReserved: u8,
    pub AllocationLength: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6INQUIRY3 {
    pub OperationCode: u8,
    pub EnableVitalProductData: u8,
    pub CommandSupportData: u8,
    pub Reserved1: u8,
    pub PageCode: u8,
    pub Reserved2: u8,
    pub AllocationLength: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6VERIFY {
    pub OperationCode: u8,
    pub Fixed: u8,
    pub ByteCompare: u8,
    pub Immediate: u8,
    pub Reserved: u8,
    pub LogicalUnitNumber: u8,
    pub VerificationLength: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECEIVE_DIAGNOSTIC {
    pub OperationCode: u8,
    pub PageCodeValid: u8,
    pub Reserved: u8,
    pub PageCode: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_DIAGNOSTIC {
    pub OperationCode: u8,
    pub UnitOffline: u8,
    pub DeviceOffline: u8,
    pub SelfTest: u8,
    pub Reserved1: u8,
    pub PageFormat: u8,
    pub SelfTestCode: u8,
    pub Reserved2: u8,
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6FORMAT {
    pub OperationCode: u8,
    pub FormatControl: u8,
    pub LogicalUnitNumber: u8,
    pub FReserved1: u8,
    pub InterleaveMsb: u8,
    pub InterleaveLsb: u8,
    pub FReserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB10 {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlockByte0: u8,
    pub LogicalBlockByte1: u8,
    pub LogicalBlockByte2: u8,
    pub LogicalBlockByte3: u8,
    pub Reserved2: u8,
    pub TransferBlocksMsb: u8,
    pub TransferBlocksLsb: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB12 {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlock: [u8; 4],
    pub TransferLength: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB16 {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub Protection: u8,
    pub LogicalBlock: [u8; 8],
    pub TransferLength: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_BUFFER_10 {
    pub OperationCode: u8,
    pub Mode: u8,
    pub ModeSpecific: u8,
    pub BufferId: u8,
    pub BufferOffset: [u8; 3],
    pub AllocationLength: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_BUFFER_16 {
    pub OperationCode: u8,
    pub Mode: u8,
    pub ModeSpecific: u8,
    pub BufferOffset: [u8; 8],
    pub AllocationLength: [u8; 4],
    pub BufferId: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_PROTOCOL_IN {
    pub OperationCode: u8,
    pub SecurityProtocol: u8,
    pub SecurityProtocolSpecific: [u8; 2],
    pub Reserved1: u8,
    pub INC_512: u8,
    pub Reserved2: u8,
    pub AllocationLength: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_PROTOCOL_OUT {
    pub OperationCode: u8,
    pub SecurityProtocol: u8,
    pub SecurityProtocolSpecific: [u8; 2],
    pub Reserved1: u8,
    pub INC_512: u8,
    pub Reserved2: u8,
    pub AllocationLength: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNMAP {
    pub OperationCode: u8,
    pub Anchor: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 4],
    pub GroupNumber: u8,
    pub Reserved3: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SANITIZE {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub AUSE: u8,
    pub Reserved1: u8,
    pub Immediate: u8,
    pub Reserved2: [u8; 5],
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAUSE_RESUME {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 6],
    pub Action: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_TOC {
    pub OperationCode: u8,
    pub Reserved0: u8,
    pub Msf: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Format2: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 3],
    pub StartingTrack: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
    pub Format: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_DISK_INFORMATION {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 5],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_TRACK_INFORMATION {
    pub OperationCode: u8,
    pub Track: u8,
    pub Reserved4: u8,
    pub Lun: u8,
    pub BlockAddress: [u8; 4],
    pub Reserved3: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESERVE_TRACK_RZONE {
    pub OperationCode: u8,
    pub Reserved1: [u8; 4],
    pub ReservationSize: [u8; 4],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_OPC_INFORMATION {
    pub OperationCode: u8,
    pub DoOpc: u8,
    pub Reserved1: u8,
    pub Exclude0: u8,
    pub Exclude1: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 4],
    pub ParameterListLength: [u8; 2],
    pub Reserved4: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPAIR_TRACK {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 2],
    pub TrackNumber: [u8; 2],
    pub Reserved3: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLOSE_TRACK {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub Track: u8,
    pub Session: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub TrackNumber: [u8; 2],
    pub Reserved4: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_BUFFER_CAPACITY {
    pub OperationCode: u8,
    pub BlockInfo: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 5],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_CUE_SHEET {
    pub OperationCode: u8,
    pub Reserved: [u8; 5],
    pub CueSheetSize: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_HEADER {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Msf: u8,
    pub Reserved2: u8,
    pub Lun: u8,
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved3: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLAY_AUDIO {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub StartingBlockAddress: [u8; 4],
    pub Reserved2: u8,
    pub PlayLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLAY_AUDIO_MSF {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: u8,
    pub StartingM: u8,
    pub StartingS: u8,
    pub StartingF: u8,
    pub EndingM: u8,
    pub EndingS: u8,
    pub EndingF: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BLANK_MEDIA {
    pub OperationCode: u8,
    pub BlankType: u8,
    pub Reserved1: u8,
    pub Immediate: u8,
    pub Reserved2: u8,
    pub AddressOrTrack: [u8; 4],
    pub Reserved3: [u8; 5],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLAY_CD {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub CMSF: u8,
    pub ExpectedSectorType: u8,
    pub Lun: u8,
    pub StartingBlockAddress: [u8; 4],
    pub PlayLength: [u8; 4],
    pub Reserved1_1: u8,
    pub StartingM: u8,
    pub StartingS: u8,
    pub StartingF: u8,
    pub EndingM: u8,
    pub EndingS: u8,
    pub EndingF: u8,
    pub Reserved2: u8,
    pub Audio: u8,
    pub Composite: u8,
    pub Port1: u8,
    pub Port2: u8,
    pub Reserved2_1: u8,
    pub Speed: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LBA {
    pub StartingBlockAddress: [u8; 4],
    pub PlayLength: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSF {
    pub Reserved1: u8,
    pub StartingM: u8,
    pub StartingS: u8,
    pub StartingF: u8,
    pub EndingM: u8,
    pub EndingS: u8,
    pub EndingF: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCAN_CD {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub Direct: u8,
    pub Lun: u8,
    pub StartingAddress: [u8; 4],
    pub Reserved2: [u8; 3],
    pub Reserved3: u8,
    pub Type: u8,
    pub Reserved4: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOP_PLAY_SCAN {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 7],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUBCHANNEL {
    pub OperationCode: u8,
    pub Reserved0: u8,
    pub Msf: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: u8,
    pub SubQ: u8,
    pub Reserved3: u8,
    pub Format: u8,
    pub Reserved4: [u8; 2],
    pub TrackNumber: u8,
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CD {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved0: u8,
    pub ExpectedSectorType: u8,
    pub Lun: u8,
    pub StartingLBA: [u8; 4],
    pub TransferBlocks: [u8; 3],
    pub Reserved2: u8,
    pub ErrorFlags: u8,
    pub IncludeEDC: u8,
    pub IncludeUserData: u8,
    pub HeaderCode: u8,
    pub IncludeSyncData: u8,
    pub SubChannelSelection: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CD_MSF {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub ExpectedSectorType: u8,
    pub Lun: u8,
    pub Reserved2: u8,
    pub StartingM: u8,
    pub StartingS: u8,
    pub StartingF: u8,
    pub EndingM: u8,
    pub EndingS: u8,
    pub EndingF: u8,
    pub Reserved4: u8,
    pub ErrorFlags: u8,
    pub IncludeEDC: u8,
    pub IncludeUserData: u8,
    pub HeaderCode: u8,
    pub IncludeSyncData: u8,
    pub SubChannelSelection: u8,
    pub Reserved5: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLXTR_READ_CDDA {
    pub OperationCode: u8,
    pub Reserved0: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlockByte0: u8,
    pub LogicalBlockByte1: u8,
    pub LogicalBlockByte2: u8,
    pub LogicalBlockByte3: u8,
    pub TransferBlockByte0: u8,
    pub TransferBlockByte1: u8,
    pub TransferBlockByte2: u8,
    pub TransferBlockByte3: u8,
    pub SubCode: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NEC_READ_CDDA {
    pub OperationCode: u8,
    pub Reserved0: u8,
    pub LogicalBlockByte0: u8,
    pub LogicalBlockByte1: u8,
    pub LogicalBlockByte2: u8,
    pub LogicalBlockByte3: u8,
    pub Reserved1: u8,
    pub TransferBlockByte0: u8,
    pub TransferBlockByte1: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_SENSE {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Dbd: u8,
    pub Reserved2: u8,
    pub LogicalUnitNumber: u8,
    pub PageCode: u8,
    pub Pc: u8,
    pub Reserved3: u8,
    pub AllocationLength: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_SENSE10 {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Dbd: u8,
    pub Reserved2: u8,
    pub LogicalUnitNumber: u8,
    pub PageCode: u8,
    pub Pc: u8,
    pub Reserved3: [u8; 4],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_SELECT {
    pub OperationCode: u8,
    pub SPBit: u8,
    pub Reserved1: u8,
    pub PFBit: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 2],
    pub ParameterListLength: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_SELECT10 {
    pub OperationCode: u8,
    pub SPBit: u8,
    pub Reserved1: u8,
    pub PFBit: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 5],
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCATE {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub CPBit: u8,
    pub BTBit: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved3: u8,
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved4: u8,
    pub Partition: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOGSENSE {
    pub OperationCode: u8,
    pub SPBit: u8,
    pub PPCBit: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub PageCode: u8,
    pub PCBit: u8,
    pub SubPageCode: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub ParameterPointer: [u8; 2],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOGSELECT {
    pub OperationCode: u8,
    pub SPBit: u8,
    pub PCRBit: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved: u8,
    pub PCBit: u8,
    pub Reserved2: [u8; 4],
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRINT {
    pub OperationCode: u8,
    pub Reserved: u8,
    pub LogicalUnitNumber: u8,
    pub TransferLength: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEEK {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved2: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERASE {
    pub OperationCode: u8,
    pub Long: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct START_STOP {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 2],
    pub Start: u8,
    pub LoadEject: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEDIA_REMOVAL {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub Reserved2: [u8; 2],
    pub Prevent: u8,
    pub Persistant: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEEK_BLOCK {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub BlockAddress: [u8; 3],
    pub Link: u8,
    pub Flag: u8,
    pub Reserved2: u8,
    pub VendorUnique: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_BLOCK_ADDRESS {
    pub OperationCode: u8,
    pub Reserved1: [u8; 3],
    pub AllocationLength: u8,
    pub Link: u8,
    pub Flag: u8,
    pub Reserved2: u8,
    pub VendorUnique: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARTITION {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Sel: u8,
    pub PartitionSelect: u8,
    pub Reserved1: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE_TAPE_MARKS {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub WriteSetMarks: u8,
    pub Reserved: u8,
    pub LogicalUnitNumber: u8,
    pub TransferLength: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPACE_TAPE_MARKS {
    pub OperationCode: u8,
    pub Code: u8,
    pub Reserved: u8,
    pub LogicalUnitNumber: u8,
    pub NumMarksMSB: u8,
    pub NumMarks: u8,
    pub NumMarksLSB: u8,
    pub value: u8,
    pub Link: u8,
    pub Flag: u8,
    pub Reserved_1: u8,
    pub VendorUnique: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_POSITION {
    pub Operation: u8,
    pub BlockType: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 7],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB6READWRITETAPE {
    pub OperationCode: u8,
    pub VendorSpecific: u8,
    pub Reserved: u8,
    pub TransferLenMSB: u8,
    pub TransferLen: u8,
    pub TransferLenLSB: u8,
    pub Link: u8,
    pub Flag: u8,
    pub Reserved1: u8,
    pub VendorUnique: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INIT_ELEMENT_STATUS {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNubmer: u8,
    pub Reserved2: [u8; 3],
    pub Reserved3: u8,
    pub NoBarCode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INITIALIZE_ELEMENT_RANGE {
    pub OperationCode: u8,
    pub Range: u8,
    pub Reserved1: u8,
    pub LogicalUnitNubmer: u8,
    pub FirstElementAddress: [u8; 2],
    pub Reserved2: [u8; 2],
    pub NumberOfElements: [u8; 2],
    pub Reserved3: u8,
    pub Reserved4: u8,
    pub NoBarCode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POSITION_TO_ELEMENT {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub TransportElementAddress: [u8; 2],
    pub DestinationElementAddress: [u8; 2],
    pub Reserved2: [u8; 2],
    pub Flip: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOVE_MEDIUM {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub TransportElementAddress: [u8; 2],
    pub SourceElementAddress: [u8; 2],
    pub DestinationElementAddress: [u8; 2],
    pub Reserved2: [u8; 2],
    pub Flip: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCHANGE_MEDIUM {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub TransportElementAddress: [u8; 2],
    pub SourceElementAddress: [u8; 2],
    pub Destination1ElementAddress: [u8; 2],
    pub Destination2ElementAddress: [u8; 2],
    pub Flip1: u8,
    pub Flip2: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_ELEMENT_STATUS {
    pub OperationCode: u8,
    pub ElementType: u8,
    pub VolTag: u8,
    pub LogicalUnitNumber: u8,
    pub StartingElementAddress: [u8; 2],
    pub NumberOfElements: [u8; 2],
    pub Reserved1: u8,
    pub AllocationLength: [u8; 3],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_VOLUME_TAG {
    pub OperationCode: u8,
    pub ElementType: u8,
    pub Reserved1: u8,
    pub LogicalUnitNumber: u8,
    pub StartingElementAddress: [u8; 2],
    pub Reserved2: u8,
    pub ActionCode: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 2],
    pub ParameterListLength: [u8; 2],
    pub Reserved5: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_VOLUME_ELEMENT_ADDRESS {
    pub OperationCode: u8,
    pub ElementType: u8,
    pub VolTag: u8,
    pub LogicalUnitNumber: u8,
    pub StartingElementAddress: [u8; 2],
    pub NumberElements: [u8; 2],
    pub Reserved1: u8,
    pub AllocationLength: [u8; 3],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOAD_UNLOAD {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 2],
    pub Start: u8,
    pub LoadEject: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 3],
    pub Slot: u8,
    pub Reserved5: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MECH_STATUS {
    pub OperationCode: u8,
    pub Reserved: u8,
    pub Lun: u8,
    pub Reserved1: [u8; 6],
    pub AllocationLength: [u8; 2],
    pub Reserved2: [u8; 1],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNCHRONIZE_CACHE10 {
    pub OperationCode: u8,
    pub RelAddr: u8,
    pub Immediate: u8,
    pub Reserved: u8,
    pub Lun: u8,
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved2: u8,
    pub BlockCount: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_EVENT_STATUS_NOTIFICATION {
    pub OperationCode: u8,
    pub Immediate: u8,
    pub Reserved: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 2],
    pub NotificationClassRequest: u8,
    pub Reserved3: [u8; 2],
    pub EventListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_PERFORMANCE {
    pub OperationCode: u8,
    pub Except: u8,
    pub Write: u8,
    pub Tolerance: u8,
    pub Reserved0: u8,
    pub StartingLBA: [u8; 4],
    pub Reserved1: [u8; 2],
    pub MaximumNumberOfDescriptors: [u8; 2],
    pub Type: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_DVD_STRUCTURE {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub RMDBlockNumber: [u8; 4],
    pub LayerNumber: u8,
    pub Format: u8,
    pub AllocationLength: [u8; 2],
    pub Reserved3: u8,
    pub AGID: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_STREAMING {
    pub OperationCode: u8,
    pub Reserved: [u8; 8],
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_DVD_STRUCTURE {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 5],
    pub Format: u8,
    pub ParameterListLength: [u8; 2],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_KEY {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 6],
    pub ParameterListLength: [u8; 2],
    pub KeyFormat: u8,
    pub AGID: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_KEY {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved2: [u8; 2],
    pub AllocationLength: [u8; 2],
    pub KeyFormat: u8,
    pub AGID: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_READ_AHEAD {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub TriggerLBA: [u8; 4],
    pub ReadAheadLBA: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_FORMATTED_CAPACITIES {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Lun: u8,
    pub Reserved2: [u8; 5],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_LUNS {
    pub OperationCode: u8,
    pub Reserved1: [u8; 5],
    pub AllocationLength: [u8; 4],
    pub Reserved2: [u8; 1],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERSISTENT_RESERVE_IN {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 5],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERSISTENT_RESERVE_OUT {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Type: u8,
    pub Scope: u8,
    pub Reserved2: [u8; 4],
    pub ParameterListLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_TIMESTAMP {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 4],
    pub AllocationLength: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_TIMESTAMP {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 4],
    pub ParameterListLength: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_SUPPORTED_OPERATION_CODES {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved0: u8,
    pub ReportOptions: u8,
    pub Reserved1: u8,
    pub ReturnCommandTimeoutsDescriptor: u8,
    pub RequestedOperationCode: u8,
    pub RequestedServiceAction: [u8; 2],
    pub AllocationLength: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_CONFIGURATION {
    pub OperationCode: u8,
    pub RequestType: u8,
    pub Reserved1: u8,
    pub StartingFeature: [u8; 2],
    pub Reserved2: [u8; 3],
    pub AllocationLength: [u8; 2],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_CD_SPEED {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub RotationControl: u8,
    pub Reserved3: u8,
    pub ReadSpeed: [u8; 2],
    pub WriteSpeed: [u8; 2],
    pub Reserved2: [u8; 5],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ12 {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlock: [u8; 4],
    pub TransferLength: [u8; 4],
    pub Reserved2: u8,
    pub Streaming: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE12 {
    pub OperationCode: u8,
    pub RelativeAddress: u8,
    pub Reserved1: u8,
    pub EBP: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub LogicalUnitNumber: u8,
    pub LogicalBlock: [u8; 4],
    pub TransferLength: [u8; 4],
    pub Reserved2: u8,
    pub Streaming: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASSTHROUGH12 {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Protocol: u8,
    pub MultipleCount: u8,
    pub TLength: u8,
    pub ByteBlock: u8,
    pub TDir: u8,
    pub Reserved2: u8,
    pub CkCond: u8,
    pub Offline: u8,
    pub Features: u8,
    pub SectorCount: u8,
    pub LbaLow: u8,
    pub LbaMid: u8,
    pub LbaHigh: u8,
    pub Device: u8,
    pub Command: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ16 {
    pub OperationCode: u8,
    pub DurationLimitDescriptor2: u8,
    pub Reserved1: u8,
    pub RebuildAssistRecoveryControl: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub ReadProtect: u8,
    pub LogicalBlock: [u8; 8],
    pub TransferLength: [u8; 4],
    pub Group: u8,
    pub DurationLimitDescriptor0: u8,
    pub DurationLimitDescriptor1: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE16 {
    pub OperationCode: u8,
    pub DurationLimitDescriptor2: u8,
    pub Reserved1: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub WriteProtect: u8,
    pub LogicalBlock: [u8; 8],
    pub TransferLength: [u8; 4],
    pub Group: u8,
    pub DurationLimitDescriptor0: u8,
    pub DurationLimitDescriptor1: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VERIFY16 {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub ByteCheck: u8,
    pub BlockVerify: u8,
    pub Reserved2: u8,
    pub DisablePageOut: u8,
    pub VerifyProtect: u8,
    pub LogicalBlock: [u8; 8],
    pub VerificationLength: [u8; 4],
    pub Reserved3: u8,
    pub Streaming: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYNCHRONIZE_CACHE16 {
    pub OperationCode: u8,
    pub Reserved1: u8,
    pub Immediate: u8,
    pub Reserved2: u8,
    pub LogicalBlock: [u8; 8],
    pub BlockCount: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CAPACITY16 {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub LogicalBlock: [u8; 8],
    pub AllocationLength: [u8; 4],
    pub PMI: u8,
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASSTHROUGH16 {
    pub OperationCode: u8,
    pub Extend: u8,
    pub Protocol: u8,
    pub MultipleCount: u8,
    pub TLength: u8,
    pub ByteBlock: u8,
    pub TDir: u8,
    pub Reserved1: u8,
    pub CkCond: u8,
    pub Offline: u8,
    pub Features15_8: u8,
    pub Features7_0: u8,
    pub SectorCount15_8: u8,
    pub SectorCount7_0: u8,
    pub LbaLow15_8: u8,
    pub LbaLow7_0: u8,
    pub LbaMid15_8: u8,
    pub LbaMid7_0: u8,
    pub LbaHigh15_8: u8,
    pub LbaHigh7_0: u8,
    pub Device: u8,
    pub Command: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LBA_STATUS {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub StartingLBA: [u8; 8],
    pub AllocationLength: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_OPERATION {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 4],
    pub ListIdentifier: [u8; 4],
    pub ParameterListLength: [u8; 4],
    pub GroupNumber: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECEIVE_TOKEN_INFORMATION {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ListIdentifier: [u8; 4],
    pub Reserved2: [u8; 4],
    pub AllocationLength: [u8; 4],
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE_BUFFER {
    pub OperationCode: u8,
    pub Mode: u8,
    pub ModeSpecific: u8,
    pub BufferID: u8,
    pub BufferOffset: [u8; 3],
    pub ParameterListLength: [u8; 3],
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLOSE_ZONE {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ZoneId: [u8; 8],
    pub Reserved2: [u8; 4],
    pub All: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FINISH_ZONE {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ZoneId: [u8; 8],
    pub Reserved2: [u8; 4],
    pub All: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPEN_ZONE {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ZoneId: [u8; 8],
    pub Reserved2: [u8; 4],
    pub All: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESET_WRITE_POINTER {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ZoneId: [u8; 8],
    pub Reserved2: [u8; 4],
    pub All: u8,
    pub Reserved3: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_ZONES {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub ZoneStartLBA: [u8; 8],
    pub AllocationLength: [u8; 4],
    pub ReportingOptions: u8,
    pub Reserved3: u8,
    pub Partial: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_PHYSICAL_ELEMENT_STATUS {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 4],
    pub StartingElement: [u8; 4],
    pub AllocationLength: [u8; 4],
    pub ReportType: u8,
    pub Reserved3: u8,
    pub Filter: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REMOVE_ELEMENT_AND_TRUNCATE {
    pub OperationCode: u8,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub RequestedCapacity: [u8; 8],
    pub ElementIdentifier: [u8; 4],
    pub Reserved2: u8,
    pub Control: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDB32GENERIC {
    pub OperationCode: u8,
    pub Control: u8,
    pub Reserved1: [u8; 4],
    pub Group: u8,
    pub Reserved2: u8,
    pub AdditionalCDBLength: u8,
    pub ServiceAction: [u8; 2],
    pub Reserved3: u8,
    pub DurationLimitDescriptor0: u8,
    pub DurationLimitDescriptor1: u8,
    pub DurationLimitDescriptor2: u8,
    pub Reserved4: u8,
    pub LogicalBlock: [u8; 8],
    pub Reserved5: [u8; 8],
    pub TransferLength: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XDWRITEREAD32 {
    pub OperationCode: u8,
    pub Control: u8,
    pub Reserved1: [u8; 4],
    pub Group: u8,
    pub Reserved2: u8,
    pub AdditionalCDBLength: u8,
    pub ServiceAction: [u8; 2],
    pub XorProtectionInfo: u8,
    pub Reservede: u8,
    pub DisableWrite: u8,
    pub ForceUnitAccess: u8,
    pub DisablePageOut: u8,
    pub WriteProtect: u8,
    pub Reserved4: u8,
    pub LogicalBlock: [u8; 8],
    pub Reserved5: [u8; 8],
    pub TransferLength: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_EVENT_STATUS_HEADER {
    pub EventDataLength: [u8; 2],
    pub NotificationClass: u8,
    pub Reserved: u8,
    pub NEA: u8,
    pub SupportedEventClasses: u8,
    pub ClassEventData: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_OPERATIONAL_STATUS {
    pub OperationalEvent: u8,
    pub Reserved1: u8,
    pub OperationalStatus: u8,
    pub Reserved2: u8,
    pub PersistentPrevented: u8,
    pub Operation: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_POWER_STATUS {
    pub PowerEvent: u8,
    pub Reserved: u8,
    pub PowerStatus: u8,
    pub Reserved2: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_EXTERNAL_STATUS {
    pub ExternalEvent: u8,
    pub Reserved1: u8,
    pub ExternalStatus: u8,
    pub Reserved2: u8,
    pub PersistentPrevented: u8,
    pub Request: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_MEDIA_STATUS {
    pub MediaEvent: u8,
    pub Reserved: u8,
    pub PowerStatus: u8,
    pub MediaStatus: u8,
    pub DoorTrayOpen: u8,
    pub MediaPresent: u8,
    pub ReservedX: u8,
    pub StartSlot: u8,
    pub EndSlot: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_MULTI_HOST_STATUS {
    pub MultiHostEvent: u8,
    pub Reserved1: u8,
    pub MultiHostStatus: u8,
    pub Reserved2: u8,
    pub PersistentPrevented: u8,
    pub Priority: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NOTIFICATION_BUSY_STATUS {
    pub DeviceBusyEvent: u8,
    pub Reserved: u8,
    pub DeviceBusyStatus: u8,
    pub Time: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUPPORTED_SECURITY_PROTOCOLS_PARAMETER_DATA {
    pub Reserved1: [u8; 6],
    pub SupportedSecurityListLength: [u8; 2],
    pub SupportedSecurityProtocol: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_DVD_STRUCTURES_HEADER {
    pub Length: [u8; 2],
    pub Reserved: [u8; 2],
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_KEY_HEADER {
    pub DataLength: [u8; 2],
    pub Reserved: [u8; 2],
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_REPORT_AGID_DATA {
    pub Reserved1: [u8; 3],
    pub Reserved2: u8,
    pub AGID: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_CHALLENGE_KEY_DATA {
    pub ChallengeKeyValue: [u8; 10],
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_KEY_DATA {
    pub Key: [u8; 5],
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_REPORT_ASF_DATA {
    pub Reserved1: [u8; 3],
    pub Success: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_TITLE_KEY_HEADER {
    pub DataLength: [u8; 2],
    pub Reserved1: [u8; 1],
    pub Reserved2: u8,
    pub CGMS: u8,
    pub CP_SEC: u8,
    pub CPM: u8,
    pub Zero: u8,
    pub TitleKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMAT_DESCRIPTOR {
    pub NumberOfBlocks: [u8; 4],
    pub FormatSubType: u8,
    pub FormatType: u8,
    pub BlockLength: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMAT_LIST_HEADER {
    pub Reserved: u8,
    pub VendorSpecific: u8,
    pub Immediate: u8,
    pub TryOut: u8,
    pub IP: u8,
    pub STPF: u8,
    pub DCRT: u8,
    pub DPRY: u8,
    pub FOV: u8,
    pub FormatDescriptorLength: [u8; 2],
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMATTED_CAPACITY_DESCRIPTOR {
    pub NumberOfBlocks: [u8; 4],
    pub Maximum: u8,
    pub Valid: u8,
    pub FormatType: u8,
    pub BlockLength: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMATTED_CAPACITY_LIST {
    pub Reserved: [u8; 3],
    pub CapacityListLength: u8,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPC_TABLE_ENTRY {
    pub Speed: [u8; 2],
    pub OPCValue: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISC_INFORMATION {
    pub Length: [u8; 2],
    pub DiscStatus: u8,
    pub LastSessionStatus: u8,
    pub Erasable: u8,
    pub Reserved1: u8,
    pub FirstTrackNumber: u8,
    pub NumberOfSessionsLsb: u8,
    pub LastSessionFirstTrackLsb: u8,
    pub LastSessionLastTrackLsb: u8,
    pub MrwStatus: u8,
    pub MrwDirtyBit: u8,
    pub Reserved2: u8,
    pub URU: u8,
    pub DBC_V: u8,
    pub DID_V: u8,
    pub DiscType: u8,
    pub NumberOfSessionsMsb: u8,
    pub LastSessionFirstTrackMsb: u8,
    pub LastSessionLastTrackMsb: u8,
    pub DiskIdentification: [u8; 4],
    pub LastSessionLeadIn: [u8; 4],
    pub LastPossibleLeadOutStartTime: [u8; 4],
    pub DiskBarCode: [u8; 8],
    pub Reserved4: u8,
    pub NumberOPCEntries: u8,
    pub OPCTable: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_INFORMATION {
    pub Length: [u8; 2],
    pub DiskStatus: u8,
    pub LastSessionStatus: u8,
    pub Erasable: u8,
    pub Reserved1: u8,
    pub FirstTrackNumber: u8,
    pub NumberOfSessions: u8,
    pub LastSessionFirstTrack: u8,
    pub LastSessionLastTrack: u8,
    pub Reserved2: u8,
    pub GEN: u8,
    pub DBC_V: u8,
    pub DID_V: u8,
    pub DiskType: u8,
    pub Reserved3: [u8; 3],
    pub DiskIdentification: [u8; 4],
    pub LastSessionLeadIn: [u8; 4],
    pub LastPossibleStartTime: [u8; 4],
    pub DiskBarCode: [u8; 8],
    pub Reserved4: u8,
    pub NumberOPCEntries: u8,
    pub OPCTable: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DATA_BLOCK_HEADER {
    pub DataMode: u8,
    pub Reserved: [u8; 4],
    pub LogicalBlockAddress: [u8; 4],
    pub Reserved_1: u8,
    pub M: u8,
    pub S: u8,
    pub F: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACK_INFORMATION {
    pub Length: [u8; 2],
    pub TrackNumber: u8,
    pub SessionNumber: u8,
    pub Reserved1: u8,
    pub TrackMode: u8,
    pub Copy: u8,
    pub Damage: u8,
    pub Reserved2: u8,
    pub DataMode: u8,
    pub FP: u8,
    pub Packet: u8,
    pub Blank: u8,
    pub RT: u8,
    pub NWA_V: u8,
    pub Reserved3: u8,
    pub TrackStartAddress: [u8; 4],
    pub NextWritableAddress: [u8; 4],
    pub FreeBlocks: [u8; 4],
    pub FixedPacketSize: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACK_INFORMATION2 {
    pub Length: [u8; 2],
    pub TrackNumberLsb: u8,
    pub SessionNumberLsb: u8,
    pub Reserved4: u8,
    pub TrackMode: u8,
    pub Copy: u8,
    pub Damage: u8,
    pub Reserved5: u8,
    pub DataMode: u8,
    pub FixedPacket: u8,
    pub Packet: u8,
    pub Blank: u8,
    pub ReservedTrack: u8,
    pub NWA_V: u8,
    pub LRA_V: u8,
    pub Reserved6: u8,
    pub TrackStartAddress: [u8; 4],
    pub NextWritableAddress: [u8; 4],
    pub FreeBlocks: [u8; 4],
    pub FixedPacketSize: [u8; 4],
    pub TrackSize: [u8; 4],
    pub LastRecordedAddress: [u8; 4],
    pub TrackNumberMsb: u8,
    pub SessionNumberMsb: u8,
    pub Reserved7: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACK_INFORMATION3 {
    pub Length: [u8; 2],
    pub TrackNumberLsb: u8,
    pub SessionNumberLsb: u8,
    pub Reserved4: u8,
    pub TrackMode: u8,
    pub Copy: u8,
    pub Damage: u8,
    pub Reserved5: u8,
    pub DataMode: u8,
    pub FixedPacket: u8,
    pub Packet: u8,
    pub Blank: u8,
    pub ReservedTrack: u8,
    pub NWA_V: u8,
    pub LRA_V: u8,
    pub Reserved6: u8,
    pub TrackStartAddress: [u8; 4],
    pub NextWritableAddress: [u8; 4],
    pub FreeBlocks: [u8; 4],
    pub FixedPacketSize: [u8; 4],
    pub TrackSize: [u8; 4],
    pub LastRecordedAddress: [u8; 4],
    pub TrackNumberMsb: u8,
    pub SessionNumberMsb: u8,
    pub Reserved7: [u8; 2],
    pub ReadCompatibilityLba: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERFORMANCE_DESCRIPTOR {
    pub RandomAccess: u8,
    pub Exact: u8,
    pub RestoreDefaults: u8,
    pub WriteRotationControl: u8,
    pub Reserved1: u8,
    pub Reserved: [u8; 3],
    pub StartLba: [u8; 4],
    pub EndLba: [u8; 4],
    pub ReadSize: [u8; 4],
    pub ReadTime: [u8; 4],
    pub WriteSize: [u8; 4],
    pub WriteTime: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INQUIRYDATA {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub DeviceTypeModifier: u8,
    pub ReservedField1: u8,
    pub ReservedField2: u8,
    pub HotPluggable: u8,
    pub LU_CONG: u8,
    pub RemovableMedia: u8,
    pub Versions: u8,
    pub ANSIVersion: u8,
    pub ECMAVersion: u8,
    pub ISOVersion: u8,
    pub ResponseDataFormat: u8,
    pub HiSupport: u8,
    pub NormACA: u8,
    pub TerminateTask: u8,
    pub AERC: u8,
    pub AdditionalLength: u8,
    pub Reserved: u8,
    pub PROTECT: u8,
    pub Reserved_1: u8,
    pub ThirdPartyCoppy: u8,
    pub TPGS: u8,
    pub ACC: u8,
    pub SCCS: u8,
    pub Addr16: u8,
    pub Addr32: u8,
    pub AckReqQ: u8,
    pub MediumChanger: u8,
    pub MultiPort: u8,
    pub ReservedBit2: u8,
    pub EnclosureServices: u8,
    pub ReservedBit3: u8,
    pub SoftReset: u8,
    pub CommandQueue: u8,
    pub TransferDisable: u8,
    pub LinkedCommands: u8,
    pub Synchronous: u8,
    pub Wide16Bit: u8,
    pub Wide32Bit: u8,
    pub RelativeAddressing: u8,
    pub VendorId: [u8; 8],
    pub ProductId: [u8; 16],
    pub ProductRevisionLevel: [u8; 4],
    pub VendorSpecific: [u8; 20],
    pub Reserved3: [u8; 2],
    pub VersionDescriptors: [u32; 8],
    pub Reserved4: [u8; 30],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_MEDIA_SERIAL_NUMBER_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: u8,
    pub SerialNumber: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_SERIAL_NUMBER_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: u8,
    pub SerialNumber: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_IDENTIFICATION_DESCRIPTOR {
    pub CodeSet: u8,
    pub Reserved: u8,
    pub IdentifierType: u8,
    pub Association: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub IdentifierLength: u8,
    pub Identifier: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_IDENTIFICATION_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: u8,
    pub Descriptors: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_EXTENDED_INQUIRY_DATA_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub RefChk: u8,
    pub AppChk: u8,
    pub GrdChk: u8,
    pub Spt: u8,
    pub ActivateMicrocode: u8,
    pub SimpSup: u8,
    pub OrdSup: u8,
    pub HeadSup: u8,
    pub PriorSup: u8,
    pub GroupSup: u8,
    pub UaskSup: u8,
    pub Reserved0: u8,
    pub VSup: u8,
    pub NvSup: u8,
    pub Obsolete0: u8,
    pub WuSup: u8,
    pub Reserved1: u8,
    pub LuiClr: u8,
    pub Reserved2: u8,
    pub PiiSup: u8,
    pub NoPiChk: u8,
    pub Reserved3: u8,
    pub Obsolete1: u8,
    pub HssRelef: u8,
    pub Reserved4: u8,
    pub RtdSup: u8,
    pub RSup: u8,
    pub LuCollectionType: u8,
    pub Multi_i_t_Nexus_Microcode_Download: u8,
    pub Reserved5: u8,
    pub ExtendedSelfTestCompletionMinutes: [u8; 2],
    pub Reserved6: u8,
    pub VsaSup: u8,
    pub HraSup: u8,
    pub PoaSup: u8,
    pub MaxSupportedSenseDataLength: u8,
    pub Nrd0: u8,
    pub Nrd1: u8,
    pub Sac: u8,
    pub Reserved7: u8,
    pub Ias: u8,
    pub Ibs: u8,
    pub MaxInquiryChangeLogs: [u8; 2],
    pub MaxModePageChangeLogs: [u8; 2],
    pub Reserved8: [u8; 45],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_ATA_INFORMATION_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub Reserved0: [u8; 4],
    pub VendorId: [u8; 8],
    pub ProductId: [u8; 16],
    pub ProductRevisionLevel: [u8; 4],
    pub DeviceSignature: [u8; 20],
    pub CommandCode: u8,
    pub Reserved1: [u8; 3],
    pub IdentifyDeviceData: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_THIRD_PARTY_COPY_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub ThirdPartyCopyDescriptors: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINDOWS_BLOCK_DEVICE_TOKEN_LIMITS_DESCRIPTOR {
    pub DescriptorType: [u8; 2],
    pub DescriptorLength: [u8; 2],
    pub VendorSpecific: [u8; 6],
    pub MaximumRangeDescriptors: [u8; 2],
    pub MaximumInactivityTimer: [u8; 4],
    pub DefaultInactivityTimer: [u8; 4],
    pub MaximumTokenTransferSize: [u8; 8],
    pub OptimalTransferCount: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_BLOCK_LIMITS_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub Reserved0: u8,
    pub MaximumCompareAndWriteLength: u8,
    pub OptimalTransferLengthGranularity: [u8; 2],
    pub MaximumTransferLength: [u8; 4],
    pub OptimalTransferLength: [u8; 4],
    pub MaxPrefetchXDReadXDWriteTransferLength: [u8; 4],
    pub MaximumUnmapLBACount: [u8; 4],
    pub MaximumUnmapBlockDescriptorCount: [u8; 4],
    pub OptimalUnmapGranularity: [u8; 4],
    pub UnmapGranularityAlignmentByte3: u8,
    pub UGAValid: u8,
    pub UnmapGranularityAlignmentByte2: u8,
    pub UnmapGranularityAlignmentByte1: u8,
    pub UnmapGranularityAlignmentByte0: u8,
    pub UnmapGranularityAlignment: [u8; 4],
    pub Reserved1: [u8; 28],
    pub Descriptors: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_BLOCK_DEVICE_CHARACTERISTICS_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub Reserved0: u8,
    pub PageLength: u8,
    pub MediumRotationRateMsb: u8,
    pub MediumRotationRateLsb: u8,
    pub MediumProductType: u8,
    pub NominalFormFactor: u8,
    pub WACEREQ: u8,
    pub WABEREQ: u8,
    pub VBULS: u8,
    pub FUAB: u8,
    pub BOCS: u8,
    pub Reserved1: u8,
    pub ZONED: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 3],
    pub DepopulationTime: [u8; 4],
    pub Reserved4: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_LOGICAL_BLOCK_PROVISIONING_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub ThresholdExponent: u8,
    pub DP: u8,
    pub ANC_SUP: u8,
    pub LBPRZ: u8,
    pub Reserved0: u8,
    pub LBPWS10: u8,
    pub LBPWS: u8,
    pub LBPU: u8,
    pub ProvisioningType: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub ProvisioningGroupDescr: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_ZONED_BLOCK_DEVICE_CHARACTERISTICS_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub PageLength: [u8; 2],
    pub URSWRZ: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub OptimalNumberOfOpenSequentialWritePreferredZone: [u8; 4],
    pub OptimalNumberOfNonSequentiallyWrittenSequentialWritePreferredZone: [u8; 4],
    pub MaxNumberOfOpenSequentialWriteRequiredZone: [u8; 4],
    pub Reserved3: [u8; 44],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPD_SUPPORTED_PAGES_PAGE {
    pub DeviceType: u8,
    pub DeviceTypeQualifier: u8,
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: u8,
    pub SupportedPageList: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOG_PARAMETER_HEADER {
    pub ParameterCode: [u8; 2],
    pub ControlByte: u8,
    pub FormatAndLinking: u8,
    pub TMC: u8,
    pub ETC: u8,
    pub TSD: u8,
    pub Obsolete: u8,
    pub DU: u8,
    pub ParameterLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOG_PARAMETER {
    pub Header: u32,
    pub AsByte: [u8; 0],
    pub ResourceCount: [u8; 4],
    pub Scope: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub Reserved: u8,
    pub Temperature: u8,
    pub Year: [u8; 4],
    pub Week: [u8; 2],
    pub WorkloadUtilization: [u8; 2],
    pub SelfTestResults: u8,
    pub Reserved1_1: u8,
    pub SelfTestCode: u8,
    pub SelfTestNumber: u8,
    pub PowerOnHours: [u8; 2],
    pub AddressOfFirstFailure: [u8; 8],
    pub SenseKey: u8,
    pub Reserved2_1: u8,
    pub AdditionalSenseCode: u8,
    pub AdditionalSenseCodeQualifier: u8,
    pub VendorSpecific: u8,
    pub Reserved_1: [u8; 3],
    pub PercentageUsed: u8,
    pub PowerOnMinutes: [u8; 4],
    pub Reserved_2: u8,
    pub ScanStatus: u8,
    pub ScansPerformed: [u8; 2],
    pub ScanProgress: [u8; 2],
    pub MediumScansPerformed: [u8; 2],
    pub ASC: u8,
    pub ASCQ: u8,
    pub MostRecentTemperature: u8,
    pub VendorSpecific_1: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THRESHOLD_RESOURCE_COUNT {
    pub ResourceCount: [u8; 4],
    pub Scope: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TEMPERATURE {
    pub Reserved: u8,
    pub Temperature: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DATE_OF_MANUFACTURE {
    pub Year: [u8; 4],
    pub Week: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WORKLOAD_UTILIZATION {
    pub WorkloadUtilization: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SELF_TEST_RESULTS {
    pub SelfTestResults: u8,
    pub Reserved1: u8,
    pub SelfTestCode: u8,
    pub SelfTestNumber: u8,
    pub PowerOnHours: [u8; 2],
    pub AddressOfFirstFailure: [u8; 8],
    pub SenseKey: u8,
    pub Reserved2: u8,
    pub AdditionalSenseCode: u8,
    pub AdditionalSenseCodeQualifier: u8,
    pub VendorSpecific: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOLID_STATE_MEDIA {
    pub Reserved: [u8; 3],
    pub PercentageUsed: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BACKGROUND_SCAN_STATUS {
    pub PowerOnMinutes: [u8; 4],
    pub Reserved: u8,
    pub ScanStatus: u8,
    pub ScansPerformed: [u8; 2],
    pub ScanProgress: [u8; 2],
    pub MediumScansPerformed: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INFORMATIONAL_EXCEPTIONS {
    pub ASC: u8,
    pub ASCQ: u8,
    pub MostRecentTemperature: u8,
    pub VendorSpecific: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOG_PAGE {
    pub PageCode: u8,
    pub SPF: u8,
    pub DS: u8,
    pub SubPageCode: u8,
    pub PageLength: [u8; 2],
    pub Parameters: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOG_PARAMETER_THRESHOLD_RESOURCE_COUNT {
    pub Header: u32,
    pub ResourceCount: [u8; 4],
    pub Scope: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOG_PAGE_LOGICAL_BLOCK_PROVISIONING {
    pub PageCode: u8,
    pub SPF: u8,
    pub DS: u8,
    pub SubPageCode: u8,
    pub PageLength: [u8; 2],
    pub Parameters: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OVERWRITE_PARAMETER_LIST {
    pub OverWriteCount: u8,
    pub Test: u8,
    pub Invert: u8,
    pub Reserved1: u8,
    pub InitializationPatternLength: [u8; 2],
    pub InitializationPattern: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SENSE_DATA {
    pub ErrorCode: u8,
    pub Valid: u8,
    pub SegmentNumber: u8,
    pub SenseKey: u8,
    pub Reserved: u8,
    pub IncorrectLength: u8,
    pub EndOfMedia: u8,
    pub FileMark: u8,
    pub Information: [u8; 4],
    pub AdditionalSenseLength: u8,
    pub CommandSpecificInformation: [u8; 4],
    pub AdditionalSenseCode: u8,
    pub AdditionalSenseCodeQualifier: u8,
    pub FieldReplaceableUnitCode: u8,
    pub SenseKeySpecific: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_SENSE_DESCRIPTOR_HEADER {
    pub DescriptorType: u8,
    pub AdditionalLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_SENSE_DESCRIPTOR_INFORMATION {
    pub Header: u32,
    pub Valid: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Information: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_SENSE_DESCRIPTOR_BLOCK_COMMAND {
    pub Header: u32,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub IncorrectLength: u8,
    pub Reserved3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_SENSE_DESCRIPTOR_ATA_STATUS_RETURN {
    pub Header: u32,
    pub Extend: u8,
    pub Reserved1: u8,
    pub Error: u8,
    pub SectorCount15_8: u8,
    pub SectorCount7_0: u8,
    pub LbaLow15_8: u8,
    pub LbaLow7_0: u8,
    pub LbaMid15_8: u8,
    pub LbaMid7_0: u8,
    pub LbaHigh15_8: u8,
    pub LbaHigh7_0: u8,
    pub Device: u8,
    pub Status: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DESCRIPTOR_SENSE_DATA {
    pub ErrorCode: u8,
    pub Reserved1: u8,
    pub SenseKey: u8,
    pub Reserved2: u8,
    pub AdditionalSenseCode: u8,
    pub AdditionalSenseCodeQualifier: u8,
    pub Reserved3: [u8; 3],
    pub AdditionalSenseLength: u8,
    pub DescriptorBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CAPACITY_DATA {
    pub LogicalBlockAddress: u32,
    pub BytesPerBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CAPACITY_DATA_EX {
    pub LogicalBlockAddress: i64,
    pub BytesPerBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_CAPACITY16_DATA {
    pub LogicalBlockAddress: i64,
    pub BytesPerBlock: u32,
    pub ProtectionEnable: u8,
    pub ProtectionType: u8,
    pub RcBasis: u8,
    pub Reserved: u8,
    pub LogicalPerPhysicalExponent: u8,
    pub ProtectionInfoExponent: u8,
    pub LowestAlignedBlock_MSB: u8,
    pub LBPRZ: u8,
    pub LBPME: u8,
    pub LowestAlignedBlock_LSB: u8,
    pub Reserved3: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LBA_STATUS_DESCRIPTOR {
    pub StartingLBA: u64,
    pub LogicalBlockCount: u32,
    pub ProvisioningStatus: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LBA_STATUS_LIST_HEADER {
    pub ParameterLength: u32,
    pub Reserved: u32,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_BLOCK_LIMITS_DATA {
    pub Reserved: u8,
    pub BlockMaximumSize: [u8; 3],
    pub BlockMinimumSize: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_BUFFER_CAPACITY_DATA {
    pub DataLength: [u8; 2],
    pub Reserved1: u8,
    pub BlockDataReturned: u8,
    pub Reserved4: u8,
    pub TotalBufferSize: [u8; 4],
    pub AvailableBufferSize: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ZONE_DESCRIPTIOR {
    pub ZoneType: u8,
    pub Reserved1: u8,
    pub Reset: u8,
    pub Non_Seq: u8,
    pub Reserved2: u8,
    pub ZoneCondition: u8,
    pub Reserved3: [u8; 6],
    pub ZoneLength: [u8; 8],
    pub ZoneStartLBA: [u8; 8],
    pub WritePointerLBA: [u8; 8],
    pub Reserved4: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPORT_ZONES_DATA {
    pub ZoneListLength: [u8; 4],
    pub Same: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub MaxLBA: [u8; 8],
    pub Reserved3: [u8; 48],
    pub ZoneDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_PARAMETER_HEADER {
    pub ModeDataLength: u8,
    pub MediumType: u8,
    pub DeviceSpecificParameter: u8,
    pub BlockDescriptorLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_PARAMETER_HEADER10 {
    pub ModeDataLength: [u8; 2],
    pub MediumType: u8,
    pub DeviceSpecificParameter: u8,
    pub Reserved: [u8; 2],
    pub BlockDescriptorLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_PARAMETER_BLOCK {
    pub DensityCode: u8,
    pub NumberOfBlocks: [u8; 3],
    pub Reserved: u8,
    pub BlockLength: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DISCONNECT_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub BufferFullRatio: u8,
    pub BufferEmptyRatio: u8,
    pub BusInactivityLimit: [u8; 2],
    pub BusDisconnectTime: [u8; 2],
    pub BusConnectTime: [u8; 2],
    pub MaximumBurstSize: [u8; 2],
    pub DataTransferDisconnect: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CACHING_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub ReadDisableCache: u8,
    pub MultiplicationFactor: u8,
    pub WriteCacheEnable: u8,
    pub Reserved2: u8,
    pub WriteRetensionPriority: u8,
    pub ReadRetensionPriority: u8,
    pub DisablePrefetchTransfer: [u8; 2],
    pub MinimumPrefetch: [u8; 2],
    pub MaximumPrefetch: [u8; 2],
    pub MaximumPrefetchCeiling: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CACHING_PAGE_EX {
    pub PageCode: u8,
    pub SubPageFormat: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub ReadDisableCache: u8,
    pub MultiplicationFactor: u8,
    pub WriteCacheEnable: u8,
    pub SizeEnable: u8,
    pub Discontinuity: u8,
    pub CachingAnalysisPermitted: u8,
    pub AbortPreFetch: u8,
    pub InitiatorControl: u8,
    pub WriteRetensionPriority: u8,
    pub ReadRetensionPriority: u8,
    pub DisablePrefetchTransfer: [u8; 2],
    pub MinimumPrefetch: [u8; 2],
    pub MaximumPrefetch: [u8; 2],
    pub MaximumPrefetchCeiling: [u8; 2],
    pub NvCacheDisable: u8,
    pub SyncCacheProgress: u8,
    pub VendorSpecific: u8,
    pub DisableReadAhead: u8,
    pub LogicalBlockCacheSegmentSize: u8,
    pub ForceSequentialWrite: u8,
    pub NumberOfCacheSegments: u8,
    pub CacheSegmentSize: [u8; 2],
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CONTROL_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub RLEC: u8,
    pub GLTSD: u8,
    pub D_SENSE: u8,
    pub DPICZ: u8,
    pub TMF_ONLY: u8,
    pub TST: u8,
    pub Obsolete1: u8,
    pub QERR: u8,
    pub NUAR: u8,
    pub QueueAlgorithmModifier: u8,
    pub Obsolete2: u8,
    pub SWP: u8,
    pub UA_INTLCK_CTRL: u8,
    pub RAC: u8,
    pub VS: u8,
    pub AutoloadMode: u8,
    pub Reserved2: u8,
    pub RWWP: u8,
    pub ATMPE: u8,
    pub TAS: u8,
    pub ATO: u8,
    pub Obsolete3: [u8; 2],
    pub BusyTimeoutPeriod: [u8; 2],
    pub ExtendeSelfTestCompletionTime: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CDROM_WRITE_PARAMETERS_PAGE2 {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub WriteType: u8,
    pub TestWrite: u8,
    pub LinkSizeValid: u8,
    pub BufferUnderrunFreeEnabled: u8,
    pub Reserved2: u8,
    pub TrackMode: u8,
    pub Copy: u8,
    pub FixedPacket: u8,
    pub MultiSession: u8,
    pub DataBlockType: u8,
    pub Reserved3: u8,
    pub LinkSize: u8,
    pub Reserved4: u8,
    pub HostApplicationCode: u8,
    pub Reserved5: u8,
    pub SessionFormat: u8,
    pub Reserved6: u8,
    pub PacketSize: [u8; 4],
    pub AudioPauseLength: [u8; 2],
    pub MediaCatalogNumber: [u8; 16],
    pub ISRC: [u8; 16],
    pub SubHeaderData: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CDROM_WRITE_PARAMETERS_PAGE {
    pub PageLength: u8,
    pub WriteType: u8,
    pub TestWrite: u8,
    pub LinkSizeValid: u8,
    pub BufferUnderrunFreeEnabled: u8,
    pub Reserved2: u8,
    pub TrackMode: u8,
    pub Copy: u8,
    pub FixedPacket: u8,
    pub MultiSession: u8,
    pub DataBlockType: u8,
    pub Reserved3: u8,
    pub LinkSize: u8,
    pub Reserved4: u8,
    pub HostApplicationCode: u8,
    pub Reserved5: u8,
    pub SessionFormat: u8,
    pub Reserved6: u8,
    pub PacketSize: [u8; 4],
    pub AudioPauseLength: [u8; 2],
    pub Reserved7: u8,
    pub MediaCatalogNumberValid: u8,
    pub MediaCatalogNumber: [u8; 13],
    pub MediaCatalogNumberZero: u8,
    pub MediaCatalogNumberAFrame: u8,
    pub Reserved8: u8,
    pub ISRCValid: u8,
    pub ISRCCountry: [u8; 2],
    pub ISRCOwner: [u8; 3],
    pub ISRCRecordingYear: [u8; 2],
    pub ISRCSerialNumber: [u8; 5],
    pub ISRCZero: u8,
    pub ISRCAFrame: u8,
    pub ISRCReserved: u8,
    pub SubHeaderData: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_MRW_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub Reserved1: u8,
    pub LbaSpace: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_FLEXIBLE_DISK_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub TransferRate: [u8; 2],
    pub NumberOfHeads: u8,
    pub SectorsPerTrack: u8,
    pub BytesPerSector: [u8; 2],
    pub NumberOfCylinders: [u8; 2],
    pub StartWritePrecom: [u8; 2],
    pub StartReducedCurrent: [u8; 2],
    pub StepRate: [u8; 2],
    pub StepPluseWidth: u8,
    pub HeadSettleDelay: [u8; 2],
    pub MotorOnDelay: u8,
    pub MotorOffDelay: u8,
    pub Reserved2: u8,
    pub MotorOnAsserted: u8,
    pub StartSectorNumber: u8,
    pub TrueReadySignal: u8,
    pub StepPlusePerCyclynder: u8,
    pub Reserved3: u8,
    pub WriteCompenstation: u8,
    pub HeadLoadDelay: u8,
    pub HeadUnloadDelay: u8,
    pub Pin2Usage: u8,
    pub Pin34Usage: u8,
    pub Pin1Usage: u8,
    pub Pin4Usage: u8,
    pub MediumRotationRate: [u8; 2],
    pub Reserved4: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_FORMAT_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub TracksPerZone: [u8; 2],
    pub AlternateSectorsPerZone: [u8; 2],
    pub AlternateTracksPerZone: [u8; 2],
    pub AlternateTracksPerLogicalUnit: [u8; 2],
    pub SectorsPerTrack: [u8; 2],
    pub BytesPerPhysicalSector: [u8; 2],
    pub Interleave: [u8; 2],
    pub TrackSkewFactor: [u8; 2],
    pub CylinderSkewFactor: [u8; 2],
    pub Reserved2: u8,
    pub SurfaceFirst: u8,
    pub RemovableMedia: u8,
    pub HardSectorFormating: u8,
    pub SoftSectorFormating: u8,
    pub Reserved3: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_RIGID_GEOMETRY_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageSavable: u8,
    pub PageLength: u8,
    pub NumberOfCylinders: [u8; 3],
    pub NumberOfHeads: u8,
    pub StartWritePrecom: [u8; 3],
    pub StartReducedCurrent: [u8; 3],
    pub DriveStepRate: [u8; 2],
    pub LandZoneCyclinder: [u8; 3],
    pub RotationalPositionLock: u8,
    pub Reserved2: u8,
    pub RotationOffset: u8,
    pub Reserved3: u8,
    pub RoataionRate: [u8; 2],
    pub Reserved4: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_READ_WRITE_RECOVERY_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub DCRBit: u8,
    pub DTEBit: u8,
    pub PERBit: u8,
    pub EERBit: u8,
    pub RCBit: u8,
    pub TBBit: u8,
    pub ARRE: u8,
    pub AWRE: u8,
    pub ReadRetryCount: u8,
    pub Reserved4: [u8; 4],
    pub WriteRetryCount: u8,
    pub Reserved5: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_READ_RECOVERY_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub DCRBit: u8,
    pub DTEBit: u8,
    pub PERBit: u8,
    pub Reserved2: u8,
    pub RCBit: u8,
    pub TBBit: u8,
    pub Reserved3: u8,
    pub ReadRetryCount: u8,
    pub Reserved4: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_INFO_EXCEPTIONS {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub Flags: u8,
    pub LogErr: u8,
    pub Reserved2: u8,
    pub Test: u8,
    pub Dexcpt: u8,
    pub Reserved3: u8,
    pub Perf: u8,
    pub ReportMethod: u8,
    pub Reserved4: u8,
    pub IntervalTimer: [u8; 4],
    pub ReportCount: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_CONDITION_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub Reserved2: u8,
    pub Standby: u8,
    pub Idle: u8,
    pub Reserved3: u8,
    pub IdleTimer: [u8; 4],
    pub StandbyTimer: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDDA_OUTPUT_PORT {
    pub ChannelSelection: u8,
    pub Reserved: u8,
    pub Volume: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDAUDIO_CONTROL_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub Reserved2: u8,
    pub StopOnTrackCrossing: u8,
    pub Immediate: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 3],
    pub Obsolete: [u8; 2],
    pub CDDAOutputPorts: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_FEATURE_SET_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub CDAudio: [u8; 2],
    pub EmbeddedChanger: [u8; 2],
    pub PacketSMART: [u8; 2],
    pub PersistantPrevent: [u8; 2],
    pub EventStatusNotification: [u8; 2],
    pub DigitalOutput: [u8; 2],
    pub CDSequentialRecordable: [u8; 2],
    pub DVDSequentialRecordable: [u8; 2],
    pub RandomRecordable: [u8; 2],
    pub KeyExchange: [u8; 2],
    pub Reserved2: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_INACTIVITY_TIMEOUT_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub Reserved2: [u8; 2],
    pub SWPP: u8,
    pub DISP: u8,
    pub Reserved3: u8,
    pub Reserved4: u8,
    pub GroupOneMinimumTimeout: [u8; 2],
    pub GroupTwoMinimumTimeout: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDVD_CAPABILITIES_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub CDRRead: u8,
    pub CDERead: u8,
    pub Method2: u8,
    pub DVDROMRead: u8,
    pub DVDRRead: u8,
    pub DVDRAMRead: u8,
    pub Reserved2: u8,
    pub CDRWrite: u8,
    pub CDEWrite: u8,
    pub TestWrite: u8,
    pub Reserved3: u8,
    pub DVDRWrite: u8,
    pub DVDRAMWrite: u8,
    pub Reserved4: u8,
    pub AudioPlay: u8,
    pub Composite: u8,
    pub DigitalPortOne: u8,
    pub DigitalPortTwo: u8,
    pub Mode2Form1: u8,
    pub Mode2Form2: u8,
    pub MultiSession: u8,
    pub BufferUnderrunFree: u8,
    pub CDDA: u8,
    pub CDDAAccurate: u8,
    pub RWSupported: u8,
    pub RWDeinterleaved: u8,
    pub C2Pointers: u8,
    pub ISRC: u8,
    pub UPC: u8,
    pub ReadBarCodeCapable: u8,
    pub Lock: u8,
    pub LockState: u8,
    pub PreventJumper: u8,
    pub Eject: u8,
    pub Reserved6: u8,
    pub LoadingMechanismType: u8,
    pub SeparateVolume: u8,
    pub SeperateChannelMute: u8,
    pub SupportsDiskPresent: u8,
    pub SWSlotSelection: u8,
    pub SideChangeCapable: u8,
    pub RWInLeadInReadable: u8,
    pub Reserved7: u8,
    pub ReadSpeedMaximum: [u8; 2],
    pub ObsoleteReserved: [u8; 2],
    pub NumberVolumeLevels: [u8; 2],
    pub BufferSize: [u8; 2],
    pub ReadSpeedCurrent: [u8; 2],
    pub ObsoleteReserved2: [u8; 2],
    pub ObsoleteReserved3: u8,
    pub Reserved8: u8,
    pub BCK: u8,
    pub RCK: u8,
    pub LSBF: u8,
    pub Length: u8,
    pub Reserved9: u8,
    pub WriteSpeedMaximum: [u8; 2],
    pub ObsoleteReserved4: [u8; 2],
    pub WriteSpeedCurrent: [u8; 2],
    pub ObsoleteReserved11: [u8; 2],
    pub CopyManagementRevision: [u8; 2],
    pub Reserved10: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LUN_LIST {
    pub LunListLength: [u8; 4],
    pub Reserved: [u8; 4],
    pub Lun: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_PAGE_SUBPAGE_HEADER {
    pub PageCode: u8,
    pub SubPageFormat: u8,
    pub PageSavable: u8,
    pub SubPageCode: u8,
    pub PageLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct T2_COMMAND_DURATION_LIMIT_DESCRIPTOR {
    pub T2CDLUNITS: u8,
    pub Reserved: u8,
    pub Reserved1: u8,
    pub MAX_INACTIVE_TIME: [u8; 2],
    pub MAX_ACTIVE_TIME: [u8; 2],
    pub MAX_ACTIVE_TIME_POLICY: u8,
    pub MAX_INACTIVE_TIME_POLICY: u8,
    pub Reserved2: [u8; 3],
    pub COMMAND_DURATION_GUIDELINE: [u8; 2],
    pub Reserved3: [u8; 2],
    pub COMMAND_DURATION_GUIDELINE_POLICY: u8,
    pub Reserved4: u8,
    pub BypassSequestration: u8,
    pub Reserved5: u8,
    pub Reserved6: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_COMMAND_DURATION_LIMIT_PAGE_T2A_SUBPAGE {
    pub PageCode: u8,
    pub SubPageFormat: u8,
    pub PageSavable: u8,
    pub SubPageCode: u8,
    pub PageLength: [u8; 2],
    pub Reserved: [u8; 3],
    pub Reserved1: u8,
    pub PerfvsCommandDurationGuidelines: u8,
    pub T2CommandDurationLimitDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_COMMAND_DURATION_LIMIT_PAGE_T2B_SUBPAGE {
    pub PageCode: u8,
    pub SubPageFormat: u8,
    pub PageSavable: u8,
    pub SubPageCode: u8,
    pub PageLength: [u8; 2],
    pub Reserved: [u8; 3],
    pub Reserved1: u8,
    pub PerfvsLatencyControls: u8,
    pub T2CommandDurationLimitDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_PARM_READ_WRITE_DATA {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_POSITION_DATA {
    pub Reserved1: u8,
    pub BlockPositionUnsupported: u8,
    pub Reserved2: u8,
    pub EndOfPartition: u8,
    pub BeginningOfPartition: u8,
    pub PartitionNumber: u8,
    pub Reserved3: u16,
    pub FirstBlock: [u8; 4],
    pub LastBlock: [u8; 4],
    pub Reserved4: u8,
    pub NumberOfBlocks: [u8; 3],
    pub NumberOfBytes: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: [u8; 2],
    pub PageData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_TYPE_DESCRIPTOR_HEADER {
    pub ElementType: u8,
    pub NumberOfPossibleElements: u8,
    pub SubEnclosureId: u8,
    pub TypeDescriptorTextLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_ENCLOSURE_DESCRIPTOR {
    pub NumberOfEnclosureServices: u8,
    pub Reserved1: u8,
    pub RelativeEnclosureServicesId: u8,
    pub Reserved2: u8,
    pub SubEnclosureId: u8,
    pub NumberOfTypeDescriptorHeaders: u8,
    pub EnclosureDescriptorLength: u8,
    pub Identifier: [u8; 8],
    pub VendorId: [u8; 8],
    pub ProductId: [u8; 16],
    pub ProductRevisionLevel: [u8; 4],
    pub VendorSpecific: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_CONFIGURATION_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub NumberOfSecondarySubEnclosures: u8,
    pub PageLength: [u8; 2],
    pub GenerationCode: [u8; 4],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_CONTROL_DESCRIPTOR {
    pub Reserved: u8,
    pub ResetSwap: u8,
    pub Disable: u8,
    pub PredictFailure: u8,
    pub Select: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub RequestIdentify: u8,
    pub RequestRemove: u8,
    pub RequestInsert: u8,
    pub RequestMissing: u8,
    pub Reserved3: u8,
    pub DoNotRemove: u8,
    pub RequestActive: u8,
    pub Reserved4: u8,
    pub EnableBypassB: u8,
    pub EnableBypassA: u8,
    pub DeviceOff: u8,
    pub RequestFault: u8,
    pub Reserved5: u8,
    pub Reserved1_1: u8,
    pub RequestIdentify_1: u8,
    pub Reserved2_1: u8,
    pub Reserved3_1: u8,
    pub RequestOn: u8,
    pub RequestFail: u8,
    pub Reserved4_1: u8,
    pub Reserved1_2: u8,
    pub RequestIdentify_2: u8,
    pub Reserved2_2: u8,
    pub RequestSpeedCode: u8,
    pub Reserved3_2: u8,
    pub RequestOn_1: u8,
    pub RequestFail_1: u8,
    pub Reserved4_2: u8,
    pub Reserved1_3: u8,
    pub RequestFail_2: u8,
    pub RequestIdentify_3: u8,
    pub Reserved2_3: u8,
    pub Reserved3_3: u8,
    pub Reserved1_4: u8,
    pub RequestFail_3: u8,
    pub RequestIdentify_4: u8,
    pub Reserved2_4: u8,
    pub Reserved3_4: u8,
    pub Reserved1_5: u8,
    pub RequestFail_4: u8,
    pub RequestIdentify_5: u8,
    pub Reserved2_5: u8,
    pub Reserved3_5: u8,
    pub Reserved1_6: u8,
    pub RequestIdentify_6: u8,
    pub PowerCycleDelay: u8,
    pub PowerCycleRequest: u8,
    pub RequestWarning: u8,
    pub RequestFailure: u8,
    pub PowerOffDuration: u8,
    pub RequestRebuildAbort: u8,
    pub RequestRebuild: u8,
    pub RequestInFailedArray: u8,
    pub RequestInCriticalArray: u8,
    pub RequestConsistencyArray: u8,
    pub RequestHotSpare: u8,
    pub RequestReservedDevice: u8,
    pub RequestOK: u8,
    pub Reserved1_7: u8,
    pub RequestIdentify_7: u8,
    pub RequestRemove_1: u8,
    pub RequestInsert_1: u8,
    pub RequestMissing_1: u8,
    pub Reserved2_6: u8,
    pub DoNotRemove_1: u8,
    pub RequestActive_1: u8,
    pub Reserved3_6: u8,
    pub EnableBypassB_1: u8,
    pub EnableBypassA_1: u8,
    pub DeviceOff_1: u8,
    pub RequestFault_1: u8,
    pub Reserved4_3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_CONTROL_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub Unrecoverable: u8,
    pub Critical: u8,
    pub NonCritical: u8,
    pub Informational: u8,
    pub Reserved: u8,
    pub PageLength: [u8; 2],
    pub ExpectedGenerationCode: [u8; 4],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_STATUS_DESCRIPTOR {
    pub ElementStatus: u8,
    pub Swap: u8,
    pub Disabled: u8,
    pub PredictedFailure: u8,
    pub Reserved1: u8,
    pub SlotAddress: u8,
    pub Report: u8,
    pub Identify: u8,
    pub Remove: u8,
    pub ReadyToInsert: u8,
    pub EnclosureBypassedB: u8,
    pub EnclosureBypassedA: u8,
    pub DoNotRemove: u8,
    pub AppBypassedA: u8,
    pub DeviceBypassedB: u8,
    pub DeviceBypassedA: u8,
    pub BypassedB: u8,
    pub BypassedA: u8,
    pub DeviceOff: u8,
    pub FaultRequested: u8,
    pub FaultSensed: u8,
    pub AppBypassedB: u8,
    pub Reserved1_1: u8,
    pub Identify_1: u8,
    pub Reserved2: u8,
    pub DCOverCurrent: u8,
    pub DCUnderVoltage: u8,
    pub DCOverVoltage: u8,
    pub Reserved3: u8,
    pub DCFail: u8,
    pub ACFail: u8,
    pub TemperatureWarning: u8,
    pub OverTemperatureFail: u8,
    pub Off: u8,
    pub RequestedOn: u8,
    pub Fail: u8,
    pub HotSwap: u8,
    pub ActualFanSpeedMSB: u8,
    pub Reserved1_2: u8,
    pub Identify_2: u8,
    pub ActualFanSpeedLSB: u8,
    pub ActualSpeedCode: u8,
    pub Reserved2_1: u8,
    pub Off_1: u8,
    pub RequestedOn_1: u8,
    pub Fail_1: u8,
    pub HotSwap_1: u8,
    pub Reserved1_3: u8,
    pub Fail_2: u8,
    pub Identify_3: u8,
    pub Temperature: u8,
    pub UnderTemperatureWarning: u8,
    pub UnderTemperatureFailure: u8,
    pub OverTemperatureWarning: u8,
    pub OverTemperatureFailure: u8,
    pub Reserved2_2: u8,
    pub CritUnder: u8,
    pub CritOver: u8,
    pub WarnUnder: u8,
    pub WarnOver: u8,
    pub Reserved1_4: u8,
    pub Fail_3: u8,
    pub Identify_4: u8,
    pub VoltageMSB: u8,
    pub VoltageLSB: u8,
    pub Reserved1_5: u8,
    pub CritOver_1: u8,
    pub Reserved2_3: u8,
    pub WarnOver_1: u8,
    pub Reserved3_1: u8,
    pub Fail_4: u8,
    pub Identify_5: u8,
    pub CurrentMSB: u8,
    pub CurrentLSB: u8,
    pub Reserved1_6: u8,
    pub Identify_6: u8,
    pub WarningIndication: u8,
    pub FailureIndication: u8,
    pub TimeUntilPowerCycle: u8,
    pub WarningRequested: u8,
    pub FailureRequested: u8,
    pub RequestedPowerOffTime: u8,
    pub RebuildAbort: u8,
    pub Rebuild: u8,
    pub InFailedArray: u8,
    pub InCriticalArray: u8,
    pub ConsistencyCheck: u8,
    pub HotSpare: u8,
    pub ReservedDevice: u8,
    pub OK: u8,
    pub Report_1: u8,
    pub Identify_7: u8,
    pub Remove_1: u8,
    pub ReadyToInsert_1: u8,
    pub EnclosureBypassedB_1: u8,
    pub EnclosureBypassedA_1: u8,
    pub DoNotRemove_1: u8,
    pub AppBypassedA_1: u8,
    pub DeviceBypassedB_1: u8,
    pub DeviceBypassedA_1: u8,
    pub BypassedB_1: u8,
    pub BypassedA_1: u8,
    pub DeviceOff_1: u8,
    pub FaultRequested_1: u8,
    pub FaultSensed_1: u8,
    pub AppBypassedB_1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_STATUS_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub Unrecoverable: u8,
    pub Critical: u8,
    pub NonCritical: u8,
    pub Informational: u8,
    pub InvalidOperation: u8,
    pub Reserved: u8,
    pub PageLength: [u8; 2],
    pub GenerationCode: [u8; 4],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_PHY_DESCRIPTOR {
    pub Reserved1: u8,
    pub DeviceType: u8,
    pub Reserved3: u8,
    pub Reserved4: u8,
    pub Reserved5: u8,
    pub SmpInitiatorPort: u8,
    pub StpInitiatorPort: u8,
    pub SspInitiatorPort: u8,
    pub Reserved6: u8,
    pub SataDevice: u8,
    pub SmpTargetPort: u8,
    pub StpTargetPort: u8,
    pub SspTargetPort: u8,
    pub Reserved7: u8,
    pub SataPortSelector: u8,
    pub AttachedSASAddress: [u8; 8],
    pub SASAddress: [u8; 8],
    pub PhyIdentifier: u8,
    pub Reserved2: [u8; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_SAS_SLOT_INFORMATION {
    pub NumberOfPhyDescriptors: u8,
    pub NotAllPhys: u8,
    pub Reserved1: u8,
    pub Type: u8,
    pub Reserved2: u8,
    pub DeviceSlotNumber: u8,
    pub PhyDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_ADDITIONAL_ELEMENT_STATUS_DESCRIPTOR {
    pub ProtocolIdentifier: u8,
    pub EIP: u8,
    pub Reserved1: u8,
    pub Invalid: u8,
    pub Length: u8,
    pub Reserved2: u8,
    pub ElementIndex: u8,
    pub ProtocolInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_ADDITIONAL_ELEMENT_STATUS_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub Reserved: u8,
    pub PageLength: [u8; 2],
    pub GenerationCode: [u8; 4],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_DOWNLOAD_MICROCODE_STATUS_DESCRIPTOR {
    pub Reserved1: u8,
    pub SubEnclosureId: u8,
    pub Status: u8,
    pub AdditionalStatus: u8,
    pub MaximumImageSize: [u8; 4],
    pub Reserved2: [u8; 3],
    pub ExpectedBufferId: u8,
    pub ExpectedBufferOffset: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_DOWNLOAD_MICROCODE_STATUS_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub NumberOfSecondarySubEnclosures: u8,
    pub PageLength: [u8; 2],
    pub GenerationCode: [u8; 4],
    pub Descriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SES_DOWNLOAD_MICROCODE_CONTROL_DIAGNOSTIC_PAGE {
    pub PageCode: u8,
    pub SubEnclosureId: u8,
    pub PageLength: [u8; 2],
    pub ExpectedGenerationCode: [u8; 4],
    pub Mode: u8,
    pub Reserved: [u8; 2],
    pub BufferID: u8,
    pub BufferOffset: [u8; 4],
    pub ImageLength: [u8; 4],
    pub DataLength: [u8; 4],
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_ELEMENT_STATUS_DATA_DESCRIPTOR {
    pub Reserved1: [u8; 4],
    pub ElementIdentifier: [u8; 4],
    pub Reserved2: [u8; 6],
    pub PhysicalElementType: u8,
    pub PhysicalElementHealth: u8,
    pub AssociatedCapacity: [u8; 8],
    pub Reserved3: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_ELEMENT_STATUS_PARAMETER_DATA {
    pub DescriptorCount: [u8; 4],
    pub ReturnedDescriptorCount: [u8; 4],
    pub ElementIdentifierBeingDepoped: [u8; 4],
    pub Reserved: [u8; 20],
    pub Descriptors: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_HISTORY_DIRECTORY_ENTRY {
    pub SupportedBufferId: u8,
    pub BufferFormat: u8,
    pub BufferSource: u8,
    pub Reserved0: u8,
    pub Reserved1: u8,
    pub MaxAvailableLength: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_HISTORY_DIRECTORY {
    pub T10VendorId: [u8; 8],
    pub ErrorHistoryVersion: u8,
    pub ClearSupport: u8,
    pub ErrorHistorySource: u8,
    pub ErrorHistoryRetrieved: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 20],
    pub DirectoryLength: [u8; 2],
    pub ErrorHistoryDirectoryList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CURRENT_INTERNAL_STATUS_PARAMETER_DATA {
    pub Reserved0: [u8; 4],
    pub IEEECompanyId: [u8; 4],
    pub CurrentInternalStatusDataSetOneLength: [u8; 2],
    pub CurrentInternalStatusDataSetTwoLength: [u8; 2],
    pub CurrentInternalStatusDataSetThreeLength: [u8; 2],
    pub CurrentInternalStatusDataSetFourLength: [u8; 4],
    pub Reserved1: [u8; 364],
    pub NewSavedDataAvailable: u8,
    pub SavedDataGenerationNumber: u8,
    pub CurrentReasonIdentifier: [u8; 128],
    pub CurrentInternalStatusData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SAVED_INTERNAL_STATUS_PARAMETER_DATA {
    pub Reserved0: [u8; 4],
    pub IEEECompanyId: [u8; 4],
    pub SavedInternalStatusDataSetOneLength: [u8; 2],
    pub SavedInternalStatusDataSetTwoLength: [u8; 2],
    pub SavedInternalStatusDataSetThreeLength: [u8; 2],
    pub SavedInternalStatusDataSetFourLength: [u8; 4],
    pub Reserved1: [u8; 364],
    pub NewSavedDataAvailable: u8,
    pub SavedDataGenerationNumber: u8,
    pub SavedReasonIdentifier: [u8; 128],
    pub SavedInternalStatusData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DEVICE_CONFIGURATION_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PS: u8,
    pub PageLength: u8,
    pub ActiveFormat: u8,
    pub CAFBit: u8,
    pub CAPBit: u8,
    pub Reserved2: u8,
    pub ActivePartition: u8,
    pub WriteBufferFullRatio: u8,
    pub ReadBufferEmptyRatio: u8,
    pub WriteDelayTime: [u8; 2],
    pub REW: u8,
    pub RBO: u8,
    pub SOCF: u8,
    pub AVC: u8,
    pub RSmk: u8,
    pub BIS: u8,
    pub DBR: u8,
    pub GapSize: u8,
    pub Reserved3: u8,
    pub SEW: u8,
    pub EEG: u8,
    pub EODdefined: u8,
    pub BufferSize: [u8; 3],
    pub DCAlgorithm: u8,
    pub Reserved4: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_MEDIUM_PARTITION_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PSBit: u8,
    pub PageLength: u8,
    pub MaximumAdditionalPartitions: u8,
    pub AdditionalPartitionDefined: u8,
    pub Reserved2: u8,
    pub PSUMBit: u8,
    pub IDPBit: u8,
    pub SDPBit: u8,
    pub FDPBit: u8,
    pub MediumFormatRecognition: u8,
    pub Reserved3: [u8; 2],
    pub Partition0Size: [u8; 2],
    pub Partition1Size: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DATA_COMPRESSION_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PageLength: u8,
    pub Reserved2: u8,
    pub DCC: u8,
    pub DCE: u8,
    pub Reserved3: u8,
    pub RED: u8,
    pub DDE: u8,
    pub CompressionAlgorithm: [u8; 4],
    pub DecompressionAlgorithm: [u8; 4],
    pub Reserved4: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CAPABILITIES_PAGE {
    pub PageCode: u8,
    pub Reserved1: u8,
    pub PageLength: u8,
    pub Reserved2: [u8; 2],
    pub RO: u8,
    pub Reserved3: u8,
    pub SPREV: u8,
    pub Reserved4: u8,
    pub Reserved5: u8,
    pub EFMT: u8,
    pub Reserved6: u8,
    pub QFA: u8,
    pub Reserved7: u8,
    pub LOCK: u8,
    pub LOCKED: u8,
    pub PREVENT: u8,
    pub UNLOAD: u8,
    pub Reserved8: u8,
    pub ECC: u8,
    pub CMPRS: u8,
    pub Reserved9: u8,
    pub BLK512: u8,
    pub BLK1024: u8,
    pub Reserved10: u8,
    pub SLOWB: u8,
    pub MaximumSpeedSupported: [u8; 2],
    pub MaximumStoredDefectedListEntries: [u8; 2],
    pub ContinuousTransferLimit: [u8; 2],
    pub CurrentSpeedSelected: [u8; 2],
    pub BufferSize: [u8; 2],
    pub Reserved11: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_CAP_PAGE {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
    pub CapabilitiesPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_MEDIUM_PART_PAGE {
    pub ParameterListHeader: u32,
    pub MediumPartPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_MEDIUM_PART_PAGE_PLUS {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
    pub MediumPartPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_TAPE_MEDIA_INFORMATION {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
    pub MediumPartPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DEVICE_CONFIG_PAGE {
    pub ParameterListHeader: u32,
    pub DeviceConfigPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DEVICE_CONFIG_PAGE_PLUS {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
    pub DeviceConfigPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DATA_COMPRESS_PAGE {
    pub ParameterListHeader: u32,
    pub DataCompressPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODE_DATA_COMPRESS_PAGE_PLUS {
    pub ParameterListHeader: u32,
    pub ParameterListBlock: u32,
    pub DataCompressPage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_INIT_DATA {
    pub VerifyInquiry: u32,
    pub QueryModeCapabilitiesPage: u8,
    pub MinitapeExtensionSize: u32,
    pub ExtensionInit: u32,
    pub DefaultTimeOutValue: u32,
    pub TapeError: u32,
    pub CommandExtensionSize: u32,
    pub CreatePartition: u32,
    pub Erase: u32,
    pub GetDriveParameters: u32,
    pub GetMediaParameters: u32,
    pub GetPosition: u32,
    pub GetStatus: u32,
    pub Prepare: u32,
    pub SetDriveParameters: u32,
    pub SetMediaParameters: u32,
    pub SetPosition: u32,
    pub WriteMarks: u32,
    pub PreProcessReadWrite: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_INIT_DATA_EX {
    pub InitDataSize: u32,
    pub VerifyInquiry: u32,
    pub QueryModeCapabilitiesPage: u8,
    pub MinitapeExtensionSize: u32,
    pub ExtensionInit: u32,
    pub DefaultTimeOutValue: u32,
    pub TapeError: u32,
    pub CommandExtensionSize: u32,
    pub CreatePartition: u32,
    pub Erase: u32,
    pub GetDriveParameters: u32,
    pub GetMediaParameters: u32,
    pub GetPosition: u32,
    pub GetStatus: u32,
    pub Prepare: u32,
    pub SetDriveParameters: u32,
    pub SetMediaParameters: u32,
    pub SetPosition: u32,
    pub WriteMarks: u32,
    pub PreProcessReadWrite: u32,
    pub TapeGetMediaTypes: u32,
    pub MediaTypesSupported: u32,
    pub TapeWMIOperations: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TAPE_PHYS_POSITION {
    pub SeekBlockAddress: u32,
    pub SpaceBlockCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_DRIVE_PARAMETERS {
    pub MaximumBlockSize: u32,
    pub MinimumBlockSize: u32,
    pub DefaultBlockSize: u32,
    pub MaximumPartitionCount: u32,
    pub CompressionCapable: u8,
    pub CompressionEnabled: u8,
    pub ReportSetmarks: u8,
    pub HardwareErrorCorrection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_MEDIA_PARAMETERS {
    pub MaximumCapacity: u64,
    pub AvailableCapacity: u64,
    pub BlockSize: u32,
    pub PartitionCount: u32,
    pub MediaWriteProtected: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_PROBLEM_WARNING {
    pub DriveProblemType: u32,
    pub TapeData: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_PROBLEM_IO_ERROR {
    pub ReadCorrectedWithoutDelay: u32,
    pub ReadCorrectedWithDelay: u32,
    pub ReadTotalErrors: u32,
    pub ReadTotalCorrectedErrors: u32,
    pub ReadTotalUncorrectedErrors: u32,
    pub ReadCorrectionAlgorithmProcessed: u32,
    pub WriteCorrectedWithoutDelay: u32,
    pub WriteCorrectedWithDelay: u32,
    pub WriteTotalErrors: u32,
    pub WriteTotalCorrectedErrors: u32,
    pub WriteTotalUncorrectedErrors: u32,
    pub WriteCorrectionAlgorithmProcessed: u32,
    pub NonMediumErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_PROBLEM_DEVICE_ERROR {
    pub ReadWarning: u8,
    pub WriteWarning: u8,
    pub HardError: u8,
    pub ReadFailure: u8,
    pub WriteFailure: u8,
    pub UnsupportedFormat: u8,
    pub TapeSnapped: u8,
    pub DriveRequiresCleaning: u8,
    pub TimetoCleanDrive: u8,
    pub DriveHardwareError: u8,
    pub ScsiInterfaceError: u8,
    pub MediaLife: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTDEV_UNIQUE_ID {
    pub UniqueIdLength: u16,
    pub UniqueId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTDEV_SUGGESTED_LINK_NAME {
    pub UseOnlyIfThereAreNoOtherLinks: u8,
    pub NameLength: u16,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTDEV_STABLE_GUID {
    pub StableGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_CREATE_POINT_INPUT {
    pub SymbolicLinkNameOffset: u16,
    pub SymbolicLinkNameLength: u16,
    pub DeviceNameOffset: u16,
    pub DeviceNameLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_MOUNT_POINT {
    pub SymbolicLinkNameOffset: u32,
    pub SymbolicLinkNameLength: u16,
    pub Reserved1: u16,
    pub UniqueIdOffset: u32,
    pub UniqueIdLength: u16,
    pub Reserved2: u16,
    pub DeviceNameOffset: u32,
    pub DeviceNameLength: u16,
    pub Reserved3: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_MOUNT_POINTS {
    pub Size: u32,
    pub NumberOfMountPoints: u32,
    pub MountPoints: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_DRIVE_LETTER_TARGET {
    pub DeviceNameLength: u16,
    pub DeviceName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_DRIVE_LETTER_INFORMATION {
    pub DriveLetterWasAssigned: u8,
    pub CurrentDriveLetter: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_VOLUME_MOUNT_POINT {
    pub SourceVolumeNameOffset: u16,
    pub SourceVolumeNameLength: u16,
    pub TargetVolumeNameOffset: u16,
    pub TargetVolumeNameLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_CHANGE_NOTIFY_INFO {
    pub EpicNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_TARGET_NAME {
    pub DeviceNameLength: u16,
    pub DeviceName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTDEV_NAME {
    pub NameLength: u16,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_VOLUME_PATHS {
    pub MultiSzLength: u32,
    pub MultiSz: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_QUERY_AUTO_MOUNT {
    pub CurrentState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_SET_AUTO_MOUNT {
    pub NewState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUNTMGR_SILO_ARRIVAL_INPUT {
    pub JobHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PDOSCSI_ADDR {
    pub PortNumber: u8,
    pub ScsiPathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PDO_INFORMATION {
    pub ScsiAddress: usize,
    pub DeviceState: u32,
    pub PathIdentifier: u64,
    pub IdentifierType: u32,
    pub IdentifierLength: u32,
    pub Identifier: [u8; 32],
    pub Pad: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_GET_DESCRIPTOR {
    pub NumberPdos: u32,
    pub DeviceName: [u32; 1],
    pub PdoInformation: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DEVINSTANCE_HEALTH_CLASS {
    pub PathId: u64,
    pub NumberReads: u64,
    pub NumberWrites: u64,
    pub NumberBytesRead: u64,
    pub NumberBytesWritten: u64,
    pub NumberRetries: u64,
    pub NumberIoErrors: u64,
    pub CreateTime: u64,
    pub FailTime: u64,
    pub DeviceOffline: u8,
    pub NumberReadsWrap: u8,
    pub NumberWritesWrap: u8,
    pub NumberBytesReadWrap: u8,
    pub NumberBytesWrittenWrap: u8,
    pub Pad: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DEVINSTANCE_HEALTH_INFO {
    pub NumberDevInstancePackets: u32,
    pub Reserved: u32,
    pub DevInstanceHealthPackets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ClearDevInstanceHealthCounters_IN {
    pub PathID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DSM_Path_V2 {
    pub DsmPathId: u64,
    pub Reserved: u64,
    pub PathWeight: u32,
    pub PrimaryPath: u32,
    pub OptimizedPath: u32,
    pub PreferredPath: u32,
    pub FailedPath: u32,
    pub TargetPortGroup_State: u32,
    pub ALUASupport: u32,
    pub SymmetricLUA: u8,
    pub TargetPortGroup_Preferred: u8,
    pub TargetPortGroup_Identifier: u16,
    pub TargetPort_Identifier: u32,
    pub Reserved32: u32,
    pub Reserved64: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_Load_Balance_Policy_V2 {
    pub Version: u32,
    pub LoadBalancePolicy: u32,
    pub DSMPathCount: u32,
    pub Reserved: u32,
    pub DSM_Paths: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_QueryLBPolicy_V2 {
    pub LoadBalancePolicy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_QuerySupportedLBPolicies_V2 {
    pub SupportedLBPoliciesCount: u32,
    pub Reserved: u32,
    pub Supported_LB_Policies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DSM_Path {
    pub DsmPathId: u64,
    pub Reserved: u64,
    pub PathWeight: u32,
    pub PrimaryPath: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_Load_Balance_Policy {
    pub Version: u32,
    pub LoadBalancePolicy: u32,
    pub DSMPathCount: u32,
    pub Reserved: u32,
    pub DSM_Paths: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_QueryLBPolicy {
    pub LoadBalancePolicy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_QuerySupportedLBPolicies {
    pub SupportedLBPoliciesCount: u32,
    pub Reserved: u32,
    pub Supported_LB_Policies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DsmSetLoadBalancePolicy_IN {
    pub LoadBalancePolicy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DsmSetLoadBalancePolicy_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DsmSetLoadBalancePolicyALUA_IN {
    pub LoadBalancePolicy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DsmSetLoadBalancePolicyALUA_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_QueryUniqueId {
    pub DsmUniqueId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PATH_HEALTH_CLASS {
    pub PathId: u64,
    pub NumberReads: u64,
    pub NumberWrites: u64,
    pub NumberBytesRead: u64,
    pub NumberBytesWritten: u64,
    pub NumberRetries: u64,
    pub NumberIoErrors: u64,
    pub CreateTime: u64,
    pub FailTime: u64,
    pub PathOffline: u8,
    pub NumberReadsWrap: u8,
    pub NumberWritesWrap: u8,
    pub NumberBytesReadWrap: u8,
    pub NumberBytesWrittenWrap: u8,
    pub OutstandingRequests: u8,
    pub Pad: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PATH_HEALTH_INFO {
    pub NumberPathPackets: u32,
    pub Reserved: u32,
    pub PathHealthPackets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DISK_HEALTH_CLASS {
    pub Name: [u32; 1],
    pub NumberReads: u64,
    pub NumberWrites: u64,
    pub NumberBytesRead: u64,
    pub NumberBytesWritten: u64,
    pub NumberRetries: u64,
    pub NumberIoErrors: u64,
    pub CreateTime: u64,
    pub PathFailures: u64,
    pub FailTime: u64,
    pub DeviceOffline: u8,
    pub NumberReadsWrap: u8,
    pub NumberWritesWrap: u8,
    pub NumberBytesReadWrap: u8,
    pub NumberBytesWrittenWrap: u8,
    pub Pad: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DISK_HEALTH_INFO {
    pub NumberDiskPackets: u32,
    pub Reserved: u32,
    pub DiskHealthPackets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_ADDR {
    pub PortNumber: u8,
    pub ScsiPathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DRIVE_INFO {
    pub NumberPaths: u32,
    pub Name: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub DsmName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_DISK_INFO {
    pub NumberDrives: u32,
    pub DriveInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_VERSION {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub ProductBuild: u32,
    pub QfeNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_COUNTERS {
    pub PathVerifyEnabled: u32,
    pub PathVerificationPeriod: u32,
    pub PDORemovePeriod: u32,
    pub RetryCount: u32,
    pub RetryInterval: u32,
    pub Reserved32: u32,
    pub Reserved64: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_PARAMETERS {
    pub DsmName: [u32; 1],
    pub DsmContext: u64,
    pub DsmVersion: u32,
    pub DsmCounters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_REGISTERED_DSM {
    pub NumberDSMs: u32,
    pub DsmParameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_ADAPTER_INFORMATION {
    pub PathId: u64,
    pub BusNumber: u8,
    pub DeviceNumber: u8,
    pub FunctionNumber: u8,
    pub Pad: u8,
    pub AdapterName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PATH_INFORMATION {
    pub NumberPaths: u32,
    pub Pad: u32,
    pub PathList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_CONTROLLER_INFO {
    pub IdentifierType: u32,
    pub IdentifierLength: u32,
    pub Identifier: [u8; 32],
    pub ControllerState: u32,
    pub Pad: u32,
    pub AssociatedDsm: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_CONTROLLER_CONFIGURATION {
    pub NumberControllers: u32,
    pub ControllerInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_TIMERS_COUNTERS {
    pub PathVerifyEnabled: u32,
    pub PathVerificationPeriod: u32,
    pub PDORemovePeriod: u32,
    pub RetryCount: u32,
    pub RetryInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIOMoveDevice_IN {
    pub DiskOrdinal: u32,
    pub Flags: u32,
    pub PathID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPathConfiguration_IN {
    pub PathID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GetPathConfiguration_OUT {
    pub EntryCount: u32,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetDSMCounters_IN {
    pub DsmContext: u64,
    pub DsmCounters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ClearPathHealthCounters_IN {
    pub PathID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ClearMpioDiskHealthCounters_IN {
    pub DiskOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_EventEntry {
    pub TimeStamp: u64,
    pub Severity: u32,
    pub Component: [u32; 1],
    pub EventDescription: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_CREATENETROOT_CONTEXT {
    pub RxContext: usize,
    pub pVNetRoot: usize,
    pub FinishEvent: u32,
    pub VirtualNetRootStatus: u32,
    pub NetRootStatus: u32,
    pub WorkQueueItem: u32,
    pub Callback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_SRVCALL_CALLBACK_CONTEXT {
    pub field_SrvCalldownStructure: u32,
    pub CallbackContextOrdinal: u32,
    pub RxDeviceObject: usize,
    pub Status: u32,
    pub RecommunicateContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_SRVCALLDOWN_STRUCTURE {
    pub FinishEvent: u32,
    pub SrvCalldownList: u32,
    pub RxContext: usize,
    pub SrvCall: usize,
    pub CallBack: usize,
    pub CalldownCancelled: u8,
    pub NumberRemaining: u32,
    pub NumberToWait: u32,
    pub BestFinisherOrdinal: u32,
    pub BestFinisher: usize,
    pub CallbackContexts: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOWIO_LOCK_LIST {
    pub Next: usize,
    pub LockNumber: u32,
    pub ByteOffset: u32,
    pub Length: i64,
    pub ExclusiveLock: u8,
    pub Key: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XXCTL_LOWIO_COMPONENT {
    pub Flags: u32,
    pub FsControlCode: u32,
    pub IoControlCode: u32,
    pub InputBufferLength: u32,
    pub pInputBuffer: usize,
    pub OutputBufferLength: u32,
    pub pOutputBuffer: usize,
    pub MinorFunction: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOWIO_CONTEXT {
    pub Operation: u16,
    pub Flags: u16,
    pub CompletionRoutine: usize,
    pub Resource: usize,
    pub ResourceThreadId: u32,
    pub Flags_1: u32,
    pub Buffer: usize,
    pub ByteOffset: u32,
    pub ByteCount: u32,
    pub Key: u32,
    pub NonPagedFcb: usize,
    pub LockList: usize,
    pub Length: i64,
    pub Flags_2: u32,
    pub ByteOffset_1: u32,
    pub Key_1: u32,
    pub FsCtl: u32,
    pub IoCtl: u32,
    pub WatchTree: u8,
    pub CompletionFilter: u32,
    pub NotificationBufferLength: u32,
    pub pNotificationBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MINIRDR_DISPATCH {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub MRxFlags: u32,
    pub MRxSrvCallSize: u32,
    pub MRxNetRootSize: u32,
    pub MRxVNetRootSize: u32,
    pub MRxFcbSize: u32,
    pub MRxSrvOpenSize: u32,
    pub MRxFobxSize: u32,
    pub MRxStart: usize,
    pub MRxStop: usize,
    pub MRxCancel: usize,
    pub MRxCreate: usize,
    pub MRxCollapseOpen: usize,
    pub MRxShouldTryToCollapseThisOpen: usize,
    pub MRxFlush: usize,
    pub MRxZeroExtend: usize,
    pub MRxTruncate: usize,
    pub MRxCleanupFobx: usize,
    pub MRxCloseSrvOpen: usize,
    pub MRxDeallocateForFcb: usize,
    pub MRxDeallocateForFobx: usize,
    pub MRxIsLockRealizable: usize,
    pub MRxForceClosed: usize,
    pub MRxAreFilesAliased: usize,
    pub MRxOpenPrintFile: usize,
    pub MRxClosePrintFile: usize,
    pub MRxWritePrintFile: usize,
    pub MRxEnumeratePrintQueue: usize,
    pub MRxClosedSrvOpenTimeOut: usize,
    pub MRxClosedFcbTimeOut: usize,
    pub MRxQueryDirectory: usize,
    pub MRxQueryFileInfo: usize,
    pub MRxSetFileInfo: usize,
    pub MRxSetFileInfoAtCleanup: usize,
    pub MRxQueryEaInfo: usize,
    pub MRxSetEaInfo: usize,
    pub MRxQuerySdInfo: usize,
    pub MRxSetSdInfo: usize,
    pub MRxQueryQuotaInfo: usize,
    pub MRxSetQuotaInfo: usize,
    pub MRxQueryVolumeInfo: usize,
    pub MRxSetVolumeInfo: usize,
    pub MRxIsValidDirectory: usize,
    pub MRxComputeNewBufferingState: usize,
    pub MRxLowIOSubmit: [usize; 1],
    pub MRxExtendForCache: usize,
    pub MRxExtendForNonCache: usize,
    pub MRxCompleteBufferingStateChangeRequest: usize,
    pub MRxCreateVNetRoot: usize,
    pub MRxFinalizeVNetRoot: usize,
    pub MRxFinalizeNetRoot: usize,
    pub MRxUpdateNetRootState: usize,
    pub MRxExtractNetRootName: usize,
    pub MRxCreateSrvCall: usize,
    pub MRxCancelCreateSrvCall: usize,
    pub MRxSrvCallWinnerNotify: usize,
    pub MRxFinalizeSrvCall: usize,
    pub MRxDevFcbXXXControlFile: usize,
    pub MRxPreparseName: usize,
    pub MRxGetConnectionId: usize,
    pub ScavengerTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_NORMAL_NODE_HEADER {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub NodeReferenceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_SRV_CALL {
    pub Context: usize,
    pub Context2: usize,
    pub RxDeviceObject: usize,
    pub pSrvCallName: usize,
    pub pPrincipalName: usize,
    pub pDomainName: usize,
    pub Flags: u32,
    pub MaximumNumberOfCloseDelayedFiles: i32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETROOT_THROTTLING_PARAMETERS {
    pub Increment: u32,
    pub MaximumDelay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_NET_ROOT {
    pub pSrvCall: usize,
    pub Context: usize,
    pub Context2: usize,
    pub Flags: u32,
    pub NumberOfFcbs: u32,
    pub NumberOfSrvOpens: u32,
    pub MRxNetRootState: u32,
    pub Type: u32,
    pub PurgeRelationship: u32,
    pub PurgeSyncLocation: u32,
    pub DeviceType: u32,
    pub pNetRootName: usize,
    pub InnerNamePrefix: u32,
    pub ParameterValidationStamp: u32,
    pub DataCollectionSize: u32,
    pub PipeReadThrottlingParameters: u32,
    pub ClusterSize: u32,
    pub ReadAheadGranularity: u32,
    pub LockThrottlingParameters: u32,
    pub RenameInfoOverallocationSize: u32,
    pub VolumeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_V_NET_ROOT {
    pub pNetRoot: usize,
    pub Context: usize,
    pub Context2: usize,
    pub Flags: u32,
    pub NumberOfOpens: u32,
    pub NumberOfFobxs: u32,
    pub LogonId: u32,
    pub pUserDomainName: usize,
    pub pUserName: usize,
    pub pPassword: usize,
    pub SessionId: u32,
    pub ConstructionStatus: u32,
    pub IsExplicitConnection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_FCB {
    pub Header: u32,
    pub pNetRoot: usize,
    pub Context: usize,
    pub Context2: usize,
    pub NodeReferenceCount: u32,
    pub FcbState: u32,
    pub UncleanCount: u32,
    pub UncachedUncleanCount: u32,
    pub OpenCount: u32,
    pub OutstandingLockOperationsCount: u32,
    pub ActualAllocationLength: u64,
    pub Attributes: u32,
    pub IsFileWritten: u8,
    pub fShouldBeOrphaned: u8,
    pub fMiniInited: u8,
    pub CachedNetRootType: u8,
    pub SrvOpenList: u32,
    pub SrvOpenListVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_SRV_OPEN {
    pub pFcb: usize,
    pub pVNetRoot: usize,
    pub Context: usize,
    pub Context2: usize,
    pub ShadowContext: usize,
    pub Flags: u32,
    pub pAlreadyPrefixedName: usize,
    pub UncleanFobxCount: u32,
    pub OpenCount: u32,
    pub Key: usize,
    pub DesiredAccess: u32,
    pub ShareAccess: u32,
    pub CreateOptions: u32,
    pub BufferingFlags: u32,
    pub ulFileSizeVersion: u32,
    pub SrvOpenQLinks: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_PIPE_HANDLE_INFORMATION {
    pub TypeOfPipe: u32,
    pub ReadMode: u32,
    pub CompletionMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_FOBX {
    pub pSrvOpen: usize,
    pub AssociatedFileObject: usize,
    pub Context: usize,
    pub Context2: usize,
    pub Flags: u32,
    pub UnicodeQueryTemplate: u32,
    pub PipeHandleInformation: usize,
    pub OffsetOfNextEaToReturn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_NAME_CACHE {
    pub Context: u32,
    pub ContextExtension: usize,
    pub PriorStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NAME_CACHE {
    pub spacer: u32,
    pub ExpireTime: i64,
    pub Link: u32,
    pub Name: u32,
    pub HashValue: u32,
    pub CaseInsensitive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NAME_CACHE_CONTROL {
    pub NameCacheLock: u32,
    pub ActiveList: u32,
    pub FreeList: u32,
    pub EntryCount: u32,
    pub MaximumEntries: u32,
    pub MRxNameCacheSize: u32,
    pub NumberActivates: u32,
    pub NumberChecks: u32,
    pub NumberNameHits: u32,
    pub NumberNetOpsSaved: u32,
    pub Spare: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GENERIC_OBJECT {
    pub Header: u32,
    pub Caller: usize,
    pub CallersCaller: usize,
    pub DriverObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SPIN_LOCK {
    pub SpinLock: u32,
    pub OldIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_EVENT {
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WORK_ITEM {
    pub Context: usize,
    pub Routine: u32,
    pub WrapperReserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_LINE_UP {
    pub LinkSpeed: u32,
    pub MaximumTotalSize: u32,
    pub Quality: u32,
    pub SendWindow: u16,
    pub RemoteAddress: [u8; 6],
    pub LocalAddress: [u8; 6],
    pub ProtocolBufferLength: u32,
    pub ProtocolBuffer: *mut u8,
    pub ProtocolType: u16,
    pub DeviceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_LINE_DOWN {
    pub RemoteAddress: [u8; 6],
    pub LocalAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_FRAGMENT {
    pub RemoteAddress: [u8; 6],
    pub LocalAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_GET_STATS {
    pub LocalAddress: [u8; 6],
    pub BytesSent: u32,
    pub BytesRcvd: u32,
    pub FramesSent: u32,
    pub FramesRcvd: u32,
    pub CRCErrors: u32,
    pub TimeoutErrors: u32,
    pub AlignmentErrors: u32,
    pub SerialOverrunErrors: u32,
    pub FramingErrors: u32,
    pub BufferOverrunErrors: u32,
    pub BytesTransmittedUncompressed: u32,
    pub BytesReceivedUncompressed: u32,
    pub BytesTransmittedCompressed: u32,
    pub BytesReceivedCompressed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET_PRIVATE {
    pub PhysicalCount: u32,
    pub TotalLength: u32,
    pub Head: usize,
    pub Tail: usize,
    pub Pool: usize,
    pub Count: u32,
    pub Flags: u32,
    pub ValidCounts: u8,
    pub NdisPacketFlags: u8,
    pub NdisPacketOobOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEDIA_SPECIFIC_INFORMATION {
    pub NextEntryOffset: u32,
    pub ClassId: u32,
    pub Size: u32,
    pub ClassInformation: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET_OOB_DATA {
    pub TimeToSend: u64,
    pub TimeSent: u64,
    pub TimeReceived: u64,
    pub HeaderSize: u32,
    pub SizeMediaSpecificInfo: u32,
    pub MediaSpecificInformation: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET {
    pub Private: u32,
    pub MiniportReserved: [u8; 1],
    pub WrapperReserved: [u8; 1],
    pub MiniportReservedEx: [u8; 1],
    pub WrapperReservedEx: [u8; 1],
    pub MacReserved: [u8; 1],
    pub Reserved: [usize; 2],
    pub ProtocolReserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET_EXTENSION {
    pub NdisPacketInfo: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET_STACK {
    pub IMReserved: [usize; 2],
    pub NdisReserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_IP_CHECKSUM_PACKET_INFO {
    pub NdisPacketChecksumV4: u32,
    pub NdisPacketChecksumV6: u32,
    pub NdisPacketTcpChecksum: u32,
    pub NdisPacketUdpChecksum: u32,
    pub NdisPacketIpChecksum: u32,
    pub NdisPacketTcpChecksumFailed: u32,
    pub NdisPacketUdpChecksumFailed: u32,
    pub NdisPacketIpChecksumFailed: u32,
    pub NdisPacketTcpChecksumSucceeded: u32,
    pub NdisPacketUdpChecksumSucceeded: u32,
    pub NdisPacketIpChecksumSucceeded: u32,
    pub NdisPacketLoopback: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PACKET_8021Q_INFO {
    pub UserPriority: u32,
    pub CanonicalFormatId: u32,
    pub VlanId: u32,
    pub Reserved: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_PACKET {
    pub WanPacketQueue: u32,
    pub CurrentBuffer: *mut u8,
    pub CurrentLength: u32,
    pub StartBuffer: *mut u8,
    pub EndBuffer: *mut u8,
    pub ProtocolReserved1: usize,
    pub ProtocolReserved2: usize,
    pub ProtocolReserved3: usize,
    pub ProtocolReserved4: usize,
    pub MacReserved1: usize,
    pub MacReserved2: usize,
    pub MacReserved3: usize,
    pub MacReserved4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_REQUEST {
    pub MacReserved: [u8; 1],
    pub RequestType: u32,
    pub Oid: u32,
    pub InformationBuffer: usize,
    pub InformationBufferLength: u32,
    pub BytesWritten: u32,
    pub BytesNeeded: u32,
    pub Oid_1: u32,
    pub InformationBuffer_1: usize,
    pub InformationBufferLength_1: u32,
    pub BytesRead: u32,
    pub BytesNeeded_1: u32,
    pub NdisReserved: [u8; 1],
    pub CallMgrReserved: [u8; 1],
    pub ProtocolReserved: [u8; 1],
    pub MiniportReserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_INFORMATION {
    pub Oid: u32,
    pub InformationBuffer: usize,
    pub InformationBufferLength: u32,
    pub BytesWritten: u32,
    pub BytesNeeded: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_INFORMATION {
    pub Oid: u32,
    pub InformationBuffer: usize,
    pub InformationBufferLength: u32,
    pub BytesRead: u32,
    pub BytesNeeded: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PHYSICAL_ADDRESS_UNIT {
    pub PhysicalAddress: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFERENCE {
    pub SpinLock: u32,
    pub ReferenceCount: u16,
    pub Closing: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RW_LOCK {
    pub SpinLock: u32,
    pub Context: usize,
    pub Reserved: [u8; 16],
    pub RefCount: [u32; 1],
    pub RefCountLock: u32,
    pub SharedRefCount: u32,
    pub WriterWaiting: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCK_STATE {
    pub LockState: u16,
    pub OldIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOCK_STATE_EX {
    pub OldIrql: u32,
    pub LockState: u8,
    pub Flags: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TIMER {
    pub Timer: u32,
    pub Dpc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RESTART_ATTRIBUTES {
    pub Next: usize,
    pub Oid: u32,
    pub DataLength: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CONFIGURATION_OBJECT {
    pub Header: u32,
    pub NdisHandle: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_DRIVER_OPTIONAL_HANDLERS {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PNP_EVENT_NOTIFICATION {
    pub Header: u32,
    pub PortNumber: u32,
    pub NetPnPEvent: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_STATUS_INDICATION {
    pub Header: u32,
    pub SourceHandle: u32,
    pub PortNumber: u32,
    pub StatusCode: u32,
    pub Flags: u32,
    pub DestinationHandle: u32,
    pub RequestId: usize,
    pub StatusBuffer: usize,
    pub StatusBufferSize: u32,
    pub Guid: u32,
    pub NdisReserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TIMER_CHARACTERISTICS {
    pub Header: u32,
    pub AllocationTag: u32,
    pub TimerFunction: usize,
    pub FunctionContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RESTART_GENERAL_ATTRIBUTES {
    pub Header: u32,
    pub MtuSize: u32,
    pub MaxXmitLinkSpeed: u32,
    pub MaxRcvLinkSpeed: u32,
    pub LookaheadSize: u32,
    pub MacOptions: u32,
    pub SupportedPacketFilters: u32,
    pub MaxMulticastListSize: u32,
    pub RecvScaleCapabilities: usize,
    pub AccessType: u32,
    pub Flags: u32,
    pub ConnectionType: u32,
    pub SupportedStatistics: u32,
    pub DataBackFillSize: u32,
    pub ContextBackFillSize: u32,
    pub SupportedOidList: usize,
    pub SupportedOidListLength: u32,
    pub MaxLookaheadSizeAccessed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROCESSOR_INFO {
    pub CpuNumber: u32,
    pub PhysicalPackageId: u32,
    pub CoreId: u32,
    pub HyperThreadID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SYSTEM_PROCESSOR_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub ProcessorVendor: u32,
    pub NumPhysicalPackages: u32,
    pub NumCores: u32,
    pub NumCoresPerPhysicalPackage: u32,
    pub MaxHyperThreadingCpusPerCore: u32,
    pub RssBaseCpu: u32,
    pub RssCpuCount: u32,
    pub RssProcessors: u32,
    pub CpuInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_INTERFACE {
    pub Header: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub FilterRunType: u32,
    pub IfIndex: u32,
    pub NetLuid: u32,
    pub FilterClass: u32,
    pub FilterInstanceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ENUM_FILTERS {
    pub Header: u32,
    pub Flags: u32,
    pub NumberOfFilters: u32,
    pub OffsetFirstFilter: u32,
    pub Filter: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_DEVICE_OBJECT_ATTRIBUTES {
    pub Header: u32,
    pub DeviceName: usize,
    pub SymbolicName: usize,
    pub MajorFunctions: usize,
    pub ExtensionSize: u32,
    pub DefaultSDDLString: usize,
    pub DeviceClassGuid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_DRIVER_WRAPPER_HANDLE {
    pub Header: u32,
    pub NdisWrapperHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SHARED_MEMORY_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
    pub SharedMemoryHandle: u32,
    pub PreferredNode: u32,
    pub Usage: u32,
    pub Length: u32,
    pub VirtualAddress: u32,
    pub SGListBufferLength: u32,
    pub SGListBuffer: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SHARED_MEMORY_PROVIDER_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub ProviderContext: u32,
    pub AllocateSharedMemoryHandler: u32,
    pub FreeSharedMemoryHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_STATE {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
    pub QueueState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SCATTER_GATHER_LIST_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
    pub SharedMemoryUsage: u32,
    pub Mdl: usize,
    pub CurrentVa: usize,
    pub Length: u32,
    pub ProcessSGListHandler: u32,
    pub Context: usize,
    pub ScatterGatherListBuffer: usize,
    pub ScatterGatherListBufferSize: u32,
    pub ScatterGatherListBufferSizeNeeded: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_VF_CONFIG_STATE {
    pub Header: u32,
    pub VFId: u32,
    pub BlockId: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_QUEUE_STATE_CHANGE {
    pub Header: u32,
    pub QueueId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NIC_STATUS_INDICATION {
    pub Header: u32,
    pub Flags: u32,
    pub SourcePortId: u32,
    pub SourceNicIndex: u32,
    pub DestinationPortId: u32,
    pub DestinationNicIndex: u32,
    pub StatusIndication: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PD_BUFFER_8021Q_INFO {
    pub UserPriority: u32,
    pub CanonicalFormatId: u32,
    pub VlanId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PD_BUFFER_VIRTUAL_SUBNET_INFO {
    pub VirtualSubnetId: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PD_BUFFER {
    pub NextPDBuffer: usize,
    pub NextPartialPDBuffer: usize,
    pub PDClientReserved: usize,
    pub PDClientContext: u32,
    pub DataBufferVirtualAddress: *mut u8,
    pub DataBufferDmaLogicalAddress: u32,
    pub DataBufferSize: u32,
    pub PDClientContextSize: u16,
    pub Attributes: u16,
    pub Flags: u16,
    pub DataStart: u16,
    pub DataLength: u32,
    pub RxFilterContext: u32,
    pub GftFlowEntryId: u32,
    pub RxHashValue: u32,
    pub RxIPHeaderChecksumSucceeded: u32,
    pub RxTCPChecksumSucceeded: u32,
    pub RxUDPChecksumSucceeded: u32,
    pub RxIPHeaderChecksumFailed: u32,
    pub RxTCPChecksumFailed: u32,
    pub RxUDPChecksumFailed: u32,
    pub RxHashComputed: u32,
    pub RxHashWithL4PortNumbers: u32,
    pub RxGftDirectionIngress: u32,
    pub RxGftExceptionPacket: u32,
    pub RxGftCopyPacket: u32,
    pub RxGftSamplePacket: u32,
    pub RxReserved1: u32,
    pub RxCoalescedSegCount: u32,
    pub RxRscTcpTimestampDelta: u32,
    pub RxOffloads: [u32; 2],
    pub TxIsIPv4: u32,
    pub TxIsIPv6: u32,
    pub TxTransportHeaderOffset: u32,
    pub TxMSS: u32,
    pub TxComputeIPHeaderChecksum: u32,
    pub TxComputeTCPChecksum: u32,
    pub TxComputeUDPChecksum: u32,
    pub TxIsEncapsulatedPacket: u32,
    pub TxInnerPacketOffsetsValid: u32,
    pub TxReserved1: u32,
    pub TxInnerFrameOffset: u32,
    pub TxInnerIpHeaderRelativeOffset: u32,
    pub TxInnerIsIPv6: u32,
    pub TxInnerTcpOptionsPresent: u32,
    pub TxOffloads: [u32; 2],
    pub VirtualSubnetInfo: usize,
    pub Ieee8021qInfo: usize,
    pub GftSourceVPortId: u16,
    pub Reserved: u32,
    pub ProviderScratch: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_POST_AND_DRAIN_ARG {
    pub PostBufferListHead: usize,
    pub DrainBufferListTail: usize,
    pub MaxDrainCount: u32,
    pub DrainCount: u32,
    pub PostCount: u32,
    pub QueueDepthThresholdReached: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_QUEUE_DISPATCH {
    pub Header: u32,
    pub Flags: u32,
    pub PDPostAndDrainBufferList: u32,
    pub PDQueryQueueDepth: u32,
    pub PDFlushQueue: u32,
    pub PDPostAndDrainBufferListEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_QUEUE {
    pub Header: u32,
    pub Flags: u32,
    pub Dispatch: usize,
    pub PDPlatformReserved: [usize; 2],
    pub PDClientReserved: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_RECEIVE_QUEUE_COUNTER {
    pub PacketsReceived: u32,
    pub BytesReceived: u32,
    pub PacketsDropped: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_TRANSMIT_QUEUE_COUNTER {
    pub PacketsTransmitted: u32,
    pub BytesTransmitted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_FILTER_COUNTER {
    pub PacketsMatched: u32,
    pub BytesMatched: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_COUNTER_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub CounterName: usize,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PD_PROVIDER_DISPATCH {
    pub Header: u32,
    pub Flags: u32,
    pub NdisPDAllocateQueue: u32,
    pub NdisPDFreeQueue: u32,
    pub NdisPDAcquireReceiveQueues: u32,
    pub NdisPDReleaseReceiveQueues: u32,
    pub NdisPDAllocateCounter: u32,
    pub NdisPDFreeCounter: u32,
    pub NdisPDQueryCounter: u32,
    pub NdisPDSetReceiveFilter: u32,
    pub NdisPDClearReceiveFilter: u32,
    pub NdisPDRequestDrainNotification: u32,
    pub NdisPDQueueControl: u32,
    pub NdisPDProviderControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NET_BUFFER_LIST_MEDIA_SPECIFIC_INFO {
    pub MediaSpecificInfo: usize,
    pub NativeWifiSpecificInfo: usize,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NBL_MEDIA_MEDIA_SPECIFIC_INFORMATION {
    pub NextEntry: usize,
    pub Tag: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NET_BUFFER_LIST_VIRTUAL_SUBNET_INFO {
    pub VirtualSubnetId: u32,
    pub ReservedVsidBits: u32,
    pub Reserved: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NET_BUFFER_LIST_GFT_OFFLOAD_INFO {
    pub Flags: u32,
    pub VPortId: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_DESTINATION {
    pub PortId: u32,
    pub NicIndex: u32,
    pub IsExcluded: u16,
    pub PreserveVLAN: u16,
    pub PreservePriority: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_FORWARDING_DESTINATION_ARRAY {
    pub Header: u32,
    pub ElementSize: u32,
    pub NumElements: u32,
    pub NumDestinations: u32,
    pub FirstElement: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NBL_MEDIA_SPECIFIC_INFORMATION_EX {
    pub Header: u32,
    pub NextEntry: usize,
    pub Tag: u32,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NET_BUFFER_LIST_FILTERING_INFO {
    pub FilterId: u16,
    pub QueueId: u16,
    pub VPortId: u16,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_PACKET_INFO {
    pub OffloadHandle: u32,
    pub NextOffloadHandle: u32,
    pub SA_DELETE_REQ: u32,
    pub CRYPTO_DONE: u32,
    pub NEXT_CRYPTO_DONE: u32,
    pub CryptoStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V1_NET_BUFFER_LIST_INFO {
    pub OffloadHandle: u32,
    pub SaDeleteReq: u16,
    pub CryptoDone: u16,
    pub NextCryptoDone: u16,
    pub Pad: u16,
    pub CryptoStatus: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ENCAPSULATION_FORMAT {
    pub Encapsulation: u32,
    pub FixedHeaderSize: u32,
    pub Reserved: u32,
    pub EncapsulationHeaderSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TASK_OFFLOAD_HEADER {
    pub Version: u32,
    pub Size: u32,
    pub Reserved: u32,
    pub OffsetFirstTask: u32,
    pub EncapsulationFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TASK_OFFLOAD {
    pub Version: u32,
    pub Size: u32,
    pub Task: u32,
    pub OffsetNextTask: u32,
    pub TaskBufferLength: u32,
    pub TaskBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TASK_TCP_IP_CHECKSUM {
    pub IpOptionsSupported: u32,
    pub TcpOptionsSupported: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
    pub IpChecksum: u32,
    pub IpOptionsSupported_1: u32,
    pub TcpOptionsSupported_1: u32,
    pub TcpChecksum_1: u32,
    pub UdpChecksum_1: u32,
    pub IpChecksum_1: u32,
    pub IpOptionsSupported_2: u32,
    pub TcpOptionsSupported_2: u32,
    pub TcpChecksum_2: u32,
    pub UdpChecksum_2: u32,
    pub IpOptionsSupported_3: u32,
    pub TcpOptionsSupported_3: u32,
    pub TcpChecksum_3: u32,
    pub UdpChecksum_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TASK_TCP_LARGE_SEND {
    pub Version: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub TcpOptions: u8,
    pub IpOptions: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TASK_IPSEC {
    pub AH_ESP_COMBINED: u32,
    pub TRANSPORT_TUNNEL_COMBINED: u32,
    pub V4_OPTIONS: u32,
    pub RESERVED: u32,
    pub MD5: u32,
    pub SHA_1: u32,
    pub Transport: u32,
    pub Tunnel: u32,
    pub Send: u32,
    pub Receive: u32,
    pub DES: u32,
    pub RESERVED_1: u32,
    pub TRIPLE_DES: u32,
    pub NULL_ESP: u32,
    pub Transport_1: u32,
    pub Tunnel_1: u32,
    pub Send_1: u32,
    pub Receive_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_SEND_OFFLOADS_SUPPLEMENTAL_NET_BUFFER_LIST_INFO {
    pub IsEncapsulatedPacket: u32,
    pub EncapsulatedPacketOffsetsValid: u32,
    pub InnerFrameOffset: u32,
    pub TransportIpHeaderRelativeOffset: u32,
    pub TcpHeaderRelativeOffset: u32,
    pub IsInnerIPv6: u32,
    pub TcpOptionsPresent: u32,
    pub Reserved: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V2_NET_BUFFER_LIST_INFO {
    pub OffloadHandle: usize,
    pub SaDeleteReq: u32,
    pub CryptoDone: u32,
    pub NextCryptoDone: u32,
    pub Reserved: u32,
    pub CryptoStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V2_TUNNEL_NET_BUFFER_LIST_INFO {
    pub TunnelHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V2_HEADER_NET_BUFFER_LIST_INFO {
    pub NextHeader: u32,
    pub PadLength: u32,
    pub AhHeaderOffset: u32,
    pub EspHeaderOffset: u32,
    pub NextHeader_1: u32,
    pub PadLength_1: u32,
    pub HeaderInfoSet: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_ALGORITHM_INFO {
    pub Identifier: u32,
    pub KeyLength: u32,
    pub KeyOffsetBytes: u32,
    pub AdditionalInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_SECURITY_ASSOCIATION {
    pub Flags: u32,
    pub Operation: u32,
    pub Spi: u32,
    pub AuthenticationAlgorithm: u32,
    pub EncryptionAlgorithm: u32,
    pub SequenceNumberHighOrder: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_ADD_SA {
    pub Header: u32,
    pub Next: usize,
    pub NumExtHdrs: u32,
    pub Flags: u32,
    pub SrcAddr: u32,
    pub DestAddr: u32,
    pub SrcAddr_1: [u8; 16],
    pub DestAddr_1: [u8; 16],
    pub OffloadHandle: u32,
    pub UdpEspEncapsulation: u32,
    pub SecAssoc: [u32; 1],
    pub KeyLength: u32,
    pub KeyData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_ADD_SA_EX {
    pub Header: u32,
    pub NumExtHdrs: u32,
    pub Flags: u32,
    pub SrcAddr: u32,
    pub DestAddr: u32,
    pub SrcAddr_1: [u8; 16],
    pub DestAddr_1: [u8; 16],
    pub OffloadHandle: u32,
    pub UdpEspEncapsulation: u32,
    pub SecAssoc: [u32; 1],
    pub KeyLength: u32,
    pub KeyOffset: u32,
    pub SourceSwitchPortId: u32,
    pub VlanId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_DELETE_SA {
    pub Header: u32,
    pub Next: usize,
    pub OffloadHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_OFFLOAD_V2_UPDATE_SA {
    pub Header: u32,
    pub OffloadHandle: u32,
    pub Operation: u32,
    pub Spi: u32,
    pub SequenceNumberHighOrder: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS40_PROTOCOL_CHARACTERISTICS {
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub Flags: u32,
    pub OpenAdapterCompleteHandler: u32,
    pub CloseAdapterCompleteHandler: u32,
    pub SendCompleteHandler: u32,
    pub WanSendCompleteHandler: u32,
    pub TransferDataCompleteHandler: u32,
    pub WanTransferDataCompleteHandler: u32,
    pub ResetCompleteHandler: u32,
    pub RequestCompleteHandler: u32,
    pub ReceiveHandler: u32,
    pub WanReceiveHandler: u32,
    pub ReceiveCompleteHandler: u32,
    pub StatusHandler: u32,
    pub StatusCompleteHandler: u32,
    pub Name: u32,
    pub ReceivePacketHandler: u32,
    pub BindAdapterHandler: u32,
    pub UnbindAdapterHandler: u32,
    pub PnPEventHandler: usize,
    pub UnloadHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS50_PROTOCOL_CHARACTERISTICS {
    pub Ndis40Chars: u32,
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub Flags: u32,
    pub OpenAdapterCompleteHandler: u32,
    pub CloseAdapterCompleteHandler: u32,
    pub SendCompleteHandler: u32,
    pub WanSendCompleteHandler: u32,
    pub TransferDataCompleteHandler: u32,
    pub WanTransferDataCompleteHandler: u32,
    pub ResetCompleteHandler: u32,
    pub RequestCompleteHandler: u32,
    pub ReceiveHandler: u32,
    pub WanReceiveHandler: u32,
    pub ReceiveCompleteHandler: u32,
    pub StatusHandler: u32,
    pub StatusCompleteHandler: u32,
    pub Name: u32,
    pub ReceivePacketHandler: u32,
    pub BindAdapterHandler: u32,
    pub UnbindAdapterHandler: u32,
    pub PnPEventHandler: usize,
    pub UnloadHandler: u32,
    pub ReservedHandlers: [usize; 4],
    pub CoSendCompleteHandler: u32,
    pub CoStatusHandler: u32,
    pub CoReceivePacketHandler: u32,
    pub CoAfRegisterNotifyHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_BIND_PARAMETERS {
    pub Header: u32,
    pub ProtocolSection: usize,
    pub AdapterName: usize,
    pub PhysicalDeviceObject: usize,
    pub MediaType: u32,
    pub MtuSize: u32,
    pub MaxXmitLinkSpeed: u32,
    pub XmitLinkSpeed: u32,
    pub MaxRcvLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub LookaheadSize: u32,
    pub PowerManagementCapabilities: usize,
    pub SupportedPacketFilters: u32,
    pub MaxMulticastListSize: u32,
    pub MacAddressLength: u16,
    pub CurrentMacAddress: [u8; 1],
    pub PhysicalMediumType: u32,
    pub RcvScaleCapabilities: usize,
    pub BoundIfNetluid: u32,
    pub BoundIfIndex: u32,
    pub LowestIfNetluid: u32,
    pub LowestIfIndex: u32,
    pub AccessType: u32,
    pub DirectionType: u32,
    pub ConnectionType: u32,
    pub IfType: u32,
    pub IfConnectorPresent: u8,
    pub ActivePorts: usize,
    pub DataBackFillSize: u32,
    pub ContextBackFillSize: u32,
    pub MacOptions: u32,
    pub CompartmentId: u32,
    pub DefaultOffloadConfiguration: usize,
    pub TcpConnectionOffloadCapabilities: usize,
    pub BoundAdapterName: usize,
    pub HDSplitCurrentConfig: usize,
    pub ReceiveFilterCapabilities: usize,
    pub PowerManagementCapabilitiesEx: usize,
    pub NicSwitchCapabilities: usize,
    pub NDKEnabled: u8,
    pub NDKCapabilities: usize,
    pub SriovCapabilities: usize,
    pub NicSwitchArray: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROTOCOL_CO_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub CoStatusHandlerEx: u32,
    pub CoAfRegisterNotifyHandler: u32,
    pub CoReceiveNetBufferListsHandler: u32,
    pub CoSendNetBufferListsCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROTOCOL_DRIVER_CHARACTERISTICS {
    pub Header: u32,
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub MajorDriverVersion: u8,
    pub MinorDriverVersion: u8,
    pub Flags: u32,
    pub Name: u32,
    pub SetOptionsHandler: u32,
    pub BindAdapterHandlerEx: u32,
    pub UnbindAdapterHandlerEx: u32,
    pub OpenAdapterCompleteHandlerEx: u32,
    pub CloseAdapterCompleteHandlerEx: u32,
    pub NetPnPEventHandler: u32,
    pub UninstallHandler: u32,
    pub OidRequestCompleteHandler: u32,
    pub StatusHandlerEx: u32,
    pub ReceiveNetBufferListsHandler: u32,
    pub SendNetBufferListsCompleteHandler: u32,
    pub DirectOidRequestCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OPEN_PARAMETERS {
    pub Header: u32,
    pub AdapterName: usize,
    pub MediumArray: usize,
    pub MediumArraySize: u32,
    pub SelectedMediumIndex: *mut u32,
    pub FrameTypeArray: usize,
    pub FrameTypeArraySize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROTOCOL_PAUSE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PauseReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROTOCOL_RESTART_PARAMETERS {
    pub Header: u32,
    pub FilterModuleNameBuffer: *mut u8,
    pub FilterModuleNameBufferLength: u32,
    pub RestartAttributes: usize,
    pub BoundIfIndex: u32,
    pub BoundIfNetluid: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_BIND_FAILED_NOTIFICATION {
    pub Header: u32,
    pub MiniportNetLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_COMMON_OPEN_BLOCK {
    pub MacHandle: usize,
    pub BindingHandle: u32,
    pub Reserved6: u32,
    pub Reserved7: u32,
    pub Reserved8: u32,
    pub Reserved9: usize,
    pub Reserved10: u32,
    pub Reserved11: u32,
    pub Reserved12: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub Reserved4: u8,
    pub Reserved13: usize,
    pub Reserved5: u32,
    pub Reserved14: u32,
    pub SendHandler: u32,
    pub WanSendHandler: u32,
    pub TransferDataHandler: u32,
    pub SendCompleteHandler: u32,
    pub TransferDataCompleteHandler: u32,
    pub ReceiveHandler: u32,
    pub ReceiveCompleteHandler: u32,
    pub WanReceiveHandler: u32,
    pub RequestCompleteHandler: u32,
    pub ReceivePacketHandler: u32,
    pub SendPacketsHandler: u32,
    pub ResetHandler: u32,
    pub RequestHandler: u32,
    pub ResetCompleteHandler: u32,
    pub StatusHandler: u32,
    pub StatusCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OPEN_BLOCK {
    pub NdisCommonOpenBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS50_MINIPORT_CHARACTERISTICS {
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub CheckForHangHandler: u32,
    pub DisableInterruptHandler: u32,
    pub EnableInterruptHandler: u32,
    pub HaltHandler: u32,
    pub HandleInterruptHandler: u32,
    pub InitializeHandler: u32,
    pub ISRHandler: u32,
    pub QueryInformationHandler: u32,
    pub ReconfigureHandler: u32,
    pub ResetHandler: u32,
    pub SendHandler: u32,
    pub WanSendHandler: u32,
    pub SetInformationHandler: u32,
    pub TransferDataHandler: u32,
    pub WanTransferDataHandler: u32,
    pub ReturnPacketHandler: u32,
    pub SendPacketsHandler: u32,
    pub AllocateCompleteHandler: u32,
    pub CoCreateVcHandler: u32,
    pub CoDeleteVcHandler: u32,
    pub CoActivateVcHandler: u32,
    pub CoDeactivateVcHandler: u32,
    pub CoSendPacketsHandler: u32,
    pub CoRequestHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_THROTTLE_PARAMETERS {
    pub MaxNblsToIndicate: u32,
    pub MoreNblsPending: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_INTERRUPT_CHARACTERISTICS {
    pub Header: u32,
    pub InterruptHandler: u32,
    pub InterruptDpcHandler: u32,
    pub DisableInterruptHandler: u32,
    pub EnableInterruptHandler: u32,
    pub MsiSupported: u8,
    pub MsiSyncWithAllMessages: u8,
    pub MessageInterruptHandler: u32,
    pub MessageInterruptDpcHandler: u32,
    pub DisableMessageInterruptHandler: u32,
    pub EnableMessageInterruptHandler: u32,
    pub InterruptType: u32,
    pub MessageInfoTable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_PNP_CHARACTERISTICS {
    pub Header: u32,
    pub MiniportAddDeviceHandler: u32,
    pub MiniportRemoveDeviceHandler: u32,
    pub MiniportFilterResourceRequirementsHandler: u32,
    pub MiniportStartDeviceHandler: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS51_MINIPORT_CHARACTERISTICS {
    pub Ndis50Chars: u32,
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub CheckForHangHandler: u32,
    pub DisableInterruptHandler: u32,
    pub EnableInterruptHandler: u32,
    pub HaltHandler: u32,
    pub HandleInterruptHandler: u32,
    pub InitializeHandler: u32,
    pub ISRHandler: u32,
    pub QueryInformationHandler: u32,
    pub ReconfigureHandler: u32,
    pub ResetHandler: u32,
    pub SendHandler: u32,
    pub WanSendHandler: u32,
    pub SetInformationHandler: u32,
    pub TransferDataHandler: u32,
    pub WanTransferDataHandler: u32,
    pub ReturnPacketHandler: u32,
    pub SendPacketsHandler: u32,
    pub AllocateCompleteHandler: u32,
    pub CoCreateVcHandler: u32,
    pub CoDeleteVcHandler: u32,
    pub CoActivateVcHandler: u32,
    pub CoDeactivateVcHandler: u32,
    pub CoSendPacketsHandler: u32,
    pub CoRequestHandler: u32,
    pub CancelSendPacketsHandler: u32,
    pub PnPEventNotifyHandler: u32,
    pub AdapterShutdownHandler: u32,
    pub Reserved1: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_INTERRUPT {
    pub InterruptObject: usize,
    pub IsDeregistered: u8,
    pub DpcCountLock: u32,
    pub Reserved: usize,
    pub MiniportIsr: u32,
    pub MiniportDpc: u32,
    pub InterruptDpc: u32,
    pub Miniport: usize,
    pub DpcCount: i32,
    pub DpcsCompletedEvent: u32,
    pub SharedInterrupt: u8,
    pub IsrRequested: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_TIMER {
    pub Timer: u32,
    pub Dpc: u32,
    pub MiniportTimerFunction: usize,
    pub MiniportTimerContext: usize,
    pub Miniport: usize,
    pub field_NextTimer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_DMA_DESCRIPTION {
    pub DemandMode: u8,
    pub AutoInitialize: u8,
    pub DmaChannelSpecified: u8,
    pub DmaWidth: u32,
    pub DmaSpeed: u32,
    pub DmaPort: u32,
    pub DmaChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_DMA_BLOCK {
    pub MapRegisterBase: usize,
    pub AllocationEvent: u32,
    pub SystemAdapterObject: usize,
    pub Miniport: usize,
    pub InProgress: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SG_DMA_DESCRIPTION {
    pub Header: u32,
    pub Flags: u32,
    pub MaximumPhysicalMapping: u32,
    pub ProcessSGListHandler: u32,
    pub SharedMemAllocateCompleteHandler: u32,
    pub ScatterGatherListSize: u32,
    pub field_PdoOverride: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_INIT_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub AllocatedResources: usize,
    pub IMDeviceInstanceContext: u32,
    pub MiniportAddDeviceContext: u32,
    pub IfIndex: u32,
    pub NetLuid: u32,
    pub DefaultPortAuthStates: usize,
    pub PciDeviceCustomProperties: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_RESTART_PARAMETERS {
    pub Header: u32,
    pub RestartAttributes: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_REGISTRATION_ATTRIBUTES {
    pub Header: u32,
    pub MiniportAdapterContext: u32,
    pub AttributeFlags: u32,
    pub CheckForHangTimeInSeconds: u32,
    pub InterfaceType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADD_DEVICE_REGISTRATION_ATTRIBUTES {
    pub Header: u32,
    pub MiniportAddDeviceContext: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_HD_SPLIT_ATTRIBUTES {
    pub Header: u32,
    pub HardwareCapabilities: u32,
    pub CurrentCapabilities: u32,
    pub HDSplitFlags: u32,
    pub BackfillSize: u32,
    pub MaxHeaderSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_HARDWARE_ASSIST_ATTRIBUTES {
    pub Header: u32,
    pub HDSplitAttributes: usize,
    pub HardwareReceiveFilterCapabilities: usize,
    pub CurrentReceiveFilterCapabilities: usize,
    pub HardwareNicSwitchCapabilities: usize,
    pub CurrentNicSwitchCapabilities: usize,
    pub HardwareSriovCapabilities: usize,
    pub CurrentSriovCapabilities: usize,
    pub HardwareQosCapabilities: usize,
    pub CurrentQosCapabilities: usize,
    pub HardwareGftOffloadCapabilities: usize,
    pub CurrentGftOffloadCapabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_OFFLOAD_ATTRIBUTES {
    pub Header: u32,
    pub DefaultOffloadConfiguration: usize,
    pub HardwareOffloadCapabilities: usize,
    pub DefaultTcpConnectionOffloadConfiguration: usize,
    pub TcpConnectionOffloadHardwareCapabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_NDK_ATTRIBUTES {
    pub Header: u32,
    pub Enabled: u8,
    pub NdkCapabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_PACKET_DIRECT_ATTRIBUTES {
    pub Header: u32,
    pub Flags: u32,
    pub DmaAddressWidth: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_PAUSE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PauseReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DEVICE_PNP_EVENT {
    pub Header: u32,
    pub PortNumber: u32,
    pub DevicePnPEvent: u32,
    pub InformationBuffer: usize,
    pub InformationBufferLength: u32,
    pub NdisReserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_DRIVER_CHARACTERISTICS {
    pub Header: u32,
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub MajorDriverVersion: u8,
    pub MinorDriverVersion: u8,
    pub Flags: u32,
    pub SetOptionsHandler: u32,
    pub InitializeHandlerEx: u32,
    pub HaltHandlerEx: u32,
    pub UnloadHandler: u32,
    pub PauseHandler: u32,
    pub RestartHandler: u32,
    pub OidRequestHandler: u32,
    pub SendNetBufferListsHandler: u32,
    pub ReturnNetBufferListsHandler: u32,
    pub CancelSendHandler: u32,
    pub CheckForHangHandlerEx: u32,
    pub ResetHandlerEx: u32,
    pub DevicePnPEventNotifyHandler: u32,
    pub ShutdownHandlerEx: u32,
    pub CancelOidRequestHandler: u32,
    pub DirectOidRequestHandler: u32,
    pub CancelDirectOidRequestHandler: u32,
    pub SynchronousOidRequestHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_CO_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub CoCreateVcHandler: u32,
    pub CoDeleteVcHandler: u32,
    pub CoActivateVcHandler: u32,
    pub CoDeactivateVcHandler: u32,
    pub CoSendNetBufferListsHandler: u32,
    pub CoOidRequestHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_BLOCK {
    pub Header: u32,
    pub NextMiniport: usize,
    pub BaseMiniport: usize,
    pub MiniportAdapterContext: u32,
    pub Reserved4: u32,
    pub Reserved10: usize,
    pub OpenQueue: u32,
    pub ShortRef: u32,
    pub Reserved12: u32,
    pub Padding1: u8,
    pub LockAcquired: u8,
    pub PmodeOpens: u8,
    pub Reserved23: u8,
    pub Lock: u32,
    pub Reserved25: usize,
    pub Interrupt: usize,
    pub Flags: u32,
    pub PnPFlags: u32,
    pub PacketList: u32,
    pub FirstPendingPacket: usize,
    pub ReturnPacketsQueue: usize,
    pub RequestBuffer: u32,
    pub Reserved26: usize,
    pub PrimaryMiniport: usize,
    pub Reserved11: usize,
    pub BusDataContext: usize,
    pub Reserved3: u32,
    pub Resources: usize,
    pub WakeUpDpcTimer: u32,
    pub Reserved20: u32,
    pub SymbolicLinkName: u32,
    pub CheckForHangSeconds: u32,
    pub Reserved5: u32,
    pub ResetStatus: u32,
    pub ResetOpen: u32,
    pub FilterDbs: u32,
    pub PacketIndicateHandler: u32,
    pub SendCompleteHandler: u32,
    pub SendResourcesHandler: u32,
    pub ResetCompleteHandler: u32,
    pub MediaType: u32,
    pub BusNumber: u32,
    pub BusType: u32,
    pub AdapterType: u32,
    pub Reserved6: usize,
    pub Reserved7: usize,
    pub Reserved8: usize,
    pub MiniportSGDmaBlock: usize,
    pub CallMgrAfList: usize,
    pub MiniportThread: usize,
    pub SetInfoBuf: usize,
    pub SetInfoBufLen: u16,
    pub MaxSendPackets: u16,
    pub FakeStatus: u32,
    pub Reserved24: usize,
    pub Reserved9: usize,
    pub Reserved21: usize,
    pub MacOptions: u32,
    pub PendingRequest: usize,
    pub MaximumLongAddresses: u32,
    pub Reserved27: u32,
    pub CurrentLookahead: u32,
    pub MaximumLookahead: u32,
    pub Reserved1: usize,
    pub DisableInterruptHandler: u32,
    pub EnableInterruptHandler: u32,
    pub SendPacketsHandler: u32,
    pub DeferredSendHandler: u32,
    pub EthRxIndicateHandler: u32,
    pub TrRxIndicateHandler: u32,
    pub Reserved2: usize,
    pub EthRxCompleteHandler: u32,
    pub TrRxCompleteHandler: u32,
    pub Reserved22: usize,
    pub StatusHandler: u32,
    pub StatusCompleteHandler: u32,
    pub TDCompleteHandler: u32,
    pub QueryCompleteHandler: u32,
    pub SetCompleteHandler: u32,
    pub WanSendCompleteHandler: u32,
    pub WanRcvHandler: u32,
    pub WanRcvCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MSIX_CONFIG_PARAMETERS {
    pub Header: u32,
    pub ConfigOperation: u32,
    pub TableEntry: u32,
    pub MessageNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_SS_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub IdleNotificationHandler: u32,
    pub CancelIdleNotificationHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NET_BUFFER_LIST_CONTEXT_TYPE_INFO {
    pub Header: u32,
    pub ContextName: *mut i8,
    pub ExtensionId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_OPTIONAL_HANDLERS {
    pub Header: u32,
    pub AllocateNetBufferListForwardingContext: u32,
    pub FreeNetBufferListForwardingContext: u32,
    pub SetNetBufferListSource: u32,
    pub AddNetBufferListDestination: u32,
    pub GrowNetBufferListDestinations: u32,
    pub GetNetBufferListDestinations: u32,
    pub UpdateNetBufferListDestinations: u32,
    pub CopyNetBufferListInfo: u32,
    pub ReferenceSwitchNic: u32,
    pub DereferenceSwitchNic: u32,
    pub ReferenceSwitchPort: u32,
    pub DereferenceSwitchPort: u32,
    pub ReportFilteredNetBufferLists: u32,
    pub SetNetBufferListSwitchContext: u32,
    pub GetNetBufferListSwitchContext: u32,
    pub SwitchPDReserved: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CLIENT_CHARACTERISTICS {
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub ClCreateVcHandler: u32,
    pub ClDeleteVcHandler: u32,
    pub ClRequestHandler: u32,
    pub ClRequestCompleteHandler: u32,
    pub ClOpenAfCompleteHandler: u32,
    pub ClCloseAfCompleteHandler: u32,
    pub ClRegisterSapCompleteHandler: u32,
    pub ClDeregisterSapCompleteHandler: u32,
    pub ClMakeCallCompleteHandler: u32,
    pub ClModifyCallQoSCompleteHandler: u32,
    pub ClCloseCallCompleteHandler: u32,
    pub ClAddPartyCompleteHandler: u32,
    pub ClDropPartyCompleteHandler: u32,
    pub ClIncomingCallHandler: u32,
    pub ClIncomingCallQoSChangeHandler: u32,
    pub ClIncomingCloseCallHandler: u32,
    pub ClIncomingDropPartyHandler: u32,
    pub ClCallConnectedHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CALL_MANAGER_CHARACTERISTICS {
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub Filler: u16,
    pub Reserved: u32,
    pub CmCreateVcHandler: u32,
    pub CmDeleteVcHandler: u32,
    pub CmOpenAfHandler: u32,
    pub CmCloseAfHandler: u32,
    pub CmRegisterSapHandler: u32,
    pub CmDeregisterSapHandler: u32,
    pub CmMakeCallHandler: u32,
    pub CmCloseCallHandler: u32,
    pub CmIncomingCallCompleteHandler: u32,
    pub CmAddPartyHandler: u32,
    pub CmDropPartyHandler: u32,
    pub CmActivateVcCompleteHandler: u32,
    pub CmDeactivateVcCompleteHandler: u32,
    pub CmModifyCallQoSHandler: u32,
    pub CmRequestHandler: u32,
    pub CmRequestCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_ADDRESS {
    pub AddressSize: u32,
    pub Address: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_ADDRESS_LIST {
    pub NumberOfAddressesAvailable: u32,
    pub NumberOfAddresses: u32,
    pub AddressList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_SPECIFIC_PARAMETERS {
    pub ParamType: u32,
    pub Length: u32,
    pub Parameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_CALL_MANAGER_PARAMETERS {
    pub Transmit: u32,
    pub Receive: u32,
    pub CallMgrSpecific: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_MEDIA_PARAMETERS {
    pub Flags: u32,
    pub ReceivePriority: u32,
    pub ReceiveSizeHint: u32,
    pub MediaSpecific: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_CALL_PARAMETERS {
    pub Flags: u32,
    pub CallMgrParameters: usize,
    pub MediaParameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_PVC {
    pub NdisAfHandle: u32,
    pub PvcParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VAR_STRING {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulStringFormat: u32,
    pub ulStringSize: u32,
    pub ulStringOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CO_CLIENT_OPTIONAL_HANDLERS {
    pub Header: u32,
    pub Reserved: u32,
    pub ClCreateVcHandler: u32,
    pub ClDeleteVcHandler: u32,
    pub ClOidRequestHandler: u32,
    pub ClOidRequestCompleteHandler: u32,
    pub ClOpenAfCompleteHandlerEx: u32,
    pub ClCloseAfCompleteHandler: u32,
    pub ClRegisterSapCompleteHandler: u32,
    pub ClDeregisterSapCompleteHandler: u32,
    pub ClMakeCallCompleteHandler: u32,
    pub ClModifyCallQoSCompleteHandler: u32,
    pub ClCloseCallCompleteHandler: u32,
    pub ClAddPartyCompleteHandler: u32,
    pub ClDropPartyCompleteHandler: u32,
    pub ClIncomingCallHandler: u32,
    pub ClIncomingCallQoSChangeHandler: u32,
    pub ClIncomingCloseCallHandler: u32,
    pub ClIncomingDropPartyHandler: u32,
    pub ClCallConnectedHandler: u32,
    pub ClNotifyCloseAfHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CO_CALL_MANAGER_OPTIONAL_HANDLERS {
    pub Header: u32,
    pub Reserved: u32,
    pub CmCreateVcHandler: u32,
    pub CmDeleteVcHandler: u32,
    pub CmOpenAfHandler: u32,
    pub CmCloseAfHandler: u32,
    pub CmRegisterSapHandler: u32,
    pub CmDeregisterSapHandler: u32,
    pub CmMakeCallHandler: u32,
    pub CmCloseCallHandler: u32,
    pub CmIncomingCallCompleteHandler: u32,
    pub CmAddPartyHandler: u32,
    pub CmDropPartyHandler: u32,
    pub CmActivateVcCompleteHandler: u32,
    pub CmDeactivateVcCompleteHandler: u32,
    pub CmModifyCallQoSHandler: u32,
    pub CmOidRequestHandler: u32,
    pub CmOidRequestCompleteHandler: u32,
    pub CmNotifyCloseAfCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_ATTACH_PARAMETERS {
    pub Header: u32,
    pub IfIndex: u32,
    pub NetLuid: u32,
    pub FilterModuleGuidName: usize,
    pub BaseMiniportIfIndex: u32,
    pub BaseMiniportInstanceName: usize,
    pub BaseMiniportName: usize,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub MiniportMediaType: u32,
    pub MiniportPhysicalMediaType: u32,
    pub MiniportMediaSpecificAttributes: u32,
    pub DefaultOffloadConfiguration: usize,
    pub MacAddressLength: u16,
    pub CurrentMacAddress: [u8; 1],
    pub BaseMiniportNetLuid: u32,
    pub LowerIfIndex: u32,
    pub LowerIfNetLuid: u32,
    pub Flags: u32,
    pub HDSplitCurrentConfig: usize,
    pub ReceiveFilterCapabilities: usize,
    pub MiniportPhysicalDeviceObject: usize,
    pub NicSwitchCapabilities: usize,
    pub BaseMiniportIfConnectorPresent: u8,
    pub SriovCapabilities: usize,
    pub NicSwitchArray: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_RESTART_PARAMETERS {
    pub Header: u32,
    pub MiniportMediaType: u32,
    pub MiniportPhysicalMediaType: u32,
    pub RestartAttributes: usize,
    pub LowerIfIndex: u32,
    pub LowerIfNetLuid: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_PAUSE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PauseReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_PARTIAL_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub SendNetBufferListsHandler: u32,
    pub SendNetBufferListsCompleteHandler: u32,
    pub CancelSendNetBufferListsHandler: u32,
    pub ReceiveNetBufferListsHandler: u32,
    pub ReturnNetBufferListsHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_DRIVER_CHARACTERISTICS {
    pub Header: u32,
    pub MajorNdisVersion: u8,
    pub MinorNdisVersion: u8,
    pub MajorDriverVersion: u8,
    pub MinorDriverVersion: u8,
    pub Flags: u32,
    pub FriendlyName: u32,
    pub UniqueName: u32,
    pub ServiceName: u32,
    pub SetOptionsHandler: u32,
    pub SetFilterModuleOptionsHandler: u32,
    pub AttachHandler: u32,
    pub DetachHandler: u32,
    pub RestartHandler: u32,
    pub PauseHandler: u32,
    pub SendNetBufferListsHandler: u32,
    pub SendNetBufferListsCompleteHandler: u32,
    pub CancelSendNetBufferListsHandler: u32,
    pub ReceiveNetBufferListsHandler: u32,
    pub ReturnNetBufferListsHandler: u32,
    pub OidRequestHandler: u32,
    pub OidRequestCompleteHandler: u32,
    pub CancelOidRequestHandler: u32,
    pub DevicePnPEventNotifyHandler: u32,
    pub NetPnPEventHandler: u32,
    pub StatusHandler: u32,
    pub DirectOidRequestHandler: u32,
    pub DirectOidRequestCompleteHandler: u32,
    pub CancelDirectOidRequestHandler: u32,
    pub SynchronousOidRequestHandler: u32,
    pub SynchronousOidRequestCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_FILTER_ATTRIBUTES {
    pub Header: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IF_PROVIDER_CHARACTERISTICS {
    pub Header: u32,
    pub QueryObjectHandler: u32,
    pub SetObjectHandler: u32,
    pub Reserved1: usize,
    pub Reserved2: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_IF_INFORMATION {
    pub Header: u32,
    pub Flags: u32,
    pub PhysicalLocation: u32,
    pub WanTunnelType: u32,
    pub PortNumber: u32,
    pub AccessType: u32,
    pub DirectionType: u32,
    pub ConnectionType: u32,
    pub ifConnectorPresent: u8,
    pub PhysAddressLength: u16,
    pub PhysAddressOffset: u16,
    pub PermanentPhysAddressOffset: u16,
    pub FriendlyNameLength: u16,
    pub FriendlyNameOffset: u16,
    pub InterfaceGuid: u32,
    pub NetworkGuid: u32,
    pub SupportedStatistics: u32,
    pub MediaType: u32,
    pub PhysicalMediumType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_CONNECTION_OFFLOAD_ENCAPSULATION {
    pub Enabled: u8,
    pub EncapsulationType: u32,
    pub HeaderSize: u32,
    pub Enabled_1: u8,
    pub EncapsulationType_1: u32,
    pub HeaderSize_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_CONNECTION_OFFLOAD_PARAMETERS {
    pub Header: u32,
    pub Encapsulation: u32,
    pub TicksPerSecond: u32,
    pub TcpAckFrequency: u8,
    pub TcpDelayedAckTicks: u8,
    pub TcpMaximumRetransmissions: u8,
    pub TcpDoubtReachabilityRetransmissions: u8,
    pub TcpSwsPreventionTicks: u32,
    pub TcpDuplicateAckThreshold: u32,
    pub TcpPushTicks: u32,
    pub NceStaleTicks: u32,
    pub CongestionAlgorithm: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_STATE_HEADER {
    pub Length: u32,
    pub RecognizedOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_OFFLOAD_STATE_CONST {
    pub Header: u32,
    pub Flags: u16,
    pub RemotePort: u16,
    pub LocalPort: u16,
    pub SndWindScale: u8,
    pub RcvWindScale: u8,
    pub RemoteMss: u16,
    pub HashValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_OFFLOAD_STATE_CACHED {
    pub Header: u32,
    pub Flags: u16,
    pub InitialRcvWnd: u32,
    pub RcvIndicationSize: u32,
    pub KaProbeCount: u8,
    pub KaTimeout: u32,
    pub KaInterval: u32,
    pub MaxRT: u32,
    pub FlowLabel: u32,
    pub TtlOrHopLimit: u8,
    pub TosOrTrafficClass: u8,
    pub UserPriority: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_OFFLOAD_STATE_DELEGATED {
    pub Header: u32,
    pub State: u32,
    pub Flags: u16,
    pub RcvNxt: u32,
    pub RcvWnd: u32,
    pub SndUna: u32,
    pub SndNxt: u32,
    pub SndMax: u32,
    pub SndWnd: u32,
    pub MaxSndWnd: u32,
    pub SendWL1: u32,
    pub CWnd: u32,
    pub SsThresh: u32,
    pub SRtt: u16,
    pub RttVar: u16,
    pub TsRecent: u32,
    pub TsRecentAge: u32,
    pub TsTime: u32,
    pub TotalRT: u32,
    pub DupAckCount: u8,
    pub SndWndProbeCount: u8,
    pub ProbeCount: u8,
    pub TimeoutDelta: u32,
    pub Count: u8,
    pub TimeoutDelta_1: u32,
    pub SendDataHead: usize,
    pub SendDataTail: usize,
    pub SendBacklogSize: u32,
    pub BufferedData: usize,
    pub ReceiveBacklogSize: u32,
    pub DWnd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PATH_OFFLOAD_STATE_CONST {
    pub Header: u32,
    pub field_SourceAddress: u32,
    pub field_DestinationAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PATH_OFFLOAD_STATE_CACHED {
    pub Header: u32,
    pub PathMtu: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PATH_OFFLOAD_STATE_DELEGATED {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NEIGHBOR_OFFLOAD_STATE_CONST {
    pub Header: u32,
    pub DlSourceAddress: [u8; 32],
    pub VlanId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NEIGHBOR_OFFLOAD_STATE_CACHED {
    pub Header: u32,
    pub DlDestinationAddress: [u8; 32],
    pub HostReachabilityDelta: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NEIGHBOR_OFFLOAD_STATE_DELEGATED {
    pub Header: u32,
    pub NicReachabilityDelta: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_OFFLOAD_STATS {
    pub InSegments: u32,
    pub OutSegments: u32,
    pub CurrentlyEstablished: u32,
    pub ResetEstablished: u32,
    pub RetransmittedSegments: u32,
    pub InErrors: u32,
    pub OutResets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IP_OFFLOAD_STATS {
    pub InReceives: u32,
    pub InOctets: u32,
    pub InDelivers: u32,
    pub OutRequests: u32,
    pub OutOctets: u32,
    pub InHeaderErrors: u32,
    pub InTruncatedPackets: u32,
    pub InDiscards: u32,
    pub OutDiscards: u32,
    pub OutNoRoutes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_HANDLE {
    pub NdisReserved: [usize; 1],
    pub MiniportOffloadContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_HANDLE_PRIVATE {
    pub Handle: u32,
    pub ProtocolBindingContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROTOCOL_OFFLOAD_BLOCK_LIST {
    pub Header: u32,
    pub field_NextBlock: u32,
    pub field_DependentBlockList: u32,
    pub Status: u32,
    pub NdisReserved: [usize; 3],
    pub OffloadHandle: usize,
    pub ProtocolReserved: [usize; 2],
    pub MiniportReserved: [usize; 2],
    pub ImReserved: [usize; 2],
    pub Scratch: [usize; 2],
    pub SourceHandle: usize,
    pub PortNumber: u32,
    pub NetBufferListChain: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_CLIENT_HANDLERS {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_OFFLOAD_CLIENT_HANDLERS {
    pub Header: u32,
    pub TcpOffloadSendCompleteHandler: u32,
    pub TcpOffloadReceiveCompleteHandler: u32,
    pub TcpOffloadDisconnectCompleteHandler: u32,
    pub TcpOffloadForwardCompleteHandler: u32,
    pub TcpOffloadEventHandler: u32,
    pub TcpOffloadReceiveIndicateHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CLIENT_CHIMNEY_OFFLOAD_GENERIC_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub InitiateOffloadCompleteHandler: u32,
    pub TerminateOffloadCompleteHandler: u32,
    pub UpdateOffloadCompleteHandler: u32,
    pub InvalidateOffloadCompleteHandler: u32,
    pub QueryOffloadCompleteHandler: u32,
    pub IndicateOffloadEventHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CLIENT_CHIMNEY_OFFLOAD_TCP_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub OffloadType: u32,
    pub TcpOffloadSendCompleteHandler: u32,
    pub TcpOffloadReceiveCompleteHandler: u32,
    pub TcpOffloadDisconnectCompleteHandler: u32,
    pub TcpOffloadForwardCompleteHandler: u32,
    pub TcpOffloadEventHandler: u32,
    pub TcpOffloadReceiveIndicateHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_OFFLOAD_BLOCK_LIST {
    pub Header: u32,
    pub field_NextBlock: u32,
    pub field_DependentBlockList: u32,
    pub Status: u32,
    pub NdisReserved: [usize; 2],
    pub field_MiniportOffloadContext: usize,
    pub NdisOffloadHandle: u32,
    pub ProtocolReserved: [usize; 2],
    pub MiniportReserved: [usize; 2],
    pub ImReserved: [usize; 2],
    pub Scratch: [usize; 2],
    pub SourceHandle: usize,
    pub PortNumber: u32,
    pub NetBufferListChain: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROVIDER_CHIMNEY_OFFLOAD_GENERIC_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub InitiateOffloadHandler: u32,
    pub TerminateOffloadHandler: u32,
    pub UpdateOffloadHandler: u32,
    pub InvalidateOffloadHandler: u32,
    pub QueryOffloadHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROVIDER_CHIMNEY_OFFLOAD_TCP_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub OffloadType: u32,
    pub TcpOffloadSendHandler: u32,
    pub TcpOffloadReceiveHandler: u32,
    pub TcpOffloadDisconnectHandler: u32,
    pub TcpOffloadForwardHandler: u32,
    pub TcpOffloadReceiveReturnHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_EVENT_HANDLERS {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_OFFLOAD_EVENT_HANDLERS {
    pub Header: u32,
    pub NdisTcpOffloadEventHandler: u32,
    pub NdisTcpOffloadReceiveHandler: u32,
    pub NdisTcpOffloadSendComplete: u32,
    pub NdisTcpOffloadReceiveComplete: u32,
    pub NdisTcpOffloadDisconnectComplete: u32,
    pub NdisTcpOffloadForwardComplete: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OPEN_NDK_ADAPTER_PARAMETERS {
    pub Version: u32,
    pub SwitchId: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_PROVIDER_CHARACTERISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub OpenNDKAdapterHandler: u32,
    pub CloseNDKAdapterHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_ADDRESS_CAPS {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulLineDeviceID: u32,
    pub ulAddressSize: u32,
    pub ulAddressOffset: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
    pub ulAddressSharing: u32,
    pub ulAddressStates: u32,
    pub ulCallInfoStates: u32,
    pub ulCallerIDFlags: u32,
    pub ulCalledIDFlags: u32,
    pub ulConnectedIDFlags: u32,
    pub ulRedirectionIDFlags: u32,
    pub ulRedirectingIDFlags: u32,
    pub ulCallStates: u32,
    pub ulDialToneModes: u32,
    pub ulBusyModes: u32,
    pub ulSpecialInfo: u32,
    pub ulDisconnectModes: u32,
    pub ulMaxNumActiveCalls: u32,
    pub ulMaxNumOnHoldCalls: u32,
    pub ulMaxNumOnHoldPendingCalls: u32,
    pub ulMaxNumConference: u32,
    pub ulMaxNumTransConf: u32,
    pub ulAddrCapFlags: u32,
    pub ulCallFeatures: u32,
    pub ulRemoveFromConfCaps: u32,
    pub ulRemoveFromConfState: u32,
    pub ulTransferModes: u32,
    pub ulParkModes: u32,
    pub ulForwardModes: u32,
    pub ulMaxForwardEntries: u32,
    pub ulMaxSpecificEntries: u32,
    pub ulMinFwdNumRings: u32,
    pub ulMaxFwdNumRings: u32,
    pub ulMaxCallCompletions: u32,
    pub ulCallCompletionConds: u32,
    pub ulCallCompletionModes: u32,
    pub ulNumCompletionMessages: u32,
    pub ulCompletionMsgTextEntrySize: u32,
    pub ulCompletionMsgTextSize: u32,
    pub ulCompletionMsgTextOffset: u32,
    pub ulAddressFeatures: u32,
    pub ulPredictiveAutoTransferStates: u32,
    pub ulNumCallTreatments: u32,
    pub ulCallTreatmentListSize: u32,
    pub ulCallTreatmentListOffset: u32,
    pub ulDeviceClassesSize: u32,
    pub ulDeviceClassesOffset: u32,
    pub ulMaxCallDataSize: u32,
    pub ulCallFeatures2: u32,
    pub ulMaxNoAnswerTimeout: u32,
    pub ulConnectedModes: u32,
    pub ulOfferingModes: u32,
    pub ulAvailableMediaModes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_ADDRESS_STATUS {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulNumInUse: u32,
    pub ulNumActiveCalls: u32,
    pub ulNumOnHoldCalls: u32,
    pub ulNumOnHoldPendCalls: u32,
    pub ulAddressFeatures: u32,
    pub ulNumRingsNoAnswer: u32,
    pub ulForwardNumEntries: u32,
    pub ulForwardSize: u32,
    pub ulForwardOffset: u32,
    pub ulTerminalModesSize: u32,
    pub ulTerminalModesOffset: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_DIAL_PARAMS {
    pub ulDialPause: u32,
    pub ulDialSpeed: u32,
    pub ulDigitDuration: u32,
    pub ulWaitForDialtone: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_CALL_INFO {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub hLine: u32,
    pub ulLineDeviceID: u32,
    pub ulAddressID: u32,
    pub ulBearerMode: u32,
    pub ulRate: u32,
    pub ulMediaMode: u32,
    pub ulAppSpecific: u32,
    pub ulCallID: u32,
    pub ulRelatedCallID: u32,
    pub ulCallParamFlags: u32,
    pub ulCallStates: u32,
    pub ulMonitorDigitModes: u32,
    pub ulMonitorMediaModes: u32,
    pub DialParams: u32,
    pub ulOrigin: u32,
    pub ulReason: u32,
    pub ulCompletionID: u32,
    pub ulNumOwners: u32,
    pub ulNumMonitors: u32,
    pub ulCountryCode: u32,
    pub ulTrunk: u32,
    pub ulCallerIDFlags: u32,
    pub ulCallerIDSize: u32,
    pub ulCallerIDOffset: u32,
    pub ulCallerIDNameSize: u32,
    pub ulCallerIDNameOffset: u32,
    pub ulCalledIDFlags: u32,
    pub ulCalledIDSize: u32,
    pub ulCalledIDOffset: u32,
    pub ulCalledIDNameSize: u32,
    pub ulCalledIDNameOffset: u32,
    pub ulConnectedIDFlags: u32,
    pub ulConnectedIDSize: u32,
    pub ulConnectedIDOffset: u32,
    pub ulConnectedIDNameSize: u32,
    pub ulConnectedIDNameOffset: u32,
    pub ulRedirectionIDFlags: u32,
    pub ulRedirectionIDSize: u32,
    pub ulRedirectionIDOffset: u32,
    pub ulRedirectionIDNameSize: u32,
    pub ulRedirectionIDNameOffset: u32,
    pub ulRedirectingIDFlags: u32,
    pub ulRedirectingIDSize: u32,
    pub ulRedirectingIDOffset: u32,
    pub ulRedirectingIDNameSize: u32,
    pub ulRedirectingIDNameOffset: u32,
    pub ulAppNameSize: u32,
    pub ulAppNameOffset: u32,
    pub ulDisplayableAddressSize: u32,
    pub ulDisplayableAddressOffset: u32,
    pub ulCalledPartySize: u32,
    pub ulCalledPartyOffset: u32,
    pub ulCommentSize: u32,
    pub ulCommentOffset: u32,
    pub ulDisplaySize: u32,
    pub ulDisplayOffset: u32,
    pub ulUserUserInfoSize: u32,
    pub ulUserUserInfoOffset: u32,
    pub ulHighLevelCompSize: u32,
    pub ulHighLevelCompOffset: u32,
    pub ulLowLevelCompSize: u32,
    pub ulLowLevelCompOffset: u32,
    pub ulChargingInfoSize: u32,
    pub ulChargingInfoOffset: u32,
    pub ulTerminalModesSize: u32,
    pub ulTerminalModesOffset: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
    pub ulCallTreatment: u32,
    pub ulCallDataSize: u32,
    pub ulCallDataOffset: u32,
    pub ulSendingFlowspecSize: u32,
    pub ulSendingFlowspecOffset: u32,
    pub ulReceivingFlowspecSize: u32,
    pub ulReceivingFlowspecOffset: u32,
    pub ulCallerIDAddressType: u32,
    pub ulCalledIDAddressType: u32,
    pub ulConnectedIDAddressType: u32,
    pub ulRedirectionIDAddressType: u32,
    pub ulRedirectingIDAddressType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_CALL_STATUS {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulCallState: u32,
    pub ulCallStateMode: u32,
    pub ulCallPrivilege: u32,
    pub ulCallFeatures: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_EXTENSION_ID {
    pub ulExtensionID0: u32,
    pub ulExtensionID1: u32,
    pub ulExtensionID2: u32,
    pub ulExtensionID3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_DEV_CAPS {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulProviderInfoSize: u32,
    pub ulProviderInfoOffset: u32,
    pub ulSwitchInfoSize: u32,
    pub ulSwitchInfoOffset: u32,
    pub ulPermanentLineID: u32,
    pub ulLineNameSize: u32,
    pub ulLineNameOffset: u32,
    pub ulStringFormat: u32,
    pub ulAddressModes: u32,
    pub ulNumAddresses: u32,
    pub ulBearerModes: u32,
    pub ulMaxRate: u32,
    pub ulMediaModes: u32,
    pub ulGenerateToneModes: u32,
    pub ulGenerateToneMaxNumFreq: u32,
    pub ulGenerateDigitModes: u32,
    pub ulMonitorToneMaxNumFreq: u32,
    pub ulMonitorToneMaxNumEntries: u32,
    pub ulMonitorDigitModes: u32,
    pub ulGatherDigitsMinTimeout: u32,
    pub ulGatherDigitsMaxTimeout: u32,
    pub ulMedCtlDigitMaxListSize: u32,
    pub ulMedCtlMediaMaxListSize: u32,
    pub ulMedCtlToneMaxListSize: u32,
    pub ulMedCtlCallStateMaxListSize: u32,
    pub ulDevCapFlags: u32,
    pub ulMaxNumActiveCalls: u32,
    pub ulAnswerMode: u32,
    pub ulRingModes: u32,
    pub ulLineStates: u32,
    pub ulUUIAcceptSize: u32,
    pub ulUUIAnswerSize: u32,
    pub ulUUIMakeCallSize: u32,
    pub ulUUIDropSize: u32,
    pub ulUUISendUserUserInfoSize: u32,
    pub ulUUICallInfoSize: u32,
    pub MinDialParams: u32,
    pub MaxDialParams: u32,
    pub DefaultDialParams: u32,
    pub ulNumTerminals: u32,
    pub ulTerminalCapsSize: u32,
    pub ulTerminalCapsOffset: u32,
    pub ulTerminalTextEntrySize: u32,
    pub ulTerminalTextSize: u32,
    pub ulTerminalTextOffset: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
    pub ulLineFeatures: u32,
    pub ulSettableDevStatus: u32,
    pub ulDeviceClassesSize: u32,
    pub ulDeviceClassesOffset: u32,
    pub PermanentLineGuid: u32,
    pub ulAddressTypes: u32,
    pub ProtocolGuid: u32,
    pub ulAvailableTracking: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINE_DEV_STATUS {
    pub ulTotalSize: u32,
    pub ulNeededSize: u32,
    pub ulUsedSize: u32,
    pub ulNumOpens: u32,
    pub ulOpenMediaModes: u32,
    pub ulNumActiveCalls: u32,
    pub ulNumOnHoldCalls: u32,
    pub ulNumOnHoldPendCalls: u32,
    pub ulLineFeatures: u32,
    pub ulNumCallCompletions: u32,
    pub ulRingMode: u32,
    pub ulSignalLevel: u32,
    pub ulBatteryLevel: u32,
    pub ulRoamMode: u32,
    pub ulDevStatusFlags: u32,
    pub ulTerminalModesSize: u32,
    pub ulTerminalModesOffset: u32,
    pub ulDevSpecificSize: u32,
    pub ulDevSpecificOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_ACCEPT {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulUserUserInfoSize: u32,
    pub UserUserInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_ANSWER {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulUserUserInfoSize: u32,
    pub UserUserInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_CLOSE {
    pub ulRequestID: u32,
    pub hdLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_CLOSE_CALL {
    pub ulRequestID: u32,
    pub hdCall: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_CONDITIONAL_MEDIA_DETECTION {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulMediaModes: u32,
    pub LineCallParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_CONFIG_DIALOG {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulDeviceClassSize: u32,
    pub ulDeviceClassOffset: u32,
    pub ulLibraryNameTotalSize: u32,
    pub ulLibraryNameNeededSize: u32,
    pub szLibraryName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_DEV_SPECIFIC {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulAddressID: u32,
    pub hdCall: u32,
    pub ulParamsSize: u32,
    pub Params: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_DIAL {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulDestAddressSize: u32,
    pub szDestAddress: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_DROP {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulUserUserInfoSize: u32,
    pub UserUserInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_ADDRESS_CAPS {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulAddressID: u32,
    pub ulExtVersion: u32,
    pub LineAddressCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_ADDRESS_ID {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulAddressID: u32,
    pub ulAddressMode: u32,
    pub ulAddressSize: u32,
    pub szAddress: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_ADDRESS_STATUS {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulAddressID: u32,
    pub LineAddressStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_CALL_ADDRESS_ID {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulAddressID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_CALL_INFO {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub LineCallInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_CALL_STATUS {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub LineCallStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_DEV_CAPS {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulExtVersion: u32,
    pub LineDevCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_DEV_CONFIG {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulDeviceClassSize: u32,
    pub ulDeviceClassOffset: u32,
    pub DeviceConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_EXTENSION_ID {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub LineExtensionID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_ID {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulAddressID: u32,
    pub hdCall: u32,
    pub ulSelect: u32,
    pub ulDeviceClassSize: u32,
    pub ulDeviceClassOffset: u32,
    pub DeviceID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GET_LINE_DEV_STATUS {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub LineDevStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_MAKE_CALL {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub htCall: u32,
    pub hdCall: u32,
    pub ulDestAddressSize: u32,
    pub ulDestAddressOffset: u32,
    pub bUseDefaultLineCallParams: u8,
    pub LineCallParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_NEGOTIATE_EXT_VERSION {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulLowVersion: u32,
    pub ulHighVersion: u32,
    pub ulExtVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_OPEN {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub htLine: u32,
    pub hdLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_PROVIDER_INITIALIZE {
    pub ulRequestID: u32,
    pub ulDeviceIDBase: u32,
    pub ulNumLineDevs: u32,
    pub ulProviderID: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_PROVIDER_SHUTDOWN {
    pub ulRequestID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SECURE_CALL {
    pub ulRequestID: u32,
    pub hdCall: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SELECT_EXT_VERSION {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulExtVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SEND_USER_USER_INFO {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulUserUserInfoSize: u32,
    pub UserUserInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_APP_SPECIFIC {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulAppSpecific: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_CALL_PARAMS {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulBearerMode: u32,
    pub ulMinRate: u32,
    pub ulMaxRate: u32,
    pub bSetLineDialParams: u8,
    pub LineDialParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_DEFAULT_MEDIA_DETECTION {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulMediaModes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_DEV_CONFIG {
    pub ulRequestID: u32,
    pub ulDeviceID: u32,
    pub ulDeviceClassSize: u32,
    pub ulDeviceClassOffset: u32,
    pub ulDeviceConfigSize: u32,
    pub DeviceConfig: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_MEDIA_MODE {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub ulMediaMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_SET_STATUS_MESSAGES {
    pub ulRequestID: u32,
    pub hdLine: u32,
    pub ulLineStates: u32,
    pub ulAddressStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_EVENT {
    pub htLine: u32,
    pub htCall: u32,
    pub ulMsg: u32,
    pub ulParam1: usize,
    pub ulParam2: usize,
    pub ulParam3: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDISTAPI_CHARACTERISTICS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_GATHER_DIGITS {
    pub ulRequestID: u32,
    pub hdCall: u32,
    pub IN_LPWSTR__lpsOrigDigitsBuffer: u32,
    pub ULONG__ulTerminationDigitsMask: u32,
    pub ULONG__ulFirstDigitTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TAPI_MONITOR_DIGITS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_CM_CAPS {
    pub ulCoTapiVersion: u32,
    pub ulNumLines: u32,
    pub ulFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_LINE_CAPS {
    pub ulLineID: u32,
    pub ulFlags: u32,
    pub LineDevCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_ADDRESS_CAPS {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulFlags: u32,
    pub LineAddressCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_TRANSLATE_TAPI_CALLPARAMS {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulFlags: u32,
    pub DestAddress: u32,
    pub LineCallParams: u32,
    pub NdisCallParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_TRANSLATE_NDIS_CALLPARAMS {
    pub ulFlags: u32,
    pub NdisCallParams: u32,
    pub LineCallInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_TRANSLATE_SAP {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulMediaModes: u32,
    pub Reserved: u32,
    pub NumberOfSaps: u32,
    pub NdisSapParams: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_TAPI_CALL_DIAGNOSTICS {
    pub ulOrigin: u32,
    pub ulReason: u32,
    pub DiagInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_AF_TAPI_MAKE_CALL_PARAMETERS {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulFlags: u32,
    pub DestAddress: u32,
    pub LineCallParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_AF_TAPI_INCOMING_CALL_PARAMETERS {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulFlags: u32,
    pub LineCallInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CO_AF_TAPI_SAP {
    pub ulLineID: u32,
    pub ulAddressID: u32,
    pub ulMediaModes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_INFO {
    pub MaxFrameSize: u32,
    pub MaxTransmit: u32,
    pub HeaderPadding: u32,
    pub TailPadding: u32,
    pub Endpoints: u32,
    pub MemoryFlags: u32,
    pub HighestAcceptableAddress: u32,
    pub FramingBits: u32,
    pub DesiredACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_SET_LINK_INFO {
    pub NdisLinkHandle: u32,
    pub MaxSendFrameSize: u32,
    pub MaxRecvFrameSize: u32,
    pub HeaderPadding: u32,
    pub TailPadding: u32,
    pub SendFramingBits: u32,
    pub RecvFramingBits: u32,
    pub SendCompressionBits: u32,
    pub RecvCompressionBits: u32,
    pub SendACCM: u32,
    pub RecvACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_GET_LINK_INFO {
    pub NdisLinkHandle: u32,
    pub MaxSendFrameSize: u32,
    pub MaxRecvFrameSize: u32,
    pub HeaderPadding: u32,
    pub TailPadding: u32,
    pub SendFramingBits: u32,
    pub RecvFramingBits: u32,
    pub SendCompressionBits: u32,
    pub RecvCompressionBits: u32,
    pub SendACCM: u32,
    pub RecvACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_GET_BRIDGE_INFO {
    pub NdisLinkHandle: u32,
    pub LanSegmentNumber: u16,
    pub BridgeNumber: u8,
    pub BridgingOptions: u8,
    pub BridgingCapabilities: u32,
    pub BridgingType: u8,
    pub MacBytes: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_SET_BRIDGE_INFO {
    pub NdisLinkHandle: u32,
    pub LanSegmentNumber: u16,
    pub BridgeNumber: u8,
    pub BridgingOptions: u8,
    pub BridgingCapabilities: u32,
    pub BridgingType: u8,
    pub MacBytes: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_COMPRESS_INFO {
    pub SessionKey: [u8; 8],
    pub MSCompType: u32,
    pub CompType: u8,
    pub CompLength: u16,
    pub CompOUI: [u8; 3],
    pub CompSubType: u8,
    pub CompValues: [u8; 32],
    pub CompValues_1: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_GET_COMP_INFO {
    pub NdisLinkHandle: u32,
    pub SendCapabilities: u32,
    pub RecvCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_SET_COMP_INFO {
    pub NdisLinkHandle: u32,
    pub SendCapabilities: u32,
    pub RecvCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_GET_STATS_INFO {
    pub NdisLinkHandle: u32,
    pub BytesSent: u32,
    pub BytesRcvd: u32,
    pub FramesSent: u32,
    pub FramesRcvd: u32,
    pub CRCErrors: u32,
    pub TimeoutErrors: u32,
    pub AlignmentErrors: u32,
    pub SerialOverrunErrors: u32,
    pub FramingErrors: u32,
    pub BufferOverrunErrors: u32,
    pub BytesTransmittedUncompressed: u32,
    pub BytesReceivedUncompressed: u32,
    pub BytesTransmittedCompressed: u32,
    pub BytesReceivedCompressed: u32,
    pub TunnelPacketsRecieved: u32,
    pub TunnelRecievePacketsPending: u32,
    pub TunnelPacketsIndicatedUp: u32,
    pub TunnelRecievePacketsRejected: u32,
    pub TunnelPacketsSent: u32,
    pub TunnelPacketsSentComplete: u32,
    pub TunnelTransmitPacketsPending: u32,
    pub TunnelPacketsTransmitError: u32,
    pub TunnelPacketsSentError: u32,
    pub TunnelTransmitPacketsRejected: u32,
    pub TunnelAcksSent: u32,
    pub TunnelAcksSentComplete: u32,
    pub TunnelGeneric1: u32,
    pub TunnelGeneric2: u32,
    pub TunnelGeneric3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MAC_LINE_UP {
    pub LinkSpeed: u32,
    pub Quality: u32,
    pub SendWindow: u16,
    pub ConnectionWrapperID: u32,
    pub NdisLinkHandle: u32,
    pub NdisLinkContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MAC_LINE_DOWN {
    pub NdisLinkContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MAC_FRAGMENT {
    pub NdisLinkContext: u32,
    pub Errors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_INFO {
    pub MaxFrameSize: u32,
    pub MaxSendWindow: u32,
    pub FramingBits: u32,
    pub DesiredACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_SET_LINK_INFO {
    pub MaxSendFrameSize: u32,
    pub MaxRecvFrameSize: u32,
    pub SendFramingBits: u32,
    pub RecvFramingBits: u32,
    pub SendCompressionBits: u32,
    pub RecvCompressionBits: u32,
    pub SendACCM: u32,
    pub RecvACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_GET_LINK_INFO {
    pub MaxSendFrameSize: u32,
    pub MaxRecvFrameSize: u32,
    pub SendFramingBits: u32,
    pub RecvFramingBits: u32,
    pub SendCompressionBits: u32,
    pub RecvCompressionBits: u32,
    pub SendACCM: u32,
    pub RecvACCM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_GET_COMP_INFO {
    pub SendCapabilities: u32,
    pub RecvCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_SET_COMP_INFO {
    pub SendCapabilities: u32,
    pub RecvCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_GET_STATS_INFO {
    pub BytesSent: u32,
    pub BytesRcvd: u32,
    pub FramesSent: u32,
    pub FramesRcvd: u32,
    pub CRCErrors: u32,
    pub TimeoutErrors: u32,
    pub AlignmentErrors: u32,
    pub SerialOverrunErrors: u32,
    pub FramingErrors: u32,
    pub BufferOverrunErrors: u32,
    pub BytesTransmittedUncompressed: u32,
    pub BytesReceivedUncompressed: u32,
    pub BytesTransmittedCompressed: u32,
    pub BytesReceivedCompressed: u32,
    pub TunnelPacketsRecieved: u32,
    pub TunnelRecievePacketsPending: u32,
    pub TunnelPacketsIndicatedUp: u32,
    pub TunnelRecievePacketsRejected: u32,
    pub TunnelPacketsSent: u32,
    pub TunnelPacketsSentComplete: u32,
    pub TunnelTransmitPacketsPending: u32,
    pub TunnelPacketsTransmitError: u32,
    pub TunnelPacketsSentError: u32,
    pub TunnelTransmitPacketsRejected: u32,
    pub TunnelAcksSent: u32,
    pub TunnelAcksSentComplete: u32,
    pub TunnelGeneric1: u32,
    pub TunnelGeneric2: u32,
    pub TunnelGeneric3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_CO_FRAGMENT {
    pub Errors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAN_CO_LINKPARAMS {
    pub TransmitSpeed: u32,
    pub ReceiveSpeed: u32,
    pub SendWindow: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAN_CO_MTULINKPARAMS {
    pub Version: u32,
    pub TransmitSpeed: u32,
    pub ReceiveSpeed: u32,
    pub SendWindow: u32,
    pub MTU: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DRIVER_CAPS {
    pub Header: u32,
    pub DriverCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SERVICE_ACTIVATION {
    pub Header: u32,
    pub ServiceActivation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_RADIO_STATE {
    pub Header: u32,
    pub RadioAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PIN {
    pub Header: u32,
    pub PinAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PIN_EX {
    pub Header: u32,
    pub PinAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PREFERRED_PROVIDERS {
    pub Header: u32,
    pub PreferredListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_REGISTER_STATE {
    pub Header: u32,
    pub SetRegisterState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_REGISTER_STATE_EX {
    pub Header: u32,
    pub SetRegisterState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_SIGNAL_INDICATION {
    pub Header: u32,
    pub SignalIndication: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_SIGNAL_INDICATION_EX {
    pub Header: u32,
    pub SignalIndication: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PACKET_SERVICE {
    pub Header: u32,
    pub PacketServiceAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PROVISIONED_CONTEXT {
    pub Header: u32,
    pub ProvisionedContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PROVISIONED_CONTEXT_V2 {
    pub Header: u32,
    pub ProvisionedContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_CONTEXT_STATE {
    pub Header: u32,
    pub SetContextState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_SMS_CONFIGURATION {
    pub Header: u32,
    pub SetSmsConfiguration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_READ {
    pub Header: u32,
    pub SmsRead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_SEND {
    pub Header: u32,
    pub SmsSend: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_DELETE {
    pub Header: u32,
    pub SmsFilter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_HOME_PROVIDER {
    pub Header: u32,
    pub HomeProvider: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PREFERRED_MULTICARRIER_PROVIDERS {
    pub Header: u32,
    pub PreferredListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_GET_VISIBLE_PROVIDERS {
    pub Header: u32,
    pub VisibleProviders: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_USSD_REQUEST {
    pub Header: u32,
    pub UssdRequest: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_COMMAND {
    pub Header: u32,
    pub Command: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SUBSCRIBE_DEVICE_SERVICE_EVENTS {
    pub Header: u32,
    pub DeviceServiceListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_ENUMERATE_DEVICE_SERVICE_COMMANDS {
    pub Header: u32,
    pub DeviceServiceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_DEVICE_SERVICE_SESSION {
    pub Header: u32,
    pub Session: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SESSION_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub Session: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SESSION_WRITE {
    pub Header: u32,
    pub WriteData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MAC_PARAMETERS {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MAC_INFO {
    pub uReserved: u32,
    pub uNdisPortNumber: u32,
    pub MacAddr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MBIM_VERSION {
    pub Header: u32,
    pub MbimVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_DEVICE_SLOT_MAPPING_INFO {
    pub Header: u32,
    pub SetDeviceSlotMappingInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_GET_SLOT_INFO {
    pub Header: u32,
    pub GetSlotInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_DEVICE_BINDINGS_INFO {
    pub Header: u32,
    pub SetDeviceBindingsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_IMS_VOICE_STATE {
    pub Header: u32,
    pub SetImsVoiceState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_LOCATION_TRIGGER_MODE {
    pub Header: u32,
    pub SetLocationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_NETWORK_IDLE_HINT {
    pub Header: u32,
    pub IdleHint: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PRESHUTDOWN_STATE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_UICC_OPEN_CHANNEL {
    pub Header: u32,
    pub SetUiccOpenChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_UICC_CLOSE_CHANNEL {
    pub Header: u32,
    pub SetUiccCloseChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_UICC_APDU {
    pub Header: u32,
    pub SetUiccApdu: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_UICC_TERMINAL_CAPABILITY {
    pub Header: u32,
    pub SetUiccTerminalCapability: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PS_MEDIA_CONFIG {
    pub Header: u32,
    pub SetPSMediaConfigItem: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_QUERY_PS_MEDIA_CONFIG {
    pub Header: u32,
    pub ServicesBitMap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_SAR_CONFIG {
    pub Header: u32,
    pub SetSarConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_SAR_TRANSMISSION_STATUS {
    pub Header: u32,
    pub SetTransmissionStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_NETWORK_BLACKLIST {
    pub Header: u32,
    pub SetNetworkBlacklist: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_LTE_ATTACH_CONTEXT {
    pub Header: u32,
    pub SetLteAttachContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_UICC_RESET {
    pub Header: u32,
    pub SetUiccReset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_DEVICE_RESET {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_BASE_STATIONS_INFO_REQ {
    pub Header: u32,
    pub BaseStationsInfoRequest: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_FILE_PATH {
    pub Header: u32,
    pub UiccFilePath: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_ACCESS_BINARY {
    pub Header: u32,
    pub UiccAccessBinary: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_ACCESS_RECORD {
    pub Header: u32,
    pub UiccAccessRecord: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_PIN_EX2 {
    pub Header: u32,
    pub PinAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PIN_APP {
    pub Header: u32,
    pub PinApp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_MODEM_LOGGING_CONFIG {
    pub Header: u32,
    pub ModemLoggingConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_REGISTER_PARAMS {
    pub Header: u32,
    pub SetRegisterParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_CAPS {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_CAPS_EX {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_READY_INFO {
    pub Header: u32,
    pub ReadyInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SERVICE_ACTIVATION_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub ServiceActivationStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_VENDOR_SPECIFIC {
    pub Header: u32,
    pub VendorSpecificData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_RADIO_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub RadioState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PIN_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub PinInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PIN_LIST {
    pub Header: u32,
    pub uStatus: u32,
    pub PinList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_HOME_PROVIDER {
    pub Header: u32,
    pub uStatus: u32,
    pub Provider: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PREFERRED_PROVIDERS {
    pub Header: u32,
    pub uStatus: u32,
    pub PreferredListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_VISIBLE_PROVIDERS {
    pub Header: u32,
    pub uStatus: u32,
    pub VisibleListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_REGISTRATION_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub RegistrationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_REGISTRATION_STATE_INFOS_EX {
    pub Header: u32,
    pub uStatus: u32,
    pub RegistrationStateInfos: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SIGNAL_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub SignalState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SIGNAL_STATE_INFOS_EX {
    pub Header: u32,
    pub uStatus: u32,
    pub SignalStateInfos: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PACKET_SERVICE_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub PacketService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_CONTEXT_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub ContextState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PROVISIONED_CONTEXTS {
    pub Header: u32,
    pub uStatus: u32,
    pub ContextListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_CONFIGURATION {
    pub Header: u32,
    pub uStatus: u32,
    pub SmsConfiguration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_RECEIVE {
    pub Header: u32,
    pub uStatus: u32,
    pub SmsListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_SEND_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub MessageReference: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_DELETE_STATUS {
    pub Header: u32,
    pub uStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SMS_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub SmsStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SUPPORTED_DEVICE_SERVICES {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceServices: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_RESPONSE {
    pub Header: u32,
    pub uStatus: u32,
    pub Response: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_EVENT {
    pub Header: u32,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SUBSCRIPTION {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceServiceListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_HOME_PROVIDER2 {
    pub Header: u32,
    pub uStatus: u32,
    pub Provider: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_USSD_EVENT {
    pub Header: u32,
    pub uStatus: u32,
    pub UssdEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SUPPORTED_COMMANDS {
    pub Header: u32,
    pub uStatus: u32,
    pub SupportedCommands: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_AUTH_CHALLENGE {
    pub Header: u32,
    pub AuthChallenge: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_AUTH_RESPONSE {
    pub Header: u32,
    pub uStatus: u32,
    pub AuthResponse: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PREFERRED_MULTICARRIER_PROVIDERS {
    pub Header: u32,
    pub uStatus: u32,
    pub PreferredListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SESSION_WRITE_COMPLETE {
    pub Header: u32,
    pub uStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SERVICE_SESSION_READ {
    pub Header: u32,
    pub ReadData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_IP_ADDRESS_STATE {
    pub Header: u32,
    pub WwanIpAddressState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SYS_CAPS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub SysCapsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_SLOT_MAPPING_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceSlotMappingInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SLOT_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub SlotInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_BINDINGS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub DeviceBindingsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_IMS_VOICE_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub ImsVoiceState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_LOCATION_STATE_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub LocationStateInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PRESHUTDOWN_STATE {
    pub Header: u32,
    pub uStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_ATR_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub AtrInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_OPEN_CHANNEL_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccOpenChannelInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_CLOSE_CHANNEL_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccCloseChannelInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_APDU_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccApduInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_TERMINAL_CAPABILITY_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccCapabilityInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PS_MEDIA_CONFIG_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub nNumItems: u32,
    pub PSMediaConfigItems: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SAR_CONFIG_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub SarConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SAR_TRANSMISSION_STATUS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub TransmissionStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_NETWORK_BLACKLIST {
    pub Header: u32,
    pub uStatus: u32,
    pub NetworkBlacklistInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_LTE_ATTACH_CONTEXTS {
    pub Header: u32,
    pub uStatus: u32,
    pub LteAttachContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_LTE_ATTACH_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub LteAttachStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MODEM_CONFIG_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub ModemConfigInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_PCO_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub PcoValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_RESET_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccResetInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_DEVICE_RESET_STATUS {
    pub Header: u32,
    pub uStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_BASE_STATIONS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub BaseStationsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MPDP_INFO {
    pub Operation: u32,
    pub ChildInterfaceGUID: u32,
    pub MacAddr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_SET_MPDP_STATE {
    pub Header: u32,
    pub Info: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MPDP_STATE {
    pub Header: u32,
    pub uStatus: u32,
    pub Info: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MPDP_LIST {
    pub Header: u32,
    pub uStatus: u32,
    pub ChildInterfaceList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_NITZ_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub NitzInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_APP_LIST {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccAppList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_FILE_STATUS {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccFileStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UICC_RESPONSE {
    pub Header: u32,
    pub uStatus: u32,
    pub UiccResponse: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_MODEM_LOGGING_CONFIG {
    pub Header: u32,
    pub uStatus: u32,
    pub ModemLoggingConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_REGISTER_PARAMS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub RegisterParamsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_NETWORK_PARAMS_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub NetworkParamsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_QUERY_UICC_TERMINAL_CAPABILITY {
    pub Header: u32,
    pub QueryUiccTerminalCapability: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_QUERY_UICC_RESET {
    pub Header: u32,
    pub QueryUiccReset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_QUERY_ATR_INFO {
    pub Header: u32,
    pub QueryAtrInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_QUERY_READY_INFO {
    pub Header: u32,
    pub QueryReadyInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WWAN_UE_POLICY_INFO {
    pub Header: u32,
    pub uStatus: u32,
    pub UePolicyInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_OBJECT_HEADER_RESERVED_BLOCK {
    pub rf: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_OBJECT_HEADER {
    pub Version: u32,
    pub ObjectType: u32,
    pub NdkReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_RESULT {
    pub Status: u32,
    pub BytesTransferred: u32,
    pub QPContext: usize,
    pub RequestContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_RESULT_EX {
    pub Status: u32,
    pub BytesTransferred: u32,
    pub QPContext: usize,
    pub RequestContext: usize,
    pub Type: u32,
    pub ProviderErrorCode: u32,
    pub TypeSpecificCompletionOutput: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_LOGICAL_ADDRESS_MAPPING {
    pub AdapterContext: usize,
    pub AdapterPageCount: u32,
    pub AdapterPageArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_SGE {
    pub VirtualAddress: usize,
    pub LogicalAddress: u32,
    pub Length: u32,
    pub MemoryRegionToken: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_EXTENSION_INTERFACE {
    pub field_Dispatch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_CQ_DISPATCH {
    pub NdkCloseCq: u32,
    pub NdkQueryExtension: u32,
    pub NdkResizeCq: u32,
    pub NdkArmCq: u32,
    pub NdkGetCqResults: u32,
    pub NdkControlCqInterruptModeration: u32,
    pub NdkGetCqResultsEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_CQ {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_MR_DISPATCH {
    pub NdkCloseMr: u32,
    pub NdkQueryExtension: u32,
    pub NdkRegisterMr: u32,
    pub NdkDeregisterMr: u32,
    pub NdkInitializeFastRegisterMr: u32,
    pub NdkGetRemoteTokenFromMr: u32,
    pub NdkGetLocalTokenFromMr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_MR {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_MW_DISPATCH {
    pub NdkCloseMw: u32,
    pub NdkQueryExtension: u32,
    pub NdkGetRemoteTokenFromMw: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_MW {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_SRQ_DISPATCH {
    pub NdkCloseSrq: u32,
    pub NdkQueryExtension: u32,
    pub NdkModifySrq: u32,
    pub NdkSrqReceive: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_SRQ {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_QP_DISPATCH {
    pub NdkCloseQp: u32,
    pub NdkQueryExtension: u32,
    pub NdkFlush: u32,
    pub NdkSend: u32,
    pub NdkReceive: u32,
    pub NdkBind: u32,
    pub NdkFastRegister: u32,
    pub NdkInvalidate: u32,
    pub NdkRead: u32,
    pub NdkWrite: u32,
    pub NdkSendAndInvalidate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_QP {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_PD_DISPATCH {
    pub NdkClosePd: u32,
    pub NdkQueryExtension: u32,
    pub NdkCreateMr: u32,
    pub NdkCreateMw: u32,
    pub NdkCreateSrq: u32,
    pub NdkCreateQp: u32,
    pub NdkCreateQpWithSrq: u32,
    pub NdkGetPrivilegedMemoryRegionToken: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_PD {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_SHARED_ENDPOINT_DISPATCH {
    pub NdkCloseSharedEndpoint: u32,
    pub NdkQueryExtension: u32,
    pub NdkGetLocalAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_SHARED_ENDPOINT {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_CONNECTOR_DISPATCH {
    pub NdkCloseConnector: u32,
    pub NdkQueryExtension: u32,
    pub NdkConnect: u32,
    pub NdkConnectWithSharedEndpoint: u32,
    pub NdkCompleteConnect: u32,
    pub NdkAccept: u32,
    pub NdkReject: u32,
    pub NdkGetConnectionData: u32,
    pub NdkGetLocalAddress: u32,
    pub NdkGetPeerAddress: u32,
    pub NdkDisconnect: u32,
    pub NdkCompleteConnectEx: u32,
    pub NdkAcceptEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_CONNECTOR {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_LISTENER_DISPATCH {
    pub NdkCloseListener: u32,
    pub NdkQueryExtension: u32,
    pub NdkListen: u32,
    pub NdkGetLocalAddress: u32,
    pub NdkControlConnectEvents: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_LISTENER {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_ADAPTER_DISPATCH {
    pub NdkQueryExtension: u32,
    pub NdkQueryAdapterInfo: u32,
    pub NdkCreateCq: u32,
    pub NdkCreatePd: u32,
    pub NdkCreateSharedEndpoint: u32,
    pub NdkCreateConnector: u32,
    pub NdkCreateListener: u32,
    pub NdkBuildLAM: u32,
    pub NdkReleaseLAM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_ADAPTER {
    pub Header: u32,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_EXTENSION_ADAPTER_RSS_DISPATCH {
    pub NdkGetRssProcessorInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_DESCRIPTOR {
    pub TransferSize: u32,
    pub DCAContext: u32,
    pub DCAContext_1: u32,
    pub Reserved: u32,
    pub DCAContext_2: u32,
    pub Reserved_1: u32,
    pub ControlFlags: u32,
    pub SourceAddress: u64,
    pub DestinationAddress: u64,
    pub NextDescriptor: u64,
    pub Reserved1: u32,
    pub NextSourceAddress: u64,
    pub Reserved2: u32,
    pub NextDestinationAddress: u64,
    pub UserContext1: u32,
    pub UserContext2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_CHANNEL_CPU_AFFINITY {
    pub DmaChannel: u32,
    pub CpuNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_CHANNEL_PARAMETERS {
    pub Revision: u16,
    pub Size: u16,
    pub Flags: u32,
    pub CompletionVirtualAddress: usize,
    pub CompletionPhysicalAddress: u64,
    pub ProcessorAffinityMask: u32,
    pub ChannelPriority: u32,
    pub CpuNumber: u32,
    pub ProcessorAffinityMaskEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_PROVIDER_CHARACTERISTICS {
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub Size: u16,
    pub Flags: u32,
    pub PhysicalDeviceObject: usize,
    pub MaxDmaChannelCount: u32,
    pub SetDmaChannelCpuAffinity: u32,
    pub AllocateDmaChannel: u32,
    pub FreeDmaChannel: u32,
    pub StartDma: u32,
    pub SuspendDma: u32,
    pub ResumeDma: u32,
    pub AbortDma: u32,
    pub AppendDma: u32,
    pub ResetChannel: u32,
    pub FriendlyName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_PROVIDER_ATTRIBUTES {
    pub MajorHwVersion: u8,
    pub MinorHwVersion: u8,
    pub Size: u16,
    pub Flags: u32,
    pub VendorId: u32,
    pub DmaChannelCount: u32,
    pub MaximumTransferSize: u32,
    pub MaximumAddressSpace: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DMA_PNP_NOTIFICATION {
    pub StructureRevision: u32,
    pub StructureSize: u32,
    pub NotificationCode: u32,
    pub Buffer: usize,
    pub BufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPI_REGISTRATION_INSTANCE {
    pub Version: u16,
    pub Size: u16,
    pub NpiId: usize,
    pub ModuleId: usize,
    pub Number: u32,
    pub OPTIONAL: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPI {
    pub Handle: usize,
    pub Dispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPI_CLIENT_CHARACTERISTICS {
    pub Version: u16,
    pub Length: u16,
    pub ClientAttachProvider: usize,
    pub ClientDetachProvider: usize,
    pub ClientCleanupBindingContext: usize,
    pub ClientRegistrationInstance: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPI_PROVIDER_CHARACTERISTICS {
    pub Version: u16,
    pub Length: u16,
    pub ProviderAttachClient: usize,
    pub ProviderDetachClient: usize,
    pub ProviderCleanupBindingContext: usize,
    pub ProviderRegistrationInstance: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PNP_EVENT {
    pub NetEvent: u32,
    pub Buffer: usize,
    pub BufferLength: u32,
    pub NdisReserved: [usize; 4],
    pub TransportReserved: [usize; 4],
    pub TdiReserved: [usize; 4],
    pub TdiClientReserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECORD_NAME {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NODE_TYPE_CODE_AND_SIZE_NO_REFCOUNT {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NODE_TYPE_CODE_AND_SIZE {
    pub NodeReferenceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPort_IN {
    pub WWPN: [u8; 8],
    pub WWNN: [u8; 8],
    pub Tag: [u8; 16],
    pub VirtualName: [u16; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPort_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveVirtualPort_IN {
    pub WWPN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveVirtualPort_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_DH_Chap_Parameters {
    pub SharedSecretLength: u32,
    pub SecretEncoding: u8,
    pub SharedSecret: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_NPIVCapabilities {
    pub DhChapAvailableOnPhysicalPort: u8,
    pub DhChapAvailableOnVirtualPorts: u8,
    pub MaxVirtualPortCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetChapForPhysicalPort_IN {
    pub CHAP: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SetChapForPhysicalPort_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveChapForPhysicalPort_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPortEx_IN {
    pub WWPN: [u8; 8],
    pub WWNN: [u8; 8],
    pub Tag: [u8; 16],
    pub VirtualName: [u16; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPortEx_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPortExUsingDHCHAP_IN {
    pub WWPN: [u8; 8],
    pub WWNN: [u8; 8],
    pub Tag: [u8; 16],
    pub VirtualName: [u16; 64],
    pub CHAP: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CreateVirtualPortExUsingDHCHAP_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveVirtualPortEx_IN {
    pub WWPN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemoveVirtualPortEx_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RescanVirtualPort_IN {
    pub WWPN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RescanVirtualPort_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_VirtualFibrePortAttributes {
    pub Status: u32,
    pub FCId: u32,
    pub VirtualName: [u16; 64],
    pub Tag: [u8; 16],
    pub WWPN: [u8; 8],
    pub WWNN: [u8; 8],
    pub FabricWWN: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_FibrePortNPIVAttributes {
    pub WWPN: [u8; 8],
    pub WWNN: [u8; 8],
    pub NumberVirtualPorts: u32,
    pub VirtualPorts: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_NPIVLUNMappingInformation {
    pub WWPNVirtualPort: [u8; 8],
    pub WWPNPhysicalPort: [u8; 8],
    pub OSBus: u8,
    pub OSTarget: u8,
    pub OSLUN: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSFC_NPIVLUNMappingInformationEx {
    pub WWPNVirtualPort: [u8; 8],
    pub WWPNPhysicalPort: [u8; 8],
    pub PortNumber: u8,
    pub OSBus: u8,
    pub OSTarget: u8,
    pub OSLUN: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGP_INFO_COMMON {
    pub MasterCap: usize,
    pub TargetCap: usize,
    pub DeviceId: u16,
    pub VendorId: u16,
    pub SubVendorId: u16,
    pub SubSystemId: u16,
    pub HwRevisionId: u8,
    pub VerifierFlags: u32,
    pub GuardPageCorruption: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGP_INFO_DRIVER {
    pub AGPReg1: u32,
    pub AGPReg2: u32,
    pub ApertureStart: u64,
    pub GartTranslationTable: u64,
    pub ApertureLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGP_BUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub AgpContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Capabilities: u32,
    pub ReserveMemory: usize,
    pub ReleaseMemory: usize,
    pub CommitMemory: usize,
    pub FreeMemory: usize,
    pub GetMappedPages: usize,
    pub SetRate: usize,
    pub AgpSize: usize,
    pub AgpBase: u64,
    pub MaxPhysicalAddress: u64,
    pub MapMemory: usize,
    pub UnMapMemory: usize,
    pub FlushChipsetCaches: usize,
    pub CheckIntegrity: usize,
    pub MapMemoryEx: usize,
    pub UnMapMemoryEx: usize,
    pub FlushGartTLB: usize,
    pub CheckGuardPage: usize,
    pub GetAgpInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OUTPUT_PACKET {
    pub Bytes: *mut u8,
    pub CurrentByte: u32,
    pub ByteCount: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERNAL_I8042_HOOK_MOUSE {
    pub Context: usize,
    pub IsrRoutine: usize,
    pub IsrWritePort: usize,
    pub QueueMousePacket: usize,
    pub CallContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERNAL_I8042_HOOK_KEYBOARD {
    pub Context: usize,
    pub InitializationRoutine: usize,
    pub IsrRoutine: usize,
    pub IsrWritePort: usize,
    pub QueueKeyboardPacket: usize,
    pub CallContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERNAL_I8042_START_INFORMATION {
    pub Size: u32,
    pub InterruptObject: usize,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_EXTENSION {
    pub DumpType: u32,
    pub DeviceObject: usize,
    pub Geometry: u32,
    pub DiskSize: i64,
    pub PartitionInfo: u32,
    pub DumpData: usize,
    pub Size: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_INITIALIZATION_DATA {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub DumpStart: usize,
    pub DumpWrite: usize,
    pub DumpFinish: usize,
    pub DumpUnload: usize,
    pub DumpData: usize,
    pub MaxPagesPerWrite: u32,
    pub Flags: u32,
    pub DumpRead: usize,
    pub DumpPreReadWrite: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLOATING_SAVE_AREA {
    pub ControlWord: u32,
    pub StatusWord: u32,
    pub TagWord: u32,
    pub ErrorOffset: u32,
    pub ErrorSelector: u32,
    pub DataOffset: u32,
    pub DataSelector: u32,
    pub RegisterArea: [u8; 1],
    pub Spare0: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NEON128 {
    pub Low: u64,
    pub High: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_OBJECT_TYPE {
    pub ObjectType: u32,
    pub Flags: u16,
    pub Level: u16,
    pub AccessMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_PARAMETER_ARRAY_ENTRY {
    pub Type: u32,
    pub Length: u32,
    pub Data: [usize; 2],
    pub Address: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_ACCESS_REASON {
    pub AccessMask: u32,
    pub AccessReasons: [u32; 32],
    pub ObjectTypeIndex: u32,
    pub AccessGranted: u32,
    pub SecurityDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_CLAIMS {
    pub Length: u32,
    pub Claims: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_PARAMETER_ARRAY {
    pub CategoryId: u32,
    pub AuditId: u32,
    pub ParameterCount: u32,
    pub Length: u32,
    pub FlatSubCategoryId: u16,
    pub Type: u16,
    pub Flags: u32,
    pub Parameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ADT_PARAMETER_ARRAY_EX {
    pub CategoryId: u32,
    pub AuditId: u32,
    pub Version: u32,
    pub ParameterCount: u32,
    pub Length: u32,
    pub FlatSubCategoryId: u16,
    pub Type: u16,
    pub Flags: u32,
    pub Parameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_BALANCED_LINKS {
    pub field_Parent: u32,
    pub field_LeftChild: u32,
    pub field_RightChild: u32,
    pub Balance: i8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_AVL_TABLE {
    pub BalancedRoot: u32,
    pub OrderedPointer: usize,
    pub WhichOrderedElement: u32,
    pub NumberGenericTableElements: u32,
    pub DepthOfTree: u32,
    pub RestartKey: usize,
    pub DeleteCount: u32,
    pub CompareRoutine: usize,
    pub AllocateRoutine: usize,
    pub FreeRoutine: usize,
    pub TableContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_SPLAY_LINKS {
    pub field_Parent: u32,
    pub field_LeftChild: u32,
    pub field_RightChild: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_GENERIC_TABLE {
    pub TableRoot: usize,
    pub InsertOrderList: u32,
    pub OrderedPointer: usize,
    pub WhichOrderedElement: u32,
    pub NumberGenericTableElements: u32,
    pub CompareRoutine: usize,
    pub AllocateRoutine: usize,
    pub FreeRoutine: usize,
    pub TableContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_DYNAMIC_HASH_TABLE_ENTRY {
    pub Linkage: u32,
    pub Signature: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_DYNAMIC_HASH_TABLE_CONTEXT {
    pub ChainHead: usize,
    pub PrevLinkage: usize,
    pub Signature: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_DYNAMIC_HASH_TABLE_ENUMERATOR {
    pub HashEntry: u32,
    pub CurEntry: usize,
    pub ChainHead: usize,
    pub BucketIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_DYNAMIC_HASH_TABLE {
    pub Flags: u32,
    pub Shift: u32,
    pub TableSize: u32,
    pub Pivot: u32,
    pub DivisorMask: u32,
    pub NumEntries: u32,
    pub NonEmptyBuckets: u32,
    pub NumEnumerators: u32,
    pub Directory: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NV_MEMORY_RANGE {
    pub field_BaseAddress: u32,
    pub Length: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUSTOM_SYSTEM_EVENT_TRIGGER_CONFIG {
    pub Size: u32,
    pub TriggerId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ALIGNMENT_INFORMATION {
    pub AlignmentRequirement: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NAME_INFORMATION {
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ATTRIBUTE_TAG_INFORMATION {
    pub FileAttributes: u32,
    pub ReparseTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_DISPOSITION_INFORMATION {
    pub DeleteFile: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_DISPOSITION_INFORMATION_EX {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_END_OF_FILE_INFORMATION {
    pub EndOfFile: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_VALID_DATA_LENGTH_INFORMATION {
    pub ValidDataLength: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_LABEL_INFORMATION {
    pub VolumeLabelLength: u32,
    pub VolumeLabel: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_VOLUME_INFORMATION {
    pub VolumeCreationTime: i64,
    pub VolumeSerialNumber: u32,
    pub VolumeLabelLength: u32,
    pub SupportsObjects: u8,
    pub VolumeLabel: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_SIZE_INFORMATION {
    pub TotalAllocationUnits: i64,
    pub AvailableAllocationUnits: i64,
    pub SectorsPerAllocationUnit: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_FULL_SIZE_INFORMATION {
    pub TotalAllocationUnits: i64,
    pub CallerAvailableAllocationUnits: i64,
    pub ActualAvailableAllocationUnits: i64,
    pub SectorsPerAllocationUnit: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_FULL_SIZE_INFORMATION_EX {
    pub ActualTotalAllocationUnits: u64,
    pub ActualAvailableAllocationUnits: u64,
    pub ActualPoolUnavailableAllocationUnits: u64,
    pub CallerTotalAllocationUnits: u64,
    pub CallerAvailableAllocationUnits: u64,
    pub CallerPoolUnavailableAllocationUnits: u64,
    pub UsedAllocationUnits: u64,
    pub TotalReservedAllocationUnits: u64,
    pub VolumeStorageReserveAllocationUnits: u64,
    pub AvailableCommittedAllocationUnits: u64,
    pub PoolAvailableAllocationUnits: u64,
    pub SectorsPerAllocationUnit: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_METADATA_SIZE_INFORMATION {
    pub TotalMetadataAllocationUnits: i64,
    pub SectorsPerAllocationUnit: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_SECTOR_SIZE_INFORMATION {
    pub LogicalBytesPerSector: u32,
    pub PhysicalBytesPerSectorForAtomicity: u32,
    pub PhysicalBytesPerSectorForPerformance: u32,
    pub FileSystemEffectivePhysicalBytesPerSectorForAtomicity: u32,
    pub Flags: u32,
    pub ByteOffsetForSectorAlignment: u32,
    pub ByteOffsetForPartitionAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_OBJECTID_INFORMATION {
    pub ObjectId: [u8; 16],
    pub ExtendedInfo: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_NAME_INFORMATION {
    pub NameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_CACHED_INFORMATION {
    pub LastWriteTime: i64,
    pub TitleIndex: u32,
    pub SubKeys: u32,
    pub MaxNameLen: u32,
    pub Values: u32,
    pub MaxValueNameLen: u32,
    pub MaxValueDataLen: u32,
    pub NameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VIRTUALIZATION_INFORMATION {
    pub VirtualizationCandidate: u32,
    pub VirtualizationEnabled: u32,
    pub VirtualTarget: u32,
    pub VirtualStore: u32,
    pub VirtualSource: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_LAYER_INFORMATION {
    pub IsTombstone: u32,
    pub IsSupersedeLocal: u32,
    pub IsSupersedeTree: u32,
    pub ClassIsInherited: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCEPTION_REGISTRATION_RECORD {
    pub field_Next: u32,
    pub Handler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NT_TIB {
    pub field_ExceptionList: u32,
    pub StackBase: usize,
    pub StackLimit: usize,
    pub SubSystemTib: usize,
    pub FiberData: usize,
    pub Version: u32,
    pub FiberData_1: usize,
    pub ArbitraryUserPointer: usize,
    pub field_Self: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NT_TIB32 {
    pub ExceptionList: u32,
    pub StackBase: u32,
    pub StackLimit: u32,
    pub SubSystemTib: u32,
    pub FiberData: u32,
    pub Version: u32,
    pub FiberData_1: u32,
    pub ArbitraryUserPointer: u32,
    pub field_Self: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NT_TIB64 {
    pub ExceptionList: u32,
    pub StackBase: u32,
    pub StackLimit: u32,
    pub SubSystemTib: u32,
    pub FiberData: u32,
    pub Version: u32,
    pub FiberData_1: u32,
    pub ArbitraryUserPointer: u32,
    pub field_Self: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAGE_PRIORITY_INFORMATION {
    pub PagePriority: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_WS_WATCH_INFORMATION {
    pub FaultingPc: usize,
    pub FaultingVa: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_BASIC_INFORMATION {
    pub ExitStatus: u32,
    pub PebBaseAddress: usize,
    pub AffinityMask: usize,
    pub BasePriority: u32,
    pub UniqueProcessId: usize,
    pub InheritedFromUniqueProcessId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_EXTENDED_BASIC_INFORMATION {
    pub Size: usize,
    pub BasicInfo: usize,
    pub Flags: u32,
    pub IsProtectedProcess: u32,
    pub IsWow64Process: u32,
    pub IsProcessDeleting: u32,
    pub IsCrossSessionCreate: u32,
    pub IsFrozen: u32,
    pub IsBackground: u32,
    pub IsStronglyNamed: u32,
    pub IsSecureProcess: u32,
    pub IsSubsystemProcess: u32,
    pub IsTrustedApp: u32,
    pub SpareBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MEMBERSHIP_INFORMATION {
    pub ServerSiloId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DEVICEMAP_INFORMATION {
    pub DirectoryHandle: usize,
    pub DriveMap: u32,
    pub DriveType: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DEVICEMAP_INFORMATION_EX {
    pub DirectoryHandle: usize,
    pub DriveMap: u32,
    pub DriveType: [u8; 32],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_SESSION_INFORMATION {
    pub SessionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_HANDLE_TRACING_ENABLE {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_HANDLE_TRACING_ENABLE_EX {
    pub Flags: u32,
    pub TotalSlots: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_HANDLE_TRACING_ENTRY {
    pub Handle: usize,
    pub ClientId: u32,
    pub Type: u32,
    pub Stacks: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_HANDLE_TRACING_QUERY {
    pub Handle: usize,
    pub TotalTraces: u32,
    pub HandleTrace: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DYNAMIC_EH_CONTINUATION_TARGET {
    pub TargetAddress: usize,
    pub Flags: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DYNAMIC_EH_CONTINUATION_TARGETS_INFORMATION {
    pub NumberOfTargets: u16,
    pub Reserved: u16,
    pub Reserved2: u32,
    pub Targets: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DYNAMIC_ENFORCED_ADDRESS_RANGE {
    pub BaseAddress: usize,
    pub Size: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_DYNAMIC_ENFORCED_ADDRESS_RANGES_INFORMATION {
    pub NumberOfRanges: u16,
    pub Reserved: u16,
    pub Reserved2: u32,
    pub Ranges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUOTA_LIMITS {
    pub PagedPoolLimit: usize,
    pub NonPagedPoolLimit: usize,
    pub MinimumWorkingSetSize: usize,
    pub MaximumWorkingSetSize: usize,
    pub PagefileLimit: usize,
    pub TimeLimit: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUOTA_LIMITS_EX {
    pub PagedPoolLimit: usize,
    pub NonPagedPoolLimit: usize,
    pub MinimumWorkingSetSize: usize,
    pub MaximumWorkingSetSize: usize,
    pub PagefileLimit: usize,
    pub TimeLimit: i64,
    pub WorkingSetLimit: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
    pub Flags: u32,
    pub CpuRateLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_COUNTERS {
    pub ReadOperationCount: u64,
    pub WriteOperationCount: u64,
    pub OtherOperationCount: u64,
    pub ReadTransferCount: u64,
    pub WriteTransferCount: u64,
    pub OtherTransferCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VM_COUNTERS {
    pub PeakVirtualSize: usize,
    pub VirtualSize: usize,
    pub PageFaultCount: u32,
    pub PeakWorkingSetSize: usize,
    pub WorkingSetSize: usize,
    pub QuotaPeakPagedPoolUsage: usize,
    pub QuotaPagedPoolUsage: usize,
    pub QuotaPeakNonPagedPoolUsage: usize,
    pub QuotaNonPagedPoolUsage: usize,
    pub PagefileUsage: usize,
    pub PeakPagefileUsage: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VM_COUNTERS_EX {
    pub PeakVirtualSize: usize,
    pub VirtualSize: usize,
    pub PageFaultCount: u32,
    pub PeakWorkingSetSize: usize,
    pub WorkingSetSize: usize,
    pub QuotaPeakPagedPoolUsage: usize,
    pub QuotaPagedPoolUsage: usize,
    pub QuotaPeakNonPagedPoolUsage: usize,
    pub QuotaNonPagedPoolUsage: usize,
    pub PagefileUsage: usize,
    pub PeakPagefileUsage: usize,
    pub PrivateUsage: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VM_COUNTERS_EX2 {
    pub CountersEx: u32,
    pub PrivateWorkingSetSize: usize,
    pub SharedCommitUsage: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HARDWARE_COUNTER {
    pub Type: u32,
    pub Reserved: u32,
    pub Index: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_ASLR_POLICY {
    pub Flags: u32,
    pub EnableBottomUpRandomization: u32,
    pub EnableForceRelocateImages: u32,
    pub EnableHighEntropy: u32,
    pub DisallowStrippedImages: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_DEP_POLICY {
    pub Flags: u32,
    pub Enable: u32,
    pub DisableAtlThunkEmulation: u32,
    pub ReservedFlags: u32,
    pub Permanent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_SEHOP_POLICY {
    pub Flags: u32,
    pub EnableSehop: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_STRICT_HANDLE_CHECK_POLICY {
    pub Flags: u32,
    pub RaiseExceptionOnInvalidHandleReference: u32,
    pub HandleExceptionsPermanentlyEnabled: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_SYSTEM_CALL_DISABLE_POLICY {
    pub Flags: u32,
    pub DisallowWin32kSystemCalls: u32,
    pub AuditDisallowWin32kSystemCalls: u32,
    pub DisallowFsctlSystemCalls: u32,
    pub AuditDisallowFsctlSystemCalls: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_EXTENSION_POINT_DISABLE_POLICY {
    pub Flags: u32,
    pub DisableExtensionPoints: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_DYNAMIC_CODE_POLICY {
    pub Flags: u32,
    pub ProhibitDynamicCode: u32,
    pub AllowThreadOptOut: u32,
    pub AllowRemoteDowngrade: u32,
    pub AuditProhibitDynamicCode: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_CONTROL_FLOW_GUARD_POLICY {
    pub Flags: u32,
    pub EnableControlFlowGuard: u32,
    pub EnableExportSuppression: u32,
    pub StrictMode: u32,
    pub EnableXfg: u32,
    pub EnableXfgAuditMode: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_BINARY_SIGNATURE_POLICY {
    pub Flags: u32,
    pub MicrosoftSignedOnly: u32,
    pub StoreSignedOnly: u32,
    pub MitigationOptIn: u32,
    pub AuditMicrosoftSignedOnly: u32,
    pub AuditStoreSignedOnly: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_FONT_DISABLE_POLICY {
    pub Flags: u32,
    pub DisableNonSystemFonts: u32,
    pub AuditNonSystemFontLoading: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_IMAGE_LOAD_POLICY {
    pub Flags: u32,
    pub NoRemoteImages: u32,
    pub NoLowMandatoryLabelImages: u32,
    pub PreferSystem32Images: u32,
    pub AuditNoRemoteImages: u32,
    pub AuditNoLowMandatoryLabelImages: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_SYSTEM_CALL_FILTER_POLICY {
    pub Flags: u32,
    pub FilterId: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_PAYLOAD_RESTRICTION_POLICY {
    pub Flags: u32,
    pub EnableExportAddressFilter: u32,
    pub AuditExportAddressFilter: u32,
    pub EnableExportAddressFilterPlus: u32,
    pub AuditExportAddressFilterPlus: u32,
    pub EnableImportAddressFilter: u32,
    pub AuditImportAddressFilter: u32,
    pub EnableRopStackPivot: u32,
    pub AuditRopStackPivot: u32,
    pub EnableRopCallerCheck: u32,
    pub AuditRopCallerCheck: u32,
    pub EnableRopSimExec: u32,
    pub AuditRopSimExec: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_CHILD_PROCESS_POLICY {
    pub Flags: u32,
    pub NoChildProcessCreation: u32,
    pub AuditNoChildProcessCreation: u32,
    pub AllowSecureProcessCreation: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_SIDE_CHANNEL_ISOLATION_POLICY {
    pub Flags: u32,
    pub SmtBranchTargetIsolation: u32,
    pub IsolateSecurityDomain: u32,
    pub DisablePageCombine: u32,
    pub SpeculativeStoreBypassDisable: u32,
    pub RestrictCoreSharing: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_USER_SHADOW_STACK_POLICY {
    pub Flags: u32,
    pub EnableUserShadowStack: u32,
    pub AuditUserShadowStack: u32,
    pub SetContextIpValidation: u32,
    pub AuditSetContextIpValidation: u32,
    pub EnableUserShadowStackStrictMode: u32,
    pub BlockNonCetBinaries: u32,
    pub BlockNonCetBinariesNonEhcont: u32,
    pub AuditBlockNonCetBinaries: u32,
    pub CetDynamicApisOutOfProcOnly: u32,
    pub SetContextIpValidationRelaxedMode: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_USER_POINTER_AUTH_POLICY {
    pub Flags: u32,
    pub EnablePointerAuthUserIp: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_MITIGATION_REDIRECTION_TRUST_POLICY {
    pub Flags: u32,
    pub EnforceRedirectionTrust: u32,
    pub AuditRedirectionTrust: u32,
    pub ReservedFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_NETWORK_COUNTERS {
    pub BytesIn: u32,
    pub BytesOut: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_KEEPALIVE_COUNT_INFORMATION {
    pub WakeCount: u32,
    pub NoWakeCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_REVOKE_FILE_HANDLES_INFORMATION {
    pub TargetDevicePath: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOLED_USAGE_AND_LIMITS {
    pub PeakPagedPoolUsage: usize,
    pub PagedPoolUsage: usize,
    pub PagedPoolLimit: usize,
    pub PeakNonPagedPoolUsage: usize,
    pub NonPagedPoolUsage: usize,
    pub NonPagedPoolLimit: usize,
    pub PeakPagefileUsage: usize,
    pub PagefileUsage: usize,
    pub PagefileLimit: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_ACCESS_TOKEN {
    pub Token: usize,
    pub Thread: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_EXCEPTION_PORT {
    pub ExceptionPortHandle: usize,
    pub StateFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KERNEL_USER_TIMES {
    pub CreateTime: i64,
    pub ExitTime: i64,
    pub KernelTime: i64,
    pub UserTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_THROTTLING_PROCESS_STATE {
    pub Version: u32,
    pub ControlMask: u32,
    pub StateMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_THROTTLING_THREAD_STATE {
    pub Version: u32,
    pub ControlMask: u32,
    pub StateMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESS_SYSCALL_PROVIDER_INFORMATION {
    pub ProviderId: u32,
    pub Level: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KPCR {
    pub NtTib: u32,
    pub TibPad0: [usize; 2],
    pub Spare1: usize,
    pub field_Self: u32,
    pub PcrReserved0: usize,
    pub LockArray: usize,
    pub Used_Self: usize,
    pub CurrentIrql: u32,
    pub SecondLevelCacheAssociativity: u8,
    pub Pad1: [u8; 2],
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub StallScaleFactor: u32,
    pub SecondLevelCacheSize: u32,
    pub SoftwareInterruptPending: u16,
    pub ApcInterrupt: u8,
    pub DispatchInterrupt: u8,
    pub InterruptPad: u16,
    pub BtiMitigation: u8,
    pub KvaVbar: u8,
    pub BtiVbar: u8,
    pub BtiCswapHvc: u8,
    pub BtiCswapSmc: u8,
    pub SsbMitigationFlags: u8,
    pub SsbMitigationFirmware: u8,
    pub SsbMitigationDynamic: u8,
    pub SsbMitigationKernel: u8,
    pub SsbMitigationUser: u8,
    pub SsbMitigationReserved: u8,
    pub BhbMitigation: u8,
    pub CachePrefetcherMitigationFlags: u8,
    pub CachePrefetcherMitigation: u8,
    pub Pad2: u8,
    pub PanicStorage: [u32; 6],
    pub KdVersionBlock: usize,
    pub HalReserved: [usize; 14],
    pub KvaUserModeTtbr1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEXCEPTION_FRAME {
    pub X19: u32,
    pub X20: u32,
    pub X21: u32,
    pub X22: u32,
    pub X23: u32,
    pub X24: u32,
    pub X25: u32,
    pub X26: u32,
    pub X27: u32,
    pub X28: u32,
    pub Fp: u32,
    pub Return: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KTRAP_FRAME {
    pub ExceptionActive: u32,
    pub ContextFromKFramesUnwound: u32,
    pub DebugRegistersValid: u32,
    pub PreviousMode: u32,
    pub PreviousIrql: u32,
    pub Reserved: u32,
    pub FaultAddress: u32,
    pub TrapFrame: u32,
    pub VfpState: u32,
    pub Bcr: [u32; 1],
    pub Bvr: [u32; 1],
    pub Wcr: [u32; 1],
    pub Wvr: [u32; 1],
    pub Spsr: u32,
    pub Esr: u32,
    pub Sp: u32,
    pub X: [u32; 19],
    pub X0: u32,
    pub X1: u32,
    pub X2: u32,
    pub X3: u32,
    pub X4: u32,
    pub X5: u32,
    pub X6: u32,
    pub X7: u32,
    pub X8: u32,
    pub X9: u32,
    pub X10: u32,
    pub X11: u32,
    pub X12: u32,
    pub X13: u32,
    pub X14: u32,
    pub X15: u32,
    pub X16: u32,
    pub X17: u32,
    pub X18: u32,
    pub Lr: u32,
    pub Fp: u32,
    pub Pc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KUMS_CONTEXT_HEADER {
    pub P1Home: u32,
    pub P2Home: u32,
    pub P3Home: u32,
    pub P4Home: u32,
    pub StackTop: usize,
    pub StackSize: u32,
    pub RspOffset: u32,
    pub Rip: u32,
    pub FltSave: usize,
    pub Volatile: u32,
    pub Reserved: u32,
    pub Flags: u32,
    pub TrapFrame: usize,
    pub ExceptionFrame: usize,
    pub field_SourceThread: u32,
    pub Return: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KARM_VFP_STATE {
    pub field_Link: u32,
    pub Fpscr: u32,
    pub Reserved: u32,
    pub Reserved2: u32,
    pub VfpD: [u64; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KARM64_VFP_STATE {
    pub field_Link: u32,
    pub Fpcr: u32,
    pub Fpsr: u32,
    pub V: [u32; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_FIRMWARE_TABLE_INFORMATION {
    pub ProviderSignature: u32,
    pub Action: u32,
    pub TableID: u32,
    pub TableBufferLength: u32,
    pub TableBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_FIRMWARE_TABLE_HANDLER {
    pub ProviderSignature: u32,
    pub Register: u8,
    pub FirmwareTableHandler: usize,
    pub DriverObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIMER_SET_COALESCABLE_TIMER_INFO {
    pub DueTime: i64,
    pub TimerApcRoutine: u32,
    pub TimerContext: u32,
    pub field_WakeContext: u32,
    pub Period: u32,
    pub TolerableDelay: u32,
    pub PreviousState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_VERIFIER_THUNK_PAIRS {
    pub PristineRoutine: usize,
    pub NewRoutine: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSTATE_FEATURE {
    pub Offset: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSTATE_CONFIGURATION {
    pub EnabledFeatures: u32,
    pub EnabledVolatileFeatures: u32,
    pub Size: u32,
    pub ControlFlags: u32,
    pub OptimizedSave: u32,
    pub CompactionEnabled: u32,
    pub ExtendedFeatureDisable: u32,
    pub Features: [u32; 1],
    pub EnabledSupervisorFeatures: u32,
    pub AlignedFeatures: u32,
    pub AllFeatureSize: u32,
    pub AllFeatures: [u32; 1],
    pub EnabledUserVisibleSupervisorFeatures: u32,
    pub ExtendedFeatureDisableFeatures: u32,
    pub AllNonLargeFeatureSize: u32,
    pub MaxSveVectorLength: u16,
    pub Spare1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KUSER_SHARED_DATA {
    pub TickCountLowDeprecated: u32,
    pub TickCountMultiplier: u32,
    pub InterruptTime: u32,
    pub SystemTime: u32,
    pub TimeZoneBias: u32,
    pub ImageNumberLow: u16,
    pub ImageNumberHigh: u16,
    pub NtSystemRoot: [u32; 260],
    pub MaxStackTraceDepth: u32,
    pub CryptoExponent: u32,
    pub TimeZoneId: u32,
    pub LargePageMinimum: u32,
    pub AitSamplingValue: u32,
    pub AppCompatFlag: u32,
    pub RNGSeedVersion: u64,
    pub GlobalValidationRunlevel: u32,
    pub TimeZoneBiasStamp: i32,
    pub NtBuildNumber: u32,
    pub NtProductType: u32,
    pub ProductTypeIsValid: u8,
    pub Reserved0: [u8; 1],
    pub NativeProcessorArchitecture: u16,
    pub NtMajorVersion: u32,
    pub NtMinorVersion: u32,
    pub ProcessorFeatures: [u8; 1],
    pub Reserved1: u32,
    pub Reserved3: u32,
    pub TimeSlip: u32,
    pub AlternativeArchitecture: u32,
    pub BootId: u32,
    pub SystemExpirationDate: i64,
    pub SuiteMask: u32,
    pub KdDebuggerEnabled: u8,
    pub MitigationPolicies: u8,
    pub NXSupportPolicy: u8,
    pub SEHValidationPolicy: u8,
    pub CurDirDevicesSkippedForDlls: u8,
    pub Reserved: u8,
    pub CyclesPerYield: u16,
    pub ActiveConsoleId: u32,
    pub DismountCount: u32,
    pub ComPlusPackage: u32,
    pub LastSystemRITEventTickCount: u32,
    pub NumberOfPhysicalPages: u32,
    pub SafeBootMode: u8,
    pub VirtualizationFlags: u8,
    pub ArchStartedInEl2: u8,
    pub QcSlIsSupported: u8,
    pub Reserved12: [u8; 2],
    pub SharedDataFlags: u32,
    pub DbgErrorPortPresent: u32,
    pub DbgElevationEnabled: u32,
    pub DbgVirtEnabled: u32,
    pub DbgInstallerDetectEnabled: u32,
    pub DbgLkgEnabled: u32,
    pub DbgDynProcessorEnabled: u32,
    pub DbgConsoleBrokerEnabled: u32,
    pub DbgSecureBootEnabled: u32,
    pub DbgMultiSessionSku: u32,
    pub DbgMultiUsersInSessionSku: u32,
    pub DbgStateSeparationEnabled: u32,
    pub SpareBits: u32,
    pub DataFlagsPad: [u32; 1],
    pub TestRetInstruction: u64,
    pub QpcFrequency: i64,
    pub SystemCall: u32,
    pub Reserved2: u32,
    pub FullNumberOfPhysicalPages: u64,
    pub SystemCallPad: [u64; 1],
    pub TickCount: u32,
    pub TickCountQuad: u32,
    pub ReservedTickCountOverlay: [u32; 3],
    pub TickCountPad: [u32; 1],
    pub Cookie: u32,
    pub CookiePad: [u32; 1],
    pub ConsoleSessionForegroundProcessId: i64,
    pub TimeUpdateLock: u64,
    pub BaselineSystemTimeQpc: u64,
    pub BaselineInterruptTimeQpc: u64,
    pub QpcSystemTimeIncrement: u64,
    pub QpcInterruptTimeIncrement: u64,
    pub QpcSystemTimeIncrementShift: u8,
    pub QpcInterruptTimeIncrementShift: u8,
    pub UnparkedProcessorCount: u16,
    pub EnclaveFeatureMask: [u32; 4],
    pub TelemetryCoverageRound: u32,
    pub UserModeGlobalLogger: [u16; 16],
    pub ImageFileExecutionOptions: u32,
    pub LangGenerationCount: u32,
    pub Reserved4: u64,
    pub InterruptTimeBias: u64,
    pub QpcBias: u64,
    pub ActiveProcessorCount: u32,
    pub ActiveGroupCount: u8,
    pub Reserved9: u8,
    pub QpcData: u16,
    pub QpcBypassEnabled: u8,
    pub QpcReserved: u8,
    pub TimeZoneBiasEffectiveStart: i64,
    pub TimeZoneBiasEffectiveEnd: i64,
    pub XState: u32,
    pub FeatureConfigurationChangeStamp: u32,
    pub Spare: u32,
    pub UserPointerAuthMask: u32,
    pub XStateArm64: u32,
    pub Reserved10: [u32; 210],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_PCCARD_DEVICE_DATA {
    pub Flags: u8,
    pub ErrorCode: u8,
    pub Reserved: u16,
    pub BusData: u32,
    pub DeviceId: u32,
    pub LegacyBaseAddress: u32,
    pub IRQMap: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ZONE_SEGMENT_HEADER {
    pub SegmentList: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ZONE_HEADER {
    pub FreeList: u32,
    pub SegmentList: u32,
    pub BlockSize: u32,
    pub TotalSegmentSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_MEMORY_RANGE {
    pub BaseAddress: u64,
    pub NumberOfBytes: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MM_COPY_ADDRESS {
    pub VirtualAddress: usize,
    pub PhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PS_CREATE_NOTIFY_INFO {
    pub Size: usize,
    pub Flags: u32,
    pub FileOpenNameAvailable: u32,
    pub IsSubsystemProcess: u32,
    pub Reserved: u32,
    pub ParentProcessId: usize,
    pub CreatingThreadId: u32,
    pub field_FileObject: u32,
    pub ImageFileName: usize,
    pub CommandLine: u32,
    pub CreationStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_INFO {
    pub Properties: u32,
    pub ImageAddressingMode: u32,
    pub SystemModeImage: u32,
    pub ImageMappedToAllPids: u32,
    pub ExtendedInfoPresent: u32,
    pub MachineTypeMismatch: u32,
    pub ImageSignatureLevel: u32,
    pub ImageSignatureType: u32,
    pub ImagePartialMap: u32,
    pub Reserved: u32,
    pub ImageBase: usize,
    pub ImageSelector: u32,
    pub ImageSize: usize,
    pub ImageSectionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_INFO_EX {
    pub Size: usize,
    pub ImageInfo: u32,
    pub field_FileObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SILO_MONITOR_REGISTRATION {
    pub Version: u8,
    pub MonitorHost: u8,
    pub MonitorExistingSilos: u8,
    pub Reserved: [u8; 5],
    pub DriverObjectName: usize,
    pub ComponentName: usize,
    pub CreateCallback: usize,
    pub TerminateCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTROLLER_OBJECT {
    pub Type: u32,
    pub Size: u32,
    pub ControllerExtension: usize,
    pub DeviceWaitQueue: u32,
    pub Spare1: u32,
    pub Spare2: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONFIGURATION_INFORMATION {
    pub DiskCount: u32,
    pub FloppyCount: u32,
    pub CdRomCount: u32,
    pub TapeCount: u32,
    pub ScsiPortCount: u32,
    pub SerialCount: u32,
    pub ParallelCount: u32,
    pub AtDiskPrimaryAddressClaimed: u8,
    pub AtDiskSecondaryAddressClaimed: u8,
    pub Version: u32,
    pub MediumChangerCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_SIGNATURE {
    pub PartitionStyle: u32,
    pub Signature: u32,
    pub CheckSum: u32,
    pub DiskId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_FOEXT_SILO_PARAMETERS {
    pub Length: u32,
    pub HasHardReference: u32,
    pub SpareFlags: u32,
    pub Flags: u32,
    pub SiloContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXN_PARAMETER_BLOCK {
    pub Length: u16,
    pub TxFsContext: u16,
    pub TransactionObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_USER_PROCESS_ECP_CONTEXT {
    pub Size: u16,
    pub Reserved: u16,
    pub AccessToken: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPLOCK_KEY_CONTEXT {
    pub Version: u16,
    pub Flags: u16,
    pub ParentOplockKey: u32,
    pub TargetOplockKey: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_DRIVER_CREATE_CONTEXT {
    pub Size: u32,
    pub field_ExtraCreateParameter: u32,
    pub DeviceObjectHint: usize,
    pub TxnParameters: usize,
    pub SiloContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_FOEXT_SHADOW_FILE {
    pub BackingFileObject: usize,
    pub BackingFltInstance: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDCB_STATUS_UPDATE_CONTEXT {
    pub StatusType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDCB_IMAGE_INFORMATION {
    pub Classification: u32,
    pub ImageFlags: u32,
    pub ImageName: u32,
    pub RegistryPath: u32,
    pub CertificatePublisher: u32,
    pub CertificateIssuer: u32,
    pub ImageHash: usize,
    pub CertificateThumbprint: usize,
    pub ImageHashAlgorithm: u32,
    pub ThumbprintHashAlgorithm: u32,
    pub ImageHashLength: u32,
    pub CertificateThumbprintLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGP_TARGET_BUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetBusData: usize,
    pub GetBusData: usize,
    pub CapabilityID: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_LOCATION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetLocationString: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_CONFLICT_INFO {
    pub OwningObject: usize,
    pub Start: u64,
    pub End: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_TEST_ALLOCATION_PARAMETERS {
    pub ArbitrationList: usize,
    pub AllocateFromCount: u32,
    pub AllocateFrom: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_RETEST_ALLOCATION_PARAMETERS {
    pub ArbitrationList: usize,
    pub AllocateFromCount: u32,
    pub AllocateFrom: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_BOOT_ALLOCATION_PARAMETERS {
    pub ArbitrationList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_QUERY_ALLOCATED_RESOURCES_PARAMETERS {
    pub field_AllocatedResources: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_QUERY_CONFLICT_PARAMETERS {
    pub PhysicalDeviceObject: usize,
    pub ConflictingResource: usize,
    pub ConflictCount: *mut u32,
    pub field_Conflicts: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_QUERY_ARBITRATE_PARAMETERS {
    pub ArbitrationList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_ADD_RESERVED_PARAMETERS {
    pub ReserveDevice: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_PARAMETERS {
    pub TestAllocation: u32,
    pub RetestAllocation: u32,
    pub BootAllocation: u32,
    pub QueryAllocatedResources: u32,
    pub QueryConflict: u32,
    pub QueryArbitrate: u32,
    pub AddReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_LIST_ENTRY {
    pub ListEntry: u32,
    pub AlternativeCount: u32,
    pub Alternatives: usize,
    pub PhysicalDeviceObject: usize,
    pub RequestSource: u32,
    pub Flags: u32,
    pub WorkSpace: isize,
    pub InterfaceType: u32,
    pub SlotNumber: u32,
    pub BusNumber: u32,
    pub Assignment: usize,
    pub SelectedAlternative: usize,
    pub Result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARBITER_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ArbiterHandler: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSLATOR_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TranslateResources: usize,
    pub TranslateResourceRequirements: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_REGISTER_PMU_NOTIFICATION_INPUT {
    pub Version: u16,
    pub Size: u16,
    pub OwnerTag: u32,
    pub CallbackRoutine: usize,
    pub CallbackContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_UNREGISTER_PMU_NOTIFICATION_INPUT {
    pub Version: u16,
    pub Size: u16,
    pub OwnerTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PM_DISPATCH_TABLE {
    pub Signature: u32,
    pub Version: u32,
    pub Function: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MAP_REGISTER_ENTRY {
    pub MapRegister: usize,
    pub WriteToDevice: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_TRANSPORT_DATA {
    pub HwContextSize: u32,
    pub SharedVisibleDataSize: u32,
    pub UseSerialFraming: u8,
    pub ValidUSBCoreId: u8,
    pub USBCoreId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_EFI_IOMMU_DATA {
    pub PciIoProtocolHandle: usize,
    pub Mapping: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_DEVICE_DESCRIPTOR {
    pub Bus: u32,
    pub Slot: u32,
    pub Segment: u16,
    pub VendorID: u16,
    pub DeviceID: u16,
    pub BaseClass: u8,
    pub SubClass: u8,
    pub ProgIf: u8,
    pub Flags: u8,
    pub DbgHalScratchAllocated: u8,
    pub DbgBarsMapped: u8,
    pub DbgScratchAllocated: u8,
    pub DbgUncachedMemory: u8,
    pub DbgSynthetic: u8,
    pub Initialized: u8,
    pub Configured: u8,
    pub BaseAddress: [u32; 1],
    pub Memory: u32,
    pub Dbg2TableIndex: u32,
    pub PortType: u16,
    pub PortSubtype: u16,
    pub OemData: usize,
    pub OemDataLength: u32,
    pub NameSpace: u32,
    pub NameSpacePath: usize,
    pub NameSpacePathLength: u32,
    pub TransportType: u32,
    pub TransportData: u32,
    pub EfiIoMmuData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_DEBUGGING_DEVICE_IN_USE {
    pub Segment: u16,
    pub Bus: u32,
    pub Slot: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_DEBUGGING_DEVICE_IN_USE {
    pub NameSpacePathLength: u32,
    pub NameSpacePath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUGGING_DEVICE_IN_USE {
    pub NameSpace: u32,
    pub StructureLength: u32,
    pub AcpiDevice: u32,
    pub PciDevice: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUGGING_DEVICE_IN_USE_INFORMATION {
    pub DeviceCount: u32,
    pub Device: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_BUS_INFORMATION {
    pub BusType: u32,
    pub ConfigurationType: u32,
    pub BusNumber: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_POWER_INFORMATION {
    pub TBD: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_PROCESSOR_SPEED_INFORMATION {
    pub ProcessorSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_CALLBACKS {
    pub SetSystemInformation: usize,
    pub BusCheck: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_PROCESSOR_FEATURE {
    pub UsableFeatureBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_AMLI_BAD_IO_ADDRESS_LIST {
    pub BadAddrBegin: u32,
    pub BadAddrSize: u32,
    pub OSVersionTrigger: u32,
    pub IOHandler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_MCA_INTERFACE {
    pub Lock: usize,
    pub Unlock: usize,
    pub ReadRegister: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCA_DRIVER_INFO {
    pub ExceptionCallback: usize,
    pub DpcCallback: usize,
    pub DeviceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_ERROR_INFO {
    pub Version: u32,
    pub InitMaxSize: u32,
    pub McaMaxSize: u32,
    pub McaPreviousEventsCount: u32,
    pub McaCorrectedEventsCount: u32,
    pub McaKernelDeliveryFails: u32,
    pub McaDriverDpcQueueFails: u32,
    pub McaReserved: u32,
    pub CmcMaxSize: u32,
    pub CmcPollingInterval: u32,
    pub CmcInterruptsCount: u32,
    pub CmcKernelDeliveryFails: u32,
    pub CmcDriverDpcQueueFails: u32,
    pub CmcGetStateFails: u32,
    pub CmcClearStateFails: u32,
    pub CmcReserved: u32,
    pub CmcLogId: u64,
    pub CpeMaxSize: u32,
    pub CpePollingInterval: u32,
    pub CpeInterruptsCount: u32,
    pub CpeKernelDeliveryFails: u32,
    pub CpeDriverDpcQueueFails: u32,
    pub CpeGetStateFails: u32,
    pub CpeClearStateFails: u32,
    pub CpeInterruptSources: u32,
    pub CpeLogId: u64,
    pub KernelReserved: [u64; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CMC_DRIVER_INFO {
    pub ExceptionCallback: usize,
    pub DpcCallback: usize,
    pub DeviceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CPE_DRIVER_INFO {
    pub ExceptionCallback: usize,
    pub DpcCallback: usize,
    pub DeviceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HAL_PLATFORM_INFORMATION {
    pub PlatformFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_COUNTER_EVENT_BUFFER_CONFIGURATION {
    pub OverflowHandler: usize,
    pub CustomEventBufferEntrySize: u32,
    pub EventThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_COUNTER_RESOURCE_DESCRIPTOR {
    pub Type: usize,
    pub Flags: u32,
    pub CounterIndex: u32,
    pub Begin: u32,
    pub End: u32,
    pub OverflowHandler: usize,
    pub EventBufferConfiguration: usize,
    pub IdentificationTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_COUNTER_RESOURCE_LIST {
    pub Count: u32,
    pub Descriptors: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCIBUSDATA {
    pub Tag: u32,
    pub Version: u32,
    pub ReadConfig: usize,
    pub WriteConfig: usize,
    pub Pin2Line: usize,
    pub Line2Pin: usize,
    pub ParentSlot: usize,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_LTR_CAPABILITY {
    pub Header: usize,
    pub Latency: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_TPH_REQUESTER_CAPABILITY {
    pub Header: usize,
    pub RequesterCapability: usize,
    pub RequesterControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_L1_PM_SS_CAPABILITY {
    pub Header: usize,
    pub L1PmSsCapabilities: usize,
    pub L1PmSsControl1: usize,
    pub L1PmSsControl2: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_DPC_RP_PIO_HEADERLOG_REGISTER {
    pub PioHeaderLogRegister: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_DPC_RP_PIO_TLPPREFIXLOG_REGISTER {
    pub PioTlpPrefixLogRegister: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_DPC_CAPABILITY {
    pub Header: usize,
    pub DpcCapabilities: usize,
    pub DpcControl: usize,
    pub DpcStatus: usize,
    pub DpcErrSrcId: usize,
    pub RpPioStatus: usize,
    pub RpPioMask: usize,
    pub RpPioSeverity: usize,
    pub RpPioSysError: usize,
    pub RpPioException: usize,
    pub RpPioHeaderLog: usize,
    pub RpPioImpSpecLog: usize,
    pub RpPioPrefixLog: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_RESIZABLE_BAR_ENTRY {
    pub Capability: usize,
    pub Control: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_RESIZABLE_BAR_CAPABILITY {
    pub Header: usize,
    pub Entry: [usize; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_DESIGNATED_VENDOR_SPECIFIC_CAPABILITY {
    pub Header: usize,
    pub DvsecHeader1: usize,
    pub DvsecHeader2: usize,
    pub DvsecRegisters: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_USB4_DESIGNATED_VENDOR_SPECIFIC_CAPABILITY {
    pub Header: usize,
    pub DvsecHeader1: usize,
    pub DvsecHeader2: usize,
    pub DvsecHeader3: usize,
    pub PortSpecificAttributes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_LINK_CONTROL3 {
    pub PerformEq: u32,
    pub LinkEqIntEn: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_LANE_ERROR_STATUS {
    pub LaneBitmap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_SECONDARY_CAPABILITY {
    pub Header: usize,
    pub LinkControl3: usize,
    pub LaneErrorStatus: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_NPEM_CAPABILITY {
    pub Header: usize,
    pub Capability: usize,
    pub Control: usize,
    pub Status: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_EVENT_COLLECTOR_ENDPOINT_ASSOCIATION_CAPABILITY {
    pub Header: usize,
    pub AssociationBitmap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_CXL_DVSEC_RANGE_SIZE_HIGH_REGISTER {
    pub MemSizeHigh: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_CXL_DVSEC_RANGE_BASE_HIGH_REGISTER {
    pub MemBaseHigh: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_CXL_DVSEC_CAPABILITY {
    pub Header: usize,
    pub DvsecHeader1: usize,
    pub DvsecHeader2: usize,
    pub Reserved: [u8; 46],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_CXL_DVSEC_CAPABILITY_V11 {
    pub Header: usize,
    pub DvsecHeader1: usize,
    pub DvsecHeader2: usize,
    pub Capability: usize,
    pub Control: usize,
    pub Status: usize,
    pub Control2: u16,
    pub Status2: u16,
    pub Lock: usize,
    pub Reserved: u16,
    pub Range1SizeHigh: usize,
    pub Range1SizeLow: usize,
    pub Range1BaseHigh: usize,
    pub Range1BaseLow: usize,
    pub Range2SizeHigh: usize,
    pub Range2SizeLow: usize,
    pub Range2BaseHigh: usize,
    pub Range2BaseLow: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_LINK_INFORMATION {
    pub Gts_2_5: u32,
    pub Gts_5_0: u32,
    pub Gts_8_0: u32,
    pub Gts_16_0: u32,
    pub Gts_32_0: u32,
    pub Gts_64_0: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
    pub LinkWidth: u16,
    pub CurrentLinkSpeed: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_LINK_CONFIG_INTERFACE_V1 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetMaximumLinkBandwidth: usize,
    pub GetLinkInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_BUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadConfig: usize,
    pub WriteConfig: usize,
    pub PinToLine: usize,
    pub LineToPin: usize,
    pub RootBusCapability: usize,
    pub ExpressWakeControl: usize,
    pub PrepareMultistageResume: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_CONFIGURATION_DD {
    pub Initialize: u32,
    pub Uninitialize: u32,
    pub Correct: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_CONFIGURATION_DEVICE_DRIVER_V1 {
    pub Version: u32,
    pub SourceGuid: u32,
    pub LogTag: u16,
    pub Reserved: [u8; 6],
    pub Initialize: u32,
    pub Uninitialize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_CONFIGURATION_DEVICE_DRIVER {
    pub Version: u32,
    pub SourceGuid: u32,
    pub LogTag: u16,
    pub Reserved: [u8; 6],
    pub Initialize: u32,
    pub Uninitialize: u32,
    pub MaxSectionDataLength: u32,
    pub MaxSectionsPerReport: u32,
    pub CreatorId: u32,
    pub PartitionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_DRIVER_BUFFER_SET {
    pub Version: u32,
    pub Data: *mut u8,
    pub DataSize: u32,
    pub SectionTypeGuid: usize,
    pub SectionFriendlyName: *mut u8,
    pub Flags: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_NOTIFICATION_DESCRIPTOR {
    pub Type: u8,
    pub Length: u8,
    pub Flags: u32,
    pub PollInterval: u32,
    pub PollInterval_1: u32,
    pub Vector: u32,
    pub SwitchToPollingThreshold: u32,
    pub SwitchToPollingWindow: u32,
    pub ErrorThreshold: u32,
    pub ErrorThresholdWindow: u32,
    pub PollInterval_2: u32,
    pub Vector_1: u32,
    pub SwitchToPollingThreshold_1: u32,
    pub SwitchToPollingWindow_1: u32,
    pub ErrorThreshold_1: u32,
    pub ErrorThresholdWindow_1: u32,
    pub PollInterval_3: u32,
    pub Vector_2: u32,
    pub SwitchToPollingThreshold_2: u32,
    pub SwitchToPollingWindow_2: u32,
    pub ErrorThreshold_2: u32,
    pub ErrorThresholdWindow_2: u32,
    pub PollInterval_4: u32,
    pub Vector_3: u32,
    pub SwitchToPollingThreshold_3: u32,
    pub SwitchToPollingWindow_3: u32,
    pub ErrorThreshold_3: u32,
    pub ErrorThresholdWindow_3: u32,
    pub PollInterval_5: u32,
    pub Vector_4: u32,
    pub SwitchToPollingThreshold_4: u32,
    pub SwitchToPollingWindow_4: u32,
    pub ErrorThreshold_4: u32,
    pub ErrorThresholdWindow_4: u32,
    pub PollInterval_6: u32,
    pub Vector_5: u32,
    pub SwitchToPollingThreshold_5: u32,
    pub SwitchToPollingWindow_5: u32,
    pub ErrorThreshold_5: u32,
    pub ErrorThresholdWindow_5: u32,
    pub PollInterval_7: u32,
    pub Vector_6: u32,
    pub SwitchToPollingThreshold_6: u32,
    pub SwitchToPollingWindow_6: u32,
    pub ErrorThreshold_6: u32,
    pub ErrorThresholdWindow_6: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_MC_BANK_DESCRIPTOR {
    pub BankNumber: u8,
    pub ClearOnInitialization: u8,
    pub StatusDataFormat: u8,
    pub Flags: u32,
    pub ControlMsr: u32,
    pub StatusMsr: u32,
    pub AddressMsr: u32,
    pub MiscMsr: u32,
    pub ControlData: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_MCE_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub NumberOfBanks: u8,
    pub Flags: u32,
    pub MCG_Capability: u64,
    pub MCG_GlobalControl: u64,
    pub Banks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_CMC_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub NumberOfBanks: u8,
    pub Reserved: u32,
    pub Notify: u32,
    pub Banks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCI_SLOT_NUMBER {
    pub DeviceNumber: u32,
    pub FunctionNumber: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_NMI_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AER_ROOTPORT_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
    pub BusNumber: u32,
    pub Slot: u32,
    pub DeviceControl: u16,
    pub Flags: u32,
    pub UncorrectableErrorMask: u32,
    pub UncorrectableErrorSeverity: u32,
    pub CorrectableErrorMask: u32,
    pub AdvancedCapsAndControl: u32,
    pub RootErrorCommand: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AER_ENDPOINT_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
    pub BusNumber: u32,
    pub Slot: u32,
    pub DeviceControl: u16,
    pub Flags: u32,
    pub UncorrectableErrorMask: u32,
    pub UncorrectableErrorSeverity: u32,
    pub CorrectableErrorMask: u32,
    pub AdvancedCapsAndControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AER_BRIDGE_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
    pub BusNumber: u32,
    pub Slot: u32,
    pub DeviceControl: u16,
    pub Flags: u32,
    pub UncorrectableErrorMask: u32,
    pub UncorrectableErrorSeverity: u32,
    pub CorrectableErrorMask: u32,
    pub AdvancedCapsAndControl: u32,
    pub SecondaryUncorrectableErrorMask: u32,
    pub SecondaryUncorrectableErrorSev: u32,
    pub SecondaryCapsAndControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GENERIC_ERROR_DESCRIPTOR {
    pub Type: u16,
    pub Reserved: u8,
    pub Enabled: u8,
    pub ErrStatusBlockLength: u32,
    pub RelatedErrorSourceId: u32,
    pub ErrStatusAddressSpaceID: u8,
    pub ErrStatusAddressBitWidth: u8,
    pub ErrStatusAddressBitOffset: u8,
    pub ErrStatusAddressAccessSize: u8,
    pub ErrStatusAddress: u32,
    pub Notify: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GENERIC_ERROR_DESCRIPTOR_V2 {
    pub Type: u16,
    pub Reserved: u8,
    pub Enabled: u8,
    pub ErrStatusBlockLength: u32,
    pub RelatedErrorSourceId: u32,
    pub ErrStatusAddressSpaceID: u8,
    pub ErrStatusAddressBitWidth: u8,
    pub ErrStatusAddressBitOffset: u8,
    pub ErrStatusAddressAccessSize: u8,
    pub ErrStatusAddress: u32,
    pub Notify: u32,
    pub ReadAckAddressSpaceID: u8,
    pub ReadAckAddressBitWidth: u8,
    pub ReadAckAddressBitOffset: u8,
    pub ReadAckAddressAccessSize: u8,
    pub ReadAckAddress: u32,
    pub ReadAckPreserveMask: u64,
    pub ReadAckWriteMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_DEVICE_DRIVER_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
    pub SourceGuid: u32,
    pub LogTag: u16,
    pub Reserved2: u16,
    pub PacketLength: u32,
    pub PacketCount: u32,
    pub PacketBuffer: *mut u8,
    pub Config: u32,
    pub CreatorId: u32,
    pub PartitionId: u32,
    pub MaxSectionDataLength: u32,
    pub MaxSectionsPerRecord: u32,
    pub PacketStateBuffer: *mut u8,
    pub OpenHandles: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_IPF_MCA_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_IPF_CMC_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_IPF_CPE_DESCRIPTOR {
    pub Type: u16,
    pub Enabled: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_DESCRIPTOR {
    pub Length: u32,
    pub Version: u32,
    pub Type: u32,
    pub State: u32,
    pub MaxRawDataLength: u32,
    pub NumRecordsToPreallocate: u32,
    pub MaxSectionsPerRecord: u32,
    pub ErrorSourceId: u32,
    pub PlatformErrorSourceId: u32,
    pub Flags: u32,
    pub XpfMceDescriptor: u32,
    pub XpfCmcDescriptor: u32,
    pub XpfNmiDescriptor: u32,
    pub IpfMcaDescriptor: u32,
    pub IpfCmcDescriptor: u32,
    pub IpfCpeDescriptor: u32,
    pub AerRootportDescriptor: u32,
    pub AerEndpointDescriptor: u32,
    pub AerBridgeDescriptor: u32,
    pub GenErrDescriptor: u32,
    pub GenErrDescriptorV2: u32,
    pub DeviceDriverDescriptor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPMI_OS_SEL_RECORD {
    pub Signature: u32,
    pub Version: u32,
    pub Length: u32,
    pub RecordType: u32,
    pub DataLength: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIMM_ADDR_VALID_BITS_DDR4 {
    pub SocketId: u32,
    pub MemoryControllerId: u32,
    pub ChannelId: u32,
    pub DimmSlot: u32,
    pub DimmRank: u32,
    pub Device: u32,
    pub ChipSelect: u32,
    pub Bank: u32,
    pub Dq: u32,
    pub Row: u32,
    pub Column: u32,
    pub Info: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIMM_ADDR_VALID_BITS_DDR5 {
    pub SocketId: u32,
    pub MemoryControllerId: u32,
    pub ChannelId: u32,
    pub SubChannelId: u32,
    pub DimmSlot: u32,
    pub DimmRank: u32,
    pub Device: u32,
    pub ChipId: u32,
    pub Bank: u32,
    pub Dq: u32,
    pub Row: u32,
    pub Column: u32,
    pub Info: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIMM_INFO {
    pub DimmAddress: u32,
    pub ValidBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_DEFECT {
    pub Version: u32,
    pub DimmInfo: u32,
    pub ErrType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_RECORD_HEADER {
    pub Signature: u32,
    pub Revision: u32,
    pub SignatureEnd: u32,
    pub SectionCount: u16,
    pub Severity: u32,
    pub ValidBits: u32,
    pub Length: u32,
    pub Timestamp: u32,
    pub PlatformId: u32,
    pub PartitionId: u32,
    pub CreatorId: u32,
    pub NotifyType: u32,
    pub RecordId: u64,
    pub Flags: u32,
    pub PersistenceInfo: u32,
    pub OsBuildNumber: u32,
    pub Reserved2: [u8; 8],
    pub Reserved: [u8; 12],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_RECORD_SECTION_DESCRIPTOR {
    pub SectionOffset: u32,
    pub SectionLength: u32,
    pub Revision: u32,
    pub ValidBits: u32,
    pub Reserved: u8,
    pub Flags: u32,
    pub SectionType: u32,
    pub FRUId: u32,
    pub SectionSeverity: u32,
    pub FRUText: [u32; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_RECORD {
    pub Header: u32,
    pub SectionDescriptor: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PROCESSOR_GENERIC_ERROR_SECTION {
    pub ValidBits: u32,
    pub ProcessorType: u8,
    pub InstructionSet: u8,
    pub ErrorType: u8,
    pub Operation: u8,
    pub Flags: u8,
    pub Level: u8,
    pub Reserved: u16,
    pub CPUVersion: u64,
    pub CPUBrandString: [u8; 128],
    pub ProcessorId: u64,
    pub TargetAddress: u64,
    pub RequesterId: u64,
    pub ResponderId: u64,
    pub InstructionPointer: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_PROCINFO {
    pub CheckInfoId: u32,
    pub ValidBits: u32,
    pub CacheCheck: u32,
    pub TlbCheck: u32,
    pub BusCheck: u32,
    pub MsCheck: u32,
    pub AsULONGLONG: u64,
    pub TargetId: u64,
    pub RequesterId: u64,
    pub ResponderId: u64,
    pub InstructionPointer: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_X86_REGISTER_STATE {
    pub Eax: u32,
    pub Ebx: u32,
    pub Ecx: u32,
    pub Edx: u32,
    pub Esi: u32,
    pub Edi: u32,
    pub Ebp: u32,
    pub Esp: u32,
    pub Cs: u16,
    pub Ds: u16,
    pub Ss: u16,
    pub Es: u16,
    pub Fs: u16,
    pub Gs: u16,
    pub Eflags: u32,
    pub Eip: u32,
    pub Cr0: u32,
    pub Cr1: u32,
    pub Cr2: u32,
    pub Cr3: u32,
    pub Cr4: u32,
    pub Gdtr: u64,
    pub Idtr: u64,
    pub Ldtr: u16,
    pub Tr: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_X64_REGISTER_STATE {
    pub Rax: u64,
    pub Rbx: u64,
    pub Rcx: u64,
    pub Rdx: u64,
    pub Rsi: u64,
    pub Rdi: u64,
    pub Rbp: u64,
    pub Rsp: u64,
    pub R8: u64,
    pub R9: u64,
    pub R10: u64,
    pub R11: u64,
    pub R12: u64,
    pub R13: u64,
    pub R14: u64,
    pub R15: u64,
    pub Cs: u16,
    pub Ds: u16,
    pub Ss: u16,
    pub Es: u16,
    pub Fs: u16,
    pub Gs: u16,
    pub Reserved: u32,
    pub Rflags: u64,
    pub Eip: u64,
    pub Cr0: u64,
    pub Cr1: u64,
    pub Cr2: u64,
    pub Cr3: u64,
    pub Cr4: u64,
    pub Cr8: u64,
    pub Gdtr: u32,
    pub Idtr: u32,
    pub Ldtr: u16,
    pub Tr: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_CONTEXT_INFO {
    pub RegisterContextType: u16,
    pub RegisterDataSize: u16,
    pub MSRAddress: u32,
    pub MmRegisterAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_PROCESSOR_ERROR_SECTION {
    pub ValidBits: u32,
    pub LocalAPICId: u64,
    pub CpuId: [u8; 48],
    pub VariableInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_ERROR_SECTION {
    pub ValidBits: u32,
    pub ErrorStatus: u32,
    pub PhysicalAddress: u64,
    pub PhysicalAddressMask: u64,
    pub Node: u16,
    pub Card: u16,
    pub Module: u16,
    pub Bank: u16,
    pub Device: u16,
    pub Row: u16,
    pub Column: u16,
    pub BitPosition: u16,
    pub RequesterId: u64,
    pub ResponderId: u64,
    pub TargetId: u64,
    pub ErrorType: u8,
    pub Extended: u8,
    pub RankNumber: u16,
    pub CardHandle: u16,
    pub ModuleHandle: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_HARDWARE_ADDRESS_INTEL {
    pub MemDef: u32,
    pub SystemAddress: u64,
    pub SpareSystemAddress: u64,
    pub DevicePhysicalAddress: u64,
    pub ChannelAddress: u64,
    pub RankAddress: u64,
    pub ProcessorSocketId: u32,
    pub MemoryControllerId: u32,
    pub TargetId: u32,
    pub LogicalChannelId: u32,
    pub ChannelId: u32,
    pub SubChannelId: u32,
    pub PhysicalRankId: u32,
    pub DimmSlotId: u32,
    pub DimmRankId: u32,
    pub Bank: u32,
    pub BankGroup: u32,
    pub Row: u32,
    pub Column: u32,
    pub LockStepRank: u32,
    pub LockStepPhysicalRank: u32,
    pub LockStepBank: u32,
    pub LockStepBankGroup: u32,
    pub ChipSelect: u32,
    pub Node: u32,
    pub ChipId: u32,
    pub Reserved: [u32; 40],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_ERROR_EXT_SECTION_INTEL {
    pub Flags: u32,
    pub ValidBits: u32,
    pub HardwareAddress: u32,
    pub Reserved: [u32; 40],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PMEM_PAGE_RANGE {
    pub StartingPfn: u32,
    pub PageCount: u32,
    pub MarkedBadBitmap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PMEM_ERROR_SECTION {
    pub ValidBits: u32,
    pub LocationInfo: [u8; 1],
    pub ErrorStatus: u32,
    pub NFITHandle: u32,
    pub PageRangeCount: u32,
    pub PageRange: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIE_ADDRESS {
    pub Segment: u32,
    pub Bus: u32,
    pub Device: u32,
    pub Function: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIE_CORRECTABLE_ERROR_DEVICES {
    pub ValidBits: u32,
    pub Address: u32,
    pub Mask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIE_CORRECTABLE_ERROR_SECTION_HEADER {
    pub Version: u32,
    pub Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIE_CORRECTABLE_ERROR_SECTION {
    pub Header: u32,
    pub Devices: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_CORRECTABLE_ERROR_HEADER {
    pub Version: u32,
    pub Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_CORRECTABLE_ERROR_DATA {
    pub ValidBits: u32,
    pub SocketId: u32,
    pub ChannelId: u32,
    pub DimmSlot: u32,
    pub CorrectableErrorCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_CORRECTABLE_ERROR_SECTION {
    pub Header: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIEXPRESS_DEVICE_ID {
    pub VendorID: u16,
    pub DeviceID: u16,
    pub ClassCode: u32,
    pub FunctionNumber: u32,
    pub DeviceNumber: u32,
    pub Segment: u32,
    pub PrimaryBusNumber: u32,
    pub SecondaryBusNumber: u32,
    pub Reserved1: u32,
    pub SlotNumber: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIEXPRESS_ERROR_SECTION {
    pub ValidBits: u32,
    pub PortType: u32,
    pub Version: u32,
    pub CommandStatus: u32,
    pub Reserved: u32,
    pub DeviceId: u32,
    pub DeviceSerialNumber: u64,
    pub BridgeControlStatus: u32,
    pub ExpressCapability: [u8; 60],
    pub AerInfo: [u8; 96],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIXBUS_ERROR_SECTION {
    pub ValidBits: u32,
    pub ErrorStatus: u32,
    pub ErrorType: u16,
    pub BusId: u32,
    pub Reserved: u32,
    pub BusAddress: u64,
    pub BusData: u64,
    pub BusCommand: u32,
    pub RequesterId: u64,
    pub CompleterId: u64,
    pub TargetId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIXDEVICE_ID {
    pub VendorId: u16,
    pub DeviceId: u16,
    pub ClassCode: u32,
    pub FunctionNumber: u32,
    pub DeviceNumber: u32,
    pub BusNumber: u32,
    pub SegmentNumber: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCIXDEVICE_ERROR_SECTION {
    pub ValidBits: u32,
    pub ErrorStatus: u32,
    pub IdInfo: u32,
    pub MemoryNumber: u32,
    pub IoNumber: u32,
    pub RegisterDataPairs: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_FIRMWARE_ERROR_RECORD_REFERENCE {
    pub Type: u8,
    pub Reserved: [u8; 7],
    pub FirmwareRecordId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCI_STATUS_BITS_COMMON {
    pub McaErrorCode: u32,
    pub ModelErrorCode: u32,
    pub Reserved: u32,
    pub ContextCorrupt: u32,
    pub AddressValid: u32,
    pub MiscValid: u32,
    pub ErrorEnabled: u32,
    pub UncorrectedError: u32,
    pub StatusOverFlow: u32,
    pub Valid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCI_STATUS_AMD_BITS {
    pub McaErrorCode: u32,
    pub ModelErrorCode: u32,
    pub ImplementationSpecific2: u32,
    pub Poison: u32,
    pub Deferred: u32,
    pub ImplementationSpecific1: u32,
    pub ContextCorrupt: u32,
    pub AddressValid: u32,
    pub MiscValid: u32,
    pub ErrorEnabled: u32,
    pub UncorrectedError: u32,
    pub StatusOverFlow: u32,
    pub Valid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCI_STATUS_INTEL_BITS {
    pub McaErrorCode: u32,
    pub ModelErrorCode: u32,
    pub OtherInfo: u32,
    pub FirmwareUpdateError: u32,
    pub CorrectedErrorCount: u32,
    pub ThresholdErrorStatus: u32,
    pub ActionRequired: u32,
    pub Signalling: u32,
    pub ContextCorrupt: u32,
    pub AddressValid: u32,
    pub MiscValid: u32,
    pub ErrorEnabled: u32,
    pub UncorrectedError: u32,
    pub StatusOverFlow: u32,
    pub Valid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AMD_EXTENDED_REGISTERS {
    pub IPID: u64,
    pub SYND: u64,
    pub CONFIG: u64,
    pub DESTAT: u64,
    pub DEADDR: u64,
    pub MISC1: u64,
    pub MISC2: u64,
    pub MISC3: u64,
    pub MISC4: u64,
    pub RasCap: u64,
    pub Reserved: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XPF_RECOVERY_INFO {
    pub NotSupported: u32,
    pub Overflow: u32,
    pub ContextCorrupt: u32,
    pub RestartIpErrorIpNotValid: u32,
    pub NoRecoveryContext: u32,
    pub MiscOrAddrNotValid: u32,
    pub InvalidAddressMode: u32,
    pub HighIrql: u32,
    pub InterruptsDisabled: u32,
    pub SwapBusy: u32,
    pub StackOverflow: u32,
    pub Reserved: u32,
    pub RecoveryAttempted: u32,
    pub HvHandled: u32,
    pub Reserved_1: u32,
    pub ActionRequired: u8,
    pub RecoverySucceeded: u8,
    pub RecoveryKernel: u8,
    pub Reserved_2: u32,
    pub Reserved2: u32,
    pub Reserved3: u32,
    pub Reserved4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_XPF_MCA_SECTION {
    pub VersionNumber: u32,
    pub CpuVendor: u32,
    pub Timestamp: i64,
    pub ProcessorNumber: u32,
    pub GlobalStatus: u32,
    pub InstructionPointer: u64,
    pub BankNumber: u32,
    pub Status: u32,
    pub Address: u64,
    pub Misc: u64,
    pub ExtendedRegisterCount: u32,
    pub ApicId: u32,
    pub ExtendedRegisters: [u64; 1],
    pub AMDExtendedRegisters: u32,
    pub GlobalCapability: u32,
    pub RecoveryInfo: u32,
    pub ExBankCount: u32,
    pub BankNumberEx: [u32; 1],
    pub StatusEx: [u32; 1],
    pub AddressEx: [u64; 1],
    pub MiscEx: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_NMI_ERROR_SECTION {
    pub Data: [u8; 8],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MSR_DUMP_SECTION {
    pub MsrDumpBuffer: u8,
    pub MsrDumpLength: u32,
    pub MsrDumpData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MU_TELEMETRY_SECTION {
    pub ComponentID: u32,
    pub SubComponentID: u32,
    pub Reserved: u32,
    pub ErrorStatusValue: u32,
    pub AdditionalInfo1: u64,
    pub AdditionalInfo2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_PROCESSOR_ERROR_SECTION {
    pub ValidBits: u32,
    pub ErrorInformationStructures: u16,
    pub ContextInformationStructures: u16,
    pub SectionLength: u32,
    pub ErrorAffinityLevel: u8,
    pub Reserved: [u8; 3],
    pub MPIDR_EL1: u64,
    pub MIDR_EL1: u64,
    pub RunningState: u32,
    pub PSCIState: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_RECOVERY_INFO_SECTION {
    pub RecoveryKernel: u8,
    pub RecoveryAction: u32,
    pub RecoveryType: u32,
    pub Irql: u32,
    pub RecoverySucceeded: u8,
    pub FailureReason: u32,
    pub ProcessName: [u32; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_CACHE_ERROR {
    pub ValidationBit: u32,
    pub TransactionType: u8,
    pub Operation: u8,
    pub Level: u8,
    pub ProcessorContextCorrupt: u8,
    pub Corrected: u8,
    pub PrecisePC: u8,
    pub RestartablePC: u8,
    pub Reserved: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_TLB_ERROR {
    pub ValidationBit: u32,
    pub TransactionType: u8,
    pub Operation: u8,
    pub Level: u8,
    pub ProcessorContextCorrupt: u8,
    pub Corrected: u8,
    pub PrecisePC: u8,
    pub RestartablePC: u8,
    pub Reserved: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_BUS_ERROR {
    pub ValidationBit: u32,
    pub TransactionType: u8,
    pub Operation: u8,
    pub Level: u8,
    pub ProcessorContextCorrupt: u8,
    pub Corrected: u8,
    pub PrecisePC: u8,
    pub RestartablePC: u8,
    pub ParticipationType: u8,
    pub TimeOut: u8,
    pub AddressSpace: u8,
    pub MemoryAccessAttributes: u16,
    pub AccessMode: u8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_PROCESSOR_ERROR_INFORMATION {
    pub Version: u8,
    pub Length: u8,
    pub ValidationBit: u32,
    pub Type: u8,
    pub MultipleError: u16,
    pub Flags: u8,
    pub ErrorInformation: u64,
    pub VirtualFaultAddress: u64,
    pub PhysicalFaultAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARMV8_AARCH32_GPRS {
    pub R0: u32,
    pub R1: u32,
    pub R2: u32,
    pub R3: u32,
    pub R4: u32,
    pub R5: u32,
    pub R6: u32,
    pub R7: u32,
    pub R8: u32,
    pub R9: u32,
    pub R10: u32,
    pub R11: u32,
    pub R12: u32,
    pub R13: u32,
    pub R14: u32,
    pub R15: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_AARCH32_EL1_CSR {
    pub DFAR: u32,
    pub DFSR: u32,
    pub IFAR: u32,
    pub ISR: u32,
    pub MAIR0: u32,
    pub MAIR1: u32,
    pub MIDR: u32,
    pub MPIDR: u32,
    pub NMRR: u32,
    pub PRRR: u32,
    pub SCTLR: u32,
    pub SPSR: u32,
    pub SPSR_abt: u32,
    pub SPSR_fiq: u32,
    pub SPSR_irq: u32,
    pub SPSR_svc: u32,
    pub SPSR_und: u32,
    pub TPIDRPRW: u32,
    pub TPIDRURO: u32,
    pub TPIDRURW: u32,
    pub TTBCR: u32,
    pub TTBR0: u32,
    pub TTBR1: u32,
    pub DACR: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_AARCH32_EL2_CSR {
    pub ELR_hyp: u32,
    pub HAMAIR0: u32,
    pub HAMAIR1: u32,
    pub HCR: u32,
    pub HCR2: u32,
    pub HDFAR: u32,
    pub HIFAR: u32,
    pub HPFAR: u32,
    pub HSR: u32,
    pub HTCR: u32,
    pub HTPIDR: u32,
    pub HTTBR: u32,
    pub SPSR_hyp: u32,
    pub VTCR: u32,
    pub VTTBR: u32,
    pub DACR32_EL2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_AARCH32_SECURE_CSR {
    pub SCTLR: u32,
    pub SPSR_mon: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARMV8_AARCH64_GPRS {
    pub X0: u64,
    pub X1: u64,
    pub X2: u64,
    pub X3: u64,
    pub X4: u64,
    pub X5: u64,
    pub X6: u64,
    pub X7: u64,
    pub X8: u64,
    pub X9: u64,
    pub X10: u64,
    pub X11: u64,
    pub X12: u64,
    pub X13: u64,
    pub X14: u64,
    pub X15: u64,
    pub X16: u64,
    pub X17: u64,
    pub X18: u64,
    pub X19: u64,
    pub X20: u64,
    pub X21: u64,
    pub X22: u64,
    pub X23: u64,
    pub X24: u64,
    pub X25: u64,
    pub X26: u64,
    pub X27: u64,
    pub X28: u64,
    pub X29: u64,
    pub X30: u64,
    pub SP: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_AARCH64_EL1_CSR {
    pub ELR_EL1: u64,
    pub ESR_EL2: u64,
    pub FAR_EL1: u64,
    pub ISR_EL1: u64,
    pub MAIR_EL1: u64,
    pub MIDR_EL1: u64,
    pub MPIDR_EL1: u64,
    pub SCTLR_EL1: u64,
    pub SP_EL0: u64,
    pub SP_EL1: u64,
    pub SPSR_EL1: u64,
    pub TCR_EL1: u64,
    pub TPIDR_EL0: u64,
    pub TPIDR_EL1: u64,
    pub TPIDRRO_EL0: u64,
    pub TTBR0_EL1: u64,
    pub TTBR1_EL1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_AARCH64_EL2_CSR {
    pub ELR_EL2: u64,
    pub ESR_EL2: u64,
    pub FAR_EL2: u64,
    pub HACR_EL2: u64,
    pub HCR_EL2: u64,
    pub HPFAR_EL2: u64,
    pub MAIR_EL2: u64,
    pub SCTLR_EL2: u64,
    pub SP_EL2: u64,
    pub SPSR_EL2: u64,
    pub TCR_EL2: u64,
    pub TPIDR_EL2: u64,
    pub TTBR0_EL2: u64,
    pub VTCR_EL2: u64,
    pub VTTBR_EL2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARMV8_AARCH64_EL3_CSR {
    pub ELR_EL3: u64,
    pub ESR_EL3: u64,
    pub FAR_EL3: u64,
    pub MAIR_EL3: u64,
    pub SCTLR_EL3: u64,
    pub SP_EL3: u64,
    pub SPSR_EL3: u64,
    pub TCR_EL3: u64,
    pub TPIDR_EL3: u64,
    pub TTBR0_EL3: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_MISC_CSR {
    pub MRSEncoding: u16,
    pub Value: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_PROCESSOR_ERROR_CONTEXT_INFORMATION_HEADER {
    pub Version: u16,
    pub RegisterContextType: u16,
    pub RegisterArraySize: u32,
    pub RegisterArray: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEA_SECTION {
    pub Esr: u32,
    pub Far: u32,
    pub Par: u32,
    pub WasKernel: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEI_SECTION {
    pub Esr: u32,
    pub Far: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ARM_RAS_NODE_SECTION {
    pub NodeFieldCount: u32,
    pub NodeIndex: u32,
    pub InterfaceType: u32,
    pub AestNodeType: u32,
    pub Reserved: [u32; 6],
    pub ErrFr: u64,
    pub ErrCtlr: u64,
    pub ErrStatus: u64,
    pub ErrAddr: u64,
    pub ErrMisc0: u64,
    pub ErrMisc1: u64,
    pub ErrMisc2: u64,
    pub ErrMisc3: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PCI_RECOVERY_SECTION {
    pub SignalType: u32,
    pub RecoveryAttempted: u8,
    pub RecoveryStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_PACKET_V1 {
    pub Signature: u32,
    pub Flags: u32,
    pub Size: u32,
    pub RawDataLength: u32,
    pub Reserved1: u64,
    pub Context: u64,
    pub ErrorType: u32,
    pub ErrorSeverity: u32,
    pub ErrorSourceId: u32,
    pub ErrorSourceType: u32,
    pub Reserved2: u32,
    pub Version: u32,
    pub Cpu: u64,
    pub ProcessorError: u32,
    pub MemoryError: u32,
    pub NmiError: u32,
    pub PciExpressError: u32,
    pub PciXBusError: u32,
    pub PciXDeviceError: u32,
    pub PmemError: u32,
    pub RawDataFormat: u32,
    pub RawDataOffset: u32,
    pub RawData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_PACKET_V2 {
    pub Signature: u32,
    pub Version: u32,
    pub Length: u32,
    pub Flags: u32,
    pub ErrorType: u32,
    pub ErrorSeverity: u32,
    pub ErrorSourceId: u32,
    pub ErrorSourceType: u32,
    pub NotifyType: u32,
    pub Context: u64,
    pub DataFormat: u32,
    pub Reserved1: u32,
    pub DataOffset: u32,
    pub DataLength: u32,
    pub PshedDataOffset: u32,
    pub PshedDataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_EVENT_LOG_ENTRY_HEADER {
    pub Signature: u32,
    pub Version: u32,
    pub Length: u32,
    pub Type: u32,
    pub OwnerTag: u32,
    pub Id: u32,
    pub Flags: u32,
    pub PayloadLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_EVENT_LOG_ENTRY {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_DEFERRED_EVENT {
    pub ListEntry: u32,
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_BAD_HEST_NOTIFY_DATA_EVENT {
    pub WheaEventLogEntry: u32,
    pub SourceId: u16,
    pub Reserved: u16,
    pub NotifyDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_STARTED_REPORT_HW_ERROR {
    pub WheaEventLogEntry: u32,
    pub ErrorPacket: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PFA_MEMORY_OFFLINED {
    pub WheaEventLogEntry: u32,
    pub DecisionType: u32,
    pub ImmediateSuccess: u8,
    pub Page: u32,
    pub NotifyVid: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PFA_MEMORY_OFFLINED_NOTIFY_CALLBACK_ACTION {
    pub WheaEventLogEntry: u32,
    pub Page: u32,
    pub ComponentTag: u32,
    pub Status: u32,
    pub ActionTaken: u32,
    pub ActionTakenAdditionalInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PSHED_INJECT_ERROR {
    pub WheaEventLogEntry: u32,
    pub ErrorType: u32,
    pub Parameter1: u64,
    pub Parameter2: u64,
    pub Parameter3: u64,
    pub Parameter4: u64,
    pub InjectionStatus: u32,
    pub InjectionAttempted: u8,
    pub InjectionByPlugin: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_OSC_IMPLEMENTED {
    pub WheaEventLogEntry: u32,
    pub OscImplemented: u8,
    pub DebugChecked: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PSHED_PLUGIN_REGISTER {
    pub WheaEventLogEntry: u32,
    pub Version: u32,
    pub Length: u32,
    pub FunctionalAreaMask: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PFA_MEMORY_REMOVE_MONITOR {
    pub WheaEventLogEntry: u32,
    pub RemoveTrigger: u32,
    pub TimeInList: u32,
    pub ErrorCount: u32,
    pub Page: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PFA_MEMORY_POLICY {
    pub WheaEventLogEntry: u32,
    pub RegistryKeysPresent: u32,
    pub DisableOffline: u8,
    pub PersistOffline: u8,
    pub PfaDisabled: u8,
    pub PageCount: u32,
    pub ErrorThreshold: u32,
    pub TimeOut: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_DROPPED_CORRECTED_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrorSourceType: u32,
    pub ErrorSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_CLEARED_POISON_EVENT {
    pub WheaEventLogEntry: u32,
    pub PhysicalAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ADD_REMOVE_ERROR_SOURCE_EVENT {
    pub WheaEventLogEntry: u32,
    pub Descriptor: u32,
    pub Status: u32,
    pub IsRemove: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ATTEMPT_RECOVERY_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrorHeader: u32,
    pub ArchitecturalRecovery: u8,
    pub PshedRecovery: u8,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_FOUND_ERROR_IN_BANK_EVENT {
    pub WheaEventLogEntry: u32,
    pub EpIndex: u32,
    pub Bank: u32,
    pub MciStatus: u32,
    pub ErrorType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_STUCK_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub EpIndex: u32,
    pub Bank: u32,
    pub MciStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ERROR_CLEARED_EVENT {
    pub WheaEventLogEntry: u32,
    pub EpIndex: u32,
    pub Bank: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PROCESS_EINJ_EVENT {
    pub WheaEventLogEntry: u32,
    pub Error: [i8; 1],
    pub InjectionActionTableValid: u8,
    pub BeginInjectionInstructionCount: u32,
    pub GetTriggerErrorActionTableInstructionCount: u32,
    pub SetErrorTypeInstructionCount: u32,
    pub GetErrorTypeInstructionCount: u32,
    pub EndOperationInstructionCount: u32,
    pub ExecuteOperationInstructionCount: u32,
    pub CheckBusyStatusInstructionCount: u32,
    pub GetCommandStatusInstructionCount: u32,
    pub SetErrorTypeWithAddressInstructionCount: u32,
    pub GetExecuteOperationTimingsInstructionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PROCESS_HEST_EVENT {
    pub WheaEventLogEntry: u32,
    pub Error: [i8; 1],
    pub EntryType: [i8; 1],
    pub EntryIndex: u32,
    pub HestValid: u8,
    pub CmcCount: u32,
    pub MceCount: u32,
    pub NmiCount: u32,
    pub AerRootCount: u32,
    pub AerBridgeCount: u32,
    pub AerEndPointCount: u32,
    pub GenericV1Count: u32,
    pub GenericV2Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_CREATE_GENERIC_RECORD_EVENT {
    pub WheaEventLogEntry: u32,
    pub Error: [i8; 1],
    pub EntryCount: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ERROR_RECORD_EVENT {
    pub WheaEventLogEntry: u32,
    pub Record: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ERR_SRC_ARRAY_INVALID_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrorSourceCount: u32,
    pub ReportedLength: u32,
    pub ExpectedLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ERR_SRC_INVALID_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrDescriptor: u32,
    pub Error: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_GENERIC_ERR_MEM_MAP_EVENT {
    pub WheaEventLogEntry: u32,
    pub MapReason: [i8; 1],
    pub PhysicalAddress: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ACPI_TIMEOUT_EVENT {
    pub WheaEventLogEntry: u32,
    pub TableType: [i8; 1],
    pub TableRequest: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_CMCI_RESTART_EVENT {
    pub WheaEventLogEntry: u32,
    pub CmciRestoreAttempts: u32,
    pub MaxCmciRestoreLimit: u32,
    pub MaxCorrectedErrorsFound: u32,
    pub MaxCorrectedErrorLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_BUGCHECK_PROGRESS {
    pub WheaEventLogEntry: u32,
    pub BugCheckCode: u32,
    pub BugCheckProgressSummary: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_BUGCHECK_RECOVERY_STATUS_START_EVENT {
    pub WheaEventLogEntry: u32,
    pub StartingIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_BUGCHECK_RECOVERY_STATUS_PHASE1_EVENT {
    pub WheaEventLogEntry: u32,
    pub Success: u8,
    pub Version: u8,
    pub EntryCount: u16,
    pub DumpPolicy: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_BUGCHECK_RECOVERY_STATUS_PHASE2_EVENT {
    pub WheaEventLogEntry: u32,
    pub BootId: u32,
    pub Success: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_BUGCHECK_RECOVERY_STATUS_MULTIPLE_BUGCHECK_EVENT {
    pub WheaEventLogEntry: u32,
    pub IsBugcheckOwner: u8,
    pub RecursionCount: u8,
    pub IsBugcheckRecoveryOwner: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_LOAD_EVENT {
    pub WheaEventLogEntry: u32,
    pub PluginName: [u32; 1],
    pub MajorVersion: u32,
    pub MinorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_UNLOAD_EVENT {
    pub WheaEventLogEntry: u32,
    pub PluginName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_ENABLE_NOTIFY_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
    pub EnableError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_PLATFORM_SUPPORT_EVENT {
    pub WheaEventLogEntry: u32,
    pub PluginName: [u32; 1],
    pub Supported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_INIT_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_HEARTBEAT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_DIMM_MISMATCH {
    pub WheaEventLogEntry: u32,
    pub FirmwareBank: u32,
    pub FirmwareCol: u32,
    pub FirmwareRow: u32,
    pub RetryRdBank: u32,
    pub RetryRdCol: u32,
    pub RetryRdRow: u32,
    pub TaBank: u32,
    pub TaCol: u32,
    pub TaRow: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ETW_OVERFLOW_EVENT {
    pub WheaEventLogEntry: u32,
    pub RecordId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_CMCI_IMPLEMENTED_EVENT {
    pub WheaEventLogEntry: u32,
    pub CmciAvailable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_DEVICE_DRV_EVENT {
    pub WheaEventLogEntry: u32,
    pub Function: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PLUGIN_PFA_EVENT {
    pub WheaEventLogEntry: u32,
    pub NoFurtherPfa: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_CMCI_INITERR_EVENT {
    pub WheaEventLogEntry: u32,
    pub Msr: u64,
    pub Type: u32,
    pub Bank: u32,
    pub EpIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AZCC_ROOT_BUS_ERR_EVENT {
    pub WheaEventLogEntry: u32,
    pub MaxBusCountPassed: u8,
    pub InvalidBusMSR: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AZCC_ROOT_BUS_LIST_EVENT {
    pub WheaEventLogEntry: u32,
    pub RootBusCount: u32,
    pub RootBuses: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_AZCC_SET_POISON_EVENT {
    pub WheaEventLogEntry: u32,
    pub Bus: u32,
    pub ReadSuccess: u8,
    pub WriteSuccess: u8,
    pub IsEnable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_OFFLINE_DONE_EVENT {
    pub WheaEventLogEntry: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_EDPC_ENABLED_EVENT {
    pub WheaEventLogEntry: u32,
    pub eDPCEnabled: u8,
    pub eDPCRecovEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SRAR_DETAIL_EVENT {
    pub WheaEventLogEntry: u32,
    pub RecoveryContextFlags: u32,
    pub RecoveryContextPa: u64,
    pub PageOfflineStatus: u32,
    pub KernelConsumerError: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_FAILED_ADD_DEFECT_LIST_EVENT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PLUGIN_DEFECT_LIST_FULL_EVENT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PLUGIN_DEFECT_LIST_UEFI_VAR_FAILED {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PLUGIN_DEFECT_LIST_CORRUPT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_SPURIOUS_AER_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrorSeverity: u32,
    pub ErrorHandlerType: u32,
    pub SpuriousErrorSourceId: u32,
    pub RootErrorCommand: u32,
    pub RootErrorStatus: u32,
    pub DeviceAssociationBitmap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PROMOTED_AER_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrorSeverity: u32,
    pub ErrorHandlerType: u32,
    pub ErrorSourceId: u32,
    pub RootErrorCommand: u32,
    pub RootErrorStatus: u32,
    pub DeviceAssociationBitmap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_DPC_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub ErrType: u32,
    pub Bus: u32,
    pub Device: u32,
    pub Function: u32,
    pub DeviceId: u16,
    pub VendorId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PCIE_OVERRIDE_INFO {
    pub WheaEventLogEntry: u32,
    pub Segment: u32,
    pub Bus: u32,
    pub Device: u32,
    pub Function: u32,
    pub ValidBits: u32,
    pub Reserved: [u32; 3],
    pub UncorrectableErrorMask: u32,
    pub UncorrectableErrorSeverity: u32,
    pub CorrectableErrorMask: u32,
    pub CapAndControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PCIE_READ_OVERRIDES_ERR {
    pub WheaEventLogEntry: u32,
    pub FailureReason: u32,
    pub FailureStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_PCIE_CONFIG_INFO {
    pub WheaEventLogEntry: u32,
    pub Segment: u32,
    pub Bus: u32,
    pub Device: u32,
    pub Function: u32,
    pub Offset: u32,
    pub Length: u32,
    pub Value: u64,
    pub Succeeded: u32,
    pub Reserved: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_PCIE_ADD_EVENT {
    pub WheaEventLogEntry: u32,
    pub Address: u32,
    pub Mask: u32,
    pub Updated: u8,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_MEMORY_ADD_OR_REMOVE_EVENT {
    pub WheaEventLogEntry: u32,
    pub SocketId: u32,
    pub ChannelId: u32,
    pub DimmSlot: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_PCIE_REMOVE_EVENT {
    pub WheaEventLogEntry: u32,
    pub Address: u32,
    pub Mask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_REGISTRY_CORRUPT_EVENT {
    pub WheaEventLogEntry: u32,
    pub ThrottleType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_ADD_ERR_SRC_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_THROTTLE_REG_DATA_IGNORED_EVENT {
    pub WheaEventLogEntry: u32,
    pub ThrottleType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_REGISTER_KEY_NOTIFICATION_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_MEMORY_THROTTLE_SUMMARY_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PI_CPU_BUSES_INIT_FAILED_EVENT {
    pub WheaEventLogEntry: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PI_TRACE_EVENT {
    pub WheaEventLogEntry: u32,
    pub Buffer: [u32; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SRAS_TABLE_NOT_FOUND {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SRAS_TABLE_ERROR {
    pub WheaEventLogEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PI_CPUID {
    pub WheaEventLogEntry: u32,
    pub CpuVendor: u32,
    pub CpuFamily: u32,
    pub CpuModel: u32,
    pub CpuStepping: u32,
    pub NumBanks: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ACPI_HEADER {
    pub Signature: u32,
    pub Length: u32,
    pub Revision: u32,
    pub Checksum: u32,
    pub OemId: [u32; 6],
    pub OemTableId: u64,
    pub OemRevision: u32,
    pub CreatorId: u32,
    pub CreatorRevision: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIGNAL_REG_VALUE {
    pub RegName: [u32; 1],
    pub MsrAddr: u32,
    pub Value: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_ACPI_RAS_SIGNAL_TABLE {
    pub Header: u32,
    pub NumberRecord: u32,
    pub Entries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SRAS_TABLE_ENTRIES_EVENT {
    pub WheaEventLogEntry: u32,
    pub LogNumber: u32,
    pub NumberSignals: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ROW_FAILURE_EVENT {
    pub WheaEventLogEntry: u32,
    pub LowOrderPage: usize,
    pub HighOrderPage: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PSHED_MEMORY_DETAILS_VALID_BITS {
    pub DdrVersion: u32,
    pub IsClosedPaged: u32,
    pub ColsPerRow: u32,
    pub PagesPerRow: u32,
    pub SocketCnt: u32,
    pub ChaOnSktCnt: u32,
    pub DimmSlotCnt: u32,
    pub SubchannelCnt: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PSHED_MEMORY_DETAILS {
    pub Version: u32,
    pub Vb: usize,
    pub DdrVersion: u32,
    pub IsClosedPaged: u8,
    pub ColsPerRow: u32,
    pub PagesPerRow: u32,
    pub SocketCnt: u32,
    pub ChaOnSktCnt: u32,
    pub DimmSlotCnt: u32,
    pub SubchannelCnt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_ROW_OFFLINE_EVENT {
    pub WheaEventLogEntry: u32,
    pub FirstPage: usize,
    pub LastPage: usize,
    pub Range: u32,
    pub Status: u32,
    pub ErrorReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEAP_BIT_OFFLINE_EVENT {
    pub WheaEventLogEntry: u32,
    pub Page: usize,
    pub Status: u32,
    pub ErrorReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_REGNOTIFY_POLICY_CHANGE_EVENT {
    pub WheaEventLogEntry: u32,
    pub PolicyName: [u32; 1],
    pub PolicyIndex: u32,
    pub PolicyValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_REGISTRY_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub RegErr: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_CRASHDUMP_EVENT_LOG_ENTRY_WITH_STATUS {
    pub WheaEventLogEntry: u32,
    pub SourceLocationId: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_CRASHDUMP_EVENT_LOG_ENTRY_ULONG1 {
    pub WheaEventLogEntry: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GAS_ERROR_EVENT {
    pub WheaEventLogEntry: u32,
    pub Error: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_SEL_RAW_EVENT {
    pub WheaEventLogEntry: u32,
    pub Payload: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GENERIC_ERROR {
    pub BlockStatus: u32,
    pub RawDataOffset: u32,
    pub RawDataLength: u32,
    pub DataLength: u32,
    pub ErrorSeverity: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GENERIC_ERROR_DATA_ENTRY_V1 {
    pub SectionType: u32,
    pub ErrorSeverity: u32,
    pub Revision: u32,
    pub ValidBits: u8,
    pub Flags: u8,
    pub ErrorDataLength: u32,
    pub FRUId: u32,
    pub FRUText: [u8; 20],
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_GENERIC_ERROR_DATA_ENTRY_V2 {
    pub SectionType: u32,
    pub ErrorSeverity: u32,
    pub Revision: u32,
    pub ValidBits: u8,
    pub Flags: u8,
    pub ErrorDataLength: u32,
    pub FRUId: u32,
    pub FRUText: [u8; 1],
    pub Timestamp: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_CONFIGURATION {
    pub Flags: u32,
    pub Correct: u32,
    pub Initialize: u32,
    pub CreateRecord: u32,
    pub Recover: u32,
    pub Uninitialize: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PACKET_LOG_DATA {
    pub LogData: [u8; 1],
    pub ExtraBytes: [u8; 1],
    pub BcParam3: usize,
    pub BcParam4: usize,
    pub LogDataLength: u32,
    pub LogTag: u16,
    pub Reserved: u16,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERROR_SOURCE_INFO {
    pub ErrorCount: u32,
    pub ErrorSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_ERROR_SOURCE_OVERRIDE_SETTINGS {
    pub Type: u32,
    pub MaxRawDataLength: u32,
    pub NumRecordsToPreallocate: u32,
    pub MaxSectionsPerRecord: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_RECOVERY_CONTEXT_PAGE_INFO {
    pub ComponentTag: u32,
    pub PageStatus: u32,
    pub ActionTaken: u32,
    pub NotifyFlags: u32,
    pub ImmediateSuccess: u8,
    pub Reserved: u32,
    pub ActionTakenAdditionalInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_RECOVERY_CONTEXT {
    pub Address: usize,
    pub Consumed: u8,
    pub ErrorCode: u32,
    pub ErrorIpValid: u8,
    pub RestartIpValid: u8,
    pub ClearPoison: u8,
    pub PmemErrInfo: usize,
    pub PartitionId: u64,
    pub VpIndex: u32,
    pub ErrorType: u32,
    pub PageCount: u32,
    pub PageInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_CALLBACKS {
    pub GetAllErrorSources: usize,
    pub Reserved: usize,
    pub GetErrorSourceInfo: usize,
    pub SetErrorSourceInfo: usize,
    pub EnableErrorSource: usize,
    pub DisableErrorSource: usize,
    pub WriteErrorRecord: usize,
    pub ReadErrorRecord: usize,
    pub ClearErrorRecord: usize,
    pub RetrieveErrorInfo: usize,
    pub FinalizeErrorRecord: usize,
    pub ClearErrorStatus: usize,
    pub AttemptRecovery: usize,
    pub GetInjectionCapabilities: usize,
    pub InjectError: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_REGISTRATION_PACKET_V1 {
    pub Length: u32,
    pub Version: u32,
    pub Context: usize,
    pub FunctionalAreaMask: u32,
    pub Reserved: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PSHED_PLUGIN_REGISTRATION_PACKET_V2 {
    pub Length: u32,
    pub Version: u32,
    pub Context: usize,
    pub FunctionalAreaMask: u32,
    pub Reserved: u32,
    pub Callbacks: usize,
    pub PluginHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PRM_ADDRESS_TRANSLATION_BUFFER_INTEL {
    pub SwSmi: u32,
    pub Command: u32,
    pub Status: u32,
    pub SystemAddress: u64,
    pub NmSystemAddress: u64,
    pub SpareSystemAddress: u64,
    pub DevicePhysicalAddress: u64,
    pub ProcessorSocketId: u64,
    pub MemoryControllerId: u64,
    pub NmMemoryControllerId: u64,
    pub TargetId: u64,
    pub LogicalChannelId: u64,
    pub ChannelAddress: u64,
    pub NmChannelAddress: u64,
    pub ChannelId: u64,
    pub NmChannelId: u64,
    pub RankAddress: u64,
    pub NmRankAddress: u64,
    pub PhysicalRankId: u64,
    pub NmPhysicalRankId: u64,
    pub DimmSlotId: u64,
    pub NmDimmSlotId: u64,
    pub DimmRankId: u64,
    pub Row: u64,
    pub NmRow: u64,
    pub Column: u64,
    pub NmColumn: u64,
    pub Bank: u64,
    pub NmBank: u64,
    pub BankGroup: u64,
    pub NmBankGroup: u64,
    pub LockStepRank: u64,
    pub LockStepPhysicalRank: u64,
    pub LockStepBank: u64,
    pub LockStepBankGroup: u64,
    pub ChipSelect: u64,
    pub NmChipSelect: u64,
    pub Node: u64,
    pub ChipId: u64,
    pub NmChipId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOC_SUBSYSTEM_FAILURE_DETAILS {
    pub SubsysType: u32,
    pub FirmwareVersion: u32,
    pub HardwareVersion: u32,
    pub UnifiedFailureRegionSize: u32,
    pub UnifiedFailureRegion: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HOOK_IP_ADDRESS {
    pub Type: u32,
    pub Address: u8,
    pub v4: u32,
    pub v6: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NLB_HOOK_IP_TUPLE {
    pub IP: u32,
    pub Port: u16,
    pub IP_1: u32,
    pub Port_1: u16,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEND_RECEIVE_HOOK_INFO {
    pub Version: u32,
    pub pAdapter: usize,
    pub IFIndex: u32,
    pub pPacket: usize,
    pub pMediaHeader: usize,
    pub cMediaHeaderLength: u32,
    pub pPayload: usize,
    pub cPayloadLength: u32,
    pub Flags: u32,
    pub Reserved: usize,
    pub NewTuple: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_HOOK_INFO {
    pub Version: u32,
    pub pAdapter: usize,
    pub IFIndex: u32,
    pub AddressFamily: u32,
    pub ServerIPAddress: usize,
    pub ServerPort: u16,
    pub ClientIPAddress: usize,
    pub ClientPort: u16,
    pub Protocol: u16,
    pub bReceiveContext: u8,
    pub Flags: u32,
    pub Reserved: usize,
    pub NewTuple: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLEAR_CLIENT_STICKINESS_HOOK_INFO {
    pub Version: u32,
    pub pAdapter: usize,
    pub PortRuleIndex: u32,
    pub BDATeaming: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NLB_PUBLIC_IOCTL_INPUT {
    pub MiniportName: [u32; 1],
    pub RequestType: u16,
    pub ClientIPAddress: u32,
    pub Protocol: u16,
    pub UsePortIndex: u8,
    pub PortIndex: u32,
    pub IP: u32,
    pub Port: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCMCIA_SOCKET_REQUEST {
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TUPLE_REQUEST {
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCMCIA_SOCKET_INFORMATION {
    pub Socket: u16,
    pub TupleCrc: u16,
    pub Manufacturer: [u8; 1],
    pub Identifier: [u8; 1],
    pub DriverName: [u8; 1],
    pub DeviceFunctionId: u8,
    pub Reserved: u8,
    pub CardInSocket: u8,
    pub CardEnabled: u8,
    pub ControllerType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCMCIA_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Context: usize,
    pub ModifyMemoryWindow: usize,
    pub SetVpp: usize,
    pub IsWriteProtected: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCMCIA_BUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadConfig: usize,
    pub WriteConfig: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDBUS_INTERFACE_PARAMETERS {
    pub Size: u16,
    pub SdioFlags: u16,
    pub TargetObject: usize,
    pub DeviceGeneratesInterrupts: u8,
    pub CallbackAtDpcLevel: u8,
    pub CallbackRoutine: usize,
    pub CallbackRoutineContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDBUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub InitializeInterface: usize,
    pub AcknowledgeInterrupt: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDBUS_REQUEST_PACKET {
    pub RequestFunction: u32,
    pub UserContext: [usize; 3],
    pub Information: usize,
    pub AsUCHAR: [u8; 16],
    pub AsULONG: [u32; 4],
    pub Type3: u32,
    pub ResponseLength: u8,
    pub Reserved: u8,
    pub Flags: u16,
    pub Property: u32,
    pub Buffer: usize,
    pub Length: u32,
    pub CmdDesc: u32,
    pub Argument: u32,
    pub Mdl: usize,
    pub Length_1: u32,
    pub EraseType: u32,
    pub StartBlock: u32,
    pub EndBlock: u32,
    pub Frequency: u32,
    pub IrpToHpi: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSENV_VARIABLE {
    pub VendorGuid: u32,
    pub VariableNameLength: u32,
    pub VariableName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSENV_VALUE {
    pub Attributes: u32,
    pub ValueLength: u32,
    pub ValueData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XVARIABLE_NAME_AND_VALUE {
    pub NextEntryOffset: u32,
    pub ValueOffset: u32,
    pub ValueLength: u32,
    pub Attributes: u32,
    pub VendorGuid: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XVARIABLE_NAME {
    pub NextEntryOffset: u32,
    pub VendorGuid: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSENV_VARIABLE_INFO {
    pub MaximumVariableStorageSize: u64,
    pub RemainingVariableStorageSize: u64,
    pub MaximumVariableSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOC_INITIALIZATION_HEADER {
    pub TableVersion: u32,
    pub TableSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIMER_FUNCTION_TABLE {
    pub Initialize: usize,
    pub QueryCounter: usize,
    pub AcknowledgeInterrupt: usize,
    pub ArmTimer: usize,
    pub Stop: usize,
    pub SetDivisor: usize,
    pub SetMessageInterruptRouting: usize,
    pub LosslessRateChange: usize,
    pub SetInterruptVector: usize,
    pub FixedStall: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIMER_INTERRUPT {
    pub LegacyIrq: i32,
    pub Gsi: i32,
    pub Polarity: u32,
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIMER_INITIALIZATION_BLOCK {
    pub Header: u32,
    pub FunctionTable: u32,
    pub InternalData: usize,
    pub InternalDataSize: u32,
    pub CounterBitWidth: u32,
    pub CounterFrequency: u64,
    pub MaxDivisor: u32,
    pub Capabilities: u32,
    pub Interrupt: u32,
    pub KnownType: u32,
    pub Identifier: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_REQUEST_LINE_BINDING_DESCRIPTION {
    pub RequestLine: u32,
    pub ChannelNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_SCATTER_GATHER_ELEMENT {
    pub Address: u64,
    pub Length: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_SCATTER_GATHER_LIST {
    pub NumberOfElements: u32,
    pub Reserved: usize,
    pub Elements: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_FUNCTION_TABLE {
    pub InitializeController: usize,
    pub ValidateRequestLineBinding: usize,
    pub QueryMaxFragments: usize,
    pub ProgramChannel: usize,
    pub ConfigureChannel: usize,
    pub FlushChannel: usize,
    pub HandleInterrupt: usize,
    pub ReadDmaCounter: usize,
    pub ReportCommonBuffer: usize,
    pub CancelTransfer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_INITIALIZATION_BLOCK {
    pub Header: u32,
    pub ControllerId: u32,
    pub ChannelCount: u32,
    pub CacheCoherent: u8,
    pub DmaAddressWidth: u32,
    pub SupportedPortWidths: u32,
    pub MinimumTransferUnit: u32,
    pub MinimumRequestLine: u32,
    pub MaximumRequestLine: u32,
    pub GeneratesInterrupt: u8,
    pub Gsi: i32,
    pub InterruptPolarity: u32,
    pub InterruptMode: u32,
    pub InternalData: usize,
    pub InternalDataSize: u32,
    pub Operations: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_CHANNEL_INITIALIZATION_BLOCK {
    pub Header: u32,
    pub ControllerId: u32,
    pub GeneratesInterrupt: u8,
    pub Gsi: i32,
    pub InterruptPolarity: u32,
    pub InterruptMode: u32,
    pub CommonBufferLength: u32,
    pub ChannelNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SID_IDENTIFIER_AUTHORITY {
    pub Value: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SID {
    pub Revision: u8,
    pub SubAuthorityCount: u8,
    pub IdentifierAuthority: u32,
    pub SubAuthority: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SID_AND_ATTRIBUTES {
    pub field_Sid: u32,
    pub Attributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SID_AND_ATTRIBUTES_HASH {
    pub SidCount: u32,
    pub SidAttr: usize,
    pub Hash: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATTRIBUTES_AND_SID {
    pub Attributes: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACE_HEADER {
    pub AceType: u8,
    pub AceFlags: u8,
    pub AceSize: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACCESS_ALLOWED_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACCESS_DENIED_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_AUDIT_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_ALARM_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_RESOURCE_ATTRIBUTE_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_SCOPED_POLICY_ID_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_MANDATORY_LABEL_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_PROCESS_TRUST_LABEL_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_ACCESS_FILTER_ACE {
    pub Header: u32,
    pub Mask: u32,
    pub SidStart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_DESCRIPTOR_RELATIVE {
    pub Revision: u8,
    pub Sbz1: u8,
    pub Control: u32,
    pub Owner: u32,
    pub Group: u32,
    pub Sacl: u32,
    pub Dacl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_DESCRIPTOR {
    pub Revision: u8,
    pub Sbz1: u8,
    pub Control: u32,
    pub Owner: usize,
    pub Group: usize,
    pub Sacl: usize,
    pub Dacl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_OBJECT_AI_PARAMS {
    pub Size: u32,
    pub ConstraintMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_TYPE_LIST {
    pub Level: u16,
    pub Sbz: u16,
    pub field_ObjectType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACCESS_REASONS {
    pub Data: [u32; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_SECURITY_DESCRIPTOR {
    pub Size: u32,
    pub Flags: u32,
    pub SecurityDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ACCESS_REQUEST {
    pub Size: u32,
    pub SeSecurityDescriptor: usize,
    pub DesiredAccess: u32,
    pub PreviouslyGrantedAccess: u32,
    pub PrincipalSelfSid: usize,
    pub GenericMapping: usize,
    pub ObjectTypeListCount: u32,
    pub ObjectTypeList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_ACCESS_REPLY {
    pub Size: u32,
    pub ResultListCount: u32,
    pub GrantedAccess: usize,
    pub AccessStatus: usize,
    pub AccessReason: usize,
    pub Privileges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_AUDIT_INFO {
    pub Size: u32,
    pub AuditType: u32,
    pub AuditOperation: u32,
    pub AuditFlags: u32,
    pub SubsystemName: u32,
    pub ObjectTypeName: u32,
    pub ObjectName: u32,
    pub HandleId: usize,
    pub TransactionId: usize,
    pub OperationId: usize,
    pub ObjectCreation: u8,
    pub GenerateOnClose: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_USER {
    pub User: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_TOKEN_USER {
    pub TokenUser: u32,
    pub User: u32,
    pub Sid: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_GROUPS {
    pub GroupCount: u32,
    pub Groups: [u32; 1],
    pub Groups_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_PRIVILEGES {
    pub PrivilegeCount: u32,
    pub Privileges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_OWNER {
    pub Owner: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_PRIMARY_GROUP {
    pub PrimaryGroup: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_DEFAULT_DACL {
    pub DefaultDacl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_USER_CLAIMS {
    pub UserClaims: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_DEVICE_CLAIMS {
    pub DeviceClaims: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_GROUPS_AND_PRIVILEGES {
    pub SidCount: u32,
    pub SidLength: u32,
    pub Sids: usize,
    pub RestrictedSidCount: u32,
    pub RestrictedSidLength: u32,
    pub RestrictedSids: usize,
    pub PrivilegeCount: u32,
    pub PrivilegeLength: u32,
    pub Privileges: usize,
    pub AuthenticationId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_LINKED_TOKEN {
    pub LinkedToken: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_ELEVATION {
    pub TokenIsElevated: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_MANDATORY_LABEL {
    pub Label: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_MANDATORY_POLICY {
    pub Policy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_ACCESS_INFORMATION {
    pub SidHash: usize,
    pub RestrictedSidHash: usize,
    pub Privileges: usize,
    pub AuthenticationId: u32,
    pub TokenType: u32,
    pub ImpersonationLevel: u32,
    pub MandatoryPolicy: u32,
    pub Flags: u32,
    pub AppContainerNumber: u32,
    pub PackageSid: usize,
    pub CapabilitiesHash: usize,
    pub TrustLevelSid: usize,
    pub SecurityAttributes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_LOGGING_INFORMATION {
    pub TokenType: u32,
    pub TokenElevation: u32,
    pub TokenElevationType: u32,
    pub ImpersonationLevel: u32,
    pub IntegrityLevel: u32,
    pub User: u32,
    pub TrustLevelSid: usize,
    pub SessionId: u32,
    pub AppContainerNumber: u32,
    pub AuthenticationId: u32,
    pub GroupCount: u32,
    pub GroupsLength: u32,
    pub Groups: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_AUDIT_POLICY {
    pub PerUserPolicy: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_SOURCE {
    pub SourceName: [i8; 1],
    pub SourceIdentifier: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_STATISTICS {
    pub TokenId: u32,
    pub AuthenticationId: u32,
    pub ExpirationTime: i64,
    pub TokenType: u32,
    pub ImpersonationLevel: u32,
    pub DynamicCharged: u32,
    pub DynamicAvailable: u32,
    pub GroupCount: u32,
    pub PrivilegeCount: u32,
    pub ModifiedId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_CONTROL {
    pub TokenId: u32,
    pub AuthenticationId: u32,
    pub ModifiedId: u32,
    pub TokenSource: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_ORIGIN {
    pub OriginatingLogonSession: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_APPCONTAINER_INFORMATION {
    pub TokenAppContainer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_SID_INFORMATION {
    pub Sid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TOKEN_BNO_ISOLATION_INFORMATION {
    pub IsolationPrefix: usize,
    pub IsolationEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLAIM_SECURITY_ATTRIBUTE_FQBN_VALUE {
    pub Version: u32,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLAIM_SECURITY_ATTRIBUTE_OCTET_STRING_VALUE {
    pub pValue: usize,
    pub ValueLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLAIM_SECURITY_ATTRIBUTE_V1 {
    pub Name: usize,
    pub ValueType: u16,
    pub Reserved: u16,
    pub Flags: u32,
    pub ValueCount: u32,
    pub pInt64: usize,
    pub pUint64: usize,
    pub field_ppString: usize,
    pub pFqbn: usize,
    pub pOctetString: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLAIM_SECURITY_ATTRIBUTE_RELATIVE_V1 {
    pub Name: u32,
    pub ValueType: u16,
    pub Reserved: u16,
    pub Flags: u32,
    pub ValueCount: u32,
    pub pInt64: [u32; 1],
    pub pUint64: [u32; 1],
    pub ppString: [u32; 1],
    pub pFqbn: [u32; 1],
    pub pOctetString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLAIM_SECURITY_ATTRIBUTES_INFORMATION {
    pub Version: u16,
    pub Reserved: u16,
    pub AttributeCount: u32,
    pub pAttributeV1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_HEAP_PARAMETERS {
    pub Length: u32,
    pub SegmentReserve: usize,
    pub SegmentCommit: usize,
    pub DeCommitFreeBlockThreshold: usize,
    pub DeCommitTotalFreeThreshold: usize,
    pub MaximumAllocationSize: usize,
    pub VirtualMemoryThreshold: usize,
    pub InitialCommit: usize,
    pub InitialReserve: usize,
    pub CommitRoutine: usize,
    pub Reserved: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_SEGMENT_HEAP_VA_CALLBACKS {
    pub CallbackContext: usize,
    pub AllocateVirtualMemory: usize,
    pub FreeVirtualMemory: usize,
    pub QueryVirtualMemory: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_SEGMENT_HEAP_MEMORY_SOURCE {
    pub Flags: u32,
    pub MemoryTypeMask: u32,
    pub NumaNode: u32,
    pub PartitionHandle: usize,
    pub field_Callbacks: usize,
    pub Reserved: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_SEGMENT_HEAP_PARAMETERS {
    pub Version: u16,
    pub Size: u16,
    pub Flags: u32,
    pub MemorySource: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_HEAP_MEMORY_LIMIT_DATA {
    pub CommitLimitBytes: usize,
    pub CommitLimitFailureCode: usize,
    pub MaxAllocationSizeBytes: usize,
    pub AllocationLimitFailureCode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_HEAP_MEMORY_LIMIT_INFO {
    pub Version: u32,
    pub Data: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERATE_NAME_CONTEXT {
    pub Checksum: u16,
    pub ChecksumInserted: u8,
    pub NameLength: u32,
    pub NameBuffer: [u32; 8],
    pub ExtensionLength: u32,
    pub ExtensionBuffer: [u32; 4],
    pub LastIndexValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PREFIX_TABLE_ENTRY {
    pub NodeTypeCode: u32,
    pub NameLength: u32,
    pub field_NextPrefixTree: u32,
    pub Links: u32,
    pub Prefix: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PREFIX_TABLE {
    pub NodeTypeCode: u32,
    pub NameLength: u32,
    pub NextPrefixTree: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNICODE_PREFIX_TABLE_ENTRY {
    pub NodeTypeCode: u32,
    pub NameLength: u32,
    pub field_NextPrefixTree: u32,
    pub field_CaseMatch: u32,
    pub Links: u32,
    pub Prefix: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNICODE_PREFIX_TABLE {
    pub NodeTypeCode: u32,
    pub NameLength: u32,
    pub NextPrefixTree: usize,
    pub LastNextEntry: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMPRESSED_DATA_INFO {
    pub CompressionFormatAndEngine: u16,
    pub CompressionUnitShift: u8,
    pub ChunkShift: u8,
    pub ClusterShift: u8,
    pub Reserved: u8,
    pub NumberOfChunks: u16,
    pub CompressedChunkSizes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_INTERACTIVE_LOGON {
    pub MessageType: u32,
    pub LogonDomainName: u32,
    pub UserName: u32,
    pub Password: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_INTERACTIVE_PROFILE {
    pub MessageType: u32,
    pub LogonCount: u16,
    pub BadPasswordCount: u16,
    pub LogonTime: i64,
    pub LogoffTime: i64,
    pub KickOffTime: i64,
    pub PasswordLastSet: i64,
    pub PasswordCanChange: i64,
    pub PasswordMustChange: i64,
    pub LogonScript: u32,
    pub HomeDirectory: u32,
    pub FullName: u32,
    pub ProfilePath: u32,
    pub HomeDirectoryDrive: u32,
    pub LogonServer: u32,
    pub UserFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_LM20_LOGON {
    pub MessageType: u32,
    pub LogonDomainName: u32,
    pub UserName: u32,
    pub Workstation: u32,
    pub ChallengeToClient: [u8; 1],
    pub CaseSensitiveChallengeResponse: u32,
    pub CaseInsensitiveChallengeResponse: u32,
    pub ParameterControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_SUBAUTH_LOGON {
    pub MessageType: u32,
    pub LogonDomainName: u32,
    pub UserName: u32,
    pub Workstation: u32,
    pub ChallengeToClient: [u8; 1],
    pub AuthenticationInfo1: u32,
    pub AuthenticationInfo2: u32,
    pub ParameterControl: u32,
    pub SubAuthPackageId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_S4U_LOGON {
    pub MessageType: u32,
    pub Flags: u32,
    pub UserPrincipalName: u32,
    pub DomainName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_LM20_LOGON_PROFILE {
    pub MessageType: u32,
    pub KickOffTime: i64,
    pub LogoffTime: i64,
    pub UserFlags: u32,
    pub UserSessionKey: [u8; 1],
    pub LogonDomainName: u32,
    pub LanmanSessionKey: [u8; 1],
    pub LogonServer: u32,
    pub UserParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_CREDENTIAL_KEY {
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_SUPPLEMENTAL_CREDENTIAL {
    pub Version: u32,
    pub Flags: u32,
    pub LmPassword: [u8; 1],
    pub NtPassword: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_SUPPLEMENTAL_CREDENTIAL_V2 {
    pub Version: u32,
    pub Flags: u32,
    pub NtPassword: [u8; 1],
    pub CredentialKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_SUPPLEMENTAL_CREDENTIAL_V3 {
    pub Version: u32,
    pub Flags: u32,
    pub CredentialKeyType: u32,
    pub NtPassword: [u8; 1],
    pub CredentialKey: u32,
    pub ShaPassword: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_IUM_SUPPLEMENTAL_CREDENTIAL {
    pub Version: u32,
    pub EncryptedCredsSize: u32,
    pub EncryptedCreds: [u8; 1],
    pub EncryptedCreds_1: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_REMOTE_SUPPLEMENTAL_CREDENTIAL {
    pub Version: u32,
    pub Flags: u32,
    pub CredentialKey: u32,
    pub CredentialKeyType: u32,
    pub EncryptedCredsSize: u32,
    pub EncryptedCreds: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_NTLM3_RESPONSE {
    pub Response: [u8; 1],
    pub RespType: u8,
    pub HiRespType: u8,
    pub Flags: u16,
    pub MsgWord: u32,
    pub TimeStamp: u64,
    pub ChallengeFromClient: [u8; 1],
    pub AvPairsOff: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_AV_PAIR {
    pub AvId: u16,
    pub AvLen: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_LM20_CHALLENGE_REQUEST {
    pub MessageType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_LM20_CHALLENGE_RESPONSE {
    pub MessageType: u32,
    pub ChallengeToClient: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_GETCHALLENRESP_REQUEST_V1 {
    pub MessageType: u32,
    pub ParameterControl: u32,
    pub LogonId: u32,
    pub Password: u32,
    pub ChallengeToClient: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_GETCHALLENRESP_REQUEST {
    pub MessageType: u32,
    pub ParameterControl: u32,
    pub LogonId: u32,
    pub Password: u32,
    pub ChallengeToClient: [u8; 1],
    pub UserName: u32,
    pub LogonDomainName: u32,
    pub ServerName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_GETCHALLENRESP_RESPONSE {
    pub MessageType: u32,
    pub CaseSensitiveChallengeResponse: u32,
    pub CaseInsensitiveChallengeResponse: u32,
    pub UserName: u32,
    pub LogonDomainName: u32,
    pub UserSessionKey: [u8; 1],
    pub LanmanSessionKey: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_ENUMUSERS_REQUEST {
    pub MessageType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_ENUMUSERS_RESPONSE {
    pub MessageType: u32,
    pub NumberOfLoggedOnUsers: u32,
    pub LogonIds: usize,
    pub EnumHandles: *mut u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_GETUSERINFO_REQUEST {
    pub MessageType: u32,
    pub LogonId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSV1_0_GETUSERINFO_RESPONSE {
    pub MessageType: u32,
    pub UserSid: usize,
    pub UserName: u32,
    pub LogonDomainName: u32,
    pub LogonServer: u32,
    pub LogonType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_128 {
    pub Identifier: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NOTIFY_INFORMATION {
    pub NextEntryOffset: u32,
    pub Action: u32,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NOTIFY_EXTENDED_INFORMATION {
    pub NextEntryOffset: u32,
    pub Action: u32,
    pub CreationTime: i64,
    pub LastModificationTime: i64,
    pub LastChangeTime: i64,
    pub LastAccessTime: i64,
    pub AllocatedLength: i64,
    pub FileSize: i64,
    pub FileAttributes: u32,
    pub ReparsePointTag: u32,
    pub EaSize: u32,
    pub FileId: i64,
    pub ParentFileId: i64,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NOTIFY_FULL_INFORMATION {
    pub NextEntryOffset: u32,
    pub Action: u32,
    pub CreationTime: i64,
    pub LastModificationTime: i64,
    pub LastChangeTime: i64,
    pub LastAccessTime: i64,
    pub AllocatedLength: i64,
    pub FileSize: i64,
    pub FileAttributes: u32,
    pub ReparsePointTag: u32,
    pub EaSize: u32,
    pub FileId: i64,
    pub ParentFileId: i64,
    pub FileNameLength: u16,
    pub FileNameFlags: u8,
    pub Reserved: u8,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_INFORMATION_DEFINITION {
    pub Class: u32,
    pub NextEntryOffset: u32,
    pub FileNameOffset: u32,
    pub FileNameLengthOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_DIRECTORY_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FULL_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_FULL_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub FileId: i64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_BOTH_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ShortNameLength: u32,
    pub ShortName: [u32; 12],
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_BOTH_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ShortNameLength: u32,
    pub ShortName: [u32; 12],
    pub FileId: i64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NAMES_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_GLOBAL_TX_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub FileId: i64,
    pub LockingTransactionId: u32,
    pub TxInfoFlags: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_EXTD_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_EXTD_BOTH_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: u32,
    pub ShortNameLength: u32,
    pub ShortName: [u32; 12],
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_64_EXTD_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: i64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_64_EXTD_BOTH_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: i64,
    pub ShortNameLength: u32,
    pub ShortName: [u32; 12],
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_ALL_EXTD_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: i64,
    pub FileId128: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_ALL_EXTD_BOTH_DIR_INFORMATION {
    pub NextEntryOffset: u32,
    pub FileIndex: u32,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub EndOfFile: i64,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub FileNameLength: u32,
    pub EaSize: u32,
    pub ReparsePointTag: u32,
    pub FileId: i64,
    pub FileId128: u32,
    pub ShortNameLength: u32,
    pub ShortName: [u32; 12],
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_OBJECTID_INFORMATION {
    pub FileReference: i64,
    pub ObjectId: [u8; 16],
    pub BirthVolumeId: [u8; 16],
    pub BirthObjectId: [u8; 16],
    pub DomainId: [u8; 16],
    pub ExtendedInfo: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_INTERNAL_INFORMATION {
    pub IndexNumber: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ID_INFORMATION {
    pub VolumeSerialNumber: u64,
    pub FileId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_EA_INFORMATION {
    pub EaSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ACCESS_INFORMATION {
    pub AccessFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MODE_INFORMATION {
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ALL_INFORMATION {
    pub BasicInformation: u32,
    pub StandardInformation: u32,
    pub InternalInformation: u32,
    pub EaInformation: u32,
    pub AccessInformation: u32,
    pub PositionInformation: u32,
    pub ModeInformation: u32,
    pub AlignmentInformation: u32,
    pub NameInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STAT_INFORMATION {
    pub FileId: i64,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub FileAttributes: u32,
    pub ReparseTag: u32,
    pub NumberOfLinks: u32,
    pub EffectiveAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STAT_LX_INFORMATION {
    pub FileId: i64,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub FileAttributes: u32,
    pub ReparseTag: u32,
    pub NumberOfLinks: u32,
    pub EffectiveAccess: u32,
    pub LxFlags: u32,
    pub LxUid: u32,
    pub LxGid: u32,
    pub LxMode: u32,
    pub LxDeviceIdMajor: u32,
    pub LxDeviceIdMinor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STAT_BASIC_INFORMATION {
    pub FileId: i64,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub FileAttributes: u32,
    pub ReparseTag: u32,
    pub NumberOfLinks: u32,
    pub DeviceType: u32,
    pub DeviceCharacteristics: u32,
    pub Reserved: u32,
    pub VolumeSerialNumber: i64,
    pub FileId128: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_CASE_SENSITIVE_INFORMATION {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_KNOWN_FOLDER_INFORMATION {
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ALLOCATION_INFORMATION {
    pub AllocationSize: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_COMPRESSION_INFORMATION {
    pub CompressedFileSize: i64,
    pub CompressionFormat: u16,
    pub CompressionUnitShift: u8,
    pub ChunkShift: u8,
    pub ClusterShift: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LINK_INFORMATION {
    pub ReplaceIfExists: u8,
    pub Flags: u32,
    pub ReplaceIfExists_1: u8,
    pub RootDirectory: usize,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MOVE_CLUSTER_INFORMATION {
    pub ClusterCount: u32,
    pub RootDirectory: usize,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_RENAME_INFORMATION {
    pub ReplaceIfExists: u8,
    pub Flags: u32,
    pub ReplaceIfExists_1: u8,
    pub RootDirectory: usize,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STREAM_INFORMATION {
    pub NextEntryOffset: u32,
    pub StreamNameLength: u32,
    pub StreamSize: i64,
    pub StreamAllocationSize: i64,
    pub StreamName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_TRACKING_INFORMATION {
    pub DestinationFile: usize,
    pub ObjectInformationLength: u32,
    pub ObjectInformation: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_COMPLETION_INFORMATION {
    pub Port: usize,
    pub Key: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_INFORMATION {
    pub ReadMode: u32,
    pub CompletionMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_LOCAL_INFORMATION {
    pub NamedPipeType: u32,
    pub NamedPipeConfiguration: u32,
    pub MaximumInstances: u32,
    pub CurrentInstances: u32,
    pub InboundQuota: u32,
    pub ReadDataAvailable: u32,
    pub OutboundQuota: u32,
    pub WriteQuotaAvailable: u32,
    pub NamedPipeState: u32,
    pub NamedPipeEnd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_REMOTE_INFORMATION {
    pub CollectDataTime: i64,
    pub MaximumCollectionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MAILSLOT_QUERY_INFORMATION {
    pub MaximumMessageSize: u32,
    pub MailslotQuota: u32,
    pub NextMessageSize: u32,
    pub MessagesAvailable: u32,
    pub ReadTimeout: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MAILSLOT_SET_INFORMATION {
    pub ReadTimeout: *mut i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REPARSE_POINT_INFORMATION {
    pub FileReference: i64,
    pub Tag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LINK_ENTRY_INFORMATION {
    pub NextEntryOffset: u32,
    pub ParentFileId: i64,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LINKS_INFORMATION {
    pub BytesNeeded: u32,
    pub EntriesReturned: u32,
    pub Entry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LINK_ENTRY_FULL_ID_INFORMATION {
    pub NextEntryOffset: u32,
    pub ParentFileId: u32,
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LINKS_FULL_ID_INFORMATION {
    pub BytesNeeded: u32,
    pub EntriesReturned: u32,
    pub Entry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NETWORK_PHYSICAL_NAME_INFORMATION {
    pub FileNameLength: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STANDARD_LINK_INFORMATION {
    pub NumberOfAccessibleLinks: u32,
    pub TotalNumberOfLinks: u32,
    pub DeletePending: u8,
    pub Directory: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STREAM_RESERVATION_INFORMATION {
    pub TrackedReservation: i64,
    pub EnforcedReservation: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MUP_PROVIDER_INFORMATION {
    pub Level: u32,
    pub Buffer: usize,
    pub BufferSize: *mut u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_GET_EA_INFORMATION {
    pub NextEntryOffset: u32,
    pub EaNameLength: u8,
    pub EaName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REMOTE_PROTOCOL_INFORMATION {
    pub StructureVersion: u16,
    pub StructureSize: u16,
    pub Protocol: u32,
    pub ProtocolMajorVersion: u16,
    pub ProtocolMinorVersion: u16,
    pub ProtocolRevision: u16,
    pub Reserved: u16,
    pub Flags: u32,
    pub Reserved_1: [u32; 8],
    pub Reserved_2: [u32; 16],
    pub Capabilities: u32,
    pub Capabilities_1: u32,
    pub ShareFlags: u32,
    pub CachingFlags: u32,
    pub ShareType: u8,
    pub Reserved0: [u8; 3],
    pub Reserved1: u32,
    pub Reserved_3: [u32; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_GET_QUOTA_INFORMATION {
    pub NextEntryOffset: u32,
    pub SidLength: u32,
    pub Sid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_QUOTA_INFORMATION {
    pub NextEntryOffset: u32,
    pub SidLength: u32,
    pub ChangeTime: i64,
    pub QuotaUsed: i64,
    pub QuotaThreshold: i64,
    pub QuotaLimit: i64,
    pub Sid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_VOLUME_NAME_INFORMATION {
    pub DeviceNameLength: u32,
    pub DeviceName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_ATTRIBUTE_INFORMATION {
    pub FileSystemAttributes: u32,
    pub MaximumComponentNameLength: i32,
    pub FileSystemNameLength: u32,
    pub FileSystemName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_DRIVER_PATH_INFORMATION {
    pub DriverInPath: u8,
    pub DriverNameLength: u32,
    pub DriverName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_VOLUME_FLAGS_INFORMATION {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_CONTROL_INFORMATION {
    pub FreeSpaceStartFiltering: i64,
    pub FreeSpaceThreshold: i64,
    pub FreeSpaceStopFiltering: i64,
    pub DefaultQuotaThreshold: i64,
    pub DefaultQuotaLimit: i64,
    pub FileSystemControlFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_DATA_COPY_INFORMATION {
    pub NumberOfCopies: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_GUID_INFORMATION {
    pub FsGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_END_OF_FILE_INFORMATION_EX {
    pub EndOfFile: i64,
    pub PagingFileSizeInMM: i64,
    pub PagingFileMaxSize: i64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PATHNAME_BUFFER {
    pub PathNameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_FAT_BPB_BUFFER {
    pub First0x24BytesOfBootSector: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOVE_FILE_DATA32 {
    pub FileHandle: u32,
    pub StartingVcn: i64,
    pub StartingLcn: i64,
    pub ClusterCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MARK_HANDLE_INFO {
    pub UsnSourceInfo: u32,
    pub CopyNumber: u32,
    pub UsnSourceInfo_1: u32,
    pub VolumeHandle: usize,
    pub HandleInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MARK_HANDLE_INFO32 {
    pub UsnSourceInfo: u32,
    pub CopyNumber: u32,
    pub UsnSourceInfo_1: u32,
    pub VolumeHandle: u32,
    pub HandleInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PREFETCH {
    pub Type: u32,
    pub Count: u32,
    pub Prefetch: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PREFETCH_EX {
    pub Type: u32,
    pub Count: u32,
    pub Context: usize,
    pub Prefetch: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILESYSTEM_STATISTICS {
    pub FileSystemType: u16,
    pub Version: u16,
    pub SizeOfCompleteStructure: u32,
    pub UserFileReads: u32,
    pub UserFileReadBytes: u32,
    pub UserDiskReads: u32,
    pub UserFileWrites: u32,
    pub UserFileWriteBytes: u32,
    pub UserDiskWrites: u32,
    pub MetaDataReads: u32,
    pub MetaDataReadBytes: u32,
    pub MetaDataDiskReads: u32,
    pub MetaDataWrites: u32,
    pub MetaDataWriteBytes: u32,
    pub MetaDataDiskWrites: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAT_STATISTICS {
    pub CreateHits: u32,
    pub SuccessfulCreates: u32,
    pub FailedCreates: u32,
    pub NonCachedReads: u32,
    pub NonCachedReadBytes: u32,
    pub NonCachedWrites: u32,
    pub NonCachedWriteBytes: u32,
    pub NonCachedDiskReads: u32,
    pub NonCachedDiskWrites: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXFAT_STATISTICS {
    pub CreateHits: u32,
    pub SuccessfulCreates: u32,
    pub FailedCreates: u32,
    pub NonCachedReads: u32,
    pub NonCachedReadBytes: u32,
    pub NonCachedWrites: u32,
    pub NonCachedWriteBytes: u32,
    pub NonCachedDiskReads: u32,
    pub NonCachedDiskWrites: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NTFS_STATISTICS {
    pub LogFileFullExceptions: u32,
    pub OtherExceptions: u32,
    pub MftReads: u32,
    pub MftReadBytes: u32,
    pub MftWrites: u32,
    pub MftWriteBytes: u32,
    pub Write: u16,
    pub Create: u16,
    pub SetInfo: u16,
    pub Flush: u16,
    pub MftWritesFlushForLogFileFull: u16,
    pub MftWritesLazyWriter: u16,
    pub MftWritesUserRequest: u16,
    pub Mft2Writes: u32,
    pub Mft2WriteBytes: u32,
    pub Write_1: u16,
    pub Create_1: u16,
    pub SetInfo_1: u16,
    pub Flush_1: u16,
    pub Mft2WritesFlushForLogFileFull: u16,
    pub Mft2WritesLazyWriter: u16,
    pub Mft2WritesUserRequest: u16,
    pub RootIndexReads: u32,
    pub RootIndexReadBytes: u32,
    pub RootIndexWrites: u32,
    pub RootIndexWriteBytes: u32,
    pub BitmapReads: u32,
    pub BitmapReadBytes: u32,
    pub BitmapWrites: u32,
    pub BitmapWriteBytes: u32,
    pub BitmapWritesFlushForLogFileFull: u16,
    pub BitmapWritesLazyWriter: u16,
    pub BitmapWritesUserRequest: u16,
    pub Write_2: u16,
    pub Create_2: u16,
    pub SetInfo_2: u16,
    pub MftBitmapReads: u32,
    pub MftBitmapReadBytes: u32,
    pub MftBitmapWrites: u32,
    pub MftBitmapWriteBytes: u32,
    pub MftBitmapWritesFlushForLogFileFull: u16,
    pub MftBitmapWritesLazyWriter: u16,
    pub MftBitmapWritesUserRequest: u16,
    pub Write_3: u16,
    pub Create_3: u16,
    pub SetInfo_3: u16,
    pub Flush_2: u16,
    pub UserIndexReads: u32,
    pub UserIndexReadBytes: u32,
    pub UserIndexWrites: u32,
    pub UserIndexWriteBytes: u32,
    pub LogFileReads: u32,
    pub LogFileReadBytes: u32,
    pub LogFileWrites: u32,
    pub LogFileWriteBytes: u32,
    pub Calls: u32,
    pub Clusters: u32,
    pub Hints: u32,
    pub RunsReturned: u32,
    pub HintsHonored: u32,
    pub HintsClusters: u32,
    pub Cache: u32,
    pub CacheClusters: u32,
    pub CacheMiss: u32,
    pub CacheMissClusters: u32,
    pub DiskResourcesExhausted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILESYSTEM_STATISTICS_EX {
    pub FileSystemType: u16,
    pub Version: u16,
    pub SizeOfCompleteStructure: u32,
    pub UserFileReads: u64,
    pub UserFileReadBytes: u64,
    pub UserDiskReads: u64,
    pub UserFileWrites: u64,
    pub UserFileWriteBytes: u64,
    pub UserDiskWrites: u64,
    pub MetaDataReads: u64,
    pub MetaDataReadBytes: u64,
    pub MetaDataDiskReads: u64,
    pub MetaDataWrites: u64,
    pub MetaDataWriteBytes: u64,
    pub MetaDataDiskWrites: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NTFS_STATISTICS_EX {
    pub LogFileFullExceptions: u32,
    pub OtherExceptions: u32,
    pub MftReads: u64,
    pub MftReadBytes: u64,
    pub MftWrites: u64,
    pub MftWriteBytes: u64,
    pub Write: u32,
    pub Create: u32,
    pub SetInfo: u32,
    pub Flush: u32,
    pub MftWritesFlushForLogFileFull: u32,
    pub MftWritesLazyWriter: u32,
    pub MftWritesUserRequest: u32,
    pub Mft2Writes: u64,
    pub Mft2WriteBytes: u64,
    pub Write_1: u32,
    pub Create_1: u32,
    pub SetInfo_1: u32,
    pub Flush_1: u32,
    pub Mft2WritesFlushForLogFileFull: u32,
    pub Mft2WritesLazyWriter: u32,
    pub Mft2WritesUserRequest: u32,
    pub RootIndexReads: u64,
    pub RootIndexReadBytes: u64,
    pub RootIndexWrites: u64,
    pub RootIndexWriteBytes: u64,
    pub BitmapReads: u64,
    pub BitmapReadBytes: u64,
    pub BitmapWrites: u64,
    pub BitmapWriteBytes: u64,
    pub BitmapWritesFlushForLogFileFull: u32,
    pub BitmapWritesLazyWriter: u32,
    pub BitmapWritesUserRequest: u32,
    pub Write_2: u32,
    pub Create_2: u32,
    pub SetInfo_2: u32,
    pub Flush_2: u32,
    pub MftBitmapReads: u64,
    pub MftBitmapReadBytes: u64,
    pub MftBitmapWrites: u64,
    pub MftBitmapWriteBytes: u64,
    pub MftBitmapWritesFlushForLogFileFull: u32,
    pub MftBitmapWritesLazyWriter: u32,
    pub MftBitmapWritesUserRequest: u32,
    pub Write_3: u32,
    pub Create_3: u32,
    pub SetInfo_3: u32,
    pub Flush_3: u32,
    pub UserIndexReads: u64,
    pub UserIndexReadBytes: u64,
    pub UserIndexWrites: u64,
    pub UserIndexWriteBytes: u64,
    pub LogFileReads: u64,
    pub LogFileReadBytes: u64,
    pub LogFileWrites: u64,
    pub LogFileWriteBytes: u64,
    pub Calls: u32,
    pub RunsReturned: u32,
    pub Hints: u32,
    pub HintsHonored: u32,
    pub Cache: u32,
    pub CacheMiss: u32,
    pub Clusters: u64,
    pub HintsClusters: u64,
    pub CacheClusters: u64,
    pub CacheMissClusters: u64,
    pub DiskResourcesExhausted: u32,
    pub VolumeTrimCount: u64,
    pub VolumeTrimTime: u64,
    pub VolumeTrimByteCount: u64,
    pub FileLevelTrimCount: u64,
    pub FileLevelTrimTime: u64,
    pub FileLevelTrimByteCount: u64,
    pub VolumeTrimSkippedCount: u64,
    pub VolumeTrimSkippedByteCount: u64,
    pub NtfsFillStatInfoFromMftRecordCalledCount: u64,
    pub NtfsFillStatInfoFromMftRecordBailedBecauseOfAttributeListCount: u64,
    pub NtfsFillStatInfoFromMftRecordBailedBecauseOfNonResReparsePointCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_OBJECTID_BUFFER {
    pub ObjectId: [u8; 16],
    pub BirthVolumeId: [u8; 16],
    pub BirthObjectId: [u8; 16],
    pub DomainId: [u8; 16],
    pub ExtendedInfo: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_SET_SPARSE_BUFFER {
    pub SetSparse: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ZERO_DATA_INFORMATION {
    pub FileOffset: i64,
    pub BeyondFinalZero: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ZERO_DATA_INFORMATION_EX {
    pub FileOffset: i64,
    pub BeyondFinalZero: i64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_ALLOCATED_RANGE_BUFFER {
    pub FileOffset: i64,
    pub Length: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENCRYPTION_BUFFER {
    pub EncryptionOperation: u32,
    pub Private: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DECRYPTION_STATUS_BUFFER {
    pub NoEncryptedStreams: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_RAW_ENCRYPTED_DATA {
    pub FileOffset: i64,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENCRYPTED_DATA_INFO {
    pub StartingFileOffset: u64,
    pub OutputBufferOffset: u32,
    pub BytesWithinFileSize: u32,
    pub BytesWithinValidDataLength: u32,
    pub CompressionFormat: u16,
    pub DataUnitShift: u8,
    pub ChunkShift: u8,
    pub ClusterShift: u8,
    pub EncryptionFormat: u8,
    pub NumberOfDataBlocks: u16,
    pub DataBlockSize: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXTENDED_ENCRYPTED_DATA_INFO {
    pub ExtendedCode: u32,
    pub Length: u32,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLEX_READ_DATA_REQUEST {
    pub ByteOffset: i64,
    pub ByteLength: u32,
    pub PlexNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SI_COPYFILE {
    pub SourceFileNameLength: u32,
    pub DestinationFileNameLength: u32,
    pub Flags: u32,
    pub FileNameBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MAKE_COMPATIBLE_BUFFER {
    pub CloseDisc: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_SET_DEFECT_MGMT_BUFFER {
    pub Disable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_QUERY_SPARING_BUFFER {
    pub SparingUnitBytes: u32,
    pub SoftwareSparing: u8,
    pub TotalSpareBlocks: u32,
    pub FreeSpareBlocks: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_QUERY_ON_DISK_VOL_INFO_BUFFER {
    pub DirectoryCount: i64,
    pub FileCount: i64,
    pub FsFormatMajVersion: u16,
    pub FsFormatMinVersion: u16,
    pub FsFormatName: [u32; 12],
    pub FormatTime: i64,
    pub LastUpdateTime: i64,
    pub CopyrightInfo: [u32; 34],
    pub AbstractInfo: [u32; 34],
    pub FormattingImplementationInfo: [u32; 34],
    pub LastModifyingImplementationInfo: [u32; 34],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_INITIATE_REPAIR_OUTPUT_BUFFER {
    pub Hint1: u64,
    pub Hint2: u64,
    pub Clsn: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SHRINK_VOLUME_INFORMATION {
    pub ShrinkRequestType: u32,
    pub Flags: u64,
    pub NewNumberOfSectors: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_MODIFY_RM {
    pub Flags: u32,
    pub LogContainerCountMax: u32,
    pub LogContainerCountMin: u32,
    pub LogContainerCount: u32,
    pub LogGrowthIncrement: u32,
    pub LogAutoShrinkPercentage: u32,
    pub Reserved: u64,
    pub LoggingMode: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_QUERY_RM_INFORMATION {
    pub BytesRequired: u32,
    pub TailLsn: u64,
    pub CurrentLsn: u64,
    pub ArchiveTailLsn: u64,
    pub LogContainerSize: u64,
    pub HighestVirtualClock: i64,
    pub LogContainerCount: u32,
    pub LogContainerCountMax: u32,
    pub LogContainerCountMin: u32,
    pub LogGrowthIncrement: u32,
    pub LogAutoShrinkPercentage: u32,
    pub Flags: u32,
    pub LoggingMode: u16,
    pub Reserved: u16,
    pub RmState: u32,
    pub LogCapacity: u64,
    pub LogFree: u64,
    pub TopsSize: u64,
    pub TopsUsed: u64,
    pub TransactionCount: u64,
    pub OnePCCount: u64,
    pub TwoPCCount: u64,
    pub NumberLogFileFull: u64,
    pub OldestTransactionAge: u64,
    pub RMName: u32,
    pub TmLogPathOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_ROLLFORWARD_REDO_INFORMATION {
    pub LastVirtualClock: i64,
    pub LastRedoLsn: u64,
    pub HighestRecoveryLsn: u64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_START_RM_INFORMATION {
    pub Flags: u32,
    pub LogContainerSize: u64,
    pub LogContainerCountMin: u32,
    pub LogContainerCountMax: u32,
    pub LogGrowthIncrement: u32,
    pub LogAutoShrinkPercentage: u32,
    pub TmLogPathOffset: u32,
    pub TmLogPathLength: u16,
    pub LoggingMode: u16,
    pub LogPathLength: u16,
    pub Reserved: u16,
    pub LogPath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_GET_METADATA_INFO_OUT {
    pub LowPart: i64,
    pub HighPart: i64,
    pub LockingTransaction: u32,
    pub LastLsn: u64,
    pub TransactionState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_LIST_TRANSACTION_LOCKED_FILES_ENTRY {
    pub Offset: u64,
    pub NameFlags: u32,
    pub FileId: i64,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub Reserved3: i64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_LIST_TRANSACTION_LOCKED_FILES {
    pub KtmTransaction: u32,
    pub NumberOfFiles: u64,
    pub BufferSizeRequired: u64,
    pub Offset: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_LIST_TRANSACTIONS_ENTRY {
    pub TransactionId: u32,
    pub TransactionState: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub Reserved3: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_LIST_TRANSACTIONS {
    pub NumberOfTransactions: u64,
    pub BufferSizeRequired: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_READ_BACKUP_INFORMATION_OUT {
    pub BufferLength: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_WRITE_BACKUP_INFORMATION {
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_GET_TRANSACTED_VERSION {
    pub ThisBaseVersion: u32,
    pub LatestVersion: u32,
    pub ThisMiniVersion: u16,
    pub FirstMiniVersion: u16,
    pub LatestMiniVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_SAVEPOINT_INFORMATION {
    pub KtmTransaction: usize,
    pub ActionCode: u32,
    pub SavepointId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_CREATE_MINIVERSION_INFO {
    pub StructureVersion: u16,
    pub StructureLength: u16,
    pub BaseVersion: u32,
    pub MiniVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TXFS_TRANSACTION_ACTIVE_INFO {
    pub TransactionsActiveAtSnapshot: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BOOT_AREA_INFO {
    pub BootSectorCount: u32,
    pub Offset: i64,
    pub BootSectors: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RETRIEVAL_POINTER_BASE {
    pub FileAreaOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_PERSISTENT_VOLUME_INFORMATION {
    pub VolumeFlags: u32,
    pub FlagMask: u32,
    pub Version: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_SYSTEM_RECOGNITION_INFORMATION {
    pub FileSystem: [i8; 9],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_OPLOCK_INPUT_BUFFER {
    pub StructureVersion: u16,
    pub StructureLength: u16,
    pub RequestedOplockLevel: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_OPLOCK_OUTPUT_BUFFER {
    pub StructureVersion: u16,
    pub StructureLength: u16,
    pub OriginalOplockLevel: u32,
    pub NewOplockLevel: u32,
    pub Flags: u32,
    pub AccessMode: u32,
    pub ShareMode: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUAL_STORAGE_TYPE {
    pub DeviceId: u32,
    pub VendorId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_QUERY_DEPENDENT_VOLUME_REQUEST {
    pub RequestLevel: u32,
    pub RequestFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_QUERY_DEPENDENT_VOLUME_LEV1_ENTRY {
    pub EntryLength: u32,
    pub DependencyTypeFlags: u32,
    pub ProviderSpecificFlags: u32,
    pub VirtualStorageType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_QUERY_DEPENDENT_VOLUME_LEV2_ENTRY {
    pub EntryLength: u32,
    pub DependencyTypeFlags: u32,
    pub ProviderSpecificFlags: u32,
    pub VirtualStorageType: u32,
    pub AncestorLevel: u32,
    pub HostVolumeNameOffset: u32,
    pub HostVolumeNameSize: u32,
    pub DependentVolumeNameOffset: u32,
    pub DependentVolumeNameSize: u32,
    pub RelativePathOffset: u32,
    pub RelativePathSize: u32,
    pub DependentDeviceNameOffset: u32,
    pub DependentDeviceNameSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_QUERY_DEPENDENT_VOLUME_RESPONSE {
    pub ResponseLevel: u32,
    pub NumberEntries: u32,
    pub Lev1Depends: [u32; 1],
    pub Lev2Depends: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_CHANGE_MACHINE_SID_INPUT {
    pub CurrentMachineSIDOffset: u16,
    pub CurrentMachineSIDLength: u16,
    pub NewMachineSIDOffset: u16,
    pub NewMachineSIDLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_CHANGE_MACHINE_SID_OUTPUT {
    pub NumSDChangedSuccess: u64,
    pub NumSDChangedFail: u64,
    pub NumSDUnused: u64,
    pub NumSDTotal: u64,
    pub NumMftSDChangedSuccess: u64,
    pub NumMftSDChangedFail: u64,
    pub NumMftSDTotal: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_QUERY_STATS_INPUT {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_QUERY_STATS_OUTPUT {
    pub SdsStreamSize: u64,
    pub SdsAllocationSize: u64,
    pub SiiStreamSize: u64,
    pub SiiAllocationSize: u64,
    pub SdhStreamSize: u64,
    pub SdhAllocationSize: u64,
    pub NumSDTotal: u64,
    pub NumSDUnused: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_ENUM_SDS_INPUT {
    pub StartingOffset: u64,
    pub MaxSDEntriesToReturn: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_ENUM_SDS_ENTRY {
    pub Hash: u32,
    pub SecurityId: u32,
    pub Offset: u64,
    pub Length: u32,
    pub Descriptor: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_ENUM_SDS_OUTPUT {
    pub NextOffset: u64,
    pub NumSDEntriesReturned: u64,
    pub NumSDBytesReturned: u64,
    pub SDEntry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_GLOBAL_CHANGE_INPUT {
    pub Flags: u32,
    pub ChangeType: u32,
    pub SdChange: u32,
    pub SdQueryStats: u32,
    pub SdEnumSds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_GLOBAL_CHANGE_OUTPUT {
    pub Flags: u32,
    pub ChangeType: u32,
    pub SdChange: u32,
    pub SdQueryStats: u32,
    pub SdEnumSds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOOKUP_STREAM_FROM_CLUSTER_INPUT {
    pub Flags: u32,
    pub NumberOfClusters: u32,
    pub Cluster: [i64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOOKUP_STREAM_FROM_CLUSTER_OUTPUT {
    pub Offset: u32,
    pub NumberOfMatches: u32,
    pub BufferSizeRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOOKUP_STREAM_FROM_CLUSTER_ENTRY {
    pub OffsetToNext: u32,
    pub Flags: u32,
    pub Reserved: i64,
    pub Cluster: i64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_TYPE_NOTIFICATION_INPUT {
    pub Flags: u32,
    pub NumFileTypeIDs: u32,
    pub FileTypeID: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_MGMT_LOCK {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_NAMESPACE_INFO {
    pub Version: u32,
    pub DeviceNumber: u32,
    pub StartingOffset: i64,
    pub SectorSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_CONTROL_PARAM {
    pub Operation: u32,
    pub Unused: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_REDIRECT_STATE {
    pub MdsNodeId: u32,
    pub DsNodeId: u32,
    pub FileRedirected: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_FILE_REVISION {
    pub FileId: i64,
    pub FileRevision: [i64; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_FILE_REVISION_FILE_ID_128 {
    pub FileId: u32,
    pub FileRevision: [i64; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_MDS_PATH {
    pub MdsNodeId: u32,
    pub DsNodeId: u32,
    pub PathLength: u32,
    pub Path: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_VOLUME_REDIRECT_STATE {
    pub MdsNodeId: u32,
    pub DsNodeId: u32,
    pub IsDiskConnected: u8,
    pub ClusterEnableDirectIo: u8,
    pub DiskConnectivity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_MDS_PATH_V2 {
    pub Version: i64,
    pub RequiredSize: u32,
    pub MdsNodeId: u32,
    pub DsNodeId: u32,
    pub Flags: u32,
    pub DiskConnectivity: u32,
    pub VolumeId: u32,
    pub IpAddressOffset: u32,
    pub IpAddressLength: u32,
    pub PathOffset: u32,
    pub PathLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_SET_VOLUME_ID {
    pub VolumeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_VOLUME_ID {
    pub VolumeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LMR_QUERY_INFO_PARAM {
    pub Operation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LMR_QUERY_SESSION_INFO {
    pub SessionId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_VETO_FILE_DIRECT_IO_OUTPUT {
    pub VetoedFromAltitudeIntegral: u64,
    pub VetoedFromAltitudeDecimal: u64,
    pub Reason: [u32; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_IS_OWNED_BY_CSVFS {
    pub OwnedByCSVFS: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LEVEL_TRIM_RANGE {
    pub Offset: u64,
    pub Length: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LEVEL_TRIM {
    pub Key: u32,
    pub NumRanges: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LEVEL_TRIM_OUTPUT {
    pub NumRangesProcessed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLUSTER_RANGE {
    pub StartingCluster: i64,
    pub ClusterCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REFERENCE_RANGE {
    pub StartingFileReferenceNumber: u64,
    pub EndingFileReferenceNumber: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_FILE_LAYOUT_INPUT {
    pub FilterEntryCount: u32,
    pub NumberOfPairs: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub Reserved: u32,
    pub ClusterRanges: [u32; 1],
    pub FileReferenceRanges: [u32; 1],
    pub StorageReserveIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_FILE_LAYOUT_OUTPUT {
    pub FileEntryCount: u32,
    pub FirstFileOffset: u32,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LAYOUT_ENTRY {
    pub Version: u32,
    pub NextFileOffset: u32,
    pub Flags: u32,
    pub FileAttributes: u32,
    pub FileReferenceNumber: u64,
    pub FirstNameOffset: u32,
    pub FirstStreamOffset: u32,
    pub ExtraInfoOffset: u32,
    pub Reserved: u32,
    pub ExtraInfoLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LAYOUT_NAME_ENTRY {
    pub NextNameOffset: u32,
    pub Flags: u32,
    pub ParentFileReferenceNumber: u64,
    pub FileNameLength: u32,
    pub Reserved: u32,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LAYOUT_INFO_ENTRY {
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub FileAttributes: u32,
    pub OwnerId: u32,
    pub SecurityId: u32,
    pub Usn: u32,
    pub StorageReserveId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_LAYOUT_ENTRY {
    pub Version: u32,
    pub NextStreamOffset: u32,
    pub Flags: u32,
    pub ExtentInformationOffset: u32,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub StreamInformationOffset: u32,
    pub AttributeTypeCode: u32,
    pub AttributeFlags: u32,
    pub StreamIdentifierLength: u32,
    pub StreamIdentifier: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_EXTENT_ENTRY {
    pub Flags: u32,
    pub RetrievalPointers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_GET_INTEGRITY_INFORMATION_BUFFER {
    pub ChecksumAlgorithm: u16,
    pub Reserved: u16,
    pub Flags: u32,
    pub ChecksumChunkSizeInBytes: u32,
    pub ClusterSizeInBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_SET_INTEGRITY_INFORMATION_BUFFER {
    pub ChecksumAlgorithm: u16,
    pub Reserved: u16,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_SET_INTEGRITY_INFORMATION_BUFFER_EX {
    pub EnableIntegrity: u8,
    pub KeepIntegrityStateUnchanged: u8,
    pub Reserved: u16,
    pub Flags: u32,
    pub Version: u8,
    pub Reserved2: [u8; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_OFFLOAD_READ_INPUT {
    pub Size: u32,
    pub Flags: u32,
    pub TokenTimeToLive: u32,
    pub Reserved: u32,
    pub FileOffset: u64,
    pub CopyLength: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_OFFLOAD_READ_OUTPUT {
    pub Size: u32,
    pub Flags: u32,
    pub TransferLength: u64,
    pub Token: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_OFFLOAD_WRITE_INPUT {
    pub Size: u32,
    pub Flags: u32,
    pub FileOffset: u64,
    pub CopyLength: u64,
    pub TransferOffset: u64,
    pub Token: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_OFFLOAD_WRITE_OUTPUT {
    pub Size: u32,
    pub Flags: u32,
    pub LengthWritten: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_PURGE_FAILURE_MODE_INPUT {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPAIR_COPIES_INPUT {
    pub Size: u32,
    pub Flags: u32,
    pub FileOffset: i64,
    pub Length: u32,
    pub SourceCopy: u32,
    pub NumberOfRepairCopies: u32,
    pub RepairCopies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPAIR_COPIES_OUTPUT {
    pub Size: u32,
    pub Status: u32,
    pub ResumeFileOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REGION_INFO {
    pub FileOffset: i64,
    pub Length: i64,
    pub Usage: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REGION_OUTPUT {
    pub Flags: u32,
    pub TotalRegionEntryCount: u32,
    pub RegionEntryCount: u32,
    pub Reserved: u32,
    pub Region: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_REGION_INPUT {
    pub FileOffset: i64,
    pub Length: i64,
    pub DesiredUsage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE_USN_REASON_INPUT {
    pub Flags: u32,
    pub UsnReasonToWrite: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STORAGE_TIER {
    pub Id: u32,
    pub Name: [u32; 1],
    pub Description: [u32; 1],
    pub Flags: u64,
    pub ProvisionedCapacity: u64,
    pub MediaType: u32,
    pub Class: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_STORAGE_CLASSES_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub TotalNumberOfTiers: u32,
    pub NumberOfTiersReturned: u32,
    pub Tiers: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_INFORMATION_ENTRY {
    pub Version: u32,
    pub Flags: u32,
    pub Class: u32,
    pub Flags_1: u32,
    pub Length: u16,
    pub Flags_2: u16,
    pub Reserved: u32,
    pub Vdl: u64,
    pub Length_1: u16,
    pub Flags_3: u16,
    pub ReparseDataSize: u32,
    pub ReparseDataOffset: u32,
    pub Length_2: u16,
    pub Flags_4: u16,
    pub EaSize: u32,
    pub EaInformationOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DesiredStorageClass {
    pub Class: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DataStream {
    pub Length: u16,
    pub Flags: u16,
    pub Reserved: u32,
    pub Vdl: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Reparse {
    pub Length: u16,
    pub Flags: u16,
    pub ReparseDataSize: u32,
    pub ReparseDataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Ea {
    pub Length: u16,
    pub Flags: u16,
    pub EaSize: u32,
    pub EaInformationOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_REGION_INFO_INPUT {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub NumberOfTierIds: u32,
    pub TierIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STORAGE_TIER_REGION {
    pub TierId: u32,
    pub Offset: u64,
    pub Length: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_REGION_INFO_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub Alignment: u64,
    pub TotalNumberOfRegions: u32,
    pub NumberOfRegionsReturned: u32,
    pub Regions: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_DESIRED_STORAGE_CLASS_INFORMATION {
    pub Class: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUPLICATE_EXTENTS_DATA {
    pub FileHandle: usize,
    pub SourceFileOffset: i64,
    pub TargetFileOffset: i64,
    pub ByteCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUPLICATE_EXTENTS_DATA32 {
    pub FileHandle: u32,
    pub SourceFileOffset: i64,
    pub TargetFileOffset: i64,
    pub ByteCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUPLICATE_EXTENTS_DATA_EX {
    pub Size: usize,
    pub FileHandle: usize,
    pub SourceFileOffset: i64,
    pub TargetFileOffset: i64,
    pub ByteCount: i64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUPLICATE_EXTENTS_DATA_EX32 {
    pub Size: u32,
    pub FileHandle: u32,
    pub SourceFileOffset: i64,
    pub TargetFileOffset: i64,
    pub ByteCount: i64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASYNC_DUPLICATE_EXTENTS_STATUS {
    pub Version: u32,
    pub State: u32,
    pub SourceFileOffset: u64,
    pub TargetFileOffset: u64,
    pub ByteCount: u64,
    pub BytesDuplicated: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_SMR_VOLUME_INFO_OUTPUT {
    pub Version: u32,
    pub Flags: u32,
    pub SizeOfRandomlyWritableTier: i64,
    pub FreeSpaceInRandomlyWritableTier: i64,
    pub SizeofSMRTier: i64,
    pub FreeSpaceInSMRTier: i64,
    pub UsableFreeSpaceInSMRTier: i64,
    pub VolumeGcState: u32,
    pub VolumeGcLastStatus: u32,
    pub CurrentGcBandFillPercentage: u32,
    pub Unused: [u64; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_SMR_VOLUME_GC_PARAMETERS {
    pub Version: u32,
    pub Flags: u32,
    pub Action: u32,
    pub Method: u32,
    pub IoGranularity: u32,
    pub CompressionFormat: u32,
    pub Unused: [u64; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAMS_QUERY_PARAMETERS_OUTPUT_BUFFER {
    pub OptimalWriteSize: u32,
    pub StreamGranularitySize: u32,
    pub StreamIdMin: u32,
    pub StreamIdMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAMS_ASSOCIATE_ID_INPUT_BUFFER {
    pub Flags: u32,
    pub StreamId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAMS_QUERY_ID_OUTPUT_BUFFER {
    pub StreamId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_BAD_RANGES_INPUT_RANGE {
    pub StartOffset: u64,
    pub LengthInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_BAD_RANGES_INPUT {
    pub Flags: u32,
    pub NumRanges: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_BAD_RANGES_OUTPUT_RANGE {
    pub Flags: u32,
    pub Reserved: u32,
    pub StartOffset: u64,
    pub LengthInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_BAD_RANGES_OUTPUT {
    pub Flags: u32,
    pub NumBadRanges: u32,
    pub NextOffsetToLookUp: u64,
    pub BadRanges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_DAX_ALLOC_ALIGNMENT_HINT_INPUT {
    pub Flags: u32,
    pub AlignmentShift: u32,
    pub FileOffsetToAlign: u64,
    pub FallbackAlignmentShift: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUAL_STORAGE_SET_BEHAVIOR_INPUT {
    pub Size: u32,
    pub BehaviorCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENCRYPTION_KEY_CTRL_INPUT {
    pub HeaderSize: u32,
    pub StructureSize: u32,
    pub KeyOffset: u16,
    pub KeySize: u16,
    pub DplLock: u32,
    pub DplUserId: u64,
    pub DplCredentialId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WOF_EXTERNAL_INFO {
    pub Version: u32,
    pub Provider: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WOF_EXTERNAL_FILE_ID {
    pub FileId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WOF_VERSION_INFO {
    pub WofVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_EXTERNAL_INFO {
    pub Version: u32,
    pub Flags: u32,
    pub DataSourceId: i64,
    pub ResourceHash: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_ADD_OVERLAY_INPUT {
    pub WimType: u32,
    pub WimIndex: u32,
    pub WimFileNameOffset: u32,
    pub WimFileNameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_UPDATE_OVERLAY_INPUT {
    pub DataSourceId: i64,
    pub WimFileNameOffset: u32,
    pub WimFileNameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_REMOVE_OVERLAY_INPUT {
    pub DataSourceId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_SUSPEND_OVERLAY_INPUT {
    pub DataSourceId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIM_PROVIDER_OVERLAY_ENTRY {
    pub NextEntryOffset: u32,
    pub DataSourceId: i64,
    pub WimGuid: u32,
    pub WimFileNameOffset: u32,
    pub WimType: u32,
    pub WimIndex: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PROVIDER_EXTERNAL_INFO_V0 {
    pub Version: u32,
    pub Algorithm: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PROVIDER_EXTERNAL_INFO_V1 {
    pub Version: u32,
    pub Algorithm: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTAINER_VOLUME_STATE {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTAINER_ROOT_INFO_INPUT {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTAINER_ROOT_INFO_OUTPUT {
    pub ContainerRootIdLength: u16,
    pub ContainerRootId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUALIZATION_INSTANCE_INFO_INPUT {
    pub NumberOfWorkerThreads: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUALIZATION_INSTANCE_INFO_INPUT_EX {
    pub HeaderSize: u16,
    pub Flags: u32,
    pub NotificationInfoSize: u32,
    pub NotificationInfoOffset: u16,
    pub ProviderMajorVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUALIZATION_INSTANCE_INFO_OUTPUT {
    pub VirtualizationInstanceID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_FILTER_FILE_IDENTIFIER_INPUT {
    pub AltitudeLength: u16,
    pub Altitude: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_FILTER_FILE_IDENTIFIER_OUTPUT {
    pub FilterFileIdentifierLength: u16,
    pub FilterFileIdentifier: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_BPIO_INPUT {
    pub Operation: u32,
    pub InFlags: u32,
    pub Reserved1: u64,
    pub Reserved2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_BPIO_RESULTS {
    pub OpStatus: u32,
    pub FailingDriverNameLen: u16,
    pub FailingDriverName: [u32; 32],
    pub FailureReasonLen: u16,
    pub FailureReason: [u32; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_BPIO_INFO {
    pub ActiveBypassIoCount: u32,
    pub StorageDriverNameLen: u16,
    pub StorageDriverName: [u32; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_BPIO_OUTPUT {
    pub Operation: u32,
    pub OutFlags: u32,
    pub Reserved1: u64,
    pub Reserved2: u64,
    pub Enable: u32,
    pub Query: u32,
    pub VolumeStackResume: u32,
    pub StreamResume: u32,
    pub GetInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPARSE_DATA_BUFFER {
    pub ReparseTag: u32,
    pub ReparseDataLength: u16,
    pub Reserved: u16,
    pub SubstituteNameOffset: u16,
    pub SubstituteNameLength: u16,
    pub PrintNameOffset: u16,
    pub PrintNameLength: u16,
    pub Flags: u32,
    pub PathBuffer: [u32; 1],
    pub SubstituteNameOffset_1: u16,
    pub SubstituteNameLength_1: u16,
    pub PrintNameOffset_1: u16,
    pub PrintNameLength_1: u16,
    pub PathBuffer_1: [u32; 1],
    pub DataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPARSE_GUID_DATA_BUFFER {
    pub ReparseTag: u32,
    pub ReparseDataLength: u16,
    pub Reserved: u16,
    pub ReparseGuid: u32,
    pub DataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPARSE_INDEX_KEY {
    pub FileReparseTag: u32,
    pub FileId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REPARSE_DATA_BUFFER_EX {
    pub Flags: u32,
    pub ExistingReparseTag: u32,
    pub ExistingReparseGuid: u32,
    pub Reserved: u64,
    pub ReparseDataBuffer: u32,
    pub ReparseGuidDataBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCRUB_DATA_INPUT {
    pub Size: u32,
    pub Flags: u32,
    pub MaximumIos: u32,
    pub ObjectId: [u32; 4],
    pub StartingByteOffset: u64,
    pub ByteCount: u64,
    pub Reserved: [u32; 36],
    pub Reserved_1: [u32; 41],
    pub ResumeContext: [u8; 1040],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCRUB_PARITY_EXTENT {
    pub Offset: i64,
    pub Length: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCRUB_PARITY_EXTENT_DATA {
    pub Size: u16,
    pub Flags: u16,
    pub NumberOfParityExtents: u16,
    pub MaximumNumberOfParityExtents: u16,
    pub ParityExtents: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCRUB_DATA_OUTPUT {
    pub Size: u32,
    pub Flags: u32,
    pub Status: u32,
    pub ErrorFileOffset: u64,
    pub ErrorLength: u64,
    pub NumberOfBytesRepaired: u64,
    pub NumberOfBytesFailed: u64,
    pub InternalFileReference: u64,
    pub ResumeContextLength: u16,
    pub ParityExtentDataOffset: u16,
    pub NextStartingByteOffset: u64,
    pub ValidDataLength: u64,
    pub Reserved: [u32; 4],
    pub Reserved_1: [u32; 9],
    pub Reserved_2: [u32; 10],
    pub NumberOfMetadataBytesProcessed: u64,
    pub NumberOfDataBytesProcessed: u64,
    pub TotalNumberOfMetadataBytesInUse: u64,
    pub TotalNumberOfDataBytesInUse: u64,
    pub Reserved2: [u64; 4],
    pub DataBytesSkippedDueToNoAllocation: u64,
    pub DataBytesSkippedDueToInvalidRun: u64,
    pub DataBytesSkippedDueToIntegrityStream: u64,
    pub DataBytesSkippedDueToRegionBeingClean: u64,
    pub DataBytesSkippedDueToLockConflict: u64,
    pub DataBytesSkippedDueToNoScrubDataFlag: u64,
    pub DataBytesSkippedDueToNoScrubNonIntegrityStreamFlag: u64,
    pub DataBytesScrubbed: u64,
    pub Reserved3: [u64; 8],
    pub ResumeContext: [u8; 1040],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SHARED_VIRTUAL_DISK_SUPPORT {
    pub SharedVirtualDiskSupport: u32,
    pub HandleState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REARRANGE_FILE_DATA {
    pub SourceStartingOffset: u64,
    pub TargetOffset: u64,
    pub SourceFileHandle: usize,
    pub Length: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REARRANGE_FILE_DATA32 {
    pub SourceStartingOffset: u64,
    pub TargetOffset: u64,
    pub SourceFileHandle: u32,
    pub Length: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SHUFFLE_FILE_DATA {
    pub StartingOffset: i64,
    pub Length: i64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_ASSIGN_EVENT_BUFFER {
    pub EventHandle: usize,
    pub KeyValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_PEEK_BUFFER {
    pub NamedPipeState: u32,
    pub ReadDataAvailable: u32,
    pub NumberOfMessages: u32,
    pub MessageLength: u32,
    pub Data: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_EVENT_BUFFER {
    pub NamedPipeState: u32,
    pub EntryType: u32,
    pub ByteCount: u32,
    pub KeyValue: u32,
    pub NumberRequests: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_WAIT_FOR_BUFFER {
    pub Timeout: i64,
    pub NameLength: u32,
    pub TimeoutSpecified: u8,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_CLIENT_PROCESS_BUFFER {
    pub ClientSession: usize,
    pub ClientProcess: usize,
    pub ClientSession_1: u64,
    pub ClientProcess_1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_CLIENT_PROCESS_BUFFER_V2 {
    pub ClientSession: u64,
    pub ClientProcess: usize,
    pub ClientProcess_1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_CLIENT_PROCESS_BUFFER_EX {
    pub ClientSession: usize,
    pub ClientProcess: usize,
    pub ClientSession_1: u64,
    pub ClientProcess_1: u64,
    pub ClientComputerNameLength: u16,
    pub ClientComputerBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_SILO_ARRIVAL_INPUT {
    pub JobHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_CREATE_SYMLINK_INPUT {
    pub NameOffset: u16,
    pub NameLength: u16,
    pub SubstituteNameOffset: u16,
    pub SubstituteNameLength: u16,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PIPE_DELETE_SYMLINK_INPUT {
    pub NameOffset: u16,
    pub NameLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINK_TRACKING_INFORMATION {
    pub Type: u32,
    pub VolumeId: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REMOTE_LINK_TRACKING_INFORMATION {
    pub TargetFileObject: usize,
    pub TargetLinkTrackingInformationLength: u32,
    pub TargetLinkTrackingInformationBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VCN_RANGE_INPUT_BUFFER {
    pub StartingVcn: i64,
    pub ClusterCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXTENT_READ_CACHE_INFO_BUFFER {
    pub AllocatedCache: i64,
    pub PopulatedCache: i64,
    pub InErrorCache: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_VOLUME_COUNTER_INFO_INPUT_BUFFER {
    pub ResetCounters: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_REFS_INFO_BUFFER {
    pub CacheSizeInBytes: i64,
    pub AllocatedCacheInBytes: i64,
    pub PopulatedCacheInBytes: i64,
    pub InErrorCacheInBytes: i64,
    pub MemoryUsedForCacheMetadata: i64,
    pub CacheLineSize: u32,
    pub CacheTransactionsOutstanding: i32,
    pub CacheLinesFree: i32,
    pub CacheLinesInError: i32,
    pub CacheHitsInBytes: i64,
    pub CacheMissesInBytes: i64,
    pub CachePopulationUpdatesInBytes: i64,
    pub CacheWriteThroughUpdatesInBytes: i64,
    pub CacheInvalidationsInBytes: i64,
    pub CacheOverReadsInBytes: i64,
    pub MetadataWrittenBytes: i64,
    pub CacheHitCounter: i32,
    pub CacheMissCounter: i32,
    pub CacheLineAllocationCounter: i32,
    pub CacheInvalidationsCounter: i32,
    pub CachePopulationUpdatesCounter: i32,
    pub CacheWriteThroughUpdatesCounter: i32,
    pub MaxCacheTransactionsOutstanding: i32,
    pub DataWritesReallocationCount: i64,
    pub DataInPlaceWriteCount: i64,
    pub MetadataAllocationsFastTierCount: i64,
    pub MetadataAllocationsSlowTierCount: i64,
    pub DataAllocationsFastTierCount: i64,
    pub DataAllocationsSlowTierCount: i64,
    pub DestagesSlowTierToFastTier: i64,
    pub DestagesFastTierToSlowTier: i64,
    pub SlowTierDataFillRatio: i32,
    pub FastTierDataFillRatio: i32,
    pub SlowTierMetadataFillRatio: i32,
    pub FastTierMetadataFillRatio: i32,
    pub SlowToFastDestageReadLatency: i64,
    pub SlowToFastDestageReadLatencyBase: i32,
    pub SlowToFastDestageWriteLatency: i64,
    pub SlowToFastDestageWriteLatencyBase: i32,
    pub FastToSlowDestageReadLatency: i64,
    pub FastToSlowDestageReadLatencyBase: i32,
    pub FastToSlowDestageWriteLatency: i64,
    pub FastToSlowDestageWriteLatencyBase: i32,
    pub SlowTierContainerFillRatio: i64,
    pub SlowTierContainerFillRatioBase: i32,
    pub FastTierContainerFillRatio: i64,
    pub FastTierContainerFillRatioBase: i32,
    pub Unused1: i32,
    pub Unused2: i32,
    pub Unused3: i32,
    pub Unused4: i32,
    pub TreeUpdateCount: i64,
    pub CheckpointCount: i64,
    pub LogWriteCount: i64,
    pub LogFillRatio: i32,
    pub ReadCacheInvalidationsForOverwrite: i32,
    pub ReadCacheInvalidationsForReuse: i32,
    pub ReadCacheInvalidationsGeneral: i32,
    pub ReadCacheChecksOnMount: i32,
    pub ReadCacheIssuesOnMount: i32,
    pub TrimLatency: i64,
    pub TrimLatencyBase: i32,
    pub DataCompactionCount: i64,
    pub CompactionReadLatency: i64,
    pub CompactionReadLatencyBase: i32,
    pub CompactionWriteLatency: i64,
    pub CompactionWriteLatencyBase: i32,
    pub DataInPlaceWriteClusterCount: i64,
    pub CompactionFailedDueToIneligibleContainer: i32,
    pub CompactionFailedDueToMaxFragmentation: i32,
    pub CompactedContainerFillRatio: i64,
    pub CompactedContainerFillRatioBase: i32,
    pub ContainerMoveRetryCount: i32,
    pub ContainerMoveFailedDueToIneligibleContainer: i32,
    pub CompactionFailureCount: i32,
    pub ContainerMoveFailureCount: i32,
    pub NumberOfDirtyMetadataPages: i64,
    pub NumberOfDirtyTableListEntries: i32,
    pub NumberOfDeleteQueueEntries: i32,
    pub MAAFilteredViewSize: i32,
    pub MAAFilteredViewInsertions: i32,
    pub MAAFilteredViewDeletions: i32,
    pub MAAFilteredViewCollisions: i32,
    pub MAAFilteredViewPurges: i32,
    pub MAARegionsVisitedPerAllocationSum: i64,
    pub MAARegionsVisitedPerAllocationBase: i32,
    pub MAAMaxRegionsVisitedPerAllocation: i32,
    pub TreeUpdateLatencyExclusive: i64,
    pub TreeUpdateLatencyTotal: i64,
    pub TreeUpdateLatencyBase: i32,
    pub CheckpointLatencyTreeUpdateExclusive: i64,
    pub CheckpointLatencyTreeUpdateTotal: i64,
    pub CheckpointLatencyTreeUpdateBase: i32,
    pub CheckpointLatencyTotal: i64,
    pub CheckpointLatencyTotalBase: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_DIRECT_ACCESS_EXTENTS {
    pub FileOffset: i64,
    pub Length: i64,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_GHOST_FILE_EXTENTS_INPUT_BUFFER {
    pub FileOffset: i64,
    pub ByteCount: i64,
    pub RecallOwnerGuid: u32,
    pub RecallMetadataBufferSize: u32,
    pub RecallMetadataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_GHOSTED_FILE_EXTENTS_INPUT_RANGE {
    pub FileOffset: i64,
    pub ByteCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GHOSTED_FILE_EXTENT {
    pub FileOffset: i64,
    pub ByteCount: i64,
    pub RecallOwnerGuid: u32,
    pub NextEntryOffset: u32,
    pub RecallMetadataBufferSize: u32,
    pub RecallMetadataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_GHOSTED_FILE_EXTENTS_OUTPUT {
    pub ExtentCount: u32,
    pub TotalExtentCount: u32,
    pub Extents: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_UNMAP_SPACE_INPUT_BUFFER {
    pub BytesToUnmap: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_UNMAP_SPACE_OUTPUT {
    pub BytesUnmapped: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCTL_QUERY_VOLUME_NUMA_INFO_OUTPUT {
    pub NumaNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_DEALLOCATE_RANGES_RANGE {
    pub StartOfRange: u64,
    pub CountOfRange: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_DEALLOCATE_RANGES_INPUT_BUFFER {
    pub RangeCount: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_DEALLOCATE_RANGES_INPUT_BUFFER_EX {
    pub RangeCount: u32,
    pub Allocator: u32,
    pub StreamReserveUpdateCount: i64,
    pub OffsetToRanges: u32,
    pub OffsetToLeakCounts: u32,
    pub Reserved: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_REMOVE_HARDLINK_BACKPOINTER {
    pub ParentDirectory: u64,
    pub Reserved: u64,
    pub FileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_EXTENT {
    pub Vcn: i64,
    pub Lcn: i64,
    pub Length: i64,
    pub Properties: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_SNAPSHOT_MANAGEMENT_INPUT_BUFFER {
    pub Operation: u32,
    pub SnapshotNameLength: u16,
    pub OperationInputBufferLength: u16,
    pub Reserved: [u64; 2],
    pub NameAndInputBuffer: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_SNAPSHOT_LIST_OUTPUT_BUFFER_ENTRY {
    pub NextEntryOffset: u32,
    pub SnapshotNameLength: u16,
    pub SnapshotCreationTime: u64,
    pub StreamSize: u64,
    pub StreamAllocationSize: u64,
    pub Reserved: [u64; 2],
    pub SnapshotName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_SNAPSHOT_LIST_OUTPUT_BUFFER {
    pub EntryCount: u32,
    pub BufferSizeRequiredForQuery: u32,
    pub Reserved: [u32; 2],
    pub Entries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_SNAPSHOT_QUERY_DELTAS_INPUT_BUFFER {
    pub StartingVcn: i64,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_STREAM_SNAPSHOT_QUERY_DELTAS_OUTPUT_BUFFER {
    pub ExtentCount: u32,
    pub Reserved: [u32; 2],
    pub Extents: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUPLICATE_CLUSTER_DATA {
    pub SourceLcn: i64,
    pub TargetLcn: i64,
    pub TargetFileOffset: i64,
    pub DuplicationLimit: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_BUFFER {
    pub Lcn: i64,
    pub LengthInClusters: i64,
    pub ReferenceCount: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_CREATE_INPUT_BUFFER {
    pub Offset: i64,
    pub Length: i64,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_VCN_MAPPING {
    pub Vcn: i64,
    pub Lcn: i64,
    pub CountOfRange: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_CREATE_OUTPUT_BUFFER {
    pub MappingCount: u32,
    pub VcnLcnMappings: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_RANGE {
    pub StartOfRange: i64,
    pub CountOfRange: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LCN_WEAK_REFERENCE_CLEAR_INPUT_BUFFER {
    pub RangeCount: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_VOLUME_DEDUP_INFO_INPUT_BUFFER {
    pub Version: u32,
    pub SetDedupState: u8,
    pub Enable: u8,
    pub SetWeakRefState: u8,
    pub EnableWeakRef: u8,
    pub SetDirtyRangeTrackingState: u8,
    pub EnableDirtyRangeTracking: u8,
    pub SetWeakRefInconsistentState: u8,
    pub SetWeakRefInconsistent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_VOLUME_DEDUP_INFO_OUTPUT_BUFFER {
    pub Version: u32,
    pub Enabled: u8,
    pub EnabledWeakRef: u8,
    pub EnabledDirtyRangeTracking: u8,
    pub WeakRefInconsistent: u8,
    pub IsClustered: u8,
    pub VolumeIdHash: u32,
    pub VolumeGuid: u32,
    pub VolumeUniqueGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_VOLUME_TOTAL_SHARED_LCNS_OUTPUT_BUFFER {
    pub Version: u32,
    pub TotalSharedLcns: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_CACHED_RUNS_STATE_INPUT_BUFFER {
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_SET_VOLUME_COMPRESSION_INFO_INPUT_BUFFER {
    pub Version: u32,
    pub CompressionFormat: u32,
    pub CompressionLevel: i16,
    pub CompressionChunkSizeBytes: u32,
    pub Flags: u32,
    pub CompressionTuning: u32,
    pub RecompressionTuning: u32,
    pub DecompressionTuning: u32,
    pub Reserved: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_VOLUME_COMPRESSION_INFO_OUTPUT_BUFFER {
    pub Version: u32,
    pub DefaultCompressionFormat: u32,
    pub DefaultCompressionLevel: i16,
    pub DefaultCompressionChunkSizeBytes: u32,
    pub VolumeClusterSizeBytes: u32,
    pub TotalVolumeClusters: u64,
    pub TotalAllocatedClusters: u64,
    pub TotalCompressibleClustersAllocated: u64,
    pub TotalCompressibleClustersInUse: u64,
    pub TotalCompressedClusters: u64,
    pub Flags: u32,
    pub CompressionTuning: u32,
    pub RecompressionTuning: u32,
    pub DecompressionTuning: u32,
    pub LastCompressionStatus: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_SET_VOLUME_IO_METRICS_INFO_INPUT_BUFFER {
    pub Version: u32,
    pub GlobalSecondsToTrack: u32,
    pub MetricsPeriodicitySeconds: u32,
    pub MetricsGenerationsPerContainer: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_VOLUME_IO_METRICS_INFO_INPUT_BUFFER {
    pub Version: u32,
    pub QueryType: u32,
    pub Reserved: [u32; 6],
    pub UnusedAlign: u64,
    pub Reserved_1: [u32; 6],
    pub ResumeKeyBlob: [u64; 2],
    pub Reserved_2: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_VOLUME_IO_METRICS_METRICS_DATA {
    pub PlaceHolder: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_VOLUME_IO_METRICS_INFO_OUTPUT_BUFFER {
    pub Version: u32,
    pub QueryType: u32,
    pub Reserved: [u32; 6],
    pub UnusedAlign: u64,
    pub GlobalSecondsToTrack: u32,
    pub MetricsPeriodicitySeconds: u32,
    pub MetricsGenerationsPerContainer: u32,
    pub Reserved_1: [u32; 6],
    pub EntryCount: u32,
    pub ResumeKeyBlob: [u64; 2],
    pub Reserved_2: [u32; 6],
    pub Metrics: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_SET_ROLLBACK_PROTECTION_INFO_INPUT_BUFFER {
    pub Version: u32,
    pub FailMountOnMismatch: u8,
    pub CustomPayloadLength: u32,
    pub CustomPayloadOffset: u32,
    pub EnableRollbackProtection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REFS_QUERY_ROLLBACK_PROTECTION_INFO_OUTPUT_BUFFER {
    pub Version: u32,
    pub VolumeGuid: u32,
    pub RollbackProtectionGuid: u32,
    pub FailMountOnMismatch: u8,
    pub FrozenVirtualClock: u64,
    pub CurrentVirtualClock: u64,
    pub ChecksumType: u16,
    pub ChecksumLength: u32,
    pub ChecksumOffset: u32,
    pub CustomPayloadLength: u32,
    pub CustomPayloadOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STORAGE_RESERVE_ID_INFORMATION {
    pub StorageReserveId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_APP_INSTANCE_EA {
    pub AppInstanceID: u32,
    pub CsvFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_SHARE_FLUSH_AND_PURGE_INPUT {
    pub Version: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMB_SHARE_FLUSH_AND_PURGE_OUTPUT {
    pub cEntriesPurged: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOV_RANGE_CHECK_DATA {
    pub RemoveZone: u8,
    pub InRange: [u64; 2],
    pub ZidForRemoval: u32,
    pub Reserved1: u64,
    pub Reserved2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PUBLIC_OBJECT_BASIC_INFORMATION {
    pub Attributes: u32,
    pub GrantedAccess: u32,
    pub HandleCount: u32,
    pub PointerCount: u32,
    pub Reserved: [u32; 10],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PUBLIC_OBJECT_TYPE_INFORMATION {
    pub TypeName: u32,
    pub reserved_padding: [u32; 22],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_BASIC_INFORMATION {
    pub BaseAddress: usize,
    pub AllocationBase: usize,
    pub AllocationProtect: u32,
    pub PartitionId: u16,
    pub RegionSize: usize,
    pub State: u32,
    pub Protect: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_RANGE_ENTRY {
    pub VirtualAddress: usize,
    pub NumberOfBytes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_CLIENT_CONTEXT {
    pub SecurityQos: u32,
    pub ClientToken: usize,
    pub DirectlyAccessClientToken: u8,
    pub DirectAccessEffectiveOnly: u8,
    pub ServerIsRemote: u8,
    pub ClientTokenControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KAPC_STATE {
    pub ApcListHead: [u32; 1],
    pub field_Process: u32,
    pub InProgressFlags: u8,
    pub KernelApcInProgress: u8,
    pub SpecialApcInProgress: u8,
    pub KernelApcPending: u8,
    pub UserApcPendingAll: u8,
    pub SpecialUserApcPending: u8,
    pub UserApcPending: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KQUEUE {
    pub Header: u32,
    pub EntryListHead: u32,
    pub CurrentCount: u32,
    pub MaximumCount: u32,
    pub ThreadListHead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_EXPORTS {
    pub SeCreateTokenPrivilege: u32,
    pub SeAssignPrimaryTokenPrivilege: u32,
    pub SeLockMemoryPrivilege: u32,
    pub SeIncreaseQuotaPrivilege: u32,
    pub SeUnsolicitedInputPrivilege: u32,
    pub SeTcbPrivilege: u32,
    pub SeSecurityPrivilege: u32,
    pub SeTakeOwnershipPrivilege: u32,
    pub SeLoadDriverPrivilege: u32,
    pub SeCreatePagefilePrivilege: u32,
    pub SeIncreaseBasePriorityPrivilege: u32,
    pub SeSystemProfilePrivilege: u32,
    pub SeSystemtimePrivilege: u32,
    pub SeProfileSingleProcessPrivilege: u32,
    pub SeCreatePermanentPrivilege: u32,
    pub SeBackupPrivilege: u32,
    pub SeRestorePrivilege: u32,
    pub SeShutdownPrivilege: u32,
    pub SeDebugPrivilege: u32,
    pub SeAuditPrivilege: u32,
    pub SeSystemEnvironmentPrivilege: u32,
    pub SeChangeNotifyPrivilege: u32,
    pub SeRemoteShutdownPrivilege: u32,
    pub SeNullSid: usize,
    pub SeWorldSid: usize,
    pub SeLocalSid: usize,
    pub SeCreatorOwnerSid: usize,
    pub SeCreatorGroupSid: usize,
    pub SeNtAuthoritySid: usize,
    pub SeDialupSid: usize,
    pub SeNetworkSid: usize,
    pub SeBatchSid: usize,
    pub SeInteractiveSid: usize,
    pub SeLocalSystemSid: usize,
    pub SeAliasAdminsSid: usize,
    pub SeAliasUsersSid: usize,
    pub SeAliasGuestsSid: usize,
    pub SeAliasPowerUsersSid: usize,
    pub SeAliasAccountOpsSid: usize,
    pub SeAliasSystemOpsSid: usize,
    pub SeAliasPrintOpsSid: usize,
    pub SeAliasBackupOpsSid: usize,
    pub SeAuthenticatedUsersSid: usize,
    pub SeRestrictedSid: usize,
    pub SeAnonymousLogonSid: usize,
    pub SeUndockPrivilege: u32,
    pub SeSyncAgentPrivilege: u32,
    pub SeEnableDelegationPrivilege: u32,
    pub SeLocalServiceSid: usize,
    pub SeNetworkServiceSid: usize,
    pub SeManageVolumePrivilege: u32,
    pub SeImpersonatePrivilege: u32,
    pub SeCreateGlobalPrivilege: u32,
    pub SeTrustedCredManAccessPrivilege: u32,
    pub SeRelabelPrivilege: u32,
    pub SeIncreaseWorkingSetPrivilege: u32,
    pub SeTimeZonePrivilege: u32,
    pub SeCreateSymbolicLinkPrivilege: u32,
    pub SeIUserSid: usize,
    pub SeUntrustedMandatorySid: usize,
    pub SeLowMandatorySid: usize,
    pub SeMediumMandatorySid: usize,
    pub SeHighMandatorySid: usize,
    pub SeSystemMandatorySid: usize,
    pub SeOwnerRightsSid: usize,
    pub SeAllAppPackagesSid: usize,
    pub SeUserModeDriversSid: usize,
    pub SeProcTrustWinTcbSid: usize,
    pub SeTrustedInstallerSid: usize,
    pub SeDelegateSessionUserImpersonatePrivilege: u32,
    pub SeAppSiloSid: usize,
    pub SeAppSiloVolumeRootMinimalCapabilitySid: usize,
    pub SeAppSiloProfilesRootMinimalCapabilitySid: usize,
    pub SeAppSiloPromptForAccessCapabilitySid: usize,
    pub SeAppSiloAccessToPublisherDirectoryCapabilitySid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_FILTER_SECTION_SYNC_OUTPUT {
    pub StructureSize: u32,
    pub SizeReturned: u32,
    pub Flags: u32,
    pub DesiredReadAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_FILTER_CALLBACK_DATA {
    pub SizeOfFsFilterCallbackData: u32,
    pub Operation: u8,
    pub Reserved: u8,
    pub field_DeviceObject: u32,
    pub field_FileObject: u32,
    pub Parameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FS_FILTER_CALLBACKS {
    pub SizeOfFsFilterCallbacks: u32,
    pub Reserved: u32,
    pub PreAcquireForSectionSynchronization: usize,
    pub PostAcquireForSectionSynchronization: usize,
    pub PreReleaseForSectionSynchronization: usize,
    pub PostReleaseForSectionSynchronization: usize,
    pub PreAcquireForCcFlush: usize,
    pub PostAcquireForCcFlush: usize,
    pub PreReleaseForCcFlush: usize,
    pub PostReleaseForCcFlush: usize,
    pub PreAcquireForModifiedPageWriter: usize,
    pub PostAcquireForModifiedPageWriter: usize,
    pub PreReleaseForModifiedPageWriter: usize,
    pub PostReleaseForModifiedPageWriter: usize,
    pub PreQueryOpen: usize,
    pub PostQueryOpen: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CREATE_STREAM_FILE_OPTIONS {
    pub Size: u16,
    pub Flags: u16,
    pub TargetDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_PRIORITY_INFO {
    pub Size: u32,
    pub ThreadPriority: u32,
    pub PagePriority: u32,
    pub IoPriority: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_IRP_EXT_TRACK_OFFSET_HEADER {
    pub Validation: u16,
    pub Flags: u16,
    pub TrackedOffsetCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COPY_INFORMATION {
    pub SourceFileObject: usize,
    pub SourceFileOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_LIST {
    pub FileObject: usize,
    pub NumberOfEntries: u32,
    pub IsImage: u32,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_MEMORY_RUN {
    pub BasePage: usize,
    pub PageCount: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_MEMORY_DESCRIPTOR {
    pub NumberOfRuns: u32,
    pub NumberOfPages: usize,
    pub Run: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_EXTENTS_DESCRIPTOR {
    pub NumberOfRuns: u32,
    pub NumberOfValidRuns: u32,
    pub Run: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_COMMON_FCB_HEADER {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub Flags: u8,
    pub IsFastIoPossible: u8,
    pub Flags2: u8,
    pub Reserved: u8,
    pub Version: u8,
    pub Resource: usize,
    pub PagingIoResource: usize,
    pub AllocationSize: i64,
    pub FileSize: i64,
    pub ValidDataLength: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_ADVANCED_FCB_HEADER {
    pub DUMMYSTRUCTNAME: u32,
    pub FastMutex: usize,
    pub FilterContexts: u32,
    pub PushLock: u32,
    pub FileContextSupportPointer: usize,
    pub Oplock: u32,
    pub ReservedForRemote: usize,
    pub AePushLock: usize,
    pub ReservedContextLegacy: usize,
    pub BypassIoOpenCount: u32,
    pub field_ReservedContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EOF_WAIT_BLOCK {
    pub EofWaitLinks: u32,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_AUXILIARY_BUFFER {
    pub Buffer: usize,
    pub Length: u32,
    pub Flags: u32,
    pub Mdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LOCK_INFO {
    pub StartingByte: i64,
    pub Length: i64,
    pub ExclusiveLock: u8,
    pub Key: u32,
    pub FileObject: usize,
    pub ProcessId: usize,
    pub EndingByte: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_LOCK {
    pub CompleteLockIrpRoutine: usize,
    pub UnlockRoutine: usize,
    pub FastIoIsQuestionable: u8,
    pub SpareC: [u8; 3],
    pub LockInformation: usize,
    pub LastReturnedLockInfo: u32,
    pub LastReturnedLock: usize,
    pub LockRequestsInProgress: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BASE_MCB {
    pub MaximumPairCount: u32,
    pub PairCount: u32,
    pub PoolType: u16,
    pub Flags: u16,
    pub Mapping: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LARGE_MCB {
    pub GuardedMutex: usize,
    pub BaseMcb: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCB {
    pub DummyFieldThatSizesThisStructureCorrectly: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPLOCK_KEY_ECP_CONTEXT {
    pub OplockKey: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUAL_OPLOCK_KEY_ECP_CONTEXT {
    pub ParentOplockKey: u32,
    pub TargetOplockKey: u32,
    pub ParentOplockKeySet: u8,
    pub TargetOplockKeySet: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_MUP_PROVIDER_INFO_LEVEL_1 {
    pub ProviderId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_MUP_PROVIDER_INFO_LEVEL_2 {
    pub ProviderId: u32,
    pub ProviderName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_PER_FILE_CONTEXT {
    pub Links: u32,
    pub OwnerId: usize,
    pub InstanceId: usize,
    pub FreeCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_PER_STREAM_CONTEXT {
    pub Links: u32,
    pub OwnerId: usize,
    pub InstanceId: usize,
    pub FreeCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSRTL_PER_FILEOBJECT_CONTEXT {
    pub Links: u32,
    pub OwnerId: usize,
    pub InstanceId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_OPEN_ECP_CONTEXT {
    pub Size: u16,
    pub Reserved: u16,
    pub Location: u32,
    pub Integrity: u32,
    pub Location_1: u32,
    pub Integrity_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_OPEN_ECP_CONTEXT_V0 {
    pub Size: u16,
    pub Reserved: u16,
    pub Location: u32,
    pub Integrity: u32,
    pub Location_1: u32,
    pub Integrity_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_APP_INSTANCE_ECP_CONTEXT {
    pub Size: u16,
    pub Reserved: u16,
    pub AppInstanceID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_APP_INSTANCE_VERSION_ECP_CONTEXT {
    pub Size: u16,
    pub Reserved: u16,
    pub VersionHigh: u64,
    pub VersionLow: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PREFETCH_OPEN_ECP_CONTEXT {
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFS_OPEN_ECP_CONTEXT {
    pub ExportAlias: usize,
    pub ClientSocketAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRV_OPEN_ECP_CONTEXT {
    pub ShareName: usize,
    pub SocketAddress: usize,
    pub OplockBlockState: u8,
    pub OplockAppState: u8,
    pub OplockFinalState: u8,
    pub Version: u16,
    pub InstanceType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RKF_BYPASS_ECP_CONTEXT {
    pub Reserved: i32,
    pub Version: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_DEVICE_HINT_ECP_CONTEXT {
    pub TargetDevice: usize,
    pub RemainingName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_DOWN_LEVEL_OPEN_ECP_CONTEXT {
    pub Version: u32,
    pub IsResume: u8,
    pub FileType: u32,
    pub SourceNodeId: u32,
    pub DestinationNodeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_FILE_REVISION_ECP_CONTEXT {
    pub FileId: i64,
    pub FileRevision: [i64; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_QUERY_FILE_REVISION_ECP_CONTEXT_FILE_ID_128 {
    pub FileId: u32,
    pub FileRevision: [i64; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_SET_HANDLE_PROPERTIES_ECP_CONTEXT {
    pub Size: usize,
    pub PauseTimeoutInSeconds: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPEN_REPARSE_LIST_ENTRY {
    pub OpenReparseListEntry: u32,
    pub ReparseTag: u32,
    pub Flags: u32,
    pub ReparseGuid: u32,
    pub Size: u16,
    pub RemainingLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPEN_REPARSE_LIST {
    pub OpenReparseList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_STOP_ON_SYMLINK_FILTER_ECP_v0 {
    pub ReparseCount: u32,
    pub RemainingPathLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_REDIRECTION_ECP_CONTEXT {
    pub Size: u16,
    pub Flags: u16,
    pub FileId: u32,
    pub VolumeGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_TIMESTAMPS {
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATOMIC_CREATE_ECP_CONTEXT {
    pub Size: u16,
    pub InFlags: u16,
    pub OutFlags: u16,
    pub ReparseBufferLength: u16,
    pub ReparseBuffer: u32,
    pub FileSize: i64,
    pub ValidDataLength: i64,
    pub FileTimestamps: usize,
    pub FileAttributes: u32,
    pub UsnSourceInfo: u32,
    pub Usn: u32,
    pub SuppressFileAttributeInheritanceMask: u32,
    pub InOpFlags: u32,
    pub OutOpFlags: u32,
    pub InGenFlags: u32,
    pub OutGenFlags: u32,
    pub CaseSensitiveFlagsMask: u32,
    pub InCaseSensitiveFlags: u32,
    pub OutCaseSensitiveFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ECP_OPEN_PARAMETERS {
    pub Size: u16,
    pub Reserved: u16,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPLOCK_NOTIFY_PARAMS {
    pub NotifyReason: u32,
    pub NotifyContext: usize,
    pub Irp: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_FILE_STAT_INFORMATION {
    pub FileId: i64,
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub FileAttributes: u32,
    pub ReparseTag: u32,
    pub NumberOfLinks: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_FILE_LX_INFORMATION {
    pub EffectiveAccess: u32,
    pub LxFlags: u32,
    pub LxUid: u32,
    pub LxGid: u32,
    pub LxMode: u32,
    pub LxDeviceIdMajor: u32,
    pub LxDeviceIdMinor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_EA_INFORMATION {
    pub EaBufferSize: u32,
    pub EaBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_USN_INFORMATION {
    pub Usn: u32,
    pub FileReferenceNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_SECURITY_INFORMATION {
    pub Reserved: u32,
    pub SecurityDescriptorSize: u32,
    pub SecurityDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_ON_CREATE_ECP_CONTEXT {
    pub RequestedClasses: u32,
    pub ClassesProcessed: u32,
    pub ClassesWithErrors: u32,
    pub ClassesWithNoData: u32,
    pub StatInformation: u32,
    pub LxInformation: u32,
    pub EaInformation: u32,
    pub Reserved: u32,
    pub CommonBufferSize: u32,
    pub CommonBuffer: usize,
    pub UsnInformation: u32,
    pub SecurityInformationRequested: u32,
    pub SecurityInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VETO_BINDING_ECP_CONTEXT {
    pub ShouldVetoBinding: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PUBLIC_BCB {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub MappedLength: u32,
    pub MappedFileOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_FILE_SIZES {
    pub AllocationSize: i64,
    pub FileSize: i64,
    pub ValidDataLength: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_AHEAD_PARAMETERS {
    pub NodeByteSize: u32,
    pub Granularity: u32,
    pub PipelinedRequestSize: u32,
    pub ReadAheadGrowthPercentage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_MANAGER_CALLBACKS {
    pub AcquireForLazyWrite: usize,
    pub ReleaseFromLazyWrite: usize,
    pub AcquireForReadAhead: usize,
    pub ReleaseFromReadAhead: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_MANAGER_CALLBACK_FUNCTIONS {
    pub AcquireForLazyWriteEx: usize,
    pub ReleaseFromLazyWrite: usize,
    pub AcquireForReadAhead: usize,
    pub ReleaseFromReadAhead: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_MANAGER_CALLBACKS_EX {
    pub Version: u16,
    pub Size: u16,
    pub Functions: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CACHE_UNINITIALIZE_EVENT {
    pub field_Next: u32,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_ASYNC_READ_CONTEXT {
    pub CompletionRoutine: usize,
    pub Context: usize,
    pub Mdl: usize,
    pub RequestorMode: u32,
    pub NestingLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_ERROR_CALLBACK_CONTEXT {
    pub NodeByteSize: u32,
    pub ErrorCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_DIRTY_PAGES_INFO {
    pub Version: u32,
    pub DirtyPageThreshold: i64,
    pub DirtyPageTarget: i64,
    pub CleanLockedCachedPagesThreshold: i64,
    pub CleanLockedCachedPagesTarget: i64,
    pub CurrentDirtyPages: i64,
    pub CurrentCleanLockedCachedPages: i64,
    pub CurrentPagesQueuedForWriting: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecHandle {
    pub dwLower: usize,
    pub dwUpper: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgInfoW {
    pub fCapabilities: u32,
    pub wVersion: u32,
    pub wRPCID: u32,
    pub cbMaxToken: u32,
    pub Name: usize,
    pub Comment: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecBuffer {
    pub cbBuffer: u32,
    pub BufferType: u32,
    pub pvBuffer: usize,
    pub pvBuffer_1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecBufferDesc {
    pub ulVersion: u32,
    pub cBuffers: u32,
    pub pBuffers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_NEGOTIATION_INFO {
    pub Size: u32,
    pub NameLength: u32,
    pub Name: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_CHANNEL_BINDINGS {
    pub dwInitiatorAddrType: u32,
    pub cbInitiatorLength: u32,
    pub dwInitiatorOffset: u32,
    pub dwAcceptorAddrType: u32,
    pub cbAcceptorLength: u32,
    pub dwAcceptorOffset: u32,
    pub cbApplicationDataLength: u32,
    pub dwApplicationDataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_CHANNEL_BINDINGS_EX {
    pub magicNumber: u32,
    pub flags: u32,
    pub cbHeaderLength: u32,
    pub cbStructureLength: u32,
    pub dwInitiatorAddrType: u32,
    pub cbInitiatorLength: u32,
    pub dwInitiatorOffset: u32,
    pub dwAcceptorAddrType: u32,
    pub cbAcceptorLength: u32,
    pub dwAcceptorOffset: u32,
    pub cbApplicationDataLength: u32,
    pub dwApplicationDataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_CHANNEL_BINDINGS_RESULT {
    pub flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_APPLICATION_PROTOCOL_LIST {
    pub ProtoNegoExt: u32,
    pub ProtocolListSize: u32,
    pub ProtocolList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_APPLICATION_PROTOCOLS {
    pub ProtocolListsSize: u32,
    pub ProtocolLists: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_SRTP_PROTECTION_PROFILES {
    pub ProfilesSize: u32,
    pub ProfilesList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_SRTP_MASTER_KEY_IDENTIFIER {
    pub MasterKeyIdentifierSize: u32,
    pub MasterKeyIdentifier: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_TOKEN_BINDING {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub KeyParametersSize: u32,
    pub KeyParameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_PRESHAREDKEY {
    pub KeySize: u32,
    pub Key: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_PRESHAREDKEY_IDENTITY {
    pub KeyIdentitySize: u32,
    pub KeyIdentity: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_DTLS_MTU {
    pub PathMTU: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_FLAGS {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_CERTIFICATE_REQUEST_CONTEXT {
    pub cbCertificateRequestContext: u32,
    pub rgCertificateRequestContext: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_APP_SESSION_STATE {
    pub AppSessionStateSize: u32,
    pub AppSessionState: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_SESSION_TICKET {
    pub SessionTicketSize: u32,
    pub SessionTicket: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_TRAFFIC_SECRETS {
    pub SymmetricAlgId: [u32; 1],
    pub ChainingMode: [u32; 1],
    pub HashAlgId: [u32; 1],
    pub KeySize: u32,
    pub IvSize: u32,
    pub MsgSequenceStart: u32,
    pub MsgSequenceEnd: u32,
    pub TrafficSecretType: u32,
    pub TrafficSecretSize: u32,
    pub TrafficSecret: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_NamesW {
    pub sUserName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_SSIProviderW {
    pub sProviderName: usize,
    pub ProviderInfoLength: u32,
    pub ProviderInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_KdcProxySettingsW {
    pub Version: u32,
    pub Flags: u32,
    pub ProxyServerOffset: u16,
    pub ProxyServerLength: u16,
    pub ClientTlsCredOffset: u16,
    pub ClientTlsCredLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_KdcNetworkSettingsW {
    pub Version: u32,
    pub Flags: u32,
    pub ProxyServerOffset: u16,
    pub ProxyServerLength: u16,
    pub ClientTlsCredOffset: u16,
    pub ClientTlsCredLength: u16,
    pub DcDiscoveryFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_Cert {
    pub EncodedCertSize: u32,
    pub EncodedCert: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_SubjectAttributes {
    pub AttributeInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_CredInfo {
    pub CredClass: u32,
    pub IsPromptingNeeded: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegoPackageInfo {
    pub PackageMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegoStatus {
    pub LastStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_Sizes {
    pub cbMaxToken: u32,
    pub cbMaxSignature: u32,
    pub cbBlockSize: u32,
    pub cbSecurityTrailer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_StreamSizes {
    pub cbHeader: u32,
    pub cbTrailer: u32,
    pub cbMaximumMessage: u32,
    pub cBuffers: u32,
    pub cbBlockSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NamesW {
    pub sUserName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_Lifespan {
    pub tsStart: u32,
    pub tsExpiry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_DceInfo {
    pub AuthzSvc: u32,
    pub pPac: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_KeyInfoW {
    pub sSignatureAlgorithmName: usize,
    pub sEncryptAlgorithmName: usize,
    pub KeySize: u32,
    pub SignatureAlgorithm: u32,
    pub EncryptAlgorithm: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_AuthorityW {
    pub sAuthorityName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_ProtoInfoW {
    pub sProtocolName: usize,
    pub majorVersion: u32,
    pub minorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_PasswordExpiry {
    pub tsPasswordExpires: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_LogoffTime {
    pub tsLogoffTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_SessionKey {
    pub SessionKeyLength: u32,
    pub SessionKey: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegoKeys {
    pub KeyType: u32,
    pub KeyLength: u32,
    pub KeyValue: usize,
    pub VerifyKeyType: u32,
    pub VerifyKeyLength: u32,
    pub VerifyKeyValue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_PackageInfoW {
    pub PackageInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_UserFlags {
    pub UserFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_Flags {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegotiationInfoW {
    pub PackageInfo: usize,
    pub NegotiationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_CredentialNameW {
    pub CredentialType: u32,
    pub field_sCredentialName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecurityFunctionTableW {
    pub dwVersion: u32,
    pub EnumerateSecurityPackagesW: u32,
    pub QueryCredentialsAttributesW: u32,
    pub AcquireCredentialsHandleW: u32,
    pub FreeCredentialsHandle: u32,
    pub Reserved2: usize,
    pub InitializeSecurityContextW: u32,
    pub AcceptSecurityContext: u32,
    pub CompleteAuthToken: u32,
    pub DeleteSecurityContext: u32,
    pub ApplyControlToken: u32,
    pub QueryContextAttributesW: u32,
    pub ImpersonateSecurityContext: u32,
    pub RevertSecurityContext: u32,
    pub MakeSignature: u32,
    pub VerifySignature: u32,
    pub FreeContextBuffer: u32,
    pub QuerySecurityPackageInfoW: u32,
    pub Reserved3: usize,
    pub Reserved4: usize,
    pub ExportSecurityContext: u32,
    pub ImportSecurityContextW: u32,
    pub AddCredentialsW: u32,
    pub Reserved8: usize,
    pub QuerySecurityContextToken: u32,
    pub EncryptMessage: u32,
    pub DecryptMessage: u32,
    pub SetContextAttributesW: u32,
    pub SetCredentialsAttributesW: u32,
    pub ChangeAccountPasswordW: u32,
    pub Reserved9: usize,
    pub QueryContextAttributesExW: u32,
    pub QueryCredentialsAttributesExW: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_IDENTITY_EX2 {
    pub Version: u32,
    pub cbHeaderLength: u32,
    pub cbStructureLength: u32,
    pub UserOffset: u32,
    pub UserLength: u32,
    pub DomainOffset: u32,
    pub DomainLength: u32,
    pub PackedCredentialsOffset: u32,
    pub PackedCredentialsLength: u32,
    pub Flags: u32,
    pub PackageListOffset: u32,
    pub PackageListLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_IDENTITY_W {
    pub field_User: u32,
    pub UserLength: u32,
    pub field_Domain: u32,
    pub DomainLength: u32,
    pub field_Password: u32,
    pub PasswordLength: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_IDENTITY_EXW {
    pub Version: u32,
    pub Length: u32,
    pub field_User: u32,
    pub UserLength: u32,
    pub field_Domain: u32,
    pub DomainLength: u32,
    pub field_Password: u32,
    pub PasswordLength: u32,
    pub Flags: u32,
    pub field_PackageList: u32,
    pub PackageListLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_USER_DATA {
    pub UserName: u32,
    pub LogonDomainName: u32,
    pub LogonServer: u32,
    pub pSid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_PATH_REQUEST {
    pub PathNameLength: u32,
    pub SecurityContext: usize,
    pub FilePathName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_PATH_REQUEST_EX {
    pub pSecurityContext: usize,
    pub EaLength: u32,
    pub pEaBuffer: usize,
    pub PathName: u32,
    pub DomainServiceName: u32,
    pub EcpList: usize,
    pub Silo: usize,
    pub Reserved: usize,
    pub Reserved_1: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_PATH_RESPONSE {
    pub LengthAccepted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DOS_HEADER {
    pub e_magic: u16,
    pub e_cblp: u16,
    pub e_cp: u16,
    pub e_crlc: u16,
    pub e_cparhdr: u16,
    pub e_minalloc: u16,
    pub e_maxalloc: u16,
    pub e_ss: u16,
    pub e_sp: u16,
    pub e_csum: u16,
    pub e_ip: u16,
    pub e_cs: u16,
    pub e_lfarlc: u16,
    pub e_ovno: u16,
    pub e_res: [u16; 4],
    pub e_oemid: u16,
    pub e_oeminfo: u16,
    pub e_res2: [u16; 10],
    pub e_lfanew: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_OS2_HEADER {
    pub ne_magic: u16,
    pub ne_ver: i8,
    pub ne_rev: i8,
    pub ne_enttab: u16,
    pub ne_cbenttab: u16,
    pub ne_crc: i32,
    pub ne_flags: u16,
    pub ne_autodata: u16,
    pub ne_heap: u16,
    pub ne_stack: u16,
    pub ne_csip: i32,
    pub ne_sssp: i32,
    pub ne_cseg: u16,
    pub ne_cmod: u16,
    pub ne_cbnrestab: u16,
    pub ne_segtab: u16,
    pub ne_rsrctab: u16,
    pub ne_restab: u16,
    pub ne_modtab: u16,
    pub ne_imptab: u16,
    pub ne_nrestab: i32,
    pub ne_cmovent: u16,
    pub ne_align: u16,
    pub ne_cres: u16,
    pub ne_exetyp: u8,
    pub ne_flagsothers: u8,
    pub ne_pretthunks: u16,
    pub ne_psegrefbytes: u16,
    pub ne_swaparea: u16,
    pub ne_expver: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_VXD_HEADER {
    pub e32_magic: u16,
    pub e32_border: u8,
    pub e32_worder: u8,
    pub e32_level: u32,
    pub e32_cpu: u16,
    pub e32_os: u16,
    pub e32_ver: u32,
    pub e32_mflags: u32,
    pub e32_mpages: u32,
    pub e32_startobj: u32,
    pub e32_eip: u32,
    pub e32_stackobj: u32,
    pub e32_esp: u32,
    pub e32_pagesize: u32,
    pub e32_lastpagesize: u32,
    pub e32_fixupsize: u32,
    pub e32_fixupsum: u32,
    pub e32_ldrsize: u32,
    pub e32_ldrsum: u32,
    pub e32_objtab: u32,
    pub e32_objcnt: u32,
    pub e32_objmap: u32,
    pub e32_itermap: u32,
    pub e32_rsrctab: u32,
    pub e32_rsrccnt: u32,
    pub e32_restab: u32,
    pub e32_enttab: u32,
    pub e32_dirtab: u32,
    pub e32_dircnt: u32,
    pub e32_fpagetab: u32,
    pub e32_frectab: u32,
    pub e32_impmod: u32,
    pub e32_impmodcnt: u32,
    pub e32_impproc: u32,
    pub e32_pagesum: u32,
    pub e32_datapage: u32,
    pub e32_preload: u32,
    pub e32_nrestab: u32,
    pub e32_cbnrestab: u32,
    pub e32_nressum: u32,
    pub e32_autodata: u32,
    pub e32_debuginfo: u32,
    pub e32_debuglen: u32,
    pub e32_instpreload: u32,
    pub e32_instdemand: u32,
    pub e32_heapsize: u32,
    pub e32_res3: [u8; 12],
    pub e32_winresoff: u32,
    pub e32_winreslen: u32,
    pub e32_devid: u16,
    pub e32_ddkver: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_FILE_HEADER {
    pub Machine: u16,
    pub NumberOfSections: u16,
    pub TimeDateStamp: u32,
    pub PointerToSymbolTable: u32,
    pub NumberOfSymbols: u32,
    pub SizeOfOptionalHeader: u16,
    pub Characteristics: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DATA_DIRECTORY {
    pub VirtualAddress: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_OPTIONAL_HEADER32 {
    pub Magic: u16,
    pub MajorLinkerVersion: u8,
    pub MinorLinkerVersion: u8,
    pub SizeOfCode: u32,
    pub SizeOfInitializedData: u32,
    pub SizeOfUninitializedData: u32,
    pub AddressOfEntryPoint: u32,
    pub BaseOfCode: u32,
    pub BaseOfData: u32,
    pub ImageBase: u32,
    pub SectionAlignment: u32,
    pub FileAlignment: u32,
    pub MajorOperatingSystemVersion: u16,
    pub MinorOperatingSystemVersion: u16,
    pub MajorImageVersion: u16,
    pub MinorImageVersion: u16,
    pub MajorSubsystemVersion: u16,
    pub MinorSubsystemVersion: u16,
    pub Win32VersionValue: u32,
    pub SizeOfImage: u32,
    pub SizeOfHeaders: u32,
    pub CheckSum: u32,
    pub Subsystem: u16,
    pub DllCharacteristics: u16,
    pub SizeOfStackReserve: u32,
    pub SizeOfStackCommit: u32,
    pub SizeOfHeapReserve: u32,
    pub SizeOfHeapCommit: u32,
    pub LoaderFlags: u32,
    pub NumberOfRvaAndSizes: u32,
    pub DataDirectory: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ROM_OPTIONAL_HEADER {
    pub Magic: u16,
    pub MajorLinkerVersion: u8,
    pub MinorLinkerVersion: u8,
    pub SizeOfCode: u32,
    pub SizeOfInitializedData: u32,
    pub SizeOfUninitializedData: u32,
    pub AddressOfEntryPoint: u32,
    pub BaseOfCode: u32,
    pub BaseOfData: u32,
    pub BaseOfBss: u32,
    pub GprMask: u32,
    pub CprMask: [u32; 4],
    pub GpValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_OPTIONAL_HEADER64 {
    pub Magic: u16,
    pub MajorLinkerVersion: u8,
    pub MinorLinkerVersion: u8,
    pub SizeOfCode: u32,
    pub SizeOfInitializedData: u32,
    pub SizeOfUninitializedData: u32,
    pub AddressOfEntryPoint: u32,
    pub BaseOfCode: u32,
    pub ImageBase: u64,
    pub SectionAlignment: u32,
    pub FileAlignment: u32,
    pub MajorOperatingSystemVersion: u16,
    pub MinorOperatingSystemVersion: u16,
    pub MajorImageVersion: u16,
    pub MinorImageVersion: u16,
    pub MajorSubsystemVersion: u16,
    pub MinorSubsystemVersion: u16,
    pub Win32VersionValue: u32,
    pub SizeOfImage: u32,
    pub SizeOfHeaders: u32,
    pub CheckSum: u32,
    pub Subsystem: u16,
    pub DllCharacteristics: u16,
    pub SizeOfStackReserve: u64,
    pub SizeOfStackCommit: u64,
    pub SizeOfHeapReserve: u64,
    pub SizeOfHeapCommit: u64,
    pub LoaderFlags: u32,
    pub NumberOfRvaAndSizes: u32,
    pub DataDirectory: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_NT_HEADERS64 {
    pub Signature: u32,
    pub FileHeader: u32,
    pub OptionalHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_NT_HEADERS32 {
    pub Signature: u32,
    pub FileHeader: u32,
    pub OptionalHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ROM_HEADERS {
    pub FileHeader: u32,
    pub OptionalHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_SECTION_HEADER {
    pub Name: [u8; 1],
    pub PhysicalAddress: u32,
    pub VirtualSize: u32,
    pub VirtualAddress: u32,
    pub SizeOfRawData: u32,
    pub PointerToRawData: u32,
    pub PointerToRelocations: u32,
    pub PointerToLinenumbers: u32,
    pub NumberOfRelocations: u16,
    pub NumberOfLinenumbers: u16,
    pub Characteristics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_SYMBOL {
    pub ShortName: [u8; 8],
    pub Short: u32,
    pub Long: u32,
    pub LongName: [u32; 2],
    pub Value: u32,
    pub SectionNumber: i16,
    pub Type: u16,
    pub StorageClass: u8,
    pub NumberOfAuxSymbols: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_SYMBOL_EX {
    pub ShortName: [u8; 8],
    pub Short: u32,
    pub Long: u32,
    pub LongName: [u32; 2],
    pub Value: u32,
    pub SectionNumber: i32,
    pub Type: u16,
    pub StorageClass: u8,
    pub NumberOfAuxSymbols: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RELOCATION {
    pub VirtualAddress: u32,
    pub RelocCount: u32,
    pub SymbolTableIndex: u32,
    pub Type: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_LINENUMBER {
    pub SymbolTableIndex: u32,
    pub VirtualAddress: u32,
    pub Linenumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_BASE_RELOCATION {
    pub VirtualAddress: u32,
    pub SizeOfBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARCHIVE_MEMBER_HEADER {
    pub Name: [u8; 16],
    pub Date: [u8; 12],
    pub UserID: [u8; 6],
    pub GroupID: [u8; 6],
    pub Mode: [u8; 8],
    pub Size: [u8; 10],
    pub EndHeader: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_EXPORT_DIRECTORY {
    pub Characteristics: u32,
    pub TimeDateStamp: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub Name: u32,
    pub Base: u32,
    pub NumberOfFunctions: u32,
    pub NumberOfNames: u32,
    pub AddressOfFunctions: u32,
    pub AddressOfNames: u32,
    pub AddressOfNameOrdinals: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_IMPORT_BY_NAME {
    pub Hint: u16,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_THUNK_DATA64 {
    pub ForwarderString: u64,
    pub Function: u64,
    pub Ordinal: u64,
    pub AddressOfData: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_THUNK_DATA32 {
    pub ForwarderString: u32,
    pub Function: u32,
    pub Ordinal: u32,
    pub AddressOfData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_TLS_DIRECTORY64 {
    pub StartAddressOfRawData: u64,
    pub EndAddressOfRawData: u64,
    pub AddressOfIndex: u64,
    pub AddressOfCallBacks: u64,
    pub SizeOfZeroFill: u32,
    pub Characteristics: u32,
    pub Reserved0: u32,
    pub Alignment: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_TLS_DIRECTORY32 {
    pub StartAddressOfRawData: u32,
    pub EndAddressOfRawData: u32,
    pub AddressOfIndex: u32,
    pub AddressOfCallBacks: u32,
    pub SizeOfZeroFill: u32,
    pub Characteristics: u32,
    pub Reserved0: u32,
    pub Alignment: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_IMPORT_DESCRIPTOR {
    pub Characteristics: u32,
    pub OriginalFirstThunk: u32,
    pub TimeDateStamp: u32,
    pub ForwarderChain: u32,
    pub Name: u32,
    pub FirstThunk: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_BOUND_IMPORT_DESCRIPTOR {
    pub TimeDateStamp: u32,
    pub OffsetModuleName: u16,
    pub NumberOfModuleForwarderRefs: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_BOUND_FORWARDER_REF {
    pub TimeDateStamp: u32,
    pub OffsetModuleName: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DELAYLOAD_DESCRIPTOR {
    pub AllAttributes: u32,
    pub RvaBased: u32,
    pub ReservedAttributes: u32,
    pub DllNameRVA: u32,
    pub ModuleHandleRVA: u32,
    pub ImportAddressTableRVA: u32,
    pub ImportNameTableRVA: u32,
    pub BoundImportAddressTableRVA: u32,
    pub UnloadInformationTableRVA: u32,
    pub TimeDateStamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RESOURCE_DIRECTORY {
    pub Characteristics: u32,
    pub TimeDateStamp: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub NumberOfNamedEntries: u16,
    pub NumberOfIdEntries: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RESOURCE_DIRECTORY_ENTRY {
    pub NameOffset: u32,
    pub NameIsString: u32,
    pub Name: u32,
    pub Id: u16,
    pub OffsetToData: u32,
    pub OffsetToDirectory: u32,
    pub DataIsDirectory: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RESOURCE_DIRECTORY_STRING {
    pub Length: u16,
    pub NameString: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RESOURCE_DIR_STRING_U {
    pub Length: u16,
    pub NameString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_RESOURCE_DATA_ENTRY {
    pub OffsetToData: u32,
    pub Size: u32,
    pub CodePage: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_LOAD_CONFIG_CODE_INTEGRITY {
    pub Flags: u16,
    pub Catalog: u16,
    pub CatalogOffset: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DYNAMIC_RELOCATION_TABLE {
    pub Version: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DYNAMIC_RELOCATION32 {
    pub Symbol: u32,
    pub BaseRelocSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DYNAMIC_RELOCATION64 {
    pub Symbol: u64,
    pub BaseRelocSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DYNAMIC_RELOCATION32_V2 {
    pub HeaderSize: u32,
    pub FixupInfoSize: u32,
    pub Symbol: u32,
    pub SymbolGroup: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DYNAMIC_RELOCATION64_V2 {
    pub HeaderSize: u32,
    pub FixupInfoSize: u32,
    pub Symbol: u64,
    pub SymbolGroup: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_PROLOGUE_DYNAMIC_RELOCATION_HEADER {
    pub PrologueByteCount: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_EPILOGUE_DYNAMIC_RELOCATION_HEADER {
    pub EpilogueCount: u32,
    pub EpilogueByteCount: u8,
    pub BranchDescriptorElementSize: u8,
    pub BranchDescriptorCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_IMPORT_CONTROL_TRANSFER_DYNAMIC_RELOCATION {
    pub PageRelativeOffset: u32,
    pub IndirectCall: u32,
    pub IATIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_IMPORT_CONTROL_TRANSFER_ARM64_RELOCATION {
    pub PageRelativeOffset: u32,
    pub IndirectCall: u32,
    pub RegisterIndex: u32,
    pub ImportType: u32,
    pub IATIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_INDIR_CONTROL_TRANSFER_DYNAMIC_RELOCATION {
    pub PageRelativeOffset: u16,
    pub IndirectCall: u16,
    pub RexWPrefix: u16,
    pub CfgCheck: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_SWITCHTABLE_BRANCH_DYNAMIC_RELOCATION {
    pub PageRelativeOffset: u16,
    pub RegisterNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_FUNCTION_OVERRIDE_HEADER {
    pub FuncOverrideSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_FUNCTION_OVERRIDE_DYNAMIC_RELOCATION {
    pub OriginalRva: u32,
    pub BDDOffset: u32,
    pub RvaSize: u32,
    pub BaseRelocSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_BDD_INFO {
    pub Version: u32,
    pub BDDSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_BDD_DYNAMIC_RELOCATION {
    pub Left: u16,
    pub Right: u16,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_LOAD_CONFIG_DIRECTORY32 {
    pub Size: u32,
    pub TimeDateStamp: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub GlobalFlagsClear: u32,
    pub GlobalFlagsSet: u32,
    pub CriticalSectionDefaultTimeout: u32,
    pub DeCommitFreeBlockThreshold: u32,
    pub DeCommitTotalFreeThreshold: u32,
    pub LockPrefixTable: u32,
    pub MaximumAllocationSize: u32,
    pub VirtualMemoryThreshold: u32,
    pub ProcessHeapFlags: u32,
    pub ProcessAffinityMask: u32,
    pub CSDVersion: u16,
    pub DependentLoadFlags: u16,
    pub EditList: u32,
    pub SecurityCookie: u32,
    pub SEHandlerTable: u32,
    pub SEHandlerCount: u32,
    pub GuardCFCheckFunctionPointer: u32,
    pub GuardCFDispatchFunctionPointer: u32,
    pub GuardCFFunctionTable: u32,
    pub GuardCFFunctionCount: u32,
    pub GuardFlags: u32,
    pub CodeIntegrity: u32,
    pub GuardAddressTakenIatEntryTable: u32,
    pub GuardAddressTakenIatEntryCount: u32,
    pub GuardLongJumpTargetTable: u32,
    pub GuardLongJumpTargetCount: u32,
    pub DynamicValueRelocTable: u32,
    pub CHPEMetadataPointer: u32,
    pub GuardRFFailureRoutine: u32,
    pub GuardRFFailureRoutineFunctionPointer: u32,
    pub DynamicValueRelocTableOffset: u32,
    pub DynamicValueRelocTableSection: u16,
    pub Reserved2: u16,
    pub GuardRFVerifyStackPointerFunctionPointer: u32,
    pub HotPatchTableOffset: u32,
    pub Reserved3: u32,
    pub EnclaveConfigurationPointer: u32,
    pub VolatileMetadataPointer: u32,
    pub GuardEHContinuationTable: u32,
    pub GuardEHContinuationCount: u32,
    pub GuardXFGCheckFunctionPointer: u32,
    pub GuardXFGDispatchFunctionPointer: u32,
    pub GuardXFGTableDispatchFunctionPointer: u32,
    pub CastGuardOsDeterminedFailureMode: u32,
    pub GuardMemcpyFunctionPointer: u32,
    pub UmaFunctionPointers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_LOAD_CONFIG_DIRECTORY64 {
    pub Size: u32,
    pub TimeDateStamp: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub GlobalFlagsClear: u32,
    pub GlobalFlagsSet: u32,
    pub CriticalSectionDefaultTimeout: u32,
    pub DeCommitFreeBlockThreshold: u64,
    pub DeCommitTotalFreeThreshold: u64,
    pub LockPrefixTable: u64,
    pub MaximumAllocationSize: u64,
    pub VirtualMemoryThreshold: u64,
    pub ProcessAffinityMask: u64,
    pub ProcessHeapFlags: u32,
    pub CSDVersion: u16,
    pub DependentLoadFlags: u16,
    pub EditList: u64,
    pub SecurityCookie: u64,
    pub SEHandlerTable: u64,
    pub SEHandlerCount: u64,
    pub GuardCFCheckFunctionPointer: u64,
    pub GuardCFDispatchFunctionPointer: u64,
    pub GuardCFFunctionTable: u64,
    pub GuardCFFunctionCount: u64,
    pub GuardFlags: u32,
    pub CodeIntegrity: u32,
    pub GuardAddressTakenIatEntryTable: u64,
    pub GuardAddressTakenIatEntryCount: u64,
    pub GuardLongJumpTargetTable: u64,
    pub GuardLongJumpTargetCount: u64,
    pub DynamicValueRelocTable: u64,
    pub CHPEMetadataPointer: u64,
    pub GuardRFFailureRoutine: u64,
    pub GuardRFFailureRoutineFunctionPointer: u64,
    pub DynamicValueRelocTableOffset: u32,
    pub DynamicValueRelocTableSection: u16,
    pub Reserved2: u16,
    pub GuardRFVerifyStackPointerFunctionPointer: u64,
    pub HotPatchTableOffset: u32,
    pub Reserved3: u32,
    pub EnclaveConfigurationPointer: u64,
    pub VolatileMetadataPointer: u64,
    pub GuardEHContinuationTable: u64,
    pub GuardEHContinuationCount: u64,
    pub GuardXFGCheckFunctionPointer: u64,
    pub GuardXFGDispatchFunctionPointer: u64,
    pub GuardXFGTableDispatchFunctionPointer: u64,
    pub CastGuardOsDeterminedFailureMode: u64,
    pub GuardMemcpyFunctionPointer: u64,
    pub UmaFunctionPointers: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_CHPE_METADATA_X86 {
    pub Version: u32,
    pub CHPECodeAddressRangeOffset: u32,
    pub CHPECodeAddressRangeCount: u32,
    pub WowA64ExceptionHandlerFunctionPointer: u32,
    pub WowA64DispatchCallFunctionPointer: u32,
    pub WowA64DispatchIndirectCallFunctionPointer: u32,
    pub WowA64DispatchIndirectCallCfgFunctionPointer: u32,
    pub WowA64DispatchRetFunctionPointer: u32,
    pub WowA64DispatchRetLeafFunctionPointer: u32,
    pub WowA64DispatchJumpFunctionPointer: u32,
    pub CompilerIATPointer: u32,
    pub WowA64RdtscFunctionPointer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_CHPE_RANGE_ENTRY {
    pub StartOffset: u32,
    pub NativeCode: u32,
    pub AddressBits: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM64EC_METADATA {
    pub Version: u32,
    pub CodeMap: u32,
    pub CodeMapCount: u32,
    pub CodeRangesToEntryPoints: u32,
    pub RedirectionMetadata: u32,
    pub tbd__os_arm64x_dispatch_call_no_redirect: u32,
    pub tbd__os_arm64x_dispatch_ret: u32,
    pub tbd__os_arm64x_dispatch_call: u32,
    pub tbd__os_arm64x_dispatch_icall: u32,
    pub tbd__os_arm64x_dispatch_icall_cfg: u32,
    pub AlternateEntryPoint: u32,
    pub AuxiliaryIAT: u32,
    pub CodeRangesToEntryPointsCount: u32,
    pub RedirectionMetadataCount: u32,
    pub GetX64InformationFunctionPointer: u32,
    pub SetX64InformationFunctionPointer: u32,
    pub ExtraRFETable: u32,
    pub ExtraRFETableSize: u32,
    pub field_os_arm64x_dispatch_fptr: u32,
    pub AuxiliaryIATCopy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM64EC_METADATA_V2 {
    pub Version: u32,
    pub CodeMap: u32,
    pub CodeMapCount: u32,
    pub CodeRangesToEntryPoints: u32,
    pub RedirectionMetadata: u32,
    pub tbd__os_arm64x_dispatch_call_no_redirect: u32,
    pub tbd__os_arm64x_dispatch_ret: u32,
    pub tbd__os_arm64x_dispatch_call: u32,
    pub tbd__os_arm64x_dispatch_icall: u32,
    pub tbd__os_arm64x_dispatch_icall_cfg: u32,
    pub AlternateEntryPoint: u32,
    pub AuxiliaryIAT: u32,
    pub CodeRangesToEntryPointsCount: u32,
    pub RedirectionMetadataCount: u32,
    pub GetX64InformationFunctionPointer: u32,
    pub SetX64InformationFunctionPointer: u32,
    pub ExtraRFETable: u32,
    pub ExtraRFETableSize: u32,
    pub field_os_arm64x_dispatch_fptr: u32,
    pub AuxiliaryIATCopy: u32,
    pub AuxDelayloadIAT: u32,
    pub AuxDelayloadIATCopy: u32,
    pub ReservedBitField: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM64EC_REDIRECTION_ENTRY {
    pub Source: u32,
    pub Destination: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM64EC_CODE_RANGE_ENTRY_POINT {
    pub StartRva: u32,
    pub EndRva: u32,
    pub EntryPoint: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DVRT_ARM64X_FIXUP_RECORD {
    pub Offset: u16,
    pub Type: u16,
    pub Size: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DVRT_ARM64X_DELTA_FIXUP_RECORD {
    pub Offset: u16,
    pub Type: u16,
    pub Sign: u16,
    pub Scale: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_HOT_PATCH_INFO {
    pub Version: u32,
    pub Size: u32,
    pub SequenceNumber: u32,
    pub BaseImageList: u32,
    pub BaseImageCount: u32,
    pub BufferOffset: u32,
    pub ExtraPatchSize: u32,
    pub MinSequenceNumber: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_HOT_PATCH_BASE {
    pub SequenceNumber: u32,
    pub Flags: u32,
    pub OriginalTimeDateStamp: u32,
    pub OriginalCheckSum: u32,
    pub CodeIntegrityInfo: u32,
    pub CodeIntegritySize: u32,
    pub PatchTable: u32,
    pub BufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_HOT_PATCH_MACHINE {
    pub field_x86: u32,
    pub Amd64: u32,
    pub Arm64: u32,
    pub Amd64EC: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_HOT_PATCH_HASHES {
    pub SHA256: [u8; 32],
    pub SHA1: [u8; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_CE_RUNTIME_FUNCTION_ENTRY {
    pub FuncStart: u32,
    pub PrologLen: u32,
    pub FuncLen: u32,
    pub ThirtyTwoBit: u32,
    pub ExceptionFlag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM_RUNTIME_FUNCTION_ENTRY {
    pub BeginAddress: u32,
    pub UnwindData: u32,
    pub Flag: u32,
    pub FunctionLength: u32,
    pub Ret: u32,
    pub H: u32,
    pub Reg: u32,
    pub R: u32,
    pub L: u32,
    pub C: u32,
    pub StackAdjust: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARM64_RUNTIME_FUNCTION_ENTRY {
    pub BeginAddress: u32,
    pub UnwindData: u32,
    pub Flag: u32,
    pub FunctionLength: u32,
    pub RegF: u32,
    pub RegI: u32,
    pub H: u32,
    pub CR: u32,
    pub FrameSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ALPHA64_RUNTIME_FUNCTION_ENTRY {
    pub BeginAddress: u64,
    pub EndAddress: u64,
    pub ExceptionHandler: u64,
    pub HandlerData: u64,
    pub PrologEndAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ALPHA_RUNTIME_FUNCTION_ENTRY {
    pub BeginAddress: u32,
    pub EndAddress: u32,
    pub ExceptionHandler: u32,
    pub HandlerData: u32,
    pub PrologEndAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _IMAGE_RUNTIME_FUNCTION_ENTRY {
    pub BeginAddress: u32,
    pub EndAddress: u32,
    pub UnwindInfoAddress: u32,
    pub UnwindData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ENCLAVE_CONFIG32 {
    pub Size: u32,
    pub MinimumRequiredConfigSize: u32,
    pub PolicyFlags: u32,
    pub NumberOfImports: u32,
    pub ImportList: u32,
    pub ImportEntrySize: u32,
    pub FamilyID: [u8; 1],
    pub ImageID: [u8; 1],
    pub ImageVersion: u32,
    pub SecurityVersion: u32,
    pub EnclaveSize: u32,
    pub NumberOfThreads: u32,
    pub EnclaveFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ENCLAVE_CONFIG64 {
    pub Size: u32,
    pub MinimumRequiredConfigSize: u32,
    pub PolicyFlags: u32,
    pub NumberOfImports: u32,
    pub ImportList: u32,
    pub ImportEntrySize: u32,
    pub FamilyID: [u8; 1],
    pub ImageID: [u8; 1],
    pub ImageVersion: u32,
    pub SecurityVersion: u32,
    pub EnclaveSize: u64,
    pub NumberOfThreads: u32,
    pub EnclaveFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ENCLAVE_IMPORT {
    pub MatchType: u32,
    pub MinimumSecurityVersion: u32,
    pub UniqueOrAuthorID: [u8; 1],
    pub FamilyID: [u8; 1],
    pub ImageID: [u8; 1],
    pub ImportName: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DEBUG_DIRECTORY {
    pub Characteristics: u32,
    pub TimeDateStamp: u32,
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub Type: u32,
    pub SizeOfData: u32,
    pub AddressOfRawData: u32,
    pub PointerToRawData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_COFF_SYMBOLS_HEADER {
    pub NumberOfSymbols: u32,
    pub LvaToFirstSymbol: u32,
    pub NumberOfLinenumbers: u32,
    pub LvaToFirstLinenumber: u32,
    pub RvaToFirstByteOfCode: u32,
    pub RvaToLastByteOfCode: u32,
    pub RvaToFirstByteOfData: u32,
    pub RvaToLastByteOfData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FPO_DATA {
    pub ulOffStart: u32,
    pub cbProcSize: u32,
    pub cdwLocals: u32,
    pub cdwParams: u16,
    pub cbProlog: u16,
    pub cbRegs: u16,
    pub fHasSEH: u16,
    pub fUseBP: u16,
    pub reserved: u16,
    pub cbFrame: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_DEBUG_MISC {
    pub DataType: u32,
    pub Length: u32,
    pub Unicode: u8,
    pub Reserved: [u8; 3],
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_FUNCTION_ENTRY {
    pub StartingAddress: u32,
    pub EndingAddress: u32,
    pub EndOfPrologue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_FUNCTION_ENTRY64 {
    pub StartingAddress: u64,
    pub EndingAddress: u64,
    pub EndOfPrologue: u64,
    pub UnwindInfoAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_SEPARATE_DEBUG_HEADER {
    pub Signature: u16,
    pub Flags: u16,
    pub Machine: u16,
    pub Characteristics: u16,
    pub TimeDateStamp: u32,
    pub CheckSum: u32,
    pub ImageBase: u32,
    pub SizeOfImage: u32,
    pub NumberOfSections: u32,
    pub ExportedNamesSize: u32,
    pub DebugDirectorySize: u32,
    pub SectionAlignment: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NON_PAGED_DEBUG_INFO {
    pub Signature: u16,
    pub Flags: u16,
    pub Size: u32,
    pub Machine: u16,
    pub Characteristics: u16,
    pub TimeDateStamp: u32,
    pub CheckSum: u32,
    pub SizeOfImage: u32,
    pub ImageBase: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARCHITECTURE_HEADER {
    pub AmaskValue: u32,
    pub AmaskShift: u32,
    pub FirstEntryRVA: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_ARCHITECTURE_ENTRY {
    pub FixupInstRVA: u32,
    pub NewInst: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PATCH_MAIN_CALLOUT_PARAMS {
    pub Version: u32,
    pub PatchPhase: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CPTABLEINFO {
    pub CodePage: u16,
    pub MaximumCharacterSize: u16,
    pub DefaultChar: u16,
    pub UniDefaultChar: u16,
    pub TransDefaultChar: u16,
    pub TransUniDefaultChar: u16,
    pub DBCSCodePage: u16,
    pub LeadByte: [u8; 1],
    pub MultiByteTable: *mut u16,
    pub WideCharTable: usize,
    pub DBCSRanges: *mut u16,
    pub DBCSOffsets: *mut u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NLSTABLEINFO {
    pub OemTableInfo: u32,
    pub AnsiTableInfo: u32,
    pub UpperCaseTable: *mut u16,
    pub LowerCaseTable: *mut u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_NLS_STATE {
    pub DefaultAcpTableInfo: u32,
    pub DefaultOemTableInfo: u32,
    pub ActiveCodePageData: *mut u16,
    pub OemCodePageData: *mut u16,
    pub LeadByteInfo: *mut u16,
    pub OemLeadByteInfo: *mut u16,
    pub CaseMappingData: *mut u16,
    pub UnicodeUpcaseTable844: *mut u16,
    pub UnicodeLowercaseTable844: *mut u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_STATE_CONTEXT {
    pub Reserved1: u32,
    pub TargetSystemState: u32,
    pub EffectiveSystemState: u32,
    pub CurrentSystemState: u32,
    pub IgnoreHibernationPath: u32,
    pub PseudoTransition: u32,
    pub KernelSoftReboot: u32,
    pub DirectedDripsTransition: u32,
    pub Reserved2: u32,
    pub ContextAsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COUNTED_REASON_CONTEXT {
    pub Version: u32,
    pub Flags: u32,
    pub ResourceFileName: u32,
    pub ResourceReasonId: u16,
    pub StringCount: u32,
    pub ReasonStrings: u32,
    pub SimpleString: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_USER_PRESENCE {
    pub UserPresence: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SESSION_CONNECT {
    pub Connected: u8,
    pub Console: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SESSION_TIMEOUTS {
    pub InputTimeout: u32,
    pub DisplayTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SESSION_RIT_STATE {
    pub Active: u8,
    pub LastInputTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SESSION_WINLOGON {
    pub SessionId: u32,
    pub Console: u8,
    pub Locked: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SESSION_ALLOW_EXTERNAL_DMA_DEVICES {
    pub IsAllowed: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_IDLE_RESILIENCY {
    pub CoalescingTimeout: u32,
    pub IdleResiliencyPeriod: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_MONITOR_INVOCATION {
    pub Console: u8,
    pub RequestReason: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_LIMIT_ATTRIBUTES {
    pub Type: usize,
    pub DomainId: u32,
    pub MaxValue: u32,
    pub MinValue: u32,
    pub MinTimeParameter: u32,
    pub MaxTimeParameter: u32,
    pub DefaultACValue: u32,
    pub DefaultDCValue: u32,
    pub SupportTimeParameter: u32,
    pub Reserved: u32,
    pub AsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_LIMIT_VALUE {
    pub Type: usize,
    pub DomainId: u32,
    pub TargetValue: u32,
    pub TimeParameter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESUME_PERFORMANCE {
    pub PostTimeMs: u32,
    pub TotalResumeTimeMs: u64,
    pub ResumeCompleteTimestamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct APPLICATIONLAUNCH_SETTING_VALUE {
    pub ActivationTime: i64,
    pub Flags: u32,
    pub ButtonInstanceID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_PLATFORM_INFORMATION {
    pub AoAc: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_IDLE_STATE {
    pub StateType: u8,
    pub StateFlags: u32,
    pub HardwareLatency: u32,
    pub Power: u32,
    pub Context: usize,
    pub Handler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_IDLE_STATES {
    pub Size: u32,
    pub Revision: u32,
    pub Count: u32,
    pub Type: u32,
    pub TargetProcessors: u32,
    pub State: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_POLICY {
    pub Revision: u32,
    pub PowerButton: usize,
    pub SleepButton: usize,
    pub LidClose: usize,
    pub LidOpenWake: u32,
    pub Reserved: u32,
    pub Idle: usize,
    pub IdleTimeout: u32,
    pub IdleSensitivity: u8,
    pub DynamicThrottle: u8,
    pub Spare2: [u8; 2],
    pub MinSleep: u32,
    pub MaxSleep: u32,
    pub ReducedLatencySleep: u32,
    pub WinLogonFlags: u32,
    pub Spare3: u32,
    pub DozeS4Timeout: u32,
    pub BroadcastCapacityResolution: u32,
    pub DischargePolicy: [u32; 1],
    pub VideoTimeout: u32,
    pub VideoDimDisplay: u8,
    pub VideoReserved: [u32; 3],
    pub SpindownTimeout: u32,
    pub OptimizeForPower: u8,
    pub FanThrottleTolerance: u8,
    pub ForcedThrottle: u8,
    pub MinThrottle: u8,
    pub OverThrottled: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_POWER_POLICY_INFO {
    pub TimeCheck: u32,
    pub DemoteLimit: u32,
    pub PromoteLimit: u32,
    pub DemotePercent: u8,
    pub PromotePercent: u8,
    pub Spare: [u8; 2],
    pub AllowDemotion: u32,
    pub AllowPromotion: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_POWER_POLICY {
    pub Revision: u32,
    pub DynamicThrottle: u8,
    pub Spare: [u8; 3],
    pub DisableCStates: u32,
    pub Reserved: u32,
    pub PolicyCount: u32,
    pub Policy: [usize; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADMINISTRATOR_POWER_POLICY {
    pub MinSleep: u32,
    pub MaxSleep: u32,
    pub MinVideoTimeout: u32,
    pub MaxVideoTimeout: u32,
    pub MinSpindownTimeout: u32,
    pub MaxSpindownTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIBERFILE_BUCKET {
    pub MaxPhysicalMemory: u32,
    pub PhysicalMemoryPercent: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_SOURCE_STATE {
    pub BatteryState: u32,
    pub InstantaneousPeakPower: u32,
    pub InstantaneousPeakPeriod: u32,
    pub SustainablePeakPower: u32,
    pub SustainablePeakPeriod: u32,
    pub PeakPower: u32,
    pub MaxOutputPower: u32,
    pub MaxInputPower: u32,
    pub BatteryRateInCurrent: i32,
    pub BatteryVoltage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_LOGGING_ENTRY {
    pub Reason: u32,
    pub States: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_STATE_DISABLE_REASON {
    pub AffectedState: [u8; 1],
    pub PowerReasonCode: u32,
    pub PowerReasonLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_POWER_STATE {
    pub IdleFunction: usize,
    pub Idle0KernelTimeLimit: u32,
    pub Idle0LastTime: u32,
    pub IdleState: usize,
    pub LastCheck: u64,
    pub IdleTimes: usize,
    pub IdleTime1: u32,
    pub PromotionCheck: u32,
    pub IdleTime2: u32,
    pub CurrentThrottle: u8,
    pub ThrottleLimit: u8,
    pub Spare1: [u8; 2],
    pub SetMember: u32,
    pub AbortThrottle: usize,
    pub DebugDelta: u64,
    pub DebugCount: u32,
    pub LastSysTime: u32,
    pub Spare2: [u32; 10],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROCESSOR_POWER_INFORMATION {
    pub Number: u32,
    pub MaxMhz: u32,
    pub CurrentMhz: u32,
    pub MhzLimit: u32,
    pub MaxIdleState: u32,
    pub CurrentIdleState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POWER_INFORMATION {
    pub MaxIdlenessAllowed: u32,
    pub Idleness: u32,
    pub TimeRemaining: u32,
    pub CoolingMode: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_HIBERFILE_INFORMATION {
    pub NumberOfMcbPairs: u32,
    pub Mcb: [i64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REASON_BUFFER {
    pub Flags: u32,
    pub ResourceFileNameOffset: usize,
    pub ResourceReasonId: u16,
    pub StringCount: u32,
    pub SubstitutionStringsOffset: usize,
    pub SimpleStringOffset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DIAGNOSTIC_BUFFER {
    pub Size: usize,
    pub CallerType: u32,
    pub ProcessImageNameOffset: usize,
    pub ProcessId: u32,
    pub ServiceTag: u32,
    pub DeviceDescriptionOffset: usize,
    pub DevicePathOffset: usize,
    pub ReasonOffset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAKE_TIMER_INFO {
    pub OffsetToNext: usize,
    pub DueTime: u64,
    pub Period: u32,
    pub ReasonContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_HEADER {
    pub Type: usize,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_DEVICE {
    pub Header: usize,
    pub InstancePath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_FIXED {
    pub Header: usize,
    pub FixedWakeSourceType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_INTERNAL {
    pub Header: usize,
    pub InternalWakeSourceType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_TIMER {
    pub Header: usize,
    pub Reason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_INFO {
    pub Count: u32,
    pub Offsets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_WAKE_SOURCE_HISTORY {
    pub Count: u32,
    pub Offsets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_SPR_ACTIVE_SESSION_DATA {
    pub Start: u8,
    pub ActiveSessionGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARPORT_REMOVAL_RELATIONS {
    pub DeviceObject: usize,
    pub Flags: u32,
    pub DeviceName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_PARCHIP_INFO {
    pub Controller: *mut u8,
    pub EcrController: *mut u8,
    pub HardwareModes: u32,
    pub ParChipSetMode: usize,
    pub ParChipClearMode: usize,
    pub Context: usize,
    pub success: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_PORT_INFORMATION {
    pub OriginalController: u64,
    pub Controller: *mut u8,
    pub SpanOfController: u32,
    pub TryAllocatePort: usize,
    pub FreePort: usize,
    pub QueryNumWaiters: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_PNP_INFORMATION {
    pub OriginalEcpController: u64,
    pub EcpController: *mut u8,
    pub SpanOfEcpController: u32,
    pub PortNumber: u32,
    pub HardwareCapabilities: u32,
    pub TrySetChipMode: usize,
    pub ClearChipMode: usize,
    pub FifoDepth: u32,
    pub FifoWidth: u32,
    pub EppControllerPhysicalAddress: u64,
    pub SpanOfEppController: u32,
    pub Ieee1284_3DeviceCount: u32,
    pub TrySelectDevice: usize,
    pub DeselectDevice: usize,
    pub Context: usize,
    pub CurrentMode: u32,
    pub PortName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_1284_COMMAND {
    pub ID: u8,
    pub Port: u8,
    pub CommandFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_CHIP_MODE {
    pub ModeFlags: u8,
    pub success: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_INTERRUPT_SERVICE_ROUTINE {
    pub InterruptServiceRoutine: usize,
    pub InterruptServiceContext: usize,
    pub DeferredPortCheckRoutine: usize,
    pub DeferredPortCheckContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_INTERRUPT_INFORMATION {
    pub InterruptObject: usize,
    pub TryAllocatePortAtInterruptLevel: usize,
    pub FreePortFromInterruptLevel: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MORE_PARALLEL_PORT_INFORMATION {
    pub InterfaceType: u32,
    pub BusNumber: u32,
    pub InterruptLevel: u32,
    pub InterruptVector: u32,
    pub InterruptAffinity: u32,
    pub InterruptMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARCLASS_INFORMATION {
    pub Controller: *mut u8,
    pub EcrController: *mut u8,
    pub SpanOfController: u32,
    pub DetermineIeeeModes: usize,
    pub NegotiateIeeeMode: usize,
    pub TerminateIeeeMode: usize,
    pub IeeeFwdToRevMode: usize,
    pub IeeeRevToFwdMode: usize,
    pub ParallelRead: usize,
    pub ParallelWrite: usize,
    pub ParclassContext: usize,
    pub HardwareCapabilities: u32,
    pub FifoDepth: u32,
    pub FifoWidth: u32,
    pub ParallelTryselect: usize,
    pub ParallelDeSelect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_PNP_EVENT_COMPLETE {
    pub QueryStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_INVALIDATE_BLOCK {
    pub VfIndex: u16,
    pub BlockMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGE_COUNT_INPUT {
    pub VfIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGE_COUNT_OUTPUT {
    pub RangeCount: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGES_INPUT {
    pub VfIndex: u16,
    pub BarNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGES_OUTPUT {
    pub BasePageNumber: u32,
    pub PageCount: u32,
    pub InterceptReads: u8,
    pub InterceptWrites: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGE_UPDATE_INPUT {
    pub VfIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_MITIGATED_RANGE_UPDATE_OUTPUT {
    pub VfIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_PROXY_QUERY_LUID_OUTPUT {
    pub DeviceLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_DEVICE_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadVfConfig: usize,
    pub WriteVfConfig: usize,
    pub ReadVfConfigBlock: usize,
    pub WriteVfConfigBlock: usize,
    pub QueryProbedBars: usize,
    pub GetVendorAndDevice: usize,
    pub GetDeviceLocation: usize,
    pub ResetVf: usize,
    pub SetVfPowerState: usize,
    pub GetResourceForBar: usize,
    pub QueryLuid: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRIOV_DEVICE_INTERFACE_STANDARD_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadVfConfig: usize,
    pub WriteVfConfig: usize,
    pub ReadVfConfigBlock: usize,
    pub WriteVfConfigBlock: usize,
    pub QueryProbedBars: usize,
    pub GetVendorAndDevice: usize,
    pub GetDeviceLocation: usize,
    pub ResetVf: usize,
    pub SetVfPowerState: usize,
    pub GetResourceForBar: usize,
    pub QueryLuid: usize,
    pub QueryProbedBars_2: usize,
    pub QueryVfLuid: usize,
    pub QueryLuidVf: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MITIGABLE_DEVICE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadWriteMitigatedRegister: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MCGEN_TRACE_CONTEXT {
    pub RegistrationHandle: u32,
    pub Logger: u32,
    pub MatchAnyKeyword: u64,
    pub MatchAllKeyword: u64,
    pub Flags: u32,
    pub IsEnabled: u32,
    pub Level: u8,
    pub Reserve: u8,
    pub EnableBitsCount: u16,
    pub EnableBitMask: *mut u32,
    pub EnableKeyWords: usize,
    pub EnableLevel: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_CONTEXT_QUERY_PARKING_PAGE {
    pub PhysicalPageAddress: u64,
    pub VirtualPageAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_UNMASKED_INTERRUPT_INFORMATION {
    pub Version: u16,
    pub Size: u16,
    pub Flags: usize,
    pub Mode: u32,
    pub Polarity: u32,
    pub Gsiv: u32,
    pub PinNumber: u16,
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_IDLE_STATE_UPDATE {
    pub Version: u32,
    pub Latency: u32,
    pub BreakEvenDuration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PLATFORM_IDLE_STATE_UPDATE {
    pub Version: u32,
    pub Latency: u32,
    pub BreakEvenDuration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_KERNEL_INFORMATION_STRUCT_V3 {
    pub Version: u16,
    pub Size: u16,
    pub Plugin: usize,
    pub RequestWorker: usize,
    pub EnumerateUnmaskedInterrupts: usize,
    pub ProcessorHalt: usize,
    pub RequestInterrupt: usize,
    pub TransitionCriticalResource: usize,
    pub ProcessorIdleVeto: usize,
    pub PlatformIdleVeto: usize,
    pub UpdateProcessorIdleState: usize,
    pub UpdatePlatformIdleState: usize,
    pub RequestCommon: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_V2 {
    pub Id: u32,
    pub Flags: u64,
    pub DeepestWakeableIdleState: u32,
    pub IdleStateCount: u32,
    pub IdleStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_DEVICE_REGISTER_V2 {
    pub Flags: u64,
    pub ComponentCount: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_POWER_CONTROL {
    pub DeviceHandle: usize,
    pub PowerControlCode: usize,
    pub RequestContext: usize,
    pub InBuffer: usize,
    pub InBufferSize: usize,
    pub OutBuffer: usize,
    pub OutBufferSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_COMPLETE_IDLE_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_COMPLETE_PERF_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Succeeded: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_ACPI_NOTIFY {
    pub DeviceHandle: usize,
    pub NotifyCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_ACPI_EVALUATE_CONTROL_METHOD_COMPLETE {
    pub DeviceHandle: usize,
    pub CompletionFlags: u32,
    pub MethodStatus: u32,
    pub CompletionContext: usize,
    pub OutputArgumentSize: usize,
    pub OutputArguments: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_INFORMATION {
    pub WorkType: usize,
    pub ActiveComplete: usize,
    pub IdleState: usize,
    pub DevicePower: usize,
    pub PowerControl: usize,
    pub DeviceIdle: usize,
    pub CompleteIdleState: usize,
    pub CompletePerfState: usize,
    pub AcpiNotify: usize,
    pub ControlMethodComplete: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_CRASHDUMP_INFORMATION {
    pub DeviceHandle: usize,
    pub DeviceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PREPARE_DEVICE {
    pub DeviceId: usize,
    pub DeviceAccepted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ABANDON_DEVICE {
    pub DeviceId: usize,
    pub DeviceAccepted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_REGISTER_DEVICE_V2 {
    pub DeviceId: usize,
    pub KernelHandle: usize,
    pub Register: usize,
    pub DeviceHandle: usize,
    pub DeviceAccepted: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_UNREGISTER_DEVICE {
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_DEVICE_POWER_STATE {
    pub DeviceHandle: usize,
    pub PowerState: u32,
    pub Complete: u8,
    pub SystemTransition: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK {
    pub WorkInformation: usize,
    pub NeedWork: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_POWER_CONTROL_REQUEST {
    pub DeviceHandle: usize,
    pub PowerControlCode: usize,
    pub InBuffer: usize,
    pub InBufferSize: usize,
    pub OutBuffer: usize,
    pub OutBufferSize: usize,
    pub BytesReturned: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_POWER_CONTROL_COMPLETE {
    pub DeviceHandle: usize,
    pub PowerControlCode: usize,
    pub RequestContext: usize,
    pub BytesReturned: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_SYSTEM_LATENCY {
    pub Latency: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_DEVICE_STARTED {
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_NOTIFY_COMPONENT_IDLE_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub IdleState: u32,
    pub DriverNotified: u8,
    pub Completed: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_REGISTER_DEBUGGER {
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_LOW_POWER_EPOCH {
    pub LowPowerEpoch: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_REGISTER_CRASHDUMP_DEVICE {
    pub PowerOnDumpDeviceCallback: usize,
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_DEVICE_PLATFORM_CONSTRAINTS {
    pub DeviceHandle: usize,
    pub MinimumDStates: usize,
    pub PlatformStateCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_PLATFORM_CONSTRAINTS {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub MinimumFStates: *mut u32,
    pub PlatformStateCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PERF_STATE {
    pub Value: u64,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_COMPONENT_PERF_CAPABILITIES {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub SetCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_COMPONENT_PERF_SET {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Set: u32,
    pub Flags: u64,
    pub Unit: usize,
    pub Type: usize,
    pub Count: u32,
    pub Minimum: u64,
    pub Maximum: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_COMPONENT_PERF_SET_NAME {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Set: u32,
    pub NameSize: u16,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_COMPONENT_PERF_STATES {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Set: u32,
    pub States: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_PERF_SET {
    pub Name: u32,
    pub Flags: u64,
    pub Unit: usize,
    pub Type: usize,
    pub Count: u32,
    pub States: usize,
    pub Minimum: u64,
    pub Maximum: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_PERF_INFO {
    pub SetCount: u32,
    pub PerfStateSets: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_REGISTER_COMPONENT_PERF_STATES {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Flags: u64,
    pub PerfStateInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_PERF_STATE_REQUEST {
    pub Set: u32,
    pub StateIndex: u32,
    pub StateValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_REQUEST_COMPONENT_PERF_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Completed: u8,
    pub Succeeded: u8,
    pub PerfRequestsCount: u32,
    pub PerfRequests: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_CURRENT_COMPONENT_PERF_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Set: u32,
    pub StateIndex: u32,
    pub StateValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_DEBUGGER_TRANSITION_REQUIREMENTS {
    pub DeviceHandle: usize,
    pub PlatformStateCount: u32,
    pub PowerTransitionRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_SOC_SUBSYSTEM_COUNT {
    pub PlatformIdleStateIndex: u32,
    pub SubsystemCount: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_SOC_SUBSYSTEM {
    pub PlatformIdleStateIndex: u32,
    pub SubsystemIndex: u32,
    pub SubsystemHandle: usize,
    pub ParentName: u32,
    pub SubsystemName: u32,
    pub MetadataCount: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_RESET_SOC_SUBSYSTEM_ACCOUNTING {
    pub PlatformIdleStateIndex: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_SOC_SUBSYSTEM_BLOCKING_TIME {
    pub PlatformIdleStateIndex: u32,
    pub SubsystemHandle: usize,
    pub SubsystemName: usize,
    pub BlockingTime: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_SOC_SUBSYSTEM_METADATA {
    pub Key: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_QUERY_SOC_SUBSYSTEM_METADATA {
    pub PlatformIdleStateIndex: u32,
    pub SubsystemHandle: usize,
    pub SubsystemName: usize,
    pub Flags: u32,
    pub MetadataCount: u32,
    pub Metadata: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_IDLE_STATE_V2 {
    pub Ulong: u32,
    pub Interruptible: u32,
    pub CacheCoherent: u32,
    pub ThreadContextRetained: u32,
    pub CStateType: u32,
    pub WakesSpuriously: u32,
    pub PlatformOnly: u32,
    pub Autonomous: u32,
    pub Reserved: u32,
    pub Latency: u32,
    pub BreakEvenDuration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_IDLE_DEPENDENCY {
    pub TargetProcessor: usize,
    pub DeviceContext: usize,
    pub ExpectedState: u8,
    pub AllowDeeperStates: u8,
    pub LooseDependency: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_CAPABILITIES {
    pub FeedbackCounterCount: u32,
    pub IdleStateCount: u32,
    pub PerformanceStatesSupported: u8,
    pub ParkingSupported: u8,
    pub DiscretePerformanceStateCount: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_IDLE_STATES_V2 {
    pub Count: u32,
    pub IdleStates: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_PLATFORM_STATES {
    pub PlatformStateCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PLATFORM_IDLE_STATE {
    pub InitiatingProcessor: usize,
    pub DeviceContext: usize,
    pub InitiatingState: u8,
    pub Latency: u32,
    pub BreakEvenDuration: u32,
    pub DependencyArrayUsed: u32,
    pub DependencyArrayCount: u32,
    pub DependencyArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_PLATFORM_STATE {
    pub StateIndex: u32,
    pub State: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PROVISIONING_SOC_IDENTIFIER {
    pub Length: u16,
    pub SocIdentifier: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_TEST_IDLE_STATE {
    pub ProcessorState: u32,
    pub PlatformState: u32,
    pub VetoReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_EXECUTE {
    pub Status: u32,
    pub ProcessorState: u32,
    pub PlatformState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_EXECUTE_V2 {
    pub Status: u32,
    pub ProcessorState: u32,
    pub PlatformState: u32,
    pub CoordinatedStateCount: u32,
    pub CoordinatedStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_COMPLETE {
    pub ProcessorState: u32,
    pub PlatformState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_COMPLETE_V2 {
    pub ProcessorState: u32,
    pub PlatformState: u32,
    pub CoordinatedStateCount: u32,
    pub CoordinatedStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IS_PROCESSOR_HALTED {
    pub Halted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_INITIATE_WAKE {
    pub NeedInterruptForCompletion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_FEEDBACK_COUNTER {
    pub Affinitized: u32,
    pub Type: u32,
    pub Counter: u32,
    pub DiscountIdle: u32,
    pub Reserved: u32,
    pub NominalRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_FEEDBACK_COUNTERS {
    pub Count: u32,
    pub Counters: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_FEEDBACK_READ {
    pub CounterIndex: u32,
    pub InstantaneousValue: u32,
    pub NominalCount: u32,
    pub ActualCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_PERF_CAPABILITIES {
    pub HighestPerformance: u32,
    pub NominalPerformance: u32,
    pub LowestNonlinearPerformance: u32,
    pub LowestPerformance: u32,
    pub DomainId: u32,
    pub DomainMembers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PERF_CONSTRAINTS {
    pub GuaranteedPerformanceLimit: u32,
    pub LimitReasons: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PERF_SET {
    pub MinimumPerformance: u32,
    pub MaximumPerformance: u32,
    pub DesiredPerformance: u32,
    pub TimeWindow: u32,
    pub PerformanceTolerance: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PERF_SET_V2 {
    pub MinimumPerformance: u32,
    pub MaximumPerformance: u32,
    pub DesiredPerformance: u32,
    pub TimeWindow: u32,
    pub PerformanceTolerance: u32,
    pub EnergyPerformancePreference: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_PARK_PREFERENCE {
    pub Processor: usize,
    pub KernelContext: usize,
    pub PoPreference: u8,
    pub PepPreference: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PARK_SELECTION {
    pub AdditionalUnparkedProcessors: u32,
    pub Count: u32,
    pub Processors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PARK_SELECTION_V2 {
    pub AdditionalUnparkedProcessors: u32,
    pub Count: u32,
    pub Processors: u32,
    pub EvaluationTime: u64,
    pub EvaluationType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_PARK_STATE {
    pub Processor: usize,
    pub KernelContext: usize,
    pub Parked: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PARK_MASK {
    pub Count: u32,
    pub EvaluationTime: u64,
    pub Processors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PERF_CHECK_COMPLETE {
    pub EvaluationTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_CST_STATE {
    pub Type: u8,
    pub Latency: u16,
    pub Power: u32,
    pub AddressSpaceId: u8,
    pub BitWidth: u8,
    pub BitOffset: u8,
    pub AccessSize: u8,
    pub Address: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_CST_STATES {
    pub Count: u32,
    pub IdleStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PLATFORM_STATE_RESIDENCY {
    pub Residency: u32,
    pub TransitionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PLATFORM_STATE_RESIDENCIES {
    pub Count: u32,
    pub States: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_VETO_REASONS {
    pub VetoReasonCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_VETO_REASON {
    pub VetoReason: u32,
    pub NameSize: u16,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_STATE_NAME {
    pub StateIndex: u32,
    pub NameSize: u16,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COORDINATED_IDLE_STATE {
    pub Latency: u32,
    pub BreakEvenDuration: u32,
    pub DependencyCount: u32,
    pub MaximumDependencySize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_COORDINATED_STATES {
    pub Count: u32,
    pub States: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COORDINATED_DEPENDENCY_OPTION {
    pub ExpectedStateIndex: u8,
    pub LooseDependency: u8,
    pub InitiatingState: u8,
    pub DependentState: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_COORDINATED_DEPENDENCY {
    pub StateIndex: u32,
    pub DependencyIndex: u32,
    pub DependencySize: u32,
    pub DependencySizeUsed: u32,
    pub TargetProcessor: usize,
    pub DeviceContext: usize,
    pub Options: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_SUPPORTED {
    pub OverrideLpiStates: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_REGISTER {
    pub AddressSpaceID: u8,
    pub BitWidth: u8,
    pub BitOffset: u8,
    pub AccessSize: u8,
    pub Address: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PROCESSOR_LPI_STATE {
    pub MinResidency: u32,
    pub WakeupLatency: u32,
    pub AsUlong: u32,
    pub Enabled: u32,
    pub Reserved: u32,
    pub AsUlong_1: u32,
    pub CoreContextLost: u32,
    pub TraceContextLost: u32,
    pub GICR: u32,
    pub GICD: u32,
    pub Reserved_1: u32,
    pub ResidencyCounterFrequency: u32,
    pub EntryMethod: usize,
    pub ResidencyCounter: usize,
    pub UsageCounter: usize,
    pub StateName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_PROCESSOR_STATES {
    pub Count: u32,
    pub LpiStates: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_COORDINATED_DEPENDENCY {
    pub TargetProcessor: usize,
    pub KernelContext: usize,
    pub OptionCount: u32,
    pub Options: *mut u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_COORDINATED_LPI_STATE {
    pub MinResidency: u32,
    pub WakeupLatency: u32,
    pub AsUlong: u32,
    pub Enabled: u32,
    pub Reserved: u32,
    pub AsUlong_1: u32,
    pub CoreContextLost: u32,
    pub TraceContextLost: u32,
    pub GICR: u32,
    pub GICD: u32,
    pub Reserved_1: u32,
    pub ResidencyCounterFrequency: u32,
    pub IntegerEntryMethod: u8,
    pub AsRegister: usize,
    pub AsInteger: u32,
    pub ResidencyCounter: usize,
    pub UsageCounter: usize,
    pub StateName: usize,
    pub DependencyCount: u32,
    pub Dependencies: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_COORDINATED_STATES {
    pub Count: u32,
    pub LpiStates: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_PRE_EXECUTE {
    pub Status: u32,
    pub CoordinatedStateCount: u32,
    pub CoordinatedStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_LPI_COMPLETE {
    pub CoordinatedStateCount: u32,
    pub CoordinatedStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_ENTER_SYSTEM_STATE {
    pub TargetState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_RESUME_FROM_SYSTEM_STATE {
    pub TargetState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_PERF_STATE {
    pub Performance: u32,
    pub Frequency: u32,
    pub Reserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_DISCRETE_PERF_STATES {
    pub Count: u32,
    pub States: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_DOMAIN_INFO {
    pub DomainId: u32,
    pub CoordinationType: u8,
    pub IdleProcessorsDiscounted: u8,
    pub SchedulerDirectedTransitionsSupported: u8,
    pub AffinitizePerfSet: u8,
    pub WorstCaseTransitionLatency: u32,
    pub WorstCaseTransitionOverhead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_PERF_SET_STATE {
    pub MinimumPerformanceState: u8,
    pub MaximumPerformanceState: u8,
    pub DesiredPerformanceState: u8,
    pub EnergyPerformancePreference: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_PERF_EPP_SUPPORT {
    pub EnergyPerformancePreferenceSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_PERF_AUTONOMOUS_SUPPORT {
    pub AutonomousCapability: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_VETO_REASON_ID {
    pub VetoIdentifier: u32,
    pub NameSize: u16,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_ALLOCATE_VETO_REASONS {
    pub BaseVetoReason: u32,
    pub VetoCount: u32,
    pub VetoReasons: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WPS_QUERY_CAPABILITIES {
    pub WpsSupported: u8,
    pub WpsLlcContainmentSupported: u8,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_WPS_INFORMATION {
    pub PerformanceRankingScore: u8,
    pub EfficiencyRankingScore: u8,
    pub ParkingPreference: usize,
    pub ParkPreferenceReason: usize,
    pub UnparkOrderingValue: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_WPS_TABLE_ENTRY {
    pub Processor: usize,
    pub KernelContext: usize,
    pub WpsInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_WPS_TABLE {
    pub EntryCount: u32,
    pub WpsTableEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_INFORMATION {
    pub Version: u16,
    pub Size: u16,
    pub AcceptDeviceNotification: usize,
    pub AcceptProcessorNotification: usize,
    pub AcceptAcpiNotification: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_CORE_DEVICE {
    pub Version: u32,
    pub ComponentCount: u32,
    pub ComponentActiveConditionCallback: usize,
    pub ComponentIdleConditionCallback: usize,
    pub ComponentCriticalTransitionCallback: usize,
    pub ComponentIdleStateCallback: usize,
    pub DeviceContext: usize,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_PREPARE_DEVICE {
    pub AcpiDeviceName: usize,
    pub InputFlags: u32,
    pub DeviceAccepted: u8,
    pub OutputFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_ABANDON_DEVICE {
    pub AcpiDeviceName: usize,
    pub DeviceAccepted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_REGISTER_DEVICE {
    pub AcpiDeviceName: usize,
    pub InputFlags: u32,
    pub KernelHandle: usize,
    pub DeviceHandle: usize,
    pub OutputFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_UNREGISTER_DEVICE {
    pub DeviceHandle: usize,
    pub InputFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_OBJECT_NAME_WITH_TYPE {
    pub Name: usize,
    pub Type: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_ENUMERATE_DEVICE_NAMESPACE {
    pub DeviceHandle: usize,
    pub RequestFlags: u32,
    pub Status: u32,
    pub ObjectCount: u32,
    pub ObjectBufferSize: usize,
    pub Objects: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_QUERY_OBJECT_INFORMATION {
    pub DeviceHandle: usize,
    pub Name: usize,
    pub Type: usize,
    pub ObjectFlags: u32,
    pub InputArgumentCount: u32,
    pub OutputArgumentCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_EVALUATE_CONTROL_METHOD {
    pub DeviceHandle: usize,
    pub RequestFlags: u32,
    pub MethodName: u32,
    pub MethodNameString: u32,
    pub MethodStatus: u32,
    pub CompletionContext: usize,
    pub InputArgumentCount: u32,
    pub InputArgumentSize: usize,
    pub InputArguments: usize,
    pub OutputArgumentCount: u32,
    pub OutputArgumentSize: usize,
    pub OutputArguments: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_QUERY_DEVICE_CONTROL_RESOURCES {
    pub DeviceHandle: usize,
    pub RequestFlags: u32,
    pub Status: u32,
    pub BiosResourcesSize: usize,
    pub BiosResources: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_TRANSLATED_DEVICE_CONTROL_RESOURCES {
    pub DeviceHandle: usize,
    pub RequestFlags: u32,
    pub Status: u32,
    pub TranslatedResourcesSize: usize,
    pub TranslatedResources: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_IO_MEMORY_RESOURCE {
    pub Type: usize,
    pub Information: u8,
    pub MinimumAddress: u64,
    pub MaximumAddress: u64,
    pub Alignment: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_INTERRUPT_RESOURCE {
    pub Type: usize,
    pub InterruptType: u32,
    pub InterruptPolarity: u32,
    pub Flags: usize,
    pub Count: u8,
    pub Pins: *mut u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_GPIO_RESOURCE {
    pub Type: usize,
    pub Flags: usize,
    pub InterruptType: u32,
    pub InterruptPolarity: u32,
    pub PinConfig: u32,
    pub IoRestrictionType: u32,
    pub DriveStrength: u16,
    pub DebounceTimeout: u16,
    pub PinTable: *mut u16,
    pub PinCount: u16,
    pub ResourceSourceIndex: u8,
    pub ResourceSourceName: usize,
    pub VendorData: *mut u8,
    pub VendorDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_SPB_RESOURCE {
    pub Type: usize,
    pub Flags: usize,
    pub TypeSpecificFlags: u16,
    pub ResourceSourceIndex: u8,
    pub ResourceSourceName: usize,
    pub VendorData: *mut i8,
    pub VendorDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_SPB_I2C_RESOURCE {
    pub SpbCommon: usize,
    pub ConnectionSpeed: u32,
    pub SlaveAddress: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_SPB_SPI_RESOURCE {
    pub SpbCommon: usize,
    pub ConnectionSpeed: u32,
    pub DataBitLength: u8,
    pub Phase: u8,
    pub Polarity: u8,
    pub DeviceSelection: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_SPB_UART_RESOURCE {
    pub SpbCommon: usize,
    pub BaudRate: u32,
    pub RxBufferSize: u16,
    pub TxBufferSize: u16,
    pub Parity: u8,
    pub LinesInUse: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_EXTENDED_ADDRESS {
    pub Type: usize,
    pub Flags: usize,
    pub ResourceFlags: u8,
    pub GeneralFlags: u8,
    pub TypeSpecificFlags: u8,
    pub RevisionId: u8,
    pub Reserved: u8,
    pub Granularity: u64,
    pub MinimumAddress: u64,
    pub MaximumAddress: u64,
    pub TranslationAddress: u64,
    pub AddressLength: u64,
    pub TypeAttribute: u64,
    pub DescriptorName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_ACPI_REQUEST_CONVERT_TO_BIOS_RESOURCES {
    pub TranslationStatus: u32,
    pub InputBuffer: usize,
    pub InputBufferSize: usize,
    pub OutputBuffer: usize,
    pub OutputBufferSize: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_KERNEL_INFORMATION_STRUCT_V1 {
    pub Version: u16,
    pub Size: u16,
    pub Plugin: usize,
    pub RequestWorker: usize,
    pub EnumerateUnmaskedInterrupts: usize,
    pub ProcessorHalt: usize,
    pub RequestInterrupt: usize,
    pub TransitionCriticalResource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_KERNEL_INFORMATION_STRUCT_V2 {
    pub Version: u16,
    pub Size: u16,
    pub Plugin: usize,
    pub RequestWorker: usize,
    pub EnumerateUnmaskedInterrupts: usize,
    pub ProcessorHalt: usize,
    pub RequestInterrupt: usize,
    pub TransitionCriticalResource: usize,
    pub ProcessorIdleVeto: usize,
    pub PlatformIdleVeto: usize,
    pub UpdateProcessorIdleState: usize,
    pub UpdatePlatformIdleState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_ACTIVE_COMPLETE {
    pub DeviceHandle: usize,
    pub Component: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_IDLE_STATE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_DEVICE_POWER {
    pub DeviceHandle: usize,
    pub PowerRequired: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_WORK_DEVICE_IDLE {
    pub DeviceHandle: usize,
    pub IgnoreIdleTimeout: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_COMPONENT_ACTIVE {
    pub DeviceHandle: usize,
    pub Component: u32,
    pub Active: u8,
    pub WorkInformation: usize,
    pub NeedWork: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_IDLE_STATE {
    pub Ulong: u32,
    pub Interruptible: u32,
    pub CacheCoherent: u32,
    pub ThreadContextRetained: u32,
    pub CStateType: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PROCESSOR_IDLE_CONSTRAINTS {
    pub IdleDuration: u64,
    pub Interruptible: u8,
    pub Type: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_IDLE_STATES {
    pub Count: u32,
    pub MaximumCoordinatedProcessors: u32,
    pub IdleStates: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_QUERY_LP_SETTINGS {
    pub RegistryRoot: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_SELECT {
    pub Constraints: usize,
    pub AbortTransition: u8,
    pub IdleStateIndex: u32,
    pub DependencyArrayUsed: u32,
    pub DependencyArrayCount: u32,
    pub DependencyArray: u32,
    pub PlatformIdleStateIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PEP_PPM_IDLE_CANCEL {
    pub CancelCode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PF_SET_EXTENSION_HOOK_INFO {
    pub ExtensionPointer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_COMPONENT_PROPERTY {
    pub Id: usize,
    pub IfIndex: u32,
    pub MiniportIfIndex: u32,
    pub LowerIfIndex: u32,
    pub VmsExtIfIndex: u32,
    pub LowestIfIndex: u32,
    pub NdisMedium: u32,
    pub IpIfIndex: u32,
    pub Vsid: u32,
    pub Vlan: u32,
    pub CompartmentId: u32,
    pub OptDataPath: u16,
    pub NdisObject: u16,
    pub EtherType: u16,
    pub IfGuid: u32,
    pub MacAddress: usize,
    pub VMSwitchName: [i8; 1],
    pub SockAddr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_COMPONENT_CONTEXT {
    pub ListLink: u32,
    pub EdgeList: u32,
    pub EdgeCount: i32,
    pub CompHandle: usize,
    pub CompType: usize,
    pub PacketType: usize,
    pub FlowEnabled: i32,
    pub DropEnabled: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_EDGE_CONTEXT {
    pub ListLink: u32,
    pub EdgeHandle: usize,
    pub field_CompContext: usize,
    pub PacketType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PACKET_HEADER_INFORMATION {
    pub SockAddrLocal: u32,
    pub SockAddrRemote: u32,
    pub IpProtocol: u8,
    pub PortLocal: u16,
    pub PortRemote: u16,
    pub PortLocal_1: u16,
    pub PortRemote_1: u16,
    pub Flags: u8,
    pub Type: u8,
    pub Code: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Udp {
    pub PortLocal: u16,
    pub PortRemote: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Tcp {
    pub PortLocal: u16,
    pub PortRemote: u16,
    pub Flags: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Icmp {
    pub Type: u8,
    pub Code: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_COMPONENT_IN {
    pub Header: usize,
    pub CompContext: usize,
    pub Name: usize,
    pub Description: usize,
    pub Type: usize,
    pub DirTagIn: usize,
    pub DirTagOut: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_EDGE_IN {
    pub Header: usize,
    pub Name: usize,
    pub DirTagIn: usize,
    pub DirTagOut: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_COMP_PROPERTY_IN {
    pub Header: usize,
    pub Id: usize,
    pub Value: usize,
    pub Size: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PACKET_HEADER_INFO {
    pub AddrFamily: u32,
    pub IpAddrLocal: usize,
    pub IpAddrRemote: usize,
    pub IpProtocol: u8,
    pub PortLocal: u16,
    pub PortRemote: u16,
    pub PortLocal_1: u16,
    pub PortRemote_1: u16,
    pub Flags: u8,
    pub Type: u8,
    pub Code: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PACKET_LOG_IN {
    pub Header: usize,
    pub Buffer: usize,
    pub BufferType: usize,
    pub PacketType: usize,
    pub Direction: usize,
    pub Flags: u32,
    pub PacketHeaderInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_DROP_REPORT_IN {
    pub Header: usize,
    pub DropReason: u32,
    pub LocationCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_CLIENT_COMP_ENABLE_IN {
    pub Header: usize,
    pub CompContext: usize,
    pub FlowEnabled: u8,
    pub DropEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PACKET_CONTEXT_IN {
    pub Header: usize,
    pub Id: i32,
    pub Value: usize,
    pub Size: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PROVIDER_DISPATCH {
    pub Size: u16,
    pub ComponentRegister: usize,
    pub ComponentUnregister: usize,
    pub SetCompProperty: usize,
    pub EdgeAdd: usize,
    pub PacketLog: usize,
    pub PacketDrop: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_CLIENT_DISPATCH {
    pub Size: u16,
    pub ClientEnable: usize,
    pub CompEnable: usize,
    pub CompClose: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_PROVIDER_SPECIFIC_CHARACTERISTICS {
    pub Header: usize,
    pub ProviderMajorVersion: u16,
    pub ProviderMinorVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_PROVIDER_CHARACTERISTICS {
    pub Header: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_DISPATCH_HEADER {
    pub Version: u32,
    pub Size: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_PROVIDER_DISPATCH {
    pub Header: usize,
    pub HandlerCount: u32,
    pub Handlers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_CLIENT_DISPATCH {
    pub Header: usize,
    pub HandlerCount: u32,
    pub Handlers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_CLIENT_DISPATCH_V2 {
    pub Header: usize,
    pub CaptureType: usize,
    pub HandlerCount: u32,
    pub Handlers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_CLIENT_CONTEXT {
    pub NmrClientHandle: usize,
    pub RundownRef: usize,
    pub Enabled: u8,
    pub ProviderContext: usize,
    pub ProviderDispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_EVT_STREAM_PACKET_DESCRIPTOR {
    pub PacketOriginalLength: u32,
    pub PacketLoggedLength: u32,
    pub PacketMetaDataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_EVT_STREAM_METADATA {
    pub PktGroupId: u64,
    pub PktCount: u32,
    pub AppearanceCount: u32,
    pub DirectionName: u32,
    pub PacketType: u32,
    pub ComponentId: u32,
    pub EdgeId: u32,
    pub FilterId: u32,
    pub DropReason: u32,
    pub DropLocation: u32,
    pub ProcNum: u32,
    pub TimeStamp: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_EVT_STREAM_PACKET_HEADER {
    pub EventId: u8,
    pub PacketDescriptor: usize,
    pub Metadata: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_CLIENT_REPORT_PACKET_DROP_OUT {
    pub BufferStart: usize,
    pub BufferEnd: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_NETEVT_CLIENT_REPORT_PACKET_EVENT_OUT {
    pub BufferStart: usize,
    pub BufferEnd: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PKTMON_HEADER {
    pub Size: u16,
    pub Version: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_REPORTED_CAPABILITIES {
    pub Flags: u32,
    pub MeasurementUnit: usize,
    pub MeasurementType: usize,
    pub Accuracy: u32,
    pub SamplingPeriod: u32,
    pub MinimumAverageInterval: u32,
    pub MaximumAverageInterval: u32,
    pub Hysteresis: u32,
    pub Writeable: u8,
    pub MinBudget: u32,
    pub MaxBudget: u32,
    pub ModelNumber: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub OEMInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_METERED_HARDWARE_INFORMATION {
    pub MeteredHardwareCount: u32,
    pub MeteredHardware: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_CAPABILITIES {
    pub Version: u32,
    pub Size: u32,
    pub CapabilityType: usize,
    pub ReportedCapabilities: usize,
    pub MeteredHardwareInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_MEASUREMENT_CONFIGURATION {
    pub AveragingInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_BUDGETING_CONFIGURATION {
    pub ConfiguredBudget: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_THRESHOLD_CONFIGURATION {
    pub LowerThreshold: u32,
    pub UpperThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_CONFIGURATION {
    pub Version: u32,
    pub Size: u16,
    pub ConfigurationType: usize,
    pub MeasurementConfiguration: usize,
    pub BudgetingConfiguration: usize,
    pub ThresholdConfiguration: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_MEASUREMENT_DATA {
    pub Version: u32,
    pub CurrentPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMI_EVENT {
    pub Version: u32,
    pub EventType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCPROPERTY_REQUEST {
    pub MajorTarget: usize,
    pub MinorTarget: usize,
    pub Node: u32,
    pub PropertyItem: usize,
    pub Verb: u32,
    pub InstanceSize: u32,
    pub Instance: usize,
    pub ValueSize: u32,
    pub Value: usize,
    pub Irp: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCMETHOD_REQUEST {
    pub MajorTarget: usize,
    pub MinorTarget: usize,
    pub Node: u32,
    pub MethodItem: usize,
    pub Verb: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCEVENT_REQUEST {
    pub MajorTarget: usize,
    pub MinorTarget: usize,
    pub Node: u32,
    pub EventItem: usize,
    pub EventEntry: usize,
    pub Verb: u32,
    pub Irp: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PC_POWER_FRAMEWORK_SETTINGS {
    pub Size: u32,
    pub EvtPcPostPoFxRegisterDevice: usize,
    pub EvtPcPrePoFxUnregisterDevice: usize,
    pub Component: usize,
    pub ComponentActiveConditionCallback: usize,
    pub ComponentIdleConditionCallback: usize,
    pub ComponentIdleStateCallback: usize,
    pub PowerControlCallback: usize,
    pub PoFxDeviceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCSTREAMRESOURCE_DESCRIPTOR {
    pub Size: u32,
    pub Flags: u32,
    pub Pdo: usize,
    pub Type: usize,
    pub Version: u32,
    pub Generic: usize,
    pub Thread: usize,
    pub ResourceSet: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCNOTIFICATION_BUFFER {
    pub NotificationBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_CONNECTION_ID {
    pub SessionID: u32,
    pub Luid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_PREFIX_ENTRY {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub CaseInsensitiveLength: u16,
    pub Spare1: u16,
    pub SavedHashValue: u32,
    pub HashLinks: u32,
    pub MemberQLinks: u32,
    pub Prefix: u32,
    pub ContainerRefCount: *mut u32,
    pub ContainingRecord: usize,
    pub Context: usize,
    pub ConnectionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_PREFIX_TABLE {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub Version: u32,
    pub MemberQueue: u32,
    pub TableLock: u32,
    pub TableEntryForNull: usize,
    pub CaseInsensitiveMatch: u8,
    pub IsNetNameTable: u8,
    pub TableSize: u32,
    pub Lookups: u32,
    pub FailedLookups: u32,
    pub Considers: u32,
    pub Compares: u32,
    pub HashBuckets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PTP_VENDOR_DATA_IN {
    pub OpCode: u16,
    pub SessionId: u32,
    pub TransactionId: u32,
    pub Params: [u32; 1],
    pub NumParams: u32,
    pub NextPhase: u32,
    pub VendorWriteData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PTP_VENDOR_DATA_OUT {
    pub ResponseCode: u16,
    pub SessionId: u32,
    pub TransactionId: u32,
    pub Params: [u32; 1],
    pub VendorReadData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_SIGNATURE {
    pub Type: u16,
    pub Size: u32,
    pub Signature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_TRANSPORT {
    pub Name: u32,
    pub pDeviceObject: usize,
    pub ControlChannel: usize,
    pub pControlChannelFileObject: usize,
    pub pProviderInfo: usize,
    pub ConnectionCount: i32,
    pub VirtualCircuitCount: i32,
    pub QualityOfService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_ADDRESS {
    pub pTransport: usize,
    pub pTransportAddress: usize,
    pub pContext: usize,
    pub pHandler: usize,
    pub pReceiveMdl: usize,
    pub hAddress: usize,
    pub pFileObject: usize,
    pub ConnectionCount: i32,
    pub VirtualCircuitCount: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_CONNECTION {
    pub pAddress: usize,
    pub VirtualCircuitCount: u32,
    pub pContext: usize,
    pub pHandler: usize,
    pub pConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_CONNECTION_COMPLETION_CONTEXT {
    pub Status: u32,
    pub AddressIndex: u32,
    pub pConnection: usize,
    pub pVc: usize,
    pub WorkQueueItem: u32,
    pub pConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_VC {
    pub pConnection: usize,
    pub hEndpoint: usize,
    pub pEndpointFileObject: usize,
    pub State: u32,
    pub ConnectionId: u32,
    pub pReceiveMdl: usize,
    pub pCleanUpEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_TRANSPORT_INFORMATION {
    pub ConnectionCount: u32,
    pub QualityOfService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_ADDRESS_EVENT_HANDLER {
    pub RxCeErrorEventHandler: usize,
    pub RxCeReceiveDatagramEventHandler: usize,
    pub RxCeDataReadyEventHandler: usize,
    pub RxCeSendPossibleEventHandler: usize,
    pub RxCeSendCompleteEventHandler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RXCE_CONNECTION_EVENT_HANDLER {
    pub RxCeDisconnectEventHandler: usize,
    pub RxCeErrorEventHandler: usize,
    pub RxCeReceiveEventHandler: usize,
    pub RxCeReceiveDatagramEventHandler: usize,
    pub RxCeReceiveExpeditedEventHandler: usize,
    pub RxCeSendPossibleEventHandler: usize,
    pub RxCeDataReadyEventHandler: usize,
    pub RxCeSendCompleteEventHandler: usize,
    pub RxCeSendSubmittedEventHandler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_TOPLEVELIRP_CONTEXT {
    pub ListEntry: u32,
    pub Signature: u32,
    pub RxDeviceObject: usize,
    pub RxContext: usize,
    pub Irp: usize,
    pub Flags: u32,
    pub Previous: usize,
    pub Thread: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_FCBTRACKER_CALLINFO {
    pub AcquireRelease: u32,
    pub SavedTrackerValue: u16,
    pub LineNumber: u16,
    pub FileName: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NT_CREATE_PARAMETERS {
    pub DesiredAccess: u32,
    pub AllocationSize: i64,
    pub FileAttributes: u32,
    pub ShareAccess: u32,
    pub Disposition: u32,
    pub CreateOptions: u32,
    pub SecurityContext: usize,
    pub ImpersonationLevel: u32,
    pub DfsContext: usize,
    pub DfsNameContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_CONTEXT {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub ReferenceCount: u32,
    pub ContextListEntry: u32,
    pub MajorFunction: u8,
    pub MinorFunction: u8,
    pub PendingReturned: u8,
    pub PostRequest: u8,
    pub RealDevice: usize,
    pub CurrentIrp: usize,
    pub CurrentIrpSp: usize,
    pub pFcb: usize,
    pub pFobx: usize,
    pub pRelevantSrvOpen: usize,
    pub NonPagedFcb: usize,
    pub RxDeviceObject: usize,
    pub OriginalThread: usize,
    pub LastExecutionThread: usize,
    pub LockManagerContext: u32,
    pub RdbssDbgExtension: usize,
    pub ScavengerEntry: u32,
    pub SerialNumber: u32,
    pub FobxSerialNumber: u32,
    pub Flags: u32,
    pub FcbResourceAcquired: u8,
    pub FcbPagingIoResourceAcquired: u8,
    pub MustSucceedDescriptorNumber: u8,
    pub StoredStatus: u32,
    pub StoredStatusAlignment: usize,
    pub InformationToReturn: usize,
    pub IoStatusBlock: u32,
    pub ForceLonglongAligmentDummyField: u64,
    pub MRxContext: [usize; 1],
    pub WriteOnlyOpenRetryContext: usize,
    pub MRxCancelRoutine: usize,
    pub ResumeRoutine: usize,
    pub WorkQueueItem: u32,
    pub OverflowListEntry: u32,
    pub SyncEvent: u32,
    pub BlockedOperations: u32,
    pub BlockedOpsMutex: usize,
    pub RxContextSerializationQLinks: u32,
    pub FsInformationClass: u32,
    pub FileInformationClass: u32,
    pub Buffer: usize,
    pub Length: i32,
    pub LengthRemaining: i32,
    pub ReplaceIfExists: u8,
    pub AdvanceOnly: u8,
    pub SuppliedPathName: u32,
    pub NetRootType: u32,
    pub pSecurityContext: usize,
    pub NtCreateParameters: u32,
    pub ReturnedCreateInformation: u32,
    pub CanonicalNameBuffer: usize,
    pub NetNamePrefixEntry: usize,
    pub pSrvCall: usize,
    pub pNetRoot: usize,
    pub pVNetRoot: usize,
    pub EaBuffer: usize,
    pub EaLength: u32,
    pub SdLength: u32,
    pub PipeType: u32,
    pub PipeReadMode: u32,
    pub PipeCompletionMode: u32,
    pub Flags_1: u16,
    pub Type: u32,
    pub RdrFlags: u8,
    pub FcbAcquired: u8,
    pub TryForScavengingOnSharingViolation: u8,
    pub ScavengingAlreadyTried: u8,
    pub ThisIsATreeConnectOpen: u8,
    pub TreeConnectOpenDeferred: u8,
    pub TransportName: u32,
    pub UserName: u32,
    pub Password: u32,
    pub UserDomainName: u32,
    pub FileIndex: u32,
    pub RestartScan: u8,
    pub ReturnSingleEntry: u8,
    pub IndexSpecified: u8,
    pub InitialQuery: u8,
    pub pVNetRoot_1: usize,
    pub UserEaList: *mut u8,
    pub UserEaListLength: u32,
    pub UserEaIndex: u32,
    pub RestartScan_1: u8,
    pub ReturnSingleEntry_1: u8,
    pub IndexSpecified_1: u8,
    pub SecurityInformation: u32,
    pub Length_1: u32,
    pub SecurityInformation_1: u32,
    pub SecurityDescriptor: usize,
    pub Length_2: u32,
    pub StartSid: usize,
    pub SidList: usize,
    pub SidListLength: u32,
    pub RestartScan_2: u8,
    pub ReturnSingleEntry_2: u8,
    pub IndexSpecified_2: u8,
    pub Length_3: u32,
    pub VNetRoot: usize,
    pub SrvCall: usize,
    pub NetRoot: usize,
    pub FlagsForLowIo: u32,
    pub LowIoContext: u32,
    pub AlsoCanonicalNameBuffer: usize,
    pub LoudCompletionString: usize,
    pub AcquireReleaseFcbTrackerX: u32,
    pub TrackerHistoryPointer: u32,
    pub TrackerHistory: [u32; 1],
    pub ShadowCritOwner: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_IRP_LIST_ITEM {
    pub IrpsList: u32,
    pub pIrp: usize,
    pub CopyDataBuffer: usize,
    pub Completed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDBSS_EXPORTS {
    pub pRxStrucSupSpinLock: usize,
    pub pRxDebugTraceIndent: *mut i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDBSS_DATA {
    pub NodeTypeCode: u32,
    pub NodeByteSize: u32,
    pub DriverObject: usize,
    pub NumberOfMinirdrsStarted: u32,
    pub MinirdrRegistrationMutex: u32,
    pub RegisteredMiniRdrs: u32,
    pub NumberOfMinirdrsRegistered: i32,
    pub OurProcess: usize,
    pub CacheManagerCallbacks: usize,
    pub Resource: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDBSS_STARTSTOP_CONTEXT {
    pub State: u32,
    pub Version: u32,
    pub pStopContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_CALLDOWN_COMPLETION_CONTEXT {
    pub WaitCount: i32,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MRX_CALLDOWN_CONTEXT {
    pub WorkQueueItem: u32,
    pub pMRxDeviceObject: usize,
    pub pCompletionContext: usize,
    pub pRoutine: usize,
    pub CompletionStatus: u32,
    pub pParameter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_DISPATCHER_CONTEXT {
    pub NumberOfWorkerThreads: u32,
    pub pTearDownEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDBSS_DEVICE_OBJECT {
    pub DeviceObject: u32,
    pub RegistrationControls: u32,
    pub RdbssExports: usize,
    pub RDBSSDeviceObject: usize,
    pub Dispatch: usize,
    pub DeviceName: u32,
    pub NetworkProviderPriority: u32,
    pub MupHandle: usize,
    pub RegisterUncProvider: u8,
    pub RegisterMailSlotProvider: u8,
    pub RegisteredAsFileSystem: u8,
    pub Unused: u8,
    pub MiniRdrListLinks: u32,
    pub NumberOfActiveFcbs: u32,
    pub NumberOfActiveContexts: u32,
    pub PagingReadBytesRequested: i64,
    pub NonPagingReadBytesRequested: i64,
    pub CacheReadBytesRequested: i64,
    pub FastReadBytesRequested: i64,
    pub NetworkReadBytesRequested: i64,
    pub ReadOperations: u32,
    pub FastReadOperations: u32,
    pub RandomReadOperations: u32,
    pub PagingWriteBytesRequested: i64,
    pub NonPagingWriteBytesRequested: i64,
    pub CacheWriteBytesRequested: i64,
    pub FastWriteBytesRequested: i64,
    pub NetworkWriteBytesRequested: i64,
    pub WriteOperations: u32,
    pub FastWriteOperations: u32,
    pub RandomWriteOperations: u32,
    pub PostedRequestCount: [u32; 1],
    pub OverflowQueueCount: [i32; 1],
    pub OverflowQueue: [u32; 1],
    pub OverflowQueueSpinLock: u32,
    pub AsynchronousRequestsPending: i32,
    pub pAsynchronousRequestsCompletionEvent: usize,
    pub StartStopContext: u32,
    pub DispatcherContext: u32,
    pub pRxNetNameTable: usize,
    pub RxNetNameTableInDeviceObject: u32,
    pub pRdbssScavenger: usize,
    pub RdbssScavengerInDeviceObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_ITEM {
    pub WorkQueueItem: u32,
    pub LastTick: u32,
    pub Options: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_TRACE_CONTROLPOINT {
    pub ControlPointNumber: u32,
    pub Name: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_BINDING_HANDLE {
    pub pTdiEmulationContext: u32,
    pub pTransportInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_BINDING_CONTEXT {
    pub pTransportName: usize,
    pub QualityOfService: u32,
    pub pBindHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_QUEUE_ITEM {
    pub pDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_DISPATCH_ITEM {
    pub WorkQueueItem: u32,
    pub DispatchRoutine: usize,
    pub DispatchRoutineParameter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_QUEUE_RUNDOWN_CONTEXT {
    pub RundownCompletionEvent: u32,
    pub NumberOfThreadsSpunDown: i32,
    pub field_ThreadPointers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_QUEUE {
    pub State: u16,
    pub SpinUpRequestPending: u8,
    pub Type: u8,
    pub SpinLock: u32,
    pub pRundownContext: usize,
    pub NumberOfWorkItemsDispatched: u32,
    pub NumberOfWorkItemsToBeDispatched: u32,
    pub CumulativeQueueLength: i32,
    pub NumberOfSpinUpRequests: i32,
    pub MaximumNumberOfWorkerThreads: i32,
    pub MinimumNumberOfWorkerThreads: i32,
    pub NumberOfActiveWorkerThreads: u32,
    pub NumberOfIdleWorkerThreads: u32,
    pub NumberOfFailedSpinUpRequests: i32,
    pub WorkQueueItemForSpinUpWorkerThreadInUse: u32,
    pub WorkQueueItemForTearDownWorkQueue: u32,
    pub WorkQueueItemForSpinUpWorkerThread: u32,
    pub WorkQueueItemForSpinDownWorkerThread: u32,
    pub Queue: u32,
    pub field_ThreadPointers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_WORK_QUEUE_DISPATCHER {
    pub WorkQueue: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_DISPATCHER {
    pub NumberOfProcessors: i32,
    pub OwnerProcess: usize,
    pub pWorkQueueDispatcher: usize,
    pub State: u32,
    pub SpinUpRequests: u32,
    pub SpinUpRequestsLock: u32,
    pub SpinUpRequestsEvent: u32,
    pub SpinUpRequestsTearDownEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RX_SCAVENGER_ENTRY {
    pub List: u32,
    pub Type: u8,
    pub Operation: u8,
    pub State: u8,
    pub Flags: u8,
    pub field_pContinuationEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDBSS_SCAVENGER {
    pub State: u32,
    pub MaximumNumberOfDormantFiles: i32,
    pub NumberOfDormantFiles: u32,
    pub TimeLimit: i64,
    pub SrvCallsToBeFinalized: u32,
    pub NetRootsToBeFinalized: u32,
    pub VNetRootsToBeFinalized: u32,
    pub FcbsToBeFinalized: u32,
    pub SrvOpensToBeFinalized: u32,
    pub FobxsToBeFinalized: u32,
    pub SrvCallFinalizationList: u32,
    pub NetRootFinalizationList: u32,
    pub VNetRootFinalizationList: u32,
    pub FcbFinalizationList: u32,
    pub SrvOpenFinalizationList: u32,
    pub FobxFinalizationList: u32,
    pub ClosePendingFobxsList: u32,
    pub WorkItem: u32,
    pub SyncEvent: u32,
    pub ScavengeEvent: u32,
    pub CurrentScavengerThread: usize,
    pub CurrentNetRootForClosePendingProcessing: usize,
    pub CurrentFcbForClosePendingProcessing: usize,
    pub ClosePendingProcessingSyncEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PURGE_SYNCHRONIZATION_CONTEXT {
    pub ContextsAwaitingPurgeCompletion: u32,
    pub PurgeInProgress: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_WMILIB_CONTEXT {
    pub GuidCount: u32,
    pub GuidList: usize,
    pub QueryWmiRegInfo: usize,
    pub QueryWmiDataBlock: usize,
    pub SetWmiDataBlock: usize,
    pub SetWmiDataItem: usize,
    pub ExecuteWmiMethod: usize,
    pub WmiFunctionControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_STREAMING_FUNCTION_INFORMATION {
    pub Size: u32,
    pub Flags: u32,
    pub FunctionInformationId: u32,
    pub FunctionNumber: u32,
    pub FunctionSdcaVersion: u32,
    pub FunctionSdcaRevision: u32,
    pub FunctionType: u32,
    pub FunctionManufacturerId: u32,
    pub FunctionId: u32,
    pub FunctionVersion: u32,
    pub SubSystemId: u32,
    pub ControllerId: u32,
    pub LinkId: u32,
    pub PeripheralId: u32,
    pub UniqueId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_PATH_DESCRIPTOR2 {
    pub Size: u32,
    pub Version: u32,
    pub FunctionInformationId: u32,
    pub TerminalEntityId: u32,
    pub DataPortMap: u32,
    pub DataPortConfig: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_PATH_DESCRIPTORS2 {
    pub Size: u32,
    pub Version: u32,
    pub SdcaPath: u32,
    pub EndpointId: u32,
    pub SpecialPathFormat: u32,
    pub Reserved: [u8; 1],
    pub DescriptorCount: u32,
    pub Descriptor: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_KWS_CAPABILITY_DESCRIPTOR {
    pub DataPathsSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VAD_DESCRIPTOR {
    pub VadInterruptPosition: u32,
    pub SupportedHistoryBufferModes: u32,
    pub HistoryBufferPreamble: u32,
    pub HistoryBufferPreambleOverFlow: u32,
    pub HistoryBufferMessageOffsetAccessMode: u32,
    pub HistoryBufferMessageOffsetDCValue: u32,
    pub HistoryBufferMessageLengthAccessMode: u32,
    pub HistoryBufferMessageLengthDCValue: u32,
    pub FormatCount: u32,
    pub Format: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENTITY_INFO {
    pub EntityType: u32,
    pub EntityId: u32,
    pub EntityLabel: [u32; 1],
    pub InputPin: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VAD_ENTITIES {
    pub EntityCount: u32,
    pub SDCAVadEntities: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_KWS_NOTIFICATIONS {
    pub Suspend: u32,
    pub Resume: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_KWS_PREPARE_PARAMS {
    pub VadMode: u32,
    pub EndpointId: u32,
    pub DetectionFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_INTERRUPT_INFO {
    pub Size: u32,
    pub SCPInterruptMask: u32,
    pub DataPortInterrupts: [u32; 1],
    pub SDCAInterruptMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_POWER {
    pub PowerDomainEntityId: u32,
    pub OldState: u32,
    pub NewState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_JACK_DETECT {
    pub GroupEntityId: u32,
    pub DetectedMode: u32,
    pub JackEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_COMMIT_GROUP {
    pub CommitGroupHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_FDL_BEGIN {
    pub FdlEntityId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_FDL_END {
    pub FdlEntityId: u32,
    pub FdlStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_NOTIFICATION_POSTURE {
    pub Posture: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_ACX_CIRCUIT_CONFIG {
    pub cbSize: u32,
    pub ComponentID: u32,
    pub ComponentUri: u32,
    pub ContainerID: u32,
    pub CircuitName: u32,
    pub CircuitType: u32,
    pub CircuitContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_INTERFACE_V0100 {
    pub InterfaceHeader: u32,
    pub EvtSetHwConfig: usize,
    pub EvtSetEndpointConfig: usize,
    pub EvtRemoveEndpointConfig: usize,
    pub EvtInterruptHandler: usize,
    pub EvtChangeNotification: usize,
    pub EvtSetXUEntities: usize,
    pub EvtRegisterForInterrupts: usize,
    pub EvtSetJackOverride: usize,
    pub EvtSetJackSelectedMode: usize,
    pub EvtPDEPowerReferenceAcquire: usize,
    pub EvtPDEPowerReferenceRelease: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_INTERFACE_V0101 {
    pub InterfaceHeader: u32,
    pub EvtSetHwConfig: usize,
    pub EvtSetEndpointConfig: usize,
    pub EvtRemoveEndpointConfig: usize,
    pub EvtInterruptHandler: usize,
    pub EvtChangeNotification: usize,
    pub EvtSetXUEntities: usize,
    pub EvtRegisterForInterrupts: usize,
    pub EvtSetJackOverride: usize,
    pub EvtSetJackSelectedMode: usize,
    pub EvtPDEPowerReferenceAcquire: usize,
    pub EvtPDEPowerReferenceRelease: usize,
    pub EvtReadDeferredAudioControls: usize,
    pub EvtWriteDeferredAudioControls: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCAXU_INTERFACE_V0102 {
    pub InterfaceHeader: u32,
    pub EvtSetHwConfig: usize,
    pub EvtSetEndpointConfig: usize,
    pub EvtRemoveEndpointConfig: usize,
    pub EvtInterruptHandler: usize,
    pub EvtChangeNotification: usize,
    pub EvtRetrieveSwftFileOverride: usize,
    pub EvtSetXUEntities: usize,
    pub EvtRegisterForInterrupts: usize,
    pub EvtSetJackOverride: usize,
    pub EvtSetJackSelectedMode: usize,
    pub EvtPDEPowerReferenceAcquire: usize,
    pub EvtPDEPowerReferenceRelease: usize,
    pub EvtReadDeferredAudioControls: usize,
    pub EvtWriteDeferredAudioControls: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDRESP_TYPE3 {
    pub Reserved1: u32,
    pub VoltageProfile: u32,
    pub Reserved2: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OCR_REGISTER_V2 {
    pub Reserved1: u32,
    pub VoltageProfile1: u32,
    pub Reserved2: u32,
    pub VoltageProfile2: u32,
    pub Reserved3: u32,
    pub CardCapacityStatus: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCMD_DESCRIPTOR {
    pub Cmd: u32,
    pub CmdClass: u32,
    pub TransferDirection: u32,
    pub TransferType: u32,
    pub ResponseType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_RW_DIRECT_ARGUMENT {
    pub Data: u32,
    pub Reserved1: u32,
    pub Address: u32,
    pub Reserved2: u32,
    pub ReadAfterWrite: u32,
    pub Function: u32,
    pub WriteToDevice: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_RW_EXTENDED_ARGUMENT {
    pub Count: u32,
    pub Address: u32,
    pub OpCode: u32,
    pub BlockMode: u32,
    pub Function: u32,
    pub WriteToDevice: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_SOCKET_DATA {
    pub HostRegisterPhysicalBase: u64,
    pub HostRegisterPhysicalBaseOriginal: u64,
    pub TransferMethod: u32,
    pub SpeedMode: u32,
    pub BusClock: u32,
    pub NumHpiIOs: u32,
    pub SupportedSpeedModes: u16,
    pub SupportedCardSpeedModes: u16,
    pub NonremovableDevice: u8,
    pub Support8BitBus: u8,
    pub SupportWideBus: u8,
    pub SupportHpi: u8,
    pub UseTuningForSDR50: u8,
    pub NeedsSoftwareRetuner: u8,
    pub SupportAutoCmd23: u8,
    pub SupportAutoCmd12: u8,
    pub SupportGPIOCardDetection: u8,
    pub SupportWriteProtect: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDBUS_ADAPTER_SOCKET_INFO {
    pub SocketData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDSTOR_DEVICE_INFO {
    pub PackedCommandLookaheadCount: u32,
    pub PackedCommandLookaheadFoundCount: u32,
    pub PackedCommandReadIssuedCount: u32,
    pub PackedCommandWriteIssuedCount: u32,
    pub PackedCommandBackedOutCount: u32,
    pub PackedCommandReadCompletedCount: u32,
    pub PackedCommandWriteCompletedCount: u32,
    pub PackedCommandLastPackedReadIrpsCount: u32,
    pub PackedCommandLastPackedWriteIrpsCount: u32,
    pub SupportPacked: u8,
    pub HostSupportHpi: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDP_NODE_HEADER {
    pub Link: u32,
    pub Type: u16,
    pub SpecificType: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDP_NODE {
    pub hdr: u32,
    pub DataSize: u32,
    pub u: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDP_TREE_ROOT_NODE {
    pub RootNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_COMMAND {
    pub Index: u32,
    pub Type: u32,
    pub Class: u32,
    pub ResponseType: u32,
    pub TransferType: u32,
    pub TransferDirection: u32,
    pub TransferMethod: u32,
    pub Argument: u32,
    pub UseAutoCmd12: u8,
    pub UseAutoCmd23: u8,
    pub BlockSize: u16,
    pub BlockCount: u16,
    pub Length: u32,
    pub DataBuffer: *mut u8,
    pub DmaVirtualAddress: usize,
    pub DmaPhysicalAddress: u64,
    pub ScatterGatherList: usize,
    pub ScatterGatherListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_REQUEST {
    pub Type: u32,
    pub Command: u32,
    pub RequiredEvents: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_BUS_OPERATION {
    pub Type: u32,
    pub ResetType: u32,
    pub FrequencyKhz: u32,
    pub Voltage: u32,
    pub PowerEnabled: u8,
    pub BusWidth: u32,
    pub BusSpeed: u32,
    pub SignalingVoltage: u32,
    pub DriveStrength: u8,
    pub DriverType: u32,
    pub PresetValueEnabled: u8,
    pub BlockGapIntEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_INITIALIZATION_DATA {
    pub StructureSize: u32,
    pub GetSlotCount: usize,
    pub GetSlotCapabilities: usize,
    pub Interrupt: usize,
    pub IssueRequest: usize,
    pub GetResponse: usize,
    pub RequestDpc: usize,
    pub ToggleEvents: usize,
    pub ClearEvents: usize,
    pub SaveContext: usize,
    pub RestoreContext: usize,
    pub Initialize: usize,
    pub IssueBusOperation: usize,
    pub GetCardDetectState: usize,
    pub GetWriteProtectState: usize,
    pub PowerControlCallback: usize,
    pub Cleanup: usize,
    pub PrivateExtensionSize: u32,
    pub CrashdumpSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_SLOT_EXTENSION {
    pub field_Miniport: u32,
    pub PrivateExtension: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_PCI_CONFIG_INFO {
    pub VendorID: u32,
    pub DeviceID: u32,
    pub RevisionID: u32,
    pub SubsysID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_CAPABILITIES {
    pub SpecVersion: u8,
    pub MaximumOutstandingRequests: u8,
    pub MaximumBlockSize: u16,
    pub MaximumBlockCount: u16,
    pub BaseClockFrequencyKhz: u32,
    pub TuningTimerCountInSeconds: u32,
    pub DmaDescriptorSize: u32,
    pub AlignmentRequirement: u32,
    pub PioTransferMaxThreshold: u32,
    pub ScatterGatherDma: u32,
    pub Address64Bit: u32,
    pub BusWidth8Bit: u32,
    pub HighSpeed: u32,
    pub SignalingVoltage18V: u32,
    pub SDR50: u32,
    pub DDR50: u32,
    pub SDR104: u32,
    pub HS200: u32,
    pub HS400: u32,
    pub Reserved: u32,
    pub DriverTypeA: u32,
    pub DriverTypeB: u32,
    pub DriverTypeC: u32,
    pub DriverTypeD: u32,
    pub TuningForSDR50: u32,
    pub SoftwareTuning: u32,
    pub AutoCmd12: u32,
    pub AutoCmd23: u32,
    pub Voltage18V: u32,
    pub Voltage30V: u32,
    pub Voltage33V: u32,
    pub Limit200mA: u32,
    pub Limit400mA: u32,
    pub Limit600mA: u32,
    pub Limit800mA: u32,
    pub SaveContext: u32,
    pub Reserved1: u32,
    pub UsePioForRead: u32,
    pub UsePioForWrite: u32,
    pub Reserved_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDPORT_PORT_CONFIG_INFO {
    pub DeviceObject: usize,
    pub BusType: u32,
    pub PciConfigInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SD_MINIPORT {
    pub InitializationData: usize,
    pub ConfigurationInfo: u32,
    pub SlotCount: u8,
    pub SlotExtensionList: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_QUERY_DEVICE_PROTOCOL_DATA {
    pub Size: u16,
    pub Reserved: u16,
    pub ProtocolGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_DEVICE_COMMAND_DATA {
    pub HeaderSize: u16,
    pub Flags: u16,
    pub Command: u32,
    pub ProtocolArgumentSize: u16,
    pub DeviceDataBufferSize: u32,
    pub Information: usize,
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_DEVICE_PASSWORD_DATA {
    pub Size: u16,
    pub Reserved: u16,
    pub Command: u32,
    pub Information: usize,
    pub PasswordLength: u8,
    pub NewPasswordLength: u8,
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_DEVICE_RPMB_DATA_FRAME {
    pub Stuff: [u8; 196],
    pub KeyOrMAC: [u8; 32],
    pub Data: [u8; 256],
    pub Nonce: [u8; 16],
    pub WriteCounter: [u8; 4],
    pub Address: [u8; 2],
    pub BlockCount: [u8; 2],
    pub OperationResult: [u8; 2],
    pub RequestOrResponseType: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_DEVICE_PARTITION_ACCESS_DATA {
    pub Size: u32,
    pub Command: u32,
    pub Reserved: [u32; 2],
    pub SizeInBytes: u32,
    pub MaxReliableWriteSizeInBytes: u32,
    pub Reserved_1: [u32; 4],
    pub ResultFrame: u32,
    pub ProgramAuthKeyFrame: u32,
    pub Reserved_2: [u32; 4],
    pub ResultFrame_1: u32,
    pub QueryWriteCounterFrame: u32,
    pub CountToWrite: u32,
    pub Reserved_3: [u32; 3],
    pub ResultFrame_2: u32,
    pub FrameDataToWrite: [u32; 1],
    pub CountToRead: u32,
    pub Reserved_4: [u32; 3],
    pub AuthenticatedReadFrame: u32,
    pub ReturnedFrameData: [u32; 1],
    pub Gpp_1_SizeInBytes: u64,
    pub Gpp_2_SizeInBytes: u64,
    pub Gpp_3_SizeInBytes: u64,
    pub Gpp_4_SizeInBytes: u64,
    pub GppBlockSize: u32,
    pub GppPartitionId: u32,
    pub Length: u32,
    pub Offset: u64,
    pub Data: [u8; 1024],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFFDISK_BKOPS_STATUS_DATA {
    pub BkopsIsSupported: u8,
    pub BkopsIsEnabled: u8,
    pub BkopsStatus: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SFF_PROTOCOL_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Context: usize,
    pub GetProperty: usize,
    pub SetProperty: usize,
    pub Read: usize,
    pub Write: usize,
    pub DeviceControl: usize,
    pub Trim: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_DEVICE_DESCRIPTOR {
    pub NumberOfEndpoints: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_ENDPOINT_CAPABILITIES {
    pub Volume: i32,
    pub Mute: i32,
    pub Sidetone: i32,
    pub Feedback: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_ENDPOINT_DESCRIPTOR {
    pub CbSize: u32,
    pub ContainerId: u32,
    pub Category: u32,
    pub Direction: u32,
    pub Capabilities: u32,
    pub FriendlyName: u32,
    pub VolumePropertyValuesSize: u32,
    pub SidetoneVolumePropertyValueSize: u32,
    pub MutePropertyValuesSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_ENDPOINT_DESCRIPTOR2 {
    pub CbSize: u32,
    pub ContainerId: u32,
    pub Category: u32,
    pub Direction: u32,
    pub Capabilities: u32,
    pub FriendlyName: u32,
    pub VolumePropertyValuesSize: u32,
    pub SidetoneVolumePropertyValueSize: u32,
    pub MutePropertyValuesSize: u32,
    pub FilterInterfacePropertyCount: u32,
    pub FilterInterfaceProperties: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_VOLUME_PARAMS {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Channel: i32,
    pub Value: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_MUTE_PARAMS {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Channel: i32,
    pub Value: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_STREAM_STATUS_PARAMS {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_CONNECTION_PARAMS {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Connected: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_SUPPORTED_FORMATS {
    pub CbSize: u32,
    pub EpIndex: u32,
    pub NumFormats: u32,
    pub field_Formats: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_SIDETONE_DESCRIPTOR {
    pub Volume: i32,
    pub Mute: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_SIDETONE_PARAMS {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Channel: i32,
    pub Sidetone: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_DEVICE_ERROR {
    pub EpIndex: u32,
    pub Immediate: i32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct T0_DATA {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct T1_DATA {
    pub IFSD: u8,
    pub LastError: u8,
    pub NAD: u8,
    pub ReplyData: *mut u8,
    pub WaitForReply: u8,
    pub InfBytesSent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct T1_BLOCK_FRAME {
    pub Nad: u8,
    pub Pcb: u8,
    pub Len: u8,
    pub Inf: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMARTCARD_REQUEST {
    pub BufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMARTCARD_REPLY {
    pub BufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLOCK_RATE_CONVERSION {
    pub F: u32,
    pub fs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BIT_RATE_ADJUSTMENT {
    pub DNumerator: u32,
    pub DDivisor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERF_INFO {
    pub NumTransmissions: u32,
    pub BytesSent: u32,
    pub BytesReceived: u32,
    pub IoTickCount: i64,
    pub TickStart: i64,
    pub TickEnd: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PTS_DATA {
    pub Type: u8,
    pub Fl: u8,
    pub Dl: u8,
    pub CLKFrequency: u32,
    pub DataRate: u32,
    pub StopBits: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_CARD_CAPABILITIES {
    pub InversConvention: u8,
    pub Buffer: [u8; 64],
    pub Length: u8,
    pub Buffer_1: [u8; 16],
    pub Length_1: u8,
    pub ClockRateConversion: usize,
    pub BitRateAdjustment: usize,
    pub Fl: u8,
    pub Dl: u8,
    pub II: u8,
    pub P: u8,
    pub N: u8,
    pub GT: u32,
    pub Supported: u32,
    pub Selected: u32,
    pub WI: u8,
    pub WT: u32,
    pub IFSC: u8,
    pub CWI: u8,
    pub BWI: u8,
    pub EDC: u8,
    pub CWT: u32,
    pub BWT: u32,
    pub BGT: u32,
    pub PtsData: usize,
    pub Reserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_READER_CAPABILITIES {
    pub SupportedProtocols: u32,
    pub Reserved: u32,
    pub CurrentState: u32,
    pub Default: u32,
    pub Max: u32,
    pub Default_1: u32,
    pub Max_1: u32,
    pub MaxIFSD: u32,
    pub PowerMgmtSupport: u32,
    pub CardConfiscated: u32,
    pub List: *mut u32,
    pub Entries: u8,
    pub List_1: *mut u32,
    pub Entries_1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DataRatesSupported {
    pub List: *mut u32,
    pub Entries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLKFrequenciesSupported {
    pub List: *mut u32,
    pub Entries: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VENDOR_ATTR {
    pub Length: u16,
    pub Buffer: [u8; 1],
    pub Length_1: u16,
    pub Buffer_1: [u8; 1],
    pub BuildNumber: u16,
    pub VersionMinor: u8,
    pub VersionMajor: u8,
    pub Length_2: u16,
    pub Buffer_2: [u8; 1],
    pub Reserved: [u32; 25],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SMARTCARD_EXTENSION {
    pub Version: u32,
    pub field_ReaderFunction: [u32; 16],
    pub LastError: u32,
    pub OsData: usize,
    pub PerfInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OS_DEP_DATA {
    pub DeviceObject: usize,
    pub CurrentIrp: usize,
    pub NotificationIrp: usize,
    pub Mutex: u32,
    pub SpinLock: u32,
    pub Removed: u8,
    pub RefCount: i32,
    pub RemoveEvent: u32,
    pub TagList: u32,
    pub DebugDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_NOTIFICATION_POSTURE {
    pub Posture: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AUDIO_ADDRESS {
    pub Size: u32,
    pub Version: u32,
    pub FunctionId: u32,
    pub FunctionNumber: u32,
    pub EntityId: u32,
    pub ControlSelector: u32,
    pub ControlNumber: u32,
    pub DualRankAttribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AUDIO_CONTROL {
    pub Address: u32,
    pub ValueSize: u32,
    pub Value: i64,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AUDIO_CONTROLS {
    pub Size: u32,
    pub Priority: u32,
    pub ControlCount: u32,
    pub Control: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDCA_AUDIO_CONTROLS_2 {
    pub Size: u32,
    pub CommitGroupHandle: u32,
    pub Priority: u32,
    pub ControlCount: u32,
    pub Control: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_MEMORY {
    pub Size: u32,
    pub Priority: u32,
    pub Address: u32,
    pub BufferSize: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_MEMORY_2 {
    pub Size: u32,
    pub CommitGroupHandle: u32,
    pub Priority: u32,
    pub Address: u32,
    pub BufferSize: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_REGISTER {
    pub Address: u32,
    pub Value: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_REGISTERS {
    pub Size: u32,
    pub Priority: u32,
    pub RegisterCount: u32,
    pub Register: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_REGISTERS_2 {
    pub Size: u32,
    pub CommitGroupHandle: u32,
    pub Priority: u32,
    pub RegisterCount: u32,
    pub Register: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_INTERRUPTS {
    pub Size: u32,
    pub Version: u32,
    pub SCPInterrupts: u32,
    pub DataPortInterrupts: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_INTERRUPT_CALLBACK {
    pub Size: u32,
    pub Context: usize,
    pub EvtInterruptCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_NOTIFICATIONS {
    pub Size: u32,
    pub Version: u32,
    pub Notifications: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_NOTIFICATION_CALLBACK {
    pub Size: u32,
    pub Context: usize,
    pub EvtNotificationCallback: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_PERIPHERAL_INFORMATION {
    pub Size: u32,
    pub Flags: u32,
    pub SubSystemId: u32,
    pub ControllerId: u32,
    pub LinkId: u32,
    pub PeripheralId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORT_CONFIGURATION {
    pub Size: u32,
    pub DataPortNumber: u32,
    pub EndpointId: u32,
    pub Modes: u32,
    pub ChannelMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORT_CONFIGURATION_2 {
    pub Size: u32,
    pub DataPortNumber: u32,
    pub EndpointId: u32,
    pub Modes: u32,
    pub ChannelMask: u32,
    pub Format: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_TRIGGER_CONFIGURATION {
    pub Size: u32,
    pub DataPortNumber: u32,
    pub EndpointId: u32,
    pub Purpose: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORT_STARTSTOP {
    pub Size: u32,
    pub CommitGroupHandle: u32,
    pub DataPortNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_PERIPHERAL_CAPABILITIES {
    pub Size: u32,
    pub Version: u32,
    pub SwInterfaceRevision: u32,
    pub SwInterfaceDisCoMinorVersion: u32,
    pub SwInterfaceDisCoMajorVersion: u32,
    pub SdcaInterfaceRevision: u32,
    pub SdcaInterfaceDraftRevision: u32,
    pub SdcaInterfaceMinorVersion: u32,
    pub SdcaInterfaceMajorVersion: u32,
    pub WakeUpUnavailable: u8,
    pub TestModeSupported: u8,
    pub ClockStopMode1Supported: u8,
    pub SimplifiedClockStopPrepareSMSupported: u8,
    pub ClockStopPrepareTimeoutInMS: u32,
    pub PeripheralChannelPrepareTimeoutInMS: u32,
    pub ClockStopPrepareHardResetBehavior: u32,
    pub ClockStopPrepareHardResetKeepSMStatus: u32,
    pub Reserved: u32,
    pub HighPHYCapable: u8,
    pub PagingSupported: u8,
    pub BankDelaySupported: u8,
    pub Port15ReadBehavior: u32,
    pub LaneBusHolder: u32,
    pub LaneBusHolderReserved0: u32,
    pub LaneBusHolder1: u32,
    pub LaneBusHolder2: u32,
    pub LaneBusHolder3: u32,
    pub LaneBusHolder4: u32,
    pub LaneBusHolder5: u32,
    pub LaneBusHolder6: u32,
    pub LaneBusHolder7: u32,
    pub LaneBusHolderReservedN: u32,
    pub LaneMapping: [i8; 1],
    pub SdcaInterruptRegisterList: u32,
    pub DataPortSourceList: u32,
    pub DataPortSinkList: u32,
    pub DataPort0Supported: u8,
    pub CommitRegisterSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_BRA_MODE {
    pub Size: u32,
    pub BRAMode: u32,
    pub BusFrequencyMin: u32,
    pub BusFrequencyMax: u32,
    pub DataPerFrameMax: u32,
    pub TransactionDelayInUS: u32,
    pub BandwidthMax: u32,
    pub BlockAlignment: u32,
    pub BusFrequencyListCount: u32,
    pub BusFrequencyList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORT_CAPS_HEADER {
    pub Size: u32,
    pub Version: u32,
    pub DataPortNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORT0_CAPABILITIES_2 {
    pub Header: u32,
    pub WordLengthsSupported: u64,
    pub BRAFlowControlled: u8,
    pub BRAImpDefResponseSupported: u8,
    pub BRARoleSupported: u32,
    pub SimplifiedChannelPrepareStateMachine: u8,
    pub ChannelPrepareTimeoutPresent: u8,
    pub ChannelPrepareTimeoutInMS: u32,
    pub ImpDefDp0InterruptsSupported: u32,
    pub ImpDefDp0Interrupt1Supported: u32,
    pub ImpDefDp0Interrupt2Supported: u32,
    pub ImpDefDp0Interrupt3Supported: u32,
    pub ImpDefDp0InterruptReserved: u32,
    pub ImpDefBptSupported: u8,
    pub LaneListCount: u32,
    pub LaneList: [u32; 1],
    pub BRAModeCount: u32,
    pub BRAModeByteOffsetFromBeginningOfThisStructure: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOUNDWIRE_DATAPORTN_CAPABILITIES_2 {
    pub Header: u32,
    pub Direction: u32,
    pub WordLengthsSupported: u64,
    pub DataPortType: u32,
    pub MaxGroupingSupportedPresent: u8,
    pub MaxGroupingSupported: u32,
    pub SimplifiedChannelPrepareStateMachine: u8,
    pub ChannelPrepareTimeoutPresent: u8,
    pub ChannelPrepareTimeoutInMS: u32,
    pub ImpDefDpNInterruptsSupported: u32,
    pub ImpDefDpNInterrupt1Supported: u32,
    pub ImpDefDpNInterrupt2Supported: u32,
    pub ImpDefDpNInterrupt3Supported: u32,
    pub ImpDefDpNInterruptReserved: u32,
    pub ChannelNumberList: u32,
    pub ModesSupportedPresent: u8,
    pub ModesSupported: u32,
    pub ModesSupportedIsochronous: u32,
    pub ModesSupportedTx: u32,
    pub ModesSupportedRx: u32,
    pub ModesSupportedAsync: u32,
    pub ModesSupportedReserved: u32,
    pub MaxAsyncBuffer: u32,
    pub BlockPackingModePresent: u8,
    pub BlockPackingMode: u8,
    pub PortEncodingTypePresent: u8,
    pub PortEncodingType: u32,
    pub PortEncodingTypeTwosComplement: u32,
    pub PortEncodingTypeSignMagnitude: u32,
    pub PortEncodingTypeIeee32: u32,
    pub PortEncodingTypeReserved: u32,
    pub LaneListCount: u32,
    pub LaneList: [u32; 1],
    pub ChannelCombinationListCount: u32,
    pub ChannelCombinationList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_UNIQUE_IDENTIFIER {
    pub Version: u32,
    pub Size: u32,
    pub StorageDeviceIdOffset: u32,
    pub StorageDeviceOffset: u32,
    pub DriveLayoutSignatureOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_LAYOUT_SIGNATURE {
    pub Version: u32,
    pub Size: u32,
    pub Mbr: u8,
    pub MbrSignature: u32,
    pub GptDiskId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_EXTENDED_MESSAGE {
    pub InitialMessageCode: u8,
    pub MessageLength: u8,
    pub MessageType: u8,
    pub Modifier: [u8; 4],
    pub TransferPeriod: u8,
    pub ReqAckOffset: u8,
    pub Width: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PORT_OUTPUT {
    pub ChannelSelection: u8,
    pub Volume: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUDIO_OUTPUT {
    pub CodePage: u8,
    pub ParameterLength: u8,
    pub Immediate: u8,
    pub Reserved: [u8; 2],
    pub LbaFormat: u8,
    pub LogicalBlocksPerSecond: [u8; 2],
    pub PortOutput: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MECHANICAL_STATUS_INFORMATION_HEADER {
    pub CurrentSlot: u8,
    pub ChangerState: u8,
    pub Fault: u8,
    pub Reserved: u8,
    pub MechanismState: u8,
    pub CurrentLogicalBlockAddress: [u8; 3],
    pub NumberAvailableSlots: u8,
    pub SlotTableLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SLOT_TABLE_INFORMATION {
    pub DiscChanged: u8,
    pub Reserved: u8,
    pub DiscPresent: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MECHANICAL_STATUS {
    pub MechanicalStatusHeader: u32,
    pub SlotTableInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNMAP_BLOCK_DESCRIPTOR {
    pub StartingLba: [u8; 8],
    pub LbaCount: [u8; 4],
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNMAP_LIST_HEADER {
    pub DataLength: [u8; 2],
    pub BlockDescrDataLength: [u8; 2],
    pub Reserved: [u8; 4],
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACCESS_RANGE {
    pub RangeStart: u32,
    pub RangeLength: u32,
    pub RangeInMemory: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_REGION {
    pub VirtualBase: *mut u8,
    pub PhysicalBase: u64,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MESSAGE_INTERRUPT_INFORMATION {
    pub MessageId: u32,
    pub MessageData: u32,
    pub MessageAddress: u32,
    pub InterruptVector: u32,
    pub InterruptLevel: u32,
    pub InterruptMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PORT_CONFIGURATION_INFORMATION {
    pub Length: u32,
    pub SystemIoBusNumber: u32,
    pub AdapterInterfaceType: u32,
    pub BusInterruptLevel: u32,
    pub BusInterruptVector: u32,
    pub InterruptMode: u32,
    pub MaximumTransferLength: u32,
    pub NumberOfPhysicalBreaks: u32,
    pub DmaChannel: u32,
    pub DmaPort: u32,
    pub DmaWidth: u32,
    pub DmaSpeed: u32,
    pub AlignmentMask: u32,
    pub NumberOfAccessRanges: u32,
    pub field_AccessRanges_: [u32; 1],
    pub Reserved: usize,
    pub NumberOfBuses: u8,
    pub InitiatorBusId: [u8; 8],
    pub ScatterGather: u8,
    pub Master: u8,
    pub CachesData: u8,
    pub AdapterScansDown: u8,
    pub AtdiskPrimaryClaimed: u8,
    pub AtdiskSecondaryClaimed: u8,
    pub Dma32BitAddresses: u8,
    pub DemandMode: u8,
    pub MapBuffers: u8,
    pub NeedPhysicalAddresses: u8,
    pub TaggedQueuing: u8,
    pub AutoRequestSense: u8,
    pub MultipleRequestPerLu: u8,
    pub ReceiveEvent: u8,
    pub RealModeInitialized: u8,
    pub BufferAccessScsiPortControlled: u8,
    pub MaximumNumberOfTargets: u8,
    pub ReservedUchars: [u8; 2],
    pub SlotNumber: u32,
    pub BusInterruptLevel2: u32,
    pub BusInterruptVector2: u32,
    pub InterruptMode2: u32,
    pub DmaChannel2: u32,
    pub DmaPort2: u32,
    pub DmaWidth2: u32,
    pub DmaSpeed2: u32,
    pub DeviceExtensionSize: u32,
    pub SpecificLuExtensionSize: u32,
    pub SrbExtensionSize: u32,
    pub Dma64BitAddresses: u8,
    pub ResetTargetSupported: u8,
    pub MaximumNumberOfLogicalUnits: u8,
    pub WmiDataProvider: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SCATTER_GATHER_ELEMENT {
    pub PhysicalAddress: u32,
    pub Length: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SCATTER_GATHER_LIST {
    pub NumberOfElements: u32,
    pub Reserved: usize,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POWER_CONTROL_HEADER {
    pub Version: u32,
    pub Size: u32,
    pub Address: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POWER_SETTING_INFO {
    pub PowerSettingGuid: u32,
    pub Value: u32,
    pub ValueLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_ADAPTER_CONTROL_POWER {
    pub Header: u32,
    pub PowerAction: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SYSTEM_POWER_HINTS {
    pub Version: u32,
    pub Size: u32,
    pub SystemPower: u32,
    pub ResumeLatencyMSec: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_FILTER_RESOURCE_REQUIREMENTS {
    pub Version: u32,
    pub Size: u32,
    pub IoResourceRequirementsList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_MAX_OPERATIONAL_POWER {
    pub Version: u32,
    pub Size: u32,
    pub Value: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_PERF_STATE_CONTEXT {
    pub Version: u32,
    pub Size: u32,
    pub ComponentIndex: u32,
    pub Succeeded: u8,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SERIAL_NUMBER {
    pub Version: u32,
    pub Size: u32,
    pub Unicode: u8,
    pub SerialNumber: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UC_DEVICE_USAGE {
    pub Address: usize,
    pub UsageType: u32,
    pub InUse: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UNIT_CONTROL_POWER {
    pub Address: usize,
    pub PowerAction: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_UNIT_POWER_INFO {
    pub Header: u32,
    pub IdlePowerEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_POWER_REQUIRED_CONTEXT {
    pub Header: u32,
    pub PowerRequired: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_ACTIVE_CONTEXT {
    pub Header: u32,
    pub ComponentIndex: u32,
    pub Active: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_FSTATE_CONTEXT {
    pub Header: u32,
    pub ComponentIndex: u32,
    pub FState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_POWER_CONTROL {
    pub Header: u32,
    pub PowerControlCode: usize,
    pub InBufferSize: usize,
    pub OutBufferSize: usize,
    pub InBuffer: usize,
    pub OutBuffer: usize,
    pub BytesReturned: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_RICH_DEVICE_DESCRIPTION {
    pub Version: u32,
    pub Size: u32,
    pub VendorId: [i8; 1],
    pub ModelNumber: [i8; 1],
    pub FirmwareRevision: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_RICH_DEVICE_DESCRIPTION_V2 {
    pub Version: u32,
    pub Size: u32,
    pub VendorId: [i8; 1],
    pub ModelNumber: [i8; 1],
    pub FirmwareRevision: [i8; 1],
    pub Address: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CRYPTO_OPERATION {
    pub OperationType: u32,
    pub OperationBufferLength: u32,
    pub OperationBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CRYPTO_OPERATION_INSERT_KEY {
    pub Version: u16,
    pub Size: u16,
    pub KeyIndex: u32,
    pub CryptoCapabilityIndex: u32,
    pub DataUnitSizeBitmask: u32,
    pub KeySize: u32,
    pub Reserved: u32,
    pub KeyVirtualAddress: usize,
    pub KeyPhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_SUPPORTED_CONTROL_TYPE_LIST {
    pub MaxControlType: u32,
    pub SupportedTypeList: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SUPPORTED_CONTROL_TYPE_LIST {
    pub MaxControlType: u32,
    pub SupportedTypeList: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UNIT_CONTROL_QUERY_BUS_TYPE {
    pub Address: usize,
    pub BusType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_FRU_ID_DESCRIPTION {
    pub Version: u32,
    pub Size: u32,
    pub Address: usize,
    pub FruId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_REPORT_INTERNAL_DATA {
    pub Version: u32,
    pub Size: u32,
    pub Address: usize,
    pub Scope: u32,
    pub CallbackContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UNIT_NVME_ICE_INFORMATION {
    pub Version: u32,
    pub Address: usize,
    pub NvmeIceEntry: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_RESET_BUS_SYNCHRONOUS_PARAMETER {
    pub Version: u32,
    pub Size: u32,
    pub PathId: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DPC_BUFFER {
    pub Type: u32,
    pub Number: u8,
    pub Importance: u8,
    pub F: usize,
    pub B: usize,
    pub DeferredRoutine: usize,
    pub DeferredContext: usize,
    pub SystemArgument1: usize,
    pub SystemArgument2: usize,
    pub DpcData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DPC {
    pub Dpc: u32,
    pub Lock: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_LOCK_HANDLE {
    pub Lock: u32,
    pub Next: usize,
    pub Lock_1: usize,
    pub OldIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_EXT_SET_PARAMETERS {
    pub Version: u32,
    pub Reserved: u32,
    pub NoWakeTolerance: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_EXT_DELETE_PARAMETERS {
    pub Version: u32,
    pub Reserved: u32,
    pub DeleteCallback: usize,
    pub DeleteContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERF_CONFIGURATION_DATA {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub ConcurrentChannels: u32,
    pub LastRedirectionMessageNumber: u32,
    pub DeviceNode: u32,
    pub Reserved: u32,
    pub MessageTargets: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STARTIO_PERFORMANCE_PARAMETERS {
    pub Version: u32,
    pub Size: u32,
    pub MessageNumber: u32,
    pub ChannelNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STARTIO_PERFORMANCE_PARAMETERS_V2 {
    pub Version: u32,
    pub Size: u32,
    pub MessageNumber: u32,
    pub ChannelNumber: u32,
    pub InitiatingProcessor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UNICODE_STRING {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Buffer: usize,
    pub Buffer_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_REQUEST_INFO_V1 {
    pub Version: u16,
    pub Size: u16,
    pub PriorityHint: u32,
    pub Flags: u32,
    pub Key: u32,
    pub Length: u32,
    pub IsWriteRequest: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_REQUEST_INFO_V2 {
    pub Version: u16,
    pub Size: u16,
    pub PriorityHint: u32,
    pub Flags: u32,
    pub Key: u32,
    pub Length: u32,
    pub IsWriteRequest: u8,
    pub Reserved: [u8; 3],
    pub FileName: usize,
    pub ProcessId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_RPMB_CAPABILITIES_DATA {
    pub Version: u32,
    pub Size: u32,
    pub RpmbSize: u32,
    pub MaxReliableRpmbWriteSize: u32,
    pub SecurityProtocol: u8,
    pub SecurityProtocolSpecifier: u16,
    pub RpmbTarget: u32,
    pub FrameFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CRYPTO_CAPABILITY {
    pub Version: u32,
    pub Size: u32,
    pub CryptoCapabilityIndex: u16,
    pub DataUnitSizeBitmask: u16,
    pub AlgorithmId: u32,
    pub KeySize: u32,
    pub MaxIVBitSize: u16,
    pub Reserved: u16,
    pub SecurityComplianceBitmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CRYPTO_CAPABILITIES_DATA {
    pub Version: u32,
    pub Size: u32,
    pub NumKeysSupported: u16,
    pub NumCryptoCapabilities: u16,
    pub CryptoCapabilities: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CRYPTO_KEY_INFO {
    pub Version: u32,
    pub Size: u32,
    pub KeyIndex: u32,
    pub Tweak: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SLIST_ENTRY {
    pub field_Next: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_LIST_ENTRY {
    pub field_Flink: u32,
    pub field_Blink: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORPORT_EXTENDED_FUNCTIONS {
    pub Version: u32,
    pub GetMessageInterruptInformation: usize,
    pub PutScatterGatherList: usize,
    pub BuildScatterGatherList: usize,
    pub FreePool: usize,
    pub AllocatePool: usize,
    pub GetSystemAddress: usize,
    pub AcquireMSISpinLock: usize,
    pub ReleaseMSISpinLock: usize,
    pub CompleteServiceIrp: usize,
    pub GetOriginalMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_INITIALIZATION_DATA {
    pub HwInitializationDataSize: u32,
    pub AdapterInterfaceType: u32,
    pub HwInitialize: usize,
    pub HwStartIo: usize,
    pub HwInterrupt: usize,
    pub HwFindAdapter: usize,
    pub HwResetBus: usize,
    pub HwDmaStarted: usize,
    pub HwAdapterState: usize,
    pub DeviceExtensionSize: u32,
    pub SpecificLuExtensionSize: u32,
    pub SrbExtensionSize: u32,
    pub NumberOfAccessRanges: u32,
    pub Reserved: usize,
    pub MapBuffers: u8,
    pub NeedPhysicalAddresses: u8,
    pub TaggedQueuing: u8,
    pub AutoRequestSense: u8,
    pub MultipleRequestPerLu: u8,
    pub ReceiveEvent: u8,
    pub VendorIdLength: u16,
    pub VendorId: usize,
    pub ReservedUshort: u16,
    pub PortVersionFlags: u16,
    pub DeviceIdLength: u16,
    pub DeviceId: usize,
    pub HwAdapterControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIRTUAL_HW_INITIALIZATION_DATA {
    pub HwInitializationDataSize: u32,
    pub AdapterInterfaceType: u32,
    pub HwInitialize: usize,
    pub HwStartIo: usize,
    pub HwInterrupt: usize,
    pub HwFindAdapter: usize,
    pub HwResetBus: usize,
    pub HwDmaStarted: usize,
    pub HwAdapterState: usize,
    pub DeviceExtensionSize: u32,
    pub SpecificLuExtensionSize: u32,
    pub SrbExtensionSize: u32,
    pub NumberOfAccessRanges: u32,
    pub Reserved: usize,
    pub MapBuffers: u8,
    pub NeedPhysicalAddresses: u8,
    pub TaggedQueuing: u8,
    pub AutoRequestSense: u8,
    pub MultipleRequestPerLu: u8,
    pub ReceiveEvent: u8,
    pub VendorIdLength: u16,
    pub VendorId: usize,
    pub ReservedUshort: u16,
    pub PortVersionFlags: u16,
    pub DeviceIdLength: u16,
    pub DeviceId: usize,
    pub HwAdapterControl: usize,
    pub HwBuildIo: usize,
    pub HwFreeAdapterResources: usize,
    pub HwProcessServiceRequest: usize,
    pub HwCompleteServiceIrp: usize,
    pub HwInitializeTracing: usize,
    pub HwCleanupTracing: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MINIPORT_MAPPINGS {
    pub Version: u16,
    pub IBFTable: usize,
    pub Nic0Map: usize,
    pub Nic1Map: usize,
    pub NumberOfAdditionalNic: u32,
    pub NicMap: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MINIPORT_DUMP_POINTERS {
    pub Version: u16,
    pub Size: u16,
    pub DriverName: [u32; 1],
    pub field_AdapterObject: u32,
    pub MappedRegisterBase: usize,
    pub CommonBufferSize: u32,
    pub MiniportPrivateDumpData: usize,
    pub SystemIoBusNumber: u32,
    pub AdapterInterfaceType: u32,
    pub MaximumTransferLength: u32,
    pub NumberOfPhysicalBreaks: u32,
    pub AlignmentMask: u32,
    pub NumberOfAccessRanges: u32,
    pub field_AccessRanges_: [u32; 1],
    pub NumberOfBuses: u8,
    pub Master: u8,
    pub MapBuffers: u8,
    pub MaximumNumberOfTargets: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MINIPORT_DUMP_INFO {
    pub Version: u32,
    pub Size: u32,
    pub Signature: u32,
    pub Reserved0: [u8; 4],
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_MINIPORT_DUMP_INFO_V1 {
    pub Version: u32,
    pub Size: u32,
    pub Signature: u32,
    pub DiskCount: u16,
    pub AddressType: u16,
    pub AddressSize: u32,
    pub Reserved0: [u8; 4],
    pub Addresses: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FREE_MINIPORT_DUMP_INFO {
    pub Version: u32,
    pub Size: u32,
    pub Signature: u32,
    pub Reserved0: [u8; 4],
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_LOG_EVENT_DETAILS {
    pub InterfaceRevision: u32,
    pub Size: u32,
    pub Flags: u32,
    pub EventAssociation: u32,
    pub PathId: u32,
    pub TargetId: u32,
    pub LunId: u32,
    pub StorportSpecificErrorCode: u8,
    pub ErrorCode: u32,
    pub UniqueId: u32,
    pub DumpDataSize: u32,
    pub DumpData: usize,
    pub StringCount: u32,
    pub StringList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_UNIT_ATTRIBUTES {
    pub DeviceAttentionSupported: u32,
    pub AsyncNotificationSupported: u32,
    pub D3ColdNotSupported: u32,
    pub BypassIOSupported: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DRIVER_PROXY_ENDPOINT_INFORMATION {
    pub Id: u32,
    pub EndpointFunction: usize,
    pub ParameterCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_COMPONENT_IDLE_STATE {
    pub Version: u32,
    pub Size: u32,
    pub TransitionLatency: u64,
    pub ResidencyRequirement: u64,
    pub NominalPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_COMPONENT {
    pub Version: u32,
    pub Size: u32,
    pub FStateCount: u32,
    pub DeepestWakeableFState: u32,
    pub Id: u32,
    pub FStates: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_COMPONENT_V2 {
    pub Version: u32,
    pub Size: u32,
    pub FStateCount: u32,
    pub DeepestWakeableFState: u32,
    pub Id: u32,
    pub DeepestAdapterPowerRequiredFState: u32,
    pub DeepestCrashDumpReadyFState: u32,
    pub FStates: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_PERF_STATE {
    pub Version: u32,
    pub Size: u32,
    pub Value: u64,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_COMPONENT_PERF_SET {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u64,
    pub PStateUnit: u32,
    pub PStateType: u32,
    pub Count: u32,
    pub Offset: u32,
    pub Minimum: u64,
    pub Maximum: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_DEVICE {
    pub Version: u32,
    pub Size: u32,
    pub ComponentCount: u32,
    pub Flags: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_DEVICE_V2 {
    pub Version: u32,
    pub Size: u32,
    pub ComponentCount: u32,
    pub Flags: u32,
    pub UnitMinIdleTimeoutInMS: u32,
    pub AdapterIdleTimeoutInMS: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_POFX_DEVICE_V3 {
    pub Version: u32,
    pub Size: u32,
    pub ComponentCount: u32,
    pub Flags: u32,
    pub UnitMinIdleTimeoutInMS: u32,
    pub AdapterIdleTimeoutInMS: u32,
    pub MinimumPowerCyclePeriodInMS: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_SET_EVENT_LOGGING {
    pub Channel: u32,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORPORT_TELEMETRY_EVENT {
    pub DriverVersion: u32,
    pub EventId: u32,
    pub EventName: [u8; 1],
    pub EventVersion: u32,
    pub Flags: u32,
    pub EventBufferLength: u32,
    pub EventBuffer: *mut u8,
    pub ParameterName0: [u8; 1],
    pub ParameterValue0: u64,
    pub ParameterName1: [u8; 1],
    pub ParameterValue1: u64,
    pub ParameterName2: [u8; 1],
    pub ParameterValue2: u64,
    pub ParameterName3: [u8; 1],
    pub ParameterValue3: u64,
    pub ParameterName4: [u8; 1],
    pub ParameterValue4: u64,
    pub ParameterName5: [u8; 1],
    pub ParameterValue5: u64,
    pub ParameterName6: [u8; 1],
    pub ParameterValue6: u64,
    pub ParameterName7: [u8; 1],
    pub ParameterValue7: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DISPATCHER_HEADER {
    pub Type: u8,
    pub Flags: u8,
    pub Size: u8,
    pub Inserted: u8,
    pub DebugActive: u8,
    pub Lock: i32,
    pub SignalState: i32,
    pub WaitListHead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_EVENT {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORPORT_LIVEDUMP_DEVICE_TELEMETRY_LOG {
    pub Version: u32,
    pub Size: u32,
    pub BucketId: [u8; 1],
    pub OrganizationID: [u8; 3],
    pub Reserved: u8,
    pub TelemetryLength: u32,
    pub DeviceTelemetry: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORPORT_CAPTURE_LIVEDUMP {
    pub Version: u32,
    pub Size: u32,
    pub LiveDumpType: u32,
    pub IssueType: u32,
    pub ComponentName: usize,
    pub DataType: u32,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DPC_WATCHDOG_INFORMATION {
    pub DpcTimeLimit: u32,
    pub DpcTimeCount: u32,
    pub DpcWatchdogLimit: u32,
    pub DpcWatchdogCount: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_GROUP_AFFINITY {
    pub Mask: u32,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MINIPORT_DATA {
    pub Name: usize,
    pub Type: u32,
    pub ArrayLength: u16,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORMQ_PROPERTY_QUEUE_DESCRIPTOR {
    pub Version: u32,
    pub QueueId: u32,
    pub NumberOfElements: u32,
    pub Reserved: u32,
    pub IsSubmissionQueue: u64,
    pub Reserved_1: u64,
    pub VirtualAddress: usize,
    pub PhysicalAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORMQ_PROPERTY_AFFINITY_DESCRIPTOR {
    pub Version: u32,
    pub NumberOfElements: u32,
    pub Affinity: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORMQ_MINIPORT_INTERFACE {
    pub Size: u32,
    pub StorMQVersion: u16,
    pub MiniportVersion: u16,
    pub ProvideVirtualAddresses: u64,
    pub Reserved: u64,
    pub HwStorMQCompletionNotifyWithCallback: usize,
    pub HwStorMQQueryProperty: usize,
    pub HwStorMQSetProperty: usize,
    pub HwStorMQStartSrb: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TRACE_INIT_INFO {
    pub Size: u32,
    pub NumDiagEventRecords: u32,
    pub DiagEventRecordUserDataSize: u32,
    pub NumErrorLogRecords: u32,
    pub TraceGuid: u32,
    pub TraceCleanupRoutine: u32,
    pub DriverObject: usize,
    pub TraceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DIAG_EVENT_RECORD {
    pub Size: u32,
    pub Reserved: u32,
    pub Info: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TRACE_DPS_INFO {
    pub EventId: u32,
    pub Flags: u32,
    pub Status: u32,
    pub NumContexts: u32,
    pub Contexts: usize,
    pub UserDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ERRORLOG_PACKET {
    pub MajorFunctionCode: u8,
    pub RetryCount: u8,
    pub DumpDataSize: u16,
    pub NumberOfStrings: u16,
    pub StringOffset: u16,
    pub EventCategory: u16,
    pub ErrorCode: u32,
    pub UniqueErrorValue: u32,
    pub FinalStatus: u32,
    pub SequenceNumber: u32,
    pub IoControlCode: u32,
    pub DeviceOffset: i64,
    pub DumpData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_DEBUGPRINT_ARGS {
    pub Message: *mut i8,
    pub ArgList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_INIT_TRACING_ARGS {
    pub InitInfo: usize,
    pub Result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_CLEANUP_TRACING_ARGS {
    pub TraceContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_WRITE_EL_RECORD_ARGS {
    pub TraceContext: usize,
    pub ErrorLogPacket: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_ALLOC_EL_RECORD_ARGS {
    pub TraceContext: usize,
    pub Size: u32,
    pub Result: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_FREE_EL_RECORD_ARGS {
    pub TraceContext: usize,
    pub ErrorLogPacket: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_LOG_DIAG_EVENT_ARGS {
    pub TraceContext: usize,
    pub ContextEvent: usize,
    pub result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_ALLOC_DIAG_EVENT_ARGS {
    pub TraceContext: usize,
    pub UserDataSize: u32,
    pub Allocate: u8,
    pub result: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_FREE_DIAG_EVENT_ARGS {
    pub TraceContext: usize,
    pub EventRecord: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_WMI_TRACE_MESSAGE_ARGS {
    pub TraceHandle: u32,
    pub MessageFlags: u32,
    pub MessageGuid: usize,
    pub MessageNumber: u16,
    pub Args: u32,
    pub result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_INIT_UNICODE_STRING_ARGS {
    pub DestinationString: usize,
    pub SourceString: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_WMI_REGCONTROL_ARGS {
    pub DeviceObject: usize,
    pub Action: u32,
    pub result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_WMI_QUERYTRACEINFO_ARGS {
    pub TraceInformationClass: u32,
    pub TraceInformation: usize,
    pub TraceInformationLength: u32,
    pub RequiredLength: *mut u32,
    pub Buffer: usize,
    pub result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STOR_INITGLOBALLOGGER_ARGS {
    pub ControlGuid: usize,
    pub Logger: usize,
    pub Flags: usize,
    pub Level: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_TIME_CONTEXT {
    pub field_HwDeviceExtension: u32,
    pub field_HwStreamObject: u32,
    pub Function: u32,
    pub Time: u64,
    pub SystemTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_EVENT_DESCRIPTOR {
    pub Enable: u8,
    pub EventEntry: usize,
    pub EventData: usize,
    pub StreamObject: usize,
    pub field_DeviceExtension: u32,
    pub EnableEventSetIndex: u32,
    pub HwInstanceExtension: usize,
    pub Reserved: u32,
    pub Reserved_1: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_CLOCK_OBJECT {
    pub HwClockFunction: usize,
    pub ClockSupportFlags: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_STREAM_OBJECT {
    pub SizeOfThisPacket: u32,
    pub StreamNumber: u32,
    pub HwStreamExtension: usize,
    pub ReceiveDataPacket: usize,
    pub ReceiveControlPacket: usize,
    pub HwClockObject: u32,
    pub Dma: u8,
    pub Pio: u8,
    pub HwDeviceExtension: usize,
    pub StreamHeaderMediaSpecific: u32,
    pub StreamHeaderWorkspace: u32,
    pub Allocator: u8,
    pub HwEventRoutine: usize,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_STREAM_HEADER {
    pub NumberOfStreams: u32,
    pub SizeOfHwStreamInformation: u32,
    pub NumDevPropArrayEntries: u32,
    pub DevicePropertiesArray: usize,
    pub NumDevEventArrayEntries: u32,
    pub DeviceEventsArray: usize,
    pub Topology: usize,
    pub DeviceEventRoutine: usize,
    pub NumDevMethodArrayEntries: i32,
    pub DeviceMethodsArray: usize,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_STREAM_INFORMATION {
    pub NumberOfPossibleInstances: u32,
    pub DataFlow: u32,
    pub DataAccessible: u8,
    pub NumberOfFormatArrayEntries: u32,
    pub StreamFormatsArray: usize,
    pub ClassReserved: [usize; 4],
    pub NumStreamPropArrayEntries: u32,
    pub StreamPropertiesArray: usize,
    pub NumStreamEventArrayEntries: u32,
    pub StreamEventsArray: usize,
    pub Category: usize,
    pub Name: usize,
    pub MediumsCount: u32,
    pub Mediums: usize,
    pub BridgeStream: u8,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_STREAM_DESCRIPTOR {
    pub StreamHeader: u32,
    pub StreamInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_TIME_REFERENCE {
    pub CurrentOnboardClockValue: u32,
    pub OnboardClockFrequency: i64,
    pub CurrentSystemTime: i64,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_DATA_INTERSECT_INFO {
    pub StreamNumber: u32,
    pub DataRange: usize,
    pub DataFormatBuffer: usize,
    pub SizeOfDataFormatBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_PROPERTY_DESCRIPTOR {
    pub Property: usize,
    pub PropertySetID: u32,
    pub PropertyInfo: usize,
    pub PropertyInputSize: u32,
    pub PropertyOutputSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_METHOD_DESCRIPTOR {
    pub MethodSetID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HW_STREAM_REQUEST_BLOCK {
    pub SizeOfThisPacket: u32,
    pub Command: u32,
    pub Status: u32,
    pub StreamObject: usize,
    pub HwDeviceExtension: usize,
    pub SRBExtension: usize,
    pub DataBufferArray: usize,
    pub StreamBuffer: usize,
    pub StreamState: u32,
    pub TimeReference: usize,
    pub PropertyInfo: usize,
    pub OpenFormat: usize,
    pub field_ConfigInfo: u32,
    pub MasterClockHandle: usize,
    pub DeviceState: u32,
    pub IntersectInfo: usize,
    pub Idle: u8,
    pub NumberOfBuffers: u32,
    pub TimeoutCounter: u32,
    pub TimeoutOriginal: u32,
    pub field_NextSRB: u32,
    pub Irp: usize,
    pub Flags: u32,
    pub HwInstanceExtension: usize,
    pub NumberOfBytesToTransfer: u32,
    pub ActualBytesTransferred: u32,
    pub ScatterGatherBuffer: usize,
    pub NumberOfPhysicalPages: u32,
    pub NumberOfScatterGatherElements: u32,
    pub Reserved: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SWENUM_INSTALL_INTERFACE {
    pub DeviceId: u32,
    pub InterfaceId: u32,
    pub ReferenceString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_INTERFACE_SWENUM {
    pub Interface: u32,
    pub ReferenceDeviceObject: usize,
    pub DereferenceDeviceObject: usize,
    pub QueryReferenceString: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL {
    pub RequestFlags: usize,
    pub RequestConnectionInformation: usize,
    pub ReturnConnectionInformation: usize,
    pub RequestSpecific: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_ASSOCIATE {
    pub AddressHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_ACCEPT {
    pub RequestConnectionInformation: usize,
    pub ReturnConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_SEND {
    pub SendLength: u32,
    pub SendFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_RECEIVE {
    pub ReceiveLength: u32,
    pub ReceiveFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_SENDDG {
    pub SendLength: u32,
    pub SendDatagramInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_RECEIVEDG {
    pub ReceiveLength: u32,
    pub ReceiveDatagramInformation: usize,
    pub ReturnDatagramInformation: usize,
    pub ReceiveFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_SET_EVENT {
    pub EventType: i32,
    pub EventHandler: usize,
    pub EventContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_QUERY_INFORMATION {
    pub QueryType: i32,
    pub RequestConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_KERNEL_SET_INFORMATION {
    pub SetType: i32,
    pub RequestConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_PNP_CONTEXT_WIN2K {
    pub ContextSize: u16,
    pub ContextType: u16,
    pub ContextData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_PNP_CONTEXT_XP {
    pub ContextSize: u16,
    pub ContextType: u16,
    pub ContextData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_NCPA_BINDING_INFO {
    pub TdiClientName: usize,
    pub TdiProviderName: usize,
    pub BindList: usize,
    pub ReconfigBuffer: usize,
    pub ReconfigBufferSize: u32,
    pub PnpOpcode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_VERSION {
    pub MajorTdiVersion: u8,
    pub MinorTdiVersion: u8,
    pub TdiVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI20_CLIENT_INTERFACE_INFO {
    pub MajorTdiVersion: u8,
    pub MinorTdiVersion: u8,
    pub TdiVersion: u16,
    pub Unused: u16,
    pub ClientName: usize,
    pub PnPPowerHandler: u32,
    pub BindingHandler: u32,
    pub BindHandler: u32,
    pub UnBindHandler: u32,
    pub AddAddressHandlerV2: u32,
    pub DelAddressHandlerV2: u32,
    pub AddAddressHandler: u32,
    pub DelAddressHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_CMSGHDR_XP {
    pub cmsg_len: usize,
    pub cmsg_level: i32,
    pub cmsg_type: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INLINE_CRYPTO_PROGRAM_KEY_IN {
    pub KeyIndex: u32,
    pub CryptoCapabilityIndex: u32,
    pub DataUnitSize: u32,
    pub KeySize: u32,
    pub KeyVirtualAddress: usize,
    pub KeyPhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INLINE_CRYPTO_PROGRAM_KEY_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TREE_RPMB_AUTHENTICATED_WRITE_EX_INPUT {
    pub WriteFrameCount: u32,
    pub ReliableWriteSizeOverride: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TREE_RPMB_AUTHENTICATED_WRITE_EX_OUTPUT {
    pub ResponseFrameCount: u32,
    pub FramesWritten: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TREE_RPMB_COMMAND_INPUT {
    pub InputFrameCount: u32,
    pub FramesPerTransaction: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TREE_RPMB_COMMAND_OUTPUT {
    pub OutputFrameCount: u32,
    pub FramesWritten: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPM_INVOKE_GENERIC_IN {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPM_INVOKE_GENERIC_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_GET_VARIABLE_IN {
    pub VendorGuid: u32,
    pub VariableName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_GET_VARIABLE_OUT {
    pub EfiStatus: u32,
    pub Attributes: u32,
    pub DataSize: usize,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_GET_NEXT_VARIABLE_NAME_IN {
    pub VendorGuid: u32,
    pub VariableName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_GET_NEXT_VARIABLE_NAME_OUT {
    pub EfiStatus: u32,
    pub VendorGuid: u32,
    pub NameLength: u32,
    pub VariableName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_SET_VARIABLE_IN {
    pub VariableNameOffset: u32,
    pub VendorGuid: u32,
    pub Attributes: u32,
    pub DataSize: usize,
    pub DataOffset: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_SET_VARIABLE_OUT {
    pub EfiStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_QUERY_VARIABLE_INFO_IN {
    pub Attributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EFI_QUERY_VARIABLE_INFO_OUT {
    pub EfiStatus: u32,
    pub MaximumVariableStorageSize: u64,
    pub RemainingVariableStorageSize: u64,
    pub MaximumVariableSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SECURE_DEPENDENCY_V1 {
    pub Type: u32,
    pub MaxRequired: u32,
    pub Id: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SECURE_SERVICE_V1 {
    pub DescriptionSize: u32,
    pub ServiceGuid: u32,
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub ExtensionOffset: u32,
    pub CountDependencies: u32,
    pub Dependencies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SECURE_SERVICE_EXTENSION_V1 {
    pub ExtensionVersion: u32,
    pub SecurityDescriptorStringOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SECURE_DEVICE_CALLBACKS_V1 {
    pub Flags: u32,
    pub EvtTrCreateSecureDeviceContext: usize,
    pub EvtTrDestroySecureDeviceContext: usize,
    pub EvtTrPrepareHardwareSecureEnvironment: usize,
    pub EvtTrReleaseHardwareSecureEnvironment: usize,
    pub EvtTrConnectSecureEnvironment: usize,
    pub EvtTrDisconnectSecureEnvironment: usize,
    pub EvtTrEnumerateSecureServices: usize,
    pub EvtTrProcessOtherDeviceIo: usize,
    pub EvtTrQueryServiceCallbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SECURE_SERVICE_CALLBACKS_V1 {
    pub Flags: u32,
    pub EvtTrCreateSecureServiceContext: usize,
    pub EvtTrDestroySecureServiceContext: usize,
    pub EvtTrConnectSecureService: usize,
    pub EvtTrDisconnectSecureService: usize,
    pub EvtTrCreateSecureSessionContext: usize,
    pub EvtTrDestroySecureSessionContext: usize,
    pub EvtTrProcessSecureServiceRequest: usize,
    pub EvtTrCancelSecureServiceRequest: usize,
    pub EvtTrProcessOtherSecureServiceIo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UART_HARDWARE_DRIVER {
    pub InitializePort: u32,
    pub SetBaud: u32,
    pub GetByte: u32,
    pub PutByte: u32,
    pub RxReady: u32,
    pub SetPowerD0: u32,
    pub SetPowerD3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CPPORT {
    pub Address: *mut u8,
    pub BaudRate: u32,
    pub Flags: u16,
    pub ByteWidth: u8,
    pub Read: u32,
    pub Write: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UART_HARDWARE_ACCESS {
    pub ReadPort8: u32,
    pub WritePort8: u32,
    pub ReadPort16: u32,
    pub WritePort16: u32,
    pub ReadPort32: u32,
    pub WritePort32: u32,
    pub ReadRegister8: u32,
    pub WriteRegister8: u32,
    pub ReadRegister16: u32,
    pub WriteRegister16: u32,
    pub ReadRegister32: u32,
    pub WriteRegister32: u32,
    pub ReadRegister64: u32,
    pub WriteRegister64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_PROPRIETARY_CHARGER {
    pub ChargerId: u32,
    pub DxState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_INTERFACE_PROPRIETARY_CHARGER {
    pub InterfaceHeader: u32,
    pub ProprietaryChargerDetect: usize,
    pub ProprietaryChargerSetProperty: usize,
    pub ProprietaryChargerAbortOperation: usize,
    pub ProprietaryChargerResetOperation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UIOMAP_DOMAIN_DEVICE_ATTACH {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UIOMAP_DOMAIN_REGION_MAP {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UIOMAP_VERSION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UIOMAP_API {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UIOMAP_INTERFACE {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INFORMATION_LEVEL_0 {
    pub TotalBandwidth: u32,
    pub ConsumedBandwidth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INFORMATION_LEVEL_1 {
    pub TotalBandwidth: u32,
    pub ConsumedBandwidth: u32,
    pub ControllerNameLength: u32,
    pub ControllerNameUnicodeString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_USBDI_V0 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetUSBDIVersion: usize,
    pub QueryBusTime: usize,
    pub SubmitIsoOutUrb: usize,
    pub QueryBusInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_USBDI_V1 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetUSBDIVersion: usize,
    pub QueryBusTime: usize,
    pub SubmitIsoOutUrb: usize,
    pub QueryBusInformation: usize,
    pub IsDeviceHighSpeed: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_USBDI_V2 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetUSBDIVersion: usize,
    pub QueryBusTime: usize,
    pub SubmitIsoOutUrb: usize,
    pub QueryBusInformation: usize,
    pub IsDeviceHighSpeed: usize,
    pub EnumLogEntry: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_INTERFACE_USBDI_V3 {
    pub Size: u16,
    pub Version: u16,
    pub BusContext: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetUSBDIVersion: usize,
    pub QueryBusTime: usize,
    pub SubmitIsoOutUrb: usize,
    pub QueryBusInformation: usize,
    pub IsDeviceHighSpeed: usize,
    pub EnumLogEntry: usize,
    pub QueryBusTimeEx: usize,
    pub QueryControllerType: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBC_FUNCTION_DESCRIPTOR {
    pub FunctionNumber: u8,
    pub NumberOfInterfaces: u8,
    pub field_InterfaceDescriptorList: usize,
    pub HardwareId: u32,
    pub CompatibleId: u32,
    pub FunctionDescription: u32,
    pub FunctionFlags: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBC_DEVICE_CONFIGURATION_INTERFACE_V1 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub StartDeviceCallback: usize,
    pub PdoEnableCallback: usize,
    pub Reserved: [usize; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBCAMD_Pipe_Config_Descriptor {
    pub StreamAssociation: i8,
    pub PipeConfigFlags: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBCAMD_DEVICE_DATA {
    pub Sig: u32,
    pub CamInitialize: usize,
    pub CamUnInitialize: usize,
    pub CamProcessUSBPacket: usize,
    pub CamNewVideoFrame: usize,
    pub CamProcessRawVideoFrame: usize,
    pub CamStartCapture: usize,
    pub CamStopCapture: usize,
    pub CamConfigure: usize,
    pub CamSaveState: usize,
    pub CamRestoreState: usize,
    pub CamAllocateBandwidth: usize,
    pub CamFreeBandwidth: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBCAMD_DEVICE_DATA2 {
    pub Sig: u32,
    pub CamInitialize: usize,
    pub CamUnInitialize: usize,
    pub CamProcessUSBPacketEx: usize,
    pub CamNewVideoFrameEx: usize,
    pub CamProcessRawVideoFrameEx: usize,
    pub CamStartCaptureEx: usize,
    pub CamStopCaptureEx: usize,
    pub CamConfigureEx: usize,
    pub CamSaveState: usize,
    pub CamRestoreState: usize,
    pub CamAllocateBandwidthEx: usize,
    pub CamFreeBandwidthEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBC_PD_ALTERNATE_MODE {
    pub SVID: u32,
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_INTERFACE_LIST_ENTRY {
    pub InterfaceDescriptor: usize,
    pub Interface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMPOSITE_DEVICE_CAPABILITIES {
    pub CapabilityFunctionSuspend: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REGISTER_COMPOSITE_DEVICE {
    pub Version: u16,
    pub Size: u16,
    pub Reserved: u32,
    pub CapabilityFlags: u32,
    pub FunctionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REQUEST_REMOTE_WAKE_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub UsbdFunctionHandle: u32,
    pub Interface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_ON_ATTACH {
    pub PortType: u32,
    pub AttachAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_INTERFACE_ATTACH {
    pub InterfaceHeader: u32,
    pub GetAttachAction: usize,
    pub GetAttachActionAbortOperation: usize,
    pub SetDeviceState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_POWER_FILTER_STATE {
    pub DeviceState: u32,
    pub PState: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_EVENT_CALLBACK_PARAMS {
    pub EventType: u32,
    pub AccessGranted: u32,
    pub HubHandle: u32,
    pub ConnectorHandle: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_HUB_PROPERTIES {
    pub ParentHubHandle: u32,
    pub HubHandle: u32,
    pub ConnectorCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_HUB_CONNECTOR_HANDLES {
    pub HubHandle: u32,
    pub ConnectorCount: u32,
    pub field_ConnectorHandles: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_CONNECTOR_PROPERTIES {
    pub ConnectorHandle: u32,
    pub ParentHubHandle: u32,
    pub ConnectorId: u32,
    pub SupportedTypeCOperatingModes: u32,
    pub SupportedTypeCSourceCurrentAdvertisements: u32,
    pub IsTypeCAudioAccessorySupported: u8,
    pub IsPdSupported: u8,
    pub SupportedPowerRoles: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_CONNECTOR_STATE {
    pub ConnectorHandle: u32,
    pub ChangeToken: u32,
    pub Attached: u8,
    pub AttachCount: u32,
    pub Partner: u32,
    pub CurrentAdvertisement: u32,
    pub PdConnState: u32,
    pub Rdo: u32,
    pub DataRole: u32,
    pub PowerRole: u32,
    pub SourceCapsCount: u32,
    pub SourceCaps: [u32; 1],
    pub SinkCapsCount: u32,
    pub SinkCaps: [u32; 1],
    pub PartnerSourceCapsCount: u32,
    pub PartnerSourceCaps: [u32; 1],
    pub PdAlternateModesEnteredCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_CLIENT_CONFIG_EXTRA_INFO {
    pub Size: u32,
    pub WdmDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_CLIENT_CONFIG {
    pub Version: u32,
    pub AccessDesired: u32,
    pub EventCallback: usize,
    pub Context: usize,
    pub ExtraInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBPM_ASSIGN_CONNECTOR_POWER_LEVEL_PARAMS {
    pub PowerRole: u32,
    pub Format: u32,
    pub MaximumPdPowerIn500mW: u32,
    pub MaximumTypeCCurrent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SIDEBANDAUDIO_EP_USBAUDIO_TRANSPORT_RESOURCES {
    pub bcdAudioSpec: u16,
    pub bDelay: u8,
    pub bSlotSize: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_HARDWARE_CONFIGURATION_DATA {
    pub InterfaceType: u32,
    pub BusNumber: u32,
    pub Version: u16,
    pub Revision: u16,
    pub Irql: u16,
    pub Vector: u16,
    pub ControlBase: u32,
    pub ControlSize: u32,
    pub CursorBase: u32,
    pub CursorSize: u32,
    pub FrameBase: u32,
    pub FrameSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_X86_BIOS_ARGUMENTS {
    pub Eax: u32,
    pub Ebx: u32,
    pub Ecx: u32,
    pub Edx: u32,
    pub Esi: u32,
    pub Edi: u32,
    pub Ebp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INT10_BIOS_ARGUMENTS {
    pub Eax: u32,
    pub Ebx: u32,
    pub Ecx: u32,
    pub Edx: u32,
    pub Esi: u32,
    pub Edi: u32,
    pub Ebp: u32,
    pub SegDs: u16,
    pub SegEs: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STATUS_BLOCK {
    pub Status: u32,
    pub Pointer: usize,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_REQUEST_PACKET {
    pub IoControlCode: u32,
    pub StatusBlock: usize,
    pub InputBuffer: usize,
    pub InputBufferLength: u32,
    pub OutputBuffer: usize,
    pub OutputBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VRB_SG {
    pub PhysicalAddress: i64,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VP_DEVICE_DESCRIPTION {
    pub ScatterGather: u8,
    pub Dma32BitAddresses: u8,
    pub Dma64BitAddresses: u8,
    pub MaximumLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VP_SCATTER_GATHER_ELEMENT {
    pub Address: u64,
    pub Length: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VP_SCATTER_GATHER_LIST {
    pub NumberOfElements: u32,
    pub Reserved: usize,
    pub Elements: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_ACCESS_RANGE {
    pub RangeStart: u64,
    pub RangeLength: u32,
    pub RangeInIoSpace: u8,
    pub RangeVisible: u8,
    pub RangeShareable: u8,
    pub RangePassive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_CONFIG_INFO {
    pub Length: u32,
    pub SystemIoBusNumber: u32,
    pub AdapterInterfaceType: u32,
    pub BusInterruptLevel: u32,
    pub BusInterruptVector: u32,
    pub InterruptMode: u32,
    pub NumEmulatorAccessEntries: u32,
    pub EmulatorAccessEntries: usize,
    pub EmulatorAccessEntriesContext: usize,
    pub VdmPhysicalVideoMemoryAddress: u64,
    pub VdmPhysicalVideoMemoryLength: u32,
    pub HardwareStateSize: u32,
    pub DmaChannel: u32,
    pub DmaPort: u32,
    pub DmaShareable: u8,
    pub InterruptShareable: u8,
    pub Master: u8,
    pub DmaWidth: u32,
    pub DmaSpeed: u32,
    pub bMapBuffers: u8,
    pub NeedPhysicalAddresses: u8,
    pub DemandMode: u8,
    pub MaximumTransferLength: u32,
    pub NumberOfPhysicalBreaks: u32,
    pub ScatterGather: u8,
    pub MaximumScatterGatherChunkSize: u32,
    pub VideoPortGetProcAddress: usize,
    pub DriverRegistryPath: usize,
    pub SystemMemorySize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CHILD_ENUM_INFO {
    pub Size: u32,
    pub ChildDescriptorSize: u32,
    pub ChildIndex: u32,
    pub ACPIHwId: u32,
    pub ChildHwDeviceExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CHILD_STATE {
    pub Id: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CHILD_STATE_CONFIGURATION {
    pub Count: u32,
    pub ChildStateArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QUERY_INTERFACE {
    pub field_InterfaceType: u32,
    pub Size: u16,
    pub Version: u16,
    pub Interface: usize,
    pub InterfaceSpecificData: usize,
    pub DeviceUid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_HW_INITIALIZATION_DATA {
    pub HwInitDataSize: u32,
    pub AdapterInterfaceType: u32,
    pub HwFindAdapter: usize,
    pub HwInitialize: usize,
    pub HwInterrupt: usize,
    pub HwStartIO: usize,
    pub HwDeviceExtensionSize: u32,
    pub StartingDeviceNumber: u32,
    pub HwResetHw: usize,
    pub HwTimer: usize,
    pub HwStartDma: usize,
    pub HwSetPowerState: usize,
    pub HwGetPowerState: usize,
    pub HwGetVideoChildDescriptor: usize,
    pub HwQueryInterface: usize,
    pub HwChildDeviceExtensionSize: u32,
    pub HwLegacyResourceList: usize,
    pub HwLegacyResourceCount: u32,
    pub HwGetLegacyResources: usize,
    pub AllowEarlyEnumeration: u8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct I2C_FNC_TABLE {
    pub Size: u32,
    pub WriteClockLine: usize,
    pub WriteDataLine: usize,
    pub ReadClockLine: usize,
    pub ReadDataLine: usize,
    pub WaitVsync: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct I2C_CALLBACKS {
    pub WriteClockLine: usize,
    pub WriteDataLine: usize,
    pub ReadClockLine: usize,
    pub ReadDataLine: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DDC_CONTROL {
    pub Size: u32,
    pub I2CCallbacks: usize,
    pub EdidSegment: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_I2C_CONTROL {
    pub WriteClockLine: usize,
    pub WriteDataLine: usize,
    pub ReadClockLine: usize,
    pub ReadDataLine: usize,
    pub I2CDelay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_AGP_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub AgpReservePhysical: usize,
    pub AgpReleasePhysical: usize,
    pub AgpCommitPhysical: usize,
    pub AgpFreePhysical: usize,
    pub AgpReserveVirtual: usize,
    pub AgpReleaseVirtual: usize,
    pub AgpCommitVirtual: usize,
    pub AgpFreeVirtual: usize,
    pub AgpAllocationLimit: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_AGP_INTERFACE_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub AgpReservePhysical: usize,
    pub AgpReleasePhysical: usize,
    pub AgpCommitPhysical: usize,
    pub AgpFreePhysical: usize,
    pub AgpReserveVirtual: usize,
    pub AgpReleaseVirtual: usize,
    pub AgpCommitVirtual: usize,
    pub AgpFreeVirtual: usize,
    pub AgpAllocationLimit: u64,
    pub AgpSetRate: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_I2C_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub I2CStart: usize,
    pub I2CStop: usize,
    pub I2CWrite: usize,
    pub I2CRead: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_I2C_INTERFACE_2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub I2CStart: usize,
    pub I2CStop: usize,
    pub I2CWrite: usize,
    pub I2CRead: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_INT10_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Int10AllocateBuffer: usize,
    pub Int10FreeBuffer: usize,
    pub Int10ReadMemory: usize,
    pub Int10WriteMemory: usize,
    pub Int10CallBios: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_WCMEMORYPROTECTION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub VideoPortProtectWCMemory: usize,
    pub VideoPortRestoreWCMemory: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPOSVERSIONINFO {
    pub Size: u32,
    pub MajorVersion: u32,
    pub MinorVersion: u32,
    pub BuildNumber: u32,
    pub ServicePackMajor: u16,
    pub ServicePackMinor: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_DEBUG_REPORT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PORT_AGP_SERVICES {
    pub AgpReservePhysical: usize,
    pub AgpReleasePhysical: usize,
    pub AgpCommitPhysical: usize,
    pub AgpFreePhysical: usize,
    pub AgpReserveVirtual: usize,
    pub AgpReleaseVirtual: usize,
    pub AgpCommitVirtual: usize,
    pub AgpFreeVirtual: usize,
    pub AllocationLimit: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VMB_CHANNEL_STATE_CHANGE_CALLBACKS {
    pub Version: u32,
    pub Size: u32,
    pub EvtChannelOpened: usize,
    pub EvtChannelClosed: usize,
    pub EvtChannelSuspend: usize,
    pub EvtChannelStarted: usize,
    pub EvtChannelPostStarted: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VMB_CHANNEL_STATE_CHANGE_CALLBACKS_V5 {
    pub Version: u32,
    pub Size: u32,
    pub CallbackContext: usize,
    pub EvtChannelOpenedEx: usize,
    pub EvtChannelClosedEx: usize,
    pub EvtChannelSuspendEx: usize,
    pub EvtChannelStartedEx: usize,
    pub EvtChannelPostStartedEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_SERVER_ONLY_METHODS {
    pub VmbChannelMapGpadl: usize,
    pub VmbChannelRestoreFromBuffer: usize,
    pub VmbChannelSaveBegin: usize,
    pub VmbChannelSaveContinue: usize,
    pub VmbChannelSaveEnd: usize,
    pub VmbChannelUnmapGpadl: usize,
    pub VmbConvertVmbusHandleToKernelHandle: usize,
    pub VmbPacketRestore: usize,
    pub VmbServerChannelInitSetFlags: usize,
    pub VmbServerChannelInitSetMmioMegabytes: usize,
    pub VmbServerChannelInitSetSaveRestorePacketCallbacks: usize,
    pub VmbServerChannelInitSetTargetInterfaceId: usize,
    pub VmbServerChannelInitSetTargetVtl: usize,
    pub VmbServerChannelInitSetVmbusHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_SERVER_ONLY_METHODS_V5 {
    pub VmbServerChannelInitSetSaveRestorePacketCallbacksEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_CLIENT_INTERFACE_V1 {
    pub VmbChannelAllocate: usize,
    pub VmbChannelCleanup: usize,
    pub VmbChannelCreateGpadlFromBuffer: usize,
    pub VmbChannelCreateGpadlFromMdl: usize,
    pub VmbChannelDeleteGpadl: usize,
    pub VmbChannelDisable: usize,
    pub VmbChannelEnable: usize,
    pub VmbChannelGetInterfaceInstance: usize,
    pub VmbChannelGetMmioSpace: usize,
    pub VmbChannelGetPointer: usize,
    pub VmbChannelInitSetClientContextSize: usize,
    pub VmbChannelInitSetFlags: usize,
    pub VmbChannelInitSetFriendlyName: usize,
    pub VmbChannelInitSetMaximumExternalData: usize,
    pub VmbChannelInitSetMaximumPacketSize: usize,
    pub VmbChannelInitSetProcessPacketCallbacks: usize,
    pub VmbChannelInitSetStateChangeCallbacks: usize,
    pub VmbChannelPacketComplete: usize,
    pub VmbChannelPacketDeferToPassive: usize,
    pub VmbChannelPacketFail: usize,
    pub VmbChannelPacketGetExternalData: usize,
    pub VmbChannelPause: usize,
    pub VmbChannelSendSynchronousRequest: usize,
    pub VmbChannelSetIncomingProcessingAtPassive: usize,
    pub VmbChannelSetInterruptLatency: usize,
    pub VmbChannelSetPointer: usize,
    pub VmbChannelSetTransactionQuota: usize,
    pub VmbChannelSizeofPacket: usize,
    pub VmbChannelStart: usize,
    pub VmbClientChannelInitSetRingBufferPageCount: usize,
    pub VmbClientChannelInitSetTargetPnp: usize,
    pub VmbPacketAllocate: usize,
    pub VmbPacketFree: usize,
    pub VmbPacketGetChannel: usize,
    pub VmbPacketGetPointer: usize,
    pub VmbPacketInitialize: usize,
    pub VmbPacketSend: usize,
    pub VmbPacketSendWithExternalMdl: usize,
    pub VmbPacketSendWithExternalPfns: usize,
    pub VmbPacketSetCompletionRoutine: usize,
    pub VmbPacketSetPointer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_CLIENT_INTERFACE_V2 {
    pub VmbChannelPacketGetClientContext: usize,
    pub VmbChannelGetTargetDeviceObject: usize,
    pub VmbChannelGetParentDeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_CLIENT_INTERFACE_V3 {
    pub VmbChannelInitSetBounceBufferSizes: usize,
    pub VmbChannelAllocateBounceBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_CLIENT_INTERFACE_V4 {
    pub VmbChannelInitSetMaximumPacketCount: usize,
    pub VmbChannelCreateGpadlFromPfnList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_CLIENT_INTERFACE_V5 {
    pub VmbChannelInitSetPrimaryChannel: usize,
    pub VmbChannelInitialize: usize,
    pub VmbChannelSetAllowableDpcCpuUsage: usize,
    pub VmbClientChannelInitSetTargetProcessorIndex: usize,
    pub VmbSizeofChannel: usize,
    pub VmbChannelInitSetInlinePacketContextSize: usize,
    pub VmbPacketGetInlinePacketContextPointer: usize,
    pub VmbChannelInitSetProcessPacketCallbacksEx: usize,
    pub VmbPacketSetCompletionRoutineEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_SERVER_INTERFACE_V1 {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMCL_SERVER_INTERFACE_V5 {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPCI_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub WriteVfConfigBlock: usize,
    pub ReadVfConfigBlock: usize,
    pub SerialNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPCI_READ_BLOCK_INPUT {
    pub BlockId: u32,
    pub BytesRequested: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPCI_WRITE_BLOCK_INPUT {
    pub BlockId: u32,
    pub DataLength: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPCI_INVALIDATE_BLOCK_OUTPUT {
    pub BlockMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSYSTEM_TIME {
    pub LowPart: u32,
    pub High1Time: i32,
    pub High2Time: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPIN_LOCK_QUEUE {
    pub Next: usize,
    pub Lock: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KLOCK_QUEUE_HANDLE {
    pub LockQueue: u32,
    pub OldIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DPC_WATCHDOG_GLOBAL_TRIAGE_BLOCK {
    pub Signature: u32,
    pub Revision: u16,
    pub Size: u16,
    pub DpcWatchdogProfileOffset: u16,
    pub DpcWatchdogProfileLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSAVE_CET_U_FORMAT {
    pub Ia32CetUMsr: u32,
    pub Ia32Pl3SspMsr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSAVE_ARM64_SVE_HEADER {
    pub VectorLength: u32,
    pub VectorRegisterOffset: u32,
    pub PredicateRegisterOffset: u32,
    pub Reserved: [u32; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSTATE_CONTEXT {
    pub Mask: u32,
    pub Length: u32,
    pub Flags: u8,
    pub Reserved0: [u8; 3],
    pub Area: u32,
    pub Reserved2: u32,
    pub Buffer: usize,
    pub Reserved3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KERNEL_CET_CONTEXT {
    pub Ssp: u32,
    pub Rip: u32,
    pub SegCs: u16,
    pub AllFlags: u16,
    pub UseWrss: u16,
    pub PopShadowStackOne: u16,
    pub Unused: u16,
    pub Fill: [u16; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERIC_MAPPING {
    pub GenericRead: u32,
    pub GenericWrite: u32,
    pub GenericExecute: u32,
    pub GenericAll: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LUID_AND_ATTRIBUTES {
    pub Luid: u32,
    pub Attributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACL {
    pub AclRevision: u8,
    pub Sbz1: u8,
    pub AclSize: u16,
    pub AceCount: u16,
    pub Sbz2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRIVILEGE_SET {
    pub PrivilegeCount: u32,
    pub Control: u32,
    pub Privilege: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_QUALITY_OF_SERVICE {
    pub Length: u32,
    pub ImpersonationLevel: u32,
    pub ContextTrackingMode: u32,
    pub EffectiveOnly: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SE_IMPERSONATION_STATE {
    pub Token: usize,
    pub CopyOnOpen: u8,
    pub EffectiveOnly: u8,
    pub Level: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXTENDED_CREATE_DUAL_OPLOCK_KEYS {
    pub ParentOplockKey: u32,
    pub TargetOplockKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXTENDED_CREATE_INFORMATION {
    pub ExtendedCreateFlags: i64,
    pub EaBuffer: usize,
    pub EaLength: u32,
    pub DualOplockKeys: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXTENDED_CREATE_INFORMATION_32 {
    pub ExtendedCreateFlags: i64,
    pub EaBuffer: u32,
    pub EaLength: u32,
    pub DualOplockKeys: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_STATUS_BLOCK {
    pub Status: u32,
    pub Pointer: usize,
    pub Information: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_STATUS_BLOCK64 {
    pub Status: u32,
    pub Pointer: usize,
    pub Information: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_STATUS_BLOCK32 {
    pub Status: u32,
    pub Information: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_SESSION_CONNECT_INFO {
    pub SessionId: u32,
    pub LocalSession: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_BASIC_INFORMATION {
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub FileAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STANDARD_INFORMATION {
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub NumberOfLinks: u32,
    pub DeletePending: u8,
    pub Directory: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_STANDARD_INFORMATION_EX {
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub NumberOfLinks: u32,
    pub DeletePending: u8,
    pub Directory: u8,
    pub AlternateStream: u8,
    pub MetadataAttribute: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_POSITION_INFORMATION {
    pub CurrentByteOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NETWORK_OPEN_INFORMATION {
    pub CreationTime: i64,
    pub LastAccessTime: i64,
    pub LastWriteTime: i64,
    pub ChangeTime: i64,
    pub AllocationSize: i64,
    pub EndOfFile: i64,
    pub FileAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FULL_EA_INFORMATION {
    pub NextEntryOffset: u32,
    pub Flags: u8,
    pub EaNameLength: u8,
    pub EaValueLength: u16,
    pub EaName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_SFIO_RESERVE_INFORMATION {
    pub RequestsPerPeriod: u32,
    pub Period: u32,
    pub RetryFailures: u8,
    pub Discardable: u8,
    pub RequestSize: u32,
    pub NumOutstandingRequests: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_SFIO_VOLUME_INFORMATION {
    pub MaximumRequestsPerPeriod: u32,
    pub MinimumPeriod: u32,
    pub MinimumTransferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_IO_PRIORITY_HINT_INFORMATION {
    pub PriorityHint: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_IO_PRIORITY_HINT_INFORMATION_EX {
    pub PriorityHint: u32,
    pub BoostOutstanding: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_IO_COMPLETION_NOTIFICATION_INFORMATION {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_PROCESS_IDS_USING_FILE_INFORMATION {
    pub NumberOfProcessIdsInList: u32,
    pub ProcessIdList: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_IS_REMOTE_DEVICE_INFORMATION {
    pub IsRemote: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_NUMA_NODE_INFORMATION {
    pub NodeNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_IOSTATUSBLOCK_RANGE_INFORMATION {
    pub IoStatusBlockRange: *mut u8,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_MEMORY_PARTITION_INFORMATION {
    pub OwnerPartitionHandle: usize,
    pub NoCrossPartitionAccess: u8,
    pub Spare: [u8; 3],
    pub AllFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_FS_DEVICE_INFORMATION {
    pub DeviceType: u32,
    pub Characteristics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_ERROR_LOG_PACKET {
    pub MajorFunctionCode: u8,
    pub RetryCount: u8,
    pub DumpDataSize: u16,
    pub NumberOfStrings: u16,
    pub StringOffset: u16,
    pub EventCategory: u16,
    pub ErrorCode: u32,
    pub UniqueErrorValue: u32,
    pub FinalStatus: u32,
    pub SequenceNumber: u32,
    pub IoControlCode: u32,
    pub DeviceOffset: i64,
    pub DumpData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_ERROR_LOG_MESSAGE {
    pub Type: u16,
    pub Size: u16,
    pub DriverNameLength: u16,
    pub TimeStamp: i64,
    pub DriverNameOffset: u32,
    pub EntryData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_BASIC_INFORMATION {
    pub LastWriteTime: i64,
    pub TitleIndex: u32,
    pub NameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_NODE_INFORMATION {
    pub LastWriteTime: i64,
    pub TitleIndex: u32,
    pub ClassOffset: u32,
    pub ClassLength: u32,
    pub NameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_FULL_INFORMATION {
    pub LastWriteTime: i64,
    pub TitleIndex: u32,
    pub ClassOffset: u32,
    pub ClassLength: u32,
    pub SubKeys: u32,
    pub MaxNameLen: u32,
    pub MaxClassLen: u32,
    pub Values: u32,
    pub MaxValueNameLen: u32,
    pub MaxValueDataLen: u32,
    pub Class: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_WRITE_TIME_INFORMATION {
    pub LastWriteTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_WOW64_FLAGS_INFORMATION {
    pub UserFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_CONTROL_FLAGS_INFORMATION {
    pub ControlFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_SET_VIRTUALIZATION_INFORMATION {
    pub VirtualTarget: u32,
    pub VirtualStore: u32,
    pub VirtualSource: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_BASIC_INFORMATION {
    pub TitleIndex: u32,
    pub Type: u32,
    pub NameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_FULL_INFORMATION {
    pub TitleIndex: u32,
    pub Type: u32,
    pub DataOffset: u32,
    pub DataLength: u32,
    pub NameLength: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_PARTIAL_INFORMATION {
    pub TitleIndex: u32,
    pub Type: u32,
    pub DataLength: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_PARTIAL_INFORMATION_ALIGN64 {
    pub Type: u32,
    pub DataLength: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_LAYER_INFORMATION {
    pub IsTombstone: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_VALUE_ENTRY {
    pub ValueName: usize,
    pub DataLength: u32,
    pub DataOffset: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEY_TRUST_INFORMATION {
    pub TrustedKey: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_NAME_INFORMATION {
    pub Name: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEM_ADDRESS_REQUIREMENTS {
    pub LowestStartingAddress: usize,
    pub HighestEndingAddress: usize,
    pub Alignment: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEMORY_PARTITION_DEDICATED_MEMORY_OPEN_INFORMATION {
    pub DedicatedMemoryTypeId: u32,
    pub HandleAttributes: u32,
    pub DesiredAccess: u32,
    pub DedicatedMemoryPartitionHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLIENT_ID {
    pub UniqueProcess: usize,
    pub UniqueThread: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_PARTIAL_RESOURCE_DESCRIPTOR {
    pub Type: u8,
    pub ShareDisposition: u8,
    pub Flags: u16,
    pub Start: u64,
    pub Length: u32,
    pub Start_1: u64,
    pub Length_1: u32,
    pub Level: u16,
    pub Group: u16,
    pub Level_1: u32,
    pub Vector: u32,
    pub Affinity: u32,
    pub Group_1: u16,
    pub Reserved: u16,
    pub MessageCount: u16,
    pub Vector_1: u32,
    pub Affinity_1: u32,
    pub Level_2: u16,
    pub Group_2: u16,
    pub Level_3: u32,
    pub Vector_2: u32,
    pub Affinity_2: u32,
    pub Start_2: u64,
    pub Length_2: u32,
    pub Channel: u32,
    pub Port: u32,
    pub Reserved1: u32,
    pub Channel_1: u32,
    pub RequestLine: u32,
    pub TransferWidth: u8,
    pub Reserved1_1: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
    pub Data: [u32; 3],
    pub Start_3: u32,
    pub Length_3: u32,
    pub Reserved_1: u32,
    pub DataSize: u32,
    pub Reserved1_2: u32,
    pub Reserved2_1: u32,
    pub Start_4: u64,
    pub Length40: u32,
    pub Start_5: u64,
    pub Length48: u32,
    pub Start_6: u64,
    pub Length64: u32,
    pub Class: u8,
    pub Type_1: u8,
    pub Reserved1_3: u8,
    pub Reserved2_2: u8,
    pub IdLowPart: u32,
    pub IdHighPart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_PARTIAL_RESOURCE_LIST {
    pub Version: u16,
    pub Revision: u16,
    pub Count: u32,
    pub PartialDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_FULL_RESOURCE_DESCRIPTOR {
    pub InterfaceType: u32,
    pub BusNumber: u32,
    pub PartialResourceList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_RESOURCE_LIST {
    pub Count: u32,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_FLAGS {
    pub Failed: u32,
    pub ReadOnly: u32,
    pub Removable: u32,
    pub ConsoleIn: u32,
    pub ConsoleOut: u32,
    pub Input: u32,
    pub Output: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_COMPONENT_INFORMATION {
    pub Flags: u32,
    pub Version: u32,
    pub Key: u32,
    pub AffinityMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_ROM_BLOCK {
    pub Address: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_INT13_DRIVE_PARAMETER {
    pub DriveSelect: u16,
    pub MaxCylinders: u32,
    pub SectorsPerTrack: u16,
    pub MaxHeads: u16,
    pub NumberDrives: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_SCSI_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub HostIdentifier: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_VIDEO_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub VideoClock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_SONIC_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub DataConfigurationRegister: u16,
    pub EthernetAddress: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_SERIAL_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub BaudClock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_MONITOR_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub HorizontalScreenSize: u16,
    pub VerticalScreenSize: u16,
    pub HorizontalResolution: u16,
    pub VerticalResolution: u16,
    pub HorizontalDisplayTimeLow: u16,
    pub HorizontalDisplayTime: u16,
    pub HorizontalDisplayTimeHigh: u16,
    pub HorizontalBackPorchLow: u16,
    pub HorizontalBackPorch: u16,
    pub HorizontalBackPorchHigh: u16,
    pub HorizontalFrontPorchLow: u16,
    pub HorizontalFrontPorch: u16,
    pub HorizontalFrontPorchHigh: u16,
    pub HorizontalSyncLow: u16,
    pub HorizontalSync: u16,
    pub HorizontalSyncHigh: u16,
    pub VerticalBackPorchLow: u16,
    pub VerticalBackPorch: u16,
    pub VerticalBackPorchHigh: u16,
    pub VerticalFrontPorchLow: u16,
    pub VerticalFrontPorch: u16,
    pub VerticalFrontPorchHigh: u16,
    pub VerticalSyncLow: u16,
    pub VerticalSync: u16,
    pub VerticalSyncHigh: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_FLOPPY_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub Size: [i8; 8],
    pub MaxDensity: u32,
    pub MountDensity: u32,
    pub StepRateHeadUnloadTime: u8,
    pub HeadLoadTime: u8,
    pub MotorOffTime: u8,
    pub SectorLengthCode: u8,
    pub SectorPerTrack: u8,
    pub ReadWriteGapLength: u8,
    pub DataTransferLength: u8,
    pub FormatGapLength: u8,
    pub FormatFillCharacter: u8,
    pub HeadSettleTime: u8,
    pub MotorSettleTime: u8,
    pub MaximumTrackValue: u8,
    pub DataTransferRate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_KEYBOARD_DEVICE_DATA {
    pub Version: u16,
    pub Revision: u16,
    pub Type: u8,
    pub Subtype: u8,
    pub KeyboardFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CM_DISK_GEOMETRY_DEVICE_DATA {
    pub BytesPerSector: u32,
    pub NumberOfCylinders: u32,
    pub SectorsPerTrack: u32,
    pub NumberOfHeads: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_QUERY_REGISTRY_TABLE {
    pub QueryRoutine: usize,
    pub Flags: u32,
    pub Name: usize,
    pub EntryContext: usize,
    pub DefaultType: u32,
    pub DefaultData: usize,
    pub DefaultLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIME_FIELDS {
    pub Year: u32,
    pub Month: u32,
    pub Day: u32,
    pub Hour: u32,
    pub Minute: u32,
    pub Second: u32,
    pub Milliseconds: u32,
    pub Weekday: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_BITMAP {
    pub SizeOfBitMap: u32,
    pub Buffer: *mut u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_BITMAP_RUN {
    pub StartingIndex: u32,
    pub NumberOfBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OSVERSIONINFOA {
    pub dwOSVersionInfoSize: u32,
    pub dwMajorVersion: u32,
    pub dwMinorVersion: u32,
    pub dwBuildNumber: u32,
    pub dwPlatformId: u32,
    pub szCSDVersion: [i8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OSVERSIONINFOW {
    pub dwOSVersionInfoSize: u32,
    pub dwMajorVersion: u32,
    pub dwMinorVersion: u32,
    pub dwBuildNumber: u32,
    pub dwPlatformId: u32,
    pub szCSDVersion: [u32; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OSVERSIONINFOEXA {
    pub dwOSVersionInfoSize: u32,
    pub dwMajorVersion: u32,
    pub dwMinorVersion: u32,
    pub dwBuildNumber: u32,
    pub dwPlatformId: u32,
    pub szCSDVersion: [i8; 128],
    pub wServicePackMajor: u16,
    pub wServicePackMinor: u16,
    pub wSuiteMask: u16,
    pub wProductType: u8,
    pub wReserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OSVERSIONINFOEXW {
    pub dwOSVersionInfoSize: u32,
    pub dwMajorVersion: u32,
    pub dwMinorVersion: u32,
    pub dwBuildNumber: u32,
    pub dwPlatformId: u32,
    pub szCSDVersion: [u32; 128],
    pub wServicePackMajor: u16,
    pub wServicePackMinor: u16,
    pub wSuiteMask: u16,
    pub wProductType: u8,
    pub wReserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_POLICY_ENTRY {
    pub Type: u32,
    pub PolicyId: u32,
    pub None: usize,
    pub BoolValue: u8,
    pub Int8Value: u32,
    pub UInt8Value: u32,
    pub Int16Value: u32,
    pub UInt16Value: u32,
    pub Int32Value: i32,
    pub UInt32Value: u32,
    pub Int64Value: i64,
    pub UInt64Value: u64,
    pub AnsiStringValue: usize,
    pub UnicodeStringValue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IMAGE_POLICY_METADATA {
    pub Version: u8,
    pub Reserved0: [u8; 7],
    pub ApplicationId: u64,
    pub Policies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_BASIC_INFORMATION {
    pub TransactionId: u32,
    pub State: u32,
    pub Outcome: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTIONMANAGER_BASIC_INFORMATION {
    pub TmIdentity: u32,
    pub VirtualClock: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTIONMANAGER_LOG_INFORMATION {
    pub LogIdentity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTIONMANAGER_LOGPATH_INFORMATION {
    pub LogPathLength: u32,
    pub LogPath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTIONMANAGER_RECOVERY_INFORMATION {
    pub LastRecoveredLsn: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_PROPERTIES_INFORMATION {
    pub IsolationLevel: u32,
    pub IsolationFlags: u32,
    pub Timeout: i64,
    pub Outcome: u32,
    pub DescriptionLength: u32,
    pub Description: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_BIND_INFORMATION {
    pub TmHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_ENLISTMENT_PAIR {
    pub EnlistmentId: u32,
    pub ResourceManagerId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_ENLISTMENTS_INFORMATION {
    pub NumberOfEnlistments: u32,
    pub EnlistmentPair: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_SUPERIOR_ENLISTMENT_INFORMATION {
    pub SuperiorEnlistmentPair: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESOURCEMANAGER_BASIC_INFORMATION {
    pub ResourceManagerId: u32,
    pub DescriptionLength: u32,
    pub Description: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESOURCEMANAGER_COMPLETION_INFORMATION {
    pub IoCompletionPortHandle: usize,
    pub CompletionKey: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENLISTMENT_BASIC_INFORMATION {
    pub EnlistmentId: u32,
    pub TransactionId: u32,
    pub ResourceManagerId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENLISTMENT_CRM_INFORMATION {
    pub CrmTransactionManagerId: u32,
    pub CrmResourceManagerId: u32,
    pub CrmEnlistmentId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_LIST_ENTRY {
    pub UOW: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_LIST_INFORMATION {
    pub NumberOfTransactions: u32,
    pub TransactionInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KTMOBJECT_CURSOR {
    pub LastQuery: u32,
    pub ObjectIdCount: u32,
    pub ObjectIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERAL_LOOKASIDE_POOL {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KAPC {
    pub Type: u8,
    pub AllFlags: u8,
    pub Size: u8,
    pub SpareByte1: u8,
    pub SpareLong0: u32,
    pub field_Thread: u32,
    pub ApcListEntry: u32,
    pub Reserved: [usize; 3],
    pub NormalContext: usize,
    pub SystemArgument1: usize,
    pub SystemArgument2: usize,
    pub ApcStateIndex: u32,
    pub ApcMode: u32,
    pub Inserted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KDPC {
    pub TargetInfoAsUlong: u32,
    pub Type: u8,
    pub Importance: u8,
    pub Number: u16,
    pub DpcListEntry: u32,
    pub ProcessorHistory: u32,
    pub DeferredRoutine: usize,
    pub DeferredContext: usize,
    pub SystemArgument1: usize,
    pub SystemArgument2: usize,
    pub DpcData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MDL {
    pub field_Next: u32,
    pub Size: u32,
    pub MdlFlags: u32,
    pub field_Process: u32,
    pub MappedSystemVa: usize,
    pub StartVa: usize,
    pub ByteCount: u32,
    pub ByteOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISPATCHER_HEADER {
    pub Lock: i32,
    pub LockNV: i32,
    pub Type: u8,
    pub Signalling: u8,
    pub Size: u8,
    pub Reserved1: u8,
    pub TimerType: u8,
    pub TimerControlFlags: u8,
    pub Absolute: u8,
    pub Wake: u8,
    pub TIMER_TOLERABLE_DELAY_BITS: u32,
    pub Hand: u8,
    pub TimerMiscFlags: u8,
    pub TIMER_EXPIRED_INDEX_BITS: u32,
    pub Index: u8,
    pub TIMER_PROCESSOR_INDEX_BITS: u32,
    pub Inserted: u8,
    pub Expired: u8,
    pub Timer2Type: u8,
    pub Timer2Flags: u8,
    pub Timer2Inserted: u8,
    pub Timer2Expiring: u8,
    pub Timer2CancelPending: u8,
    pub Timer2SetPending: u8,
    pub Timer2Running: u8,
    pub Timer2Disabled: u8,
    pub Timer2ReservedFlags: u8,
    pub Timer2ComponentId: u8,
    pub Timer2RelativeId: u8,
    pub QueueType: u8,
    pub QueueControlFlags: u8,
    pub Abandoned: u8,
    pub DisableIncrement: u8,
    pub QueueReservedControlFlags: u8,
    pub QueueSize: u8,
    pub QueueReserved: u8,
    pub ThreadType: u8,
    pub ThreadReserved: u8,
    pub ThreadControlFlags: u8,
    pub CycleProfiling: u8,
    pub CounterProfiling: u8,
    pub GroupScheduling: u8,
    pub AffinitySet: u8,
    pub Tagged: u8,
    pub EnergyProfiling: u8,
    pub SchedulerAssist: u8,
    pub ThreadReservedControlFlags: u8,
    pub Instrumented: u8,
    pub DebugActive: u8,
    pub ActiveDR7: u8,
    pub Instrumented_1: u8,
    pub Minimal: u8,
    pub Reserved4: u8,
    pub AltSyscall: u8,
    pub Emulation: u8,
    pub Reserved5: u8,
    pub MutantType: u8,
    pub MutantSize: u8,
    pub DpcActive: u8,
    pub MutantReserved: u8,
    pub SignalState: i32,
    pub WaitListHead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEVENT {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KGATE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KTIMER {
    pub Header: u32,
    pub DueTime: u64,
    pub TimerListEntry: u32,
    pub field_Dpc: u32,
    pub Processor: u16,
    pub TimerType: u16,
    pub Period: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAST_MUTEX {
    pub Count: i32,
    pub Owner: usize,
    pub Contention: u32,
    pub Event: u32,
    pub OldIrql: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KFLOATING_SAVE {
    pub Dummy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEM_POOL_ZEROING_INFORMATION {
    pub PoolZeroingSupportPresent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCEPTION_RECORD {
    pub ExceptionCode: u32,
    pub ExceptionFlags: u32,
    pub field_ExceptionRecord: u32,
    pub ExceptionAddress: usize,
    pub NumberParameters: u32,
    pub ExceptionInformation: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCEPTION_RECORD32 {
    pub ExceptionCode: u32,
    pub ExceptionFlags: u32,
    pub ExceptionRecord: u32,
    pub ExceptionAddress: u32,
    pub NumberParameters: u32,
    pub ExceptionInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCEPTION_RECORD64 {
    pub ExceptionCode: u32,
    pub ExceptionFlags: u32,
    pub ExceptionRecord: u32,
    pub ExceptionAddress: u32,
    pub NumberParameters: u32,
    pub field_unusedAlignment: u32,
    pub ExceptionInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXCEPTION_POINTERS {
    pub ExceptionRecord: usize,
    pub ContextRecord: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KWAIT_BLOCK {
    pub WaitListEntry: u32,
    pub WaitType: u8,
    pub BlockState: u8,
    pub WaitKey: u16,
    pub SpareLong: i32,
    pub field_Thread: u32,
    pub field_NotificationQueue: u32,
    pub field_Dpc: u32,
    pub Object: usize,
    pub SparePtr: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KDEVICE_QUEUE {
    pub Type: u32,
    pub Size: u32,
    pub DeviceListHead: u32,
    pub Lock: u32,
    pub Busy: u8,
    pub Reserved: u32,
    pub Hint: u32,
    pub Busy_1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KDEVICE_QUEUE_ENTRY {
    pub DeviceListEntry: u32,
    pub SortKey: u32,
    pub Inserted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KMUTANT {
    pub Header: u32,
    pub MutantListEntry: u32,
    pub field_OwnerThread: u32,
    pub MutantFlags: u8,
    pub Abandoned: u8,
    pub Spare1: u8,
    pub ApcDisable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSEMAPHORE {
    pub Header: u32,
    pub Limit: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KE_SRCU_LOCK {
    pub Placeholder: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KDPC_WATCHDOG_INFORMATION {
    pub DpcTimeLimit: u32,
    pub DpcTimeCount: u32,
    pub DpcWatchdogLimit: u32,
    pub DpcWatchdogCount: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_CALLBACK_RECORD {
    pub Entry: u32,
    pub CallbackRoutine: usize,
    pub Buffer: u32,
    pub Length: u32,
    pub Component: *mut u8,
    pub Checksum: usize,
    pub State: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_REASON_CALLBACK_RECORD {
    pub Entry: u32,
    pub CallbackRoutine: usize,
    pub Component: *mut u8,
    pub Checksum: usize,
    pub Reason: usize,
    pub State: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_SECONDARY_DUMP_DATA {
    pub InBuffer: usize,
    pub InBufferLength: u32,
    pub MaximumAllowed: u32,
    pub Guid: u32,
    pub OutBuffer: usize,
    pub OutBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_SECONDARY_DUMP_DATA_EX {
    pub InBuffer: usize,
    pub InBufferLength: u32,
    pub MaximumAllowed: u32,
    pub Guid: u32,
    pub OutBuffer: usize,
    pub OutBufferLength: u32,
    pub Context: usize,
    pub Flags: u32,
    pub DumpType: u32,
    pub BugCheckCode: u32,
    pub BugCheckParameter1: usize,
    pub BugCheckParameter2: usize,
    pub BugCheckParameter3: usize,
    pub BugCheckParameter4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_DUMP_IO {
    pub Offset: u32,
    pub Buffer: usize,
    pub BufferLength: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_ADD_PAGES {
    pub Context: usize,
    pub Flags: u32,
    pub BugCheckCode: u32,
    pub Address: usize,
    pub Count: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_REMOVE_PAGES {
    pub Context: usize,
    pub Flags: u32,
    pub BugCheckCode: u32,
    pub Address: usize,
    pub Count: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KADDRESS_RANGE {
    pub Address: u32,
    pub Size: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KADDRESS_RANGE_DESCRIPTOR {
    pub field_AddressRanges: u32,
    pub AddressRangeCount: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KTRIAGE_DUMP_DATA_ARRAY {
    pub List: u32,
    pub NumBlocksUsed: u32,
    pub NumBlocksTotal: u32,
    pub DataSize: u32,
    pub MaxDataSize: u32,
    pub ComponentNameBufferLength: u32,
    pub ComponentName: *mut u8,
    pub Blocks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KBUGCHECK_TRIAGE_DUMP_DATA {
    pub DataArray: u32,
    pub Flags: u32,
    pub MaxVirtMemSize: u32,
    pub BugCheckCode: u32,
    pub BugCheckParameter1: usize,
    pub BugCheckParameter2: usize,
    pub BugCheckParameter3: usize,
    pub BugCheckParameter4: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KE_PROCESSOR_CHANGE_NOTIFY_CONTEXT {
    pub State: u32,
    pub NtNumber: u32,
    pub Status: u32,
    pub ProcNumber: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XSTATE_SAVE {
    pub Prev: usize,
    pub Thread: usize,
    pub Level: u8,
    pub XStateContext: u32,
    pub Dummy: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub Prev_1: usize,
    pub Reserved3: usize,
    pub Thread_1: usize,
    pub Reserved4: usize,
    pub Level_1: u8,
    pub XStateContext_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXT_SET_PARAMETERS {
    pub Version: u32,
    pub Reserved: u32,
    pub NoWakeTolerance: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KWAIT_CHAIN {
    pub Head: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOL_EXTENDED_PARAMS_SECURE_POOL {
    pub SecurePoolHandle: usize,
    pub Buffer: usize,
    pub Cookie: usize,
    pub SecurePoolFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOL_EXTENDED_PARAMETER {
    pub POOL_EXTENDED_PARAMETER_TYPE_BITS: u32,
    pub POOL_EXTENDED_PARAMETER_REQUIRED_FIELD_BITS: u32,
    pub POOL_EXTENDED_PARAMETER_RESERVED_BITS: u32,
    pub Reserved2: u32,
    pub Reserved3: usize,
    pub Priority: u32,
    pub SecurePoolParams: usize,
    pub PreferredNode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOL_CREATE_EXTENDED_PARAMETER {
    pub Type: usize,
    pub PoolName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOL_CREATE_EXTENDED_PARAMS {
    pub Version: u32,
    pub ParameterCount: u32,
    pub Parameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EX_RCU_FREE_POOL_CONTEXT {
    pub Placeholder: [usize; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOOKASIDE_LIST_EX {
    pub L: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WORK_QUEUE_ITEM {
    pub List: u32,
    pub WorkerRoutine: usize,
    pub Parameter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OWNER_ENTRY {
    pub OwnerThread: u32,
    pub IoPriorityBoosted: u32,
    pub OwnerReferenced: u32,
    pub IoQoSPriorityBoosted: u32,
    pub OwnerCount: u32,
    pub TableSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERESOURCE {
    pub SystemResourcesList: u32,
    pub OwnerTable: usize,
    pub ActiveCount: i16,
    pub Flag: u16,
    pub ReservedLowFlags: u8,
    pub WaiterPriority: u8,
    pub SharedWaiters: usize,
    pub ExclusiveWaiters: usize,
    pub OwnerEntry: u32,
    pub ActiveEntries: u32,
    pub ContentionCount: u32,
    pub NumberOfSharedWaiters: u32,
    pub NumberOfExclusiveWaiters: u32,
    pub MiscFlags: i8,
    pub Reserved1: [u8; 3],
    pub ResourceTimeoutCount: u32,
    pub Address: usize,
    pub CreatorBackTraceIndex: usize,
    pub SpinLock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESOURCE_HASH_ENTRY {
    pub ListEntry: u32,
    pub Address: usize,
    pub ContentionCount: u32,
    pub Number: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESOURCE_PERFORMANCE_DATA {
    pub ActiveResourceCount: u32,
    pub TotalResourceCount: u32,
    pub ExclusiveAcquire: u32,
    pub SharedFirstLevel: u32,
    pub SharedSecondLevel: u32,
    pub StarveFirstLevel: u32,
    pub StarveSecondLevel: u32,
    pub WaitForExclusive: u32,
    pub OwnerTableExpands: u32,
    pub MaximumTableExpand: u32,
    pub HashTable: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EX_RUNDOWN_REF {
    pub Count: u32,
    pub Ptr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXT_DELETE_PARAMETERS {
    pub Version: u32,
    pub Reserved: u32,
    pub DeleteCallback: usize,
    pub DeleteContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_DELETE_KEY_INFORMATION {
    pub Object: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_SET_VALUE_KEY_INFORMATION {
    pub Object: usize,
    pub ValueName: usize,
    pub TitleIndex: u32,
    pub Type: u32,
    pub Data: usize,
    pub DataSize: u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_DELETE_VALUE_KEY_INFORMATION {
    pub Object: usize,
    pub ValueName: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_SET_INFORMATION_KEY_INFORMATION {
    pub Object: usize,
    pub KeySetInformationClass: u32,
    pub KeySetInformation: usize,
    pub KeySetInformationLength: u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_ENUMERATE_KEY_INFORMATION {
    pub Object: usize,
    pub Index: u32,
    pub KeyInformationClass: u32,
    pub KeyInformation: usize,
    pub Length: u32,
    pub ResultLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_ENUMERATE_VALUE_KEY_INFORMATION {
    pub Object: usize,
    pub Index: u32,
    pub KeyValueInformationClass: u32,
    pub KeyValueInformation: usize,
    pub Length: u32,
    pub ResultLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_QUERY_KEY_INFORMATION {
    pub Object: usize,
    pub KeyInformationClass: u32,
    pub KeyInformation: usize,
    pub Length: u32,
    pub ResultLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_QUERY_VALUE_KEY_INFORMATION {
    pub Object: usize,
    pub ValueName: usize,
    pub KeyValueInformationClass: u32,
    pub KeyValueInformation: usize,
    pub Length: u32,
    pub ResultLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_QUERY_MULTIPLE_VALUE_KEY_INFORMATION {
    pub Object: usize,
    pub ValueEntries: usize,
    pub EntryCount: u32,
    pub ValueBuffer: usize,
    pub BufferLength: *mut u32,
    pub RequiredBufferLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_RENAME_KEY_INFORMATION {
    pub Object: usize,
    pub NewName: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_KEY_HANDLE_CLOSE_INFORMATION {
    pub Object: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_CREATE_KEY_INFORMATION {
    pub CompleteName: usize,
    pub RootObject: usize,
    pub ObjectType: usize,
    pub CreateOptions: u32,
    pub Class: usize,
    pub SecurityDescriptor: usize,
    pub SecurityQualityOfService: usize,
    pub DesiredAccess: u32,
    pub GrantedAccess: u32,
    pub Disposition: *mut u32,
    pub field_ResultObject: usize,
    pub CallContext: usize,
    pub RootObjectContext: usize,
    pub Transaction: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_CREATE_KEY_INFORMATION_V1 {
    pub CompleteName: usize,
    pub RootObject: usize,
    pub ObjectType: usize,
    pub Options: u32,
    pub Class: usize,
    pub SecurityDescriptor: usize,
    pub SecurityQualityOfService: usize,
    pub DesiredAccess: u32,
    pub GrantedAccess: u32,
    pub Disposition: *mut u32,
    pub field_ResultObject: usize,
    pub CallContext: usize,
    pub RootObjectContext: usize,
    pub Transaction: usize,
    pub Version: usize,
    pub RemainingName: usize,
    pub Wow64Flags: u32,
    pub Attributes: u32,
    pub CheckAccessMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_POST_OPERATION_INFORMATION {
    pub Object: usize,
    pub Status: u32,
    pub PreInformation: usize,
    pub ReturnStatus: u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_PRE_CREATE_KEY_INFORMATION {
    pub CompleteName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_POST_CREATE_KEY_INFORMATION {
    pub CompleteName: usize,
    pub Object: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_LOAD_KEY_INFORMATION {
    pub Object: usize,
    pub KeyName: usize,
    pub SourceFile: usize,
    pub Flags: u32,
    pub TrustClassObject: usize,
    pub UserEvent: usize,
    pub DesiredAccess: u32,
    pub RootHandle: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_UNLOAD_KEY_INFORMATION {
    pub Object: usize,
    pub UserEvent: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_CALLBACK_CONTEXT_CLEANUP_INFORMATION {
    pub Object: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_QUERY_KEY_SECURITY_INFORMATION {
    pub Object: usize,
    pub SecurityInformation: usize,
    pub SecurityDescriptor: usize,
    pub Length: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_SET_KEY_SECURITY_INFORMATION {
    pub Object: usize,
    pub SecurityInformation: usize,
    pub SecurityDescriptor: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_RESTORE_KEY_INFORMATION {
    pub Object: usize,
    pub FileHandle: usize,
    pub Flags: u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_SAVE_KEY_INFORMATION {
    pub Object: usize,
    pub FileHandle: usize,
    pub Format: u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_REPLACE_KEY_INFORMATION {
    pub Object: usize,
    pub OldFileName: usize,
    pub NewFileName: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_QUERY_KEY_NAME {
    pub Object: usize,
    pub ObjectNameInfo: usize,
    pub Length: u32,
    pub ReturnLength: *mut u32,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_LOAD_KEY_INFORMATION_V2 {
    pub Object: usize,
    pub KeyName: usize,
    pub SourceFile: usize,
    pub Flags: u32,
    pub TrustClassObject: usize,
    pub UserEvent: usize,
    pub DesiredAccess: u32,
    pub RootHandle: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Version: usize,
    pub FileAccessToken: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REG_SAVE_MERGED_KEY_INFORMATION {
    pub Object: usize,
    pub FileHandle: usize,
    pub HighKeyObject: usize,
    pub LowKeyObject: usize,
    pub CallContext: usize,
    pub ObjectContext: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MM_PHYSICAL_ADDRESS_LIST {
    pub PhysicalAddress: u64,
    pub NumberOfBytes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_SUBJECT_CONTEXT {
    pub ClientToken: usize,
    pub ImpersonationLevel: u32,
    pub PrimaryToken: usize,
    pub ProcessAuditId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INITIAL_PRIVILEGE_SET {
    pub PrivilegeCount: u32,
    pub Control: u32,
    pub Privilege: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACCESS_STATE {
    pub OperationID: u32,
    pub SecurityEvaluated: u8,
    pub GenerateAudit: u8,
    pub GenerateOnClose: u8,
    pub PrivilegesAllocated: u8,
    pub Flags: u32,
    pub RemainingDesiredAccess: u32,
    pub PreviouslyGrantedAccess: u32,
    pub OriginalDesiredAccess: u32,
    pub SubjectSecurityContext: u32,
    pub SecurityDescriptor: usize,
    pub AuxData: usize,
    pub InitialPrivilegeSet: u32,
    pub PrivilegeSet: usize,
    pub AuditPrivileges: u8,
    pub ObjectName: u32,
    pub ObjectTypeName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NAMED_PIPE_CREATE_PARAMETERS {
    pub NamedPipeType: u32,
    pub ReadMode: u32,
    pub CompletionMode: u32,
    pub MaximumInstances: u32,
    pub InboundQuota: u32,
    pub OutboundQuota: u32,
    pub DefaultTimeout: i64,
    pub TimeoutSpecified: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MAILSLOT_CREATE_PARAMETERS {
    pub MailslotQuota: u32,
    pub MaximumMessageSize: u32,
    pub ReadTimeout: i64,
    pub TimeoutSpecified: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAST_IO_DISPATCH {
    pub SizeOfFastIoDispatch: u32,
    pub FastIoCheckIfPossible: usize,
    pub FastIoRead: usize,
    pub FastIoWrite: usize,
    pub FastIoQueryBasicInfo: usize,
    pub FastIoQueryStandardInfo: usize,
    pub FastIoLock: usize,
    pub FastIoUnlockSingle: usize,
    pub FastIoUnlockAll: usize,
    pub FastIoUnlockAllByKey: usize,
    pub FastIoDeviceControl: usize,
    pub AcquireFileForNtCreateSection: usize,
    pub ReleaseFileForNtCreateSection: usize,
    pub FastIoDetachDevice: usize,
    pub FastIoQueryNetworkOpenInfo: usize,
    pub AcquireForModWrite: usize,
    pub MdlRead: usize,
    pub MdlReadComplete: usize,
    pub PrepareMdlWrite: usize,
    pub MdlWriteComplete: usize,
    pub FastIoReadCompressed: usize,
    pub FastIoWriteCompressed: usize,
    pub MdlReadCompleteCompressed: usize,
    pub MdlWriteCompleteCompressed: usize,
    pub FastIoQueryOpen: usize,
    pub ReleaseForModWrite: usize,
    pub AcquireForCcFlush: usize,
    pub ReleaseForCcFlush: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_SECURITY_CONTEXT {
    pub SecurityQos: usize,
    pub AccessState: usize,
    pub DesiredAccess: u32,
    pub FullCreateOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VPB {
    pub Type: u32,
    pub Size: u32,
    pub Flags: u16,
    pub VolumeLabelLength: u16,
    pub field_DeviceObject: u32,
    pub field_RealDevice: u32,
    pub SerialNumber: u32,
    pub ReferenceCount: u32,
    pub VolumeLabel: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAIT_CONTEXT_BLOCK {
    pub WaitQueueEntry: u32,
    pub DmaWaitEntry: u32,
    pub NumberOfChannels: u32,
    pub SyncCallback: u32,
    pub DmaContext: u32,
    pub ZeroMapRegisters: u32,
    pub Reserved: u32,
    pub NumberOfRemapPages: u32,
    pub DeviceRoutine: usize,
    pub DeviceContext: usize,
    pub NumberOfMapRegisters: u32,
    pub DeviceObject: usize,
    pub CurrentIrp: usize,
    pub BufferChainingDpc: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVOBJ_EXTENSION {
    pub Type: u32,
    pub Size: u16,
    pub DeviceObject: usize,
    pub PowerFlags: u32,
    pub field_Dope: u32,
    pub ExtensionFlags: u32,
    pub DeviceNode: usize,
    pub AttachedTo: usize,
    pub StartIoCount: u32,
    pub StartIoKey: i32,
    pub StartIoFlags: u32,
    pub Vpb: usize,
    pub DependencyNode: usize,
    pub InterruptContext: usize,
    pub InterruptCount: u32,
    pub VerifierContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_EXTENSION {
    pub field_DriverObject: u32,
    pub AddDevice: usize,
    pub Count: u32,
    pub ServiceKeyName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_OBJECT {
    pub Type: u32,
    pub Size: u32,
    pub DeviceObject: usize,
    pub Flags: u32,
    pub DriverStart: usize,
    pub DriverSize: u32,
    pub DriverSection: usize,
    pub DriverExtension: usize,
    pub DriverName: u32,
    pub HardwareDatabase: usize,
    pub FastIoDispatch: usize,
    pub DriverInit: usize,
    pub DriverStartIo: usize,
    pub DriverUnload: usize,
    pub MajorFunction: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECTION_OBJECT_POINTERS {
    pub DataSectionObject: usize,
    pub SharedCacheMap: usize,
    pub ImageSectionObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_COMPLETION_CONTEXT {
    pub Port: usize,
    pub Key: usize,
    pub UsageCount: isize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_OBJECT {
    pub Type: u32,
    pub Size: u32,
    pub DeviceObject: usize,
    pub Vpb: usize,
    pub FsContext: usize,
    pub FsContext2: usize,
    pub SectionObjectPointer: usize,
    pub PrivateCacheMap: usize,
    pub FinalStatus: u32,
    pub field_RelatedFileObject: u32,
    pub LockOperation: u8,
    pub DeletePending: u8,
    pub ReadAccess: u8,
    pub WriteAccess: u8,
    pub DeleteAccess: u8,
    pub SharedRead: u8,
    pub SharedWrite: u8,
    pub SharedDelete: u8,
    pub Flags: u32,
    pub FileName: u32,
    pub CurrentByteOffset: i64,
    pub Waiters: u32,
    pub Busy: u32,
    pub LastLock: usize,
    pub Lock: u32,
    pub Event: u32,
    pub CompletionContext: u32,
    pub IrpListLock: u32,
    pub IrpList: u32,
    pub field_FileObjectExtension: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_RELATIONS {
    pub Count: u32,
    pub Objects: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_CAPABILITIES {
    pub Size: u32,
    pub Version: u16,
    pub DeviceD1: u32,
    pub DeviceD2: u32,
    pub LockSupported: u32,
    pub EjectSupported: u32,
    pub Removable: u32,
    pub DockDevice: u32,
    pub UniqueID: u32,
    pub SilentInstall: u32,
    pub RawDeviceOK: u32,
    pub SurpriseRemovalOK: u32,
    pub WakeFromD0: u32,
    pub WakeFromD1: u32,
    pub WakeFromD2: u32,
    pub WakeFromD3: u32,
    pub HardwareDisabled: u32,
    pub NonDynamic: u32,
    pub WarmEjectSupported: u32,
    pub NoDisplayInUI: u32,
    pub Reserved1: u32,
    pub WakeFromInterrupt: u32,
    pub SecureDevice: u32,
    pub ChildOfVgaEnabledBridge: u32,
    pub DecodeIoOnBoot: u32,
    pub Reserved: u32,
    pub Address: u32,
    pub UINumber: u32,
    pub DeviceState: [u32; 1],
    pub SystemWake: u32,
    pub DeviceWake: u32,
    pub D1Latency: u32,
    pub D2Latency: u32,
    pub D3Latency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_SEQUENCE {
    pub SequenceD1: u32,
    pub SequenceD2: u32,
    pub SequenceD3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_STACK_LOCATION {
    pub MajorFunction: u8,
    pub MinorFunction: u8,
    pub Flags: u8,
    pub Control: u8,
    pub SecurityContext: usize,
    pub Options: u32,
    pub FileAttributes: u32,
    pub ShareAccess: u16,
    pub EaLength: u32,
    pub SecurityContext_1: usize,
    pub Options_1: u32,
    pub Reserved: u32,
    pub ShareAccess_1: u16,
    pub Parameters: usize,
    pub SecurityContext_2: usize,
    pub Options_2: u32,
    pub Reserved_1: u32,
    pub ShareAccess_2: u16,
    pub Parameters_1: usize,
    pub Length: u32,
    pub Key: u32,
    pub Flags_1: u32,
    pub ByteOffset: i64,
    pub Length_1: u32,
    pub Key_1: u32,
    pub Flags_2: u32,
    pub ByteOffset_1: i64,
    pub Length_2: u32,
    pub FileName: usize,
    pub FileInformationClass: u32,
    pub FileIndex: u32,
    pub Length_3: u32,
    pub CompletionFilter: u32,
    pub Length_4: u32,
    pub CompletionFilter_1: u32,
    pub DirectoryNotifyInformationClass: u32,
    pub Length_5: u32,
    pub FileInformationClass_1: u32,
    pub Length_6: u32,
    pub FileInformationClass_2: u32,
    pub FileObject: usize,
    pub ReplaceIfExists: u8,
    pub AdvanceOnly: u8,
    pub ClusterCount: u32,
    pub DeleteHandle: usize,
    pub Length_7: u32,
    pub EaList: usize,
    pub EaListLength: u32,
    pub EaIndex: u32,
    pub Length_8: u32,
    pub Length_9: u32,
    pub FsInformationClass: u32,
    pub Length_10: u32,
    pub FsInformationClass_1: u32,
    pub OutputBufferLength: u32,
    pub InputBufferLength: u32,
    pub FsControlCode: u32,
    pub Type3InputBuffer: usize,
    pub Length_11: *mut i64,
    pub Key_2: u32,
    pub ByteOffset_2: i64,
    pub OutputBufferLength_1: u32,
    pub InputBufferLength_1: u32,
    pub IoControlCode: u32,
    pub Type3InputBuffer_1: usize,
    pub SecurityInformation: u32,
    pub Length_12: u32,
    pub SecurityInformation_1: u32,
    pub SecurityDescriptor: usize,
    pub Vpb: usize,
    pub DeviceObject: usize,
    pub OutputBufferLength_2: u32,
    pub Vpb_1: usize,
    pub DeviceObject_1: usize,
    pub field_Srb: u32,
    pub Length_13: u32,
    pub StartSid: usize,
    pub SidList: usize,
    pub SidListLength: u32,
    pub Length_14: u32,
    pub Type: u32,
    pub field_InterfaceType: u32,
    pub Size: u16,
    pub Version: u16,
    pub Interface: usize,
    pub InterfaceSpecificData: usize,
    pub Capabilities: usize,
    pub IoResourceRequirementList: usize,
    pub WhichSpace: u32,
    pub Buffer: usize,
    pub Offset: u32,
    pub Length_15: u32,
    pub Lock: u8,
    pub IdType: u32,
    pub DeviceTextType: u32,
    pub LocaleId: u32,
    pub InPath: u8,
    pub Reserved_2: [u8; 3],
    pub Type_1: u32,
    pub PowerState: u32,
    pub PowerSequence: usize,
    pub SystemContext: u32,
    pub SystemPowerStateContext: u32,
    pub Type_2: usize,
    pub State: usize,
    pub ShutdownType: usize,
    pub SystemContext_1: u32,
    pub Type_3: usize,
    pub State_1: usize,
    pub ShutdownType_1: usize,
    pub AllocatedResources: usize,
    pub AllocatedResourcesTranslated: usize,
    pub ProviderId: usize,
    pub DataPath: usize,
    pub BufferSize: u32,
    pub Buffer_1: usize,
    pub Argument1: usize,
    pub Argument2: usize,
    pub Argument3: usize,
    pub Argument4: usize,
    pub DeviceObject_2: usize,
    pub FileObject_1: usize,
    pub CompletionRoutine: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SHARE_ACCESS {
    pub OpenCount: u32,
    pub Readers: u32,
    pub Writers: u32,
    pub Deleters: u32,
    pub SharedRead: u32,
    pub SharedWrite: u32,
    pub SharedDelete: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LINK_SHARE_ACCESS {
    pub OpenCount: u32,
    pub Deleters: u32,
    pub SharedDelete: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BOOTDISK_INFORMATION {
    pub BootPartitionOffset: i64,
    pub SystemPartitionOffset: i64,
    pub BootDeviceSignature: u32,
    pub SystemDeviceSignature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BOOTDISK_INFORMATION_EX {
    pub BootPartitionOffset: i64,
    pub SystemPartitionOffset: i64,
    pub BootDeviceSignature: u32,
    pub SystemDeviceSignature: u32,
    pub BootDeviceGuid: u32,
    pub SystemDeviceGuid: u32,
    pub BootDeviceIsGpt: u8,
    pub SystemDeviceIsGpt: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LOADER_PARTITION_INFORMATION_EX {
    pub PartitionStyle: u32,
    pub PartitionNumber: u32,
    pub Signature: u32,
    pub DeviceId: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BOOTDISK_INFORMATION_LITE {
    pub BootDeviceSignature: u32,
    pub SystemDeviceSignature: u32,
    pub BootDeviceGuid: u32,
    pub SystemDeviceGuid: u32,
    pub BootDeviceIsGpt: u8,
    pub SystemDeviceIsGpt: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_INTERRUPT_MESSAGE_INFO_ENTRY {
    pub MessageAddress: u64,
    pub TargetProcessorSet: u32,
    pub InterruptObject: usize,
    pub MessageData: u32,
    pub Vector: u32,
    pub Irql: u32,
    pub Mode: u32,
    pub Polarity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_INTERRUPT_MESSAGE_INFO {
    pub UnifiedIrql: u32,
    pub MessageCount: u32,
    pub MessageInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CONNECT_INTERRUPT_FULLY_SPECIFIED_PARAMETERS {
    pub PhysicalDeviceObject: usize,
    pub field_InterruptObject: usize,
    pub ServiceRoutine: usize,
    pub ServiceContext: usize,
    pub SpinLock: u32,
    pub SynchronizeIrql: u32,
    pub FloatingSave: u8,
    pub ShareVector: u8,
    pub Vector: u32,
    pub Irql: u32,
    pub InterruptMode: u32,
    pub ProcessorEnableMask: u32,
    pub Group: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CONNECT_INTERRUPT_LINE_BASED_PARAMETERS {
    pub PhysicalDeviceObject: usize,
    pub field_InterruptObject: usize,
    pub ServiceRoutine: usize,
    pub ServiceContext: usize,
    pub SpinLock: u32,
    pub SynchronizeIrql: u32,
    pub FloatingSave: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CONNECT_INTERRUPT_MESSAGE_BASED_PARAMETERS {
    pub PhysicalDeviceObject: usize,
    pub field_Generic: usize,
    pub field_InterruptMessageTable: usize,
    pub field_InterruptObject: usize,
    pub MessageServiceRoutine: usize,
    pub ServiceContext: usize,
    pub SpinLock: u32,
    pub SynchronizeIrql: u32,
    pub FloatingSave: u8,
    pub FallBackServiceRoutine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CONNECT_INTERRUPT_PARAMETERS {
    pub Version: u32,
    pub FullySpecified: u32,
    pub LineBased: u32,
    pub MessageBased: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_DISCONNECT_INTERRUPT_PARAMETERS {
    pub Version: u32,
    pub Generic: usize,
    pub InterruptObject: usize,
    pub InterruptMessageTable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_REPORT_INTERRUPT_ACTIVE_STATE_PARAMETERS {
    pub Version: u32,
    pub Generic: usize,
    pub InterruptObject: usize,
    pub InterruptMessageTable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_REMOVE_LOCK_COMMON_BLOCK {
    pub Removed: u8,
    pub reserved_padding: [u32; 3],
    pub IoCount: u32,
    pub RemoveEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_REMOVE_LOCK_DBG_BLOCK {
    pub Signature: i32,
    pub HighWatermark: u32,
    pub MaxLockedTicks: i64,
    pub AllocateTag: i32,
    pub LockList: u32,
    pub Spin: u32,
    pub LowMemoryCount: u32,
    pub Reserved1: [u32; 4],
    pub Reserved2: usize,
    pub Blocks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_REMOVE_LOCK {
    pub Common: u32,
    pub Dbg: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CSQ_IRP_CONTEXT {
    pub Type: u32,
    pub Irp: usize,
    pub Csq: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_CSQ {
    pub Type: u32,
    pub CsqInsertIrp: usize,
    pub CsqRemoveIrp: usize,
    pub CsqPeekNextIrp: usize,
    pub CsqAcquireLock: usize,
    pub CsqReleaseLock: usize,
    pub CsqCompleteCanceledIrp: usize,
    pub ReservePointer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_ATTRIBUTION_INFORMATION {
    pub Version: u32,
    pub MajorCode: u32,
    pub IoStart: u32,
    pub QueueOnly: u32,
    pub IoFailed: u32,
    pub VirtualDevice: u32,
    pub Spare: u32,
    pub AllFlags: u32,
    pub Length: u32,
    pub ServiceStartTime: u64,
    pub CurrentTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_SESSION_STATE_NOTIFICATION {
    pub Size: u32,
    pub Flags: u32,
    pub IoObject: usize,
    pub EventMask: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_SESSION_STATE_INFORMATION {
    pub SessionId: u32,
    pub SessionState: u32,
    pub LocalSession: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_PROXY_ENDPOINT_INFORMATION {
    pub Id: u32,
    pub EndpointFunction: usize,
    pub ParameterCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVER_PROXY_HOTSWAP_WORKER_ROUTINE_START_CONTEXT {
    pub WorkerRoutine: usize,
    pub Context: usize,
    pub WaitType: u32,
    pub WaitReason: u32,
    pub WaitMode: u32,
    pub Altertable: u8,
    pub HasTimeout: u8,
    pub Timeout: i64,
    pub EventCount: u32,
    pub Events: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_TRACE_SESSION_SETTINGS {
    pub Version: u32,
    pub BufferSize: u32,
    pub MinimumBuffers: u32,
    pub MaximumBuffers: u32,
    pub LoggerMode: u32,
    pub FlushTimer: u32,
    pub FlushThreshold: u32,
    pub ClockType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_BUS_INFORMATION {
    pub BusTypeGuid: u32,
    pub LegacyBusType: u32,
    pub BusNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LEGACY_BUS_INFORMATION {
    pub BusTypeGuid: u32,
    pub LegacyBusType: u32,
    pub BusNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub TranslateBusAddress: usize,
    pub GetDmaAdapter: usize,
    pub SetBusData: usize,
    pub GetBusData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_VIRTUALIZATION_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetVirtualFunctionData: usize,
    pub GetVirtualFunctionData: usize,
    pub GetLocation: usize,
    pub GetResources: usize,
    pub EnableVirtualization: usize,
    pub GetVirtualFunctionProbedBars: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_SECURITY_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetAccessControlServices: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_SECURITY_INTERFACE2 {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Flags: u32,
    pub SupportedScenarios: u32,
    pub SetAccessControlServices: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_ATS_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetAddressTranslationServices: usize,
    pub InvalidateQueueDepth: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REENUMERATE_SELF_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SurpriseRemoveAndReenumerateSelf: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_EXTENDED_ADDRESS_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub QueryExtendedAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3COLD_SUPPORT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetD3ColdSupport: usize,
    pub GetIdleWakeInfo: usize,
    pub GetD3ColdCapability: usize,
    pub GetBusDriverD3ColdSupport: usize,
    pub GetLastTransitionStatus: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3COLD_AUX_POWER_AND_TIMING_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub RequestCorePowerRail: usize,
    pub RequestAuxPower: usize,
    pub RequestPerstDelay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FPGA_CONTROL_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub BusScan: usize,
    pub ControlLink: usize,
    pub ControlConfigSpace: usize,
    pub ControlErrorReporting: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PTM_CONTROL_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub QueryGranularity: usize,
    pub QueryTimeSource: usize,
    pub Enable: usize,
    pub Disable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUS_RESOURCE_UPDATE_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GetUpdatedBusResource: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPEM_CONTROL_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetNpemSupportState: usize,
    pub QueryStandardCapabilities: usize,
    pub SetStandardControl: usize,
    pub QueryNpemControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub GpeConnectVector: usize,
    pub GpeDisconnectVector: usize,
    pub GpeEnableEvent: usize,
    pub GpeDisableEvent: usize,
    pub GpeClearStatus: usize,
    pub RegisterForDeviceNotifications: usize,
    pub UnregisterForDeviceNotifications: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PLUGPLAY_NOTIFICATION_HEADER {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HWPROFILE_CHANGE_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_INTERFACE_CHANGE_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
    pub InterfaceClassGuid: u32,
    pub SymbolicLinkName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TARGET_DEVICE_REMOVAL_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
    pub FileObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KERNEL_SOFT_RESTART_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TARGET_DEVICE_CUSTOM_NOTIFICATION {
    pub Version: u16,
    pub Size: u16,
    pub Event: u32,
    pub FileObject: usize,
    pub NameBufferOffset: i32,
    pub CustomDataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_REPLACE_MEMORY_LIST {
    pub AllocatedCount: u32,
    pub Count: u32,
    pub TotalLength: u64,
    pub Address: u64,
    pub Length: u64,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_REPLACE_PROCESSOR_LIST {
    pub Affinity: usize,
    pub GroupCount: u32,
    pub AllocatedCount: u32,
    pub Count: u32,
    pub ApicIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_REPLACE_PROCESSOR_LIST_V1 {
    pub AffinityMask: u32,
    pub AllocatedCount: u32,
    pub Count: u32,
    pub ApicIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_REPLACE_PARAMETERS {
    pub Size: u32,
    pub Version: u32,
    pub Target: u32,
    pub Spare: u32,
    pub TargetProcessors: usize,
    pub SpareProcessors: usize,
    pub TargetMemory: usize,
    pub SpareMemory: usize,
    pub MapMemory: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_REPLACE_DRIVER_INTERFACE {
    pub Size: u32,
    pub Version: u32,
    pub Flags: u32,
    pub Unload: usize,
    pub BeginReplace: usize,
    pub EndReplace: usize,
    pub MirrorPhysicalMemory: usize,
    pub SetProcessorId: usize,
    pub Swap: usize,
    pub InitiateHardwareMirror: usize,
    pub MirrorPlatformMemory: usize,
    pub GetMemoryDestination: usize,
    pub EnableDisableHardwareQuiesce: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRASHDUMP_FUNCTIONS_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub PowerOn: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FUNCTION_LEVEL_DEVICE_RESET_PARAMETERS {
    pub Size: u32,
    pub DeviceResetCompletion: usize,
    pub CompletionContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_BUS_SPECIFIC_RESET_INFO {
    pub BusTypeGuid: u32,
    pub ResetTypeSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_RESET_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DeviceReset: usize,
    pub SupportedResetTypes: u32,
    pub Reserved: usize,
    pub QueryBusSpecificResetInfo: usize,
    pub DeviceBusSpecificReset: usize,
    pub GetDeviceResetStatus: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_DRIVER_INTERFACE {
    pub InterfaceHeader: u32,
    pub ProcessReference: usize,
    pub ProcessDereference: usize,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SDEV_IDENTIFIER_INTERFACE {
    pub InterfaceHeader: u32,
    pub GetIdentifier: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DESCRIPTION {
    pub Version: u32,
    pub Master: u8,
    pub ScatterGather: u8,
    pub DemandMode: u8,
    pub AutoInitialize: u8,
    pub Dma32BitAddresses: u8,
    pub IgnoreCount: u8,
    pub Reserved1: u8,
    pub Dma64BitAddresses: u8,
    pub BusNumber: u32,
    pub DmaChannel: u32,
    pub InterfaceType: u32,
    pub DmaWidth: u32,
    pub DmaSpeed: u32,
    pub MaximumLength: u32,
    pub DmaPort: u32,
    pub DmaAddressWidth: u32,
    pub DmaControllerInstance: u32,
    pub DmaRequestLine: u32,
    pub DeviceAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_ADAPTER_INFO_V1 {
    pub ReadDmaCounterAvailable: u32,
    pub ScatterGatherLimit: u32,
    pub DmaAddressWidth: u32,
    pub Flags: u32,
    pub MinimumTransferUnit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_ADAPTER_INFO_CRASHDUMP {
    pub DeviceDescription: u32,
    pub DeviceIdSize: usize,
    pub DeviceId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_TRANSFER_INFO_V1 {
    pub MapRegisterCount: u32,
    pub ScatterGatherElementCount: u32,
    pub ScatterGatherListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_TRANSFER_INFO_V2 {
    pub MapRegisterCount: u32,
    pub ScatterGatherElementCount: u32,
    pub ScatterGatherListSize: u32,
    pub LogicalPageCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_TRANSFER_INFO {
    pub Version: u32,
    pub V1: u32,
    pub V2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCATTER_GATHER_ELEMENT {
    pub Address: u64,
    pub Length: u32,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCATTER_GATHER_LIST {
    pub NumberOfElements: u32,
    pub Reserved: usize,
    pub Elements: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_ADAPTER {
    pub Version: u16,
    pub Size: u16,
    pub DmaOperations: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_COMMON_BUFFER_EXTENDED_CONFIGURATION {
    pub ConfigType: u32,
    pub MinimumAddress: u64,
    pub MaximumAddress: u64,
    pub Offset: u64,
    pub Length: u32,
    pub HardwareAccessType: u32,
    pub Reserved: [u64; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_OPERATIONS {
    pub Size: u32,
    pub PutDmaAdapter: usize,
    pub AllocateCommonBuffer: usize,
    pub FreeCommonBuffer: usize,
    pub AllocateAdapterChannel: usize,
    pub FlushAdapterBuffers: usize,
    pub FreeAdapterChannel: usize,
    pub FreeMapRegisters: usize,
    pub MapTransfer: usize,
    pub GetDmaAlignment: usize,
    pub ReadDmaCounter: usize,
    pub GetScatterGatherList: usize,
    pub PutScatterGatherList: usize,
    pub CalculateScatterGatherList: usize,
    pub BuildScatterGatherList: usize,
    pub BuildMdlFromScatterGatherList: usize,
    pub GetDmaAdapterInfo: usize,
    pub GetDmaTransferInfo: usize,
    pub InitializeDmaTransferContext: usize,
    pub AllocateCommonBufferEx: usize,
    pub AllocateAdapterChannelEx: usize,
    pub ConfigureAdapterChannel: usize,
    pub CancelAdapterChannel: usize,
    pub MapTransferEx: usize,
    pub GetScatterGatherListEx: usize,
    pub BuildScatterGatherListEx: usize,
    pub FlushAdapterBuffersEx: usize,
    pub FreeAdapterObject: usize,
    pub CancelMappedTransfer: usize,
    pub AllocateDomainCommonBuffer: usize,
    pub FlushDmaBuffer: usize,
    pub JoinDmaDomain: usize,
    pub LeaveDmaDomain: usize,
    pub GetDmaDomain: usize,
    pub AllocateCommonBufferWithBounds: usize,
    pub AllocateCommonBufferVector: usize,
    pub GetCommonBufferFromVectorByIndex: usize,
    pub FreeCommonBufferFromVector: usize,
    pub FreeCommonBufferVector: usize,
    pub CreateCommonBufferFromMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAULT_INFORMATION_X64_FLAGS {
    pub FaultAddressValid: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAULT_INFORMATION_ARM64_FLAGS {
    pub WriteNotRead: u32,
    pub InstructionNotData: u32,
    pub Privileged: u32,
    pub FaultAddressValid: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAULT_INFORMATION_X64 {
    pub DomainHandle: usize,
    pub FaultAddress: usize,
    pub Flags: u32,
    pub Type: u32,
    pub IommuBaseAddress: u32,
    pub PciSegment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAULT_INFORMATION_ARM64 {
    pub DomainHandle: usize,
    pub FaultAddress: usize,
    pub PhysicalDeviceObject: usize,
    pub InputMappingId: u32,
    pub Flags: u32,
    pub Type: u32,
    pub IommuBaseAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FAULT_INFORMATION {
    pub Type: u32,
    pub IsStage1: u8,
    pub Arm64: u32,
    pub X64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOMAIN_CONFIGURATION_ARM64 {
    pub Ttbr0: u64,
    pub Ttbr1: u64,
    pub Mair0: u32,
    pub Mair1: u32,
    pub InputSize0: u8,
    pub InputSize1: u8,
    pub CoherentTableWalks: u8,
    pub TranslationEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOMAIN_CONFIGURATION_X64 {
    pub FirstLevelPageTableRoot: u64,
    pub TranslationEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOMAIN_CONFIGURATION {
    pub Type: u32,
    pub Arm64: u32,
    pub X64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_FAULT_CONFIGURATION {
    pub FaultHandler: usize,
    pub FaultContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DEVICE_CREATION_CONFIGURATION_ACPI {
    pub InputMappingBase: u32,
    pub MappingsCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DEVICE_CREATION_CONFIGURATION_PASID {
    pub ConfigType: u32,
    pub SuppressPasidFaults: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DEVICE_CREATION_CONFIGURATION {
    pub NextConfiguration: u32,
    pub ConfigType: u32,
    pub Acpi: u32,
    pub DeviceId: usize,
    pub Pasid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_MAP_PHYSICAL_ADDRESS {
    pub MapType: u32,
    pub Mdl: usize,
    pub Base: u64,
    pub Size: usize,
    pub PageFrame: usize,
    pub NumberOfPages: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DMA_RESERVED_REGION {
    pub field_RegionNext: u32,
    pub Base: u32,
    pub NumberOfPages: usize,
    pub ShouldMap: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DMA_LOGICAL_ALLOCATOR_CONFIG {
    pub LogicalAllocatorType: u32,
    pub AddressWidth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DMA_LOGICAL_ADDRESS_TOKEN {
    pub LogicalAddressBase: u32,
    pub Size: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DMA_LOGICAL_ADDRESS_TOKEN_MAPPED_SEGMENT {
    pub OwningToken: usize,
    pub Offset: usize,
    pub Size: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_INTERFACE_STATE_CHANGE {
    pub PresentFields: u32,
    pub AvailableDomainTypes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IOMMU_DMA_DEVICE_INFORMATION {
    pub DefaultPasidEnabled: u8,
    pub PasidTaggedDmaEnabled: u8,
    pub PasidFaultsSuppressed: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INPUT_MAPPING_ELEMENT {
    pub InputMappingId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_IOMMU_INTERFACE {
    pub Version: u32,
    pub CreateDomain: usize,
    pub DeleteDomain: usize,
    pub AttachDevice: usize,
    pub DetachDevice: usize,
    pub FlushDomain: usize,
    pub FlushDomainByVaList: usize,
    pub QueryInputMappings: usize,
    pub MapLogicalRange: usize,
    pub UnmapLogicalRange: usize,
    pub MapIdentityRange: usize,
    pub UnmapIdentityRange: usize,
    pub SetDeviceFaultReporting: usize,
    pub ConfigureDomain: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_IOMMU_INTERFACE_V1 {
    pub CreateDomain: usize,
    pub DeleteDomain: usize,
    pub AttachDevice: usize,
    pub DetachDevice: usize,
    pub FlushDomain: usize,
    pub FlushDomainByVaList: usize,
    pub QueryInputMappings: usize,
    pub MapLogicalRange: usize,
    pub UnmapLogicalRange: usize,
    pub MapIdentityRange: usize,
    pub UnmapIdentityRange: usize,
    pub SetDeviceFaultReporting: usize,
    pub ConfigureDomain: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_IOMMU_INTERFACE_V2 {
    pub CreateDomainEx: usize,
    pub DeleteDomain: usize,
    pub AttachDeviceEx: usize,
    pub DetachDeviceEx: usize,
    pub FlushDomain: usize,
    pub FlushDomainByVaList: usize,
    pub QueryInputMappings: usize,
    pub MapLogicalRangeEx: usize,
    pub UnmapLogicalRange: usize,
    pub MapIdentityRangeEx: usize,
    pub UnmapIdentityRangeEx: usize,
    pub SetDeviceFaultReportingEx: usize,
    pub ConfigureDomain: usize,
    pub QueryAvailableDomainTypes: usize,
    pub ReserveLogicalAddressRange: usize,
    pub FreeReservedLogicalAddressRange: usize,
    pub MapReservedLogicalRange: usize,
    pub UnmapReservedLogicalRange: usize,
    pub CreateDevice: usize,
    pub DeleteDevice: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_IOMMU_INTERFACE_V3 {
    pub CreateDomainEx: usize,
    pub DeleteDomain: usize,
    pub AttachDeviceEx: usize,
    pub DetachDeviceEx: usize,
    pub FlushDomain: usize,
    pub FlushDomainByVaList: usize,
    pub QueryInputMappings: usize,
    pub MapLogicalRangeEx: usize,
    pub UnmapLogicalRange: usize,
    pub MapIdentityRangeEx: usize,
    pub UnmapIdentityRangeEx: usize,
    pub SetDeviceFaultReportingEx: usize,
    pub ConfigureDomain: usize,
    pub QueryAvailableDomainTypes: usize,
    pub ReserveLogicalAddressRange: usize,
    pub FreeReservedLogicalAddressRange: usize,
    pub MapReservedLogicalRange: usize,
    pub UnmapReservedLogicalRange: usize,
    pub CreateDevice: usize,
    pub DeleteDevice: usize,
    pub CreatePasidDevice: usize,
    pub DeletePasidDevice: usize,
    pub AttachPasidDevice: usize,
    pub DetachPasidDevice: usize,
    pub QueryDeviceInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMA_IOMMU_INTERFACE_EX {
    pub Size: usize,
    pub Version: u32,
    pub V1: u32,
    pub V2: u32,
    pub V3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_COMPONENT_IDLE_STATE {
    pub TransitionLatency: u64,
    pub ResidencyRequirement: u64,
    pub NominalPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_COMPONENT_V1 {
    pub Id: u32,
    pub IdleStateCount: u32,
    pub DeepestWakeableIdleState: u32,
    pub IdleStates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_DEVICE_V1 {
    pub Version: u32,
    pub ComponentCount: u32,
    pub ComponentActiveConditionCallback: usize,
    pub ComponentIdleConditionCallback: usize,
    pub ComponentIdleStateCallback: usize,
    pub DevicePowerRequiredCallback: usize,
    pub DevicePowerNotRequiredCallback: usize,
    pub PowerControlCallback: usize,
    pub DeviceContext: usize,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_COMPONENT_V2 {
    pub Id: u32,
    pub Flags: u64,
    pub DeepestWakeableIdleState: u32,
    pub IdleStateCount: u32,
    pub IdleStates: u32,
    pub ProviderCount: u32,
    pub Providers: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_DEVICE_V2 {
    pub Version: u32,
    pub Flags: u64,
    pub ComponentActiveConditionCallback: usize,
    pub ComponentIdleConditionCallback: usize,
    pub ComponentIdleStateCallback: usize,
    pub DevicePowerRequiredCallback: usize,
    pub DevicePowerNotRequiredCallback: usize,
    pub PowerControlCallback: usize,
    pub DeviceContext: usize,
    pub ComponentCount: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_DEVICE_V3 {
    pub Version: u32,
    pub Flags: u64,
    pub ComponentActiveConditionCallback: usize,
    pub ComponentIdleConditionCallback: usize,
    pub ComponentIdleStateCallback: usize,
    pub DevicePowerRequiredCallback: usize,
    pub DevicePowerNotRequiredCallback: usize,
    pub PowerControlCallback: usize,
    pub DirectedPowerUpCallback: usize,
    pub DirectedPowerDownCallback: usize,
    pub DirectedFxTimeoutInSeconds: u32,
    pub DeviceContext: usize,
    pub ComponentCount: u32,
    pub Components: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_PERF_STATE {
    pub Value: u64,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_COMPONENT_PERF_SET {
    pub Name: u32,
    pub Flags: u64,
    pub Unit: usize,
    pub Type: usize,
    pub Count: u32,
    pub States: u32,
    pub Minimum: u64,
    pub Maximum: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_COMPONENT_PERF_INFO {
    pub PerfStateSetsCount: u32,
    pub PerfStateSets: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PO_FX_PERF_STATE_CHANGE {
    pub Set: u32,
    pub StateIndex: u32,
    pub StateValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_HANDLE_INFORMATION {
    pub HandleAttributes: u32,
    pub GrantedAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_PRE_CREATE_HANDLE_INFORMATION {
    pub DesiredAccess: u32,
    pub OriginalDesiredAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_PRE_DUPLICATE_HANDLE_INFORMATION {
    pub DesiredAccess: u32,
    pub OriginalDesiredAccess: u32,
    pub SourceProcess: usize,
    pub TargetProcess: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_PRE_OPERATION_INFORMATION {
    pub Operation: u32,
    pub Flags: u32,
    pub KernelHandle: u32,
    pub Reserved: u32,
    pub Object: usize,
    pub ObjectType: usize,
    pub CallContext: usize,
    pub Parameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_POST_CREATE_HANDLE_INFORMATION {
    pub GrantedAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_POST_DUPLICATE_HANDLE_INFORMATION {
    pub GrantedAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_POST_OPERATION_INFORMATION {
    pub Operation: u32,
    pub Flags: u32,
    pub KernelHandle: u32,
    pub Reserved: u32,
    pub Object: usize,
    pub ObjectType: usize,
    pub CallContext: usize,
    pub ReturnStatus: u32,
    pub Parameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_OPERATION_REGISTRATION {
    pub field_ObjectType: usize,
    pub Operations: u32,
    pub PreOperation: usize,
    pub PostOperation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OB_CALLBACK_REGISTRATION {
    pub Version: u16,
    pub OperationRegistrationCount: u16,
    pub Altitude: u32,
    pub RegistrationContext: usize,
    pub field_OperationRegistration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct type1 {
    pub BaseAddresses: [u32; 1],
    pub PrimaryBus: u8,
    pub SecondaryBus: u8,
    pub SubordinateBus: u8,
    pub SecondaryLatency: u8,
    pub IOBase: u8,
    pub IOLimit: u8,
    pub SecondaryStatus: u16,
    pub MemoryBase: u16,
    pub MemoryLimit: u16,
    pub PrefetchBase: u16,
    pub PrefetchLimit: u16,
    pub PrefetchBaseUpper32: u32,
    pub PrefetchLimitUpper32: u32,
    pub IOBaseUpper16: u16,
    pub IOLimitUpper16: u16,
    pub CapabilitiesPtr: u8,
    pub Reserved1: [u8; 3],
    pub ROMBaseAddress: u32,
    pub InterruptLine: u8,
    pub InterruptPin: u8,
    pub BridgeControl: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct type2 {
    pub SocketRegistersBaseAddress: u32,
    pub CapabilitiesPtr: u8,
    pub Reserved: u8,
    pub SecondaryStatus: u16,
    pub PrimaryBus: u8,
    pub SecondaryBus: u8,
    pub SubordinateBus: u8,
    pub SecondaryLatency: u8,
    pub Base: u32,
    pub Limit: u32,
    pub Range: [u32; 1],
    pub InterruptLine: u8,
    pub InterruptPin: u8,
    pub BridgeControl: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_DEVICE_PRESENCE_PARAMETERS {
    pub Size: u32,
    pub Flags: u32,
    pub VendorID: u16,
    pub DeviceID: u16,
    pub RevisionID: u8,
    pub SubVendorID: u16,
    pub SubSystemID: u16,
    pub BaseClass: u8,
    pub SubClass: u8,
    pub ProgIf: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_DEVICE_PRESENT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub IsDevicePresent: usize,
    pub IsDevicePresentEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_LINK_QUIESCENT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub PciExpressEnterLinkQuiescentMode: usize,
    pub PciExpressExitLinkQuiescentMode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_EXPRESS_ROOT_PORT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub ReadConfigSpace: usize,
    pub WriteConfigSpace: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCI_MSIX_TABLE_CONFIG_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub SetTableEntry: usize,
    pub MaskTableEntry: usize,
    pub UnmaskTableEntry: usize,
    pub GetTableEntry: usize,
    pub GetTableSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_LSN {
    pub Internal: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_NODE_ID {
    pub cType: u32,
    pub cbNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_WRITE_ENTRY {
    pub Buffer: usize,
    pub ByteLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_INFORMATION {
    pub TotalAvailable: i64,
    pub CurrentAvailable: i64,
    pub TotalReservation: i64,
    pub BaseFileSize: u64,
    pub ContainerSize: u64,
    pub TotalContainers: u32,
    pub FreeContainers: u32,
    pub TotalClients: u32,
    pub Attributes: u32,
    pub FlushThreshold: u32,
    pub SectorSize: u32,
    pub MinArchiveTailLsn: u32,
    pub BaseLsn: u32,
    pub LastFlushedLsn: u32,
    pub LastLsn: u32,
    pub RestartLsn: u32,
    pub Identity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_CLIENT_INFORMATION {
    pub ClfsInfo: u32,
    pub ClientAttributes: u32,
    pub ClientUndoCommitment: i64,
    pub ClientArchiveTailLsn: u32,
    pub ClientBaseLsn: u32,
    pub ClientLastLsn: u32,
    pub ClientRestartLsn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_LOG_NAME_INFORMATION {
    pub NameLengthInBytes: u16,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_STREAM_ID_INFORMATION {
    pub StreamIdentifier: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_PHYSICAL_LSN_INFORMATION {
    pub StreamIdentifier: u8,
    pub VirtualLsn: u32,
    pub PhysicalLsn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_CONTAINER_INFORMATION {
    pub FileAttributes: u32,
    pub CreationTime: u64,
    pub LastAccessTime: u64,
    pub LastWriteTime: u64,
    pub ContainerSize: i64,
    pub FileNameActualLength: u32,
    pub FileNameLength: u32,
    pub reserved_padding: [u32; 1],
    pub State: u32,
    pub PhysicalContainerId: u32,
    pub LogicalContainerId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_IO_STATISTICS_HEADER {
    pub ubMajorVersion: u8,
    pub ubMinorVersion: u8,
    pub eStatsClass: u32,
    pub cbLength: u16,
    pub coffData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_IO_STATISTICS {
    pub hdrIoStats: u32,
    pub cFlush: u64,
    pub cbFlush: u64,
    pub cMetaFlush: u64,
    pub cbMetaFlush: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_SCAN_CONTEXT {
    pub cidNode: u32,
    pub hLog: usize,
    pub cIndex: u32,
    pub cContainers: u32,
    pub cContainersReturned: u32,
    pub eScanMode: u32,
    pub pinfoContainer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLS_ARCHIVE_DESCRIPTOR {
    pub coffLow: u64,
    pub coffHigh: u64,
    pub infoContainer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_MGMT_POLICY {
    pub Version: u32,
    pub LengthInBytes: u32,
    pub PolicyFlags: u32,
    pub PolicyType: u32,
    pub Containers: u32,
    pub Containers_1: u32,
    pub SizeInBytes: u32,
    pub AbsoluteGrowthInContainers: u32,
    pub RelativeGrowthPercentage: u32,
    pub MinimumAvailablePercentage: u32,
    pub MinimumAvailableContainers: u32,
    pub Percentage: u32,
    pub Enabled: u32,
    pub PrefixLengthInBytes: u16,
    pub PrefixString: [u32; 1],
    pub NextContainerSuffix: u64,
    pub ExtensionLengthInBytes: u16,
    pub ExtensionString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_MGMT_NOTIFICATION {
    pub Notification: u32,
    pub Lsn: u32,
    pub LogIsPinned: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLFS_MGMT_CLIENT_REGISTRATION {
    pub Version: u32,
    pub AdvanceTailCallback: usize,
    pub AdvanceTailCallbackData: usize,
    pub LogGrowthCompleteCallback: usize,
    pub LogGrowthCompleteCallbackData: usize,
    pub LogUnpinnedCallback: usize,
    pub LogUnpinnedCallbackData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCW_COUNTER_DESCRIPTOR {
    pub Id: u16,
    pub StructIndex: u16,
    pub Offset: u16,
    pub Size: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCW_DATA {
    pub field_Data: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCW_COUNTER_INFORMATION {
    pub CounterMask: u32,
    pub InstanceMask: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCW_MASK_INFORMATION {
    pub CounterMask: u32,
    pub InstanceMask: usize,
    pub InstanceId: u32,
    pub CollectMultiple: u8,
    pub Buffer: usize,
    pub CancelEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PCW_REGISTRATION_INFORMATION {
    pub Version: u32,
    pub Name: usize,
    pub CounterCount: u32,
    pub Counters: u32,
    pub Callback: usize,
    pub CallbackContext: u32,
    pub Flags: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_MofData {
    pub Unused1: u32,
    pub Unused2: u32,
    pub Size: u32,
    pub Unused4: u32,
    pub BinaryMofData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_ProviderInfo {
    pub VariableData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_PnPDeviceId {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_PnPInstanceNames {
    pub Count: u32,
    pub InstanceNameList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_Guid {
    pub Guid: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSWmi_GuidRegistrationInfo {
    pub Operation: u32,
    pub GuidCount: u32,
    pub GuidList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSAcpiInfo {
    pub BootArchitecture: u32,
    pub PreferredProfile: u32,
    pub Capabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_INJECTION_CAPABILITIES_OUT {
    pub Status: u32,
    pub Capabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INJECT_HARDWARE_ERROR_IN {
    pub ErrorType: u32,
    pub Parameter1: u64,
    pub Parameter2: u64,
    pub Parameter3: u64,
    pub Parameter4: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INJECT_HARDWARE_ERROR_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_ALL_ERROR_SOURCES_OUT {
    pub Status: u32,
    pub Count: u32,
    pub Length: u32,
    pub ErrorSourceArray: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_ERROR_SOURCE_INFO_IN {
    pub ErrorSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_ERROR_SOURCE_INFO_OUT {
    pub Status: u32,
    pub Length: u32,
    pub ErrorSourceInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_ERROR_SOURCE_INFO_IN {
    pub Length: u32,
    pub ErrorSourceInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_ERROR_SOURCE_INFO_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_ERROR_SOURCE_IN {
    pub ErrorSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_ERROR_SOURCE_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISABLE_ERROR_SOURCE_IN {
    pub ErrorSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISABLE_ERROR_SOURCE_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_ALL_POLICY_OUT {
    pub Status: u32,
    pub Count: u32,
    pub Length: u32,
    pub Values: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_SINGLE_POLICY_IN {
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_SINGLE_POLICY_OUT {
    pub Status: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_SINGLE_POLICY_IN {
    pub Type: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_SINGLE_POLICY_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMMIT_POLICY_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RESET_POLICY_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PLUGIN_CTL_INTERFACE_IN {
    pub InputLength: u32,
    pub InputBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PLUGIN_CTL_INTERFACE_OUT {
    pub Status: u32,
    pub OutputLength: u32,
    pub OutputBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PAGE_OFFLINING_INTERFACE_IN {
    pub InputLength: u32,
    pub InputBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WHEA_PAGE_OFFLINING_INTERFACE_OUT {
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSSmBios_RawSMBiosTables {
    pub Used20CallingMethod: u8,
    pub SmbiosMajorVersion: u8,
    pub SmbiosMinorVersion: u8,
    pub DmiRevision: u8,
    pub Size: u32,
    pub SMBiosData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSSmBios_SMBiosEventlog {
    pub VariableData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSID_UUID {
    pub Uuid: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSSmBios_SysidUUIDList {
    pub Count: u32,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSID_1394 {
    pub x1394: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSSmBios_Sysid1394List {
    pub Count: u32,
    pub List: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_Header {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_BusError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub Participation: u32,
    pub MemoryHierarchyLevel: u32,
    pub RequestType: u32,
    pub MemOrIo: u32,
    pub Address: u64,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_TLBError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub TransactionType: u32,
    pub MemoryHierarchyLevel: u32,
    pub Address: u64,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_MemoryHierarchyError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub TransactionType: u32,
    pub MemoryHierarchyLevel: u32,
    pub RequestType: u32,
    pub Address: u64,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_CPUError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub MajorErrorType: u32,
    pub Level: u32,
    pub CacheOp: u32,
    pub CacheMesi: u32,
    pub TLBOp: u32,
    pub BusType: u32,
    pub BusSev: u32,
    pub RegFileId: u32,
    pub RegFileOp: u32,
    pub MSSid: u32,
    pub MSOp: u32,
    pub MSArrayId: u32,
    pub MSIndex: u32,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_MemoryError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub MEM_ERROR_STATUS: u64,
    pub MEM_PHYSICAL_ADDR: u64,
    pub MEM_PHYSICAL_MASK: u64,
    pub RESPONDER_ID: u64,
    pub TARGET_ID: u64,
    pub REQUESTOR_ID: u64,
    pub BUS_SPECIFIC_DATA: u64,
    pub MEM_NODE: u16,
    pub MEM_CARD: u16,
    pub MEM_BANK: u16,
    pub xMEM_DEVICE: u16,
    pub MEM_MODULE: u16,
    pub MEM_ROW: u16,
    pub MEM_COLUMN: u16,
    pub MEM_BIT_POSITION: u16,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_PCIBusError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub PCI_BUS_ERROR_STATUS: u64,
    pub PCI_BUS_ADDRESS: u64,
    pub PCI_BUS_DATA: u64,
    pub PCI_BUS_CMD: u64,
    pub PCI_BUS_REQUESTOR_ID: u64,
    pub PCI_BUS_RESPONDER_ID: u64,
    pub PCI_BUS_TARGET_ID: u64,
    pub PCI_BUS_ERROR_TYPE: u16,
    pub PCI_BUS_ID_BusNumber: u8,
    pub PCI_BUS_ID_SegmentNumber: u8,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_PCIComponentError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub PCI_COMP_ERROR_STATUS: u64,
    pub PCI_COMP_INFO_VendorId: u16,
    pub PCI_COMP_INFO_DeviceId: u16,
    pub PCI_COMP_INFO_ClassCodeInterface: u8,
    pub PCI_COMP_INFO_ClassCodeSubClass: u8,
    pub PCI_COMP_INFO_ClassCodeBaseClass: u8,
    pub PCI_COMP_INFO_FunctionNumber: u8,
    pub PCI_COMP_INFO_DeviceNumber: u8,
    pub PCI_COMP_INFO_BusNumber: u8,
    pub PCI_COMP_INFO_SegmentNumber: u8,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_SystemEventError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub SEL_TIME_STAMP: u64,
    pub SEL_RECORD_ID: u16,
    pub SEL_GENERATOR_ID: u16,
    pub SEL_RECORD_TYPE: u8,
    pub SEL_EVM_REV: u8,
    pub SEL_SENSOR_TYPE: u8,
    pub SEL_SENSOR_NUM: u8,
    pub SEL_EVENT_DIR_TYPE: u8,
    pub SEL_DATA1: u8,
    pub SEL_DATA2: u8,
    pub SEL_DATA3: u8,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_SMBIOSError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub SMBIOS_EVENT_TYPE: u8,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_PlatformSpecificError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub VALIDATION_BITS: u64,
    pub PLATFORM_ERROR_STATUS: u64,
    pub PLATFORM_REQUESTOR_ID: u64,
    pub PLATFORM_RESPONDER_ID: u64,
    pub PLATFORM_TARGET_ID: u64,
    pub PLATFORM_BUS_SPECIFIC_DATA: u64,
    pub OEM_COMPONENT_ID: [u8; 16],
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_InvalidError {
    pub RecordId: u64,
    pub ErrorSeverity: u8,
    pub Type: u32,
    pub Cpu: u32,
    pub AdditionalErrors: u32,
    pub LogToEventlog: u32,
    pub Size: u32,
    pub RawRecord: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAEvent_MemoryPageRemoved {
    pub PhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAInfo_Entry {
    pub Length: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAInfo_RawMCAData {
    pub Count: u32,
    pub Records: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAInfo_RawCMCEvent {
    pub Count: u32,
    pub Records: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAInfo_RawMCAEvent {
    pub Count: u32,
    pub Records: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMCAInfo_RawCorrectedPlatformEvent {
    pub Count: u32,
    pub Records: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSPower_DeviceEnable {
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSPower_DeviceWakeEnable {
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkAddress {
    pub Address: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkShortAddress {
    pub Address: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkLinkSpeed {
    pub Outbound: u32,
    pub Inbound: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EnumerateAdapter {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NotifyAdapterRemoval {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NotifyAdapterArrival {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_DeviceWakeOnMagicPacketOnly {
    pub EnableWakeOnMagicPacketOnly: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_RSSEnabled {
    pub RSSEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkDirectState {
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkDirectVersion {
    pub MajorVersionNumber: u16,
    pub MinorVersionNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkDirectAdapterInfo {
    pub Version: u32,
    pub VendorId: u32,
    pub DeviceId: u32,
    pub MaxRegistrationSize: u64,
    pub MaxWindowSize: u64,
    pub FRMRPageCount: u32,
    pub MaxInitiatorRequestSge: u32,
    pub MaxReceiveRequestSge: u32,
    pub MaxReadRequestSge: u32,
    pub MaxTransferLength: u32,
    pub MaxInlineDataSize: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxReceiveQueueDepth: u32,
    pub MaxInitiatorQueueDepth: u32,
    pub MaxSrqDepth: u32,
    pub MaxCqDepth: u32,
    pub LargeRequestThreshold: u32,
    pub MaxCallerData: u32,
    pub MaxCalleeData: u32,
    pub InOrderDMA: u8,
    pub SupportsCQResize: u8,
    pub SupportsLoopbackConnections: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkDirectAdapterCapabilities {
    pub MaxQpCount: u32,
    pub MaxCqCount: u32,
    pub MaxMrCount: u32,
    pub MaxPdCount: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxMwCount: u32,
    pub MaxSrqCount: u32,
    pub MissingCounterMask: u64,
    pub NdAdapterInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NetworkDirectCapabilities {
    pub NdCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_HardwareStatus {
    pub NdisHardwareStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MediaSupported {
    pub NumberElements: u32,
    pub NdisMediaSupported: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MediaInUse {
    pub NumberElements: u32,
    pub NdisMediaInUse: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MaximumLookahead {
    pub NdisMaximumLookahead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MaximumFrameSize {
    pub NdisMaximumFrameSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_LinkSpeed {
    pub NdisLinkSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TransmitBufferSpace {
    pub NdisTransmitBufferSpace: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveBufferSpace {
    pub NdisReceiveBufferSpace: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TransmitBlockSize {
    pub NdisTransmitBlockSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveBlockSize {
    pub NdisReceiveBlockSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_VendorID {
    pub NdisVendorID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_VendorDescription {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CurrentPacketFilter {
    pub NdisCurrentPacketFilter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CurrentLookahead {
    pub NdisCurrentLookahead: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_DriverVersion {
    pub NdisDriverVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MaximumTotalSize {
    pub NdisMaximumTotalSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MacOptions {
    pub NdisMacOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MediaConnectStatus {
    pub NdisMediaConnectStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_MaximumSendPackets {
    pub NdisMaximumSendPackets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_VendorDriverVersion {
    pub NdisVendorDriverVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_VlanIdentifier {
    pub NdisVlanId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PhysicalMediumType {
    pub NdisPhysicalMediumType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TransmitsOk {
    pub NdisTransmitsOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceivesOk {
    pub NdisReceivesOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TransmitsError {
    pub NdisTransmitsError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveError {
    pub NdisReceiveError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveNoBuffer {
    pub NdisReceiveNoBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoHardwareStatus {
    pub NdisCoHardwareStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoMediaSupported {
    pub NumberElements: u32,
    pub NdisCoMediaSupported: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoMediaInUse {
    pub NumberElements: u32,
    pub NdisCoMediaInUse: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoLinkSpeed {
    pub NdisCoLinkSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoVendorId {
    pub NdisCoVendorID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoVendorDescription {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoDriverVersion {
    pub NdisCoDriverVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoMacOptions {
    pub NdisCoMacOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoMediaConnectStatus {
    pub NdisCoMediaConnectStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoVendorDriverVersion {
    pub NdisCoVendorDriverVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoMinimumLinkSpeed {
    pub NdisCoMinimumLinkSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoTransmitPdusOk {
    pub NdisCoTransmitPdusOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoReceivePdusOk {
    pub NdisCoReceivePdusOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoTransmitPduErrors {
    pub NdisCoTransmitPduErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoReceivePduErrors {
    pub NdisCoReceivePduErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CoReceivePdusNoBuffer {
    pub NdisCoReceivePdusNoBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmSupportedVcRates {
    pub MinCellRate: u32,
    pub MaxCellRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmSupportedServiceCategory {
    pub NdisAtmSupportedServiceCategory: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmSupportedAalTypes {
    pub NdisAtmSupportedAalTypes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmHardwareCurrentAddress {
    pub NdisAtmHardwareCurrentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxActiveVcs {
    pub NdisAtmMaxActiveVcs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxActiveVciBits {
    pub NdisAtmMaxActiveVciBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxActiveVpiBits {
    pub NdisAtmMaxActiveVpiBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxAal0PacketSize {
    pub NdisAtmMaxAal0PacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxAal1PacketSize {
    pub NdisAtmMaxAal1PacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxAal34PacketSize {
    pub NdisAtmMaxAal34PacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmMaxAal5PacketSize {
    pub NdisAtmMaxAal5PacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmReceiveCellsOk {
    pub NdisAtmReceiveCellsOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmTransmitCellsOk {
    pub NdisAtmTransmitCellsOk: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_AtmReceiveCellsDropped {
    pub NdisAtmReceiveCellsDropped: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetPermanentAddress {
    pub NdisPermanentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetCurrentAddress {
    pub NdisCurrentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetMulticastList {
    pub NumberElements: u32,
    pub NdisMulticastList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetMaximumMulticastListSize {
    pub NdisEthernetMaximumMulticastListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetMacOptions {
    pub NdisEthernetMacOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetReceiveErrorAlignment {
    pub NdisEthernetReceiveErrorAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetOneTransmitCollision {
    pub NdisEthernetOneTransmitCollision: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EthernetMoreTransmitCollisions {
    pub NdisEthernetMoreTransmitCollisions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingPermanentAddress {
    pub NdisPermanentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingCurrentAddress {
    pub NdisCurrentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingCurrentFunctional {
    pub NdisTokenRingCurrentFunctional: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingCurrentGroup {
    pub NdisTokenRingCurrentGroup: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingLastOpenStatus {
    pub NdisTokenRingLastOpenStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingCurrentRingStatus {
    pub NdisTokenRingCurrentRingStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingCurrentRingState {
    pub NdisTokenRingCurrentRingState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingLineErrors {
    pub NdisTokenRingLineErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TokenRingLostFrames {
    pub NdisTokenRingLostFrames: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLongPermanentAddress {
    pub NdisPermanentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLongCurrentAddress {
    pub NdisCurrentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLongMulticastList {
    pub NumberElements: u32,
    pub NdisMulticastList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLongMaximumListSize {
    pub NdisFddiLongMaximumListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiShortPermanentAddress {
    pub NdisPermanentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiShortCurrentAddress {
    pub NdisCurrentAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiShortMulticastList {
    pub NumberElements: u32,
    pub NdisMulticastList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiShortMaximumListSize {
    pub NdisFddiShortMaximumListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiAttachmentType {
    pub NdisFddiAttachmentType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiUpstreamNodeLong {
    pub NdisFddiUpstreamNodeLong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiDownstreamNodeLong {
    pub NdisFddiDownstreamNodeLong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiFrameErrors {
    pub NdisFddiFrameErrors: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiFramesLost {
    pub NdisFddiFramesLost: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiRingManagmentState {
    pub NdisFddiRingManagmentState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLctFailures {
    pub NdisFddiLctFailures: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLemRejects {
    pub NdisFddiLemRejects: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_FddiLConnectionState {
    pub NdisFddiLConnectionState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ObjectHeader {
    pub Type: u8,
    pub Revision: u8,
    pub Size: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiMethodHeader {
    pub Header: u32,
    pub PortNumber: u32,
    pub NetLuid: u64,
    pub RequestId: u64,
    pub Timeout: u32,
    pub Padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiSetHeader {
    pub Header: u32,
    pub PortNumber: u32,
    pub NetLuid: u64,
    pub RequestId: u64,
    pub Timeout: u32,
    pub Padding: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiOutputInfo {
    pub Header: u32,
    pub Flags: u32,
    pub SupportedRevision: u8,
    pub Padding1: u8,
    pub Padding2: u16,
    pub DataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiEnumAdapter {
    pub Header: u32,
    pub IfIndex: u32,
    pub NetLuid: u64,
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_LinkStateData {
    pub Header: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub XmitLinkSpeed: u64,
    pub RcvLinkSpeed: u64,
    pub PauseFunctions: u32,
    pub AutoNegotiationFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_LinkParameters {
    pub Header: u32,
    pub MediaDuplexState: u32,
    pub XmitLinkSpeed: u64,
    pub RcvLinkSpeed: u64,
    pub PauseFunctions: u32,
    pub AutoNegotiationFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_InterruptModerationParameters {
    pub Header: u32,
    pub Flags: u32,
    pub InterruptModeration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatisticsInfo {
    pub Header: u32,
    pub SupportedStatistics: u32,
    pub ifInDiscards: u64,
    pub ifInErrors: u64,
    pub ifHCInOctets: u64,
    pub ifHCInUcastPkts: u64,
    pub ifHCInMulticastPkts: u64,
    pub ifHCInBroadcastPkts: u64,
    pub ifHCOutOctets: u64,
    pub ifHCOutUcastPkts: u64,
    pub ifHCOutMulticastPkts: u64,
    pub ifHCOutBroadcastPkts: u64,
    pub ifOutErrors: u64,
    pub ifOutDiscards: u64,
    pub ifHCInUcastOctets: u64,
    pub ifHCInMulticastOctets: u64,
    pub ifHCInBroadcastOctets: u64,
    pub ifHCOutUcastOctets: u64,
    pub ifHCOutMulticastOctets: u64,
    pub ifHCOutBroadcastOctets: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PortStateData {
    pub Header: u32,
    pub MediaConnectState: u32,
    pub XmitLinkSpeed: u64,
    pub RcvLinkSpeed: u64,
    pub Direction: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PortAuthParameters {
    pub Header: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PortChar {
    pub Header: u32,
    pub PortNumber: u32,
    pub Flags: u32,
    pub Type: u32,
    pub MediaConnectState: u32,
    pub XmitLinkSpeed: u64,
    pub RcvLinkSpeed: u64,
    pub Direction: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PortArray {
    pub Header: u32,
    pub NumberOfPorts: u32,
    pub OffsetFirstPort: u32,
    pub ElementSize: u32,
    pub Port: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PciDeviceProperty {
    pub Header: u32,
    pub DeviceType: u32,
    pub CurrentSpeedAndMode: u32,
    pub CurrentPayloadSize: u32,
    pub MaxPayloadSize: u32,
    pub MaxReadRequestSize: u32,
    pub CurrentLinkSpeed: u32,
    pub CurrentLinkWidth: u32,
    pub MaxLinkSpeed: u32,
    pub MaxLinkWidth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpLargeSendOffloadV1_IPv4 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub TcpOptions: u32,
    pub IpOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpLargeSendOffloadV1 {
    pub WmiIPv4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpIpChecksumOffload_IPv4TransmitReceive {
    pub Encapsulation: u32,
    pub IpOptionsSupported: u32,
    pub TcpOptionsSupported: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
    pub IpChecksum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpIpChecksumOffload_IPv6TransmitReceive {
    pub Encapsulation: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpIpChecksumOffload {
    pub IPv4Transmit: u32,
    pub IPv4Receive: u32,
    pub IPv6Transmit: u32,
    pub IPv6Receive: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiIPSecOffloadV1_Supported {
    pub Encapsulation: u32,
    pub AhEspCombined: u32,
    pub TransportTunnelCombined: u32,
    pub IPv4Options: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiIPSecOffloadV1_IPv4AH {
    pub Md5: u32,
    pub Sha_1: u32,
    pub Transport: u32,
    pub Tunnel: u32,
    pub Send: u32,
    pub Receive: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiIPSecOffloadV1_IPv4ESP {
    pub Des: u32,
    pub Reserved: u32,
    pub TripleDes: u32,
    pub NullEsp: u32,
    pub Transport: u32,
    pub Tunnel: u32,
    pub Send: u32,
    pub Receive: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiIPSecOffloadV1 {
    pub WmiSupported: u32,
    pub WmiIPv4AH: u32,
    pub WmiIPv4ESP: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpLargeSendOffloadV2_IPv4 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpLargeSendOffloadV2_IPv6 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpLargeSendOffloadV2 {
    pub WmiIPv4: u32,
    pub WmiIPv6: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiOffload {
    pub Header: u32,
    pub Checksum: u32,
    pub LsoV1: u32,
    pub IPsecV1: u32,
    pub LsoV2: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_TcpOffloadParameters {
    pub Header: u32,
    pub IPv4Checksum: u8,
    pub TCPIPv4Checksum: u8,
    pub UDPIPv4Checksum: u8,
    pub TCPIPv6Checksum: u8,
    pub UDPIPv6Checksum: u8,
    pub LsoV1: u8,
    pub IPsec: u8,
    pub LsoV2IPv4: u8,
    pub LsoV2IPv6: u8,
    pub TcpConnectionIPv4: u8,
    pub TcpConnectionIPv6: u8,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiTcpConnectionOffload {
    pub Header: u32,
    pub Encapsulation: u32,
    pub SupportIp4: u32,
    pub SupportIp6: u32,
    pub SupportIp6ExtensionHeaders: u32,
    pub SupportSack: u32,
    pub TcpConnectionOffloadCapacity: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiHDSplitCurrentConfig {
    pub Header: u32,
    pub HardwareCapabilities: u32,
    pub CurrentCapabilities: u32,
    pub HDSplitFlags: u32,
    pub HDSplitCombineFlags: u32,
    pub BackfillSize: u32,
    pub MaxHeaderSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_HDSplitParameters {
    pub Header: u32,
    pub HDSplitCombineFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_WmiReceiveScaleCapabilities {
    pub Header: u32,
    pub CapabilitiesFlags: u32,
    pub NumberOfInterruptMessages: u32,
    pub NumberOfReceiveQueues: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterCapabilities {
    pub Header: u32,
    pub Flags: u32,
    pub EnabledFilterTypes: u32,
    pub EnabledQueueTypes: u32,
    pub NumQueues: u32,
    pub SupportedQueueProperties: u32,
    pub SupportedFilterTests: u32,
    pub SupportedHeaders: u32,
    pub SupportedMacHeaderFields: u32,
    pub MaxMacHeaderFilters: u32,
    pub MaxQueueGroups: u32,
    pub MaxQueuesPerQueueGroup: u32,
    pub MinLookaheadSplitSize: u32,
    pub MaxLookaheadSplitSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterGlobalParameters {
    pub Header: u32,
    pub Flags: u32,
    pub EnabledFilterTypes: u32,
    pub EnabledQueueTypes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_CountedString {
    pub Length: u16,
    pub String: [i16; 257],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterInfo {
    pub Header: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub FilterId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterInfoArray {
    pub Header: u32,
    pub QueueId: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
    pub Filter: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterFieldParameters {
    pub Header: u32,
    pub Flags: u32,
    pub FrameHeader: u32,
    pub ReceiveFilterTest: u32,
    pub MacHeaderField: u32,
    pub FieldByteArrayValue: [u8; 16],
    pub ResultByteArrayValue: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveFilterParameters {
    pub Header: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub QueueId: u32,
    pub FilterId: u32,
    pub FieldParametersArrayOffset: u32,
    pub FieldParametersArrayNumElements: u32,
    pub FieldParametersArrayElementSize: u32,
    pub RequestedFilterIdBitCount: u32,
    pub FieldParameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_NicSwitchCapabilities {
    pub Header: u32,
    pub Flags: u32,
    pub NdisReserved1: u32,
    pub NumTotalMacAddresses: u32,
    pub NumMacAddressesPerPort: u32,
    pub NumVlansPerPort: u32,
    pub NdisReserved2: u32,
    pub NdisReserved3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_GroupAffinity {
    pub Mask: u64,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveQueueParameters {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveQueueInfo {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub QueueState: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_ReceiveQueueInfoArray {
    pub Header: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
    pub Queue: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryLinkState_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryLinkState_OUT {
    pub LinkState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryInterruptModeration_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryInterruptModeration_OUT {
    pub InterruptModeration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetInterruptModeration_IN {
    pub MethodHeader: u32,
    pub InterruptModeration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetInterruptModeration_OUT {
    pub OutputInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetLinkParameters_IN {
    pub MethodHeader: u32,
    pub LinkParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetLinkParameters_OUT {
    pub OutputInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryStatisticsInfo_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryStatisticsInfo_OUT {
    pub StatisticsInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPortState_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPortState_OUT {
    pub PortState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumeratePorts_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumeratePorts_OUT {
    pub Ports: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetPortParameters_IN {
    pub MethodHeader: u32,
    pub PortParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetPortParameters_OUT {
    pub OutputInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPciDeviceCustomProperty_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPciDeviceCustomProperty_OUT {
    pub PciDeviceProperty: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_EnumerateAdapterEx {
    pub EnumerateAdapter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryCurrentOffloadConfig_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryCurrentOffloadConfig_OUT {
    pub Offload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryHardwareOffloadConfig_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryHardwareOffloadConfig_OUT {
    pub Offload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetTcpOffloadParameters_IN {
    pub MethodHeader: u32,
    pub TcpOffloadParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetTcpOffloadParameters_OUT {
    pub OutputInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryTcpConnectionOffloadCurrentConfig_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryTcpConnectionOffloadCurrentConfig_OUT {
    pub Offload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryTcpConnectionOffloadHardwareConfig_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryTcpConnectionOffloadHardwareConfig_OUT {
    pub Offload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveScaleCapabilities_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveScaleCapabilities_OUT {
    pub RssCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryHDSplitCurrentConfig_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryHDSplitCurrentConfig_OUT {
    pub HdSplitCurrentConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetHDSplitParameters_IN {
    pub MethodHeader: u32,
    pub HDSplitParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetHDSplitParameters_OUT {
    pub OutputInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPhysicalMediumTypeEx_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryPhysicalMediumTypeEx_OUT {
    pub NdisPhysicalMediumTypeEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterHardwareCapabilities_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterHardwareCapabilities_OUT {
    pub ReceiveFilterHardwareCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterGlobalParameters_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterGlobalParameters_OUT {
    pub ReceiveFilterGlobalParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumReceiveFilters_IN {
    pub Header: u32,
    pub ReceiveFilterInfoArray: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumReceiveFilters_OUT {
    pub ReceiveFilterInfoArray: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterParameters_IN {
    pub Header: u32,
    pub ReceiveFilterParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterParameters_OUT {
    pub ReceiveFilterParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterCurrentCapabilities_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveFilterCurrentCapabilities_OUT {
    pub ReceiveFilterCurrentCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryNICSwitchHardwareCapabilities_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryNICSwitchHardwareCapabilities_OUT {
    pub NicSwitchHardwareCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryNICSwitchCurrentCapabilities_IN {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryNICSwitchCurrentCapabilities_OUT {
    pub NicSwitchCurrentCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveQueueParameters_IN {
    pub Header: u32,
    pub ReceiveQueueParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiQueryReceiveQueueParameters_OUT {
    pub ReceiveQueueParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumReceiveQueues_IN {
    pub Header: u32,
    pub ReceiveQueueInfoArray: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiEnumReceiveQueues_OUT {
    pub ReceiveQueueInfoArray: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_BaseServiceSetIdentifier {
    pub Ndis80211MacAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ServiceSetIdentifier {
    pub Ndis80211SsId: [u8; 33],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_NetworkType {
    pub Ndis80211NetworkType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_NetworkTypesSupported {
    pub NumberOfItems: u32,
    pub Ndis80211NetworkTypes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_NetworkTypeInUse {
    pub Ndis80211NetworkTypeInUse: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_PowerMode {
    pub Ndis80211PowerMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_TransmitPowerLevel {
    pub Ndis80211TransmitPowerLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ReceivedSignalStrength {
    pub Ndis80211ReceivedSignalStrength: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ReceivedSignalStrengthEventTrigger {
    pub Ndis80211ReceivedSignalStrengthTrigger: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_NetworkInfrastructure {
    pub Ndis80211NetworkInfrastructure: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ConfigurationFH {
    pub FHLength: u32,
    pub HopPattern: u32,
    pub HopSet: u32,
    pub DwellTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ConfigurationInfo {
    pub ConfigLength: u32,
    pub BeaconPeriod: u32,
    pub ATIMWindow: u32,
    pub DSConfig: u32,
    pub FHConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_WLanBssId {
    pub Ndis80211WLanBssIdLength: u32,
    pub Ndis80211MacAddress: [u8; 6],
    pub Reserved: u16,
    pub Ndis80211SsIdLength: u32,
    pub Ndis80211SsId: [u8; 32],
    pub Ndis80211Privacy: u32,
    pub Ndis80211Rssi: u32,
    pub Ndis80211NetworkTypeInUse: u32,
    pub Ndis80211Configuration: u32,
    pub Ndis80211InfrastructureMode: u32,
    pub Ndis80211SupportedRate: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_BSSIList {
    pub NumberOfItems: u32,
    pub Ndis80211BSSIList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_InfrastructureMode {
    pub Ndis80211InfrastructureMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_FragmentationThreshold {
    pub Ndis80211FragmentationThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_RTSThreshold {
    pub Ndis80211RTSThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_NumberOfAntennas {
    pub Ndis80211NumberOfAntennas: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ReceiveAntennaSelected {
    pub Ndis80211ReceiveAntennaSelected: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_TransmitAntennaSelected {
    pub Ndis80211TransmitAntennaSelected: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_DataRates {
    pub Ndis80211DataRate: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_DesiredDataRates {
    pub Ndis80211DesiredRate: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_Configuration {
    pub Ndis80211Config: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_Statistics {
    pub StatisticsLength: u32,
    pub TransmittedFragmentCount: u64,
    pub MulticastTransmittedFrameCount: u64,
    pub FailedCount: u64,
    pub RetryCount: u64,
    pub MultipleRetryCount: u64,
    pub RTSSuccessCount: u64,
    pub RTSFailureCount: u64,
    pub ACKFailureCount: u64,
    pub FrameDuplicateCount: u64,
    pub ReceivedFragmentCount: u64,
    pub MulticastReceivedFrameCount: u64,
    pub FCSErrorCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_AddWEP {
    pub Length: u32,
    pub KeyIndex: u32,
    pub KeyLength: u32,
    pub KeyMaterial: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_RemoveWEP {
    pub Ndis80211KeyIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_Disassociate {
    pub UnusedParameter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_BssIdListScan {
    pub UnusedParameter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_AuthenticationMode {
    pub Ndis80211AuthenticationMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_PrivacyFilter {
    pub Ndis80211PrivacyFilter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_WEPStatus {
    pub Ndis80211WEPStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_80211_ReloadDefaults {
    pub Ndis80211ReloadDefaults: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMAdminConfigState {
    pub NdisPMAdminConfigState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMAdminConfigParam {
    pub Header: u32,
    pub WakeOnPattern: u32,
    pub WakeOnMagicPacket: u32,
    pub DeviceSleepOnDisconnect: u32,
    pub PMARPOffload: u32,
    pub PMNDOffload: u32,
    pub PMWiFiRekeyOffload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMAdminConfig {
    pub PMAdminConfigParam: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMCapabilityState {
    pub NdisPMCapabilityState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMCapabilitiesParam {
    pub Header: u32,
    pub WakeOnPattern: u32,
    pub WakeOnMagicPacket: u32,
    pub DeviceSleepOnDisconnect: u32,
    pub PMARPOffload: u32,
    pub PMNDOffload: u32,
    pub PMWiFiRekeyOffload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_PMCapabilities {
    pub PMCapabilitiesParam: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusMediaSpecificIndication {
    pub NumberElements: u32,
    pub NdisStatusMediaSpecificIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusLinkSpeedChange {
    pub NdisStatusLinkSpeedChange: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusProtocolBind {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusProtocolUnbind {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDevicePowerOn {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDevicePowerOff {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDevicePowerOnEx {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDevicePowerOffEx {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusTaskOffloadChange {
    pub NumberElements: u32,
    pub TaskOffloadCapabilities: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusPacketFilterChange {
    pub NumberElements: u32,
    pub NdisPacketFilterStatusIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusNetworkChange {
    pub NumberElements: u32,
    pub NdisNetworkChangeStatusIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusOperationalStatus {
    pub NumberElements: u32,
    pub NdisOperationalStatusStatusIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusLinkState {
    pub NumberElements: u32,
    pub NdisLinkStateStatusIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusPortState {
    pub NumberElements: u32,
    pub NdisPortStateStatusIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusExternalConnectivityChange {
    pub NdisMediaConnectStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusMediaSpecificIndicationEx {
    pub NumberElements: u32,
    pub NdisStatusMediaSpecificIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusHDSplitCurrentConfig {
    pub NumberElements: u32,
    pub HDSplitCurrentConfig: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11ScanConfirm {
    pub NumberElements: u32,
    pub NdisStatusDot11ScanConfirmIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11MPDUMaxLengthChange {
    pub NumberElements: u32,
    pub NdisStatusDot11MPDUMaxLengthChangeIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11AssociationStart {
    pub NumberElements: u32,
    pub NdisStatusDot11AssociationStartIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11AssociationCompletion {
    pub NumberElements: u32,
    pub NdisStatusDot11AssociationCompletionIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11ConnectionStart {
    pub NumberElements: u32,
    pub NdisStatusDot11ConnectionStartIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11ConnectionCompletion {
    pub NumberElements: u32,
    pub NdisStatusDot11ConnectionCompletionIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11RoamingStart {
    pub NumberElements: u32,
    pub NdisStatusDot11RoamingStartIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11RoamingCompletion {
    pub NumberElements: u32,
    pub NdisStatusDot11RoamingCompletionIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11Disassociation {
    pub NumberElements: u32,
    pub NdisStatusDot11DisassociationIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11TkipmicFailure {
    pub NumberElements: u32,
    pub NdisStatusDot11TkipmicFailureIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11PmkidCandidateList {
    pub NumberElements: u32,
    pub NdisStatusDot11PmkidCandidateListIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11PhyStateChange {
    pub NumberElements: u32,
    pub NdisStatusDot11PhyStateChangeIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSNdis_StatusDot11LinkQuality {
    pub NumberElements: u32,
    pub NdisStatusDot11LinkQualityIndication: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_PORT_WMI_STD_DATA {
    pub ConnectorType: u32,
    pub DataQueueSize: u32,
    pub ErrorCount: u32,
    pub FunctionKeys: u32,
    pub Indicators: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_ID_EX {
    pub Type: u32,
    pub Subtype: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POINTER_PORT_WMI_STD_DATA {
    pub ConnectorType: u32,
    pub DataQueueSize: u32,
    pub ErrorCount: u32,
    pub Buttons: u32,
    pub HardwareType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSMouse_ClassInformation {
    pub DeviceId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSKeyboard_ClassInformation {
    pub DeviceId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AGP_STD_DATA {
    pub ApertureBase: u64,
    pub ApertureLength: u32,
    pub AgpStatus: u32,
    pub AgpCommand: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSAcpi_ThermalZoneTemperature {
    pub ThermalStamp: u32,
    pub ThermalConstant1: u32,
    pub ThermalConstant2: u32,
    pub Reserved: u32,
    pub SamplingPeriod: u32,
    pub CurrentTemperature: u32,
    pub PassiveTripPoint: u32,
    pub CriticalTripPoint: u32,
    pub ActiveTripPointCount: u32,
    pub ActiveTripPoint: [u32; 10],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_DISK_GEOMETRY {
    pub Cylinders: i64,
    pub MediaType: u32,
    pub TracksPerCylinder: u32,
    pub SectorsPerTrack: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_DISK_PERFORMANCE {
    pub BytesRead: i64,
    pub BytesWritten: i64,
    pub ReadTime: i64,
    pub WriteTime: i64,
    pub IdleTime: i64,
    pub ReadCount: u32,
    pub WriteCount: u32,
    pub QueueDepth: u32,
    pub SplitCount: u32,
    pub QueryTime: i64,
    pub StorageDeviceNumber: u32,
    pub StorageManagerName: [u16; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSDiskDriver_Performance {
    pub PerfData: u32,
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FAILURE_PREDICT_STATUS {
    pub Reason: u32,
    pub PredictFailure: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FAILURE_PREDICT_DATA {
    pub Length: u32,
    pub VendorSpecific: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATAPI_FAILURE_PREDICT_DATA {
    pub Length: u32,
    pub VendorSpecific: [u8; 362],
    pub OfflineCollectionStatus: u8,
    pub SelfTestStatus: u8,
    pub TotalTime: u16,
    pub VendorSpecific2: u8,
    pub OfflineCollectCapability: u8,
    pub SmartCapability: u16,
    pub ErrorLogCapability: u8,
    pub VendorSpecific3: u8,
    pub ShortPollTimeInMinutes: u8,
    pub ExtendedPollTimeInMinutes: u8,
    pub Reserved: [u8; 12],
    pub VendorSpecific4: [u8; 125],
    pub Checksum: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FAILURE_PREDICT_THRESHOLDS {
    pub VendorSpecific: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FAILURE_PREDICT_EVENT {
    pub Length: u32,
    pub VendorSpecific: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ALLOW_PERFORMANCE_HIT_IN {
    pub Allow: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_DISABLE_FP_IN {
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_DISABLE_FP_POLLING_IN {
    pub Period: u32,
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_FP_CAPABILITY_OUT {
    pub Capability: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_OFFLINE_DIAGS_OUT {
    pub Success: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_LOG_SECTORS_IN {
    pub LogAddress: u8,
    pub SectorCount: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_LOG_SECTORS_OUT {
    pub Length: u32,
    pub LogSectors: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE_LOG_SECTORS_IN {
    pub LogAddress: u8,
    pub SectorCount: u8,
    pub Length: u32,
    pub LogSectors: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WRITE_LOG_SECTORS_OUT {
    pub Success: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXECUTE_SELF_TEST_IN {
    pub Subcommand: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EXECUTE_SELF_TEST_OUT {
    pub ReturnCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_SCSI_INFO_EXCEPTIONS {
    pub PageSavable: u8,
    pub Flags: u8,
    pub MRIE: u8,
    pub Padding: u8,
    pub IntervalTimer: u32,
    pub ReportCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSIde_PortDeviceInfo {
    pub Bus: u8,
    pub Target: u8,
    pub Lun: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSSerial_PortName {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_WMI_COMM_DATA {
    pub BaudRate: u32,
    pub BitsPerByte: u32,
    pub Parity: u32,
    pub ParityCheckEnable: u8,
    pub StopBits: u32,
    pub XoffCharacter: u32,
    pub XoffXmitThreshold: u32,
    pub XonCharacter: u32,
    pub XonXmitThreshold: u32,
    pub MaximumBaudRate: u32,
    pub MaximumOutputBufferSize: u32,
    pub MaximumInputBufferSize: u32,
    pub Support16BitMode: u8,
    pub SupportDTRDSR: u8,
    pub SupportIntervalTimeouts: u8,
    pub SupportParityCheck: u8,
    pub SupportRTSCTS: u8,
    pub SupportXonXoff: u8,
    pub SettableBaudRate: u8,
    pub SettableDataBits: u8,
    pub SettableFlowControl: u8,
    pub SettableParity: u8,
    pub SettableParityCheck: u8,
    pub SettableStopBits: u8,
    pub IsBusy: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_WMI_HW_DATA {
    pub IrqNumber: u32,
    pub IrqVector: u32,
    pub IrqLevel: u32,
    pub IrqAffinityMask: u64,
    pub InterruptType: u32,
    pub BaseIOAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_WMI_PERF_DATA {
    pub ReceivedCount: u32,
    pub TransmittedCount: u32,
    pub FrameErrorCount: u32,
    pub SerialOverrunErrorCount: u32,
    pub BufferOverrunErrorCount: u32,
    pub ParityErrorCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_WMI_COMMPROP {
    pub wPacketLength: u16,
    pub wPacketVersion: u16,
    pub dwServiceMask: u32,
    pub dwReserved1: u32,
    pub dwMaxTxQueue: u32,
    pub dwMaxRxQueue: u32,
    pub dwMaxBaud: u32,
    pub dwProvSubType: u32,
    pub dwProvCapabilities: u32,
    pub dwSettableParams: u32,
    pub dwSettableBaud: u32,
    pub wSettableData: u16,
    pub wSettableStopParity: u16,
    pub dwCurrentTxQueue: u32,
    pub dwCurrentRxQueue: u32,
    pub dwProvSpec1: u32,
    pub dwProvSpec2: u32,
    pub dwProvCharSize: u32,
    pub wcProvChar: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARPORT_WMI_ALLOC_FREE_COUNTS {
    pub PortAllocates: u32,
    pub PortFrees: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARALLEL_WMI_LOG_INFO {
    pub Flags1: u32,
    pub Flags2: u32,
    pub spare: [u32; 2],
    pub SppWriteCount: i64,
    pub NibbleReadCount: i64,
    pub BoundedEcpWriteCount: i64,
    pub BoundedEcpReadCount: i64,
    pub HwEcpWriteCount: i64,
    pub HwEcpReadCount: i64,
    pub SwEcpWriteCount: i64,
    pub SwEcpReadCount: i64,
    pub HwEppWriteCount: i64,
    pub HwEppReadCount: i64,
    pub SwEppWriteCount: i64,
    pub SwEppReadCount: i64,
    pub ByteReadCount: i64,
    pub ChannelNibbleReadCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REDBOOK_WMI_STD_DATA {
    pub NumberOfBuffers: u32,
    pub SectorsPerRead: u32,
    pub SectorsPerReadMask: u32,
    pub MaximumSectorsPerRead: u32,
    pub PlayEnabled: u8,
    pub CDDASupported: u8,
    pub CDDAAccurate: u8,
    pub Reserved1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REDBOOK_WMI_PERF_DATA {
    pub TimeReadDelay: i64,
    pub TimeReading: i64,
    pub TimeStreamDelay: i64,
    pub TimeStreaming: i64,
    pub DataProcessed: i64,
    pub StreamPausedCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RegisteredGuids {
    pub GuidType: u32,
    pub LoggerId: u32,
    pub EnableLevel: u32,
    pub EnableFlags: u32,
    pub IsEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CounterCorruptionStatus {
    pub ProfileSource: u32,
    pub LastKnownGoodTimestamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_TAPE_SYMBOLIC_NAME {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_CHANGER_PARAMETERS {
    pub NumberOfSlots: u32,
    pub NumberOfDrives: u32,
    pub NumberOfIEPorts: u32,
    pub NumberOfTransports: u32,
    pub NumberOfDoors: u32,
    pub NumberOfCleanerSlots: u32,
    pub MagazineSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_CHANGER_PROBLEM_WARNING {
    pub ChangerProblemType: u32,
    pub ChangerData: [u8; 512],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMI_CHANGER_PROBLEM_DEVICE_ERROR {
    pub ChangerProblemType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_UI_FIRMWARE_REVISION {
    pub VariableData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSVerifierIrpLogEntry {
    pub Count: u32,
    pub Major: u8,
    pub Minor: u8,
    pub Flags: u8,
    pub Control: u8,
    pub Arg1: u64,
    pub Arg2: u64,
    pub Arg3: u64,
    pub Arg4: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSVerifierIrpLogInformation {
    pub DeviceType: u32,
    pub EntryCount: u32,
    pub Entries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorBrightness {
    pub CurrentBrightness: u8,
    pub Levels: u32,
    pub Level: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetBrightness_IN {
    pub Timeout: u32,
    pub Brightness: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetALSBrightnessState_IN {
    pub State: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiSetALSBrightness_IN {
    pub Brightness: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorBrightnessEvent {
    pub Brightness: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorRawEEdidV1Block {
    pub Id: u8,
    pub Type: u8,
    pub Content: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiGetMonitorRawEEdidV1Block_IN {
    pub BlockId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiGetMonitorRawEEdidV1Block_OUT {
    pub BlockType: u8,
    pub BlockContent: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorID {
    pub ProductCodeID: [u16; 16],
    pub SerialNumberID: [u16; 16],
    pub ManufacturerName: [u16; 16],
    pub WeekOfManufacture: u8,
    pub YearOfManufacture: u16,
    pub UserFriendlyNameLength: u16,
    pub UserFriendlyName: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorSupportedDisplayFeatures {
    pub StandbySupported: u8,
    pub SuspendSupported: u8,
    pub ActiveOffSupported: u8,
    pub DisplayType: u8,
    pub sRGBSupported: u8,
    pub HasPreferredTimingMode: u8,
    pub GTFSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorBasicDisplayParams {
    pub VideoInputType: u8,
    pub MaxHorizontalImageSize: u8,
    pub MaxVerticalImageSize: u8,
    pub DisplayTransferCharacteristic: u8,
    pub SupportedDisplayFeatures: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorConnectionParams {
    pub VideoOutputTechnology: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorAnalogVideoInputParams {
    pub SignalLevelStandard: u8,
    pub SetupExpected: u8,
    pub SeparateSyncsSupported: u8,
    pub CompositeSyncSupported: u8,
    pub SyncOnGreenVideoSupported: u8,
    pub SerrationOfVsyncRequired: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorDigitalVideoInputParams {
    pub IsDFP1xCompatible: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorColorXYZinCIE {
    pub X: u16,
    pub Y: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorColorCharacteristics {
    pub Red: u32,
    pub Blue: u32,
    pub Green: u32,
    pub DefaultWhite: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VideoModeDescriptor {
    pub PixelClockRate: u32,
    pub VerticalRefreshRateNumerator: u32,
    pub VerticalRefreshRateDenominator: u32,
    pub HorizontalRefreshRateNumerator: u32,
    pub HorizontalRefreshRateDenominator: u32,
    pub HorizontalActivePixels: u16,
    pub VerticalActivePixels: u16,
    pub HorizontalBlankingPixels: u16,
    pub VerticalBlankingPixels: u16,
    pub HorizontalSyncOffset: u16,
    pub VerticalSyncOffset: u16,
    pub HorizontalSyncPulseWidth: u16,
    pub VerticalSyncPulseWidth: u16,
    pub HorizontalImageSize: u16,
    pub VerticalImageSize: u16,
    pub HorizontalBorder: u16,
    pub VerticalBorder: u16,
    pub IsInterlaced: u8,
    pub StereoModeType: u8,
    pub SyncSignalType: u8,
    pub IsSerrationRequired: u8,
    pub IsSyncOnRGB: u8,
    pub CompositePolarityType: u8,
    pub VerticalPolarityType: u8,
    pub HorizontalPolarityType: u8,
    pub VideoStandardType: u8,
    pub Origin: u8,
    pub TimingType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorListedSupportedSourceModes {
    pub NumOfMonitorSourceModes: u16,
    pub PreferredMonitorSourceModeIndex: u16,
    pub MonitorSourceModes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FrequencyRangeDescriptor {
    pub Origin: u8,
    pub MinVSyncNumerator: u32,
    pub MinVSyncDenominator: u32,
    pub MaxVSyncNumerator: u32,
    pub MaxVSyncDenominator: u32,
    pub MinHSyncNumerator: u32,
    pub MinHSyncDenominator: u32,
    pub MaxHSyncNumerator: u32,
    pub MaxHSyncDenominator: u32,
    pub ConstraintType: u32,
    pub ActiveWidth: u32,
    pub ActiveHeight: u32,
    pub MaxPixelRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WmiMonitorListedFrequencyRanges {
    pub NumOfMonitorFreqRanges: u16,
    pub MonitorFreqRanges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelPerfState {
    pub Frequency: u32,
    pub Power: u32,
    pub PercentFrequency: u8,
    pub IncreaseLevel: u8,
    pub DecreaseLevel: u8,
    pub Type: u8,
    pub IncreaseTime: u32,
    pub DecreaseTime: u32,
    pub Control: u64,
    pub Status: u64,
    pub HitCount: u32,
    pub Reserved1: u32,
    pub Reserved2: u64,
    pub Reserved3: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelPerfStates {
    pub Count: u32,
    pub MaxFrequency: u32,
    pub CurrentState: u32,
    pub MaxPerfState: u32,
    pub MinPerfState: u32,
    pub LowestPerfState: u32,
    pub ThermalConstraint: u32,
    pub BusyAdjThreshold: u8,
    pub PolicyType: u8,
    pub Type: u8,
    pub Reserved: u8,
    pub TimerInterval: u32,
    pub TargetProcessors: u64,
    pub PStateHandler: u32,
    pub PStateContext: u32,
    pub TStateHandler: u32,
    pub TStateContext: u32,
    pub FeedbackHandler: u32,
    pub Reserved1: u32,
    pub Reserved2: u64,
    pub State: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelIdleState {
    pub Latency: u32,
    pub Power: u32,
    pub TimeCheck: u32,
    pub PromotePercent: u8,
    pub DemotePercent: u8,
    pub StateType: u8,
    pub Reserved: u8,
    pub StateFlags: u32,
    pub Context: u32,
    pub IdleHandler: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelIdleStates {
    pub Type: u32,
    pub Count: u32,
    pub TargetState: u32,
    pub OldState: u32,
    pub TargetProcessors: u64,
    pub State: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelPerfStateChange {
    pub State: u32,
    pub Status: u32,
    pub Latency: u32,
    pub Speed: u32,
    pub Processor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelPerfStateDomainChange {
    pub State: u32,
    pub Latency: u32,
    pub Speed: u32,
    pub Processors: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelIdleStateChange {
    pub NewState: u32,
    pub OldState: u32,
    pub Processors: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelThermalConstraintChange {
    pub ThermalConstraint: u32,
    pub Processors: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IdleStateAccounting {
    pub IdleTransitions: u32,
    pub FailedTransitions: u32,
    pub InvalidBucketIndex: u32,
    pub TotalTime: u64,
    pub IdleTimeBuckets: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IdleAccounting {
    pub StateCount: u32,
    pub TotalTransitions: u32,
    pub ResetCount: u32,
    pub StartTime: u64,
    pub State: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IdleStateBucketEx {
    pub TotalTimeUs: u64,
    pub MinTimeUs: u32,
    pub MaxTimeUs: u32,
    pub Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IdleStateAccountingEx {
    pub TotalTime: u64,
    pub IdleTransitions: u32,
    pub FailedTransitions: u32,
    pub InvalidBucketIndex: u32,
    pub MinTimeUs: u32,
    pub MaxTimeUs: u32,
    pub IdleTimeBuckets: [u32; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IdleAccountingEx {
    pub StateCount: u32,
    pub TotalTransitions: u32,
    pub ResetCount: u32,
    pub StartTime: u64,
    pub State: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KernelThermalPolicyChange {
    pub CoolingMode: u8,
    pub Processors: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ProcessorPerformance {
    pub frequency: u32,
    pub power: u32,
    pub percentage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMIGUIDREGINFO {
    pub Guid: usize,
    pub InstanceCount: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WMILIB_CONTEXT {
    pub GuidCount: u32,
    pub GuidList: u32,
    pub QueryWmiRegInfo: usize,
    pub QueryWmiDataBlock: usize,
    pub SetWmiDataBlock: usize,
    pub SetWmiDataItem: usize,
    pub ExecuteWmiMethod: usize,
    pub WmiFunctionControl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_LTE_INFO {
    pub version: u32,
    pub bLTEAttached: i32,
    pub LastAttachAPN: [u32; 1],
    pub LastAttachBearerIPType: u32,
    pub bUnbridgedBearer: i32,
    pub bUnbridgedBearerModemUsage: i32,
    pub UnbridgedBearerAPN: [u32; 1],
    pub UnbridgedBearerIPType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_LTE_ATTACH_BEARER_DATA {
    pub AccessString: [u32; 1],
    pub IPType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_LTE_BRIDGE_EVENT_DATA {
    pub bBridging: i32,
    pub AccessString: [u32; 1],
    pub IPType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_ACTIVATION_OPTION_RESPONSE {
    pub ResponseCode: u32,
    pub CurrentActivationOption: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_DATA_DORMANCY_HINT_STATE_RESPONSE {
    pub ResponseCode: u32,
    pub CurrentDataDormancyHintState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_MODEM_EXECUTOR_IDS {
    pub modemNumber: u32,
    pub ExecutorID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_DM_IPV6_EHRPD_CONFIG_RESPONSE {
    pub ResponseCode: u32,
    pub CurrentIpv6eHrpdConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_DM_CONN_CONFIG_RESPONSE_V2 {
    pub ResponseCode: u32,
    pub ulMaxVersion: u32,
    pub CurrentDMConnConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_DM_CONN_CONFIG_RESPONSE {
    pub ResponseCode: u32,
    pub CurrentDMConnConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_WINPHONE_DM_CONFIG_RESPONSE {
    pub ulVersion: u32,
    pub ResponseCode: u32,
    pub uAdditionalInfo: u32,
    pub CurrentDMConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WPP_TRIAGE_INFO {
    pub WppAutoLogHeaderSize: u32,
    pub WppDriverContextOffset: u32,
    pub WppAutoLogHeaderSizeOffset: u32,
    pub WppSizeOfAutoLogHeaderSizeField: u32,
    pub WppDriverContextSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECORDER_CONFIGURE_PARAMS {
    pub Size: u32,
    pub CreateDefaultLog: u8,
    pub UseTimeStamp: u32,
    pub PreciseTimeStamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECORDER_CONFIGURE_PARAMS_V1 {
    pub Size: u32,
    pub CreateDefaultLog: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECORDER_LOG_CREATE_PARAMS {
    pub Size: u32,
    pub LogTag: u32,
    pub TotalBufferSize: u32,
    pub ErrorPartitionSize: u32,
    pub LogIdentifierAppendValue: usize,
    pub LogIdentifierAppendValueSet: u8,
    pub LogIdentifierSize: u32,
    pub LogIdentifier: [i8; 1],
    pub UseTimeStamp: u32,
    pub PreciseTimeStamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECORDER_LOG_CREATE_PARAMS_V1 {
    pub Size: u32,
    pub LogTag: u32,
    pub TotalBufferSize: u32,
    pub ErrorPartitionSize: u32,
    pub LogIdentifierAppendValue: usize,
    pub LogIdentifierAppendValueSet: u8,
    pub LogIdentifierSize: u32,
    pub LogIdentifier: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_SOCKET {
    pub field_Dispatch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_CHARACTERISTICS {
    pub HighestVersion: u16,
    pub LowestVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_TRANSPORT {
    pub Version: u16,
    pub SocketType: u16,
    pub Protocol: u32,
    pub AddressFamily: u32,
    pub ProviderId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_BUF {
    pub Mdl: usize,
    pub Offset: u32,
    pub Length: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_DATA_INDICATION {
    pub field_Next: u32,
    pub Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_DATAGRAM_INDICATION {
    pub field_Next: u32,
    pub Buffer: u32,
    pub ControlInfo: u32,
    pub ControlInfoLength: u32,
    pub RemoteAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_INSPECT_ID {
    pub Key: usize,
    pub SerialNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_TDI_MAP {
    pub SocketType: u16,
    pub AddressFamily: u32,
    pub Protocol: u32,
    pub TdiDeviceName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_TDI_MAP_INFO {
    pub ElementCount: u32,
    pub field_Map: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_EVENT_CALLBACK_CONTROL {
    pub NpiId: usize,
    pub EventMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_EXTENSION_CONTROL_IN {
    pub NpiId: usize,
    pub ClientContext: usize,
    pub ClientDispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_EXTENSION_CONTROL_OUT {
    pub ProviderContext: usize,
    pub ProviderDispatch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_BUF_LIST {
    pub field_Next: u32,
    pub Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_DISPATCH {
    pub Version: u16,
    pub Reserved: u16,
    pub WskClientEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_LISTEN_DISPATCH {
    pub WskAcceptEvent: usize,
    pub WskInspectEvent: usize,
    pub WskAbortEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_DATAGRAM_DISPATCH {
    pub WskReceiveFromEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_CONNECTION_DISPATCH {
    pub WskReceiveEvent: usize,
    pub WskDisconnectEvent: usize,
    pub WskSendBacklogEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_STREAM_DISPATCH {
    pub Listen: usize,
    pub Connect: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_DISPATCH {
    pub Version: u16,
    pub Reserved: u16,
    pub WskSocket: usize,
    pub WskSocketConnect: usize,
    pub WskControlClient: usize,
    pub WskGetAddressInfo: usize,
    pub WskFreeAddressInfo: usize,
    pub WskGetNameInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_BASIC_DISPATCH {
    pub WskControlSocket: usize,
    pub WskCloseSocket: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_LISTEN_DISPATCH {
    pub Basic: u32,
    pub WskBind: usize,
    pub WskAccept: usize,
    pub WskInspectComplete: usize,
    pub WskGetLocalAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_DATAGRAM_DISPATCH {
    pub Basic: u32,
    pub WskBind: usize,
    pub WskSendTo: usize,
    pub WskReceiveFrom: usize,
    pub WskRelease: usize,
    pub WskGetLocalAddress: usize,
    pub WskSendMessages: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_CONNECTION_DISPATCH {
    pub Basic: u32,
    pub WskBind: usize,
    pub WskConnect: usize,
    pub WskGetLocalAddress: usize,
    pub WskGetRemoteAddress: usize,
    pub WskSend: usize,
    pub WskReceive: usize,
    pub WskDisconnect: usize,
    pub WskRelease: usize,
    pub WskConnectEx: usize,
    pub WskSendEx: usize,
    pub WskReceiveEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_STREAM_DISPATCH {
    pub Basic: u32,
    pub WskBind: usize,
    pub WskAccept: usize,
    pub WskConnect: usize,
    pub WskListen: usize,
    pub WskSend: usize,
    pub WskReceive: usize,
    pub WskDisconnect: usize,
    pub WskRelease: usize,
    pub WskGetLocalAddress: usize,
    pub WskGetRemoteAddress: usize,
    pub WskConnectEx: usize,
    pub WskSendEx: usize,
    pub WskReceiveEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_CLIENT_NPI {
    pub ClientContext: usize,
    pub field_Dispatch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_NPI {
    pub Client: usize,
    pub field_Dispatch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_REGISTRATION {
    pub ReservedRegistrationState: u64,
    pub ReservedRegistrationContext: usize,
    pub ReservedRegistrationLock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSK_PROVIDER_NDK_DISPATCH {
    pub WskOpenNdkAdapter: usize,
    pub WskCloseNdkAdapter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LIST_HEADER {
    pub ElementType: u32,
    pub ElementCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_OFFSET_SIZE_ELEMENT {
    pub Offset: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DRIVER_CAPS {
    pub ulMajorVersion: u32,
    pub ulMinorVersion: u32,
    pub ulDriverCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_CAPS {
    pub WwanDeviceType: u32,
    pub WwanCellularClass: u32,
    pub WwanVoiceClass: u32,
    pub WwanSimClass: u32,
    pub WwanDataClass: u32,
    pub CustomDataClass: [u32; 1],
    pub WwanGsmBandClass: u32,
    pub WwanCdmaBandClass: u32,
    pub CustomBandClass: [u32; 1],
    pub WwanSmsCaps: u32,
    pub WwanControlCaps: u32,
    pub DeviceId: [u32; 1],
    pub Manufacturer: [u32; 1],
    pub Model: [u32; 1],
    pub FirmwareInfo: [u32; 1],
    pub MaxActivatedContexts: u32,
    pub WwanAuthAlgoCaps: u32,
    pub CellularClassListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REJECTED_SNSSAI {
    pub CauseCode: u32,
    pub SliceServiceType: u32,
    pub SliceDifferentiator: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SINGLE_NSSAI {
    pub Bitmasks: u32,
    pub SliceServiceType: u32,
    pub MappedSliceServiceType: u32,
    pub SliceDifferentiator: u32,
    pub MappedSliceDifferentiator: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_CAPS_EX_REV1 {
    pub WwanDeviceType: u32,
    pub WwanCellularClass: u32,
    pub WwanVoiceClass: u32,
    pub WwanSimClass: u32,
    pub WwanDataClass: u32,
    pub CustomDataClass: [u32; 1],
    pub WwanGsmBandClass: u32,
    pub WwanCdmaBandClass: u32,
    pub CustomBandClass: [u32; 1],
    pub WwanSmsCaps: u32,
    pub WwanControlCaps: u32,
    pub DeviceId: [u32; 1],
    pub Manufacturer: [u32; 1],
    pub Model: [u32; 1],
    pub FirmwareInfo: [u32; 1],
    pub MaxActivatedContexts: u32,
    pub WwanAuthAlgoCaps: u32,
    pub ExecutorIndex: u32,
    pub WwanOptionalServiceCaps: u32,
    pub CellularClassListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_CAPS_EX {
    pub WwanDeviceType: u32,
    pub WwanCellularClass: u32,
    pub WwanVoiceClass: u32,
    pub WwanSimClass: u32,
    pub WwanDataClass: u32,
    pub CustomDataClass: [u32; 1],
    pub WwanGsmBandClass: u32,
    pub WwanCdmaBandClass: u32,
    pub CustomBandClass: [u32; 1],
    pub WwanSmsCaps: u32,
    pub WwanControlCaps: u32,
    pub DeviceId: [u32; 1],
    pub Manufacturer: [u32; 1],
    pub Model: [u32; 1],
    pub FirmwareInfo: [u32; 1],
    pub MaxActivatedContexts: u32,
    pub WwanAuthAlgoCaps: u32,
    pub ExecutorIndex: u32,
    pub WwanOptionalServiceCaps: u32,
    pub WwanSimClassBitMasks: u32,
    pub WwanWCDMABandClass: u32,
    pub WwanLTEBandClass: [u16; 1],
    pub WwanNRBandClass: [u16; 1],
    pub WwanDataSubClass: u64,
    pub WwanMiscCaps: u64,
    pub CellularClassListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_READY_INFO_REV1 {
    pub ReadyState: u32,
    pub EmergencyMode: u32,
    pub SubscriberId: [u32; 1],
    pub SimIccId: [u32; 1],
    pub CdmaShortMsgSize: u8,
    pub TNListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_READY_INFO_REV2_PRE2170 {
    pub ReadyState: u32,
    pub EmergencyMode: u32,
    pub SubscriberId: [u32; 1],
    pub SimIccId: [u32; 1],
    pub CdmaShortMsgSize: u8,
    pub StatusFlags: u32,
    pub TNListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_READY_INFO {
    pub ReadyState: u32,
    pub EmergencyMode: u32,
    pub SubscriberId: [u32; 1],
    pub SimIccId: [u32; 1],
    pub CdmaShortMsgSize: u8,
    pub StatusFlags: u32,
    pub SlotId: u32,
    pub TNListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SERVICE_ACTIVATION {
    pub uVendorSpecificBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_RADIO_STATE {
    pub HwRadioState: u32,
    pub SwRadioState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_INFO {
    pub PinType: u32,
    pub PinState: u32,
    pub AttemptsRemaining: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_ACTION {
    pub PinType: u32,
    pub PinOperation: u32,
    pub Pin: [u32; 1],
    pub NewPin: [u32; 1],
    pub RequestPinOperationPrompt: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_DESC {
    pub PinMode: u32,
    pub PinFormat: u32,
    pub PinLengthMin: u32,
    pub PinLengthMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_LIST {
    pub WwanPinDescPin1: u32,
    pub WwanPinDescPin2: u32,
    pub WwanPinDescDeviceSimPin: u32,
    pub WwanPinDescDeviceFirstSimPin: u32,
    pub WwanPinDescNetworkPin: u32,
    pub WwanPinDescNetworkSubsetPin: u32,
    pub WwanPinDescSvcProviderPin: u32,
    pub WwanPinDescCorporatePin: u32,
    pub WwanPinDescSubsidyLock: u32,
    pub WwanPinDescCustom: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PROVIDER {
    pub ProviderId: [u32; 1],
    pub ProviderState: u32,
    pub ProviderName: [u32; 1],
    pub WwanDataClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_REGISTER_STATE {
    pub ProviderId: [u32; 1],
    pub RegisterAction: u32,
    pub WwanDataClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_REGISTER_STATE_EX {
    pub ProviderId: [u32; 1],
    pub RegisterAction: u32,
    pub WwanDataClass: u32,
    pub WwanVoiceClass: u32,
    pub WwanVoiceDomain: u32,
    pub WwanCdmaRoamMode: u32,
    pub AcquisitionOrderListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REGISTRATION_STATE {
    pub uNwError: u32,
    pub RegisterState: u32,
    pub RegisterMode: u32,
    pub ProviderId: [u32; 1],
    pub ProviderName: [u32; 1],
    pub RoamingText: [u32; 1],
    pub WwanRegFlags: u32,
    pub CurrentCellularClass: u32,
    pub PreferredDataClasses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REGISTRATION_STATE_EX {
    pub uNwError: u32,
    pub RegisterState: u32,
    pub RegisterMode: u32,
    pub ProviderId: [u32; 1],
    pub ProviderName: [u32; 1],
    pub RoamingText: [u32; 1],
    pub WwanRegFlags: u32,
    pub CurrentCellularClass: u32,
    pub AvailableDataClasses: u32,
    pub VoiceSupport: u32,
    pub CurrentRATClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REGISTRATION_STATE_INFOS {
    pub RegistrationStateListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SINGLE_TAI {
    pub Mcc: u32,
    pub Mnc: u32,
    pub Tac: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PACKET_SERVICE {
    pub uNwError: u32,
    pub PacketServiceState: u32,
    pub AvailableDataClass: u32,
    pub CurrentDataClass: u32,
    pub FrequencyRange: u32,
    pub CurrentDataSubClass: u32,
    pub CurrentTai: u32,
    pub Rel3GppVer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SIGNAL_STATE_EXT {
    pub RSRP: u32,
    pub RSRPThreshold: u32,
    pub SNR: u32,
    pub SNRThreshold: u32,
    pub DataClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SIGNAL_STATE {
    pub Rssi: u32,
    pub ErrorRate: u32,
    pub RssiInterval: u32,
    pub RssiThreshold: u32,
    pub SignalStateListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SIGNAL_STATE_EX {
    pub Rssi: u32,
    pub ErrorRate: u32,
    pub RssiInterval: u32,
    pub RssiThreshold: u32,
    pub ErrorRateThreshold: u32,
    pub Snr: u32,
    pub SnrThreshold: u32,
    pub DataClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SIGNAL_STATE_INFOS {
    pub SignalStateListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_SIGNAL_INDICATION {
    pub RssiInterval: u32,
    pub RssiThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_SIGNAL_INDICATION_EX {
    pub RssiInterval: u32,
    pub RssiThreshold: u32,
    pub ErrorRateThreshold: u32,
    pub SnrThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_CONTEXT_STATE {
    pub ConnectionId: u32,
    pub ActivationCommand: u32,
    pub AccessString: [u32; 1],
    pub UserName: [u32; 1],
    pub Password: [u32; 1],
    pub Compression: u32,
    pub AuthType: u32,
    pub IPType: u32,
    pub MediaPreference: u32,
    pub ConnectionMediaSource: u32,
    pub ActivationOption: u32,
    pub Snssai: u32,
    pub TrafficParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TLV_IE {
    pub Type: u32,
    pub Reserved: u8,
    pub PaddingLength: u8,
    pub DataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_CONTEXT_STATE {
    pub uNwError: u32,
    pub ConnectionId: u32,
    pub ActivationState: u32,
    pub VoiceCallState: u32,
    pub IPType: u32,
    pub ConnectionMedia: u32,
    pub AccessString: [u32; 1],
    pub Snssai: u32,
    pub FoundMatchingConnectionId: u32,
    pub TrafficParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_CONTEXT {
    pub ContextId: u32,
    pub ContextType: u32,
    pub AccessString: [u32; 1],
    pub UserName: [u32; 1],
    pub Password: [u32; 1],
    pub Compression: u32,
    pub AuthType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_CONTEXT {
    pub ContextId: u32,
    pub ContextType: u32,
    pub AccessString: [u32; 1],
    pub UserName: [u32; 1],
    pub Password: [u32; 1],
    pub Compression: u32,
    pub AuthType: u32,
    pub ProviderId: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_CONTEXT_V2 {
    pub basicInfo: u32,
    pub IPType: u32,
    pub Enable: u32,
    pub Roaming: u32,
    pub MediaType: u32,
    pub Source: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_CONTEXT_V2 {
    pub basicInfo: u32,
    pub Operation: u32,
    pub IPType: u32,
    pub Enable: u32,
    pub Roaming: u32,
    pub MediaType: u32,
    pub Source: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PROVISIONED_CONTEXT_V2 {
    pub basicInfo: u32,
    pub IPType: u32,
    pub Enable: u32,
    pub Roaming: u32,
    pub MediaType: u32,
    pub Source: u32,
    pub Reserved: u32,
    pub Snssai: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_PROVISIONED_CONTEXT_V2 {
    pub basicInfo: u32,
    pub Operation: u32,
    pub IPType: u32,
    pub Enable: u32,
    pub Roaming: u32,
    pub MediaType: u32,
    pub Source: u32,
    pub Reserved: u32,
    pub Snssai: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_LTE_ATTACH {
    pub SetContextArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LTE_ATTACH_STATUS {
    pub LteAttachState: u32,
    pub IPType: u32,
    pub basicInfo: u32,
    pub NetworkError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LTE_ATTACH_CONFIG {
    pub ElementCount: u32,
    pub LteContextArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_SMS_CONFIGURATION {
    pub ScAddress: [i8; 1],
    pub SmsFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_CONFIGURATION {
    pub ScAddress: [i8; 1],
    pub SmsFormat: u32,
    pub ulMaxMessageIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_FILTER {
    pub Flag: u32,
    pub MessageIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_READ {
    pub SmsFormat: u32,
    pub ReadFilter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_CDMA_RECORD {
    pub MessageIndex: u32,
    pub MsgStatus: u32,
    pub Address: [i8; 1],
    pub ScTimeStamp: [i8; 1],
    pub EncodingId: u32,
    pub LanguageId: u32,
    pub SizeInBytes: u16,
    pub SizeInCharacters: u8,
    pub EncodedMsg: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_PDU_RECORD {
    pub MessageIndex: u32,
    pub MsgStatus: u32,
    pub Size: u8,
    pub PduData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_SEND_CDMA {
    pub EncodingId: u32,
    pub LanguageId: u32,
    pub Address: [i8; 1],
    pub SizeInBytes: u16,
    pub SizeInCharacters: u8,
    pub EncodedMsg: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_SEND_PDU {
    pub Size: u8,
    pub PduData: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_SEND {
    pub SmsFormat: u32,
    pub Pdu: u32,
    pub Cdma: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SMS_STATUS {
    pub uFlag: u32,
    pub MessageIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SERVICE_ACTIVATION_STATUS {
    pub uNwError: u32,
    pub uVendorSpecificBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_VENDOR_SPECIFIC {
    pub uVendorSpecificBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_ENTRY {
    pub DeviceServiceGuid: u32,
    pub SessionCapability: u32,
    pub uMaxSessionInstances: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SUPPORTED_DEVICE_SERVICES {
    pub uMaxCommandDataSize: u32,
    pub uMaxSessionDataSize: u32,
    pub uMaxSessionCount: u32,
    pub ListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_COMMAND {
    pub DeviceServiceGuid: u32,
    pub CommandID: u32,
    pub uDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_RESPONSE {
    pub DeviceServiceGuid: u32,
    pub ResponseID: u32,
    pub uDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_EVENT {
    pub DeviceServiceGuid: u32,
    pub EventID: u32,
    pub uDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_SUPPORTED_COMMANDS {
    pub DeviceServiceGuid: u32,
    pub ListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_SESSION {
    pub DeviceServiceGuid: u32,
    pub State: u32,
    pub uSessionID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_SESSION_READ {
    pub uSessionID: u32,
    pub uDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SERVICE_SESSION_WRITE {
    pub uSessionID: u32,
    pub uDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PROVIDER2 {
    pub Provider: u32,
    pub WwanCellularClass: u32,
    pub Rssi: u32,
    pub ErrorRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_USSD_STRING {
    pub DataCodingScheme: u8,
    pub StringLength: u8,
    pub String: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_USSD_REQUEST {
    pub RequestType: u32,
    pub UssdString: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_USSD_EVENT {
    pub EventType: u32,
    pub SessionState: u32,
    pub UssdString: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_GET_VISIBLE_PROVIDERS {
    pub Action: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_SIM_CHALLENGE {
    pub Rand1: [u8; 1],
    pub Rand2: [u8; 1],
    pub Rand3: [u8; 1],
    pub n: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_AKA_CHALLENGE {
    pub Rand: [u8; 1],
    pub Autn: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_AKAP_CHALLENGE {
    pub Rand: [u8; 1],
    pub Autn: [u8; 1],
    pub NetworkName: [u8; 1],
    pub NetworkNameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_CHALLENGE {
    pub AuthMethod: u32,
    pub AuthSim: u32,
    pub AuthAka: u32,
    pub AuthAkap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_SIM_RESPONSE {
    pub Sres1: [u8; 1],
    pub Kc1: [u8; 1],
    pub Sres2: [u8; 1],
    pub Kc2: [u8; 1],
    pub Sres3: [u8; 1],
    pub Kc3: [u8; 1],
    pub n: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_AKA_RESPONSE {
    pub Res: [u8; 1],
    pub ResLength: u32,
    pub IK: [u8; 1],
    pub CK: [u8; 1],
    pub Auts: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_AKAP_RESPONSE {
    pub Res: [u8; 1],
    pub ResLength: u32,
    pub IKP: [u8; 1],
    pub CKP: [u8; 1],
    pub Auts: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_AUTH_RESPONSE {
    pub AuthMethod: u32,
    pub AuthSim: u32,
    pub AuthAka: u32,
    pub AuthAkap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_IPV4_ADDRESS {
    pub OnLinkPrefixLength: u32,
    pub IPV4Address: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_IPV6_ADDRESS {
    pub OnLinkPrefixLength: u32,
    pub IPV6Address: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_IPADDRESS_ENTRY {
    pub IsIpv6: u32,
    pub IsReported: u32,
    pub Ipv4: u32,
    pub Ipv6: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_IP_ADDRESS_STATE {
    pub IPv4Flags: u32,
    pub IPv6Flags: u32,
    pub IPv4MTU: u32,
    pub IPv6MTU: u32,
    pub IpTable: usize,
    pub IpCount: u32,
    pub GatewayTable: usize,
    pub GatewayCount: u32,
    pub DnsTable: usize,
    pub DnsCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SYS_CAPS_INFO {
    pub NumberOfExecutors: u32,
    pub NumberOfSlots: u32,
    pub Concurrency: u32,
    pub ModemID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_MBIM_VERSION {
    pub MbimVersion: u16,
    pub MbimExtendedVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_SLOT_MAPPING_INFO {
    pub SlotMapListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_GET_SLOT_INFO {
    pub SlotIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SLOT_INFO {
    pub SlotIndex: u32,
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_DEVICE_BINDINGS_INFO {
    pub ApplicationListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_IMS_VOICE_STATE {
    pub ImsVoiceStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_IMS_VOICE_STATE {
    pub ImsVoiceStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_LOCATION_STATE {
    pub Trigger: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LOCATION_STATE_INFO {
    pub Trigger: u32,
    pub DataClass: u32,
    pub AreaCode: u32,
    pub CellId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NITZ_INFO {
    pub Year: u32,
    pub Month: u32,
    pub Day: u32,
    pub Hour: u32,
    pub Minute: u32,
    pub Second: u32,
    pub TimeZoneOffsetMinutes: u32,
    pub DaylightSavingTimeOffsetMinutes: u32,
    pub DataClasses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NETWORK_IDLE_HINT {
    pub IsEnabled: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_ATR_INFO {
    pub AtrLength: u32,
    pub Atr: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_UICC_OPEN_CHANNEL {
    pub AppIdLength: u32,
    pub AppId: [u8; 1],
    pub SelectP2Arg: u32,
    pub ChannelGroup: u32,
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_OPEN_CHANNEL_INFO {
    pub Status: [u8; 1],
    pub Channel: u32,
    pub ResponseLength: u32,
    pub Response: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_UICC_CLOSE_CHANNEL {
    pub Channel: u32,
    pub ChannelGroup: u32,
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_CLOSE_CHANNEL_INFO {
    pub Status: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_UICC_APDU {
    pub Channel: u32,
    pub SecureMessaging: u32,
    pub Type: u32,
    pub CommandLength: u32,
    pub SlotId: u32,
    pub Command: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_APDU_INFO {
    pub Status: [u8; 1],
    pub ResponseLength: u32,
    pub Response: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_TERMINAL_CAPABILITY_TLV {
    pub Data: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_UICC_TERMINAL_CAPABILITY {
    pub SlotId: u32,
    pub CapabilityListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_TERMINAL_CAPABILITY_INFO {
    pub CapabilityListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PS_MEDIA_CONFIG_ITEM {
    pub ServicesBitMap: u32,
    pub MediaPreference: u32,
    pub MediaPreferenceSource: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SAR_CONFIG_INDICES {
    pub SarAntennaIndex: u32,
    pub SarBackoffIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_SAR_CONFIG {
    pub SarMode: u32,
    pub SarBackoffStatus: u32,
    pub SarConfigIndexListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SAR_CONFIG_INFO {
    pub SarMode: u32,
    pub SarBackoffStatus: u32,
    pub SarWifiIntegration: u32,
    pub SarConfigIndexListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_SAR_TRANSMISSION_STATUS {
    pub ChannelNotification: u32,
    pub HysteresisTimer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SAR_TRANSMISSION_STATUS_INFO {
    pub ChannelNotification: u32,
    pub TransmissionStatus: u32,
    pub HysteresisTimer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NETWORK_BLACKLIST_PROVIDER {
    pub MCC: u32,
    pub MNC: u32,
    pub NetworkBlacklistType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NETWORK_BLACKLIST_INFO {
    pub BlacklistState: u32,
    pub BlacklistProviderList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_MODEM_CONFIG_ID {
    pub ConfigIdLen: u32,
    pub ConfigId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_MODEM_CONFIG_INFO {
    pub ConfigMode: u32,
    pub ConfigState: u32,
    pub ConfigReason: u32,
    pub PreviousConfigID: u32,
    pub CurrentConfigID: u32,
    pub IsCurrentConfigDefault: u32,
    pub ConfigName: [u32; 1],
    pub NSSAIListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PCO_VALUE {
    pub Size: u32,
    pub Type: u32,
    pub PcoData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SET_UICC_RESET {
    pub PassThroughAction: u32,
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_RESET_INFO {
    pub PassThroughStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_BASE_STATIONS_INFO_REQ {
    pub MaxGSMCount: u32,
    pub MaxUMTSCount: u32,
    pub MaxTDSCDMACount: u32,
    pub MaxLTECount: u32,
    pub MaxCDMACount: u32,
    pub MaxNRCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_BASE_STATIONS_INFO {
    pub SystemType: u32,
    pub SystemSubType: u32,
    pub GSMServingCellOffset: u32,
    pub GSMServingCellSize: u32,
    pub UMTSServingCellOffset: u32,
    pub UMTSServingCellSize: u32,
    pub TDSCDMAServingCellOffset: u32,
    pub TDSCDMAServingCellSize: u32,
    pub LTEServingCellOffset: u32,
    pub LTEServingCellSize: u32,
    pub GSMNmrOffset: u32,
    pub GSMNmrSize: u32,
    pub UMTSMrlOffset: u32,
    pub UMTSMrlSize: u32,
    pub TDSCDMAMrlOffset: u32,
    pub TDSCDMAMrlSize: u32,
    pub LTEMrlOffset: u32,
    pub LTEMrlSize: u32,
    pub CDMAMrlOffset: u32,
    pub CDMAMrlSize: u32,
    pub NRServingCellsOffset: u32,
    pub NRServingCellsSize: u32,
    pub NRNeighborCellsOffset: u32,
    pub NRNeighborCellsSize: u32,
    pub BaseStationsData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_GSM_SERVING_CELL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub TimingAdvance: u32,
    pub ARFCN: u32,
    pub BaseStationId: u32,
    pub RxLevel: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_GSM_NMR_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub ARFCN: u32,
    pub BaseStationId: u32,
    pub RxLevel: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_GSM_NMR {
    pub ElementCount: u32,
    pub GSMNmr: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UMTS_SERVING_CELL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub FrequencyInfoUL: u32,
    pub FrequencyInfoDL: u32,
    pub FrequencyInfoNT: u32,
    pub UARFCN: u32,
    pub PrimaryScramblingCode: u32,
    pub RSCP: u32,
    pub ECNO: u32,
    pub PathLoss: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UMTS_MRL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub UARFCN: u32,
    pub PrimaryScramblingCode: u32,
    pub RSCP: u32,
    pub ECNO: u32,
    pub PathLoss: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UMTS_MRL {
    pub ElementCount: u32,
    pub UMTSMrl: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TDSCDMA_SERVING_CELL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub UARFCN: u32,
    pub CellParameterId: u32,
    pub TimingAdvance: u32,
    pub RSCP: u32,
    pub PathLoss: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TDSCDMA_MRL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub LocationAreaCode: u32,
    pub CellId: u32,
    pub UARFCN: u32,
    pub CellParameterId: u32,
    pub TimingAdvance: u32,
    pub RSCP: u32,
    pub PathLoss: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TDSCDMA_MRL {
    pub ElementCount: u32,
    pub TDSCDMAMrl: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LTE_SERVING_CELL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub CellId: u32,
    pub EARFCN: u32,
    pub PhysicalCellId: u32,
    pub TAC: u32,
    pub RSRP: u32,
    pub RSRQ: u32,
    pub TimingAdvance: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LTE_MRL_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub CellId: u32,
    pub EARFCN: u32,
    pub PhysicalCellId: u32,
    pub TAC: u32,
    pub RSRP: u32,
    pub RSRQ: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LTE_MRL {
    pub ElementCount: u32,
    pub LTEMrl: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_CDMA_MRL_INFO {
    pub ServingCellFlag: u32,
    pub NID: u32,
    pub SID: u32,
    pub BaseStationId: u32,
    pub BaseLatitude: u32,
    pub BaseLongitude: u32,
    pub RefPn: u32,
    pub GPSSeconds: u32,
    pub PilotStrength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_CDMA_MRL {
    pub ElementCount: u32,
    pub CDMAMrl: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NR_SERVING_CELLS {
    pub ElementCount: u32,
    pub NRServingCells: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NR_SERVING_CELLS_INFO {
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub NCI: u64,
    pub PhysicalCellID: u32,
    pub NRARFCN: u32,
    pub TAC: u32,
    pub RSRP: u32,
    pub RSRQ: u32,
    pub SINR: u32,
    pub TimingAdvance: u64,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NR_NEIGHBOR_CELLS {
    pub ElementCount: u32,
    pub NRNeighborCells: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NR_NEIGHBOR_CELLS_INFO {
    pub SystemSubType: u32,
    pub ProviderIdOffset: u32,
    pub ProviderIdSize: u32,
    pub CellIDOffset: u32,
    pub CellIDSize: u32,
    pub PhysicalCellID: u32,
    pub TAC: u32,
    pub RSRP: u32,
    pub RSRQ: u32,
    pub SINR: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NETWORK_ISOLATION {
    pub RuleGroupId: [u32; 1],
    pub IsolationSettingState: u32,
    pub DataSizeInBytes: u32,
    pub Ipv4AllowedRangeCount: u32,
    pub Ipv6AllowedRangeCount: u32,
    pub AllowedServicesCount: u32,
    pub AllowedProcessesCount: u32,
    pub AllowedApplicationsCount: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_APP_INFO {
    pub AppType: u32,
    pub AppIdSize: u32,
    pub AppId: [u8; 1],
    pub AppNameLength: u32,
    pub AppName: [u8; 1],
    pub NumPins: u32,
    pub PinRef: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_APP_LIST {
    pub Version: u32,
    pub AppCount: u32,
    pub ActiveAppIndex: u32,
    pub AppListSize: u32,
    pub Response: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_FILE_PATH {
    pub Version: u32,
    pub AppIdLength: u8,
    pub AppId: [u8; 1],
    pub FilePathLength: u8,
    pub FilePath: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_FILE_STATUS {
    pub Version: u32,
    pub StatusWord1: u32,
    pub StatusWord2: u32,
    pub FileAccessibility: u32,
    pub FileType: u32,
    pub FileStructure: u32,
    pub ItemCount: u32,
    pub ItemSize: u32,
    pub FileLockStatus: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_ACCESS_BINARY {
    pub Version: u32,
    pub AppId: [u8; 1],
    pub UiccFilePath: u32,
    pub FileOffset: u32,
    pub NumberOfBytes: u32,
    pub LocalPinSize: u8,
    pub LocalPin: [u8; 1],
    pub BinaryDataSize: u16,
    pub BinaryData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_ACCESS_RECORD {
    pub Version: u32,
    pub AppId: [u8; 1],
    pub UiccFilePath: u32,
    pub RecordNumber: u32,
    pub LocalPinSize: u8,
    pub LocalPin: [u8; 1],
    pub RecordDataSize: u8,
    pub RecordData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UICC_RESPONSE {
    pub Version: u32,
    pub StatusWord1: u32,
    pub StatusWord2: u32,
    pub ResponseDataSize: u16,
    pub ResponseData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_ACTION_EX2 {
    pub PinType: u32,
    pub PinOperation: u32,
    pub Pin: [u32; 1],
    pub NewPin: [u32; 1],
    pub AppIdLength: u8,
    pub AppId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PIN_APP {
    pub Version: u32,
    pub AppIdLength: u8,
    pub AppId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_MODEM_LOGGING_CONFIG {
    pub Version: u32,
    pub MaxSegmentSize: u32,
    pub MaxFlushTime: u32,
    pub LevelConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REGISTRATION_PARAMS_INFO {
    pub MicoMode: u32,
    pub DRXParam: u32,
    pub LADNInfo: u32,
    pub DefaultPDUSessionHint: u32,
    pub ReRegisterIfNeeded: u32,
    pub OSId: u32,
    pub PreConfiguredNSSAIListHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_QUERY_UICC_TERMINAL_CAPABILITY {
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_PLMN {
    pub Mcc: u32,
    pub Mnc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TAI_LIST_SINGLE_PLMN {
    pub Plmn: u32,
    pub ElementCount: u32,
    pub Tacs: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TAI_LIST_MULTI_PLMN {
    pub ElementCount: u32,
    pub Tais: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_TAI_LIST {
    pub TaiListType: u32,
    pub DataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_SNSSAI_LIST {
    pub ElementCount: u32,
    pub Snssais: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_REJECTED_SNSSAI_LIST {
    pub ElementCount: u32,
    pub Snssais: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LADN {
    pub AccessString: [u32; 1],
    pub TaiList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_LADN_LIST {
    pub ElementCount: u32,
    pub Ladns: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_NETWORK_PARAMS_INFO {
    pub CurrentMicoIndication: u32,
    pub CurrentDRXParams: u32,
    pub TaiList: u32,
    pub AllowedNssaiList: u32,
    pub ConfiguredNssaiList: u32,
    pub RejectedNssaiList: u32,
    pub DefaultConfiguredNssaiList: u32,
    pub LadnList: u32,
    pub DataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_QUERY_UICC_RESET {
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_QUERY_ATR_INFO {
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_QUERY_READY_INFO {
    pub SlotId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WWAN_UE_POLICY_INFO {
    pub RawTdOnlyData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_CIRCUIT_PNPPOWER_CALLBACKS {
    pub Size: u32,
    pub EvtAcxCircuitPrepareHardware: usize,
    pub EvtAcxCircuitReleaseHardware: usize,
    pub EvtAcxCircuitPowerUp: usize,
    pub EvtAcxCircuitPowerDown: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_CIRCUIT_COMPOSITE_CALLBACKS {
    pub Size: u32,
    pub EvtAcxCircuitCompositeCircuitInitialize: usize,
    pub EvtAcxCircuitCompositeInitialize: usize,
    pub EvtAcxCircuitCompositeDeinitialize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_FACTORY_CIRCUIT_ADD_CIRCUIT {
    pub Size: u32,
    pub Flags: u32,
    pub CircuitUniqueId: u32,
    pub CompositeProperties: usize,
    pub FactoryProperties: usize,
    pub CircuitProperties: usize,
    pub TargetSymbolicLinkName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_FACTORY_CIRCUIT_REMOVE_CIRCUIT {
    pub Size: u32,
    pub Flags: u32,
    pub CircuitUniqueId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_FACTORY_CIRCUIT_OPERATION_CALLBACKS {
    pub Size: u32,
    pub EvtAcxFactoryCircuitCreateCircuitDevice: usize,
    pub EvtAcxFactoryCircuitDeleteCircuitDevice: usize,
    pub EvtAcxFactoryCircuitCreateCircuit: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_FACTORY_CIRCUIT_PNPPOWER_CALLBACKS {
    pub Size: u32,
    pub EvtAcxFactoryCircuitPrepareHardware: usize,
    pub EvtAcxFactoryCircuitReleaseHardware: usize,
    pub EvtAcxFactoryCircuitPowerUp: usize,
    pub EvtAcxFactoryCircuitPowerDown: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DATAFORMAT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Type: u32,
    pub KsFormat: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DATAFORMAT_LIST_CONFIG {
    pub Size: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DATAFORMAT_LIST_ITERATOR {
    pub Size: u32,
    pub Flags: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DEVICEINIT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub SynchronizationScope: u32,
    pub ExecutionLevel: u32,
    pub Context: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DEVICE_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Context: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DRIVER_CONFIG {
    pub Size: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DRIVER_VERSION_AVAILABLE_PARAMS {
    pub Size: u32,
    pub MajorVersion: u32,
    pub MinorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_ELEMENT_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Type: usize,
    pub Name: usize,
    pub Flags: u32,
    pub PropertiesCount: u32,
    pub MethodsCount: u32,
    pub Properties: usize,
    pub Methods: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MUTE_CALLBACKS {
    pub Size: u32,
    pub EvtAcxMuteAssignState: usize,
    pub EvtAcxMuteRetrieveState: usize,
    pub EvtAcxMuteProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MUTE_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub ChannelsCount: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_VOLUME_CALLBACKS {
    pub Size: u32,
    pub EvtAcxVolumeAssignLevel: usize,
    pub EvtAcxRampedVolumeAssignLevel: usize,
    pub EvtAcxVolumeRetrieveLevel: usize,
    pub EvtAcxVolumeProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_VOLUME_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub ChannelsCount: u32,
    pub Maximum: i32,
    pub Minimum: i32,
    pub SteppingDelta: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PEAKMETER_CALLBACKS {
    pub Size: u32,
    pub EvtAcxPeakMeterRetrieveLevel: usize,
    pub EvtAcxPeakMeterProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PEAKMETER_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub ChannelsCount: u32,
    pub Maximum: i32,
    pub Minimum: i32,
    pub SteppingDelta: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_AUDIOENGINE_CALLBACKS {
    pub Size: u32,
    pub EvtAcxAudioEngineRetrieveBufferSizeLimits: usize,
    pub EvtAcxAudioEngineAssignEffectsState: usize,
    pub EvtAcxAudioEngineRetrieveEffectsState: usize,
    pub EvtAcxAudioEngineRetrieveEngineMixFormat: usize,
    pub EvtAcxAudioEngineAssignEngineDeviceFormat: usize,
    pub EvtAcxAudioEngineProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_AUDIOENGINE_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub HostPin: u32,
    pub OffloadPin: u32,
    pub LoopbackPin: u32,
    pub VolumeElement: u32,
    pub MuteElement: u32,
    pub PeakMeterElement: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_STREAMAUDIOENGINE_CALLBACKS {
    pub Size: u32,
    pub EvtAcxStreamAudioEngineAssignEffectsState: usize,
    pub EvtAcxStreamAudioEngineRetrieveEffectsState: usize,
    pub EvtAcxStreamAudioEngineRetrievePresentationPosition: usize,
    pub EvtAcxStreamAudioEngineAssignCurrentWritePosition: usize,
    pub EvtAcxStreamAudioEngineRetrieveLinearBufferPosition: usize,
    pub EvtAcxStreamAudioEngineAssignLastBufferPosition: usize,
    pub EvtAcxStreamAudioEngineAssignLoopbackProtection: usize,
    pub EvtAcxStreamAudioEngineProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_STREAMAUDIOENGINE_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub VolumeElement: u32,
    pub MuteElement: u32,
    pub PeakMeterElement: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_KEYWORDSPOTTER_CALLBACKS {
    pub Size: u32,
    pub EvtAcxKeywordSpotterRetrieveArm: usize,
    pub EvtAcxKeywordSpotterAssignArm: usize,
    pub EvtAcxKeywordSpotterAssignPatterns: usize,
    pub EvtAcxKeywordSpotterAssignReset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_KEYWORDSPOTTER_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub Pattern: usize,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_AUDIOMODULE_CALLBACKS {
    pub Size: u32,
    pub EvtAcxAudioModuleProcessCommand: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_AUDIOMODULE_DESCRIPTOR {
    pub ClassId: u32,
    pub InstanceId: u32,
    pub VersionMajor: u32,
    pub VersionMinor: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_AUDIOMODULE_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Name: usize,
    pub Flags: u32,
    pub Descriptor: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_EVENT_CALLBACKS {
    pub Size: u32,
    pub EvtAcxEventEnable: usize,
    pub EvtAcxEventDisable: usize,
    pub EvtAcxEventProcessRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_EVENT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Set: usize,
    pub Id: u32,
    pub Type: u32,
    pub Callbacks: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PNPEVENT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Event: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_CIRCUIT_TEMPLATE_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub ContainerId: usize,
    pub FactoryProperties: u32,
    pub FactoryId: usize,
    pub FactoryUri: usize,
    pub FactoryName: usize,
    pub CircuitProperties: u32,
    pub CircuitId: usize,
    pub CircuitUri: usize,
    pub CircuitName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_COMPOSITE_TEMPLATE_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Properties: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_OBJECTBAG_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub ParentKey: u32,
    pub Name: usize,
    pub Handle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_JACK_SINK_INFORMATION {
    pub ConnType: u32,
    pub ManufacturerId: u16,
    pub ProductId: u16,
    pub AudioLatency: u16,
    pub HDCPCapable: u8,
    pub AICapable: u8,
    pub SinkDescriptionLength: u8,
    pub SinkDescription: [u32; 1],
    pub PortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PIN_CALLBACKS {
    pub Size: u32,
    pub EvtAcxPinSetDataFormat: usize,
    pub EvtAcxPinRetrieveName: usize,
    pub EvtAcxPinRetrieveJackSinkInfo: usize,
    pub EvtAcxPinProcessJackRequest: usize,
    pub EvtAcxPinConnected: usize,
    pub EvtAcxPinDisconnected: usize,
    pub EvtAcxPinDataFormatChangeNotification: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_INTERLEAVED_AUDIO_FORMAT_INFORMATION {
    pub Size: u32,
    pub PrimaryChannelCount: u32,
    pub PrimaryChannelStartPosition: u32,
    pub PrimaryChannelMask: u32,
    pub InterleavedChannelCount: u32,
    pub InterleavedChannelStartPosition: u32,
    pub InterleavedChannelMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MICROPHONE_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub MicrophoneSnrDB: i32,
    pub MicrophoneSensitivityDB: i32,
    pub MicArrayGeometry: u32,
    pub InterleavedFormat: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PIN_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Id: u32,
    pub Type: u32,
    pub Communication: u32,
    pub Qualifier: u32,
    pub Category: usize,
    pub Name: usize,
    pub PinCallbacks: usize,
    pub PropertiesCount: u32,
    pub MethodsCount: u32,
    pub Properties: usize,
    pub Methods: usize,
    pub MicrophoneConfig: usize,
    pub MaxStreams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_CONNECTION {
    pub Size: u32,
    pub Flags: u32,
    pub FromObject: u32,
    pub Object: u32,
    pub Id: u32,
    pub ToObject: u32,
    pub Object_1: u32,
    pub Id_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PHYSICAL_CONNECTION {
    pub Size: u32,
    pub Flags: u32,
    pub Object: u32,
    pub Id: u32,
    pub TargetName: u32,
    pub TargetPinId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_JACK_DESCRIPTION {
    pub ChannelMapping: u32,
    pub Color: u32,
    pub ConnectionType: u32,
    pub GeoLocation: u32,
    pub GenLocation: u32,
    pub PortConnection: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_JACK_CALLBACKS {
    pub Size: u32,
    pub EvtAcxJackRetrievePresenceState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_JACK_CONFIG {
    pub Size: u32,
    pub Id: u32,
    pub Flags: u32,
    pub Callbacks: usize,
    pub Description: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MICROPHONE_COORDINATES {
    pub Type: u16,
    pub XCoord: i16,
    pub YCoord: i16,
    pub ZCoord: i16,
    pub VerticalAngle: i16,
    pub HorizontalAngle: i16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MIC_ARRAY_GEOMETRY {
    pub Version: u16,
    pub MicArrayType: u16,
    pub VerticalAngleBegin: i16,
    pub VerticalAngleEnd: i16,
    pub HorizontalAngleBegin: i16,
    pub HorizontalAngleEnd: i16,
    pub FrequencyBandLo: u32,
    pub FrequencyBandHi: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_MICARRAYGEOMETRY_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Geometry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_REQUEST_PARAMETERS {
    pub Size: u16,
    pub MajorFunction: u8,
    pub MinorFunction: u8,
    pub Type: u32,
    pub Set: u32,
    pub Id: u32,
    pub Verb: u32,
    pub ItemType: u32,
    pub ItemId: u32,
    pub Control: usize,
    pub ControlCb: u32,
    pub Value: usize,
    pub ValueCb: u32,
    pub Set_1: u32,
    pub Id_1: u32,
    pub Verb_1: u32,
    pub ItemType_1: u32,
    pub ItemId_1: u32,
    pub Args: usize,
    pub ArgsCb: u32,
    pub Result: usize,
    pub ResultCb: u32,
    pub Set_2: u32,
    pub Id_2: u32,
    pub Verb_2: u32,
    pub ItemType_2: u32,
    pub ItemId_2: u32,
    pub Data: usize,
    pub DataCb: u32,
    pub EventData: u32,
    pub Control_1: usize,
    pub ControlCb_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_PROPERTY_ITEM {
    pub Set: usize,
    pub Id: u32,
    pub Flags: u32,
    pub EvtAcxObjectProcessRequest: usize,
    pub Reserved: usize,
    pub ControlCb: u32,
    pub ValueCb: u32,
    pub ValueType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_METHOD_ITEM {
    pub Set: usize,
    pub Id: u32,
    pub Flags: u32,
    pub EvtAcxObjectProcessRequest: usize,
    pub Reserved: usize,
    pub ArgsCb: u32,
    pub ResultCb: u32,
    pub ResultType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_EVENT_ITEM {
    pub Set: usize,
    pub Id: u32,
    pub Flags: u32,
    pub EvtAcxObjectProcessRequest: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_RTPACKET {
    pub Size: u32,
    pub RtPacketBuffer: u32,
    pub RtPacketOffset: u32,
    pub RtPacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACXDRMFORWARD {
    pub Flags: u32,
    pub DeviceObject: usize,
    pub FileObject: usize,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACXDRMRIGHTS {
    pub CopyProtect: u32,
    pub Reserved: u32,
    pub DigitalOutputDisable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_STREAM_CALLBACKS {
    pub Size: u32,
    pub EvtAcxStreamPrepareHardware: usize,
    pub EvtAcxStreamReleaseHardware: usize,
    pub EvtAcxStreamRun: usize,
    pub EvtAcxStreamPause: usize,
    pub EvtAcxStreamAssignDrmContentId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_RT_STREAM_CALLBACKS {
    pub Size: u32,
    pub EvtAcxStreamGetHwLatency: usize,
    pub EvtAcxStreamAllocateRtPackets: usize,
    pub EvtAcxStreamFreeRtPackets: usize,
    pub EvtAcxStreamSetRenderPacket: usize,
    pub EvtAcxStreamGetCurrentPacket: usize,
    pub EvtAcxStreamGetCapturePacket: usize,
    pub EvtAcxStreamGetPresentationPosition: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_STREAM_BRIDGE_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub Type: u32,
    pub InModesCount: u32,
    pub InModes: usize,
    pub OutMode: usize,
    pub OutStreamVarArguments: u32,
    pub TargetStreamAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_TARGET_CIRCUIT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub SymbolicLinkName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_TARGET_STREAM_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub TargetCircuit: u32,
    pub PinId: u32,
    pub DataFormat: u32,
    pub SignalProcessingMode: usize,
    pub OptionalParameters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACX_TARGET_FACTORY_CIRCUIT_CONFIG {
    pub Size: u32,
    pub Flags: u32,
    pub IoTarget: u32,
    pub SymbolicLinkName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n64x2 {
    pub val: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n64x3 {
    pub val: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n64x4 {
    pub val: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n128x2 {
    pub val: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n128x3 {
    pub val: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct __n128x4 {
    pub val: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _CrtMemState {
    pub pBlockHeader: usize,
    pub lCounts: [u32; 1],
    pub lSizes: [u32; 1],
    pub lHighWaterCount: u32,
    pub lTotalCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct diskfree_t {
    pub total_clusters: u32,
    pub avail_clusters: u32,
    pub sectors_per_cluster: u32,
    pub bytes_per_sector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddata32_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddata32i64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddata64i32_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddata64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddata_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct finddatai64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddata32_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddata32i64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddata64i32_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddata64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddata_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: u32,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct wfinddatai64_t {
    pub attrib: u32,
    pub time_create: u32,
    pub time_access: u32,
    pub time_write: u32,
    pub size: i64,
    pub name: [u32; 260],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _CRT_LIST_ENTRY {
    pub field_Flink: u32,
    pub field_Blink: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _CRT_CRITICAL_SECTION_DEBUG {
    pub Type: u32,
    pub CreatorBackTraceIndex: u32,
    pub field_CriticalSection: u32,
    pub ProcessLocksList: u32,
    pub EntryCount: u32,
    pub ContentionCount: u32,
    pub Depth: u32,
    pub OwnerBackTrace: [usize; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _CRT_CRITICAL_SECTION {
    pub DebugInfo: u32,
    pub LockCount: u32,
    pub RecursionCount: u32,
    pub OwningThread: usize,
    pub LockSemaphore: usize,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _HEAPINFO {
    pub field_pentry: usize,
    pub field_size: u32,
    pub field_useflag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct exception {
    pub field_type: u32,
    pub field_name: u32,
    pub arg1: u32,
    pub arg2: u32,
    pub retval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct complex {
    pub x: u32,
    pub y: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct iobuf {
    pub field_reserved1: u32,
    pub field_reserved2: u32,
    pub field_reserved3: u32,
    pub field_reserved4: u32,
    pub field_reserved5: u32,
    pub field_reserved6: u32,
    pub field_reserved7: u32,
    pub field_reserved8: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _RTC_vardesc {
    pub addr: u32,
    pub size: u32,
    pub field_name: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _RTC_framedesc {
    pub varCount: u32,
    pub field_variables: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _RTC_ALLOCA_NODE {
    pub guard1: i32,
    pub field_next: u32,
    pub dummypad: i32,
    pub allocaSize: u32,
    pub dummypad2: i32,
    pub guard2: [i32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _JUMP_BUFFER {
    pub Frame: i64,
    pub Reserved: i64,
    pub X19: i64,
    pub X20: i64,
    pub X21: i64,
    pub X22: i64,
    pub X23: i64,
    pub X24: i64,
    pub X25: i64,
    pub X26: i64,
    pub X27: i64,
    pub X28: i64,
    pub Fp: i64,
    pub Lr: i64,
    pub Sp: i64,
    pub Fpcr: i32,
    pub Fpsr: i32,
    pub D: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct div_t {
    pub quot: u32,
    pub rem: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ldiv_t {
    pub quot: u32,
    pub rem: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct type_info_node {
    pub field_memPtr: u32,
    pub next: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stat32 {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: u32,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stat32i64 {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: i64,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stat64i32 {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: u32,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stat64 {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: i64,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stat {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: u32,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct stati64 {
    pub st_dev: u32,
    pub st_ino: u32,
    pub st_mode: u32,
    pub st_nlink: u32,
    pub st_uid: u32,
    pub st_gid: u32,
    pub st_rdev: u32,
    pub st_size: i64,
    pub st_atime: u32,
    pub st_mtime: u32,
    pub st_ctime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct timeb {
    pub time: u32,
    pub millitm: u32,
    pub timezone: u32,
    pub dstflag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct timeb32 {
    pub time: u32,
    pub millitm: u32,
    pub timezone: u32,
    pub dstflag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct timeb64 {
    pub time: u32,
    pub millitm: u32,
    pub timezone: u32,
    pub dstflag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct utimbuf {
    pub actime: u32,
    pub modtime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct utimbuf32 {
    pub actime: u32,
    pub modtime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct utimbuf64 {
    pub actime: u32,
    pub modtime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDSPICX_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDSPICX_REPORT {
    pub ReportType: u8,
    pub ReportContentLength: u16,
    pub ReportId: u8,
    pub ReportContent: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDSPICX_DEVICE_CONFIG {
    pub Size: u32,
    pub EvtResetDevice: usize,
    pub EvtNotifyPowerDown: usize,
    pub InputReportQueue: u32,
    pub OutputReportQueue: u32,
    pub NumberOfInputReportRequestsToPend: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MBB_DEVICE_CONFIG {
    pub Size: u32,
    pub SendMbimFragment: usize,
    pub ReceiveMbimFragment: usize,
    pub SendDeviceServiceSessionData: usize,
    pub CreateAdapter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MBB_DEVICE_MBIM_PARAMETERS {
    pub Size: u32,
    pub Version: u32,
    pub ExtendedVersion: u32,
    pub MaximumFragmentSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MBB_DEVICE_WAKE_CAPABILITIES {
    pub Size: u32,
    pub RegisterState: u8,
    pub SmsReceive: u8,
    pub UssdReceive: u8,
    pub PacketState: u8,
    pub UiccChange: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MBB_DRIVER_GLOBALS {
    pub Unused: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CONFIGURATION_PARAMETER {
    pub ParameterType: u32,
    pub IntegerData: u32,
    pub StringData: u32,
    pub BinaryData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_ENCAPSULATION {
    pub Header: u32,
    pub Enabled: u32,
    pub EncapsulationType: u32,
    pub HeaderSize: u32,
    pub Enabled_1: u32,
    pub EncapsulationType_1: u32,
    pub HeaderSize_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_BDF {
    pub SegmentNumber: u32,
    pub BusNumber: u32,
    pub DeviceNumber: u32,
    pub FunctionNumber: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_PF_ENUM_ELEMENT {
    pub Header: u32,
    pub PfNumber: u32,
    pub PfState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_ENUMERATE_PFS {
    pub Header: u32,
    pub ElementSize: u32,
    pub NumberOfElements: u32,
    pub OffsetToFirstElement: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_ADD_PF {
    pub Header: u32,
    pub AddedFunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_REMOVE_PF {
    pub Header: u32,
    pub Bdf: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_KDNET_QUERY_PF_INFORMATION {
    pub Header: u32,
    pub Bdf: u32,
    pub NetworkAdddress: [u8; 6],
    pub UsageTag: u32,
    pub MaximumNumberOfSupportedPfs: u32,
    pub DeviceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_GENERAL_ATTRIBUTES {
    pub Header: u32,
    pub Flags: u32,
    pub MediaType: u32,
    pub PhysicalMediumType: u32,
    pub MtuSize: u32,
    pub MaxXmitLinkSpeed: u32,
    pub XmitLinkSpeed: u32,
    pub MaxRcvLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub LookaheadSize: u32,
    pub PowerManagementCapabilities: usize,
    pub MacOptions: u32,
    pub SupportedPacketFilters: u32,
    pub MaxMulticastListSize: u32,
    pub MacAddressLength: u16,
    pub PermanentMacAddress: [u8; 1],
    pub CurrentMacAddress: [u8; 1],
    pub RecvScaleCapabilities: usize,
    pub AccessType: u32,
    pub DirectionType: u32,
    pub ConnectionType: u32,
    pub IfType: u32,
    pub IfConnectorPresent: u8,
    pub SupportedStatistics: u32,
    pub SupportedPauseFunctions: u32,
    pub DataBackFillSize: u32,
    pub ContextBackFillSize: u32,
    pub SupportedOidList: usize,
    pub SupportedOidListLength: u32,
    pub AutoNegotiationFlags: u32,
    pub PowerManagementCapabilitiesEx: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MONITOR_CONFIG {
    pub Header: u32,
    pub RxDropReportEnable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_DATA {
    pub field_Next: u32,
    pub field_CurrentMdl: u32,
    pub CurrentMdlOffset: u32,
    pub NbDataLength: u32,
    pub field_MdlChain: u32,
    pub DataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_SHARED_MEMORY {
    pub field_NextSharedMemorySegment: u32,
    pub SharedMemoryFlags: u32,
    pub SharedMemoryHandle: u32,
    pub SharedMemoryOffset: u32,
    pub SharedMemoryLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER {
    pub field_Next: u32,
    pub field_CurrentMdl: u32,
    pub CurrentMdlOffset: u32,
    pub DataLength: u32,
    pub stDataLength: usize,
    pub field_MdlChain: u32,
    pub DataOffset: u32,
    pub Link: u32,
    pub NetBufferHeader: u32,
    pub ChecksumBias: u16,
    pub Reserved: u16,
    pub NdisPoolHandle: u32,
    pub NdisReserved: [u32; 2],
    pub ProtocolReserved: [u32; 6],
    pub MiniportReserved: [u32; 4],
    pub DataPhysicalAddress: u64,
    pub field_SharedMemoryInfo: u32,
    pub field_ScatterGatherList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_LIST_CONTEXT {
    pub field_Next: u32,
    pub Size: u16,
    pub Offset: u16,
    pub ContextData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_LIST_DATA {
    pub field_Next: u32,
    pub field_FirstNetBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_LIST {
    pub field_Next: u32,
    pub field_FirstNetBuffer: u32,
    pub Link: u32,
    pub NetBufferListHeader: u32,
    pub field_Context: u32,
    pub field_ParentNetBufferList: u32,
    pub NdisPoolHandle: u32,
    pub NdisReserved: [u32; 2],
    pub ProtocolReserved: [u32; 4],
    pub MiniportReserved: [u32; 2],
    pub Scratch: usize,
    pub SourceHandle: u32,
    pub NblFlags: u32,
    pub ChildRefCount: i32,
    pub Flags: u32,
    pub Status: u32,
    pub NdisReserved2: u32,
    pub NetBufferListInfo: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NET_BUFFER_LIST_8021Q_INFO {
    pub UserPriority: u32,
    pub CanonicalFormatId: u32,
    pub VlanId: u32,
    pub Reserved: u32,
    pub UserPriority_1: u32,
    pub CanonicalFormatId_1: u32,
    pub VlanId_1: u32,
    pub WMMInfo: u32,
    pub Reserved_1: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_LIST_POOL_PARAMETERS {
    pub NDIS_OBJECT_TYPE_DEFAULT: usize,
    pub NET_BUFFER_LIST_POOL_PARAMETERS_REVISION_2: usize,
    pub sizeof_Parameters_: usize,
    pub Header: u32,
    pub ProtocolId: u8,
    pub fAllocateNetBuffer: u8,
    pub ContextSize: u16,
    pub PoolTag: u32,
    pub DataSize: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_POOL_PARAMETERS {
    pub NDIS_OBJECT_TYPE_DEFAULT: usize,
    pub NET_BUFFER_POOL_PARAMETERS_REVISION_2: usize,
    pub sizeof_Parameters_: usize,
    pub Header: u32,
    pub PoolTag: u32,
    pub DataSize: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_IP_CHECKSUM_NET_BUFFER_LIST_INFO {
    pub IsIPv4: u32,
    pub IsIPv6: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
    pub IpHeaderChecksum: u32,
    pub Reserved: u32,
    pub TcpHeaderOffset: u32,
    pub TcpChecksumFailed: u32,
    pub UdpChecksumFailed: u32,
    pub IpChecksumFailed: u32,
    pub TcpChecksumSucceeded: u32,
    pub UdpChecksumSucceeded: u32,
    pub IpChecksumSucceeded: u32,
    pub Loopback: u32,
    pub TcpChecksumValueInvalid: u32,
    pub IpChecksumValueInvalid: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_LARGE_SEND_OFFLOAD_NET_BUFFER_LIST_INFO {
    pub Unused: u32,
    pub Type: u32,
    pub Reserved2: u32,
    pub MSS: u32,
    pub TcpHeaderOffset: u32,
    pub Type_1: u32,
    pub Reserved2_1: u32,
    pub TcpPayload: u32,
    pub Type_2: u32,
    pub Reserved2_2: u32,
    pub MSS_1: u32,
    pub TcpHeaderOffset_1: u32,
    pub Type_3: u32,
    pub IPVersion: u32,
    pub Reserved: u32,
    pub Type_4: u32,
    pub Reserved2_3: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_BUFFER_LIST_TIMESTAMP {
    pub Timestamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_UDP_RSC_OFFLOAD_NET_BUFFER_LIST_INFO {
    pub SegCount: u16,
    pub SegSize: u16,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_UDP_RSC_OFFLOAD {
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_UDP_SEGMENTATION_OFFLOAD_NET_BUFFER_LIST_INFO {
    pub MSS: u32,
    pub UdpHeaderOffset: u32,
    pub Reserved: u32,
    pub IPVersion: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OID_REQUEST {
    pub NDIS_OBJECT_TYPE_OID_REQUEST: u32,
    pub NDIS_OID_REQUEST_REVISION_2: u32,
    pub sizeof_Request_: u32,
    pub Header: u32,
    pub RequestType: u32,
    pub PortNumber: u32,
    pub Timeout: u32,
    pub RequestId: usize,
    pub RequestHandle: u32,
    pub Oid: u32,
    pub Oid_1: u32,
    pub InformationBuffer: usize,
    pub InformationBufferLength: u32,
    pub BytesWritten: u32,
    pub BytesNeeded: u32,
    pub Oid_2: u32,
    pub InformationBuffer_1: usize,
    pub InformationBufferLength_1: u32,
    pub BytesRead: u32,
    pub BytesNeeded_1: u32,
    pub Oid_3: u32,
    pub InformationBuffer_2: usize,
    pub InputBufferLength: u32,
    pub OutputBufferLength: u32,
    pub MethodId: u32,
    pub BytesWritten_1: u32,
    pub BytesRead_1: u32,
    pub BytesNeeded_2: u32,
    pub NdisReserved: [u8; 1],
    pub MiniportReserved: [u8; 1],
    pub SourceReserved: [u8; 1],
    pub SupportedRevision: u8,
    pub Reserved1: u8,
    pub Reserved2: u16,
    pub SwitchId: u32,
    pub VPortId: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct METHOD_INFORMATION {
    pub Oid: u32,
    pub InformationBuffer: usize,
    pub InputBufferLength: u32,
    pub OutputBufferLength: u32,
    pub MethodId: u32,
    pub BytesWritten: u32,
    pub BytesRead: u32,
    pub BytesNeeded: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_POLL_TRANSMIT_DATA {
    pub MaxNblsToComplete: u32,
    pub Reserved1: [u32; 3],
    pub field_CompletedNblChain: u32,
    pub NumberOfCompletedNbls: u32,
    pub NumberOfRemainingNbls: u32,
    pub SendCompleteFlags: u32,
    pub Reserved2: u32,
    pub Reserved3: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_POLL_RECEIVE_DATA {
    pub MaxNblsToIndicate: u32,
    pub Reserved1: [u32; 3],
    pub field_IndicatedNblChain: u32,
    pub NumberOfIndicatedNbls: u32,
    pub NumberOfRemainingNbls: u32,
    pub Flags: u32,
    pub Reserved2: u32,
    pub Reserved3: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_POLL_DATA {
    pub Header: u32,
    pub Transmit: u32,
    pub Receive: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_POLL_NOTIFICATION {
    pub Header: u32,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_POLL_CHARACTERISTICS {
    pub Header: u32,
    pub SetPollNotificationHandler: u32,
    pub PollHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_MINIPORT_ADAPTER_NATIVE_802_11_ATTRIBUTES {
    pub Header: u32,
    pub OpModeCapability: u32,
    pub NumOfTXBuffers: u32,
    pub NumOfRXBuffers: u32,
    pub MultiDomainCapabilityImplemented: u8,
    pub NumSupportedPhys: u32,
    pub SupportedPhyAttributes: usize,
    pub ExtSTAAttributes: usize,
    pub VWiFiAttributes: usize,
    pub ExtAPAttributes: usize,
    pub WFDAttributes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_LINK_LAYER_CAPABILITIES {
    pub Size: u32,
    pub MaxTxLinkSpeed: u32,
    pub MaxRxLinkSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_POWER_OFFLOAD_ARP_CAPABILITIES {
    pub Size: u32,
    pub ArpOffload: u8,
    pub MaximumOffloadCount: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_POWER_OFFLOAD_NS_CAPABILITIES {
    pub Size: u32,
    pub NSOffload: u8,
    pub MaximumOffloadCount: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_BITMAP_CAPABILITIES {
    pub Size: u32,
    pub BitmapPattern: u8,
    pub MaximumPatternCount: usize,
    pub MaximumPatternSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_MEDIA_CHANGE_CAPABILITIES {
    pub Size: u32,
    pub MediaConnect: u8,
    pub MediaDisconnect: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_MAGIC_PACKET_CAPABILITIES {
    pub Size: u32,
    pub MagicPacket: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_PACKET_FILTER_CAPABILITIES {
    pub Size: u32,
    pub PacketFilterMatch: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_DMA_CAPABILITIES {
    pub Size: u32,
    pub DmaEnabler: u32,
    pub MaximumPhysicalAddress: u64,
    pub CacheEnabled: u32,
    pub PreferredNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RX_CAPABILITIES {
    pub Size: u32,
    pub AllocationMode: u32,
    pub AttachmentMode: u32,
    pub FragmentRingNumberOfElementsHint: u32,
    pub MaximumFrameSize: usize,
    pub MaximumNumberOfQueues: usize,
    pub EvtAdapterReturnRxBuffer: usize,
    pub MappingRequirement: u32,
    pub FragmentBufferAlignment: usize,
    pub DmaCapabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_TX_CAPABILITIES {
    pub Size: u32,
    pub MappingRequirement: u32,
    pub PayloadBackfill: usize,
    pub MaximumNumberOfFragments: usize,
    pub FragmentBufferAlignment: usize,
    pub FragmentRingNumberOfElementsHint: u32,
    pub MaximumNumberOfQueues: usize,
    pub DmaCapabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_LINK_STATE {
    pub Size: u32,
    pub TxLinkSpeed: u32,
    pub RxLinkSpeed: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub SupportedPauseFunctions: u32,
    pub AutoNegotiationFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_DATAPATH_CALLBACKS {
    pub Size: u32,
    pub EvtAdapterCreateTxQueue: usize,
    pub EvtAdapterCreateRxQueue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_CHECKSUM_CAPABILITIES {
    pub Size: u32,
    pub IPv4: u8,
    pub Tcp: u8,
    pub Udp: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_REASON_PACKET {
    pub Size: u32,
    pub PatternId: u32,
    pub OriginalPacketSize: u32,
    pub WakePacket: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DRIVER_GLOBALS {
    pub Unused: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_EXTENSION_QUERY {
    pub Size: u32,
    pub Name: usize,
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DEVICE_POWER_POLICY_EVENT_CALLBACKS {
    pub Size: u32,
    pub EvtDevicePreviewBitmapPattern: usize,
    pub EvtDevicePreviewArpOffload: usize,
    pub EvtDevicePreviewNSOffload: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_QUEUE_CONFIG {
    pub Size: u32,
    pub EvtStart: usize,
    pub EvtStop: usize,
    pub EvtAdvance: usize,
    pub EvtSetNotificationEnabled: usize,
    pub EvtCancel: usize,
    pub ExecutionContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_POWER_OFFLOAD_ARP_PARAMETERS {
    pub Size: u32,
    pub Id: u32,
    pub RemoteIPv4Address: u32,
    pub HostIPv4Address: u32,
    pub LinkLayerAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_POWER_OFFLOAD_NS_PARAMETERS {
    pub Size: u32,
    pub Id: u32,
    pub RemoteIPv6Address: u32,
    pub SolicitedNodeIPv6Address: u32,
    pub TargetIPv6Addresses: [u32; 2],
    pub LinkLayerAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_POWER_OFFLOAD_LIST {
    pub Size: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RECEIVE_SCALING_HASH_SECRET_KEY {
    pub Key: usize,
    pub Length: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RECEIVE_SCALING_INDIRECTION_ENTRY {
    pub PacketQueue: u32,
    pub Status: u32,
    pub Index: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RECEIVE_SCALING_INDIRECTION_ENTRIES {
    pub Entries: usize,
    pub Length: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RECEIVE_SCALING_CAPABILITIES {
    pub Size: u32,
    pub NumberOfQueues: usize,
    pub IndirectionTableSize: usize,
    pub UnhashedTargetIndex: usize,
    pub UnhashedTargetType: u32,
    pub ReceiveScalingHashTypes: u32,
    pub ReceiveScalingProtocolTypes: u32,
    pub Outer: u32,
    pub Inner: u32,
    pub SynchronizeSetIndirectionEntries: u8,
    pub EvtAdapterReceiveScalingEnable: usize,
    pub EvtAdapterReceiveScalingDisable: usize,
    pub EvtAdapterReceiveScalingSetHashSecretKey: usize,
    pub EvtAdapterReceiveScalingSetIndirectionEntries: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ReceiveScalingEncapsulationTypes {
    pub Outer: u32,
    pub Inner: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_RXQUEUE_BUFFER_LAYOUT_HINT {
    pub MinimumBackfillSize: u32,
    pub L3HeaderAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_WAKE_SOURCE_BITMAP_PARAMETERS {
    pub Size: u32,
    pub Id: u32,
    pub Pattern: usize,
    pub PatternSize: usize,
    pub Mask: usize,
    pub MaskSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_WAKE_SOURCE_MEDIA_CHANGE_PARAMETERS {
    pub Size: u32,
    pub MediaConnect: u8,
    pub MediaDisconnect: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_WAKE_SOURCE_LIST {
    pub Size: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_LINK_LAYER_ADDRESS {
    pub Length: u32,
    pub Address: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_WAKE_EAPOL_PACKET_CAPABILITIES {
    pub Size: u32,
    pub EapolPacket: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_RECEIVE_FILTER_CAPABILITIES {
    pub Size: u32,
    pub SupportedPacketFilters: u32,
    pub MaximumMulticastAddresses: usize,
    pub EvtSetReceiveFilter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_TX_CHECKSUM_CAPABILITIES {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_RX_CHECKSUM_CAPABILITIES {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_GSO_CAPABILITIES {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_RSC_CAPABILITIES {
    pub Size: u32,
    pub TcpTimestampOption: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_OFFLOAD_IEEE8021Q_TAG_CAPABILITIES {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_ADAPTER_TX_DEMUX {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_DEVICE_RESET_CAPABILITIES {
    pub Size: u32,
    pub ResetDiagnosticsGuid: u32,
    pub EvtNetDeviceCollectResetDiagnostics: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_EXECUTION_CONTEXT_CONFIG {
    pub Size: u32,
    pub EvtPreAdvance: usize,
    pub EvtPostAdvance: usize,
    pub EvtSetNotificationEnabled: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_EXECUTION_CONTEXT_TASK_CONFIG {
    pub Size: u32,
    pub EvtTask: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX_ACTIVITY {
    pub Size: u32,
    pub Transmitting: u8,
    pub Receiving: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX_CONFIG {
    pub Size: u32,
    pub PowerManaged: u32,
    pub EvtSerCxFileOpen: usize,
    pub EvtSerCxFileClose: usize,
    pub EvtSerCxFileCleanup: usize,
    pub EvtSerCxTransmit: usize,
    pub EvtSerCxReceive: usize,
    pub EvtSerCxWaitmask: usize,
    pub EvtSerCxPurge: usize,
    pub EvtSerCxControl: usize,
    pub EvtSerCxApplyConfig: usize,
    pub EvtSerCxTransmitCancel: usize,
    pub EvtSerCxReceiveCancel: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_CONFIG {
    pub Size: u32,
    pub EvtSerCx2FileOpen: usize,
    pub EvtSerCx2FileClose: usize,
    pub EvtSerCx2SetWaitMask: usize,
    pub EvtSerCx2PurgeFifos: usize,
    pub EvtSerCx2Control: usize,
    pub EvtSerCx2ApplyConfig: usize,
    pub EvtSerCx2SelectNextReceiveTransactionType: usize,
    pub EvtSerCx2SelectNextTransmitTransactionType: usize,
    pub RequestAttributes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_SYSTEM_DMA_TRANSMIT_CONFIG {
    pub Size: u32,
    pub MaximumTransferLength: u32,
    pub MinimumTransactionLength: u32,
    pub DmaAlignment: u32,
    pub MaximumScatterGatherFragments: u32,
    pub DmaWidth: u32,
    pub DeviceAddress: u64,
    pub DmaDescriptor: usize,
    pub MinimumTransferUnitOverride: u32,
    pub Exclusive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_SYSTEM_DMA_RECEIVE_CONFIG {
    pub Size: u32,
    pub MaximumTransferLength: u32,
    pub MinimumTransactionLength: u32,
    pub DmaAlignment: u32,
    pub MaximumScatterGatherFragments: u32,
    pub DmaWidth: u32,
    pub DeviceAddress: u64,
    pub DmaDescriptor: usize,
    pub MinimumTransferUnitOverride: u32,
    pub Exclusive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_PIO_TRANSMIT_CONFIG {
    pub Size: u32,
    pub EvtSerCx2PioTransmitInitializeTransaction: usize,
    pub EvtSerCx2PioTransmitCleanupTransaction: usize,
    pub EvtSerCx2PioTransmitWriteBuffer: usize,
    pub EvtSerCx2PioTransmitEnableReadyNotification: usize,
    pub EvtSerCx2PioTransmitCancelReadyNotification: usize,
    pub EvtSerCx2PioTransmitDrainFifo: usize,
    pub EvtSerCx2PioTransmitCancelDrainFifo: usize,
    pub EvtSerCx2PioTransmitPurgeFifo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_PIO_RECEIVE_CONFIG {
    pub Size: u32,
    pub EvtSerCx2PioReceiveInitializeTransaction: usize,
    pub EvtSerCx2PioReceiveCleanupTransaction: usize,
    pub EvtSerCx2PioReceiveReadBuffer: usize,
    pub EvtSerCx2PioReceiveEnableReadyNotification: usize,
    pub EvtSerCx2PioReceiveCancelReadyNotification: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_CUSTOM_RECEIVE_CONFIG {
    pub Size: u32,
    pub Alignment: u32,
    pub MinimumTransactionLength: u32,
    pub MaximumTransactionLength: u32,
    pub MinimumTransferUnit: u32,
    pub Exclusive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_CUSTOM_TRANSMIT_CONFIG {
    pub Size: u32,
    pub Alignment: u32,
    pub MinimumTransactionLength: u32,
    pub MaximumTransactionLength: u32,
    pub MinimumTransferUnit: u32,
    pub Exclusive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_CUSTOM_RECEIVE_TRANSACTION_CONFIG {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERCX2_CUSTOM_TRANSMIT_TRANSACTION_CONFIG {
    pub Size: u32,
    pub EvtSerCx2CustomTransmitTransactionInitialize: usize,
    pub EvtSerCx2CustomTransmitTransactionStart: usize,
    pub EvtSerCx2CustomTransmitTransactionCleanup: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPB_CONTROLLER_CONFIG {
    pub Size: u32,
    pub ControllerDispatchType: u32,
    pub PowerManaged: u32,
    pub EvtSpbTargetConnect: usize,
    pub EvtSpbTargetDisconnect: usize,
    pub EvtSpbControllerLock: usize,
    pub EvtSpbControllerUnlock: usize,
    pub EvtSpbIoRead: usize,
    pub EvtSpbIoWrite: usize,
    pub EvtSpbIoSequence: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPB_CONNECTION_PARAMETERS {
    pub Size: u16,
    pub ConnectionTag: usize,
    pub ConnectionParameters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SPB_REQUEST_PARAMETERS {
    pub Size: u16,
    pub Type: u32,
    pub Position: u32,
    pub PreviousTransferDirection: u32,
    pub Length: u32,
    pub SequenceTransferCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_MANAGER_CONFIG {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_CONNECTOR_TYPEC_CONFIG {
    pub Size: u32,
    pub IsSupported: u8,
    pub SupportedOperatingModes: u32,
    pub SupportedPowerSourcingCapabilities: u32,
    pub AudioAccessoryCapable: u8,
    pub EvtSetDataRole: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_CONNECTOR_PD_CONFIG {
    pub Size: u32,
    pub IsSupported: u8,
    pub SupportedPowerRoles: u32,
    pub EvtSetPowerRole: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_CONNECTOR_CONFIG {
    pub Size: u32,
    pub ConnectorId: u64,
    pub TypeCConfig: usize,
    pub PdConfig: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_CONNECTOR_TYPEC_ATTACH_PARAMS {
    pub Size: u32,
    pub Partner: u32,
    pub CurrentAdvertisement: u32,
    pub ChargingState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCM_CONNECTOR_PD_CONN_STATE_CHANGED_PARAMS {
    pub Size: u32,
    pub PdConnState: u32,
    pub Rdo: u32,
    pub ChargingState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_DEVICE_CONFIG {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_IDENTIFICATION {
    pub Size: u32,
    pub VendorId: u32,
    pub ProductId: u32,
    pub DeviceId: u32,
    pub TypeCRevisionInBcd: u32,
    pub PDRevisionAndVersionInBcd: u32,
    pub PDInterfaceRevisionAndVersionInBcd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_CAPABILITIES {
    pub Size: u32,
    pub IsPowerDeliveryCapable: u8,
    pub DeviceCapabilities1: u32,
    pub DeviceCapabilities2: u32,
    pub StandardInputCapabilities: u32,
    pub StandardOutputCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_CONFIG {
    pub Size: u32,
    pub Identification: usize,
    pub Capabilities: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_ALERT_DATA {
    pub Size: u32,
    pub AlertType: u32,
    pub CCStatus: u32,
    pub PowerStatus: u32,
    pub FaultStatus: u32,
    pub ReceiveBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_GET_STATUS_IN_PARAMS {
    pub PortControllerObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_GET_STATUS_OUT_PARAMS {
    pub CCStatus: u32,
    pub PowerStatus: u32,
    pub FaultStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_GET_CONTROL_IN_PARAMS {
    pub PortControllerObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_GET_CONTROL_OUT_PARAMS {
    pub TCPCControl: u32,
    pub RoleControl: u32,
    pub FaultControl: u32,
    pub PowerControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_CONTROL_IN_PARAMS {
    pub PortControllerObject: u32,
    pub ControlType: u32,
    pub TCPCControl: u32,
    pub RoleControl: u32,
    pub FaultControl: u32,
    pub PowerControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_TRANSMIT_IN_PARAMS {
    pub PortControllerObject: u32,
    pub Transmit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_TRANSMIT_BUFFER_IN_PARAMS {
    pub PortControllerObject: u32,
    pub TransmitBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_RECEIVE_DETECT_IN_PARAMS {
    pub PortControllerObject: u32,
    pub ReceiveDetect: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_CONFIG_STANDARD_OUTPUT_IN_PARAMS {
    pub PortControllerObject: u32,
    pub ConfigStandardOutput: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_COMMAND_IN_PARAMS {
    pub PortControllerObject: u32,
    pub Command: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_SET_MESSAGE_HEADER_INFO_IN_PARAMS {
    pub PortControllerObject: u32,
    pub MessageHeaderInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_ALTERNATE_MODE_ENTERED_IN_PARAMS {
    pub PortControllerObject: u32,
    pub SVID: u32,
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_ALTERNATE_MODE_EXITED_IN_PARAMS {
    pub PortControllerObject: u32,
    pub SVID: u32,
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_DISPLAYPORT_CONFIGURED_IN_PARAMS {
    pub PortControllerObject: u32,
    pub PinAssignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_DISPLAYPORT_HPD_STATUS_CHANGED_IN_PARAMS {
    pub PortControllerObject: u32,
    pub HPDStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_DISPLAYPORT_DISPLAY_OUT_STATUS_CHANGED_IN_PARAMS {
    pub PortControllerObject: u32,
    pub DisplayOutStatus: u32,
    pub PinAssignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_TRANSMIT_BUFFER {
    pub TransmitByteCount: u32,
    pub Header: [u32; 2],
    pub DataObjects: [u32; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMTCPCI_PORT_CONTROLLER_RECEIVE_BUFFER {
    pub ReceiveByteCount: u32,
    pub ReceivedFrameType: u32,
    pub Header: [u32; 2],
    pub DataObjects: [u32; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_DEVICE_CONFIG {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_CONNECTOR_INFO {
    pub Size: u32,
    pub ConnectorId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_PPM_CONFIG {
    pub Size: u32,
    pub UsbDeviceControllerEnabled: u8,
    pub ConnectorCollectionHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_PPM_SEND_UCSI_DATA_BLOCK_IN_PARAMS {
    pub PpmObject: u32,
    pub UcmUcsiDataBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_PPM_GET_UCSI_DATA_BLOCK_IN_PARAMS {
    pub PpmObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCMUCSI_PPM_GET_UCSI_DATA_BLOCK_OUT_PARAMS {
    pub UcmUcsiDataBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CAPABILITY_IN {
    pub AsUInt32: u32,
    pub DisabledStateSupport: u32,
    pub BatteryCharging: u32,
    pub UsbPowerDelivery: u32,
    pub UsbTypeCCurrent: u32,
    pub bmPowerSource: u32,
    pub bNumConnectors: u32,
    pub SetUomSupported: u32,
    pub SetPdmSupported: u32,
    pub AlternateModeDetailsAvailable: u32,
    pub AlternateModeOverrideSupported: u32,
    pub PdoDetailsAvailable: u32,
    pub CableDetailsAvailable: u32,
    pub ExternalSupplyNotificationSupported: u32,
    pub PdResetNotificationSupported: u32,
    pub OptionalFeatures: u32,
    pub bNumAltModes: u32,
    pub bcdBcVersion: u32,
    pub bcdPdVersion: u32,
    pub bcdUsbTypeCVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CONNECTOR_CAPABILITY_IN {
    pub AsUInt8: u32,
    pub DfpOnly: u32,
    pub UfpOnly: u32,
    pub Drp: u32,
    pub AudioAccessoryMode: u32,
    pub DebugAccessoryMode: u32,
    pub Usb2: u32,
    pub Usb3: u32,
    pub AlternateMode: u32,
    pub Provider: u32,
    pub Consumer: u32,
    pub SwapToDfp: u32,
    pub SwapToUfp: u32,
    pub SwapToSrc: u32,
    pub SwapToSnk: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_ALTERNATE_MODE {
    pub SVID: u32,
    pub ModeL: u32,
    pub ModeH: u32,
    pub Mode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_ALTERNATE_MODES_IN {
    pub AlternateModes: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CAM_SUPPORTED_IN {
    pub bmAlternateModeSupported: [u32; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CURRENT_CAM_IN {
    pub CurrentAlternateMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_PDOS_IN {
    pub Pdos: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CABLE_PROPERTY_IN {
    pub AsUInt16: u32,
    pub SpeedExponent: u32,
    pub Mantissa: u32,
    pub bCurrentCapability: u32,
    pub VBusInCable: u32,
    pub CableType: u32,
    pub Directionality: u32,
    pub PlugEndType: u32,
    pub ModeSupport: u32,
    pub Latency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_CONNECTOR_STATUS_IN {
    pub AsUInt16: u32,
    pub ExternalSupplyChange: u32,
    pub PowerOperationModeChange: u32,
    pub SupportedProviderCapabilitiesChange: u32,
    pub NegotiatedPowerLevelChange: u32,
    pub PdResetComplete: u32,
    pub SupportedCamChange: u32,
    pub BatteryChargingStatusChange: u32,
    pub ConnectorPartnerChange: u32,
    pub PowerDirectionChange: u32,
    pub ConnectChange: u32,
    pub Error: u32,
    pub PowerOperationMode: u32,
    pub ConnectStatus: u32,
    pub PowerDirection: u32,
    pub ConnectorPartnerFlags: u32,
    pub ConnectorPartnerType: u32,
    pub RequestDataObject: u32,
    pub BatteryChargingStatus: u32,
    pub PowerBudgetLimitedReason: u32,
    pub PowerBudgetLowered: u32,
    pub ReachingPowerBudgetLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_GET_ERROR_STATUS_IN {
    pub AsUInt16: u32,
    pub UnrecognizedCommandError: u32,
    pub NonExistentConnectorNumberError: u32,
    pub InvalidCommandParametersError: u32,
    pub IncompatibleConnectorPartnerError: u32,
    pub CcCommunicationError: u32,
    pub CommandFailureDueToDeadBattery: u32,
    pub ContractNegotiationFailure: u32,
    pub VendorDefined: [u32; 14],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCSI_DATA_BLOCK {
    pub UcsiVersion: u32,
    pub CCI: u32,
    pub Control: u32,
    pub MessageIn: u32,
    pub MessageOut: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_CONTROLLER_PCI_INFORMATION {
    pub VendorId: u32,
    pub DeviceId: u32,
    pub RevisionId: u16,
    pub BusNumber: u32,
    pub DeviceNumber: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_CONTROLLER_ACPI_INFORMATION {
    pub VendorId: [i8; 1],
    pub DeviceId: [i8; 1],
    pub RevisionId: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_CONTROLLER_CONFIG {
    pub Size: u32,
    pub NumberOfPresentedDeviceMgmtEvtCallbacks: u32,
    pub EvtControllerQueryUsbCapability: usize,
    pub Reserved1: usize,
    pub EvtControllerGetCurrentFrameNumber: usize,
    pub EvtControllerUsbDeviceAdd: usize,
    pub EvtControllerReset: usize,
    pub Reserved2: usize,
    pub Reserved3: usize,
    pub Reserved4: usize,
    pub ParentBusType: u32,
    pub PciDeviceInfo: u32,
    pub AcpiDeviceInfo: u32,
    pub DeviceDescription: [u8; 1],
    pub ManufacturerNameString: u32,
    pub ModelNameString: u32,
    pub ModelNumberString: u32,
    pub Reserved5: usize,
    pub Reserved6: usize,
    pub Reserved7: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_CONTROLLER_RESET_COMPLETE_INFO {
    pub Size: u32,
    pub UcxControllerState: u32,
    pub UcxCoordinated: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENDPOINTS_CONFIGURE_FAILURE_FLAGS {
    pub InsufficientBandwidth: u32,
    pub InsufficientHardwareResourcesForEndpoints: u32,
    pub MaxExitLatencyTooLarge: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENDPOINTS_CONFIGURE {
    pub Header: u32,
    pub EndpointsToEnableCount: u32,
    pub EndpointsToEnable: usize,
    pub EndpointsToDisableCount: u32,
    pub EndpointsToDisable: usize,
    pub EndpointsEnabledAndUnchangedCount: u32,
    pub EndpointsEnabledAndUnchanged: usize,
    pub FailureFlags: u32,
    pub ExitLatencyDelta: u32,
    pub ConfigurationValue: u8,
    pub InterfaceNumber: u8,
    pub AlternateSetting: u8,
    pub Reserved1: u32,
    pub Reserved2: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENDPOINT_RESET {
    pub Header: u32,
    pub Endpoint: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEFAULT_ENDPOINT_UPDATE {
    pub Header: u32,
    pub DefaultEndpoint: u32,
    pub MaxPacketSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_ENDPOINT_EVENT_CALLBACKS {
    pub Size: u32,
    pub EvtEndpointPurge: usize,
    pub EvtEndpointStart: usize,
    pub EvtEndpointAbort: usize,
    pub EvtEndpointReset: usize,
    pub EvtEndpointOkToCancelTransfers: usize,
    pub EvtEndpointStaticStreamsAdd: usize,
    pub EvtEndpointStaticStreamsEnable: usize,
    pub EvtEndpointStaticStreamsDisable: usize,
    pub Reserved1: usize,
    pub EvtEndpointSetCharacteristic: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_DEFAULT_ENDPOINT_EVENT_CALLBACKS {
    pub Size: u32,
    pub EvtEndpointPurge: usize,
    pub EvtEndpointStart: usize,
    pub EvtEndpointAbort: usize,
    pub EvtEndpointOkToCancelTransfers: usize,
    pub EvtDefaultEndpointUpdate: usize,
    pub Reserved1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_DRIVER_GLOBALS {
    pub Size: u32,
    pub WdfDriverGlobals: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_INFO {
    pub Size: u32,
    pub ControllerType: u32,
    pub NumberOf20Ports: u16,
    pub NumberOf30Ports: u16,
    pub MaxU1ExitLatency: u16,
    pub MaxU2ExitLatency: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_20PORT_INFO {
    pub PortNumber: u16,
    pub MinorRevision: u8,
    pub HubDepth: u8,
    pub Removable: u32,
    pub IntegratedHubImplemented: u32,
    pub DebugCapable: u32,
    pub ControllerUsb20HardwareLpmFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_20PORTS_INFO {
    pub Size: u32,
    pub NumberOfPorts: u16,
    pub PortInfoSize: u16,
    pub PortInfoArray: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_30PORT_INFO {
    pub PortNumber: u16,
    pub MinorRevision: u8,
    pub HubDepth: u8,
    pub Removable: u32,
    pub DebugCapable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_30PORT_INFO_EX {
    pub Info: u32,
    pub MaxSpeedsCount: u16,
    pub SpeedsCount: u16,
    pub Speeds: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ROOTHUB_30PORTS_INFO {
    pub Size: u32,
    pub NumberOfPorts: u16,
    pub PortInfoSize: u16,
    pub PortInfoArray: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HUB_INFO_FROM_PARENT {
    pub IoTarget: usize,
    pub DeviceDescriptor: u32,
    pub U1ExitLatency: u16,
    pub U2ExitLatency: u16,
    pub ExitLatencyOfSlowestLinkForU1: u16,
    pub DepthOfSlowestLinkForU1: u8,
    pub ExitLatencyOfSlowestLinkForU2: u16,
    pub DepthOfSlowestLinkForU2: u8,
    pub HostInitiatedU1ExitLatency: u16,
    pub HostInitiatedU2ExitLatency: u16,
    pub TotalHubDepth: u8,
    pub TotalTPPropogationDelay: u16,
    pub HubFlags: usize,
    pub SublinkSpeedAttr: usize,
    pub SublinkSpeedAttrCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_ROOTHUB_CONFIG {
    pub Size: u32,
    pub NumberOfPresentedControlUrbCallbacks: u32,
    pub EvtRootHubClearHubFeature: usize,
    pub EvtRootHubClearPortFeature: usize,
    pub EvtRootHubGetHubStatus: usize,
    pub EvtRootHubGetPortStatus: usize,
    pub EvtRootHubSetHubFeature: usize,
    pub EvtRootHubSetPortFeature: usize,
    pub EvtRootHubGetPortErrorCount: usize,
    pub EvtRootHubControlUrb: usize,
    pub EvtRootHubInterruptTx: usize,
    pub EvtRootHubGetInfo: usize,
    pub EvtRootHubGet20PortInfo: usize,
    pub EvtRootHubGet30PortInfo: usize,
    pub WdfRequestAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAM_INFO {
    pub Size: u32,
    pub WdfQueue: u32,
    pub StreamId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_MGMT_HEADER {
    pub Size: u32,
    pub Hub: u32,
    pub UsbDevice: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDRESS0_OWNERSHIP_ACQUIRE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_ENABLE_FAILURE_FLAGS {
    pub InsufficientHardwareResourcesForDefaultEndpoint: u32,
    pub InsufficientHardwareResourcesForDevice: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_ENABLE {
    pub Header: u32,
    pub DefaultEndpoint: u32,
    pub FailureFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_RESET {
    pub Header: u32,
    pub DefaultEndpoint: u32,
    pub EndpointsToDisableCount: u32,
    pub EndpointsToDisable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_PURGEIO {
    pub Header: u32,
    pub OnSuspend: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_ABORTIO {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_STARTIO {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_TREE_PURGEIO {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_ADDRESS {
    pub Header: u32,
    pub Reserved: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_UPDATE_FLAGS {
    pub UpdateDeviceDescriptor: u32,
    pub UpdateBosDescriptor: u32,
    pub UpdateMaxExitLatency: u32,
    pub UpdateIsHub: u32,
    pub UpdateAllowIoOnInvalidPipeHandles: u32,
    pub Update20HardwareLpmParameters: u32,
    pub UpdateRootPortResumeTime: u32,
    pub UpdateTunnelState: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_UPDATE_FAILURE_FLAGS {
    pub MaxExitLatencyTooLarge: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_UPDATE_20_HARDWARE_LPM_PARAMETERS {
    pub HardwareLpmEnable: u32,
    pub RemoteWakeEnable: u32,
    pub HostInitiatedResumeDurationMode: u32,
    pub BestEffortServiceLatency: u32,
    pub BestEffortServiceLatencyDeep: u32,
    pub L1Timeout: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_UPDATE {
    pub Header: u32,
    pub Flags: u32,
    pub DeviceDescriptor: usize,
    pub BosDescriptor: usize,
    pub MaxExitLatency: u32,
    pub IsHub: u8,
    pub FailureFlags: u32,
    pub Usb20HardwareLpmParameters: u32,
    pub RootPortResumeTime: u16,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_HUB_INFO {
    pub Header: u32,
    pub NumberOfPorts: u32,
    pub NumberOfTTs: u32,
    pub TTThinkTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBDEVICE_DISABLE {
    pub Header: u32,
    pub DefaultEndpoint: u32,
    pub UsbDeviceRecoveryAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_PORT_PATH {
    pub Size: u32,
    pub PortPathDepth: u32,
    pub TTHubDepth: u32,
    pub PortPath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCXUSBDEVICE_INFO {
    pub Size: u32,
    pub DeviceSpeed: u32,
    pub TtHub: u32,
    pub PortPath: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_USBDEVICE_EVENT_CALLBACKS {
    pub Size: u32,
    pub EvtUsbDeviceEndpointsConfigure: usize,
    pub EvtUsbDeviceEnable: usize,
    pub EvtUsbDeviceDisable: usize,
    pub EvtUsbDeviceReset: usize,
    pub EvtUsbDeviceAddress: usize,
    pub EvtUsbDeviceUpdate: usize,
    pub EvtUsbDeviceHubInfo: usize,
    pub EvtUsbDeviceDefaultEndpointAdd: usize,
    pub EvtUsbDeviceEndpointAdd: usize,
    pub EvtUsbDeviceSuspend: usize,
    pub EvtUsbDeviceResume: usize,
    pub EvtUsbDeviceGetCharacteristic: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_CONTROLLER_TRANSPORT_CHARACTERISTICS {
    pub TransportCharacteristicsFlags: u32,
    pub CurrentRoundtripLatencyInMilliSeconds: u32,
    pub MaxPotentialBandwidth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_ENDPOINT_ISOCH_TRANSFER_PATH_DELAYS {
    pub MaximumSendPathDelayInMilliSeconds: u32,
    pub MaximumCompletionPathDelayInMilliSeconds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_ENDPOINT_CHARACTERISTIC {
    pub Size: u32,
    pub CharacteristicType: u32,
    pub Priority: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_USBDEVICE_CHARACTERISTIC_PATH_DELAY {
    pub MaximumSendPathDelayInMilliSeconds: u32,
    pub MaximumCompletionPathDelayInMilliSeconds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UCX_USBDEVICE_CHARACTERISTIC {
    pub Size: u32,
    pub CharacteristicType: u32,
    pub PathDelay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_DRIVER_GLOBALS {
    pub Unused: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_USB_ENDPOINT_INIT_AND_METADATA {
    pub UdecxUsbEndpointInit: usize,
    pub EndpointDescriptorBufferLength: u32,
    pub EndpointDescriptor: usize,
    pub SuperSpeedEndpointCompanionDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_ENDPOINTS_CONFIGURE_PARAMS {
    pub Size: u32,
    pub ConfigureType: u32,
    pub NewConfigurationValue: u8,
    pub InterfaceNumber: u8,
    pub NewInterfaceSetting: u8,
    pub EndpointsToConfigureCount: u32,
    pub EndpointsToConfigure: usize,
    pub ReleasedEndpointsCount: u32,
    pub ReleasedEndpoints: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_USB_DEVICE_STATE_CHANGE_CALLBACKS {
    pub Size: u32,
    pub EvtUsbDeviceLinkPowerEntry: usize,
    pub EvtUsbDeviceLinkPowerExit: usize,
    pub EvtUsbDeviceSetFunctionSuspendAndWake: usize,
    pub EvtUsbDeviceReset: usize,
    pub EvtUsbDeviceDefaultEndpointAdd: usize,
    pub EvtUsbDeviceEndpointAdd: usize,
    pub EvtUsbDeviceEndpointsConfigure: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_USB_DEVICE_PLUG_IN_OPTIONS {
    pub Size: u32,
    pub Usb20PortNumber: u32,
    pub Usb30PortNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_USB_ENDPOINT_CALLBACKS {
    pub Size: u32,
    pub EvtUsbEndpointReset: usize,
    pub EvtUsbEndpointStart: usize,
    pub EvtUsbEndpointPurge: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UDECX_WDF_DEVICE_CONFIG {
    pub Size: u32,
    pub NumberOfUsb20Ports: u16,
    pub NumberOfUsb30Ports: u16,
    pub EvtUdecxWdfDeviceQueryUsbCapability: usize,
    pub ResetAction: u32,
    pub EvtUdecxWdfDeviceReset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_GLOBALS {
    pub Size: u32,
    pub WdfDriverGlobals: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_DEVICE_CAPABILITIES {
    pub Size: u32,
    pub MaxSpeed: u32,
    pub RemoteWakeSignalDelay: u32,
    pub PdcpSupported: u8,
    pub InEndpointBitmap: u16,
    pub OutEndpointBitmap: u16,
    pub SharesConnectors: u8,
    pub GroupId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_HARDWARE_FAILURE_CONTEXT {
    pub Size: u32,
    pub ExceptionCode: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_DEVICE_CALLBACKS {
    pub Size: u32,
    pub EvtDeviceHostConnect: usize,
    pub EvtDeviceHostDisconnect: usize,
    pub EvtDeviceAddressed: usize,
    pub EvtDeviceEndpointAdd: usize,
    pub EvtDeviceDefaultEndpointAdd: usize,
    pub EvtDeviceUsbStateChange: usize,
    pub EvtDevicePortChange: usize,
    pub EvtDevicePortDetect: usize,
    pub EvtDeviceRemoteWakeupSignal: usize,
    pub EvtDeviceControllerReset: usize,
    pub EvtDeviceTestModeSet: usize,
    pub EvtDeviceTestHook: usize,
    pub EvtDeviceSuperSpeedPowerFeature: usize,
    pub EvtDeviceProprietaryChargerDetect: usize,
    pub EvtDeviceProprietaryChargerSetProperty: usize,
    pub EvtDeviceProprietaryChargerReset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFX_ENDPOINT_CALLBACKS {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URS_CONFIG {
    pub Size: u32,
    pub HostInterfaceType: u32,
    pub EvtUrsFilterRemoveResourceRequirements: usize,
    pub EvtUrsSetRole: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URS_DRIVER_GLOBALS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_DEVICE_CONFIG {
    pub Size: u32,
    pub WdiVersion: u32,
    pub SendCommand: usize,
    pub CreateAdapter: usize,
    pub CreateWifiDirectDevice: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_DEVICE_CAPABILITIES {
    pub Size: u32,
    pub HardwareRadioState: u8,
    pub SoftwareRadioState: u8,
    pub FirmwareVersion: [u8; 1],
    pub ActionFramesSupported: u8,
    pub NumRxStreams: u32,
    pub NumTxStreams: u32,
    pub Support_eCSA: u8,
    pub MACAddressRandomization: u8,
    pub MACAddressRandomizationMask: u32,
    pub BluetoothCoexistenceSupport: u32,
    pub SupportsNonWdiOidRequests: u8,
    pub FastTransitionSupported: u8,
    pub MU_MIMOSupported: u8,
    pub BSSTransitionSupported: u8,
    pub SAEAuthenticationSupported: u8,
    pub MBOSupported: u8,
    pub BeaconReportsImplemented: u8,
    pub NumRadios: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_STATION_CAPABILITIES {
    pub Size: u32,
    pub ScanSSIDListSize: u32,
    pub DesiredSSIDListSize: u32,
    pub PrivacyExemptionListSize: u32,
    pub KeyMappingTableSize: u32,
    pub DefaultKeyTableSize: u32,
    pub WEPKeyValueMaxLength: u32,
    pub MaxNumPerSTA: u32,
    pub SupportedQOSFlags: u32,
    pub HostFIPSModeImplemented: u32,
    pub MFPCapable: u32,
    pub AutoPowerSaveMode: u8,
    pub BSSListCachemanagement: u8,
    pub ConnectBSSSelectionOverride: u8,
    pub MaxNetworkOffloadListSize: u32,
    pub HESSIDConnectionSupported: u8,
    pub FTMAsInitiatorSupport: u8,
    pub FTMNumberOfSupportedTargets: u32,
    pub HostWPA3FIPSModeEnabled_Deprecated: u8,
    pub NumSupportedUnicastAlgorithms: u32,
    pub UnicastAlgorithmsList: usize,
    pub NumSupportedMulticastDataAlgorithms: u32,
    pub MulticastDataAlgorithmsList: usize,
    pub NumSupportedMulticastMgmtAlgorithms: u32,
    pub MulticastMgmtAlgorithmsList: usize,
    pub NumSecondaryStaBandCombinations: u32,
    pub SecondaryStaBandsCombinations: usize,
    pub MaxMLOLinksSupported: u32,
    pub DoNotUseOsReserved: u8,
    pub MLOAddressesList: usize,
    pub NumAkmsSupported: u32,
    pub AkmsList: usize,
    pub NumFIPSCertifiedCipherAlgorithms: u32,
    pub FIPSCertifiedCipherAlgorithmsList: usize,
    pub MSCSSupported: u8,
    pub DSCPToUPMappingSupported: u8,
    pub MaxNumConfigurableActionFrameWakePatterns: u32,
    pub CrossAkmCipherRoamSupported: u8,
    pub IsWiFi7EnterpriseSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_WIFIDIRECT_CAPABILITIES {
    pub Size: u32,
    pub WFDRoleCount: u32,
    pub ConcurrentGOCount: u32,
    pub ConcurrentClientCount: u32,
    pub ServiceDiscoverySupported: u8,
    pub ServiceNameDiscoverySupported: u8,
    pub ServiceInformationDiscoverySupported: u8,
    pub ServiceNameAdvertisementsMaxBytesSupported: u32,
    pub ServiceInformationAdvertisementsMaxBytesSupported: u32,
    pub BackgroundDiscoverySupported: u8,
    pub ClientDiscoverabilitySupported: u8,
    pub InfrastructureManagementSupported: u8,
    pub DeviceAddress: [u32; 6],
    pub DiscoveryFilterListSize: u32,
    pub GOClientTableSize: u32,
    pub MaxVendorSpecificExtensionIESize: u32,
    pub PassiveAvailabilityListenStateSupported: u8,
    pub OperatingChannelUpdatesSupported: u8,
    pub GOon5GHzBandSupported: u8,
    pub ScanSSIDListSize: u32,
    pub DesiredSSIDListSize: u32,
    pub PrivacyExemptionListSize: u32,
    pub AssociationTableSize: u32,
    pub DefaultKeyTableSize: u32,
    pub WEPKeyValueMaxLength: u32,
    pub NumSupportedUnicastAlgorithms: u32,
    pub UnicastAlgorithms: usize,
    pub NumSupportedMulticastDataAlgorithms: u32,
    pub MulticastDataAlgorithms: usize,
    pub NumInterfaceAddresses: u32,
    pub InterfaceAddressList: usize,
    pub GOon6GHzBandSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_BAND_INFO {
    pub BandID: u32,
    pub BandState: u8,
    pub NumValidPhyTypes: u32,
    pub ValidPhyTypeList: usize,
    pub NumValidChannelTypes: u32,
    pub ValidChannelTypes: usize,
    pub NumChannelWidths: u32,
    pub ChannelWidthList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_BAND_CAPABILITIES {
    pub Size: u32,
    pub NumBands: u32,
    pub BandInfoList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_PHY_INFO {
    pub PhyType: u32,
    pub NumberDataRateEntries: usize,
    pub DataRateList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_PHY_CAPABILITIES {
    pub Size: u32,
    pub NumPhyTypes: u32,
    pub PhyInfoList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_ADAPTER_POWER_OFFLOAD_RSN_REKEY_CAPABILITIES {
    pub Size: u32,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_ADAPTER_WAKE_CAPABILITIES {
    pub Size: u32,
    pub NloDiscovery: u8,
    pub ApAssociationLost: u8,
    pub GtkHandshakeError: u8,
    pub FourWayHandshakeRequest: u8,
    pub IncomingActionFrame: u8,
    pub ClientDriverDiagnostic: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_ADAPTER_TX_DEMUX {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_POWER_OFFLOAD_80211RSNREKEY_PARAMETERS {
    pub Size: u32,
    pub ProtocolOffloadId: u32,
    pub ReplayCounter: u64,
    pub KCKLength: u32,
    pub KEKLength: u32,
    pub reserved_padding: [u32; 1],
    pub reserved_padding_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_POWER_OFFLOAD_LIST {
    pub Size: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_DRIVER_GLOBALS {
    pub Unused: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_WAKE_SOURCE_LIST {
    pub Size: u32,
    pub Reserved: [usize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WIFI_POWER_OFFLOAD_ACTION_FRAME_WAKE_PATTERN_PARAMETERS {
    pub Size: u32,
    pub FilterOnFrameAction: u8,
    pub ActionFrameCategory: u32,
    pub ActionFrameAction: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_V1 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_INTEGER_V1 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub IntegerArgument: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_STRING_V1 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub StringLength: u32,
    pub String: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_METHOD_ARGUMENT_V1 {
    pub Type: u16,
    pub DataLength: u16,
    pub Argument: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_COMPLEX_V1 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub Size: u32,
    pub ArgumentCount: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_OUTPUT_BUFFER_V1 {
    pub Signature: u32,
    pub Length: u32,
    pub Count: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_V1_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_INTEGER_V1_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub IntegerArgument: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_STRING_V1_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub StringLength: u32,
    pub String: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_COMPLEX_V1_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub Size: u32,
    pub ArgumentCount: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_V2 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_INTEGER_V2 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub IntegerArgument: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_STRING_V2 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub StringLength: u32,
    pub String: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_METHOD_ARGUMENT_V2 {
    pub Type: u16,
    pub DataLength: u32,
    pub Argument: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_COMPLEX_V2 {
    pub Signature: u32,
    pub MethodName: [u8; 4],
    pub MethodNameAsUlong: u32,
    pub Size: u32,
    pub ArgumentCount: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_OUTPUT_BUFFER_V2 {
    pub Signature: u32,
    pub Length: u32,
    pub Count: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_V2_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_INTEGER_V2_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub IntegerArgument: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_SIMPLE_STRING_V2_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub StringLength: u32,
    pub String: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_EVAL_INPUT_BUFFER_COMPLEX_V2_EX {
    pub Signature: u32,
    pub MethodName: [i8; 256],
    pub Size: u32,
    pub ArgumentCount: u32,
    pub Argument: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_MANIPULATE_GLOBAL_LOCK_BUFFER {
    pub Signature: u32,
    pub LockObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_ENUM_CHILDREN_INPUT_BUFFER {
    pub Signature: u32,
    pub Flags: u32,
    pub NameLength: u32,
    pub Name: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_ENUM_CHILD {
    pub Flags: u32,
    pub NameLength: u32,
    pub Name: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_ENUM_CHILDREN_OUTPUT_BUFFER {
    pub Signature: u32,
    pub NumberOfChildren: u32,
    pub Children: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_DEVICE_INFORMATION_OUTPUT_BUFFER {
    pub Signature: u32,
    pub Size: u16,
    pub Revision: u8,
    pub Reserved0: u8,
    pub VendorIdStringOffset: u16,
    pub VendorStringLength: u16,
    pub DeviceIdStringOffset: u16,
    pub SubSystemIdStringOffset: u16,
    pub SubSystemStringLength: u16,
    pub SubDeviceIdStringOffset: u16,
    pub InstanceIdLength: u16,
    pub InstanceIdOffset: u16,
    pub BaseClassCode: u16,
    pub HardwareRevision: u16,
    pub ProgrammingInterface: u8,
    pub Reserved1: u8,
    pub SubClassCode: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_GET_DEVICE_SPECIFIC_DATA {
    pub Signature: u32,
    pub Section: u32,
    pub PropertyNameLength: u32,
    pub PropertyName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADAPTER_NOTIFY {
    pub Version: u8,
    pub Reserved: [u8; 3],
    pub PeakPowerThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_NOTIFY_V2 {
    pub PowerState: u32,
    pub LowCapacity: u32,
    pub HighCapacity: u32,
    pub Version: u8,
    pub Reserved: [u8; 3],
    pub BatteryPresentRateThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_PEAK_POWER {
    pub Version: u8,
    pub Reserved: [u8; 3],
    pub InstantaneousPeakPower: u32,
    pub InstantaneousPeakPeriod: u32,
    pub SustainablePeakPower: u32,
    pub SustainablePeakPeriod: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_PEAK_POWER_THRESHOLDS {
    pub Version: u8,
    pub Reserved: [u8; 3],
    pub InstantaneousPeakPowerThreshold: u32,
    pub SustainablePeakPowerThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADAPTER_INFORMATION {
    pub AcAdapterCount: u32,
    pub RecSupported: u8,
    pub ChargeRequirement: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADAPTER_MINIPORT_INFO {
    pub MajorVersion: u16,
    pub MinorVersion: u16,
    pub Context: usize,
    pub Pdo: usize,
    pub Fdo: usize,
    pub Description: usize,
    pub AdapterInformation: u32,
    pub QueryStatus: u32,
    pub SetStatus: u32,
    pub SetStatusNotify: u32,
    pub DisableStatusNotify: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_NON_CATASTROPHIC_EVENT {
    pub BatteryChargeLimitState: u32,
    pub BatteryChargingState: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_MINIPORT_UPDATE_DATA {
    pub Version: u32,
    pub NotifyEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_STATUS {
    pub Tag: u32,
    pub RemainingCapacity: u32,
    pub ChargeRate: i32,
    pub DischargeRate: i32,
    pub Voltage: u32,
    pub PowerOnline: u8,
    pub Charging: u8,
    pub Discharging: u8,
    pub Critical: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_RUNTIME {
    pub Tag: u32,
    pub EstimatedRuntime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_TEMPERATURE {
    pub Tag: u32,
    pub Temperature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_FULL_CHARGED_CAPACITY {
    pub Tag: u32,
    pub FullChargedCapacity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_CYCLE_COUNT {
    pub Tag: u32,
    pub CycleCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_STATIC_DATA {
    pub Tag: u32,
    pub ManufactureDate: [u32; 25],
    pub reserved_padding: [u32; 4],
    pub Capabilities: u32,
    pub Technology: u8,
    pub Chemistry: u32,
    pub DesignedCapacity: u32,
    pub DefaultAlert1: u32,
    pub DefaultAlert2: u32,
    pub CriticalBias: u32,
    pub Strings: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WMI_STATUS_CHANGE {
    pub Tag: u32,
    pub PowerOnline: u8,
    pub Charging: u8,
    pub Discharging: u8,
    pub Critical: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_TAG_CHANGE {
    pub Tag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_KEY_LENGTHS_STRUCT {
    pub dwMinLength: u32,
    pub dwMaxLength: u32,
    pub dwIncrement: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_OID {
    pub cbOID: u32,
    pub pbOID: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_OID_LIST {
    pub dwOIDCount: u32,
    pub field_pOIDs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_PKCS1_PADDING_INFO {
    pub pszAlgId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_PSS_PADDING_INFO {
    pub pszAlgId: usize,
    pub cbSalt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_OAEP_PADDING_INFO {
    pub pszAlgId: usize,
    pub pbLabel: *mut u8,
    pub cbLabel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_PQDSA_PADDING_INFO {
    pub pbCtx: *mut u8,
    pub cbCtx: u32,
    pub pszPrehashAlgId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO {
    pub cbSize: u32,
    pub dwInfoVersion: u32,
    pub pbNonce: *mut u8,
    pub cbNonce: u32,
    pub pbAuthData: *mut u8,
    pub cbAuthData: u32,
    pub pbTag: *mut u8,
    pub cbTag: u32,
    pub pbMacContext: *mut u8,
    pub cbMacContext: u32,
    pub cbAAD: u32,
    pub cbData: u64,
    pub dwFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_PKCS11_RSA_AES_WRAP_BLOB {
    pub dwMagic: u32,
    pub cbKey: u32,
    pub cbPaddingAlgId: u32,
    pub cbPaddingLabel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCryptBuffer {
    pub cbBuffer: u32,
    pub BufferType: u32,
    pub pvBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCryptBufferDesc {
    pub ulVersion: u32,
    pub cBuffers: u32,
    pub pBuffers: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_KEY_BLOB {
    pub Magic: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_RSAKEY_BLOB {
    pub Magic: u32,
    pub BitLength: u32,
    pub cbPublicExp: u32,
    pub cbModulus: u32,
    pub cbPrime1: u32,
    pub cbPrime2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_ECCKEY_BLOB {
    pub dwMagic: u32,
    pub cbKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SSL_ECCKEY_BLOB {
    pub dwCurveType: u32,
    pub cbKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_ECCFULLKEY_BLOB {
    pub dwMagic: u32,
    pub dwVersion: u32,
    pub dwCurveType: u32,
    pub dwCurveGenerationAlgId: u32,
    pub cbFieldLength: u32,
    pub cbSubgroupOrder: u32,
    pub cbCofactor: u32,
    pub cbSeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DH_KEY_BLOB {
    pub dwMagic: u32,
    pub cbKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DH_PARAMETER_HEADER {
    pub cbLength: u32,
    pub dwMagic: u32,
    pub cbKeyLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DSA_KEY_BLOB {
    pub dwMagic: u32,
    pub cbKey: u32,
    pub Count: [u8; 4],
    pub Seed: [u8; 20],
    pub q: [u8; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DSA_KEY_BLOB_V2 {
    pub dwMagic: u32,
    pub cbKey: u32,
    pub hashAlgorithm: u32,
    pub standardVersion: u32,
    pub cbSeedLength: u32,
    pub cbGroupSize: u32,
    pub Count: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_KEY_DATA_BLOB_HEADER {
    pub dwMagic: u32,
    pub dwVersion: u32,
    pub cbKeyData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DSA_PARAMETER_HEADER {
    pub cbLength: u32,
    pub dwMagic: u32,
    pub cbKeyLength: u32,
    pub Count: [u8; 4],
    pub Seed: [u8; 20],
    pub q: [u8; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_DSA_PARAMETER_HEADER_V2 {
    pub cbLength: u32,
    pub dwMagic: u32,
    pub cbKeyLength: u32,
    pub hashAlgorithm: u32,
    pub standardVersion: u32,
    pub cbSeedLength: u32,
    pub cbGroupSize: u32,
    pub Count: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_ECC_CURVE_NAMES {
    pub dwEccCurveNames: u32,
    pub field_pEccCurveNames: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_MULTI_HASH_OPERATION {
    pub iHash: u32,
    pub hashOperation: u32,
    pub pbBuffer: u32,
    pub cbBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_MULTI_OBJECT_LENGTH_STRUCT {
    pub cbPerObject: u32,
    pub cbPerElement: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_ALGORITHM_IDENTIFIER {
    pub pszName: usize,
    pub dwClass: u32,
    pub dwFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_PROVIDER_NAME {
    pub pszProviderName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BCRYPT_INTERFACE_VERSION {
    pub MajorVersion: u16,
    pub MinorVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_INTERFACE_REG {
    pub dwInterface: u32,
    pub dwFlags: u32,
    pub cFunctions: u32,
    pub field_rgpszFunctions: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_IMAGE_REG {
    pub pszImage: usize,
    pub cInterfaces: u32,
    pub field_rgpInterfaces: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_PROVIDER_REG {
    pub cAliases: u32,
    pub field_rgpszAliases: usize,
    pub pUM: usize,
    pub pKM: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_PROVIDERS {
    pub cProviders: u32,
    pub field_rgpszProviders: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_CONTEXT_CONFIG {
    pub dwFlags: u32,
    pub dwReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_CONTEXT_FUNCTION_CONFIG {
    pub dwFlags: u32,
    pub dwReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_CONTEXTS {
    pub cContexts: u32,
    pub field_rgpszContexts: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_CONTEXT_FUNCTIONS {
    pub cFunctions: u32,
    pub field_rgpszFunctions: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_CONTEXT_FUNCTION_PROVIDERS {
    pub cProviders: u32,
    pub field_rgpszProviders: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_PROPERTY_REF {
    pub pszProperty: usize,
    pub cbValue: u32,
    pub pbValue: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_IMAGE_REF {
    pub pszImage: usize,
    pub dwFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_PROVIDER_REF {
    pub dwInterface: u32,
    pub pszFunction: usize,
    pub pszProvider: usize,
    pub cProperties: u32,
    pub field_rgpProperties: usize,
    pub pUM: usize,
    pub pKM: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CRYPT_PROVIDER_REFS {
    pub cProviders: u32,
    pub field_rgpProviders: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSP_BDA_NODE_PIN {
    pub Property: u32,
    pub ulNodeType: u32,
    pub ulInputPinId: u32,
    pub ulOutputPinId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_PIN {
    pub Method: u32,
    pub PinId: u32,
    pub PinType: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_PIN_PAIR {
    pub Method: u32,
    pub InputPinId: u32,
    pub InputPinType: u32,
    pub OutputPinId: u32,
    pub OutputPinType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_DEBUG_LEVEL {
    pub Method: u32,
    pub ucDebugLevel: u8,
    pub ulDebugStringSize: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_DEBUG_DATA {
    pub lResult: usize,
    pub uuidDebugDataType: u32,
    pub ulDataSize: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_EVENT_DATA {
    pub lResult: usize,
    pub ulEventID: u32,
    pub uuidEventType: u32,
    pub ulEventDataLength: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_EVENT_COMPLETE {
    pub Method: u32,
    pub ulEventID: u32,
    pub ulEventResult: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_DRM_SETDRM {
    pub NodeMethod: u32,
    pub NewDRMuuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_BUFFER {
    pub NodeMethod: u32,
    pub ulBufferSize: u32,
    pub argbBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_WMDRM_RENEWLICENSE {
    pub NodeMethod: u32,
    pub ulXMRLicenseLength: u32,
    pub ulEntitlementTokenLength: u32,
    pub argbDataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_WMDRMTUNER_PURCHASEENTITLEMENT {
    pub NodeMethod: u32,
    pub ulDialogRequest: u32,
    pub cLanguage: [i8; 12],
    pub ulPurchaseTokenLength: u32,
    pub argbDataBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_WMDRMTUNER_SETPIDPROTECTION {
    pub NodeMethod: u32,
    pub ulPID: u32,
    pub uuidKeyID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_WMDRMTUNER_GETPIDPROTECTION {
    pub NodeMethod: u32,
    pub ulPID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_WMDRMTUNER_SYNCVALUE {
    pub NodeMethod: u32,
    pub ulSyncValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_TUNER_TUNEREQUEST {
    pub Method: u32,
    pub ulTuneLength: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_GPNV_GETVALUE {
    pub Method: u32,
    pub ulNameLength: u32,
    pub cLanguage: [i8; 12],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_GPNV_SETVALUE {
    pub Method: u32,
    pub ulDialogRequest: u32,
    pub cLanguage: [i8; 12],
    pub ulNameLength: u32,
    pub ulValueLength: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_GPNV_NAMEINDEX {
    pub Method: u32,
    pub ulValueNameIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_SCAN_CAPABILTIES {
    pub Method: u32,
    pub uuidBroadcastStandard: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_SCAN_FILTER {
    pub Method: u32,
    pub ulScanModulationTypeSize: u32,
    pub AnalogVideoStandards: u32,
    pub argbScanModulationTypes: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_SCAN_START {
    pub Method: u32,
    pub LowerFrequency: u32,
    pub HigherFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_GDDS_TUNEXMLFROMIDX {
    pub Method: u32,
    pub ulIdx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_GDDS_SERVICEFROMTUNEXML {
    pub Method: u32,
    pub ulTuneXmlLength: u32,
    pub argbTuneXml: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_USERACTIVITY_USEREASON {
    pub Method: u32,
    pub ulUseReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_CAS_ENTITLEMENTTOKEN {
    pub NodeMethod: u32,
    pub ulDialogRequest: u32,
    pub cLanguage: [i8; 12],
    pub ulRequestType: u32,
    pub ulEntitlementTokenLen: u32,
    pub argbEntitlementToken: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_CAS_CAPTURETOKEN {
    pub NodeMethod: u32,
    pub ulTokenLength: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_CAS_OPENBROADCASTMMI {
    pub NodeMethod: u32,
    pub ulDialogRequest: u32,
    pub cLanguage: [i8; 12],
    pub ulEventId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_CAS_CLOSEMMIDIALOG {
    pub NodeMethod: u32,
    pub ulDialogRequest: u32,
    pub cLanguage: [i8; 12],
    pub ulDialogNumber: u32,
    pub ulReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_ISDBCAS_REQUEST {
    pub NodeMethod: u32,
    pub ulRequestID: u32,
    pub ulIsdbCommandSize: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSM_BDA_TS_SELECTOR_SETTSID {
    pub NodeMethod: u32,
    pub usTSID: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ChannelChangeInfo {
    pub state: u32,
    pub TimeStamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ChannelTypeInfo {
    pub channelType: u32,
    pub timeStamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ChannelInfo {
    pub lFrequency: i32,
    pub lONID: i32,
    pub lTSID: i32,
    pub lSID: i32,
    pub lProgNumber: i32,
    pub lProgNumber_1: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SpanningEventDescriptor {
    pub wDataLen: u16,
    pub wProgNumber: u16,
    pub wSID: u16,
    pub bDescriptor: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVBScramblingControlSpanningEvent {
    pub ulPID: u32,
    pub fScrambled: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SpanningEventEmmMessage {
    pub bCAbroadcasterGroupId: u8,
    pub bMessageControl: u8,
    pub wServiceId: u16,
    pub wTableIdExtension: u16,
    pub bDeletionStatus: u8,
    pub bDisplayingDuration1: u8,
    pub bDisplayingDuration2: u8,
    pub bDisplayingDuration3: u8,
    pub bDisplayingCycle: u8,
    pub bFormatVersion: u8,
    pub bDisplayPosition: u8,
    pub wMessageLength: u16,
    pub szMessageArea: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LanguageInfo {
    pub LangID: u32,
    pub lISOLangCode: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DualMonoInfo {
    pub LangID1: u32,
    pub LangID2: u32,
    pub lISOLangCode1: i32,
    pub lISOLangCode2: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PIDListSpanningEvent {
    pub wPIDCount: u16,
    pub pulPIDs: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PBDAParentalControl {
    pub rating_system_count: u32,
    pub rating_systems: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TEMPLATE_CONNECTION {
    pub FromNodeType: u32,
    pub FromNodePinType: u32,
    pub ToNodeType: u32,
    pub ToNodePinType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TEMPLATE_PIN_JOINT {
    pub uliTemplateConnection: u32,
    pub ulcInstancesMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_ETHERNET_ADDRESS {
    pub rgbAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_ETHERNET_ADDRESS_LIST {
    pub ulcAddresses: u32,
    pub rgAddressl: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_IPv4_ADDRESS {
    pub rgbAddress: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_IPv4_ADDRESS_LIST {
    pub ulcAddresses: u32,
    pub rgAddressl: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_IPv6_ADDRESS {
    pub rgbAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_IPv6_ADDRESS_LIST {
    pub ulcAddresses: u32,
    pub rgAddressl: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDANODE_DESCRIPTOR {
    pub ulBdaNodeType: u32,
    pub guidFunction: u32,
    pub guidName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TABLE_SECTION {
    pub ulPrimarySectionId: u32,
    pub ulSecondarySectionId: u32,
    pub ulcbSectionLength: u32,
    pub argbSectionData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_DISEQC_SEND {
    pub ulRequestId: u32,
    pub ulPacketLength: u32,
    pub argbPacketData: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_DISEQC_RESPONSE {
    pub ulRequestId: u32,
    pub ulPacketLength: u32,
    pub argbPacketData: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_PID_MAP {
    pub MediaSampleContent: u32,
    pub ulcPIDs: u32,
    pub aulPIDs: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_PID_UNMAP {
    pub ulcPIDs: u32,
    pub aulPIDs: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CA_MODULE_UI {
    pub ulFormat: u32,
    pub ulbcDesc: u32,
    pub ulDesc: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_PROGRAM_PID_LIST {
    pub ulProgramNumber: u32,
    pub ulcPIDs: u32,
    pub ulPID: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_DRM_DRMSTATUS {
    pub lResult: usize,
    pub DRMuuid: u32,
    pub ulDrmUuidListStringSize: u32,
    pub argbDrmUuidListString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_WMDRM_STATUS {
    pub lResult: usize,
    pub ulMaxCaptureTokenSize: u32,
    pub uMaxStreamingPid: u32,
    pub ulMaxLicense: u32,
    pub ulMinSecurityLevel: u32,
    pub ulRevInfoSequenceNumber: u32,
    pub ulRevInfoIssuedTime: u64,
    pub ulRevListVersion: u32,
    pub ulRevInfoTTL: u32,
    pub ulState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_WMDRM_KEYINFOLIST {
    pub lResult: usize,
    pub ulKeyuuidBufferLen: u32,
    pub argKeyuuidBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_BUFFER {
    pub lResult: usize,
    pub ulBufferSize: u32,
    pub argbBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_WMDRM_RENEWLICENSE {
    pub lResult: usize,
    pub ulDescrambleStatus: u32,
    pub ulXmrLicenseOutputLength: u32,
    pub argbXmrLicenceOutputBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_WMDRMTUNER_PIDPROTECTION {
    pub lResult: usize,
    pub uuidKeyID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_WMDRMTUNER_PURCHASEENTITLEMENT {
    pub lResult: usize,
    pub ulDescrambleStatus: u32,
    pub ulCaptureTokenLength: u32,
    pub argbCaptureTokenBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TUNER_TUNERSTATE {
    pub lResult: usize,
    pub ulTuneLength: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TUNER_DIAGNOSTICS {
    pub lResult: usize,
    pub ulSignalLevel: u32,
    pub ulSignalLevelQuality: u32,
    pub ulSignalNoiseRatio: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_STRING {
    pub lResult: usize,
    pub ulStringSize: u32,
    pub argbString: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_SCAN_CAPABILTIES {
    pub lResult: usize,
    pub ul64AnalogStandardsSupported: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_SCAN_STATE {
    pub lResult: usize,
    pub ulSignalLock: u32,
    pub ulSecondsLeft: u32,
    pub ulCurrentFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_SCAN_START {
    pub lResult: usize,
    pub LowerFrequency: u32,
    pub HigerFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_GDDS_DATATYPE {
    pub lResult: usize,
    pub uuidDataType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_GDDS_DATA {
    pub lResult: usize,
    pub ulDataLength: u32,
    pub ulPercentageProgress: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_USERACTIVITY_INTERVAL {
    pub lResult: usize,
    pub ulActivityInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CAS_CHECK_ENTITLEMENTTOKEN {
    pub lResult: usize,
    pub ulDescrambleStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CAS_CLOSE_MMIDIALOG {
    pub lResult: usize,
    pub SessionResult: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CAS_REQUESTTUNERDATA {
    pub ucRequestPriority: u8,
    pub ucRequestReason: u8,
    pub ucRequestConsequences: u8,
    pub ulEstimatedTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CAS_OPENMMIDATA {
    pub ulDialogNumber: u32,
    pub ulDialogRequest: u32,
    pub uuidDialogType: u32,
    pub usDialogDataLength: u16,
    pub argbDialogData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_CAS_CLOSEMMIDATA {
    pub ulDialogNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_ISDBCAS_REQUESTHEADER {
    pub bInstruction: u8,
    pub bReserved: [u8; 3],
    pub ulDataLength: u32,
    pub argbIsdbCommand: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_ISDBCAS_RESPONSEDATA {
    pub lResult: usize,
    pub ulRequestID: u32,
    pub ulIsdbStatus: u32,
    pub ulIsdbDataSize: u32,
    pub argbIsdbCommandData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_ISDBCAS_EMG_REQ {
    pub bCLA: u8,
    pub bINS: u8,
    pub bP1: u8,
    pub bP2: u8,
    pub bLC: u8,
    pub bCardId: [u8; 6],
    pub bProtocol: u8,
    pub bCABroadcasterGroupId: u8,
    pub bMessageControl: u8,
    pub bMessageCode: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_MUX_PIDLISTITEM {
    pub usPIDNumber: u16,
    pub usProgramNumber: u16,
    pub ePIDType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TS_SELECTORINFO {
    pub bTSInfolength: u8,
    pub bReserved: [u8; 2],
    pub guidNetworkType: u32,
    pub bTSIDCount: u8,
    pub usTSID: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_TS_SELECTORINFO_ISDBS_EXT {
    pub bTMCC: [u8; 48],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_DVBT2_L1_SIGNALLING_DATA {
    pub L1Pre_TYPE: u8,
    pub L1Pre_BWT_S1_S2: u8,
    pub L1Pre_REPETITION_GUARD_PAPR: u8,
    pub L1Pre_MOD_COD_FEC: u8,
    pub L1Pre_POSTSIZE_INFO_PILOT: [u8; 5],
    pub L1Pre_TX_ID_AVAIL: u8,
    pub L1Pre_CELL_ID: [u8; 2],
    pub L1Pre_NETWORK_ID: [u8; 2],
    pub L1Pre_T2SYSTEM_ID: [u8; 2],
    pub L1Pre_NUM_T2_FRAMES: u8,
    pub L1Pre_NUM_DATA_REGENFLAG_L1POSTEXT: [u8; 2],
    pub L1Pre_NUMRF_CURRENTRF_RESERVED: [u8; 2],
    pub L1Pre_CRC32: [u8; 4],
    pub L1PostData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_RATING_PINRESET {
    pub bPinLength: u8,
    pub argbNewPin: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPEG2_TRANSPORT_STRIDE {
    pub dwOffset: u32,
    pub dwPacketLength: u32,
    pub dwStride: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BDA_SIGNAL_TIMEOUTS {
    pub ulCarrierTimeoutMs: u32,
    pub ulScanningTimeoutMs: u32,
    pub ulTuningTimeoutMs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_DEVICE_INFO {
    pub flags: u32,
    pub address: u32,
    pub classOfDevice: u32,
    pub name: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_RADIO_IN_RANGE {
    pub deviceInfo: u32,
    pub previousDeviceFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_L2CAP_EVENT_INFO {
    pub bthAddress: u32,
    pub psm: u16,
    pub connected: u8,
    pub initiated: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_HCI_EVENT_INFO {
    pub bthAddress: u32,
    pub connectionType: u8,
    pub connected: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_DEVICE_INFO_LIST {
    pub numOfDevices: u32,
    pub deviceList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_RADIO_INFO {
    pub lmpSupportedFeatures: u64,
    pub mfg: u16,
    pub lmpSubversion: u16,
    pub lmpVersion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_LOCAL_RADIO_INFO {
    pub localInfo: u32,
    pub flags: u32,
    pub hciRevision: u16,
    pub hciVersion: u8,
    pub radioInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_CONNECT {
    pub bthAddress: u32,
    pub fSdpConnect: u32,
    pub HANDLE_SDP_FIELD_NAME: u32,
    pub requestTimeout: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_DISCONNECT {
    pub HANDLE_SDP_FIELD_NAME: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_RECORD {
    pub fSecurity: u32,
    pub fOptions: u32,
    pub fCodService: u32,
    pub recordLength: u32,
    pub record: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_SERVICE_SEARCH_REQUEST {
    pub HANDLE_SDP_FIELD_NAME: u32,
    pub uuids: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_ATTRIBUTE_SEARCH_REQUEST {
    pub HANDLE_SDP_FIELD_NAME: u32,
    pub searchFlags: u32,
    pub recordHandle: u32,
    pub range: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_SERVICE_ATTRIBUTE_SEARCH_REQUEST {
    pub HANDLE_SDP_FIELD_NAME: u32,
    pub searchFlags: u32,
    pub uuids: [u32; 1],
    pub range: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_SDP_STREAM_RESPONSE {
    pub requiredSize: u32,
    pub responseSize: u32,
    pub response: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_COMMAND_HEADER {
    pub OpCode: u16,
    pub TotalParameterLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_VENDOR_SPECIFIC_COMMAND {
    pub ManufacturerId: u32,
    pub LmpVersion: u8,
    pub MatchAnySinglePattern: u8,
    pub HciHeader: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_VENDOR_PATTERN {
    pub Offset: u8,
    pub Size: u8,
    pub Pattern: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_VENDOR_EVENT_INFO {
    pub BthAddress: u32,
    pub EventSize: u32,
    pub EventInfo: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BTH_HOST_FEATURE_MASK {
    pub Mask: u64,
    pub Reserved1: u64,
    pub Reserved2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SdpQueryUuid {
    pub u: u32,
    pub uuidType: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVSHADERCAPS2_0 {
    pub Caps: u32,
    pub DynamicFlowControlDepth: i32,
    pub NumTemps: i32,
    pub StaticFlowControlDepth: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DPSHADERCAPS2_0 {
    pub Caps: u32,
    pub DynamicFlowControlDepth: i32,
    pub NumTemps: i32,
    pub StaticFlowControlDepth: i32,
    pub NumInstructionSlots: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DOVERLAYCAPS {
    pub Caps: u32,
    pub MaxOverlayDisplayWidth: u32,
    pub MaxOverlayDisplayHeight: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCONTENTPROTECTIONCAPS {
    pub Caps: u32,
    pub KeyExchangeType: u32,
    pub BufferAlignmentStart: u32,
    pub BlockAlignmentSize: u32,
    pub ProtectedMemorySize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCAPS9 {
    pub DeviceType: u32,
    pub AdapterOrdinal: u32,
    pub Caps: u32,
    pub Caps2: u32,
    pub Caps3: u32,
    pub PresentationIntervals: u32,
    pub CursorCaps: u32,
    pub DevCaps: u32,
    pub PrimitiveMiscCaps: u32,
    pub RasterCaps: u32,
    pub ZCmpCaps: u32,
    pub SrcBlendCaps: u32,
    pub DestBlendCaps: u32,
    pub AlphaCmpCaps: u32,
    pub ShadeCaps: u32,
    pub TextureCaps: u32,
    pub TextureFilterCaps: u32,
    pub CubeTextureFilterCaps: u32,
    pub VolumeTextureFilterCaps: u32,
    pub TextureAddressCaps: u32,
    pub VolumeTextureAddressCaps: u32,
    pub LineCaps: u32,
    pub MaxTextureHeight: u32,
    pub MaxVolumeExtent: u32,
    pub MaxTextureRepeat: u32,
    pub MaxTextureAspectRatio: u32,
    pub MaxAnisotropy: u32,
    pub MaxVertexW: u32,
    pub GuardBandLeft: u32,
    pub GuardBandTop: u32,
    pub GuardBandRight: u32,
    pub GuardBandBottom: u32,
    pub ExtentsAdjust: u32,
    pub StencilCaps: u32,
    pub FVFCaps: u32,
    pub TextureOpCaps: u32,
    pub MaxTextureBlendStages: u32,
    pub MaxSimultaneousTextures: u32,
    pub VertexProcessingCaps: u32,
    pub MaxActiveLights: u32,
    pub MaxUserClipPlanes: u32,
    pub MaxVertexBlendMatrices: u32,
    pub MaxVertexBlendMatrixIndex: u32,
    pub MaxPointSize: u32,
    pub MaxPrimitiveCount: u32,
    pub MaxVertexIndex: u32,
    pub MaxStreams: u32,
    pub MaxStreamStride: u32,
    pub VertexShaderVersion: u32,
    pub MaxVertexShaderConst: u32,
    pub PixelShaderVersion: u32,
    pub PixelShader1xMaxValue: u32,
    pub DevCaps2: u32,
    pub MaxNpatchTessellationLevel: u32,
    pub Reserved5: u32,
    pub MasterAdapterOrdinal: u32,
    pub AdapterOrdinalInGroup: u32,
    pub NumberOfAdaptersInGroup: u32,
    pub DeclTypes: u32,
    pub NumSimultaneousRTs: u32,
    pub StretchRectFilterCaps: u32,
    pub VS20Caps: u32,
    pub PS20Caps: u32,
    pub VertexTextureFilterCaps: u32,
    pub MaxVShaderInstructionsExecuted: u32,
    pub MaxPShaderInstructionsExecuted: u32,
    pub MaxVertexShader30InstructionSlots: u32,
    pub MaxPixelShader30InstructionSlots: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVECTOR {
    pub x: u32,
    pub y: u32,
    pub z: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCOLORVALUE {
    pub r: u32,
    pub g: u32,
    pub b: u32,
    pub a: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DRECT {
    pub x1: i32,
    pub y1: i32,
    pub x2: i32,
    pub y2: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DMATRIX {
    pub field_14: u32,
    pub field_24: u32,
    pub field_34: u32,
    pub field_44: u32,
    pub m: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVIEWPORT9 {
    pub X: u32,
    pub Y: u32,
    pub Width: u32,
    pub Height: u32,
    pub MinZ: u32,
    pub MaxZ: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCLIPSTATUS9 {
    pub ClipUnion: u32,
    pub ClipIntersection: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DMATERIAL9 {
    pub Diffuse: u32,
    pub Ambient: u32,
    pub Specular: u32,
    pub Emissive: u32,
    pub Power: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DLIGHT9 {
    pub Type: u32,
    pub Diffuse: u32,
    pub Specular: u32,
    pub Ambient: u32,
    pub Position: u32,
    pub Direction: u32,
    pub Range: u32,
    pub Falloff: u32,
    pub Attenuation0: u32,
    pub Attenuation1: u32,
    pub Attenuation2: u32,
    pub Theta: u32,
    pub Phi: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVERTEXELEMENT9 {
    pub Stream: u16,
    pub Offset: u16,
    pub Type: u8,
    pub Method: u8,
    pub Usage: u8,
    pub UsageIndex: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDISPLAYMODE {
    pub Width: u32,
    pub Height: u32,
    pub RefreshRate: u32,
    pub Format: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVICE_CREATION_PARAMETERS {
    pub AdapterOrdinal: u32,
    pub DeviceType: u32,
    pub hFocusWindow: u32,
    pub BehaviorFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DPRESENT_PARAMETERS {
    pub BackBufferWidth: u32,
    pub BackBufferHeight: u32,
    pub BackBufferFormat: u32,
    pub BackBufferCount: u32,
    pub MultiSampleType: u32,
    pub MultiSampleQuality: u32,
    pub SwapEffect: u32,
    pub hDeviceWindow: u32,
    pub Windowed: i32,
    pub EnableAutoDepthStencil: i32,
    pub AutoDepthStencilFormat: u32,
    pub Flags: u32,
    pub FullScreen_RefreshRateInHz: u32,
    pub PresentationInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DGAMMARAMP {
    pub reserved_padding: [u32; 256],
    pub green: [u16; 256],
    pub reserved_padding_1: [u32; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVERTEXBUFFER_DESC {
    pub Format: u32,
    pub Type: u32,
    pub Usage: u32,
    pub Pool: u32,
    pub Size: u32,
    pub FVF: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DINDEXBUFFER_DESC {
    pub Format: u32,
    pub Type: u32,
    pub Usage: u32,
    pub Pool: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DSURFACE_DESC {
    pub Format: u32,
    pub Type: u32,
    pub Usage: u32,
    pub Pool: u32,
    pub MultiSampleType: u32,
    pub MultiSampleQuality: u32,
    pub Width: u32,
    pub Height: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DVOLUME_DESC {
    pub Format: u32,
    pub Type: u32,
    pub Usage: u32,
    pub Pool: u32,
    pub Width: u32,
    pub Height: u32,
    pub Depth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DLOCKED_RECT {
    pub Pitch: i32,
    pub pBits: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DBOX {
    pub Left: u32,
    pub Top: u32,
    pub Right: u32,
    pub Bottom: u32,
    pub Front: u32,
    pub Back: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DLOCKED_BOX {
    pub RowPitch: i32,
    pub SlicePitch: i32,
    pub pBits: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DRANGE {
    pub Offset: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DRECTPATCH_INFO {
    pub StartVertexOffsetWidth: u32,
    pub StartVertexOffsetHeight: u32,
    pub Width: u32,
    pub Height: u32,
    pub Stride: u32,
    pub Basis: u32,
    pub Degree: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DTRIPATCH_INFO {
    pub StartVertexOffset: u32,
    pub NumVertices: u32,
    pub Basis: u32,
    pub Degree: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DADAPTER_IDENTIFIER9 {
    pub Driver: [u32; 1],
    pub Description: [u32; 1],
    pub DeviceName: [u32; 32],
    pub DriverVersion: i64,
    pub DriverVersionLowPart: u32,
    pub DriverVersionHighPart: u32,
    pub VendorId: u32,
    pub DeviceId: u32,
    pub SubSysId: u32,
    pub Revision: u32,
    pub DeviceIdentifier: u32,
    pub WHQLLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DRASTER_STATUS {
    pub InVBlank: i32,
    pub ScanLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DRESOURCESTATS {
    pub bThrashing: i32,
    pub ApproxBytesDownloaded: u32,
    pub NumEvicts: u32,
    pub NumVidCreates: u32,
    pub LastPri: u32,
    pub NumUsed: u32,
    pub NumUsedInVidMem: u32,
    pub WorkingSet: u32,
    pub WorkingSetBytes: u32,
    pub TotalManaged: u32,
    pub TotalBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_RESOURCEMANAGER {
    pub stats: [u32; 1],
    pub stats_1: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3DVERTEXSTATS {
    pub NumRenderedTriangles: u32,
    pub NumExtraClippingTriangles: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_VCACHE {
    pub Pattern: u32,
    pub OptMethod: u32,
    pub CacheSize: u32,
    pub MagicNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3D9PIPELINETIMINGS {
    pub VertexProcessingTimePercent: u32,
    pub PixelProcessingTimePercent: u32,
    pub OtherGPUProcessingTimePercent: u32,
    pub GPUIdleTimePercent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3D9INTERFACETIMINGS {
    pub WaitingForGPUToUseApplicationResourceTimePercent: u32,
    pub WaitingForGPUToAcceptMoreCommandsTimePercent: u32,
    pub WaitingForGPUToStayWithinLatencyTimePercent: u32,
    pub WaitingForGPUExclusiveResourceTimePercent: u32,
    pub WaitingForGPUOtherTimePercent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3D9STAGETIMINGS {
    pub MemoryProcessingPercent: u32,
    pub ComputationProcessingPercent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3D9BANDWIDTHTIMINGS {
    pub MaxBandwidthUtilized: u32,
    pub FrontEndUploadMemoryUtilizedPercent: u32,
    pub VertexRateUtilizedPercent: u32,
    pub TriangleSetupRateUtilizedPercent: u32,
    pub FillRateUtilizedPercent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDEVINFO_D3D9CACHEUTILIZATION {
    pub TextureCacheHitRate: u32,
    pub PostTransformVertexCacheHitRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DMEMORYPRESSURE {
    pub BytesEvictedFromProcess: u64,
    pub SizeOfInefficientAllocation: u64,
    pub LevelOfEfficiency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCOMPOSERECTDESC {
    pub Y: u32,
    pub Height: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DCOMPOSERECTDESTINATION {
    pub SrcRectIndex: u16,
    pub Reserved: u16,
    pub Y: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DPRESENTSTATS {
    pub PresentCount: u32,
    pub PresentRefreshCount: u32,
    pub SyncRefreshCount: u32,
    pub SyncQPCTime: i64,
    pub SyncGPUTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3D_OMAC {
    pub Omac: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERY_INPUT {
    pub QueryType: u32,
    pub hChannel: usize,
    pub SequenceNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERY_OUTPUT {
    pub omac: u32,
    pub QueryType: u32,
    pub hChannel: usize,
    pub SequenceNumber: u32,
    pub ReturnCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_PROTECTION_FLAGS {
    pub ProtectionEnabled: u32,
    pub OverlayOrFullscreenRequired: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYPROTECTION_OUTPUT {
    pub Output: u32,
    pub ProtectionFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYCHANNELTYPE_OUTPUT {
    pub Output: u32,
    pub ChannelType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYDEVICEHANDLE_OUTPUT {
    pub Output: u32,
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYCRYPTOSESSION_INPUT {
    pub Input: u32,
    pub DXVA2DecodeHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYCRYPTOSESSION_OUTPUT {
    pub Output: u32,
    pub DXVA2DecodeHandle: usize,
    pub CryptoSessionHandle: usize,
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYRESTRICTEDSHAREDRESOURCEPROCESSCOUNT_OUTPUT {
    pub Output: u32,
    pub NumRestrictedSharedResourceProcesses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYRESTRICTEDSHAREDRESOURCEPROCESS_INPUT {
    pub Input: u32,
    pub ProcessIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYRESTRICTEDSHAREDRESOURCEPROCESS_OUTPUT {
    pub Output: u32,
    pub ProcessIndex: u32,
    pub ProcessIdentifer: u32,
    pub ProcessHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYUNRESTRICTEDPROTECTEDSHAREDRESOURCECOUNT_OUTPUT {
    pub Output: u32,
    pub NumUnrestrictedProtectedSharedResources: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYOUTPUTIDCOUNT_INPUT {
    pub Input: u32,
    pub DeviceHandle: usize,
    pub CryptoSessionHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYOUTPUTIDCOUNT_OUTPUT {
    pub Output: u32,
    pub DeviceHandle: usize,
    pub CryptoSessionHandle: usize,
    pub NumOutputIDs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYOUTPUTID_INPUT {
    pub Input: u32,
    pub DeviceHandle: usize,
    pub CryptoSessionHandle: usize,
    pub OutputIDIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYOUTPUTID_OUTPUT {
    pub Output: u32,
    pub DeviceHandle: usize,
    pub CryptoSessionHandle: usize,
    pub OutputIDIndex: u32,
    pub OutputID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYINFOBUSTYPE_OUTPUT {
    pub Output: u32,
    pub BusType: u32,
    pub bAccessibleInContiguousBlocks: i32,
    pub bAccessibleInNonContiguousBlocks: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYEVICTIONENCRYPTIONGUIDCOUNT_OUTPUT {
    pub Output: u32,
    pub NumEncryptionGuids: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYEVICTIONENCRYPTIONGUID_INPUT {
    pub Input: u32,
    pub EncryptionGuidIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYEVICTIONENCRYPTIONGUID_OUTPUT {
    pub Output: u32,
    pub EncryptionGuidIndex: u32,
    pub EncryptionGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_QUERYUNCOMPRESSEDENCRYPTIONLEVEL_OUTPUT {
    pub Output: u32,
    pub EncryptionGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGURE_INPUT {
    pub omac: u32,
    pub ConfigureType: u32,
    pub hChannel: usize,
    pub SequenceNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGURE_OUTPUT {
    pub omac: u32,
    pub ConfigureType: u32,
    pub hChannel: usize,
    pub SequenceNumber: u32,
    pub ReturnCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGUREINITIALIZE {
    pub Parameters: u32,
    pub StartSequenceQuery: u32,
    pub StartSequenceConfigure: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGUREPROTECTION {
    pub Parameters: u32,
    pub Protections: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGURECRYPTOSESSION {
    pub Parameters: u32,
    pub DXVA2DecodeHandle: usize,
    pub CryptoSessionHandle: usize,
    pub DeviceHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGURESHAREDRESOURCE {
    pub Parameters: u32,
    pub ProcessIdentiferType: u32,
    pub ProcessHandle: usize,
    pub AllowAccess: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAUTHENTICATEDCHANNEL_CONFIGUREUNCOMPRESSEDENCRYPTION {
    pub Parameters: u32,
    pub EncryptionGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DENCRYPTED_BLOCK_INFO {
    pub NumEncryptedBytesAtBeginning: u32,
    pub NumBytesInSkipPattern: u32,
    pub NumBytesInEncryptPattern: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DAES_CTR_IV {
    pub IV: u64,
    pub Count: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTBANKPREFERENCE {
    pub Bank0: u32,
    pub Direction0: u32,
    pub Bank1: u32,
    pub Direction1: u32,
    pub Bank2: u32,
    pub Direction2: u32,
    pub Bank3: u32,
    pub Direction3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONLIST {
    pub hDeviceSpecificAllocation: usize,
    pub WriteOperation: u32,
    pub SegmentId: u32,
    pub Reserved: u32,
    pub PhysicalAddress: u64,
    pub VirtualAddress: u32,
    pub PhysicalAddress_1: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RENDER {
    pub pCommand: usize,
    pub CommandLength: u32,
    pub pDmaBuffer: usize,
    pub DmaSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub pAllocationList: usize,
    pub AllocationListSize: u32,
    pub pPatchLocationListIn: usize,
    pub PatchLocationListInSize: u32,
    pub pPatchLocationListOut: usize,
    pub PatchLocationListOutSize: u32,
    pub MultipassOffset: u32,
    pub DmaBufferSegmentId: u32,
    pub DmaBufferPhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRESENTFLAGS {
    pub Blt: u32,
    pub ColorFill: u32,
    pub Flip: u32,
    pub FlipWithNoWait: u32,
    pub SrcColorKey: u32,
    pub DstColorKey: u32,
    pub LinearToSrgb: u32,
    pub Rotate: u32,
    pub FlipStereo: u32,
    pub FlipStereoTemporaryMono: u32,
    pub FlipStereoPreferRight: u32,
    pub BltStereoUseRight: u32,
    pub FlipWithMultiPlaneOverlay: u32,
    pub RedirectedFlip: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRESENTALLOCATIONINFO {
    pub hDeviceSpecificAllocation: usize,
    pub AllocationVirtualAddress: u32,
    pub PhysicalAddress: u64,
    pub SegmentId: u16,
    pub PhysicalAdapterIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRESENTMULTIPLANEOVERLAYLIST {
    pub LayerIndex: u32,
    pub Enabled: i32,
    pub hDeviceSpecificAllocation: usize,
    pub SegmentId: u32,
    pub Reserved: u32,
    pub PhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRESENTMULTIPLANEOVERLAYINFO {
    pub VidPnSourceId: u32,
    pub PlaneListCount: u32,
    pub pPlaneList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_PRESENT {
    pub pDmaBuffer: usize,
    pub DmaSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub pAllocationList: usize,
    pub pAllocationInfo: usize,
    pub pPresentMultiPlaneOverlayInfo: usize,
    pub pPatchLocationListOut: usize,
    pub PatchLocationListOutSize: u32,
    pub pAllocationList_1: usize,
    pub pPatchLocationListOut_1: usize,
    pub PatchLocationListOutSize_1: u32,
    pub MultipassOffset: u32,
    pub Color: u32,
    pub DstRect: u32,
    pub SrcRect: u32,
    pub SubRectCnt: u32,
    pub pDstSubRects: usize,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub DmaBufferSegmentId: u32,
    pub DmaBufferPhysicalAddress: u64,
    pub Reserved: u32,
    pub DmaBufferGpuVirtualAddress: u32,
    pub NumSrcAllocations: u32,
    pub NumDstAllocations: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_FLAGS {
    pub VerticalFlip: u32,
    pub HorizontalFlip: u32,
    pub StaticCheck: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_BLEND {
    pub AlphaBlend: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_YCbCr_FLAGS {
    pub NominalRange: u32,
    pub Bt709: u32,
    pub xvYCC: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_ATTRIBUTES {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub ClipRect: u32,
    pub Rotation: u32,
    pub Blend: u32,
    pub NumFilters: u32,
    pub pFilters: usize,
    pub VideoFrameFormat: u32,
    pub YCbCrFlags: u32,
    pub StereoFormat: u32,
    pub StereoLeftViewFrame0: i32,
    pub StereoBaseViewFrame0: i32,
    pub StereoFlipMode: u32,
    pub StretchQuality: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CHECK_MULTIPLANE_OVERLAY_SUPPORT_PLANE {
    pub hAllocation: usize,
    pub VidPnSourceId: u32,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_PLANE {
    pub LayerIndex: u32,
    pub Enabled: i32,
    pub AllocationSegment: u32,
    pub AllocationAddress: u64,
    pub hAllocation: usize,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_ATTRIBUTES2 {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub ClipRect: u32,
    pub Rotation: u32,
    pub Blend: u32,
    pub VideoFrameFormat: u32,
    pub ColorSpaceType: u32,
    pub StereoFormat: u32,
    pub StereoLeftViewFrame0: i32,
    pub StereoBaseViewFrame0: i32,
    pub StereoFlipMode: u32,
    pub StretchQuality: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_PLANE_WITH_SOURCE {
    pub hAllocation: usize,
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CHECKMULTIPLANEOVERLAYSUPPORT2 {
    pub PlaneCount: u32,
    pub pPlanes: usize,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_BITBLT {
    pub SrcRect: u32,
    pub DstRect: u32,
    pub SrcAllocationIndex: u32,
    pub DstAllocationIndex: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub Rop: u16,
    pub Rop3: u16,
    pub SrcPitch: u32,
    pub DstPitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_STRETCHBLT {
    pub SrcRect: u32,
    pub DstRect: u32,
    pub DstAllocationIndex: u32,
    pub SrcAllocationIndex: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub Mode: u32,
    pub MirrorX: u32,
    pub MirrorY: u32,
    pub Flags: u32,
    pub SrcPitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_COLORFILL {
    pub DstRect: u32,
    pub DstAllocationIndex: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub Color: u32,
    pub Rop: u16,
    pub Rop3: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_ALPHABLEND {
    pub SrcRect: u32,
    pub DstRect: u32,
    pub SrcAllocationIndex: u32,
    pub DstAllocationIndex: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub SourceConstantAlpha: u8,
    pub SourceHasAlpha: u8,
    pub SrcPitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKM_TRANSPARENTBLTFLAGS {
    pub HonorAlpha: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_TRANSPARENTBLT {
    pub SrcRect: u32,
    pub DstRect: u32,
    pub SrcAllocationIndex: u32,
    pub DstAllocationIndex: u32,
    pub Color: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub Flags: u32,
    pub SrcPitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GDIARG_CLEARTYPEBLEND {
    pub DstRect: u32,
    pub TmpSurfAllocationIndex: u32,
    pub GammaSurfAllocationIndex: u32,
    pub AlphaSurfAllocationIndex: u32,
    pub DstAllocationIndex: u32,
    pub DstToAlphaOffsetX: i32,
    pub DstToAlphaOffsetY: i32,
    pub Color: u32,
    pub Gamma: u32,
    pub NumSubRects: u32,
    pub pSubRects: usize,
    pub AlphaSurfPitch: u32,
    pub Color2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_RENDERKM_COMMAND {
    pub OpCode: u32,
    pub CommandSize: u32,
    pub BitBlt: u32,
    pub ColorFill: u32,
    pub AlphaBlend: u32,
    pub StretchBlt: u32,
    pub TransparentBlt: u32,
    pub ClearTypeBlend: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_STOPCAPTURE {
    pub hAllocation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_PRESENT_DISPLAYONLY_PROGRESS {
    pub VidPnSourceId: u32,
    pub ProgressId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_VSYNC_INFO {
    pub LayerIndex: u32,
    pub Enabled: i32,
    pub PhysicalAddress: u64,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKCB_NOTIFY_INTERRUPT_DATA_FLAGS {
    pub ValidPhysicalAdapterMask: u32,
    pub HsyncFlipCompletion: u32,
    pub EvaluateLegacyMonitoredFences: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKCB_NOTIFY_MPO_VSYNC_FLAGS {
    pub PostPresentNeeded: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_VSYNC_INFO2 {
    pub LayerIndex: u32,
    pub PresentId: u64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FLIPQUEUE_LOG_ENTRY {
    pub PresentId: u64,
    pub PresentTimestamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETFLIPQUEUELOGBUFFER {
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub NumberOfEntries: u32,
    pub LogBufferAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEFLIPQUEUELOG {
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub FirstFreeFlipQueueLogEntryIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CANCELQUEUEDFLIPS {
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub PresentIdCancelRequested: u64,
    pub PresentIdCancelled: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CANCELFLIPS_PLANE {
    pub PresentIdCancelRequested: u64,
    pub PresentIdCancelled: u64,
    pub LayerIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CANCELFLIPS {
    pub VidPnSourceId: u32,
    pub PlaneCount: u32,
    pub ppPlanes: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETINTERRUPTTARGETPRESENTID {
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub InterruptTargetPresentId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_VSYNC_INFO3 {
    pub LayerIndex: u32,
    pub FirstFreeFlipQueueLogEntryIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETALLOCATIONBACKINGSTORE {
    pub hDriverAllocation: usize,
    pub pBackingStore: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATECPUEVENTFLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATECPUEVENT {
    pub Flags: u32,
    pub hKmdCpuEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_NOTIFY_INTERRUPT_DATA {
    pub InterruptType: u32,
    pub SubmissionFenceId: u32,
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub PreemptionFenceId: u32,
    pub LastCompletedFenceId: u32,
    pub NodeOrdinal_1: u32,
    pub EngineOrdinal_1: u32,
    pub FaultedFenceId: u32,
    pub Status: u32,
    pub NodeOrdinal_2: u32,
    pub EngineOrdinal_2: u32,
    pub VidPnTargetId: u32,
    pub PhysicalAddress: u64,
    pub PhysicalAdapterMask: u32,
    pub VidPnTargetId_1: u32,
    pub VidPnTargetId_2: u32,
    pub PhysicalAdapterMask_1: u32,
    pub MultiPlaneOverlayVsyncInfoCount: u32,
    pub field_pMultiPlaneOverlayVsyncInfo: u32,
    pub DisplayOnlyPresentProgress: u32,
    pub VidPnTargetId_3: u32,
    pub ChunkInfo: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDataDriverSize: u32,
    pub Status_1: u32,
    pub FaultedFenceId_1: u32,
    pub FaultedPrimitiveAPISequenceNumber: u64,
    pub FaultedPipelineStage: u32,
    pub FaultedBindTableEntry: u32,
    pub PageFaultFlags: u32,
    pub FaultedVirtualAddress: u32,
    pub NodeOrdinal_3: u32,
    pub EngineOrdinal_3: u32,
    pub PageTableLevel: u32,
    pub FaultErrorCode: u32,
    pub FaultedProcessHandle: usize,
    pub VidPnTargetId_4: u32,
    pub PhysicalAdapterMask_2: u32,
    pub MultiPlaneOverlayVsyncInfoCount_1: u32,
    pub field_pMultiPlaneOverlayVsyncInfo_1: u32,
    pub GpuFrequency: u64,
    pub GpuClockCounter: u64,
    pub NodeOrdinal_4: u32,
    pub EngineOrdinal_4: u32,
    pub NodeOrdinal_5: u32,
    pub EngineOrdinal_5: u32,
    pub ContextSwitchFence: u64,
    pub FaultedFenceId_2: u64,
    pub FaultedVirtualAddress_1: u32,
    pub FaultedPrimitiveAPISequenceNumber_1: u64,
    pub FaultedHwQueue: usize,
    pub FaultedHwContext: usize,
    pub FaultedProcessHandle_1: usize,
    pub NodeOrdinal_6: u32,
    pub EngineOrdinal_6: u32,
    pub FaultedPipelineStage_1: u32,
    pub FaultedBindTableEntry_1: u32,
    pub PageFaultFlags_1: u32,
    pub PageTableLevel_1: u32,
    pub FaultErrorCode_1: u32,
    pub VidPnTargetId_5: u32,
    pub NotificationID: u32,
    pub NodeOrdinal_7: u32,
    pub EngineOrdinal_7: u32,
    pub NodeOrdinal_8: u32,
    pub EngineOrdinal_8: u32,
    pub hContext: usize,
    pub ContextSuspendFence: u64,
    pub VidPnTargetId_6: u32,
    pub PhysicalAdapterMask_3: u32,
    pub MultiPlaneOverlayVsyncInfoCount_2: u32,
    pub field_pMultiPlaneOverlayVsyncInfo_2: u32,
    pub GpuFrequency_1: u64,
    pub GpuClockCounter_1: u64,
    pub NodeOrdinal_9: u32,
    pub EngineOrdinal_9: u32,
    pub SignaledNativeFenceCount: u32,
    pub pSignaledNativeFenceArray: usize,
    pub hHWQueue: usize,
    pub NodeOrdinal_10: u32,
    pub EngineOrdinal_10: u32,
    pub NewState: u32,
    pub Reserved: [u32; 16],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPENALLOCATIONINFO {
    pub hAllocation: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub hDeviceSpecificAllocation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPENALLOCATIONFLAGS {
    pub Create: u32,
    pub ReadOnly: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_OPENALLOCATION {
    pub NumAllocations: u32,
    pub pOpenAllocation: usize,
    pub pPrivateDriverData: usize,
    pub PrivateDriverSize: u32,
    pub Flags: u32,
    pub SubresourceIndex: u32,
    pub SubresourceOffset: usize,
    pub Pitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CLOSEALLOCATION {
    pub NumAllocations: u32,
    pub pOpenHandleList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKCB_GETHANDLEDATAFLAGS {
    pub DeviceSpecific: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_GETHANDLEDATA {
    pub hObject: u32,
    pub Type: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_RELEASEHANDLEDATA {
    pub ReleaseHandle: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ENUMHANDLECHILDREN {
    pub hObject: u32,
    pub Index: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_GETCAPTUREADDRESS {
    pub hAllocation: u32,
    pub SegmentId: u32,
    pub PhysicalAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEVICEINFOFLAGS {
    pub GuaranteedDmaBufferContract: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEVICEINFO {
    pub DmaBufferSize: u32,
    pub DmaBufferSegmentSet: u32,
    pub DmaBufferPrivateDataSize: u32,
    pub AllocationListSize: u32,
    pub PatchLocationListSize: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATEDEVICEFLAGS {
    pub SystemDevice: u32,
    pub GdiDevice: u32,
    pub Reserved: u32,
    pub DXGK_DEVICE_RESERVED0: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEDEVICE {
    pub hDevice: usize,
    pub Flags: u32,
    pub pInfo: usize,
    pub Pasid: u32,
    pub hKmdProcess: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATECONTEXTFLAGS {
    pub SystemContext: u32,
    pub GdiContext: u32,
    pub VirtualAddressing: u32,
    pub SystemProtectedContext: u32,
    pub HwQueueSupported: u32,
    pub TestContext: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CONTEXTINFO_CAPS {
    pub NoPatchingRequired: u32,
    pub DriverManagesResidency: u32,
    pub UseIoMmu: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CONTEXTINFO {
    pub DmaBufferSize: u32,
    pub DmaBufferSegmentSet: u32,
    pub DmaBufferPrivateDataSize: u32,
    pub AllocationListSize: u32,
    pub PatchLocationListSize: u32,
    pub Reserved: u32,
    pub Caps: u32,
    pub PagingCompanionNodeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATECONTEXT {
    pub hContext: usize,
    pub NodeOrdinal: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub ContextInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HWCONTEXT_CAPS {
    pub UseIoMmu: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEHWCONTEXT {
    pub hHwContext: usize,
    pub NodeOrdinal: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData: usize,
    pub ContextCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEHWQUEUE {
    pub hHwQueue: usize,
    pub Flags: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData: usize,
    pub hHwQueueProgressFence: u32,
    pub HwQueueProgressFenceCPUVirtualAddress: usize,
    pub HwQueueProgressFenceGPUVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POINTERFLAGS {
    pub Monochrome: u32,
    pub Color: u32,
    pub MaskedColor: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETPOINTERSHAPE {
    pub Flags: u32,
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
    pub VidPnSourceId: u32,
    pub pPixels: usize,
    pub XHot: u32,
    pub YHot: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SETPOINTERPOSITIONFLAGS {
    pub Visible: u32,
    pub Procedural: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETPOINTERPOSITION {
    pub VidPnSourceId: u32,
    pub X: i32,
    pub Y: i32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GAMMARAMPCAPS {
    pub Gamma_Rgb256x3x16: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_COLORTRANSFORMCAPS {
    pub Gamma_Rgb256x3x16: u32,
    pub Gamma_Dxgi1: u32,
    pub Transform_3x4Matrix: u32,
    pub Transform_3x4Matrix_WideColor: u32,
    pub Transform_3x4Matrix_HighColor: u32,
    pub Transform_Matrix_V2: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRESENTATIONCAPS {
    pub NoScreenToScreenBlt: u32,
    pub NoOverlapScreenBlt: u32,
    pub SupportKernelModeCommandBuffer: u32,
    pub NoSameBitmapAlphaBlend: u32,
    pub NoSameBitmapStretchBlt: u32,
    pub NoSameBitmapTransparentBlt: u32,
    pub NoSameBitmapOverlappedAlphaBlend: u32,
    pub NoSameBitmapOverlappedStretchBlt: u32,
    pub DriverSupportsCddDwmInterop: u32,
    pub Reserved0: u32,
    pub AlignmentShift: u32,
    pub MaxTextureWidthShift: u32,
    pub MaxTextureHeightShift: u32,
    pub SupportAllBltRops: u32,
    pub SupportMirrorStretchBlt: u32,
    pub SupportMonoStretchBltModes: u32,
    pub StagingRectStartPitchAligned: u32,
    pub NoSameBitmapBitBlt: u32,
    pub NoSameBitmapOverlappedBitBlt: u32,
    pub Reserved1: u32,
    pub NoTempSurfaceForClearTypeBlend: u32,
    pub SupportSoftwareDeviceBitmaps: u32,
    pub NoCacheCoherentApertureMemory: u32,
    pub SupportLinearHeap: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FLIPCAPS {
    pub FlipOnVSyncWithNoWait: u32,
    pub FlipOnVSyncMmIo: u32,
    pub FlipInterval: u32,
    pub FlipImmediateMmIo: u32,
    pub FlipIndependent: u32,
    pub DdiPresentForIFlip: u32,
    pub FlipImmediateOnHSync: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDSCHCAPS {
    pub MultiEngineAware: u32,
    pub VSyncPowerSaveAware: u32,
    pub PreemptionAware: u32,
    pub NoDmaPatching: u32,
    pub CancelCommandAware: u32,
    pub No64BitAtomics: u32,
    pub LowIrqlPreemptCommand: u32,
    pub HwQueuePacketCap: u32,
    pub NativeGpuFence: u32,
    pub OptimizedNativeFenceSignaledInterrupt: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PAGETABLEUPDATEADDRESS {
    pub CpuVirtual: usize,
    pub GpuPhysical: u32,
    pub GpuVirtual: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PAGE_TABLE_LEVEL_DESC {
    pub PageTableIndexBitCount: u32,
    pub PageTableSegmentId: u32,
    pub PagingProcessPageTableSegmentId: u32,
    pub PageTableSizeInBytes: u32,
    pub PageTableAlignmentInBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_UPDATEPAGETABLEFLAGS {
    pub Repeat: u32,
    pub InitialUpdate: u32,
    pub NotifyEviction: u32,
    pub Use64KBPages: u32,
    pub NativeFence: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYGPUMMUCAPSIN {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYPAGETABLELEVELDESCIN {
    pub LevelIndex: u16,
    pub PhysicalAdapterIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYHISTORYBUFFERPRECISIONIN {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUMMUCAPS {
    pub ReadOnlyMemorySupported: u32,
    pub NoExecuteMemorySupported: u32,
    pub ZeroInPteSupported: u32,
    pub ExplicitPageTableInvalidation: u32,
    pub CacheCoherentMemorySupported: u32,
    pub PageTableUpdateRequireAddressSpaceIdle: u32,
    pub LargePageSupported: u32,
    pub DualPteSupported: u32,
    pub AllowNonAlignedLargePageAddress: u32,
    pub SysMem64KBPageSupported: u32,
    pub InvalidTlbEntriesNotCached: u32,
    pub SysMemLargePageSupported: u32,
    pub CachedPageTables: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Value: u32,
    pub PageTableUpdateMode: u32,
    pub VirtualAddressBitCount: u32,
    pub LeafPageTableSizeFor64KPagesInBytes: u32,
    pub PageTableLevelCount: u32,
    pub SourcePageTableVaInTransfer: u32,
    pub Reserved_5: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYPHYSICALADAPTERCAPSIN {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PHYSICALADAPTERFLAGS {
    pub IoMmuSupported: u32,
    pub GpuMmuSupported: u32,
    pub MovePagingSupported: u32,
    pub VPRPagingContextRequired: u32,
    pub AllowHardwareProtectedNoVpr: u32,
    pub VirtualCopyEngineSupported: u32,
    pub GpuVaIommuRequired: u32,
    pub GpuVaIommuGlobalRequired: u32,
    pub GpuVaIommuCacheCoherent: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PHYSICALADAPTERCAPS {
    pub NumExecutionNodes: u16,
    pub PagingNodeIndex: u16,
    pub DxgkPhysicalAdapterHandle: usize,
    pub Flags: u32,
    pub VPRPagingNode: u32,
    pub VirtualCopyNodeIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CPUHOSTAPERTURE {
    pub PhysicalAddress: u64,
    pub SizeInPages: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FRAMEBUFFERSAVEAREA {
    pub MaximumSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PHYSICAL_MEMORY_RANGE {
    pub BaseAddress: u64,
    pub NumberOfBytes: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HARDWARERESERVEDRANGES {
    pub NumRanges: u32,
    pub field_pPhysicalRanges: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDMMCAPS {
    pub OutOfOrderLock: u32,
    pub DedicatedPagingEngine: u32,
    pub PagingEngineCanSwizzle: u32,
    pub SectionBackedPrimary: u32,
    pub CrossAdapterResource: u32,
    pub VirtualAddressingSupported: u32,
    pub GpuMmuSupported: u32,
    pub IoMmuSupported: u32,
    pub ReplicateGdiContent: u32,
    pub NonCpuVisiblePrimary: u32,
    pub ParavirtualizationSupported: u32,
    pub IoMmuSecureModeSupported: u32,
    pub DisableSelfRefreshVRAMInS3: u32,
    pub IoMmuSecureModeRequired: u32,
    pub MapAperture2Supported: u32,
    pub CrossAdapterResourceTexture: u32,
    pub CrossAdapterResourceScanout: u32,
    pub AlwaysPoweredVRAM: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub Reserved_6: u32,
    pub Reserved_7: u32,
    pub Reserved_8: u32,
    pub Value: u32,
    pub PagingNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUENGINETOPOLOGY {
    pub NbAsymetricProcessingNodes: u32,
    pub Reserved: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HWQUEUEDFLIP_CAPS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DRIVERCAPS {
    pub HighestAcceptableAddress: u64,
    pub MaxAllocationListSlotId: u32,
    pub ApertureSegmentCommitLimit: usize,
    pub MaxPointerWidth: u32,
    pub MaxPointerHeight: u32,
    pub PointerCaps: u32,
    pub InterruptMessageNumber: u32,
    pub NumberOfSwizzlingRanges: u32,
    pub MaxOverlays: u32,
    pub GammaRampCaps: u32,
    pub ColorTransformCaps: u32,
    pub PresentationCaps: u32,
    pub MaxQueuedFlipOnVSync: u32,
    pub FlipCaps: u32,
    pub SchedulingCaps: u32,
    pub MemoryManagementCaps: u32,
    pub GpuEngineTopology: u32,
    pub WDDMVersion: u32,
    pub Reserved: u32,
    pub Reserved1: u32,
    pub PreemptionCaps: u32,
    pub SupportNonVGA: u8,
    pub SupportSmoothRotation: u8,
    pub SupportPerEngineTDR: u8,
    pub SupportDirectFlip: u8,
    pub SupportMultiPlaneOverlay: u8,
    pub SupportRuntimePowerManagement: u8,
    pub SupportSurpriseRemovalInHibernation: u8,
    pub HybridDiscrete: u8,
    pub MaxOverlayPlanes: u32,
    pub HybridIntegrated: u8,
    pub InternalGpuVirtualAddressRangeStart: u32,
    pub InternalGpuVirtualAddressRangeEnd: u32,
    pub SupportSurpriseRemoval: u8,
    pub SupportMultiPlaneOverlayImmediateFlip: u8,
    pub CursorScaledWithMultiPlaneOverlayPlane0: u8,
    pub HybridAcpiChainingRequired: u8,
    pub MaxQueuedMultiPlaneOverlayFlipVSync: u32,
    pub SupportContextlessPresent: u32,
    pub Detachable: u32,
    pub VirtualGpuOnly: u32,
    pub ComputeOnly: u32,
    pub IndependentVidPnVSyncControl: u32,
    pub NoHybridDiscreteDListDllSupport: u32,
    pub DisplayableSupport: u32,
    pub NoHybridDiscreteDListDllMuxSupport: u32,
    pub CursorDoesNotSupportXorBlendWithMultiPlaneOverlay: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub Reserved_6: u32,
    pub Reserved_7: u32,
    pub Value: u32,
    pub MaxHwQueuedFlips: u32,
    pub HwQueuedFlipCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_WDDMDEVICECAPSIN {
    pub DxgkrnlInterfaceVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_WDDMDEVICECAPS {
    pub WDDMVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUPCAPS {
    pub VirtualMachineHibernation: u32,
    pub HotDriverUpdate: u32,
    pub LiveMigration: u32,
    pub ScatterMapReserve: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTFLAGS {
    pub Aperture: u32,
    pub Agp: u32,
    pub CpuVisible: u32,
    pub UseBanking: u32,
    pub CacheCoherent: u32,
    pub PitchAlignment: u32,
    pub PopulatedFromSystemMemory: u32,
    pub PreservedDuringStandby: u32,
    pub PreservedDuringHibernate: u32,
    pub PartiallyPreservedDuringHibernate: u32,
    pub DirectFlip: u32,
    pub Use64KBPages: u32,
    pub ReservedSysMem: u32,
    pub SupportsCpuHostAperture: u32,
    pub SupportsCachedCpuHostAperture: u32,
    pub ApplicationTarget: u32,
    pub VprSupported: u32,
    pub VprPreservedDuringStandby: u32,
    pub EncryptedPagingSupported: u32,
    pub LocalBudgetGroup: u32,
    pub NonLocalBudgetGroup: u32,
    pub PopulatedByReservedDDRByFirmware: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTDESCRIPTOR {
    pub BaseAddress: u64,
    pub CpuTranslatedAddress: u64,
    pub Size: usize,
    pub NbOfBanks: u32,
    pub pBankRangeTable: usize,
    pub CommitLimit: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTIN {
    pub AgpApertureBase: u64,
    pub AgpApertureSize: i64,
    pub AgpFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTOUT {
    pub NbSegment: u32,
    pub pSegmentDescriptor: usize,
    pub PagingBufferSegmentId: u32,
    pub PagingBufferSize: u32,
    pub PagingBufferPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTFLAGS2 {
    pub Aperture: u32,
    pub PopulatedFromSystemMemory: u32,
    pub SystemMemoryReservedByBios: u32,
    pub CpuVisible: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTDESCRIPTOR2 {
    pub Flags: u32,
    pub Size: usize,
    pub pMdl: usize,
    pub BaseAddress: u64,
    pub CpuTranslatedAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTOUT2 {
    pub SegmentCount: u32,
    pub pSegmentDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTDESCRIPTOR3 {
    pub Flags: u32,
    pub BaseAddress: u64,
    pub CpuTranslatedAddress: u64,
    pub Size: usize,
    pub NbOfBanks: u32,
    pub pBankRangeTable: usize,
    pub CommitLimit: usize,
    pub SystemMemoryEndAddress: usize,
    pub Reserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTOUT3 {
    pub NbSegment: u32,
    pub pSegmentDescriptor: usize,
    pub PagingBufferSegmentId: u32,
    pub PagingBufferSize: u32,
    pub PagingBufferPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTIN4 {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTDESCRIPTOR4 {
    pub Flags: u32,
    pub BaseAddress: u64,
    pub Size: usize,
    pub CommitLimit: usize,
    pub SystemMemoryEndAddress: usize,
    pub CpuTranslatedAddress: u64,
    pub CpuHostAperture: u32,
    pub NumInvalidMemoryRanges: u32,
    pub VprRangeStartOffset: usize,
    pub VprRangeSize: usize,
    pub VprAlignment: u32,
    pub NumVprSupported: u32,
    pub VprReserveSize: u32,
    pub NumUEFIFrameBufferRanges: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTOUT4 {
    pub NbSegment: u32,
    pub pSegmentDescriptor: usize,
    pub PagingBufferSegmentId: u32,
    pub PagingBufferSize: u32,
    pub PagingBufferPrivateDataSize: u32,
    pub SegmentDescriptorStride: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MEMORYRANGE {
    pub SegmentOffset: u64,
    pub SizeInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTMEMORYSTATE {
    pub DriverSegmentId: u16,
    pub PhysicalAdapterIndex: u16,
    pub NumInvalidMemoryRanges: u32,
    pub NumUEFIFrameBufferRanges: u32,
    pub NumInvalidMemoryRanges_1: u32,
    pub pMemoryRanges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAY_DRIVERCAPS_EXTENSION {
    pub SecureDisplaySupport: u32,
    pub VirtualModeSupport: u32,
    pub Reserved: u32,
    pub NonSpecificPrimarySupport: u32,
    pub HdrFP16ScanoutSupport: u32,
    pub HdrARGB10ScanoutSupport: u32,
    pub Hdr10MetadataSupport: u32,
    pub VirtualRefreshRateSupport: u32,
    pub SupportUsb4Targets: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_COLORIMETRY {
    pub RedPoint: u32,
    pub GreenPoint: u32,
    pub BluePoint: u32,
    pub WhitePoint: u32,
    pub MinLuminance: u32,
    pub MaxLuminance: u32,
    pub MaxFullFrameLuminance: u32,
    pub FormatBitDepths: u32,
    pub StandardColorimetryFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DEVICE_TYPE_CAPS {
    pub Discrete: u32,
    pub Detachable: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYINTEGRATEDDISPLAYOUT {
    pub Flags: u32,
    pub NativeTiming: u32,
    pub LinkCapabilities: u32,
    pub Colorimetry: u32,
    pub DisplayTechnology: u32,
    pub IntendedUsage: u32,
    pub Instance: u8,
    pub DescriptorType: u32,
    pub Descriptor: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QAITARGETIN {
    pub TargetId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DKGK_GAMMA_DATA_CAP {
    pub DataType: u32,
    pub BitCountOfInteger: u32,
    pub BitCountOfFraction: u32,
    pub BitCountOfExponent: u32,
    pub BitCountOfMantissa: u32,
    pub Value: u32,
    pub NumericRangeMin: u32,
    pub NumericRangeMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GAMMA_1DLUT_CAP {
    pub NumberOfLUTEntries: u32,
    pub GammaDataCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GAMMA_MATRIX_CAP {
    pub MatrixSizeX: u32,
    pub MatrixSizeY: u32,
    pub Value: u32,
    pub GammaDataCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TARGET_GAMMA_CAPS {
    pub Version: u32,
    pub LookupTable1DDegammaCap: u32,
    pub ColorMatrix3x3Cap: u32,
    pub LookupTable1DRegammaCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QAISOURCEIN {
    pub Source: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERY_SCANOUT_CAPS_OUT {
    pub Caps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYDISPLAYIDIN {
    pub TargetId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYDISPLAYIDOUT {
    pub Length: u32,
    pub pDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYINTEGRATEDDISPLAYOUT2 {
    pub Flags: u32,
    pub NativeTiming: u32,
    pub LinkCapabilities: u32,
    pub Colorimetry: u32,
    pub DisplayTechnology: u32,
    pub IntendedUsage: u32,
    pub Instance: u8,
    pub DescriptorType: u32,
    pub DitheringSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PHYSICAL_MEMORY_CAPS {
    pub HighestVisibleAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_IOMMU_CAPS {
    pub IommuIsolationSupported: u32,
    pub IommuIsolationRequired: u32,
    pub DmaRemappingSupported: u32,
    pub GpuVaIommuRequired: u32,
    pub GpuVaIommuGlobalRequired: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ADL_FLAGS {
    pub Contiguous: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ADL {
    pub PageCount: u32,
    pub Flags: u32,
    pub BasePageNumber: u32,
    pub field_Pages: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NATIVE_FENCE_CAPS {
    pub MonitoredValueStride: u32,
    pub MapToGpuSystemProcess: u8,
    pub MinimumAddress: u32,
    pub MaximumAddress: u32,
    pub Reserved: [u8; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATENATIVEFENCE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATENATIVEFENCE {
    pub hGlobalNativeFence: usize,
    pub Type: u32,
    pub CurrentValueSystemProcessGpuVa: u32,
    pub MonitoredValueSystemProcessGpuVa: u32,
    pub pPrivateDriverData: [u8; 1],
    pub Flags: u32,
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OPENNATIVEFENCE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_OPENNATIVEFENCE {
    pub hGlobalNativeFence: usize,
    pub hLocalNativeFence: usize,
    pub hDevice: usize,
    pub CurrentValueGpuVa: u32,
    pub MonitoredValueGpuVa: u32,
    pub Flags: u32,
    pub pPrivateDriverData: [u8; 1],
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CLOSENATIVEFENCE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CLOSENATIVEFENCE {
    pub hLocalNativeFence: usize,
    pub Flags: u32,
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DESTROYNATIVEFENCE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESTROYNATIVEFENCE {
    pub hGlobalNativeFence: usize,
    pub Flags: u32,
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEMONITOREDVALUES_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEMONITOREDVALUES {
    pub NativeFenceArray: usize,
    pub UpdatedValueArray: usize,
    pub MonitoredValueKernelCpuVa: usize,
    pub NumFences: u32,
    pub Flags: u32,
    pub Reserved: [u8; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_UPDATECURRENTVALUESFROMCPU_FLAGS {
    pub AlwaysSignaled: u32,
    pub NotificationOnly: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATECURRENTVALUESFROMCPU {
    pub NativeFenceArray: usize,
    pub UpdatedValueArray: usize,
    pub CurrentValueKernelCpuVa: usize,
    pub NumFences: u32,
    pub Flags: u32,
    pub Reserved: [u8; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETNATIVEFENCELOGBUFFER_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETNATIVEFENCELOGBUFFER {
    pub hHwQueue: usize,
    pub NumberOfEntries: u32,
    pub LogBufferCpuVa: usize,
    pub LogBufferGpuVa: u32,
    pub LogBufferSystemProcessGpuVa: u32,
    pub Flags: u32,
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATENATIVEFENCELOGS_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATENATIVEFENCELOGS {
    pub NumberOfQueues: u32,
    pub hHWQueue: usize,
    pub Flags: u32,
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_RUNTIME_STATE {
    pub TransitionLatency: u64,
    pub ResidencyRequirement: u64,
    pub NominalPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_COMPONENT_MAPPING {
    pub NodeIndex: u32,
    pub VidPnSourceID: u32,
    pub VidPnTargetID: u32,
    pub SegmentID: u32,
    pub SharedTypeFlag: u16,
    pub DriverCustomValueSet: u16,
    pub SharedType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_COMPONENT_INDEX {
    pub PhysicalAdapterComponentIndex: u16,
    pub PhysicalAdapterIndex: u16,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_COMPONENT_FLAGS {
    pub Reserved0: u32,
    pub DriverCompletesFStateTransition: u32,
    pub TransitionTo_F0_OnDx: u32,
    pub NoDebounce: u32,
    pub ActiveInD3: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_RUNTIME_COMPONENT {
    pub StateCount: u32,
    pub States: [u32; 1],
    pub ComponentMapping: u32,
    pub Flags: u32,
    pub ComponentGuid: u32,
    pub ComponentName: [u8; 1],
    pub ProviderCount: u32,
    pub Providers: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_P_STATE {
    pub OperatingFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_COMPONENT_P_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_POWER_P_COMPONENT {
    pub StateCount: u32,
    pub States: [u32; 1],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYADAPTERINFOFLAGS {
    pub VirtualMachineData: u32,
    pub SecureVirtualMachine: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYADAPTERINFO {
    pub Type: u32,
    pub pInputData: usize,
    pub InputDataSize: u32,
    pub pOutputData: usize,
    pub OutputDataSize: u32,
    pub Flags: u32,
    pub hKmdProcessHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_ACQUIRESWIZZLINGRANGE {
    pub hAllocation: usize,
    pub PrivateDriverData: u32,
    pub RangeId: u32,
    pub SegmentId: u32,
    pub RangeSize: usize,
    pub CPUTranslatedAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RELEASESWIZZLINGRANGE {
    pub hAllocation: usize,
    pub PrivateDriverData: u32,
    pub RangeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONINFOFLAGS {
    pub CpuVisible: u32,
    pub PermanentSysMem: u32,
    pub Cached: u32,
    pub Protected: u32,
    pub ExistingSysMem: u32,
    pub ExistingKernelSysMem: u32,
    pub FromEndOfSegment: u32,
    pub Swizzled: u32,
    pub Overlay: u32,
    pub Capture: u32,
    pub UseAlternateVA: u32,
    pub SynchronousPaging: u32,
    pub LinkMirrored: u32,
    pub LinkInstanced: u32,
    pub HistoryBuffer: u32,
    pub AccessedPhysically: u32,
    pub ExplicitResidencyNotification: u32,
    pub HardwareProtected: u32,
    pub CpuVisibleOnDemand: u32,
    pub Reserved: u32,
    pub DXGK_ALLOC_RESERVED16: u32,
    pub DXGK_ALLOC_RESERVED15: u32,
    pub DXGK_ALLOC_RESERVED14: u32,
    pub DXGK_ALLOC_RESERVED13: u32,
    pub DXGK_ALLOC_RESERVED12: u32,
    pub DXGK_ALLOC_RESERVED11: u32,
    pub DXGK_ALLOC_RESERVED10: u32,
    pub DXGK_ALLOC_RESERVED9: u32,
    pub DXGK_ALLOC_RESERVED4: u32,
    pub DXGK_ALLOC_RESERVED3: u32,
    pub DXGK_ALLOC_RESERVED2: u32,
    pub DXGK_ALLOC_RESERVED1: u32,
    pub DXGK_ALLOC_RESERVED0: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONINFOFLAGS_WDDM2_0 {
    pub CpuVisible: u32,
    pub PermanentSysMem: u32,
    pub Cached: u32,
    pub Protected: u32,
    pub ExistingSysMem: u32,
    pub ExistingKernelSysMem: u32,
    pub FromEndOfSegment: u32,
    pub DisableLargePageMapping: u32,
    pub Overlay: u32,
    pub Capture: u32,
    pub CreateInVpr: u32,
    pub Reserved00: u32,
    pub DXGK_ALLOC_RESERVED17: u32,
    pub Reserved02: u32,
    pub MapApertureCpuVisible: u32,
    pub Reserved03: u32,
    pub HistoryBuffer: u32,
    pub AccessedPhysically: u32,
    pub ExplicitResidencyNotification: u32,
    pub HardwareProtected: u32,
    pub CpuVisibleOnDemand: u32,
    pub DXGK_ALLOC_RESERVED16: u32,
    pub DXGK_ALLOC_RESERVED15: u32,
    pub DXGK_ALLOC_RESERVED14: u32,
    pub DXGK_ALLOC_RESERVED13: u32,
    pub DXGK_ALLOC_RESERVED12: u32,
    pub DXGK_ALLOC_RESERVED11: u32,
    pub DXGK_ALLOC_RESERVED10: u32,
    pub DXGK_ALLOC_RESERVED9: u32,
    pub DXGK_ALLOC_RESERVED4: u32,
    pub DXGK_ALLOC_RESERVED3: u32,
    pub DXGK_ALLOC_RESERVED2: u32,
    pub DXGK_ALLOC_RESERVED1: u32,
    pub DXGK_ALLOC_RESERVED0: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONINFOFLAGS2 {
    pub ShareBackingStoreWithKmd: u32,
    pub NoImplicitSynchronization: u32,
    pub DisablePartialResidency: u32,
    pub RestrictedToSingleSegment: u32,
    pub NotifyEviction: u32,
    pub NotifyIoMmuUnmap: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONUSAGEINFO1 {
    pub PrivateFormat: u32,
    pub Swizzled: u32,
    pub MipMap: u32,
    pub Cube: u32,
    pub Volume: u32,
    pub Vertex: u32,
    pub Index: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub Format: u32,
    pub PrivateFormat_1: u32,
    pub SwizzledFormat: u32,
    pub ByteOffset: u32,
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
    pub Depth: u32,
    pub SlicePitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONUSAGEHINT {
    pub Version: u32,
    pub v1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONINFO {
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub Alignment: u32,
    pub MinimumPageSize: u32,
    pub RecommendedPageSize: u32,
    pub Size: usize,
    pub PitchAlignedSize: usize,
    pub HintedBank: u32,
    pub PreferredSegment: u32,
    pub SupportedReadSegmentSet: u32,
    pub MmuSet: u32,
    pub SupportedWriteSegmentSet: u32,
    pub EvictionSegmentSet: u32,
    pub MaximumRenamingListLength: u32,
    pub PhysicalAdapterIndex: u32,
    pub hAllocation: usize,
    pub Flags: u32,
    pub FlagsWddm2: u32,
    pub pAllocationUsageHint: usize,
    pub AllocationPriority: u32,
    pub Flags2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ALLOCATIONINFO_TEST {
    pub Alignment: u32,
    pub Size: u64,
    pub PitchAlignedSize: u64,
    pub HintedBank: u32,
    pub PreferredSegment: u32,
    pub SupportedReadSegmentSet: u32,
    pub SupportedWriteSegmentSet: u32,
    pub EvictionSegmentSet: u32,
    pub PhysicalAdapterIndex: u32,
    pub FlagsWddm2: u32,
    pub AllocationPriority: u32,
    pub Flags2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATEALLOCATIONFLAGS {
    pub Resource: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEALLOCATION {
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub NumAllocations: u32,
    pub pAllocationInfo: usize,
    pub hResource: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATECONTEXTALLOCATIONFLAGS {
    pub SharedAcrossContexts: u32,
    pub MapGpuVirtualAddress: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_CREATECONTEXTALLOCATION {
    pub ContextAllocationFlags: u32,
    pub hAdapter: usize,
    pub hDevice: usize,
    pub hContext: usize,
    pub hDriverAllocation: usize,
    pub Size: usize,
    pub Alignment: u32,
    pub SupportedSegmentSet: u32,
    pub EvictionSegmentSet: u32,
    pub PreferredSegment: u32,
    pub HintedBank: u32,
    pub Flags: u32,
    pub hAllocation: usize,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DESCRIBEALLOCATIONFLAGS {
    pub CheckDisplayMode: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESCRIBEALLOCATION {
    pub hAllocation: usize,
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub MultisampleMethod: u32,
    pub RefreshRate: u32,
    pub PrivateDriverFormatAttribute: u32,
    pub Flags: u32,
    pub Rotation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_FENCESTORAGEFLAGS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_FENCESTORAGESURFACEDATA {
    pub PhysicalAdapterIndex: u32,
    pub FenceStorageValueType: u32,
    pub FenceStorageType: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData: usize,
    pub Flags: u32,
    pub Reserved: u32,
    pub AllocationInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETSTANDARDALLOCATIONDRIVERDATA {
    pub StandardAllocationType: u32,
    pub pCreateSharedPrimarySurfaceData: usize,
    pub pCreateShadowSurfaceData: usize,
    pub pCreateStagingSurfaceData: usize,
    pub pCreateGdiSurfaceData: usize,
    pub pCreateVirtualGpuSurfaceData: usize,
    pub pCreateFenceStorageData: usize,
    pub pAllocationPrivateDriverData: usize,
    pub AllocationPrivateDriverDataSize: u32,
    pub pResourcePrivateDriverData: usize,
    pub ResourcePrivateDriverDataSize: u32,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DESTROYALLOCATIONFLAGS {
    pub DestroyResource: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESTROYALLOCATION {
    pub NumAllocations: u32,
    pub pAllocationList: usize,
    pub hResource: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PATCHFLAGS {
    pub Paging: u32,
    pub Present: u32,
    pub RedirectedPresent: u32,
    pub NullRendering: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_PATCH {
    pub hDevice: usize,
    pub hContext: usize,
    pub DmaBufferSegmentId: u32,
    pub DmaBufferPhysicalAddress: u64,
    pub pDmaBuffer: usize,
    pub DmaBufferSize: u32,
    pub DmaBufferSubmissionStartOffset: u32,
    pub DmaBufferSubmissionEndOffset: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub DmaBufferPrivateDataSubmissionStartOffset: u32,
    pub DmaBufferPrivateDataSubmissionEndOffset: u32,
    pub pAllocationList: usize,
    pub AllocationListSize: u32,
    pub pPatchLocationList: usize,
    pub PatchLocationListSize: u32,
    pub PatchLocationListSubmissionStart: u32,
    pub PatchLocationListSubmissionLength: u32,
    pub SubmissionFenceId: u32,
    pub Flags: u32,
    pub EngineOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SUBMITCOMMANDFLAGS {
    pub Paging: u32,
    pub Present: u32,
    pub RedirectedPresent: u32,
    pub NullRendering: u32,
    pub Flip: u32,
    pub FlipWithNoWait: u32,
    pub ContextSwitch: u32,
    pub Resubmission: u32,
    pub VirtualMachineData: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SUBMITCOMMAND {
    pub hDevice: usize,
    pub hContext: usize,
    pub DmaBufferSegmentId: u32,
    pub DmaBufferPhysicalAddress: u64,
    pub DmaBufferSize: u32,
    pub DmaBufferSubmissionStartOffset: u32,
    pub DmaBufferSubmissionEndOffset: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub DmaBufferPrivateDataSubmissionStartOffset: u32,
    pub DmaBufferPrivateDataSubmissionEndOffset: u32,
    pub SubmissionFenceId: u32,
    pub VidPnSourceId: u32,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub EngineOrdinal: u32,
    pub DmaBufferVirtualAddress: u32,
    pub NodeOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PREEMPTCOMMANDFLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_PREEMPTCOMMAND {
    pub PreemptionFenceId: u32,
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CANCELCOMMAND {
    pub hContext: usize,
    pub pDmaBuffer: usize,
    pub DmaBufferSize: u32,
    pub DmaBufferSubmissionStartOffset: u32,
    pub DmaBufferSubmissionEndOffset: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub DmaBufferPrivateDataSubmissionStartOffset: u32,
    pub DmaBufferPrivateDataSubmissionEndOffset: u32,
    pub pAllocationList: usize,
    pub AllocationListSize: u32,
    pub pPatchLocationList: usize,
    pub PatchLocationListSize: u32,
    pub PatchLocationListSubmissionStart: u32,
    pub PatchLocationListSubmissionLength: u32,
    pub DmaBufferVirtualAddress: u32,
    pub DmaBufferUmdPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYCURRENTFENCE {
    pub CurrentFence: u32,
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONTROLINTERRUPT2 {
    pub InterruptType: u32,
    pub InterruptState: u32,
    pub CrtcVsyncState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONTROLINTERRUPT3 {
    pub InterruptType: u32,
    pub InterruptState: u32,
    pub CrtcVsyncState: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TRANSFERFLAGS {
    pub Swizzle: u32,
    pub Unswizzle: u32,
    pub AllocationIsIdle: u32,
    pub TransferStart: u32,
    pub TransferEnd: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISCARDCONTENTFLAGS {
    pub AllocationIsIdle: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MAPAPERTUREFLAGS {
    pub CacheCoherent: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_COPY_RANGE {
    pub NumPageTableEntries: u32,
    pub SrcPageTableAddress: u32,
    pub DstPageTableAddress: u32,
    pub SrcStartPteIndex: u32,
    pub DstStartPteIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_FLUSHTLB {
    pub RootPageTableAddress: u32,
    pub hProcess: usize,
    pub StartVirtualAddress: u32,
    pub EndVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_UPDATEPAGETABLE {
    pub PageTableLevel: u32,
    pub hAllocation: usize,
    pub PageTableAddress: u32,
    pub pPageTableEntries: usize,
    pub StartIndex: u32,
    pub NumPageTableEntries: u32,
    pub Reserved0: u32,
    pub Flags: u32,
    pub DriverProtection: u64,
    pub AllocationOffsetInBytes: u64,
    pub hProcess: usize,
    pub UpdateMode: u32,
    pub pPageTableEntries64KB: usize,
    pub FirstPteVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_FILLVIRTUAL {
    pub hAllocation: usize,
    pub AllocationOffsetInBytes: u64,
    pub FillSizeInBytes: u64,
    pub FillPattern: u32,
    pub DestinationVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TRANSFERVIRTUALFLAGS {
    pub Src64KBPages: u32,
    pub Dst64KBPages: u32,
    pub Reserved: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_TRANSFERVIRTUAL {
    pub hAllocation: usize,
    pub AllocationOffsetInBytes: u64,
    pub TransferSizeInBytes: u64,
    pub SourceVirtualAddress: u32,
    pub DestinationVirtualAddress: u32,
    pub SourcePageTable: u32,
    pub TransferDirection: u32,
    pub Flags: u32,
    pub DestinationPageTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_NOTIFYRESIDENCY {
    pub hAllocation: usize,
    pub PhysicalAddress: u32,
    pub Resident: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_COPYPAGETABLEENTRIES {
    pub NumRanges: u32,
    pub pRanges: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_UPDATECONTEXTALLOCATION {
    pub ContextAllocation: u32,
    pub ContextAllocationSize: u64,
    pub pDriverPrivateData: usize,
    pub DriverPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_SIGNALMONITOREDFENCE {
    pub MonitoredFenceGpuVa: u32,
    pub MonitoredFenceValue: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FENCE_RESIDENCY_INFO {
    pub hGlobalNativeFence: usize,
    pub CurrentValuePhysicalAddress: u32,
    pub MonitoredValuePhysicalAddress: u32,
    pub CurrentValueKernelCpuVa: usize,
    pub MonitoredValueKernelCpuVa: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_NOTIFY_FENCE_RESIDENCY {
    pub NumFences: u32,
    pub Reserved: u32,
    pub FenceResidencyInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_MAPMMU {
    pub hAllocation: usize,
    pub VirtualAddress: u64,
    pub MmuId: u32,
    pub SegmentId: u32,
    pub AllocationOffsetInPages: u32,
    pub Adl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_UNMAPMMU {
    pub hAllocation: usize,
    pub VirtualAddress: u64,
    pub MmuId: u32,
    pub Reserved0: u32,
    pub AllocationOffset: u32,
    pub NumberOfPages: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_NOTIFYRESIDENCY2 {
    pub hAllocation: usize,
    pub AllocationOffsetInPages: u32,
    pub SizeInPages: u32,
    pub SegmentId: u32,
    pub Padding0: u32,
    pub PhysicalAddress: u32,
    pub Mdl: usize,
    pub Resident: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NOTIFYALLOCFLAGS {
    pub Eviction: u32,
    pub IoMmuUnmap: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BUILDPAGINGBUFFER_NOTIFYALLOC {
    pub hAllocation: usize,
    pub hKmdProcessHandle: usize,
    pub Flags: u32,
    pub OffsetInBytes: u64,
    pub SizeInBytes: u64,
    pub GpuVirtualAddressAtOffset: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_BUILDPAGINGBUFFER {
    pub pDmaBuffer: usize,
    pub DmaSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub Operation: u32,
    pub MultipassOffset: u32,
    pub hAllocation: usize,
    pub TransferOffset: u32,
    pub TransferSize: usize,
    pub SegmentId: u32,
    pub SegmentAddress: i64,
    pub pMdl: usize,
    pub SegmentId_1: u32,
    pub SegmentAddress_1: i64,
    pub pMdl_1: usize,
    pub Flags: u32,
    pub MdlOffset: u32,
    pub hAllocation_1: usize,
    pub FillSize: usize,
    pub FillPattern: u32,
    pub SegmentId_2: u32,
    pub SegmentAddress_2: i64,
    pub hAllocation_2: usize,
    pub Flags_1: u32,
    pub SegmentId_3: u32,
    pub SegmentAddress_3: u64,
    pub SegmentId_4: u32,
    pub PhysicalAddress: u64,
    pub SegmentId_5: u32,
    pub PhysicalAddress_1: u64,
    pub hDevice: usize,
    pub hAllocation_3: usize,
    pub SegmentId_6: u32,
    pub OffsetInPages: usize,
    pub NumberOfPages: usize,
    pub pMdl_2: usize,
    pub Flags_2: u32,
    pub MdlOffset_1: u32,
    pub hDevice_1: usize,
    pub hAllocation_4: usize,
    pub SegmentId_7: u32,
    pub OffsetInPages_1: usize,
    pub NumberOfPages_1: usize,
    pub DummyPage: u64,
    pub hAllocation_5: usize,
    pub TransferOffset_1: u32,
    pub TransferSize_1: usize,
    pub SegmentId_8: u32,
    pub SegmentAddress_4: i64,
    pub pMdl_3: usize,
    pub SegmentId_9: u32,
    pub SegmentAddress_5: i64,
    pub pMdl_4: usize,
    pub Flags_3: u32,
    pub SwizzlingRangeId: u32,
    pub SwizzlingRangeData: u32,
    pub hAllocation_6: usize,
    pub SegmentId_10: u32,
    pub SegmentAddress_6: i64,
    pub pMdl_5: usize,
    pub VirtualAddress: usize,
    pub GpuVirtualAddress: u32,
    pub TransferVirtual: u32,
    pub FillVirtual: u32,
    pub UpdatePageTable: u32,
    pub FlushTlb: u32,
    pub CopyPageTableEntries: u32,
    pub UpdateContextAllocation: u32,
    pub NotifyResidency: u32,
    pub SignalMonitoredFence: u32,
    pub hDevice_2: usize,
    pub hAllocation_7: usize,
    pub SegmentId_11: u32,
    pub OffsetInPages_2: usize,
    pub NumberOfPages_2: usize,
    pub Adl: u32,
    pub Flags_4: u32,
    pub AdlOffset: u32,
    pub CpuVisibleAddress: usize,
    pub NotifyFenceResidency: u32,
    pub MmapMmu: u32,
    pub UnmapMmu: u32,
    pub NotifyResidency2: u32,
    pub NotifyAllocation: u32,
    pub Reserved: [u32; 64],
    pub hSystemContext: usize,
    pub DmaBufferGpuVirtualAddress: u32,
    pub DmaBufferWriteOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETROOTPAGETABLE {
    pub hContext: usize,
    pub Address: u32,
    pub NumEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETROOTPAGETABLESIZE {
    pub NumberOfPte: u32,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CREATEPROCESSFLAGS {
    pub SystemProcess: u32,
    pub GdiProcess: u32,
    pub VirtualMachineProcess: u32,
    pub VirtualMachineWorkerProcess: u32,
    pub SessionIsolatedContainer: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEPROCESS {
    pub hDxgkProcess: usize,
    pub hKmdProcess: usize,
    pub Flags: u32,
    pub NumPasid: u32,
    pub pPasid: usize,
    pub hKmdVmWorkerProcess: usize,
    pub ProcessNameLength: u32,
    pub pProcessName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SUBMITCOMMANDVIRTUAL {
    pub hContext: usize,
    pub DmaBufferVirtualAddress: u32,
    pub DmaBufferSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub DmaBufferUmdPrivateDataSize: u32,
    pub SubmissionFenceId: u32,
    pub VidPnSourceId: u32,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub EngineOrdinal: u32,
    pub NodeOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SUBMITCOMMANDTOHWQUEUE {
    pub hHwQueue: usize,
    pub HwQueueProgressFenceId: u64,
    pub DmaBufferVirtualAddress: u32,
    pub DmaBufferSize: u32,
    pub DmaBufferPrivateDataSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub Flags: u32,
    pub HwQueueProgressFenceGpuVa: u32,
    pub HwQueueProgressFenceCpuVa: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SWITCHTOHWCONTEXTLIST {
    pub hHwContextFirst: usize,
    pub hHwContextSecond: usize,
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_UPDATEHWCONTEXTSTATE_FLAGS {
    pub Suspended: u32,
    pub InterruptOnSwitchCompletion: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEHWCONTEXTSTATE {
    pub hHwContext: usize,
    pub ContextSwitchFence: u64,
    pub Priority: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SCHEDULING_LOG_CONTEXT_STATE_CHANGE {
    pub hKmdContext: usize,
    pub newContextState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SCHEDULING_LOG_HEADER {
    pub FirstFreeEntryIndex: u32,
    pub WraparoundCount: u32,
    pub AtomicWraparoundAndEntryIndex: u64,
    pub NumberOfEntries: u64,
    pub Reserved: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SCHEDULING_LOG_ENTRY {
    pub GpuTimeStamp: u64,
    pub OperationType: u32,
    pub ReservedOperationTypeBits: u32,
    pub ContextStateChange: u32,
    pub ReservedOperationData: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SCHEDULING_LOG_BUFFER {
    pub Header: u32,
    pub Entries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETSCHEDULINGLOGBUFFER {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub NumberOfEntries: u32,
    pub LogBufferCpuVa: usize,
    pub LogBufferGpuVa: u32,
    pub InterruptEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETUPPRIORITYBANDS {
    pub gracePeriodForBand: [u64; 1],
    pub processQuantumForBand: [u64; 1],
    pub processGracePeriodForBand: [u64; 1],
    pub targetNormalBandPercentage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETCONTEXTSCHEDULINGPROPERTIES {
    pub hContext: usize,
    pub priorityBand: u32,
    pub realtimeBandPriorityLevel: i32,
    pub inProcessPriority: i32,
    pub quantum: u64,
    pub gracePeriodSamePriority: u64,
    pub gracePeriodLowerPriority: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SUSPENDCONTEXT {
    pub hContext: usize,
    pub contextSuspendFence: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESUMECONTEXT {
    pub hContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIRTUALMACHINEDATAFLAGS {
    pub SecureVirtualMachine: u32,
    pub LinuxVirtualMachine: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALMACHINEDATA {
    pub hKmdVmWorkerProcess: usize,
    pub pVmGuid: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SIGNALMONITOREDFENCE {
    pub KernelSubmissionType: u32,
    pub pDmaBuffer: usize,
    pub DmaBufferGpuVirtualAddress: u32,
    pub DmaSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub MultipassOffset: u32,
    pub MonitoredFenceGpuVa: u32,
    pub MonitoredFenceValue: u64,
    pub MonitoredFenceCpuVa: usize,
    pub hHwQueue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VALIDATESUBMITCOMMANDFLAGS {
    pub HardwareQueueSubmission: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_VALIDATESUBMITCOMMAND {
    pub Commands: u32,
    pub CommandLength: u32,
    pub Flags: u32,
    pub ContextCount: u32,
    pub Context: [usize; 1],
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub UmdPrivateDataSize: u32,
    pub HwQueueProgressFenceId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RENDERGDI {
    pub pCommand: usize,
    pub CommandLength: u32,
    pub pDmaBuffer: usize,
    pub DmaBufferGpuVirtualAddress: u32,
    pub DmaSize: u32,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferPrivateDataSize: u32,
    pub pAllocationList: usize,
    pub AllocationListSize: u32,
    pub MultipassOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_MAPCPUHOSTAPERTURE {
    pub hAllocation: usize,
    pub SegmentId: u16,
    pub PhysicalAdapterIndex: u16,
    pub NumberOfPages: u64,
    pub pCpuHostAperturePages: usize,
    pub pMemorySegmentPages: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UNMAPCPUHOSTAPERTURE {
    pub NumberOfPages: u64,
    pub pCpuHostAperturePages: usize,
    pub SegmentId: u16,
    pub PhysicalAdapterIndex: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDEOPROTECTEDREGION {
    pub PhysicalAdapterIndex: u32,
    pub SegmentIndex: u32,
    pub VprIndex: u32,
    pub CurrentStartOffset: usize,
    pub CurrentSize: usize,
    pub NewStartOffset: usize,
    pub NewSize: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_ESCAPE {
    pub hDevice: usize,
    pub Flags: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub hContext: usize,
    pub hKmdProcessHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COLLECTDBGINFO_EXT {
    pub BucketingKey: u32,
    pub CurrentDmaBufferOffset: u32,
    pub Reserved2: u32,
    pub Reserved3: u32,
    pub Reserved4: u32,
    pub Reserved5: u32,
    pub Reserved6: u32,
    pub Reserved7: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COLLECTDBGINFO {
    pub Reason: u32,
    pub pBuffer: usize,
    pub BufferSize: usize,
    pub pExtension: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_OVERLAYINFO {
    pub hAllocation: usize,
    pub PhysicalAddress: u64,
    pub SegmentId: u32,
    pub DstRect: u32,
    pub SrcRect: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEOVERLAY {
    pub VidPnSourceId: u32,
    pub OverlayInfo: u32,
    pub hOverlay: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEOVERLAY {
    pub OverlayInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_FLIPOVERLAY {
    pub hSource: usize,
    pub SrcPhysicalAddress: u64,
    pub SrcSegmentId: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETSCANLINE {
    pub VidPnTargetId: u32,
    pub InVerticalBlank: u8,
    pub ScanLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_ISSUPPORTEDVIDPN {
    pub hDesiredVidPn: u32,
    pub IsVidPnSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ENUM_PIVOT {
    pub VidPnSourceId: u32,
    pub VidPnTargetId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_ENUMVIDPNCOFUNCMODALITY {
    pub hConstrainingVidPn: u32,
    pub EnumPivotType: u32,
    pub EnumPivot: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RECOMMENDFUNCTIONALVIDPN {
    pub NumberOfVidPnTargets: u32,
    pub pVidPnTargetPrioritizationVector: usize,
    pub hRecommendedFunctionalVidPn: u32,
    pub RequestReason: u32,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SETVIDPNSOURCEADDRESS_FLAGS {
    pub ModeChange: u32,
    pub FlipImmediate: u32,
    pub FlipOnNextVSync: u32,
    pub FlipStereo: u32,
    pub FlipStereoTemporaryMono: u32,
    pub FlipStereoPreferRight: u32,
    pub SharedPrimaryTransition: u32,
    pub IndependentFlipExclusive: u32,
    pub MoveFlip: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SETVIDPNSOURCEADDRESS_INPUT_FLAGS {
    pub FlipStereo: u32,
    pub FlipStereoTemporaryMono: u32,
    pub FlipStereoPreferRight: u32,
    pub RetryAtLowerIrql: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SETVIDPNSOURCEADDRESS_OUTPUT_FLAGS {
    pub PrePresentNeeded: u32,
    pub HwFlipQueueDrainNeeded: u32,
    pub HwFlipQueueDrainAllPlanes: u32,
    pub HwFlipQueueDrainAllSources: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PLANE_SPECIFIC_INPUT_FLAGS {
    pub Enabled: u32,
    pub FlipImmediate: u32,
    pub FlipOnNextVSync: u32,
    pub SharedPrimaryTransition: u32,
    pub IndependentFlipExclusive: u32,
    pub FlipImmediateNoTearing: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PLANE_SPECIFIC_OUTPUT_FLAGS {
    pub FlipConvertedToImmediate: u32,
    pub PostPresentNeeded: u32,
    pub HsyncInterruptCompletion: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_ATTRIBUTES3 {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub ClipRect: u32,
    pub Rotation: u32,
    pub Blend: u32,
    pub ColorSpaceType: u32,
    pub StretchQuality: u32,
    pub SDRWhiteLevel: u32,
    pub DirtyRectCnt: u32,
    pub pDirtyRects: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HDR_METADATA {
    pub Type: u32,
    pub Size: u32,
    pub pMetaData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRIMARYDATA {
    pub hAllocation: usize,
    pub SegmentId: u16,
    pub SegmentAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDPNSOURCEADDRESS {
    pub VidPnSourceId: u32,
    pub PrimarySegment: u32,
    pub PrimaryAddress: u64,
    pub hAllocation: usize,
    pub ContextCount: u32,
    pub Context: [usize; 1],
    pub Flags: u32,
    pub Duration: u32,
    pub PrimaryData: [u32; 1],
    pub DriverPrivateDataSize: u32,
    pub pDriverPrivateData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDPNSOURCEADDRESSWITHMULTIPLANEOVERLAY {
    pub ContextCount: u32,
    pub Context: [usize; 1],
    pub Flags: u32,
    pub VidPnSourceId: u32,
    pub PlaneCount: u32,
    pub pPlanes: usize,
    pub Duration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_PLANE2 {
    pub LayerIndex: u32,
    pub Enabled: i32,
    pub AllocationSegment: u32,
    pub AllocationAddress: u64,
    pub hAllocation: usize,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDPNSOURCEADDRESSWITHMULTIPLANEOVERLAY2 {
    pub ContextCount: u32,
    pub Context: [usize; 1],
    pub Flags: u32,
    pub VidPnSourceId: u32,
    pub PlaneCount: u32,
    pub pPlanes: usize,
    pub Duration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PRIMARYCONTEXTDATA {
    pub hContext: usize,
    pub hAllocation: usize,
    pub SegmentId: u16,
    pub MmuId: u16,
    pub SegmentAddress: u64,
    pub VirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_PLANE3 {
    pub LayerIndex: u32,
    pub PresentId: u64,
    pub InputFlags: u32,
    pub OutputFlags: u32,
    pub MaxImmediateFlipLine: u32,
    pub ContextCount: u32,
    pub ppContextData: usize,
    pub DriverPrivateDataSize: u32,
    pub pDriverPrivateData: usize,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_POST_COMPOSITION_FLAGS {
    pub VerticalFlip: u32,
    pub HorizontalFlip: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_POST_COMPOSITION {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub Rotation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDPNSOURCEADDRESSWITHMULTIPLANEOVERLAY3 {
    pub VidPnSourceId: u32,
    pub InputFlags: u32,
    pub OutputFlags: u32,
    pub PlaneCount: u32,
    pub ppPlanes: usize,
    pub pPostComposition: usize,
    pub Duration: u32,
    pub pHDRMetaData: usize,
    pub TargetFlipTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_POSTMULTIPLANEOVERLAYPRESENT {
    pub VidPnTargetId: u32,
    pub PhysicalAdapterMask: u32,
    pub LayerIndex: u32,
    pub PresentID: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_VALIDATEUPDATEALLOCPROPERTY {
    pub hAllocation: usize,
    pub SupportedSegmentSet: u32,
    pub PreferredSegment: u32,
    pub Flags: u32,
    pub SetAccessedPhysically: u32,
    pub SetSupportedSegmentSet: u32,
    pub SetPreferredSegment: u32,
    pub Reserved: u32,
    pub PropertyMaskValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEPERIODICFRAMENOTIFICATION {
    pub hAdapter: usize,
    pub VidPnTargetId: u32,
    pub Time: u64,
    pub NotificationID: u32,
    pub hNotification: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESTROYPERIODICFRAMENOTIFICATION {
    pub hNotification: usize,
    pub hAdapter: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANEOVERLAYCAPS {
    pub Rotation: u32,
    pub RotationWithoutIndependentFlip: u32,
    pub VerticalFlip: u32,
    pub HorizontalFlip: u32,
    pub StretchRGB: u32,
    pub StretchYUV: u32,
    pub BilinearFilter: u32,
    pub HighFilter: u32,
    pub Shared: u32,
    pub Immediate: u32,
    pub Plane0ForVirtualModeOnly: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETMULTIPLANEOVERLAYCAPS {
    pub VidPnSourceId: u32,
    pub MaxPlanes: u32,
    pub MaxRGBPlanes: u32,
    pub MaxYUVPlanes: u32,
    pub OverlayCaps: u32,
    pub MaxStretchFactor: u32,
    pub MaxShrinkFactor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GETPOSTCOMPOSITIONCAPS {
    pub VidPnSourceId: u32,
    pub MaxStretchFactor: u32,
    pub MaxShrinkFactor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CHECKMULTIPLANEOVERLAYSUPPORT {
    pub PlaneCount: u32,
    pub pPlanes: usize,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_PLANE_WITH_SOURCE2 {
    pub hAllocation: usize,
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MULTIPLANE_OVERLAY_POST_COMPOSITION_WITH_SOURCE {
    pub VidPnSourceId: u32,
    pub PostComposition: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CHECKMULTIPLANEOVERLAYSUPPORT3 {
    pub PlaneCount: u32,
    pub ppPlanes: usize,
    pub PostCompositionCount: u32,
    pub ppPostComposition: usize,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIDPNSOURCEVISIBILITY {
    pub VidPnSourceId: u32,
    pub Visible: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COMMITVIDPN_FLAGS {
    pub PathPowerTransition: u32,
    pub PathPoweredOff: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COMMITVIDPN {
    pub hFunctionalVidPn: u32,
    pub AffectedVidPnSourceId: u32,
    pub MonitorConnectivityChecks: u32,
    pub hPrimaryAllocation: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEACTIVEVIDPNPRESENTPATH {
    pub VidPnPresentPathInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RECOMMENDVIDPNTOPOLOGY {
    pub hVidPn: u32,
    pub VidPnSourceId: u32,
    pub RequestReason: u32,
    pub hFallbackTopology: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDPNTOPOLOGY_INTERFACE {
    pub pfnGetNumPaths: usize,
    pub pfnGetNumPathsFromSource: usize,
    pub pfnEnumPathTargetsFromSource: usize,
    pub pfnGetPathSourceFromTarget: usize,
    pub pfnAcquirePathInfo: usize,
    pub pfnAcquireFirstPathInfo: usize,
    pub pfnAcquireNextPathInfo: usize,
    pub pfnUpdatePathSupportInfo: usize,
    pub pfnReleasePathInfo: usize,
    pub pfnCreateNewPathInfo: usize,
    pub pfnAddPath: usize,
    pub pfnRemovePath: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDPNSOURCEMODESET_INTERFACE {
    pub pfnGetNumModes: usize,
    pub pfnAcquireFirstModeInfo: usize,
    pub pfnAcquireNextModeInfo: usize,
    pub pfnAcquirePinnedModeInfo: usize,
    pub pfnReleaseModeInfo: usize,
    pub pfnCreateNewModeInfo: usize,
    pub pfnAddMode: usize,
    pub pfnPinMode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDPNTARGETMODESET_INTERFACE {
    pub pfnGetNumModes: usize,
    pub pfnAcquireFirstModeInfo: usize,
    pub pfnAcquireNextModeInfo: usize,
    pub pfnAcquirePinnedModeInfo: usize,
    pub pfnReleaseModeInfo: usize,
    pub pfnCreateNewModeInfo: usize,
    pub pfnAddMode: usize,
    pub pfnPinMode: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_VIDPN_INTERFACE {
    pub Version: u32,
    pub pfnGetTopology: usize,
    pub pfnAcquireSourceModeSet: usize,
    pub pfnReleaseSourceModeSet: usize,
    pub pfnCreateNewSourceModeSet: usize,
    pub pfnAssignSourceModeSet: usize,
    pub pfnAssignMultisamplingMethodSet: usize,
    pub pfnAcquireTargetModeSet: usize,
    pub pfnReleaseTargetModeSet: usize,
    pub pfnCreateNewTargetModeSet: usize,
    pub pfnAssignTargetModeSet: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITORSOURCEMODESET_INTERFACE {
    pub pfnGetNumModes: usize,
    pub pfnAcquirePreferredModeInfo: usize,
    pub pfnAcquireFirstModeInfo: usize,
    pub pfnAcquireNextModeInfo: usize,
    pub pfnCreateNewModeInfo: usize,
    pub pfnAddMode: usize,
    pub pfnReleaseModeInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITORFREQUENCYRANGESET_INTERFACE {
    pub pfnGetNumFrequencyRanges: usize,
    pub pfnAcquireFirstFrequencyRangeInfo: usize,
    pub pfnAcquireNextFrequencyRangeInfo: usize,
    pub pfnReleaseFrequencyRangeInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITORDESCRIPTORSET_INTERFACE {
    pub pfnGetNumDescriptors: usize,
    pub pfnAcquireFirstDescriptorInfo: usize,
    pub pfnAcquireNextDescriptorInfo: usize,
    pub pfnReleaseDescriptorInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITOR_INTERFACE {
    pub Version: u32,
    pub pfnAcquireMonitorSourceModeSet: usize,
    pub pfnReleaseMonitorSourceModeSet: usize,
    pub pfnGetMonitorFrequencyRangeSet: usize,
    pub pfnGetMonitorDescriptorSet: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITOR_INTERFACE_V2 {
    pub Version: u32,
    pub pfnAcquireMonitorSourceModeSet: usize,
    pub pfnReleaseMonitorSourceModeSet: usize,
    pub pfnGetMonitorFrequencyRangeSet: usize,
    pub pfnGetMonitorDescriptorSet: usize,
    pub pfnGetAdditionalMonitorModeSet: usize,
    pub pfnReleaseAdditionalMonitorModeSet: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITOR_DESCRIPTOR {
    pub DescriptorType: u32,
    pub DescriptorLength: u32,
    pub DescriptorBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITOR_INTERFACE_V3 {
    pub Version: u32,
    pub pfnAcquireMonitorSourceModeSet: usize,
    pub pfnReleaseMonitorSourceModeSet: usize,
    pub pfnGetMonitorFrequencyRangeSet: usize,
    pub pfnGetAdditionalMonitorModeSet: usize,
    pub pfnReleaseAdditionalMonitorModeSet: usize,
    pub pfnGetMonitorDescriptor: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYVIDPNHWCAPABILITY {
    pub hFunctionalVidPn: u32,
    pub SourceId: u32,
    pub TargetId: u32,
    pub VidPnHWCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_DISPLAY_ONLY_FLAGS {
    pub Rotate: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_PRESENT_DISPLAYONLY {
    pub VidPnSourceId: u32,
    pub pSource: usize,
    pub BytesPerPixel: u32,
    pub Pitch: i32,
    pub Flags: u32,
    pub NumMoves: u32,
    pub pMoves: usize,
    pub NumDirtyRects: u32,
    pub pDirtyRect: usize,
    pub pfnPresentDisplayOnlyProgress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYDEPENDENTENGINEGROUP {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub DependentNodeOrdinalMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ENGINESTATUS {
    pub Responsive: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYENGINESTATUS {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub EngineStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESETENGINE {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub LastAbortedFenceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESETHWENGINE {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_INVALIDATEHWCONTEXTFLAGS {
    pub CollateralDamage: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_INVALIDATEHWCONTEXT {
    pub hAdapter: usize,
    pub hHwContext: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_INTERFACESPECIFICDATA {
    pub hAdapter: usize,
    pub pfnGetHandleDataCb: u32,
    pub pfnGetHandleParentCb: u32,
    pub pfnEnumHandleChildrenCb: u32,
    pub pfnNotifyInterruptCb: u32,
    pub pfnNotifyDpcCb: u32,
    pub pfnQueryVidPnInterfaceCb: u32,
    pub pfnGetCaptureAddressCb: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETDISPLAYPRIVATEDRIVERFORMAT {
    pub VidPnSourceId: u32,
    pub PrimaryAllocation: usize,
    pub PrivateDriverFormatAttribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RECOMMENDMONITORMODES {
    pub VideoPresentTargetId: u32,
    pub hMonitorSourceModeSet: u32,
    pub pMonitorSourceModeSetInterface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_MAPCONTEXTALLOCATION {
    pub BaseAddress: u32,
    pub MinimumAddress: u32,
    pub MaximumAddress: u32,
    pub hAllocation: usize,
    pub OffsetInPages: u32,
    pub SizeInPages: u32,
    pub Protection: u32,
    pub DriverProtection: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_UPDATECONTEXTALLOCATION {
    pub hAllocation: usize,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_RESERVEGPUVIRTUALADDRESSRANGE {
    pub hDxgkProcess: usize,
    pub SizeInBytes: u64,
    pub Alignment: u32,
    pub StartVirtualAddress: u64,
    pub BaseAddress: u64,
    pub AllowUserModeMapping: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_HISTORYBUFFERPRECISION {
    pub PrecisionBits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HISTORY_BUFFER_HEADER {
    pub RenderCbSequence: u32,
    pub NumTimestamps: u32,
    pub PrivateDataSize: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_HISTORY_BUFFER {
    pub Header: u32,
    pub DriverPrivateData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_FORMATHISTORYBUFFER {
    pub pHistoryBuffer: usize,
    pub HistoryBufferSize: u32,
    pub pFormattedBuffer: usize,
    pub FormattedBufferSize: u32,
    pub NumTimestamps: u32,
    pub Precision: u32,
    pub Offset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONTROLMODEBEHAVIOR {
    pub Request: u32,
    pub Satisfied: u32,
    pub NotSatisfied: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MONITORLINKINFO {
    pub UsageHints: u32,
    pub Capabilities: u32,
    pub DitheringSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_UPDATEMONITORLINKINFO {
    pub VideoPresentTargetId: u32,
    pub MonitorLinkInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CONNECTION_MONITOR_CONNECT_FLAGS {
    pub Usb4DisplayPortMonitor: u32,
    pub DisplayMuxConnectionChange: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CONNECTION_USB4_INFO {
    pub Dpcd_DP_IN_Adapter_Number: u32,
    pub Dpcd_USB4_Driver_ID: u32,
    pub Dpcd_USB4_ROUTER_TOPOLOGY_ID: [u8; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_CONNECTION_CHANGE {
    pub ConnectionChangeId: u64,
    pub TargetId: u32,
    pub ConnectionStatus: u32,
    pub Reserved: u32,
    pub LinkTargetType: u32,
    pub MonitorConnectFlags: u32,
    pub BaseTargetType: u32,
    pub NewTargetId: u32,
    pub BaseTargetType_1: u32,
    pub NewTargetId_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SET_TIMING_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SET_TIMING_RESULTS {
    pub ConnectionStatusChanges: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SET_TIMING_PATH_INFO {
    pub VidPnTargetId: u32,
    pub OutputColorSpace: u32,
    pub OutputWireColorSpace: u32,
    pub SelectedWireFormat: u32,
    pub VidPnPathUpdates: u32,
    pub Active: u32,
    pub IgnoreConnectivity: u32,
    pub PreserveInherited: u32,
    pub SyncLockGroup: u32,
    pub SyncLockStyle: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub InputFlags: u32,
    pub RecheckMPO: u32,
    pub Reserved_2: u32,
    pub OutputFlags: u32,
    pub TargetState: u32,
    pub GlitchCause: u32,
    pub GlitchEffect: u32,
    pub GlitchDuration: u32,
    pub Reserved_3: u32,
    pub DiagnosticInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTIMINGSFROMVIDPN {
    pub hFunctionalVidPn: u32,
    pub SetFlags: u32,
    pub pResultsFlags: usize,
    pub PathCount: u32,
    pub pSetTimingPathInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTARGETGAMMA {
    pub TargetId: u32,
    pub GammaRamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTARGETCONTENTTYPE {
    pub TargetId: u32,
    pub ContentType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTARGETANALOGCOPYPROTECTION {
    pub TargetId: u32,
    pub CopyProtectionType: u32,
    pub APSTriggerBits: u32,
    pub CopyProtectionSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DISPLAYDETECTCONTROL {
    pub TargetId: u32,
    pub Type: u32,
    pub NonDestructiveOnly: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYCONNECTIONCHANGE {
    pub ConnectionChange: u32,
    pub Usb4MonitorInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_INHERITED_TIMING_INFO {
    pub OutputColorSpace: u32,
    pub OutputWireColorSpace: u32,
    pub SelectedWireFormat: u32,
    pub GlitchCause: u32,
    pub GlitchEffect: u32,
    pub GlitchDuration: u32,
    pub Reserved: u32,
    pub DiagnosticInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEPROTECTEDSESSION {
    pub hProtectedSession: usize,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_PROTECTEDSESSIONSTATUS {
    pub hProtectedSession: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ALLOCATECONTIGUOUSMEMORY {
    pub NumberOfBytes: usize,
    pub LowestAcceptableAddress: u64,
    pub HighestAcceptableAddress: u64,
    pub BoundaryAddressMultiple: u64,
    pub CacheType: u32,
    pub hMemoryHandle: usize,
    pub pMemory: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_FREECONTIGUOUSMEMORY {
    pub hMemoryHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ALLOCATEPAGESFORMDL {
    pub LowAddress: u64,
    pub HighAddress: u64,
    pub SkipBytes: u64,
    pub TotalBytes: usize,
    pub CacheType: u32,
    pub Flags: u32,
    pub hMemoryHandle: usize,
    pub pMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_FREEPAGESFROMMDL {
    pub hMemoryHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_MAPMDLTOIOMMU {
    pub pMdl: usize,
    pub hMemoryHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_UNMAPMDLFROMIOMMU {
    pub hMemoryHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_PINFRAMEBUFFERFORSAVE {
    pub PhysicalAdapterIndex: u32,
    pub CommitSize: usize,
    pub pMdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_UNPINFRAMEBUFFERFORSAVE {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_MAPFRAMEBUFFERPOINTER {
    pub PhysicalAdapterIndex: u32,
    pub Size: usize,
    pub Offset: usize,
    pub pBaseAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_UNMAPFRAMEBUFFERPOINTER {
    pub PhysicalAdapterIndex: u32,
    pub pBaseAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_BEGINEXCLUSIVEACCESS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_ENDEXCLUSIVEACCESS {
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESUMEHWENGINE {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TRACKEDWORKLOAD_STATE_FLAGS {
    pub Saturated: u32,
    pub OptimalLevel: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETTRACKEDWORKLOADPOWERLEVEL {
    pub PowerLevel: u32,
    pub EffectivePowerLevel: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_SIGNALEVENT {
    pub hDxgkProcess: usize,
    pub hEvent: usize,
    pub CpuEventObject: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ISFEATUREENABLED {
    pub DeviceHandle: usize,
    pub FeatureId: u32,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_KSR_MEMORY_RANGE {
    pub MemoryRangeDesc: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_SAVEMEMORYFORHOTUPDATE {
    pub NumDataMemoryRanges: u32,
    pub pDataMemoryRanges: usize,
    pub pDataMdl: usize,
    pub DataSize: u32,
    pub pData: usize,
    pub MetaDataSize: u32,
    pub pMetaData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SAVEMEMORYFORHOTUPDATEFLAGS {
    pub CancelHotUpdate: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SAVEMEMORYFORHOTUPDATE {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_RESTOREMEMORYFORHOTUPDATEFLAGS {
    pub RestoreComplete: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESTOREMEMORYFORHOTUPDATE {
    pub Flags: u32,
    pub pDataMdl: usize,
    pub MetaDataSize: u32,
    pub pMetaData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_NOTIFYCURSORSUPPORTCHANGE {
    pub DeviceHandle: usize,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDICB_PARTITIONING_EVENT_NOTIFICATION {
    pub hAdapter: usize,
    pub EventType: u32,
    pub PartitionId: u32,
    pub EventDescription: [u32; 1],
    pub EventData1: u64,
    pub EventData2: u64,
    pub EventData3: u64,
    pub EventData4: u64,
    pub EventData5: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_CREATE_PHYSICAL_MEMORY_OBJECT {
    pub hAdapter: u32,
    pub Size: usize,
    pub Context: u32,
    pub Type: u32,
    pub CacheType: u32,
    pub LowAddress: u64,
    pub HighAddress: u64,
    pub SkipBytes: u64,
    pub Flags: u32,
    pub LowestAcceptableAddress: u64,
    pub HighestAcceptableAddress: u64,
    pub BoundaryAddressMultiple: u64,
    pub DesiredAccess: u32,
    pub ObjectAttributes: usize,
    pub PageProtection: u32,
    pub AllocationAttributes: u32,
    pub BaseAddress: u64,
    pub hPhysicalMemoryObject: usize,
    pub hAdapterMemoryObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_DESTROY_PHYSICAL_MEMORY_OBJECT {
    pub hPhysicalMemoryObject: usize,
    pub hAdapterMemoryObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_MAP_PHYSICAL_MEMORY {
    pub hPhysicalMemoryObject: usize,
    pub AccessMode: u32,
    pub Offset: usize,
    pub Size: usize,
    pub pMappedAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_UNMAP_PHYSICAL_MEMORY {
    pub hPhysicalMemoryObject: usize,
    pub pBaseAddress: usize,
    pub Size: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ALLOCATE_ADL {
    pub hAdapterMemoryObject: usize,
    pub Offset: usize,
    pub Size: usize,
    pub RequireContiguous: u32,
    pub PreferContiguous: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub field_pAdl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_FREE_ADL {
    pub hAdapterMemoryObject: usize,
    pub pAdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_OPEN_PHYSICAL_MEMORY_OBJECT {
    pub hPhysicalMemoryObject: usize,
    pub hAdapter: usize,
    pub hAdapterMemoryObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_CLOSE_PHYSICAL_MEMORY_OBJECT {
    pub hAdapterMemoryObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_QUERYFEATURESUPPORT {
    pub DeviceHandle: usize,
    pub FeatureId: u32,
    pub DriverSupportState: u32,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_PINFRAMEBUFFERFORSAVE2 {
    pub PhysicalAdapterIndex: u32,
    pub CommitSize: usize,
    pub PreferContiguous: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub pAdl: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_USERMODESUBMISSION_CAPS {
    pub SecondaryDoorbellSupported: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub DoorbellSizeInBytes: u32,
    pub SecondaryDoorbellSizeInBytes: u32,
    pub Reserved_1: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEDOORBELL_FLAGS {
    pub ResizeRingBufferOperation: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEDOORBELL {
    pub hHwQueue: usize,
    pub hDoorbell: usize,
    pub PrivateDriverDataSize: u32,
    pub PrivateDriverData: usize,
    pub hRingBuffer: usize,
    pub hRingBufferControl: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONNECTDOORBELL_FLAGS {
    pub RequireSecondaryAddress: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CONNECTDOORBELL {
    pub hDoorbell: usize,
    pub Flags: u32,
    pub KernelCpuVirtualAddress: usize,
    pub SecondaryKernelCpuVirtualAddress: usize,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DISCONNECTDOORBELL_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DISCONNECTDOORBELL {
    pub hDoorbell: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_DESTROYDOORBELL {
    pub hDoorbell: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_NOTIFYWORKSUBMISSION_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_NOTIFYWORKSUBMISSION {
    pub hHwQueue: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_DISCONNECTDOORBELL_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_DISCONNECTDOORBELL {
    pub hHwQueue: usize,
    pub hDoorbell: usize,
    pub Flags: u32,
    pub DisconnectReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIRTY_BIT_TRACKING_CAPS {
    pub DirtyBitTrackingSupported: u32,
    pub DirtyBitTrackingPerformant: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DIRTY_BIT_TRACKING_SEGMENT_CAPS {
    pub PageSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_CREATEMEMORYBASIS {
    pub SegmentId: u32,
    pub RangeCount: u64,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYDIRTYBITDATA {
    pub MemoryBasis: usize,
    pub SubrangeIndex: u64,
    pub SubrangeOffset: u64,
    pub SubrangeSize: u64,
    pub Buffer: usize,
    pub BufferSize: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSCATTERRESERVEIN {
    pub SegmentId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSCATTERRESERVEOUT {
    pub SetVGPUResourcesPageSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_PREPARE_LIVE_MIGRATION {
    pub vfIndex: u32,
    pub MigrationType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_SAVE_IMMUTABLE_MIGRATION_DATA {
    pub vfIndex: u32,
    pub DataSize: usize,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_SAVE_MUTABLE_MIGRATION_DATA {
    pub vfIndex: u32,
    pub DataSize: usize,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_RESTORE_IMMUTABLE_MIGRATION_DATA {
    pub vfIndex: u32,
    pub DataSize: u64,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_RESTORE_MUTABLE_MIGRATION_DATA {
    pub vfIndex: u32,
    pub DataSize: u64,
    pub Data: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_INTERRUPT_TABLE_ENTRY {
    pub MessageAddress: u64,
    pub MessageData: u32,
    pub VectorControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_GPUP_WRITE_VIRTUALIZED_MSIX {
    pub vfIndex: u32,
    pub InterruptTableIndex: u32,
    pub WriteValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPU_PHYSICAL_RESERVE_DESCRIPTOR {
    pub DriverAllocationHandle: usize,
    pub MemoryBasis: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALGPURESOURCES2 {
    pub vfIndex: u32,
    pub SegmentCount: u32,
    pub SegmentDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETVIRTUALFUNCTIONPAUSESTATE {
    pub vfIndex: u32,
    pub bPause: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYFEATURESUPPORT {
    pub FeatureId: u32,
    pub MinSupportedVersion: u32,
    pub MaxSupportedVersion: u32,
    pub AllowExperimental: u8,
    pub SupportedByDriver: u8,
    pub SupportedOnCurrentConfig: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ISFEATUREENABLED2_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_ISFEATUREENABLED2 {
    pub FeatureId: u32,
    pub Flags: u32,
    pub Result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_QUERYFEATUREINTERFACE {
    pub FeatureId: u32,
    pub Version: u32,
    pub InterfaceSize: u32,
    pub Interface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_QUERYFEATUREINTERFACE {
    pub FeatureId: u32,
    pub Version: u32,
    pub InterfaceSize: u32,
    pub Interface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKISFEATUREENABLED2IOCTL {
    pub DriverObject: usize,
    pub pArgs: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_FEATURE_SAMPLE_GETVALUE {
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_FEATURE_SAMPLE_ADDVALUE {
    pub InputValue: u32,
    pub OutputValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_FEATURE_SAMPLE_SUBTRACTVALUE {
    pub InputValue: u32,
    pub OutputValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKCBINT_FEATURE_SAMPLE_4 {
    pub GetValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDIINT_FEATURE_SAMPLE_4 {
    pub AddValue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDIINT_FEATURE_SAMPLE_5 {
    pub AddValue: usize,
    pub SubtractValue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARGCB_FEATURE_NATIVEFENCE_CAPS_1 {
    pub SupportOptimizedDefaultFenceType: u32,
    pub SupportIntraGpuFenceType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKCBINT_FEATURE_NATIVEFENCE_1 {
    pub GetOSCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TDR_PAYLOAD_ENGINE_TIMEOUT {
    pub NodeOrdinal: u32,
    pub EngineOrdinal: u32,
    pub LastHwCompletedFenceId: u64,
    pub LastHwSubmittedFenceId: u64,
    pub NumberOfPendingSuspendRequests: u32,
    pub NumberOfReadyInteractiveHwQueues: u32,
    pub hContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TDR_PAYLOAD_VSYNC_TIMEOUT {
    pub VidPnSourceId: u32,
    pub LayerIndex: u32,
    pub PresentId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_COLLECTDBGINFO2 {
    pub Reason: u32,
    pub pBuffer: usize,
    pub BufferSize: usize,
    pub pExtension: usize,
    pub TdrType: u32,
    pub TdrPayloadSize: u32,
    pub TdrPayload: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_BUILDTESTCOMMANDBUFFER {
    pub hContext: usize,
    pub Command: u32,
    pub pDmaBuffer: usize,
    pub pDmaBufferPrivateData: usize,
    pub DmaBufferSize: u32,
    pub DmaBufferPrivateDataSize: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKDDI_KERNELMODETESTINGINTERFACE {
    pub pfnBuildTestCommandBuffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYPAGINGBUFFERINFOIN {
    pub PhysicalAdapterIndex: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYPAGINGBUFFERINFOOUT {
    pub PagingBufferSize: u32,
    pub PagingBufferPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTCOUNTIN {
    pub PhysicalAdapterIndex: u32,
    pub Padding: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTCOUNTOUT {
    pub SegmentCount: u32,
    pub Padding: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_SEGMENTDESCRIPTOR5 {
    pub SegmentType: u32,
    pub Flags: u32,
    pub BaseAddress: u64,
    pub Size: u64,
    pub SystemMemoryEndAddress: usize,
    pub CpuTranslatedAddress: u64,
    pub CpuHostAperture: u32,
    pub VprRangeStartOffset: usize,
    pub VprRangeSize: usize,
    pub VprAlignment: u32,
    pub NumInvalidMemoryRanges: u32,
    pub NumVprSupported: u32,
    pub VprReserveSize: u32,
    pub NumUEFIFrameBufferRanges: u32,
    pub SlabSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTIN5 {
    pub PhysicalAdapterIndex: u32,
    pub Padding: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYSEGMENTOUT5 {
    pub SegmentDescriptors: usize,
    pub Reserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYMMUCOUNTIN {
    pub PhysicalAdapterIndex: u32,
    pub Padding: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYMMUCOUNTOUT {
    pub MmuCount: u32,
    pub Padding: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_MMUDESCRIPTOR {
    pub Flags: u32,
    pub Size: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYMMUSIN {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_QUERYMMUSOUT {
    pub MmuDescriptors: usize,
    pub DisplayMmuId: u32,
    pub Reserved0: u32,
    pub Reserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_NOTIFYCONTEXTPRIORITYCHANGE {
    pub hContext: usize,
    pub SchedulingPriority: i32,
    pub InProcessPriority: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_RESETDISPLAYENGINE {
    pub InputFlags: u64,
    pub OutputFlags: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_64_BIT_ONLY_CAPS {
    pub SupportsOnly64Bit: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_WDDM3_ON_VB_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DxgkDdiSetAllocationBackingStore: usize,
    pub DxgkDdiCreateCpuEvent: usize,
    pub DxgkDdiDestroyCpuEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDEO_PRESENT_SOURCE {
    pub Id: u32,
    pub dwReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_COLOR_COEFF_DYNAMIC_RANGES {
    pub FirstChannel: u32,
    pub SecondChannel: u32,
    pub ThirdChannel: u32,
    pub FourthChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_2DREGION {
    pub cx: u32,
    pub cy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_GRAPHICS_RENDERING_FORMAT {
    pub PrimSurfSize: u32,
    pub VisibleRegionSize: u32,
    pub Stride: u32,
    pub PixelFormat: u32,
    pub ColorBasis: u32,
    pub PixelValueAccessMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_SOURCE_MODE {
    pub Id: u32,
    pub Type: u32,
    pub Graphics: u32,
    pub Text: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDEO_PRESENT_TARGET {
    pub Id: u32,
    pub VideoOutputTechnology: u32,
    pub VideoOutputHpdAwareness: u32,
    pub MonitorOrientationAwareness: u32,
    pub SupportsSdtvModes: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDEO_SIGNAL_INFO {
    pub VideoStandard: u32,
    pub TotalSize: u32,
    pub ActiveSize: u32,
    pub VSyncFreq: u32,
    pub HSyncFreq: u32,
    pub PixelRate: usize,
    pub ScanLineOrdering: u32,
    pub VSyncFreqDivider: u32,
    pub Reserved: u32,
    pub ScanLineOrdering_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_TARGET_MODE {
    pub Id: u32,
    pub VideoSignalInfo: u32,
    pub WireFormatAndPreference: u32,
    pub Preference: u32,
    pub Preference_1: u32,
    pub MinimumVSyncFreq: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISPLAYID_DETAILED_TIMING_TYPE_I {
    pub PixelClock: u32,
    pub AspectRatio: u32,
    pub Reserved: u32,
    pub ScanningType: u32,
    pub StereoMode: u32,
    pub PreferredTiming: u32,
    pub HorizontalActivePixels: u16,
    pub HorizontalBlankPixels: u16,
    pub HorizontalFrontPorch: u16,
    pub HorizontalSyncPolarity: u16,
    pub HorizontalSyncWidth: u16,
    pub VerticalActiveLines: u16,
    pub VerticalBlankLines: u16,
    pub VerticalFrontPorch: u16,
    pub VerticalSyncPolarity: u16,
    pub VerticalSyncWidth: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_TARGETMODE_DETAIL_TIMING {
    pub VideoStandard: u32,
    pub TimingId: u32,
    pub DetailTiming: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_HW_CAPABILITY {
    pub DriverRotation: u32,
    pub DriverScaling: u32,
    pub DriverCloning: u32,
    pub DriverColorConvert: u32,
    pub DriverLinkedAdapaterOutput: u32,
    pub DriverRemoteDisplay: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_MONITOR_SOURCE_MODE {
    pub Id: u32,
    pub VideoSignalInfo: u32,
    pub ColorBasis: u32,
    pub ColorCoeffDynamicRanges: u32,
    pub Origin: u32,
    pub Preference: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_FREQUENCY_RANGE {
    pub MinVSyncFreq: u32,
    pub MaxVSyncFreq: u32,
    pub MinHSyncFreq: u32,
    pub MaxHSyncFreq: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_MONITOR_FREQUENCY_RANGE {
    pub Origin: u32,
    pub RangeLimits: u32,
    pub ConstraintType: u32,
    pub ActiveSize: u32,
    pub MaxPixelRate: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH_SCALING_SUPPORT {
    pub Identity: u32,
    pub Centered: u32,
    pub Stretched: u32,
    pub AspectRatioCenteredMax: u32,
    pub Custom: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH_ROTATION_SUPPORT {
    pub Identity: u32,
    pub Rotate90: u32,
    pub Rotate180: u32,
    pub Rotate270: u32,
    pub Offset0: u32,
    pub Offset90: u32,
    pub Offset180: u32,
    pub Offset270: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH_TRANSFORMATION {
    pub Scaling: u32,
    pub ScalingSupport: u32,
    pub Rotation: u32,
    pub RotationSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH_COPYPROTECTION_SUPPORT {
    pub NoProtection: u32,
    pub MacroVisionApsTrigger: u32,
    pub MacroVisionFull: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH_COPYPROTECTION {
    pub CopyProtectionType: u32,
    pub APSTriggerBits: u32,
    pub OEMCopyProtection: [u8; 1],
    pub CopyProtectionSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_GAMMA_RAMP {
    pub Type: u32,
    pub DataSize: usize,
    pub pRgb256x3x16: usize,
    pub pDxgi1: usize,
    pub p3x4: usize,
    pub pMatrixV2: usize,
    pub pRaw: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIDPN_PRESENT_PATH {
    pub VidPnSourceId: u32,
    pub VidPnTargetId: u32,
    pub ImportanceOrdinal: u32,
    pub ContentTransformation: u32,
    pub VisibleFromActiveTLOffset: u32,
    pub VisibleFromActiveBROffset: u32,
    pub VidPnTargetColorBasis: u32,
    pub VidPnTargetColorCoeffDynamicRanges: u32,
    pub Content: u32,
    pub CopyProtection: u32,
    pub GammaRamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_MONITOR_DESCRIPTOR {
    pub Id: u32,
    pub Type: u32,
    pub DataSize: usize,
    pub pData: usize,
    pub Origin: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_PREEMPTION_CAPS {
    pub GraphicsPreemptionGranularity: u32,
    pub ComputePreemptionGranularity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_1_2_CAPS {
    pub PreemptionCaps: u32,
    pub SupportNonVGA: u32,
    pub SupportSmoothRotation: u32,
    pub SupportPerEngineTDR: u32,
    pub SupportKernelModeCommandBuffer: u32,
    pub SupportCCD: u32,
    pub SupportSoftwareDeviceBitmaps: u32,
    pub SupportGammaRamp: u32,
    pub SupportHWCursor: u32,
    pub SupportHWVSync: u32,
    pub SupportSurpriseRemovalInHibernation: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_SHAREDPRIMARYSURFACEDATA {
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub RefreshRate: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_SHADOWSURFACEDATA {
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub Pitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_STAGINGSURFACEDATA {
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_GDISURFACEFLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_GDISURFACEDATA {
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub Type: u32,
    pub Flags: u32,
    pub Pitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_VIRTUALGPUSURFACEDATA {
    pub Size: u64,
    pub Alignment: u32,
    pub DriverSegmentId: u32,
    pub PrivateDriverData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_PALETTEDATA {
    pub Red: u8,
    pub Green: u8,
    pub Blue: u8,
    pub Unused: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKARG_SETPALETTE {
    pub VidPnSourceId: u32,
    pub FirstEntry: u32,
    pub NumEntries: u32,
    pub pLookupTable: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MOVE_RECT {
    pub SourcePoint: usize,
    pub DestRect: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAY_INFORMATION {
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
    pub ColorFormat: u32,
    pub PhysicAddress: u64,
    pub TargetId: u32,
    pub AcpiId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_RANDOM_NUMBER {
    pub abRandomNumber: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_OMAC {
    pub abOMAC: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_ENCRYPTED_PARAMETERS {
    pub abEncryptedParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_GET_INFO_PARAMETERS {
    pub omac: u32,
    pub rnRandomNumber: u32,
    pub guidInformation: u32,
    pub ulSequenceNumber: u32,
    pub cbParametersSize: u32,
    pub abParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_COPP_COMPATIBLE_GET_INFO_PARAMETERS {
    pub rnRandomNumber: u32,
    pub guidInformation: u32,
    pub ulSequenceNumber: u32,
    pub cbParametersSize: u32,
    pub abParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_HDCP_KEY_SELECTION_VECTOR {
    pub abKeySelectionVector: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_CONNECTED_HDCP_DEVICE_INFORMATION {
    pub rnRandomNumber: u32,
    pub ulStatusFlags: u32,
    pub ulHDCPFlags: u32,
    pub ksvB: u32,
    pub Reserved: [u8; 11],
    pub Reserved2: [u8; 16],
    pub Reserved3: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_REQUESTED_INFORMATION {
    pub omac: u32,
    pub cbRequestedInformationSize: u32,
    pub abRequestedInformation: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_STANDARD_INFORMATION {
    pub rnRandomNumber: u32,
    pub ulStatusFlags: u32,
    pub ulInformation: u32,
    pub ulReserved: u32,
    pub ulReserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_ACTUAL_OUTPUT_FORMAT {
    pub rnRandomNumber: u32,
    pub ulStatusFlags: u32,
    pub ulDisplayWidth: u32,
    pub ulDisplayHeight: u32,
    pub ifInterleaveFormat: u32,
    pub d3dFormat: u32,
    pub ulFrequencyNumerator: u32,
    pub ulFrequencyDenominator: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_ACP_AND_CGMSA_SIGNALING {
    pub rnRandomNumber: u32,
    pub ulStatusFlags: u32,
    pub ulAvailableTVProtectionStandards: u32,
    pub ulActiveTVProtectionStandard: u32,
    pub ulReserved: u32,
    pub ulAspectRatioValidMask1: u32,
    pub ulAspectRatioData1: u32,
    pub ulAspectRatioValidMask2: u32,
    pub ulAspectRatioData2: u32,
    pub ulAspectRatioValidMask3: u32,
    pub ulAspectRatioData3: u32,
    pub ulReserved2: [u32; 4],
    pub ulReserved3: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_OUTPUT_ID {
    pub rnRandomNumber: u32,
    pub ulStatusFlags: u32,
    pub OutputId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_CONFIGURE_PARAMETERS {
    pub omac: u32,
    pub guidSetting: u32,
    pub ulSequenceNumber: u32,
    pub cbParametersSize: u32,
    pub abParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_SET_PROTECTION_LEVEL_PARAMETERS {
    pub ulProtectionType: u32,
    pub ulProtectionLevel: u32,
    pub Reserved: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_SET_ACP_AND_CGMSA_SIGNALING_PARAMETERS {
    pub ulNewTVProtectionStandard: u32,
    pub ulAspectRatioChangeMask1: u32,
    pub ulAspectRatioData1: u32,
    pub ulAspectRatioChangeMask2: u32,
    pub ulAspectRatioData2: u32,
    pub ulAspectRatioChangeMask3: u32,
    pub ulAspectRatioData3: u32,
    pub ulReserved: [u32; 4],
    pub ulReserved2: [u32; 4],
    pub ulReserved3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_SET_HDCP_SRM_PARAMETERS {
    pub ulSRMVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKMDT_OPM_CREATE_VIDEO_OUTPUT_FOR_TARGET_PARAMETERS {
    pub AdapterLuid: u32,
    pub TargetId: u32,
    pub Vos: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_CAPS {
    pub SmoothBrightness: u32,
    pub AdaptiveBrightness: u32,
    pub NitsBrightness: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_STATE {
    pub SmoothBrightness: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BACKLIGHT_INFO {
    pub BacklightUsersetting: u16,
    pub BacklightEffective: u16,
    pub GammaRamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_SENSOR_DATA_CHROMATICITY {
    pub ChromaticityX: u32,
    pub ChromaticityY: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_SENSOR_DATA {
    pub Size: u32,
    pub AlsReadingValid: u32,
    pub ChromaticityValid: u32,
    pub ColorTemperatureValid: u32,
    pub Reserved: u32,
    pub ValidSensorValues: u32,
    pub AlsReading: u32,
    pub Chromaticity: u32,
    pub ColorTemperature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_SET_IN {
    pub BrightnessMillinits: u32,
    pub TransitionTimeMs: u32,
    pub SensorReadings: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_GET_OUT {
    pub CurrentBrightnessMillinits: u32,
    pub TargetBrightnessMillinits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_NIT_RANGE {
    pub MinimumLevelMillinit: u32,
    pub MaximumLevelMillinit: u32,
    pub StepSizeMillinit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_BRIGHTNESS_GET_NIT_RANGES_OUT {
    pub NormalRangeCount: u32,
    pub RangeCount: u32,
    pub PreferredMaximumBrightness: u32,
    pub SupportedRanges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_1_3_CAPS {
    pub SupportMiracast: u32,
    pub IsHybridIntegratedGPU: u32,
    pub IsHybridDiscreteGPU: u32,
    pub SupportPowerManagementPStates: u32,
    pub SupportVirtualModes: u32,
    pub SupportCrossAdapterResource: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NODEMETADATA_FLAGS {
    pub ContextSchedulingSupported: u32,
    pub RingBufferFenceRelease: u32,
    pub SupportTrackedWorkload: u32,
    pub UserModeSubmission: u32,
    pub SupportBuildTestCommandBuffer: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub MaxInFlightHwQueueBuffers: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NODEMETADATA {
    pub EngineType: u32,
    pub FriendlyName: [u32; 1],
    pub Flags: u32,
    pub Reserved: u32,
    pub GpuMmuSupported: u8,
    pub IoMmuSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUCLOCKDATA_FLAGS {
    pub ContextManagementProcessor: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NODE_PERFDATA {
    pub Frequency: u64,
    pub MaxFrequency: u64,
    pub MaxFrequencyOC: u64,
    pub Voltage: u32,
    pub VoltageMax: u32,
    pub VoltageMaxOC: u32,
    pub MaxTransitionLatency: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ADAPTER_PERFDATA {
    pub MemoryFrequency: u64,
    pub MaxMemoryFrequency: u64,
    pub MaxMemoryFrequencyOC: u64,
    pub MemoryBandwidth: u64,
    pub PCIEBandwidth: u64,
    pub FanRPM: u32,
    pub Power: u32,
    pub Temperature: u32,
    pub PowerStateOverride: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ADAPTER_PERFDATACAPS {
    pub MaxMemoryBandwidth: u64,
    pub MaxPCIEBandwidth: u64,
    pub MaxFanRPM: u32,
    pub TemperatureMax: u32,
    pub TemperatureWarning: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUVERSION {
    pub BiosVersion: [u32; 1],
    pub GpuArchitecture: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GPUCLOCKDATA {
    pub GpuFrequency: u64,
    pub GpuClockCounter: u64,
    pub CpuClockCounter: u64,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_2_0_CAPS {
    pub Support64BitAtomics: u32,
    pub GpuMmuSupported: u32,
    pub IoMmuSupported: u32,
    pub FlipOverwriteSupported: u32,
    pub SupportContextlessPresent: u32,
    pub SupportSurpriseRemoval: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_2_7_CAPS {
    pub HwSchSupported: u32,
    pub HwSchEnabled: u32,
    pub HwSchEnabledByDefault: u32,
    pub IndependentVidPnVSyncControl: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_2_9_CAPS {
    pub HwSchSupportState: u32,
    pub HwSchEnabled: u32,
    pub SelfRefreshMemorySupported: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_3_0_CAPS {
    pub HwFlipQueueSupportState: u32,
    pub HwFlipQueueEnabled: u32,
    pub DisplayableSupported: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WDDM_3_1_CAPS {
    pub NativeGpuFenceSupported: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_DISPLAYMUX_SET_INTERNAL_PANEL_INFO {
    pub Brightness3Supported: u8,
    pub Brightness3Caps: u32,
    pub Bridgtness3Ranges: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_TRACKEDWORKLOAD_SUPPORT {
    pub PhysicalAdapterIndex: u32,
    pub EngineType: u32,
    pub Support: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_NODEMETADATA {
    pub NodeOrdinalAndAdapterIndex: u32,
    pub NodeData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYCLOCKCALIBRATION {
    pub hAdapter: u32,
    pub NodeOrdinal: u32,
    pub PhysicalAdapterIndex: u32,
    pub ClockData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_FAULT_ERROR_CODE {
    pub IsDeviceSpecificCode: u32,
    pub GeneralErrorCode: u32,
    pub IsDeviceSpecificCodeReservedBit: u32,
    pub DeviceSpecificCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DRIVERCAPS_EXT {
    pub VirtualModeSupport: u32,
    pub Usb4MonitorSupport: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DISPLAY_CAPS {
    pub PreferPhysicallyContiguous: u64,
    pub CursorScaledWithMultiPlaneOverlayPlane0: u64,
    pub Reserved: u64,
    pub Value: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEDEVICEFLAGS {
    pub LegacyMode: u32,
    pub RequestVSync: u32,
    pub DisableGpuTimeout: u32,
    pub TestDevice: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEDEVICE {
    pub hAdapter: u32,
    pub pAdapter: usize,
    pub Flags: u32,
    pub hDevice: u32,
    pub pCommandBuffer_: u32,
    pub CommandBufferSize: u32,
    pub pAllocationList_: u32,
    pub AllocationListSize: u32,
    pub pPatchLocationList_: u32,
    pub PatchLocationListSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYDEVICE {
    pub hDevice: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATECONTEXT {
    pub hDevice: u32,
    pub NodeOrdinal: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub ClientHint: u32,
    pub hContext: u32,
    pub pCommandBuffer_: u32,
    pub CommandBufferSize: u32,
    pub pAllocationList_: u32,
    pub AllocationListSize: u32,
    pub pPatchLocationList_: u32,
    pub PatchLocationListSize: u32,
    pub CommandBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYCONTEXT {
    pub hContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATESYNCHRONIZATIONOBJECT {
    pub hDevice: u32,
    pub Info: u32,
    pub hSyncObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATESYNCHRONIZATIONOBJECT2 {
    pub hDevice: u32,
    pub Info: u32,
    pub hSyncObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATENATIVEFENCE_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATENATIVEFENCE {
    pub hDevice: u32,
    pub hSyncObject: u32,
    pub PrivateDriverData: [u8; 1],
    pub Info: u32,
    pub Flags: u32,
    pub Reserved: [u8; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETNATIVEFENCELOGDETAIL_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETNATIVEFENCELOGDETAIL {
    pub hHwQueue: u32,
    pub Flags: u32,
    pub WaitLogNumberOfEntries: u32,
    pub SignalLogNumberOfEntries: u32,
    pub WaitLogGpuBaseAddress: u32,
    pub SignalLogGpuBaseAddress: u32,
    pub Reserved: [u8; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYSYNCHRONIZATIONOBJECT {
    pub hSyncObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENSYNCHRONIZATIONOBJECT {
    pub hSharedHandle: u32,
    pub hSyncObject: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORSYNCHRONIZATIONOBJECT {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORSYNCHRONIZATIONOBJECT2 {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray: [u32; 1],
    pub FenceValue: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SIGNALSYNCHRONIZATIONOBJECT {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray: [u32; 1],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SIGNALSYNCHRONIZATIONOBJECT2 {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray: [u32; 1],
    pub Flags: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub FenceValue: u32,
    pub CpuEventHandle: usize,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_LOCK {
    pub hDevice: u32,
    pub hAllocation: u32,
    pub PrivateDriverData: u32,
    pub NumPages: u32,
    pub pPages_: u32,
    pub pData_: u32,
    pub Flags: u32,
    pub GpuVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UNLOCK {
    pub hDevice: u32,
    pub NumAllocations: u32,
    pub phAllocations_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_DISPLAYMODE_FLAGS {
    pub ValidatedAgainstMonitorCaps: u8,
    pub RoundedFakeMode: u8,
    pub ModePruningReason: u32,
    pub Reserved: u32,
    pub ValidatedAgainstMonitorCaps_1: u32,
    pub RoundedFakeMode_1: u32,
    pub ModePruningReason_1: u32,
    pub Stereo: u32,
    pub AdvancedScanCapable: u32,
    pub Reserved_1: u32,
    pub PreferredTiming: u32,
    pub PhysicalModeSupported: u32,
    pub Reserved_2: u32,
    pub VirtualRefreshRate: u32,
    pub Reserved_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DISPLAYMODE {
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub IntegerRefreshRate: u32,
    pub RefreshRate: u32,
    pub ScanLineOrdering: u32,
    pub DisplayOrientation: u32,
    pub DisplayFixedOutput: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETDISPLAYMODELIST {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub pModeList_: u32,
    pub ModeCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DISPLAYMODELIST {
    pub VidPnSourceId: u32,
    pub ModeCount: u32,
    pub pModeList: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETDISPLAYMODE_FLAGS {
    pub PreserveVidPn: u8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETDISPLAYMODE {
    pub hDevice: u32,
    pub hPrimaryAllocation: u32,
    pub ScanLineOrdering: u32,
    pub DisplayOrientation: u32,
    pub PrivateDriverFormatAttribute: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTISAMPLEMETHOD {
    pub NumSamples: u32,
    pub NumQualityLevels: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETMULTISAMPLEMETHODLIST {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub Width: u32,
    pub Height: u32,
    pub Format: u32,
    pub pMethodList_: u32,
    pub MethodCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENTFLAGS {
    pub Blt: u32,
    pub ColorFill: u32,
    pub Flip: u32,
    pub FlipDoNotFlip: u32,
    pub FlipDoNotWait: u32,
    pub FlipRestart: u32,
    pub DstRectValid: u32,
    pub SrcRectValid: u32,
    pub RestrictVidPnSource: u32,
    pub SrcColorKey: u32,
    pub DstColorKey: u32,
    pub LinearToSrgb: u32,
    pub PresentCountValid: u32,
    pub Rotate: u32,
    pub PresentToBitmap: u32,
    pub RedirectedFlip: u32,
    pub RedirectedBlt: u32,
    pub FlipStereo: u32,
    pub FlipStereoTemporaryMono: u32,
    pub FlipStereoPreferRight: u32,
    pub BltStereoUseRight: u32,
    pub PresentHistoryTokenOnly: u32,
    pub PresentRegionsValid: u32,
    pub PresentDDA: u32,
    pub ProtectedContentBlankedOut: u32,
    pub RemoteSession: u32,
    pub CrossAdapter: u32,
    pub DurationValid: u32,
    pub PresentIndirect: u32,
    pub PresentHMD: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPMODEL_PRESENTHISTORYTOKENFLAGS {
    pub Video: u32,
    pub RestrictedContent: u32,
    pub ClipToView: u32,
    pub StereoPreferRight: u32,
    pub TemporaryMono: u32,
    pub FlipRestart: u32,
    pub HDRMetaDataChanged: u32,
    pub AlphaMode: u32,
    pub SignalLimitOnTokenCompletion: u32,
    pub YCbCrFlags: u32,
    pub IndependentFlip: u32,
    pub IndependentFlipStage: u32,
    pub IndependentFlipReleaseCount: u32,
    pub IndependentFlipForceNotifyDwm: u32,
    pub UseCustomDuration: u32,
    pub IndependentFlipRequestDwmConfirm: u32,
    pub IndependentFlipCandidate: u32,
    pub IndependentFlipCheckNeeded: u32,
    pub IndependentFlipTrueImmediate: u32,
    pub IndependentFlipRequestDwmExit: u32,
    pub CompSurfaceNotifiedEarly: u32,
    pub IndependentFlipDoNotFlip: u32,
    pub RequirePairedToken: u32,
    pub VariableRefreshOverrideEligible: u32,
    pub VailToken: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DIRTYREGIONS {
    pub NumRects: u32,
    pub Rects: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_COMPOSITION_PRESENTHISTORYTOKEN {
    pub hPrivateData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPMANAGER_PRESENTHISTORYTOKEN {
    pub hPrivateData: u32,
    pub PresentAtQpc: u32,
    pub Discard: u32,
    pub PresentAt: u32,
    pub hPrivateDataIsPointer: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_AUXILIARYPRESENTINFO {
    pub size: u32,
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPMANAGER_AUXILIARYPRESENTINFO {
    pub auxiliaryPresentInfo: u32,
    pub flipManagerTracingId: u32,
    pub customDurationChanged: i32,
    pub pFlipManagerProcessedEvent: usize,
    pub FlipAdapterLuid: u32,
    pub VidPnSourceId: u32,
    pub independentFlipStage: u32,
    pub FlipCompletedQpc: u32,
    pub HwPresentDurationQpc: u32,
    pub WasCanceled: i32,
    pub ConvertedToNonIFlip: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GDIMODEL_PRESENTHISTORYTOKEN {
    pub hLogicalSurface: u32,
    pub hPhysicalSurface: u32,
    pub ScrollRect: u32,
    pub ScrollOffset: usize,
    pub DirtyRegions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GDIMODEL_SYSMEM_PRESENTHISTORYTOKEN {
    pub hlsurf: u32,
    pub dwDirtyFlags: u32,
    pub uiCookie: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FENCE_PRESENTHISTORYTOKEN {
    pub Key: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BLTMODEL_PRESENTHISTORYTOKEN {
    pub hLogicalSurface: u32,
    pub hPhysicalSurface: u32,
    pub EventId: u32,
    pub DirtyRegions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SCATTERBLT {
    pub hLogicalSurfaceDestination: u32,
    pub hDestinationCompSurfDWM: u32,
    pub DestinationCompositionBindingId: u32,
    pub SourceRect: u32,
    pub DestinationOffset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SCATTERBLTS {
    pub NumBlts: u32,
    pub Blts: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPMODEL_PRESENTHISTORYTOKEN {
    pub FenceValue: u32,
    pub hLogicalSurface: u32,
    pub dxgContext: u32,
    pub VidPnSourceId: u32,
    pub SwapChainIndex: u32,
    pub PresentLimitSemaphoreId: u32,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub hCompSurf: u32,
    pub compSurfLuid: u32,
    pub confirmationCookie: u32,
    pub CompositionSyncKey: u32,
    pub ScrollRect: u32,
    pub ScrollOffset: usize,
    pub PresentCount: u32,
    pub RevealColor: [u32; 4],
    pub Rotation: u32,
    pub ScatterBlts: u32,
    pub hSyncObject: usize,
    pub HDRMetaDataType: u32,
    pub HDRMetaDataHDR10: u32,
    pub HDRMetaDataHDR10Plus: u32,
    pub InkCookie: u32,
    pub SourceRect: u32,
    pub DestWidth: u32,
    pub DestHeight: u32,
    pub TargetRect: u32,
    pub Transform: [u32; 6],
    pub CustomDuration: u32,
    pub CustomDurationFlipInterval: u32,
    pub PlaneIndex: u32,
    pub ColorSpace: u32,
    pub DirtyRegions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SURFACECOMPLETE_PRESENTHISTORYTOKEN {
    pub hLogicalSurface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENTHISTORYTOKEN {
    pub Model: u32,
    pub TokenSize: u32,
    pub CompositionBindingId: u64,
    pub MaxSize: [u8; 1064],
    pub Flip: u32,
    pub Blt: u32,
    pub VistaBlt: u32,
    pub Gdi: u32,
    pub Fence: u32,
    pub GdiSysMem: u32,
    pub Composition: u32,
    pub FlipManager: u32,
    pub SurfaceComplete: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_RGNS {
    pub DirtyRectCount: u32,
    pub pDirtyRects_: u32,
    pub MoveRectCount: u32,
    pub pMoveRects_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT {
    pub hDevice: u32,
    pub hContext: u32,
    pub hWindow_: u32,
    pub VidPnSourceId: u32,
    pub hSource: u32,
    pub hDestination: u32,
    pub Color: u32,
    pub DstRect: u32,
    pub SrcRect: u32,
    pub SubRectCnt: u32,
    pub pSrcSubRects_: u32,
    pub PresentCount: u32,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub PresentLimitSemaphore: usize,
    pub PresentHistoryToken: u32,
    pub pPresentRegions: usize,
    pub hAdapter: u32,
    pub hIndirectContext: u32,
    pub Duration: u32,
    pub BroadcastSrcAllocation_: u32,
    pub BroadcastDstAllocation_: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: usize,
    pub bOptimizeForComposition: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_REDIRECTED_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_REDIRECTED {
    pub hSyncObj: u32,
    pub hDevice: u32,
    pub WaitedFenceValue: u32,
    pub PresentHistoryToken: u32,
    pub Flags: u32,
    pub hSource: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CANCEL_PRESENTS_FLAGS {
    pub NewVSyncInterruptState: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CANCEL_PRESENTS {
    pub cbSize: u32,
    pub hDevice: u32,
    pub Flags: u32,
    pub Operation: u32,
    pub CancelFromPresentId: u32,
    pub CompSurfaceLuid: u32,
    pub BindId: u32,
    pub hFlipManagerProcessedEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATE_DOORBELL_FLAGS {
    pub RequireSecondaryCpuVA: u32,
    pub ResizeRingBufferOperation: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATE_DOORBELL {
    pub hHwQueue: u32,
    pub hRingBuffer: u32,
    pub hRingBufferControl: u32,
    pub Flags: u32,
    pub PrivateDriverDataSize: u32,
    pub PrivateDriverData_: u32,
    pub DoorbellCPUVirtualAddress_: u32,
    pub DoorbellSecondaryCPUVirtualAddress_: u32,
    pub DoorbellStatusCPUVirtualAddress_: u32,
    pub HwQueueProgressFenceLastQueuedValueCPUVirtualAddress_: u32,
    pub hDoorbell: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CONNECT_DOORBELL_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CONNECT_DOORBELL {
    pub hDoorbell: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROY_DOORBELL {
    pub hDoorbell: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_NOTIFY_WORK_SUBMISSION_FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_NOTIFY_WORK_SUBMISSION {
    pub hDoorbell: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ISFEATUREENABLED {
    pub hAdapter: u32,
    pub FeatureId: u32,
    pub Result: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITPRESENTBLTTOHWQUEUE {
    pub hHwQueue: u32,
    pub HwQueueProgressFenceId: u32,
    pub PrivatePresentData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITPRESENTTOHWQUEUE {
    pub hHwQueues_: u32,
    pub PrivatePresentData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKMULTIPLANEOVERLAYSUPPORT {
    pub hDevice: u32,
    pub PlaneCount: u32,
    pub pOverlayPlanes_: u32,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_ATTRIBUTES2 {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub ClipRect: u32,
    pub Rotation: u32,
    pub Blend: u32,
    pub DirtyRectCount: u32,
    pub pDirtyRects_: u32,
    pub VideoFrameFormat: u32,
    pub ColorSpace: u32,
    pub StereoFormat: u32,
    pub StereoLeftViewFrame0: i32,
    pub StereoBaseViewFrame0: i32,
    pub StereoFlipMode: u32,
    pub StretchQuality: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECK_MULTIPLANE_OVERLAY_PLANE2 {
    pub LayerIndex: u32,
    pub hResource: u32,
    pub CompSurfaceLuid: u32,
    pub VidPnSourceId: u32,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKMULTIPLANEOVERLAYSUPPORT2 {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub PlaneCount: u32,
    pub pOverlayPlanes_: u32,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY2 {
    pub LayerIndex: u32,
    pub Enabled: i32,
    pub hAllocation: u32,
    pub PlaneAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_MULTIPLANE_OVERLAY2 {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub hContext: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub VidPnSourceId: u32,
    pub PresentCount: u32,
    pub FlipInterval: u32,
    pub Flags: u32,
    pub PresentPlaneCount: u32,
    pub pPresentPlanes_: u32,
    pub Duration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_ATTRIBUTES3 {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub ClipRect: u32,
    pub Rotation: u32,
    pub Blend: u32,
    pub DirtyRectCount: u32,
    pub pDirtyRects_: u32,
    pub ColorSpace: u32,
    pub StretchQuality: u32,
    pub SDRWhiteLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECK_MULTIPLANE_OVERLAY_PLANE3 {
    pub LayerIndex: u32,
    pub hResource: u32,
    pub CompSurfaceLuid: u32,
    pub VidPnSourceId: u32,
    pub pPlaneAttributes_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_POST_COMPOSITION_FLAGS {
    pub VerticalFlip: u32,
    pub HorizontalFlip: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_POST_COMPOSITION {
    pub Flags: u32,
    pub SrcRect: u32,
    pub DstRect: u32,
    pub Rotation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_POST_COMPOSITION_WITH_SOURCE {
    pub VidPnSourceId: u32,
    pub PostComposition: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKMULTIPLANEOVERLAYSUPPORT3 {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub PlaneCount: u32,
    pub ppOverlayPlanes: usize,
    pub PostCompositionCount: u32,
    pub ppPostComposition: usize,
    pub Supported: i32,
    pub ReturnInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PLANE_SPECIFIC_INPUT_FLAGS {
    pub Enabled: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PLANE_SPECIFIC_OUTPUT_FLAGS {
    pub FlipConvertedToImmediate: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY3 {
    pub LayerIndex: u32,
    pub InputFlags: u32,
    pub FlipInterval: u32,
    pub MaxImmediateFlipLine: u32,
    pub AllocationCount: u32,
    pub pAllocationList_: u32,
    pub DriverPrivateDataSize: u32,
    pub pDriverPrivateData_: u32,
    pub pPlaneAttributes_: u32,
    pub hFlipToFence: u32,
    pub hFlipAwayFence: u32,
    pub FlipToFenceValue: u32,
    pub FlipAwayFenceValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_MULTIPLANE_OVERLAY_FLAGS {
    pub FlipStereo: u32,
    pub FlipStereoTemporaryMono: u32,
    pub FlipStereoPreferRight: u32,
    pub FlipDoNotWait: u32,
    pub FlipDoNotFlip: u32,
    pub FlipRestart: u32,
    pub DurationValid: u32,
    pub HDRMetaDataValid: u32,
    pub HMD: u32,
    pub TrueImmediate: u32,
    pub FromDDisplay: u32,
    pub IndirectDisplay: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_MULTIPLANE_OVERLAY3 {
    pub hAdapter: u32,
    pub ContextCount: u32,
    pub pContextList_: u32,
    pub VidPnSourceId: u32,
    pub PresentCount: u32,
    pub Flags: u32,
    pub PresentPlaneCount: u32,
    pub ppPresentPlanes: usize,
    pub pPostComposition_: u32,
    pub Duration: u32,
    pub HDRMetaDataType: u32,
    pub HDRMetaDataSize: u32,
    pub pHDRMetaData_: u32,
    pub BoostRefreshRateMultiplier: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANE_OVERLAY_CAPS {
    pub Rotation: u32,
    pub RotationWithoutIndependentFlip: u32,
    pub VerticalFlip: u32,
    pub HorizontalFlip: u32,
    pub StretchRGB: u32,
    pub StretchYUV: u32,
    pub BilinearFilter: u32,
    pub HighFilter: u32,
    pub Shared: u32,
    pub Immediate: u32,
    pub Plane0ForVirtualModeOnly: u32,
    pub Version3DDISupport: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_MULTIPLANE_OVERLAY_CAPS {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub MaxPlanes: u32,
    pub MaxRGBPlanes: u32,
    pub MaxYUVPlanes: u32,
    pub OverlayCaps: u32,
    pub MaxStretchFactor: u32,
    pub MaxShrinkFactor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_POST_COMPOSITION_CAPS {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub MaxStretchFactor: u32,
    pub MaxShrinkFactor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANEOVERLAY_STRETCH_SUPPORT {
    pub VidPnSourceId: u32,
    pub Update: i32,
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RENDERFLAGS {
    pub ResizeCommandBuffer: u32,
    pub ResizeAllocationList: u32,
    pub ResizePatchLocationList: u32,
    pub NullRendering: u32,
    pub PresentRedirected: u32,
    pub RenderKm: u32,
    pub RenderKmReadback: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPLPRESENTFLAGS {
    pub ProtectedContentBlankedOut: u32,
    pub RemoteSession: u32,
    pub FullScreenPresent: u32,
    pub PresentIndirect: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPLPRESENT {
    pub hContext: u32,
    pub hSource: u32,
    pub VidPnSourceId: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub PresentRegions: u32,
    pub Flags: u32,
    pub hIndirectContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPLPRESENTTOHWQUEUE {
    pub hSource: u32,
    pub VidPnSourceId: u32,
    pub BroadcastHwQueueCount: u32,
    pub hHwQueues_: u32,
    pub PresentRegions: u32,
    pub Flags: u32,
    pub hIndirectHwQueue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RENDER {
    pub hDevice: u32,
    pub hContext: u32,
    pub CommandOffset: u32,
    pub CommandLength: u32,
    pub AllocationCount: u32,
    pub PatchLocationCount: u32,
    pub pNewCommandBuffer_: u32,
    pub NewCommandBufferSize: u32,
    pub pNewAllocationList_: u32,
    pub NewAllocationListSize: u32,
    pub pNewPatchLocationList_: u32,
    pub NewPatchLocationListSize: u32,
    pub Flags: u32,
    pub PresentHistoryToken: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub QueuedBufferCount: u32,
    pub NewCommandBuffer: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_STANDARDALLOCATION_EXISTINGHEAP {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATESTANDARDALLOCATIONFLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATESTANDARDALLOCATION {
    pub Type: u32,
    pub ExistingHeapData: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEALLOCATIONFLAGS {
    pub CreateResource: u32,
    pub CreateShared: u32,
    pub NonSecure: u32,
    pub CreateProtected: u32,
    pub RestrictSharedAccess: u32,
    pub ExistingSysMem: u32,
    pub NtSecuritySharing: u32,
    pub ReadOnly: u32,
    pub CreateWriteCombined: u32,
    pub CreateCached: u32,
    pub SwapChainBackBuffer: u32,
    pub CrossAdapter: u32,
    pub OpenCrossAdapter: u32,
    pub PartialSharedCreation: u32,
    pub Zeroed: u32,
    pub WriteWatch: u32,
    pub StandardAllocation: u32,
    pub ExistingSection: u32,
    pub AllowNotZeroed: u32,
    pub PhysicallyContiguous: u32,
    pub NoKmdAccess: u32,
    pub SharedDisplayable: u32,
    pub NoImplicitSynchronization: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub Reserved_6: u32,
    pub Reserved_7: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEALLOCATION {
    pub hDevice: u32,
    pub hResource: u32,
    pub hGlobalShare: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub pStandardAllocation: usize,
    pub pPrivateDriverData: usize,
    pub PrivateDriverDataSize: u32,
    pub NumAllocations: u32,
    pub pAllocationInfo: usize,
    pub pAllocationInfo2: usize,
    pub Flags: u32,
    pub hPrivateRuntimeResourceHandle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENRESOURCE {
    pub hDevice: u32,
    pub hGlobalShare: u32,
    pub NumAllocations: u32,
    pub pOpenAllocationInfo: usize,
    pub pOpenAllocationInfo2: usize,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub pResourcePrivateDriverData_: u32,
    pub ResourcePrivateDriverDataSize: u32,
    pub pTotalPrivateDriverDataBuffer_: u32,
    pub TotalPrivateDriverDataBufferSize: u32,
    pub hResource: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENRESOURCEFROMNTHANDLE {
    pub hDevice: u32,
    pub hNtHandle_: u32,
    pub NumAllocations: u32,
    pub pOpenAllocationInfo2_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub pPrivateRuntimeData_: u32,
    pub ResourcePrivateDriverDataSize: u32,
    pub pResourcePrivateDriverData_: u32,
    pub TotalPrivateDriverDataBufferSize: u32,
    pub pTotalPrivateDriverDataBuffer_: u32,
    pub hResource: u32,
    pub hKeyedMutex: u32,
    pub pKeyedMutexPrivateRuntimeData_: u32,
    pub KeyedMutexPrivateRuntimeDataSize: u32,
    pub hSyncObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENSYNCOBJECTFROMNTHANDLE {
    pub hNtHandle_: u32,
    pub hSyncObject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENSYNCOBJECTFROMNTHANDLE2 {
    pub hNtHandle_: u32,
    pub hDevice: u32,
    pub Flags: u32,
    pub hSyncObject: u32,
    pub FenceValueCPUVirtualAddress_: u32,
    pub FenceValueGPUVirtualAddress: u32,
    pub EngineAffinity: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENNATIVEFENCEFROMNTHANDLE {
    pub hNtHandle_: u32,
    pub hDevice: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub hSyncObject: u32,
    pub NativeFenceMapping: u32,
    pub PrivateDriverData: [u8; 1],
    pub Reserved: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENSYNCOBJECTNTHANDLEFROMNAME {
    pub dwDesiredAccess: u32,
    pub pObjAttrib_: u32,
    pub hNtHandle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENNTHANDLEFROMNAME {
    pub dwDesiredAccess: u32,
    pub pObjAttrib_: u32,
    pub hNtHandle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYRESOURCEINFOFROMNTHANDLE {
    pub hDevice: u32,
    pub hNtHandle_: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub TotalPrivateDriverDataSize: u32,
    pub ResourcePrivateDriverDataSize: u32,
    pub NumAllocations: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYRESOURCEINFO {
    pub hDevice: u32,
    pub hGlobalShare: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub TotalPrivateDriverDataSize: u32,
    pub ResourcePrivateDriverDataSize: u32,
    pub NumAllocations: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYALLOCATION {
    pub hDevice: u32,
    pub hResource: u32,
    pub phAllocationList_: u32,
    pub AllocationCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYALLOCATION2 {
    pub hDevice: u32,
    pub hResource: u32,
    pub phAllocationList_: u32,
    pub AllocationCount: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETALLOCATIONPRIORITY {
    pub hDevice: u32,
    pub hResource: u32,
    pub phAllocationList_: u32,
    pub AllocationCount: u32,
    pub pPriorities_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYALLOCATIONRESIDENCY {
    pub hDevice: u32,
    pub hResource: u32,
    pub phAllocationList_: u32,
    pub AllocationCount: u32,
    pub pResidencyStatus_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETRUNTIMEDATA {
    pub hAdapter: u32,
    pub hGlobalShare: u32,
    pub pRuntimeData_: u32,
    pub RuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UMDFILENAMEINFO {
    pub Version: u32,
    pub UmdFileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENGLINFO {
    pub UmdOpenGlIcdFileName: [u32; 1],
    pub Version: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SEGMENTSIZEINFO {
    pub DedicatedVideoMemorySize: u32,
    pub DedicatedSystemMemorySize: u32,
    pub SharedSystemMemorySize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SEGMENTGROUPSIZEINFO {
    pub PhysicalAdapterIndex: u32,
    pub LegacyInfo: u32,
    pub LocalMemory: u32,
    pub NonLocalMemory: u32,
    pub NonBudgetMemory: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WORKINGSETFLAGS {
    pub UseDefault: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WORKINGSETINFO {
    pub Flags: u32,
    pub MinimumWorkingSetPercentile: u32,
    pub MaximumWorkingSetPercentile: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPINFOFLAGS {
    pub FlipInterval: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPQUEUEINFO {
    pub MaxHardwareFlipQueueLength: u32,
    pub MaxSoftwareFlipQueueLength: u32,
    pub FlipFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTERADDRESS {
    pub BusNumber: u32,
    pub DeviceNumber: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTERREGISTRYINFO {
    pub AdapterString: [u32; 1],
    pub BiosString: [u32; 1],
    pub DacType: [u32; 1],
    pub ChipType: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CURRENTDISPLAYMODE {
    pub VidPnSourceId: u32,
    pub DisplayMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTERTYPE {
    pub RenderSupported: u32,
    pub DisplaySupported: u32,
    pub SoftwareDevice: u32,
    pub PostDevice: u32,
    pub HybridDiscrete: u32,
    pub HybridIntegrated: u32,
    pub IndirectDisplayDevice: u32,
    pub Paravirtualized: u32,
    pub ACGSupported: u32,
    pub SupportSetTimingsFromVidPn: u32,
    pub Detachable: u32,
    pub ComputeOnly: u32,
    pub Prototype: u32,
    pub RuntimePowerManagement: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPLCONTEXTSCOUNT {
    pub VidPnSourceId: u32,
    pub OutputDuplicationCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UMD_DRIVER_VERSION {
    pub DriverVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_KMD_DRIVER_VERSION {
    pub DriverVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DIRECTFLIP_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANEOVERLAY_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANEOVERLAY_HUD_SUPPORT {
    pub VidPnSourceId: u32,
    pub Update: i32,
    pub KernelSupported: i32,
    pub HudSupported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DLIST_DRIVER_NAME {
    pub DListFileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CPDRIVERNAME {
    pub ContentProtectionFileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MIRACASTCOMPANIONDRIVERNAME {
    pub MiracastCompanionDriverName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_XBOX {
    pub IsXBOX: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_INDEPENDENTFLIP_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANEOVERLAY_DECODE_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ISBADDRIVERFORHWPROTECTIONDISABLED {
    pub Disabled: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MULTIPLANEOVERLAY_SECONDARY_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_INDEPENDENTFLIP_SECONDARY_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PANELFITTER_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PHYSICAL_ADAPTER_COUNT {
    pub Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICE_IDS {
    pub VendorID: u32,
    pub DeviceID: u32,
    pub SubVendorID: u32,
    pub SubSystemID: u32,
    pub RevisionID: u32,
    pub BusType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_DEVICE_IDS {
    pub PhysicalAdapterIndex: u32,
    pub DeviceIds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_PHYSICAL_ADAPTER_PNP_KEY {
    pub PhysicalAdapterIndex: u32,
    pub PnPKeyType: u32,
    pub pDest_: u32,
    pub pCchDest_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_MIRACAST_DRIVER_TYPE {
    pub MiracastDriverType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GPUMMU_CAPS {
    pub ReadOnlyMemorySupported: u32,
    pub NoExecuteMemorySupported: u32,
    pub CacheCoherentMemorySupported: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub VirtualAddressBitCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_GPUMMU_CAPS {
    pub PhysicalAdapterIndex: u32,
    pub Caps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MPO3DDI_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_HWDRM_SUPPORT {
    pub Supported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MPOKERNELCAPS_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_DEVICE_VIDPN_OWNERSHIP_INFO {
    pub hDevice: u32,
    pub bFailedDwmAcquireVidPn: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BLOCKLIST_INFO {
    pub Size: u32,
    pub BlockList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_ADAPTER_UNIQUE_GUID {
    pub AdapterUniqueGUID: [u32; 40],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_NODE_PERFDATA {
    pub NodeOrdinal: u32,
    pub PhysicalAdapterIndex: u32,
    pub Frequency: u32,
    pub MaxFrequency: u32,
    pub MaxFrequencyOC: u32,
    pub Voltage: u32,
    pub VoltageMax: u32,
    pub VoltageMaxOC: u32,
    pub MaxTransitionLatency: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTER_PERFDATA {
    pub PhysicalAdapterIndex: u32,
    pub MemoryFrequency: u32,
    pub MaxMemoryFrequency: u32,
    pub MaxMemoryFrequencyOC: u32,
    pub MemoryBandwidth: u32,
    pub PCIEBandwidth: u32,
    pub FanRPM: u32,
    pub Power: u32,
    pub Temperature: u32,
    pub PowerStateOverride: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTER_PERFDATACAPS {
    pub PhysicalAdapterIndex: u32,
    pub MaxMemoryBandwidth: u32,
    pub MaxPCIEBandwidth: u32,
    pub MaxFanRPM: u32,
    pub TemperatureMax: u32,
    pub TemperatureWarning: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GPUVERSION {
    pub PhysicalAdapterIndex: u32,
    pub BiosVersion: [u32; 1],
    pub GpuArchitecture: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DRIVER_DESCRIPTION {
    pub DriverDescription: [u32; 4096],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERY_SCANOUT_CAPS {
    pub VidPnSourceId: u32,
    pub Caps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DISPLAY_UMD_FILENAMEINFO {
    pub Version: u32,
    pub UmdFileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WSAUMDIMAGENAME {
    pub WsaUmdImageName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VGPUINTERFACEID {
    pub VirtualGpuIntefaceId: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PARAVIRTUALIZATION {
    pub SecureContainer: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_HYBRID_DLIST_DLL_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_HYBRID_DLIST_DLL_MUX_SUPPORT {
    pub Supported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CROSSADAPTERRESOURCE_SUPPORT {
    pub SupportTier: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYADAPTERINFO {
    pub hAdapter: u32,
    pub Type: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENADAPTERFROMHDC {
    pub hDc_: u32,
    pub hAdapter: u32,
    pub AdapterLuid: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENADAPTERFROMGDIDISPLAYNAME {
    pub DeviceName: [u32; 32],
    pub hAdapter: u32,
    pub AdapterLuid: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENADAPTERFROMDEVICENAME {
    pub pDeviceName_: u32,
    pub hAdapter: u32,
    pub AdapterLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTERINFO {
    pub hAdapter: u32,
    pub AdapterLuid: u32,
    pub NumOfSources: u32,
    pub bPrecisePresentRegionsPreferred: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ENUMADAPTERS {
    pub NumAdapters: u32,
    pub Adapters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ENUMADAPTERS2 {
    pub NumAdapters: u32,
    pub pAdapters_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENADAPTERFROMLUID {
    pub AdapterLuid: u32,
    pub hAdapter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYREMOTEVIDPNSOURCEFROMGDIDISPLAYNAME {
    pub DeviceName: [u32; 32],
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ENUMADAPTERS3 {
    pub Filter: u32,
    pub NumAdapters: u32,
    pub pAdapters_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CLOSEADAPTER {
    pub hAdapter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETSHAREDPRIMARYHANDLE {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub hSharedPrimary: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SHAREDPRIMARYLOCKNOTIFICATION {
    pub AdapterLuid: u32,
    pub VidPnSourceId: u32,
    pub LockRect: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SHAREDPRIMARYUNLOCKNOTIFICATION {
    pub AdapterLuid: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PINDIRECTFLIPRESOURCES {
    pub hDevice: u32,
    pub ResourceCount: u32,
    pub pResourceList_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UNPINDIRECTFLIPRESOURCES {
    pub hDevice: u32,
    pub ResourceCount: u32,
    pub pResourceList_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DOD_SET_DIRTYRECT_MODE {
    pub bForceFullScreenDirty: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_HISTORY_BUFFER_STATUS {
    pub Enabled: u8,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VAD_DESC {
    pub VadIndex: u32,
    pub VadAddress: u32,
    pub NumMappedRanges: u32,
    pub VadType: u32,
    pub StartAddress: u32,
    pub EndAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VA_RANGE_DESC {
    pub VadAddress: u32,
    pub VaRangeIndex: u32,
    pub PhysicalAdapterIndex: u32,
    pub StartAddress: u32,
    pub EndAddress: u32,
    pub DriverProtection: u32,
    pub OwnerType: u32,
    pub pOwner: u32,
    pub OwnerOffset: u32,
    pub Protection: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_EVICTION_CRITERIA {
    pub MinimumSize: u32,
    pub MaximumSize: u32,
    pub Primary: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PAGE_TABLE_LEVEL_DESC {
    pub IndexBitCount: u32,
    pub IndexMask: u32,
    pub IndexShift: u32,
    pub LowerLevelsMask: u32,
    pub EntryCoverageInPages: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ESCAPE_GPUMMUCAPS {
    pub ReadOnlyMemorySupported: u8,
    pub NoExecuteMemorySupported: u8,
    pub ZeroInPteSupported: u8,
    pub CacheCoherentMemorySupported: u8,
    pub LargePageSupported: u8,
    pub DualPteSupported: u8,
    pub AllowNonAlignedLargePageAddress: u8,
    pub PageTable64KSupported: u8,
    pub Reserved: u8,
    pub VirtualAddressBitCount: u32,
    pub PageTableLevelCount: u32,
    pub PageTableLevelDesk: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_GPUMMU_CAPS {
    pub PhysicalAdapterIndex: u32,
    pub GpuMmuCaps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_PTE {
    pub PhysicalAdapterIndex: u32,
    pub PageTableLevel: u32,
    pub PageTableIndex: [u32; 1],
    pub b64KBPte: u8,
    pub NumPtes: u32,
    pub Pte: [u32; 1],
    pub NumValidEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_PTE_EXT {
    pub DriverProtection: [u64; 1],
    pub AllocationData: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SEGMENT_CAPS {
    pub Size: u32,
    pub PageSize: u32,
    pub SegmentId: u32,
    pub bAperture: u8,
    pub bReservedSysMem: u8,
    pub BudgetGroup: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GET_SEGMENT_CAPS {
    pub PhysicalAdapterIndex: u32,
    pub NumSegments: u32,
    pub SegmentCaps: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ESCAPE_VIRTUAL_REFRESH_RATE {
    pub Type: u32,
    pub VidPnSourceId: u32,
    pub ProcessBoostEligible: u8,
    pub VSyncMultiplier: u32,
    pub BaseDesktopDuration: u32,
    pub Reserved: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VIDMM_ESCAPE {
    pub Type: u32,
    pub ProbeAndLock: u32,
    pub SplitPoint: u32,
    pub NoDemotion: u32,
    pub SwizzlingAperture: u32,
    pub PagingPathLockSubRange: u32,
    pub PagingPathLockMinRange: u32,
    pub ComplexLock: u32,
    pub FailVARotation: u32,
    pub NoWriteCombined: u32,
    pub NoPrePatching: u32,
    pub AlwaysRepatch: u32,
    pub ExpectPreparationFailure: u32,
    pub FailUserModeVAMapping: u32,
    pub NeverDiscardOfferedAllocation: u32,
    pub AlwaysDiscardOfferedAllocation: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
    pub ResourceHandle: u32,
    pub AllocationHandle: u32,
    pub hProcess_: u32,
    pub NtHandle: u32,
    pub NumVads: u32,
    pub GetVad: u32,
    pub GetVadRange: u32,
    pub GetGpuMmuCaps: u32,
    pub GetPte: u32,
    pub GetSegmentCaps: u32,
    pub Command: u32,
    pub Status: u32,
    pub GetPteExt: u32,
    pub LocalMemoryBudget: u32,
    pub SystemMemoryBudget: u32,
    pub hProcess__1: u32,
    pub bAllowWakeOnSubmission: i32,
    pub hProcess__2: u32,
    pub NumBytesToTrim: u32,
    pub MinTrimInterval: u32,
    pub MaxTrimInterval: u32,
    pub IdleTrimInterval: u32,
    pub EvictByCriteria: u32,
    pub bFlush: i32,
    pub Operation: u32,
    pub SegmentId: u32,
    pub TotalCommitted: u32,
    pub TotalFree: u32,
    pub LargestGapBefore: u32,
    pub LargestGapAfter: u32,
    pub hPagingQueue: u32,
    pub PhysicalAdapterIndex: u32,
    pub Milliseconds: u32,
    pub PagingFenceValue: u32,
    pub PhysicalAdapterIndex_1: u32,
    pub SegmentId_1: u32,
    pub TimerValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VIDSCH_ESCAPE {
    pub Type: u32,
    pub PreemptionControl: i32,
    pub EnableContextDelay: i32,
    pub TdrControl: u32,
    pub NodeOrdinal: u32,
    pub SuspendScheduler: i32,
    pub TdrControl_1: u32,
    pub SuspendTime: u32,
    pub Count: u32,
    pub Time: u32,
    pub PfnControl: usize,
    pub VirtualRefreshRateControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_TDRDBGCTRL_ESCAPE {
    pub TdrControl: u32,
    pub NodeOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DMM_ESCAPE {
    pub Type: u32,
    pub ProvidedBufferSize: u32,
    pub MinRequiredBufferSize: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BRIGHTNESS_POSSIBLE_LEVELS {
    pub LevelCount: u8,
    pub BrightnessLevels: [u8; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BRIGHTNESS_INFO {
    pub Type: u32,
    pub ChildUid: u32,
    pub PossibleLevels: u32,
    pub Brightness: u8,
    pub BrightnessCaps: u32,
    pub BrightnessState: u32,
    pub OptimizationLevel: u32,
    pub ReductionInfo: u32,
    pub VerboseLogging: u8,
    pub NitRanges: u32,
    pub GetBrightnessMillinits: u32,
    pub SetBrightnessMillinits: u32,
    pub BrightnessInterfaceSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BDDFALLBACK_CTL {
    pub ForceBddHeadlessNextFallback: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_REQUEST_MACHINE_CRASH_ESCAPE {
    pub Param1: u32,
    pub Param2: u32,
    pub Param3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PROCESS_VERIFIER_VIDMM_RESTRICT_BUDGET {
    pub LocalBudget: u32,
    pub NonLocalBudget: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PROCESS_VERIFIER_OPTION {
    pub hProcess_: u32,
    pub Type: u32,
    pub Mode: u32,
    pub Data: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTER_VERIFIER_VIDMM_TRIM_INTERVAL {
    pub MinimumTrimInterval: u32,
    pub MaximumTrimInterval: u32,
    pub IdleTrimInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADAPTER_VERIFIER_OPTION {
    pub Type: u32,
    pub Mode: u32,
    pub Data: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICE_ESCAPE {
    pub Type: u32,
    pub hPrimaryAllocation: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEBUG_SNAPSHOT_ESCAPE {
    pub Length: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OUTPUTDUPL_CONTEXT_DEBUG_INFO {
    pub Status: u32,
    pub ProcessID_: u32,
    pub AccumulatedPresents: u32,
    pub LastPresentTime: u32,
    pub LastMouseTime: u32,
    pub ProcessName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_SNAPSHOT {
    pub Size: u32,
    pub SessionProcessCount: u32,
    pub SessionActiveConnectionsCount: u32,
    pub NumVidPnSources: u32,
    pub NumOutputDuplContexts: u32,
    pub Padding: u32,
    pub OutputDuplDebugInfos: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ACTIVATE_SPECIFIC_DIAG_ESCAPE {
    pub Type: u32,
    pub Activate: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ESCAPE {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub Type: u32,
    pub Flags: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub hContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_COUNTER {
    pub Count: u32,
    pub Bytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_DMA_PACKET_TYPE_INFORMATION {
    pub PacketSubmited: u32,
    pub PacketCompleted: u32,
    pub PacketPreempted: u32,
    pub PacketFaulted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUEUE_PACKET_TYPE_INFORMATION {
    pub PacketSubmited: u32,
    pub PacketCompleted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PACKET_INFORMATION {
    pub QueuePacket: [u32; 1],
    pub DmaPacket: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PREEMPTION_INFORMATION {
    pub PreemptionCounter: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_NODE_INFORMATION {
    pub RunningTime: u32,
    pub ContextSwitch: u32,
    pub PreemptionStatistics: u32,
    pub PacketStatistics: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_NODE_INFORMATION {
    pub GlobalInformation: u32,
    pub SystemInformation: u32,
    pub NodePerfData: u32,
    pub Reserved: [u32; 3],
    pub Reserved_1: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_VIDPNSOURCE_INFORMATION {
    pub Frame: u32,
    pub CancelledFrame: u32,
    pub QueuedPresent: u32,
    pub Padding: u32,
    pub IsVSyncEnabled: u32,
    pub VSyncOnTotalTimeMs: u32,
    pub VSyncOffKeepPhaseTotalTimeMs: u32,
    pub VSyncOffNoPhaseTotalTimeMs: u32,
    pub Reserved: [u32; 4],
    pub Reserved_1: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_VIDPNSOURCE_INFORMATION {
    pub GlobalInformation: u32,
    pub SystemInformation: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_REFERENCE_DMA_BUFFER {
    pub NbCall: u32,
    pub NbAllocationsReferenced: u32,
    pub MaxNbAllocationsReferenced: u32,
    pub NbNULLReference: u32,
    pub NbWriteReference: u32,
    pub NbRenamedAllocationsReferenced: u32,
    pub NbIterationSearchingRenamedAllocation: u32,
    pub NbLockedAllocationReferenced: u32,
    pub NbAllocationWithValidPrepatchingInfoReferenced: u32,
    pub NbAllocationWithInvalidPrepatchingInfoReferenced: u32,
    pub NbDMABufferSuccessfullyPrePatched: u32,
    pub NbPrimariesReferencesOverflow: u32,
    pub NbAllocationWithNonPreferredResources: u32,
    pub NbAllocationInsertedInMigrationTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_RENAMING {
    pub NbAllocationsRenamed: u32,
    pub NbAllocationsShrinked: u32,
    pub NbRenamedBuffer: u32,
    pub MaxRenamingListLength: u32,
    pub NbFailuresDueToRenamingLimit: u32,
    pub NbFailuresDueToCreateAllocation: u32,
    pub NbFailuresDueToOpenAllocation: u32,
    pub NbFailuresDueToLowResource: u32,
    pub NbFailuresDueToNonRetiredLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_PREPRATION {
    pub BroadcastStall: u32,
    pub NbDMAPrepared: u32,
    pub NbDMAPreparedLongPath: u32,
    pub ImmediateHighestPreparationPass: u32,
    pub AllocationsTrimmed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_PAGING_FAULT {
    pub Faults: u32,
    pub FaultsFirstTimeAccess: u32,
    pub FaultsReclaimed: u32,
    pub FaultsMigration: u32,
    pub FaultsIncorrectResource: u32,
    pub FaultsLostContent: u32,
    pub FaultsEvicted: u32,
    pub AllocationsMEM_RESET: u32,
    pub AllocationsUnresetSuccess: u32,
    pub AllocationsUnresetFail: u32,
    pub AllocationsUnresetSuccessRead: u32,
    pub AllocationsUnresetFailRead: u32,
    pub Evictions: u32,
    pub EvictionsDueToPreparation: u32,
    pub EvictionsDueToLock: u32,
    pub EvictionsDueToClose: u32,
    pub EvictionsDueToPurge: u32,
    pub EvictionsDueToSuspendCPUAccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_PAGING_TRANSFER {
    pub BytesFilled: u32,
    pub BytesDiscarded: u32,
    pub BytesMappedIntoAperture: u32,
    pub BytesUnmappedFromAperture: u32,
    pub BytesTransferredFromMdlToMemory: u32,
    pub BytesTransferredFromMemoryToMdl: u32,
    pub BytesTransferredFromApertureToMemory: u32,
    pub BytesTransferredFromMemoryToAperture: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_SWIZZLING_RANGE {
    pub NbRangesAcquired: u32,
    pub NbRangesReleased: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_LOCKS {
    pub NbLocks: u32,
    pub NbLocksWaitFlag: u32,
    pub NbLocksDiscardFlag: u32,
    pub NbLocksNoOverwrite: u32,
    pub NbLocksNoReadSync: u32,
    pub NbLocksLinearization: u32,
    pub NbComplexLocks: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_ALLOCATIONS {
    pub Created: u32,
    pub Destroyed: u32,
    pub Opened: u32,
    pub Closed: u32,
    pub MigratedSuccess: u32,
    pub MigratedFail: u32,
    pub MigratedAbandoned: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATSTICS_TERMINATIONS {
    pub TerminatedShared: u32,
    pub TerminatedNonShared: u32,
    pub DestroyedShared: u32,
    pub DestroyedNonShared: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_ADAPTER_INFORMATION_FLAGS {
    pub NumberOfMemoryGroups: u64,
    pub SupportsDemotion: u64,
    pub Reserved: u64,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_ADAPTER_INFORMATION {
    pub NbSegments: u32,
    pub NodeCount: u32,
    pub VidPnSourceCount: u32,
    pub VSyncEnabled: u32,
    pub TdrDetectedCount: u32,
    pub ZeroLengthDmaBuffers: u32,
    pub RestartedPeriod: u32,
    pub ReferenceDmaBuffer: u32,
    pub Renaming: u32,
    pub Preparation: u32,
    pub PagingFault: u32,
    pub PagingTransfer: u32,
    pub SwizzlingRange: u32,
    pub Locks: u32,
    pub Allocations: u32,
    pub Terminations: u32,
    pub Flags: u32,
    pub Reserved: [u32; 7],
    pub Reserved_1: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PHYSICAL_ADAPTER_INFORMATION {
    pub AdapterPerfData: u32,
    pub AdapterPerfDataCaps: u32,
    pub GpuVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_SYSTEM_MEMORY {
    pub BytesAllocated: u32,
    pub BytesReserved: u32,
    pub SmallAllocationBlocks: u32,
    pub LargeAllocationBlocks: u32,
    pub WriteCombinedBytesAllocated: u32,
    pub WriteCombinedBytesReserved: u32,
    pub CachedBytesAllocated: u32,
    pub CachedBytesReserved: u32,
    pub SectionBytesAllocated: u32,
    pub SectionBytesReserved: u32,
    pub BytesZeroed: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_INFORMATION {
    pub NodeCount: u32,
    pub VidPnSourceCount: u32,
    pub SystemMemory: u32,
    pub Reserved: [u32; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_DMA_BUFFER {
    pub Size: u32,
    pub AllocationListBytes: u32,
    pub PatchLocationListBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_COMMITMENT_DATA {
    pub TotalBytesEvictedFromProcess: u32,
    pub BytesBySegmentPreference: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_POLICY {
    pub PreferApertureForRead: [u32; 1],
    pub PreferAperture: [u32; 1],
    pub MemResetOnPaging: u32,
    pub RemovePagesFromWorkingSetOnPaging: u32,
    pub MigrationEnabled: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_INTERFERENCE_COUNTERS {
    pub InterferenceCount: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_ADAPTER_INFORMATION {
    pub NbSegments: u32,
    pub NodeCount: u32,
    pub VidPnSourceCount: u32,
    pub VirtualMemoryUsage: u32,
    pub DmaBuffer: u32,
    pub CommitmentData: u32,
    pub field_Policy: u32,
    pub ProcessInterferenceCounters: u32,
    pub Reserved: [u32; 9],
    pub ClientHint: u32,
    pub Reserve: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_MEMORY {
    pub TotalBytesEvicted: u32,
    pub AllocsCommitted: u32,
    pub AllocsResident: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_SEGMENT_INFORMATION {
    pub CommitLimit: u32,
    pub BytesCommitted: u32,
    pub BytesResident: u32,
    pub CommitLimit_1: u32,
    pub BytesCommitted_1: u32,
    pub BytesResident_1: u32,
    pub Memory: u32,
    pub Aperture: u32,
    pub TotalBytesEvictedByPriority: [u32; 1],
    pub SystemMemoryEndAddress: u32,
    pub PreservedDuringStandby: u64,
    pub PreservedDuringHibernate: u64,
    pub PartiallyPreservedDuringHibernate: u64,
    pub Reserved: u64,
    pub SystemMemory: u64,
    pub PopulatedByReservedDDRByFirmware: u64,
    pub SegmentType: u64,
    pub SegmentGroup: u64,
    pub FullyCPUVisible: u64,
    pub Reserved_1: u64,
    pub Reserved_2: u64,
    pub Reserved_3: u64,
    pub Reserved_4: [u32; 5],
    pub Reserved_5: [u32; 6],
    pub Reserved_6: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_VIDEO_MEMORY {
    pub AllocsCommitted: u32,
    pub AllocsResidentInP: [u32; 1],
    pub AllocsResidentInNonPreferred: u32,
    pub TotalBytesEvictedDueToPreparation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_SEGMENT_POLICY {
    pub UseMRU: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_SEGMENT_INFORMATION {
    pub BytesCommitted: u32,
    pub MaximumWorkingSet: u32,
    pub MinimumWorkingSet: u32,
    pub NbReferencedAllocationEvictedInPeriod: u32,
    pub Padding: u32,
    pub BytesCommitted_1: u32,
    pub NbReferencedAllocationEvictedInPeriod_1: u32,
    pub MaximumWorkingSet_1: u32,
    pub MinimumWorkingSet_1: u32,
    pub VideoMemory: u32,
    pub field_Policy: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_PROCESS_SEGMENT_GROUP_INFORMATION {
    pub Budget: u32,
    pub Requested: u32,
    pub Usage: u32,
    pub Demoted: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_SEGMENT {
    pub SegmentId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_NODE {
    pub NodeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_VIDPNSOURCE {
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_PHYSICAL_ADAPTER {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_MEMORY_USAGE {
    pub AllocatedBytes: u32,
    pub FreeBytes: u32,
    pub ZeroBytes: u32,
    pub ModifiedBytes: u32,
    pub StandbyBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_SEGMENT2 {
    pub PhysicalAdapterIndex: u32,
    pub SegmentId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_SEGMENT_USAGE {
    pub PhysicalAdapterIndex: u32,
    pub SegmentId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_SEGMENT_GROUP_USAGE {
    pub PhysicalAdapterIndex: u32,
    pub SegmentGroup: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_ADAPTER2 {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_ADAPTER_INFORMATION2 {
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_PROCESS_SEGMENT_GROUP2 {
    pub PhysicalAdapterIndex: u32,
    pub SegmentGroup: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS_QUERY_NODE2 {
    pub PhysicalAdapterIndex: u32,
    pub NodeOrdinal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYSTATISTICS {
    pub Type: u32,
    pub AdapterLuid: u32,
    pub hProcess_: u32,
    pub QueryResult: u32,
    pub QuerySegment: u32,
    pub QueryProcessSegment: u32,
    pub QueryProcessSegmentGroup: u32,
    pub QueryNode: u32,
    pub QueryProcessNode: u32,
    pub QueryVidPnSource: u32,
    pub QueryProcessVidPnSource: u32,
    pub QueryPhysAdapter: u32,
    pub QueryAdapter2: u32,
    pub QuerySegment2: u32,
    pub QueryProcessAdapter2: u32,
    pub QueryProcessSegment2: u32,
    pub QueryProcessSegmentGroup2: u32,
    pub QuerySegmentUsage: u32,
    pub QuerySegmentGroupUsage: u32,
    pub QueryNode2: u32,
    pub QueryProcessNode2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_STATS_DWM2 {
    pub cbSize: u32,
    pub PresentCount: u32,
    pub PresentRefreshCount: u32,
    pub PresentQPCTime: u32,
    pub SyncRefreshCount: u32,
    pub SyncQPCTime: u32,
    pub CustomPresentDuration: u32,
    pub VirtualSyncRefreshCount: u32,
    pub VirtualSyncQPCTime: u32,
    pub VSyncDurationQPCTime: u32,
    pub VSyncMultiplier: u32,
    pub VirtualPresentRefreshCount: u32,
    pub VirtualPresentQPCTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_VIDPNSOURCEOWNER_FLAGS {
    pub AllowOutputDuplication: u32,
    pub DisableDWMVirtualMode: u32,
    pub UseNtHandles: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETVIDPNSOURCEOWNER {
    pub hDevice: u32,
    pub pType_: u32,
    pub pVidPnSourceId_: u32,
    pub VidPnSourceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETVIDPNSOURCEOWNER1 {
    pub Version0: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETVIDPNSOURCEOWNER2 {
    pub Version1: u32,
    pub pVidPnSourceNtHandles_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKVIDPNEXCLUSIVEOWNERSHIP {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETPRESENTHISTORY {
    pub hAdapter: u32,
    pub ProvidedSize: u32,
    pub WrittenSize: u32,
    pub pTokens_: u32,
    pub NumTokens: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEOVERLAY {
    pub VidPnSourceId: u32,
    pub hDevice: u32,
    pub OverlayInfo: u32,
    pub hOverlay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UPDATEOVERLAY {
    pub hDevice: u32,
    pub hOverlay: u32,
    pub OverlayInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLIPOVERLAY {
    pub hDevice: u32,
    pub hOverlay: u32,
    pub hSource: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETOVERLAYSTATE {
    pub hDevice: u32,
    pub hOverlay: u32,
    pub OverlayEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYOVERLAY {
    pub hDevice: u32,
    pub hOverlay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORVERTICALBLANKEVENT {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORVERTICALBLANKEVENT2 {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub VidPnSourceId: u32,
    pub NumObjects: u32,
    pub ObjectHandleArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETVERTICALBLANKEVENT {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub VidPnSourceId: u32,
    pub phEvent_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETSYNCREFRESHCOUNTWAITTARGET {
    pub hAdapter: u32,
    pub hDevice: u32,
    pub VidPnSourceId: u32,
    pub TargetSyncRefreshCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETGAMMARAMP {
    pub hDevice: u32,
    pub VidPnSourceId: u32,
    pub Type: u32,
    pub pGammaRampRgb256x3x16: usize,
    pub pGammaRampDXGI1: usize,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ADJUSTFULLSCREENGAMMA {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub Scale: u32,
    pub Offset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SET_COLORSPACE_TRANSFORM {
    pub AdapterLuid: u32,
    pub VidPnTargetId: u32,
    pub Type: u32,
    pub Size: u32,
    pub pColorSpaceTransform: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETVIDPNSOURCEHWPROTECTION {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub HwProtected: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETHWPROTECTIONTEARDOWNRECOVERY {
    pub hAdapter: u32,
    pub Recovered: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICERESET_STATE {
    pub DesktopSwitched: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_STATS {
    pub PresentCount: u32,
    pub PresentRefreshCount: u32,
    pub SyncRefreshCount: u32,
    pub SyncQPCTime: u32,
    pub SyncGPUTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICEPRESENT_STATE {
    pub VidPnSourceId: u32,
    pub PresentStats: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_PRESENT_STATS_DWM {
    pub PresentCount: u32,
    pub PresentRefreshCount: u32,
    pub PresentQPCTime: u32,
    pub SyncRefreshCount: u32,
    pub SyncQPCTime: u32,
    pub CustomPresentDuration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICEPAGEFAULT_STATE {
    pub FaultedPrimitiveAPISequenceNumber: u32,
    pub FaultedPipelineStage: u32,
    pub FaultedBindTableEntry: u32,
    pub PageFaultFlags: u32,
    pub FaultErrorCode: u32,
    pub FaultedVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICEPRESENT_STATE_DWM {
    pub VidPnSourceId: u32,
    pub PresentStatsDWM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DEVICEPRESENT_QUEUE_STATE {
    pub VidPnSourceId: u32,
    pub bQueuedPresentLimitReached: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETDEVICESTATE {
    pub hDevice: u32,
    pub StateType: u32,
    pub ExecutionState: u32,
    pub PresentState: u32,
    pub ResetState: u32,
    pub PresentStateDWM: u32,
    pub PageFaultState: u32,
    pub PresentQueueState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEDCFROMMEMORY {
    pub pMemory_: u32,
    pub Format: u32,
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
    pub hDeviceDc_: u32,
    pub pColorTable_: u32,
    pub hDc_: u32,
    pub hBitmap_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYDCFROMMEMORY {
    pub hDc_: u32,
    pub hBitmap_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETCONTEXTSCHEDULINGPRIORITY {
    pub hContext: u32,
    pub Priority: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETCONTEXTINPROCESSSCHEDULINGPRIORITY {
    pub hContext: u32,
    pub Priority: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHANGESURFACEPOINTER {
    pub hDC_: u32,
    pub hBitmap_: u32,
    pub pSurfacePointer_: u32,
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETCONTEXTSCHEDULINGPRIORITY {
    pub hContext: u32,
    pub Priority: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETCONTEXTINPROCESSSCHEDULINGPRIORITY {
    pub hContext: u32,
    pub Priority: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETSCANLINE {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub InVerticalBlank: u8,
    pub ScanLine: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETQUEUEDLIMIT {
    pub hDevice: u32,
    pub Type: u32,
    pub QueuedPresentLimit: u32,
    pub VidPnSourceId: u32,
    pub QueuedPendingFlipLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_POLLDISPLAYCHILDREN {
    pub hAdapter: u32,
    pub NonDestructiveOnly: u32,
    pub SynchronousPolling: u32,
    pub DisableModeReset: u32,
    pub PollAllAdapters: u32,
    pub PollInterruptible: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_INVALIDATEACTIVEVIDPN {
    pub hAdapter: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKOCCLUSION {
    pub hWindow_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORIDLE {
    pub hDevice: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKMONITORPOWERSTATE {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETDISPLAYPRIVATEDRIVERFORMAT {
    pub hDevice: u32,
    pub VidPnSourceId: u32,
    pub PrivateDriverFormatAttribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEKEYEDMUTEX {
    pub InitialValue: u32,
    pub hSharedHandle: u32,
    pub hKeyedMutex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENKEYEDMUTEX {
    pub hSharedHandle: u32,
    pub hKeyedMutex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYKEYEDMUTEX {
    pub hKeyedMutex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ACQUIREKEYEDMUTEX {
    pub hKeyedMutex: u32,
    pub Key: u32,
    pub pTimeout_: u32,
    pub FenceValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RELEASEKEYEDMUTEX {
    pub hKeyedMutex: u32,
    pub Key: u32,
    pub FenceValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEKEYEDMUTEX2_FLAGS {
    pub NtSecuritySharing: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEKEYEDMUTEX2 {
    pub InitialValue: u32,
    pub hSharedHandle: u32,
    pub hKeyedMutex: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENKEYEDMUTEX2 {
    pub hSharedHandle: u32,
    pub hKeyedMutex: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENKEYEDMUTEXFROMNTHANDLE {
    pub hNtHandle_: u32,
    pub hKeyedMutex: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_ACQUIREKEYEDMUTEX2 {
    pub hKeyedMutex: u32,
    pub Key: u32,
    pub pTimeout_: u32,
    pub FenceValue: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RELEASEKEYEDMUTEX2 {
    pub hKeyedMutex: u32,
    pub Key: u32,
    pub FenceValue: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CONFIGURESHAREDRESOURCE {
    pub hDevice: u32,
    pub hResource: u32,
    pub IsDwm: u8,
    pub hProcess_: u32,
    pub AllowAccess: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHECKSHAREDRESOURCEACCESS {
    pub hResource: u32,
    pub ClientPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OFFER_FLAGS {
    pub OfferImmediately: u32,
    pub AllowDecommit: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OFFERALLOCATIONS {
    pub hDevice: u32,
    pub pResources_: u32,
    pub HandleList_: u32,
    pub NumAllocations: u32,
    pub Priority: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RECLAIMALLOCATIONS {
    pub hDevice: u32,
    pub pResources_: u32,
    pub HandleList_: u32,
    pub pDiscarded_: u32,
    pub NumAllocations: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_RECLAIMALLOCATIONS2 {
    pub hPagingQueue: u32,
    pub NumAllocations: u32,
    pub pResources_: u32,
    pub HandleList_: u32,
    pub pDiscarded: usize,
    pub pResults: usize,
    pub pDiscarded_: u32,
    pub PagingFenceValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPLCREATIONFLAGS {
    pub CompositionUiCaptureOnly: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_KEYEDMUTEX {
    pub hSharedSurfaceNt_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATE_OUTPUTDUPL {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub KeyedMutexCount: u32,
    pub RequiredKeyedMutexCount: u32,
    pub KeyedMutexs: [u32; 1],
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROY_OUTPUTDUPL {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub bDestroyAllContexts: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_POINTER_POSITION {
    pub Position: usize,
    pub Visible: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_FRAMEINFO {
    pub LastPresentTime: u32,
    pub LastMouseUpdateTime: u32,
    pub AccumulatedFrames: u32,
    pub RectsCoalesced: i32,
    pub ProtectedContentMaskedOut: i32,
    pub PointerPosition: u32,
    pub TotalMetadataBufferSize: u32,
    pub PointerShapeBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_GET_FRAMEINFO {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub FrameInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_METADATA {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub Type: u32,
    pub BufferSizeSupplied: u32,
    pub pBuffer_: u32,
    pub BufferSizeRequired: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTDUPL_POINTER_SHAPE_INFO {
    pub Type: u32,
    pub Width: u32,
    pub Height: u32,
    pub Pitch: u32,
    pub HotSpot: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_GET_POINTER_SHAPE_DATA {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub BufferSizeSupplied: u32,
    pub pShapeBuffer_: u32,
    pub BufferSizeRequired: u32,
    pub ShapeInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OUTPUTDUPL_RELEASE_FRAME {
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub NextKeyMutexIdx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETSHAREDRESOURCEADAPTERLUID {
    pub hGlobalShare: u32,
    pub hNtHandle_: u32,
    pub AdapterLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_HYBRID_LIST {
    pub State: u32,
    pub AdapterLuid: u32,
    pub bUserPreferenceQuery: i32,
    pub QueryType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MIRACAST_DISPLAY_DEVICE_STATUS {
    pub State: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MIRACAST_DISPLAY_DEVICE_CAPS {
    pub HdcpSupported: u8,
    pub DefaultControlPort: u32,
    pub UsesIhvSolution: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MIRACAST_DISPLAY_STOP_SESSIONS {
    pub AdapterLuid: u32,
    pub TargetId: u32,
    pub StopReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORSYNCHRONIZATIONOBJECTFROMCPU {
    pub hDevice: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub FenceValueArray_: u32,
    pub hAsyncEvent_: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SIGNALSYNCHRONIZATIONOBJECTFROMCPU {
    pub hDevice: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub FenceValueArray_: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITFORSYNCHRONIZATIONOBJECTFROMGPU {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub MonitoredFenceValueArray: usize,
    pub FenceValue: u32,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SIGNALSYNCHRONIZATIONOBJECTFROMGPU {
    pub hContext: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub MonitoredFenceValueArray: usize,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SIGNALSYNCHRONIZATIONOBJECTFROMGPU2 {
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub Flags: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContextArray_: u32,
    pub FenceValue: u32,
    pub CpuEventHandle: usize,
    pub MonitoredFenceValueArray: usize,
    pub Reserved: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEPAGINGQUEUE {
    pub hDevice: u32,
    pub Priority: u32,
    pub hPagingQueue: u32,
    pub hSyncObject: u32,
    pub FenceValueCPUVirtualAddress_: u32,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_EVICT {
    pub hDevice: u32,
    pub NumAllocations: u32,
    pub AllocationList_: u32,
    pub Flags: u32,
    pub NumBytesToTrim: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_LOCK2 {
    pub hDevice: u32,
    pub hAllocation: u32,
    pub Flags: u32,
    pub pData_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UNLOCK2 {
    pub hDevice: u32,
    pub hAllocation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_INVALIDATECACHE {
    pub hDevice: u32,
    pub hAllocation: u32,
    pub Offset: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FREEGPUVIRTUALADDRESS {
    pub hAdapter: u32,
    pub BaseAddress: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UPDATEGPUVIRTUALADDRESS {
    pub hDevice: u32,
    pub hContext: u32,
    pub hFenceObject: u32,
    pub NumOperations: u32,
    pub Operations_: u32,
    pub Reserved0: u32,
    pub Reserved1: u32,
    pub FenceValue: u32,
    pub DoNotWait: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATECONTEXTVIRTUAL {
    pub hDevice: u32,
    pub NodeOrdinal: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub ClientHint: u32,
    pub hContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITCOMMANDFLAGS {
    pub NullRendering: u32,
    pub PresentRedirected: u32,
    pub NoKmdAccess: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITCOMMAND {
    pub Commands: u32,
    pub CommandLength: u32,
    pub Flags: u32,
    pub PresentHistoryToken: u32,
    pub BroadcastContextCount: u32,
    pub BroadcastContext: [u32; 1],
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub NumPrimaries: u32,
    pub WrittenPrimaries: [u32; 1],
    pub NumHistoryBuffers: u32,
    pub HistoryBufferArray_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITCOMMANDTOHWQUEUE {
    pub hHwQueue: u32,
    pub HwQueueProgressFenceId: u32,
    pub CommandBuffer: u32,
    pub CommandLength: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: u32,
    pub NumPrimaries: u32,
    pub WrittenPrimaries_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITWAITFORSYNCOBJECTSTOHWQUEUE {
    pub hHwQueue: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub FenceValueArray_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SUBMITSIGNALSYNCOBJECTSTOHWQUEUE {
    pub Flags: u32,
    pub BroadcastHwQueueCount: u32,
    pub BroadcastHwQueueArray_: u32,
    pub ObjectCount: u32,
    pub ObjectHandleArray_: u32,
    pub FenceValueArray_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYVIDEOMEMORYINFO {
    pub hProcess_: u32,
    pub hAdapter: u32,
    pub MemorySegmentGroup: u32,
    pub Budget: u32,
    pub CurrentUsage: u32,
    pub CurrentReservation: u32,
    pub AvailableForReservation: u32,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CHANGEVIDEOMEMORYRESERVATION {
    pub hProcess_: u32,
    pub hAdapter: u32,
    pub MemorySegmentGroup: u32,
    pub Reservation: u32,
    pub PhysicalAdapterIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETSTABLEPOWERSTATE {
    pub hAdapter: u32,
    pub Enabled: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SHAREOBJECTWITHHOST {
    pub hDevice: u32,
    pub hObject: u32,
    pub Reserved: u32,
    pub hVailProcessNtHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATESYNCFILE {
    pub hDevice: u32,
    pub hMonitoredFence: u32,
    pub FenceValue: u32,
    pub hSyncFile: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_WAITSYNCFILE {
    pub hSyncFile: u32,
    pub hContext: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENSYNCOBJECTFROMSYNCFILE {
    pub hSyncFile: u32,
    pub hDevice: u32,
    pub hSyncObject: u32,
    pub FenceValue: u32,
    pub FenceValueCPUVirtualAddress_: u32,
    pub FenceValueGPUVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_TRIMNOTIFICATION {
    pub Context_: u32,
    pub Flags: u32,
    pub NumBytesToTrim: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_REGISTERTRIMNOTIFICATION {
    pub AdapterLuid: u32,
    pub hDevice: u32,
    pub Callback: usize,
    pub Context_: u32,
    pub Handle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UNREGISTERTRIMNOTIFICATION {
    pub Handle_: u32,
    pub Callback_: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_BUDGETCHANGENOTIFICATION {
    pub Context_: u32,
    pub Budget: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_REGISTERBUDGETCHANGENOTIFICATION {
    pub hDevice: u32,
    pub Callback_: usize,
    pub Context_: u32,
    pub Handle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_UNREGISTERBUDGETCHANGENOTIFICATION {
    pub Handle_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYVIDPNEXCLUSIVEOWNERSHIP {
    pub hProcess_: u32,
    pub hWindow_: u32,
    pub VidPnSourceId: u32,
    pub AdapterLuid: u32,
    pub OwnerType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_MARKDEVICEASERROR {
    pub hDevice: u32,
    pub Reason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_FLUSHHEAPTRANSITIONS {
    pub hAdapter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYPROCESSOFFERINFO {
    pub cbSize: u32,
    pub hProcess_: u32,
    pub DecommitUniqueness: u32,
    pub DecommittableBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_TRIMPROCESSCOMMITMENT {
    pub cbSize: u32,
    pub hProcess_: u32,
    pub Flags: u32,
    pub DecommitRequested: u32,
    pub NumBytesDecommitted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEHWCONTEXT {
    pub hDevice: u32,
    pub NodeOrdinal: u32,
    pub EngineAffinity: u32,
    pub Flags: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: u32,
    pub hHwContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYHWCONTEXT {
    pub hHwContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEHWQUEUE {
    pub hHwContext: u32,
    pub Flags: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: u32,
    pub hHwQueue: u32,
    pub hHwQueueProgressFence: u32,
    pub HwQueueProgressFenceCPUVirtualAddress_: u32,
    pub HwQueueProgressFenceGPUVirtualAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYHWQUEUE {
    pub hHwQueue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETALLOCATIONPRIORITY {
    pub hDevice: u32,
    pub hResource: u32,
    pub phAllocationList_: u32,
    pub AllocationCount: u32,
    pub pPriorities_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_SETFSEBLOCK {
    pub AdapterLuid: u32,
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYFSEBLOCK {
    pub AdapterLuid: u32,
    pub hAdapter: u32,
    pub VidPnSourceId: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_CREATEPROTECTEDSESSION {
    pub hDevice: u32,
    pub hSyncObject: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
    pub hHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_DESTROYPROTECTEDSESSION {
    pub hHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYPROTECTEDSESSIONSTATUS {
    pub hHandle: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_QUERYPROTECTEDSESSIONINFOFROMNTHANDLE {
    pub hNtHandle_: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateRuntimeData_: u32,
    pub PrivateRuntimeDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_OPENPROTECTEDSESSIONFROMNTHANDLE {
    pub hNtHandle_: u32,
    pub hHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMT_GETPROCESSDEVICEREMOVALSUPPORT {
    pub hProcess_: u32,
    pub AdapterLuid: u32,
    pub Support: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GRAPHICSPOWER_REGISTER_INPUT_V_1_2 {
    pub Version: u32,
    pub PrivateHandle: usize,
    pub PowerNotificationCb: usize,
    pub RemovalNotificationCb: usize,
    pub FStateNotificationCb: usize,
    pub InitialComponentStateCb: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_GRAPHICSPOWER_REGISTER_OUTPUT {
    pub DeviceHandle: usize,
    pub InitialGrfxPowerState: u32,
    pub SetSharedPowerComponentStateCb: usize,
    pub UnregisterCb: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GPUP_DRIVER_ESCAPE_INPUT {
    pub vfLUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_HEAD {
    pub Luid: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_READ_PCI_CONFIG {
    pub Header: u32,
    pub Offset: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_WRITE_PCI_CONFIG {
    pub Header: u32,
    pub Offset: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_READ_VGPU_TYPE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_POWERTRANSITIONCOMPLETE {
    pub Header: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_INITIALIZE {
    pub Header: u32,
    pub VmGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_RELEASE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_PAUSE {
    pub Header: u32,
    pub DeviceLuid: u32,
    pub GuestVmRunning: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGKVGPU_ESCAPE_RESUME {
    pub Header: u32,
    pub DeviceLuid: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_PTE {
    pub Valid: u64,
    pub Zero: u64,
    pub CacheCoherent: u64,
    pub ReadOnly: u64,
    pub NoExecute: u64,
    pub Segment: u64,
    pub LargePage: u64,
    pub PhysicalAdapterIndex: u64,
    pub PageTablePageSize: u64,
    pub SystemReserved0: u64,
    pub Reserved: u64,
    pub Flags: u64,
    pub PageAddress: u64,
    pub PageTableAddress: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DGPU_PHYSICAL_ADDRESS {
    pub SegmentId: u32,
    pub Padding: u32,
    pub SegmentOffset: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_RATIONAL {
    pub Numerator: u32,
    pub Denominator: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_ALLOCATIONINFO {
    pub hAllocation: u32,
    pub pSystemMem_: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub VidPnSourceId: u32,
    pub Primary: u32,
    pub Stereo: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_ALLOCATIONINFO2 {
    pub hAllocation: u32,
    pub hSection: usize,
    pub pSystemMem: usize,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub VidPnSourceId: u32,
    pub Primary: u32,
    pub Stereo: u32,
    pub OverridePriority: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
    pub GpuVirtualAddress: u32,
    pub Priority: u32,
    pub Unused: u32,
    pub Reserved_3: [u32; 5],
    pub Reserved_4: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_OPENALLOCATIONINFO {
    pub hAllocation: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_OPENALLOCATIONINFO2 {
    pub hAllocation: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
    pub GpuVirtualAddress: u32,
    pub Reserved: [u32; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_ALLOCATIONLIST {
    pub hAllocation: u32,
    pub WriteOperation: u32,
    pub DoNotRetireInstance: u32,
    pub OfferPriority: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_PATCHLOCATIONLIST {
    pub AllocationIndex: u32,
    pub SlotId: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub DriverId: u32,
    pub AllocationOffset: u32,
    pub PatchOffset: u32,
    pub SplitOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDICB_LOCKFLAGS {
    pub ReadOnly: u32,
    pub WriteOnly: u32,
    pub DonotWait: u32,
    pub IgnoreSync: u32,
    pub LockEntire: u32,
    pub DonotEvict: u32,
    pub AcquireAperture: u32,
    pub Discard: u32,
    pub NoExistingReference: u32,
    pub UseAlternateVA: u32,
    pub IgnoreReadSync: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDICB_LOCK2FLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDICB_DESTROYALLOCATION2FLAGS {
    pub AssumeNotInUse: u32,
    pub SynchronousDestroy: u32,
    pub Reserved: u32,
    pub SystemUseOnly: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_ESCAPEFLAGS {
    pub HardwareAccess: u32,
    pub DeviceStatusQuery: u32,
    pub ChangeFrameLatency: u32,
    pub NoAdapterSynchronization: u32,
    pub Reserved: u32,
    pub VirtualMachineData: u32,
    pub DriverKnownEscape: u32,
    pub DriverCommonEscape: u32,
    pub Reserved2: u32,
    pub Reserved2_1: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_DRIVERESCAPE_TRANSLATEALLOCATIONEHANDLE {
    pub EscapeType: u32,
    pub hAllocation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_DRIVERESCAPE_TRANSLATERESOURCEHANDLE {
    pub EscapeType: u32,
    pub hResource: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_DRIVERESCAPE_CPUEVENTUSAGE {
    pub EscapeType: u32,
    pub hSyncObject: u32,
    pub hKmdCpuEvent: u32,
    pub Usage: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_CREATECONTEXTFLAGS {
    pub NullRendering: u32,
    pub InitialData: u32,
    pub DisableGpuTimeout: u32,
    pub SynchronizationOnly: u32,
    pub HwQueueSupported: u32,
    pub NoKmdAccess: u32,
    pub TestContext: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_CREATEHWCONTEXTFLAGS {
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_CREATEHWQUEUEFLAGS {
    pub DisableGpuTimeout: u32,
    pub NoBroadcastSignal: u32,
    pub NoBroadcastWait: u32,
    pub NoKmdAccess: u32,
    pub UserModeSubmission: u32,
    pub NativeProgressFence: u32,
    pub TestQueue: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_SEGMENTPREFERENCE {
    pub SegmentId0: u32,
    pub Direction0: u32,
    pub SegmentId1: u32,
    pub Direction1: u32,
    pub SegmentId2: u32,
    pub Direction2: u32,
    pub SegmentId3: u32,
    pub Direction3: u32,
    pub SegmentId4: u32,
    pub Direction4: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDIRECT {
    pub left: i32,
    pub top: i32,
    pub right: i32,
    pub bottom: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_KERNELOVERLAYINFO {
    pub hAllocation: u32,
    pub DstRect: u32,
    pub SrcRect: u32,
    pub pPrivateDriverData_: u32,
    pub PrivateDriverDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_GAMMA_RAMP_RGB256x3x16 {
    pub Red: [u16; 256],
    pub Green: [u16; 256],
    pub Blue: [u16; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_GAMMA_RAMP_DXGI_1 {
    pub Scale: u32,
    pub Offset: u32,
    pub GammaCurve: [u32; 1025],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_3x4_COLORSPACE_TRANSFORM {
    pub ColorMatrix3x4: [u32; 3],
    pub ScalarMultiplier: u32,
    pub LookupTable1D: [u32; 4096],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DKMDT_COLORSPACE_TRANSFORM_MATRIX_V2 {
    pub StageControlLookupTable1DDegamma: u32,
    pub LookupTable1DDegamma: [u32; 4096],
    pub StageControlColorMatrix3x3: u32,
    pub ColorMatrix3x3: [u32; 3],
    pub StageControlLookupTable1DRegamma: u32,
    pub LookupTable1DRegamma: [u32; 4096],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_HDR_METADATA_HDR10 {
    pub RedPrimary: [u32; 2],
    pub GreenPrimary: [u32; 2],
    pub BluePrimary: [u32; 2],
    pub WhitePoint: [u32; 2],
    pub MaxMasteringLuminance: u32,
    pub MinMasteringLuminance: u32,
    pub MaxContentLightLevel: u32,
    pub MaxFrameAverageLightLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_MULTISAMPLINGMETHOD {
    pub NumSamples: u32,
    pub NumQualityLevels: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_RESOURCEFLAGS {
    pub RenderTarget: u32,
    pub ZBuffer: u32,
    pub Dynamic: u32,
    pub HintStatic: u32,
    pub AutogenMipmap: u32,
    pub DMap: u32,
    pub WriteOnly: u32,
    pub NotLockable: u32,
    pub Points: u32,
    pub RtPatches: u32,
    pub NPatches: u32,
    pub SharedResource: u32,
    pub DiscardRenderTarget: u32,
    pub Video: u32,
    pub CaptureBuffer: u32,
    pub Primary: u32,
    pub Texture: u32,
    pub CubeMap: u32,
    pub Volume: u32,
    pub VertexBuffer: u32,
    pub IndexBuffer: u32,
    pub DecodeRenderTarget: u32,
    pub DecodeCompressedBuffer: u32,
    pub VideoProcessRenderTarget: u32,
    pub CpuOptimized: u32,
    pub MightDrawFromLocked: u32,
    pub Overlay: u32,
    pub MatchGdiPrimary: u32,
    pub InterlacedRefresh: u32,
    pub TextApi: u32,
    pub RestrictedContent: u32,
    pub RestrictSharedAccess: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_SURFACEINFO {
    pub Width: u32,
    pub Height: u32,
    pub Depth: u32,
    pub pSysMem: usize,
    pub SysMemPitch: u32,
    pub SysMemSlicePitch: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDIARG_CREATERESOURCE {
    pub Format: u32,
    pub Pool: u32,
    pub MultisampleType: u32,
    pub MultisampleQuality: u32,
    pub pSurfList: usize,
    pub SurfCount: u32,
    pub MipLevels: u32,
    pub Fvf: u32,
    pub VidPnSourceId: u32,
    pub RefreshRate: u32,
    pub hResource: usize,
    pub Flags: u32,
    pub Rotation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_RESOURCEFLAGS2 {
    pub VideoEncoder: u32,
    pub UserMemory: u32,
    pub CrossAdapter: u32,
    pub IsDisplayable: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDIARG_CREATERESOURCE2 {
    pub Format: u32,
    pub Pool: u32,
    pub MultisampleType: u32,
    pub MultisampleQuality: u32,
    pub pSurfList: usize,
    pub SurfCount: u32,
    pub MipLevels: u32,
    pub Fvf: u32,
    pub VidPnSourceId: u32,
    pub RefreshRate: u32,
    pub hResource: usize,
    pub Flags: u32,
    pub Rotation: u32,
    pub Flags2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDICB_SIGNALFLAGS {
    pub SignalAtSubmission: u32,
    pub EnqueueCpuEvent: u32,
    pub AllowFenceRewind: u32,
    pub Reserved: u32,
    pub DXGK_SIGNAL_FLAG_INTERNAL0: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDIGPUVIRTUALADDRESS_PROTECTION_TYPE {
    pub Write: u64,
    pub Execute: u64,
    pub Zero: u64,
    pub NoAccess: u64,
    pub SystemUseOnly: u64,
    pub Reserved: u64,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_UPDATEGPUVIRTUALADDRESS_OPERATION {
    pub OperationType: u32,
    pub BaseAddress: u32,
    pub SizeInBytes: u32,
    pub hAllocation: u32,
    pub AllocationOffsetInBytes: u32,
    pub AllocationSizeInBytes: u32,
    pub BaseAddress_1: u32,
    pub SizeInBytes_1: u32,
    pub hAllocation_1: u32,
    pub AllocationOffsetInBytes_1: u32,
    pub AllocationSizeInBytes_1: u32,
    pub Protection: u32,
    pub DriverProtection: u32,
    pub BaseAddress_2: u32,
    pub SizeInBytes_2: u32,
    pub Protection_1: u32,
    pub SourceAddress: u32,
    pub SizeInBytes_3: u32,
    pub DestAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_GETRESOURCEPRESENTPRIVATEDRIVERDATA {
    pub hResource: u32,
    pub PrivateDriverDataSize: u32,
    pub pPrivateDriverData_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_SYNCHRONIZATIONOBJECTINFO {
    pub Type: u32,
    pub InitialState: i32,
    pub MaxCount: u32,
    pub InitialCount: u32,
    pub Reserved: [u32; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_SYNCHRONIZATIONOBJECT_FLAGS {
    pub Shared: u32,
    pub NtSecuritySharing: u32,
    pub CrossAdapter: u32,
    pub TopOfPipeline: u32,
    pub NoSignal: u32,
    pub NoWait: u32,
    pub NoSignalMaxValueOnTdr: u32,
    pub NoGPUAccess: u32,
    pub SignalByKmd: u32,
    pub Unused: u32,
    pub UnwaitCpuWaitersOnlyOnDestroy: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Reserved_2: u32,
    pub Reserved_3: u32,
    pub Reserved_4: u32,
    pub Reserved_5: u32,
    pub D3DDDI_SYNCHRONIZATIONOBJECT_FLAGS_RESERVED0: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_SYNCHRONIZATIONOBJECTINFO2 {
    pub Type: u32,
    pub Flags: u32,
    pub InitialState: i32,
    pub MaxCount: u32,
    pub InitialCount: u32,
    pub FenceValue: u32,
    pub Event_: u32,
    pub InitialFenceValue: u32,
    pub FenceValueCPUVirtualAddress_: u32,
    pub FenceValueGPUVirtualAddress: u32,
    pub EngineAffinity: u32,
    pub Padding: u32,
    pub hAdapter: u32,
    pub VidPnTargetId: u32,
    pub Time: u32,
    pub FenceValueCPUVirtualAddress__1: u32,
    pub FenceValueGPUVirtualAddress_1: u32,
    pub EngineAffinity_1: u32,
    pub Padding_1: u32,
    pub Reserved: [u32; 8],
    pub SharedHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_NATIVEFENCEMAPPING {
    pub CurrentValueCpuVa_: u32,
    pub CurrentValueGpuVa: u32,
    pub MonitoredValueGpuVa: u32,
    pub Reserved: [u32; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_NATIVEFENCEINFO {
    pub InitialFenceValue: u32,
    pub EngineAffinity: u32,
    pub Type: u32,
    pub Flags: u32,
    pub NativeFenceMapping: u32,
    pub Reserved: [u32; 28],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NATIVE_FENCE_LOG_HEADER {
    pub FirstFreeEntryIndex: u32,
    pub WraparoundCount: u32,
    pub AtomicWraparoundAndEntryIndex: u64,
    pub Type: u32,
    pub NumberOfEntries: u64,
    pub Reserved: [u64; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NATIVE_FENCE_LOG_ENTRY {
    pub FenceValue: u64,
    pub hNativeFence: u32,
    pub OperationType: u32,
    pub Reserved0: u64,
    pub FenceObservedGpuTimestamp: u64,
    pub Reserved1: u64,
    pub FenceEndGpuTimestamp: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_NATIVE_FENCE_LOG_BUFFER {
    pub Header: u32,
    pub Entries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_WAITFORSYNCHRONIZATIONOBJECTFROMCPU_FLAGS {
    pub WaitAny: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_QUERYREGISTRY_FLAGS {
    pub TranslatePath: u32,
    pub MutableValue: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_QUERYREGISTRY_INFO {
    pub QueryType: u32,
    pub QueryFlags: u32,
    pub ValueName: [u32; 1],
    pub ValueType: u32,
    pub PhysicalAdapterIndex: u32,
    pub OutputValueSize: u32,
    pub Status: u32,
    pub OutputDword: u32,
    pub OutputQword: u32,
    pub OutputString: [u32; 1],
    pub OutputBinary: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_ISFEATUREENABLED_RESULT {
    pub Version: u32,
    pub Enabled: u32,
    pub KnownFeature: u32,
    pub SupportedByDriver: u32,
    pub SupportedOnCurrentConfig: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_TESTCOMMANDBUFFER_COPY {
    pub SrcAddress: u32,
    pub DstAddress: u32,
    pub NumBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_TESTCOMMANDBUFFER_FILL {
    pub DstAddress: u32,
    pub NumBytes: u32,
    pub Pattern: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_TESTCOMMANDBUFFER {
    pub Copy: u32,
    pub Fill: u32,
    pub Reserved: [u32; 64],
    pub Operation: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_BUILDTESTCOMMANDBUFFERFLAGS {
    pub HardwareQueue: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct D3DDDI_DRIVERESCAPE_BUILDTESTCOMMANDBUFFER {
    pub EscapeType: u32,
    pub hDevice: u32,
    pub hContext: u32,
    pub Flags: u32,
    pub Command: u32,
    pub pDmaBuffer_: usize,
    pub pDmaBufferPrivateData_: usize,
    pub DmaBufferSize: u32,
    pub DmaBufferPrivateDataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCICMD {
    pub dwCommand: u32,
    pub dwParam1: u32,
    pub dwParam2: u32,
    pub dwVersion: u32,
    pub dwReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCICREATEINPUT {
    pub cmd: u32,
    pub dwCompression: u32,
    pub dwMask: [u32; 3],
    pub dwWidth: u32,
    pub dwHeight: u32,
    pub dwDCICaps: u32,
    pub dwBitCount: u32,
    pub lpSurface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCISURFACEINFO {
    pub dwSize: u32,
    pub dwDCICaps: u32,
    pub dwCompression: u32,
    pub dwMask: [u32; 3],
    pub dwWidth: u32,
    pub dwHeight: u32,
    pub lStride: i32,
    pub dwBitCount: u32,
    pub dwOffSurface: usize,
    pub wSelSurface: u16,
    pub wReserved: u16,
    pub dwReserved1: u32,
    pub dwReserved2: u32,
    pub dwReserved3: u32,
    pub LPRECT_: usize,
    pub field_LPVOID_: usize,
    pub field_LPVOID__1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCIENUMINPUT {
    pub cmd: u32,
    pub rSrc: u32,
    pub rDst: u32,
    pub LPVOID_: usize,
    pub lpContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCIOFFSCREEN {
    pub dciInfo: u32,
    pub field_LPVOID_: usize,
    pub LPRGNDATA_: usize,
    pub LPRECT_: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DCIOVERLAY {
    pub dciInfo: u32,
    pub dwChromakeyValue: u32,
    pub dwChromakeyMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVPROPKEY {
    pub fmtid: u32,
    pub pid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVPROPCOMPKEY {
    pub Key: u32,
    pub Store: u32,
    pub field_LocaleName: u32,
    pub LocaleName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVPROPERTY {
    pub CompKey: u32,
    pub Type: u32,
    pub BufferSize: u32,
    pub field_Buffer: u32,
    pub Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DLSID {
    pub ulData1: u32,
    pub usData2: u16,
    pub usData3: u16,
    pub abData4: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DLSVERSION {
    pub dwVersionMS: u32,
    pub dwVersionLS: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONNECTION {
    pub usSource: u16,
    pub usControl: u16,
    pub usDestination: u16,
    pub usTransform: u16,
    pub lScale: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONNECTIONLIST {
    pub cbSize: u32,
    pub cConnections: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RGNRANGE {
    pub usLow: u16,
    pub usHigh: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDILOCALE {
    pub ulBank: u32,
    pub ulInstrument: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RGNHEADER {
    pub RangeKey: u32,
    pub RangeVelocity: u32,
    pub fusOptions: u16,
    pub usKeyGroup: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INSTHEADER {
    pub cRegions: u32,
    pub Locale: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DLSHEADER {
    pub cInstruments: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAVELINK {
    pub fusOptions: u16,
    pub usPhaseGroup: u16,
    pub ulChannel: u32,
    pub ulTableIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOLCUE {
    pub ulOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POOLTABLE {
    pub cbSize: u32,
    pub cCues: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSMPL {
    pub cbSize: u32,
    pub usUnityNote: u16,
    pub sFineTune: i16,
    pub lAttenuation: i32,
    pub fulOptions: u32,
    pub cSampleLoops: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WLOOP {
    pub cbSize: u32,
    pub ulType: u32,
    pub ulStart: u32,
    pub ulLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_DOWNLOADINFO {
    pub dwDLType: u32,
    pub dwDLId: u32,
    pub dwNumOffsetTableEntries: u32,
    pub cbSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_OFFSETTABLE {
    pub ulOffsetTable: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_INSTRUMENT {
    pub ulPatch: u32,
    pub ulFirstRegionIdx: u32,
    pub ulGlobalArtIdx: u32,
    pub ulFirstExtCkIdx: u32,
    pub ulCopyrightIdx: u32,
    pub ulFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_REGION {
    pub RangeKey: u32,
    pub RangeVelocity: u32,
    pub fusOptions: u16,
    pub usKeyGroup: u16,
    pub ulRegionArtIdx: u32,
    pub ulNextRegionIdx: u32,
    pub ulFirstExtCkIdx: u32,
    pub WaveLink: u32,
    pub WSMP: u32,
    pub WLOOP: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_LFOPARAMS {
    pub pcFrequency: usize,
    pub tcDelay: u32,
    pub gcVolumeScale: u32,
    pub pcPitchScale: usize,
    pub gcMWToVolume: u32,
    pub pcMWToPitch: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_VEGPARAMS {
    pub tcAttack: u32,
    pub tcDecay: u32,
    pub ptSustain: usize,
    pub tcRelease: u32,
    pub tcVel2Attack: u32,
    pub tcKey2Decay: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_PEGPARAMS {
    pub tcAttack: u32,
    pub tcDecay: u32,
    pub ptSustain: usize,
    pub tcRelease: u32,
    pub tcVel2Attack: u32,
    pub tcKey2Decay: u32,
    pub pcRange: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_MSCPARAMS {
    pub ptDefaultPan: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_ARTICPARAMS {
    pub LFO: u32,
    pub VolEG: u32,
    pub PitchEG: u32,
    pub Misc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_EXTENSIONCHUNK {
    pub cbSize: u32,
    pub ulNextExtCkIdx: u32,
    pub ExtCkID: u32,
    pub byExtCk: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_COPYRIGHT {
    pub cbSize: u32,
    pub byCopyright: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_WAVEDATA {
    pub cbSize: u32,
    pub byData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_WAVE {
    pub ulFirstExtCkIdx: u32,
    pub ulCopyrightIdx: u32,
    pub ulWaveDataIdx: u32,
    pub WaveformatEx: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_NOTERANGE {
    pub dwLowNote: u32,
    pub dwHighNote: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_WAVEARTDL {
    pub ulDownloadIdIdx: u32,
    pub ulBus: u32,
    pub ulBuffers: u32,
    pub ulMasterDLId: u32,
    pub usOptions: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_WAVEDL {
    pub cbWaveData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DMUS_EVENTHEADER {
    pub cbEvent: u32,
    pub dwChannelGroup: u32,
    pub rtDelta: u32,
    pub dwFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LUID {
    pub LowPart: u32,
    pub HighPart: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BAND_MANAGEMENT_CAPABILITIES {
    pub StructSize: u32,
    pub Capabilities: u32,
    pub KeyProtectionMechanism: u64,
    pub MinAuthKeyLength: u32,
    pub MaxAuthKeyLength: u32,
    pub MaxBandCount: u32,
    pub MaxSimultaneousReencryptionCount: u32,
    pub BandMetadataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACTIVATE_REVERT_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub AuthKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUTH_KEY {
    pub KeySize: u32,
    pub Key: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENUMERATE_BANDS_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub BandSize: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BAND_LOCATION_INFO {
    pub StructSize: u32,
    pub Reserved: u32,
    pub BandStart: i64,
    pub BandSize: i64,
    pub Metadata: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BAND_SECURITY_INFO {
    pub StructSize: u32,
    pub ReadLock: u32,
    pub WriteLock: u32,
    pub CryptoAlgoIdType: u32,
    pub Offset: u32,
    pub Length: u32,
    pub CryptoAlgoNumericId: u32,
    pub Metadata: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BAND_TABLE_ENTRY {
    pub BandId: u32,
    pub LocationInfo: u32,
    pub SecurityInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BAND_TABLE {
    pub StructSize: u32,
    pub BandTableOffset: u32,
    pub BandTableEntryCount: u32,
    pub BandTableEntrySize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_BAND_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub BandLocationInfoOffset: u32,
    pub BandSecurityInfoOffset: u32,
    pub AuthKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_BAND_LOCATION_PARAMETERS {
    pub StructSize: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub AuthKeyOffset: u32,
    pub BandLocationInfoOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_BAND_SECURITY_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub CurrentAuthKeyOffset: u32,
    pub NewAuthKeyOffset: u32,
    pub BandSecurityInfoOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_BAND_METADATA_PARAMETERS {
    pub StructSize: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub MetadataOffset: u32,
    pub MetadataSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_BAND_METADATA_PARAMETERS {
    pub StructSize: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub MetadataOffset: u32,
    pub MetadataSize: u32,
    pub BufferOffset: u32,
    pub AuthKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DELETE_BAND_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub AuthKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ERASE_BAND_PARAMETERS {
    pub StructSize: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub BandId: u32,
    pub BandStart: i64,
    pub NewAuthKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_SID_PARAMETERS {
    pub StructSize: u32,
    pub CurrentSidPinOffset: u32,
    pub NewSidPinOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENUM_PDO_ENTRY {
    pub field_type: u8,
    pub state: u8,
    pub capabilities: u8,
    pub ulSTID: u32,
    pub bSpecificationMajor: u8,
    pub bSpecificationMinor: u8,
    pub bImplementationMajor: u8,
    pub bImplementationMinor: u8,
    pub wszDeviceInstancePath: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENUM_PDO_RESULTS {
    pub cEntries: u32,
    pub rgEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EHSTOR_DEVICE_PROPERTIES {
    pub StructSize: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SILO_DRIVER_CAPABILITIES {
    pub StructSize: u32,
    pub Capabilities: u32,
    pub MaxLbaFilterCount: u32,
    pub RedirectedIoctlListCount: u32,
    pub RedirectedIoctlListOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LBA_FILTER_TABLE_ENTRY {
    pub StartLba: u64,
    pub LbaCount: u64,
    pub ReadLock: u8,
    pub WriteLock: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LBA_FILTER_TABLE {
    pub StructSize: u32,
    pub GlobalReadLock: u8,
    pub Reserved1: i32,
    pub GlobalWriteLock: u8,
    pub Reserved2: i32,
    pub LbaFilterCount: u32,
    pub LbaFilterSize: u32,
    pub LbaFiltersOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AUTHZ_STATE {
    pub AuthzState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_DATA_DESCRIPTOR {
    pub Ptr: u64,
    pub Size: u32,
    pub Reserved: u32,
    pub Type: u8,
    pub Reserved1: u8,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_DESCRIPTOR {
    pub Id: u16,
    pub Version: u8,
    pub Channel: u8,
    pub Level: u8,
    pub Opcode: u8,
    pub event: u32,
    pub Task: u16,
    pub Keyword: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_FILTER_DESCRIPTOR {
    pub Ptr: u64,
    pub Size: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_FILTER_HEADER {
    pub Id: u16,
    pub Version: u8,
    pub Reserved: [u8; 5],
    pub InstanceId: u64,
    pub Size: u32,
    pub NextOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_FILTER_EVENT_ID {
    pub FilterIn: u8,
    pub Reserved: u8,
    pub Count: u16,
    pub Events: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_FILTER_EVENT_NAME {
    pub MatchAnyKeyword: u64,
    pub MatchAllKeyword: u64,
    pub Level: u8,
    pub FilterIn: u8,
    pub NameCount: u16,
    pub Names: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_FILTER_LEVEL_KW {
    pub MatchAnyKeyword: u64,
    pub MatchAllKeyword: u64,
    pub Level: u8,
    pub FilterIn: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE_HEADER {
    pub Size: u16,
    pub FieldTypeFlags: u16,
    pub HeaderType: u8,
    pub MarkerFlags: u8,
    pub Version: u32,
    pub Type: u8,
    pub Level: u8,
    pub Version_1: u16,
    pub ThreadId: u32,
    pub ProcessId: u32,
    pub TimeStamp: i64,
    pub Guid: u32,
    pub GuidPtr: u64,
    pub KernelTime: u32,
    pub UserTime: u32,
    pub ProcessorTime: u32,
    pub ClientContext: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_INSTANCE_HEADER {
    pub Size: u16,
    pub FieldTypeFlags: u16,
    pub HeaderType: u8,
    pub MarkerFlags: u8,
    pub Version: u32,
    pub Type: u8,
    pub Level: u8,
    pub Version_1: u16,
    pub ThreadId: u32,
    pub ProcessId: u32,
    pub TimeStamp: i64,
    pub RegHandle: u64,
    pub InstanceId: u32,
    pub ParentInstanceId: u32,
    pub KernelTime: u32,
    pub UserTime: u32,
    pub ProcessorTime: u32,
    pub EventId: u32,
    pub Flags: u32,
    pub ParentRegHandle: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOF_FIELD {
    pub DataPtr: u32,
    pub Length: u32,
    pub DataType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_LOGFILE_HEADER {
    pub BufferSize: u32,
    pub Version: u32,
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub SubVersion: u8,
    pub SubMinorVersion: u8,
    pub ProviderVersion: u32,
    pub NumberOfProcessors: u32,
    pub EndTime: i64,
    pub TimerResolution: u32,
    pub MaximumFileSize: u32,
    pub LogFileMode: u32,
    pub BuffersWritten: u32,
    pub LogInstanceGuid: u32,
    pub StartBuffers: u32,
    pub PointerSize: u32,
    pub EventsLost: u32,
    pub CpuSpeedInMHz: u32,
    pub LoggerName: usize,
    pub LogFileName: usize,
    pub TimeZone: u32,
    pub LoggerName_1: usize,
    pub LogFileName_1: usize,
    pub TimeZone_1: u32,
    pub BootTime: i64,
    pub PerfFreq: i64,
    pub StartTime: i64,
    pub ReservedFlags: u32,
    pub BuffersLost: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_LOGFILE_HEADER32 {
    pub BufferSize: u32,
    pub Version: u32,
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub SubVersion: u8,
    pub SubMinorVersion: u8,
    pub ProviderVersion: u32,
    pub NumberOfProcessors: u32,
    pub EndTime: i64,
    pub TimerResolution: u32,
    pub MaximumFileSize: u32,
    pub LogFileMode: u32,
    pub BuffersWritten: u32,
    pub LogInstanceGuid: u32,
    pub StartBuffers: u32,
    pub PointerSize: u32,
    pub EventsLost: u32,
    pub CpuSpeedInMHz: u32,
    pub LoggerName: u32,
    pub LogFileName: u32,
    pub TimeZone: u32,
    pub LoggerName_1: u32,
    pub LogFileName_1: u32,
    pub TimeZone_1: u32,
    pub BootTime: i64,
    pub PerfFreq: i64,
    pub StartTime: i64,
    pub ReservedFlags: u32,
    pub BuffersLost: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_LOGFILE_HEADER64 {
    pub BufferSize: u32,
    pub Version: u32,
    pub MajorVersion: u8,
    pub MinorVersion: u8,
    pub SubVersion: u8,
    pub SubMinorVersion: u8,
    pub ProviderVersion: u32,
    pub NumberOfProcessors: u32,
    pub EndTime: i64,
    pub TimerResolution: u32,
    pub MaximumFileSize: u32,
    pub LogFileMode: u32,
    pub BuffersWritten: u32,
    pub LogInstanceGuid: u32,
    pub StartBuffers: u32,
    pub PointerSize: u32,
    pub EventsLost: u32,
    pub CpuSpeedInMHz: u32,
    pub LoggerName: u32,
    pub LogFileName: u32,
    pub TimeZone: u32,
    pub LoggerName_1: u32,
    pub LogFileName_1: u32,
    pub TimeZone_1: u32,
    pub BootTime: i64,
    pub PerfFreq: i64,
    pub StartTime: i64,
    pub ReservedFlags: u32,
    pub BuffersLost: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE_PROPERTIES {
    pub Wnode: u32,
    pub BufferSize: u32,
    pub MinimumBuffers: u32,
    pub MaximumBuffers: u32,
    pub MaximumFileSize: u32,
    pub LogFileMode: u32,
    pub FlushTimer: u32,
    pub EnableFlags: u32,
    pub AgeLimit: i32,
    pub FlushThreshold: i32,
    pub NumberOfBuffers: u32,
    pub FreeBuffers: u32,
    pub EventsLost: u32,
    pub BuffersWritten: u32,
    pub LogBuffersLost: u32,
    pub RealTimeBuffersLost: u32,
    pub LoggerThreadId: usize,
    pub LogFileNameOffset: u32,
    pub LoggerNameOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE_PROPERTIES_V2 {
    pub Wnode: u32,
    pub BufferSize: u32,
    pub MinimumBuffers: u32,
    pub MaximumBuffers: u32,
    pub MaximumFileSize: u32,
    pub LogFileMode: u32,
    pub FlushTimer: u32,
    pub EnableFlags: u32,
    pub AgeLimit: i32,
    pub FlushThreshold: i32,
    pub NumberOfBuffers: u32,
    pub FreeBuffers: u32,
    pub EventsLost: u32,
    pub BuffersWritten: u32,
    pub LogBuffersLost: u32,
    pub RealTimeBuffersLost: u32,
    pub LoggerThreadId: usize,
    pub LogFileNameOffset: u32,
    pub LoggerNameOffset: u32,
    pub VersionNumber: u32,
    pub V2Control: u32,
    pub FilterDescCount: u32,
    pub FilterDesc: usize,
    pub Wow: u32,
    pub QpcDeltaTracking: u32,
    pub LargeMdlPages: u32,
    pub ExcludeKernelStack: u32,
    pub V2Options: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_GUID_REGISTRATION {
    pub Guid: usize,
    pub RegHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_GUID_PROPERTIES {
    pub Guid: u32,
    pub GuidType: u32,
    pub LoggerId: u32,
    pub EnableLevel: u32,
    pub EnableFlags: u32,
    pub IsEnable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_BUFFER_CONTEXT {
    pub ProcessorNumber: u8,
    pub Alignment: u8,
    pub ProcessorIndex: u16,
    pub LoggerId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_ENABLE_INFO {
    pub IsEnabled: u32,
    pub Level: u8,
    pub Reserved1: u8,
    pub LoggerId: u16,
    pub EnableProperty: u32,
    pub Reserved2: u32,
    pub MatchAnyKeyword: u64,
    pub MatchAllKeyword: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_PROVIDER_INSTANCE_INFO {
    pub NextOffset: u32,
    pub EnableCount: u32,
    pub Pid: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_GUID_INFO {
    pub InstanceCount: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROFILE_SOURCE_INFO {
    pub NextEntryOffset: u32,
    pub Source: u32,
    pub MinInterval: u32,
    pub MaxInterval: u32,
    pub Reserved: u32,
    pub Description: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_PMC_COUNTER_OWNER {
    pub OwnerType: u32,
    pub ProfileSource: u32,
    pub OwnerTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_PMC_COUNTER_OWNERSHIP_STATUS {
    pub ProcessorNumber: u32,
    pub NumberOfCounters: u32,
    pub CounterOwners: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE {
    pub Header: u32,
    pub InstanceId: u32,
    pub ParentInstanceId: u32,
    pub ParentGuid: u32,
    pub MofData: usize,
    pub MofLength: u32,
    pub ClientContext: u32,
    pub BufferContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE_LOGFILEW {
    pub LogFileName: usize,
    pub LoggerName: usize,
    pub CurrentTime: i64,
    pub BuffersRead: u32,
    pub LogFileMode: u32,
    pub ProcessTraceMode: u32,
    pub CurrentEvent: u32,
    pub LogfileHeader: u32,
    pub BufferSize: u32,
    pub Filled: u32,
    pub EventsLost: u32,
    pub EventCallback: usize,
    pub EventRecordCallback: usize,
    pub IsKernelTrace: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EVENT_TRACE_LOGFILEA {
    pub LogFileName: usize,
    pub LoggerName: usize,
    pub CurrentTime: i64,
    pub BuffersRead: u32,
    pub LogFileMode: u32,
    pub ProcessTraceMode: u32,
    pub CurrentEvent: u32,
    pub LogfileHeader: u32,
    pub BufferSize: u32,
    pub Filled: u32,
    pub EventsLost: u32,
    pub EventCallback: usize,
    pub EventRecordCallback: usize,
    pub IsKernelTrace: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_TRACE_PARAMETERS_V1 {
    pub Version: u32,
    pub EnableProperty: u32,
    pub ControlFlags: u32,
    pub SourceId: u32,
    pub EnableFilterDesc: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENABLE_TRACE_PARAMETERS {
    pub Version: u32,
    pub EnableProperty: u32,
    pub ControlFlags: u32,
    pub SourceId: u32,
    pub EnableFilterDesc: usize,
    pub FilterDescCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASSIC_EVENT_ID {
    pub EventGuid: u32,
    pub Type: u8,
    pub Reserved: [u8; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_STACK_CACHING_INFO {
    pub Enabled: u8,
    pub CacheSize: u32,
    pub BucketCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_PROFILE_INTERVAL {
    pub Source: u32,
    pub Interval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_VERSION_INFO {
    pub EtwTraceProcessingVersion: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACE_PERIODIC_CAPTURE_STATE_INFO {
    pub CaptureStateFrequencyInSeconds: u32,
    pub ProviderCount: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_TRACE_PARTITION_INFORMATION {
    pub PartitionId: u32,
    pub ParentId: u32,
    pub QpcOffsetFromRoot: u32,
    pub PartitionType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETW_TRACE_PARTITION_INFORMATION_V2 {
    pub QpcOffsetFromRoot: u32,
    pub PartitionType: u32,
    pub PartitionId: usize,
    pub ParentId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_FULL_INFORMATION {
    pub NextEntryOffset: u32,
    pub FrameID: u32,
    pub NumberOfInstances: u32,
    pub FilterNameLength: u16,
    pub FilterNameBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_AGGREGATE_BASIC_INFORMATION {
    pub NextEntryOffset: u32,
    pub Flags: u32,
    pub FrameID: u32,
    pub NumberOfInstances: u32,
    pub FilterNameLength: u16,
    pub FilterNameBufferOffset: u16,
    pub FilterAltitudeLength: u16,
    pub FilterAltitudeBufferOffset: u16,
    pub FilterNameLength_1: u16,
    pub FilterNameBufferOffset_1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_AGGREGATE_STANDARD_INFORMATION {
    pub NextEntryOffset: u32,
    pub Flags: u32,
    pub Flags_1: u32,
    pub FrameID: u32,
    pub NumberOfInstances: u32,
    pub FilterNameLength: u16,
    pub FilterNameBufferOffset: u16,
    pub FilterAltitudeLength: u16,
    pub FilterAltitudeBufferOffset: u16,
    pub Flags_2: u32,
    pub FilterNameLength_1: u16,
    pub FilterNameBufferOffset_1: u16,
    pub FilterAltitudeLength_1: u16,
    pub FilterAltitudeBufferOffset_1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_VOLUME_BASIC_INFORMATION {
    pub FilterVolumeNameLength: u16,
    pub FilterVolumeName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_VOLUME_STANDARD_INFORMATION {
    pub NextEntryOffset: u32,
    pub Flags: u32,
    pub FrameID: u32,
    pub FileSystemType: u32,
    pub FilterVolumeNameLength: u16,
    pub FilterVolumeName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INSTANCE_BASIC_INFORMATION {
    pub NextEntryOffset: u32,
    pub InstanceNameLength: u16,
    pub InstanceNameBufferOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INSTANCE_PARTIAL_INFORMATION {
    pub NextEntryOffset: u32,
    pub InstanceNameLength: u16,
    pub InstanceNameBufferOffset: u16,
    pub AltitudeLength: u16,
    pub AltitudeBufferOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INSTANCE_FULL_INFORMATION {
    pub NextEntryOffset: u32,
    pub InstanceNameLength: u16,
    pub InstanceNameBufferOffset: u16,
    pub AltitudeLength: u16,
    pub AltitudeBufferOffset: u16,
    pub VolumeNameLength: u16,
    pub VolumeNameBufferOffset: u16,
    pub FilterNameLength: u16,
    pub FilterNameBufferOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INSTANCE_AGGREGATE_STANDARD_INFORMATION {
    pub NextEntryOffset: u32,
    pub Flags: u32,
    pub Flags_1: u32,
    pub FrameID: u32,
    pub VolumeFileSystemType: u32,
    pub InstanceNameLength: u16,
    pub InstanceNameBufferOffset: u16,
    pub AltitudeLength: u16,
    pub AltitudeBufferOffset: u16,
    pub VolumeNameLength: u16,
    pub VolumeNameBufferOffset: u16,
    pub FilterNameLength: u16,
    pub FilterNameBufferOffset: u16,
    pub SupportedFeatures: u32,
    pub Flags_2: u32,
    pub AltitudeLength_1: u16,
    pub AltitudeBufferOffset_1: u16,
    pub VolumeNameLength_1: u16,
    pub VolumeNameBufferOffset_1: u16,
    pub FilterNameLength_1: u16,
    pub FilterNameBufferOffset_1: u16,
    pub SupportedFeatures_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_MESSAGE_HEADER {
    pub ReplyLength: u32,
    pub MessageId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILTER_REPLY_HEADER {
    pub Status: u32,
    pub MessageId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_PARTITION_CONFIGURATION_INFORMATION {
    pub Signature: u32,
    pub DiskNumber: u32,
    pub ByteOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_STRIPE_SET_CONFIGURATION_INFORMATION {
    pub StripeSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_MIRROR_SET_CONFIGURATION_INFORMATION {
    pub MemberSize: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_STRIPE_SET_WITH_PARITY_CONFIGURATION_INFORMATION {
    pub MemberSize: i64,
    pub StripeSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_REDISTRIBUTION_CONFIGURATION_INFORMATION {
    pub StripeSize: u32,
    pub FirstMemberWidth: u16,
    pub SecondMemberWidth: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_MIRROR_AND_SWP_STATE_INFORMATION {
    pub IsDirty: u8,
    pub IsInitializing: u8,
    pub UnhealthyMemberNumber: u16,
    pub UnhealthyMemberState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FT_REDISTRIBUTION_STATE_INFORMATION {
    pub BytesRedistributed: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GLDISPATCHTABLE {
    pub reserved_padding: u32,
    pub reserved_padding_1: u32,
    pub reserved_padding_2: u32,
    pub reserved_padding_3: u32,
    pub reserved_padding_4: u32,
    pub reserved_padding_5: u32,
    pub reserved_padding_6: u32,
    pub reserved_padding_7: u32,
    pub reserved_padding_8: u32,
    pub reserved_padding_9: u32,
    pub reserved_padding_10: u32,
    pub reserved_padding_11: u32,
    pub reserved_padding_12: u32,
    pub reserved_padding_13: u32,
    pub reserved_padding_14: u32,
    pub reserved_padding_15: u32,
    pub reserved_padding_16: u32,
    pub reserved_padding_17: u32,
    pub reserved_padding_18: u32,
    pub reserved_padding_19: u32,
    pub reserved_padding_20: u32,
    pub reserved_padding_21: u32,
    pub reserved_padding_22: u32,
    pub reserved_padding_23: u32,
    pub reserved_padding_24: u32,
    pub reserved_padding_25: u32,
    pub reserved_padding_26: u32,
    pub reserved_padding_27: u32,
    pub reserved_padding_28: u32,
    pub reserved_padding_29: u32,
    pub reserved_padding_30: u32,
    pub reserved_padding_31: u32,
    pub reserved_padding_32: u32,
    pub reserved_padding_33: u32,
    pub reserved_padding_34: u32,
    pub reserved_padding_35: u32,
    pub reserved_padding_36: u32,
    pub reserved_padding_37: u32,
    pub reserved_padding_38: u32,
    pub reserved_padding_39: u32,
    pub reserved_padding_40: u32,
    pub reserved_padding_41: u32,
    pub reserved_padding_42: u32,
    pub reserved_padding_43: u32,
    pub reserved_padding_44: u32,
    pub reserved_padding_45: u32,
    pub reserved_padding_46: u32,
    pub reserved_padding_47: u32,
    pub reserved_padding_48: u32,
    pub reserved_padding_49: u32,
    pub reserved_padding_50: u32,
    pub reserved_padding_51: u32,
    pub reserved_padding_52: u32,
    pub reserved_padding_53: u32,
    pub reserved_padding_54: u32,
    pub reserved_padding_55: u32,
    pub reserved_padding_56: u32,
    pub reserved_padding_57: u32,
    pub reserved_padding_58: u32,
    pub reserved_padding_59: u32,
    pub reserved_padding_60: u32,
    pub reserved_padding_61: u32,
    pub reserved_padding_62: u32,
    pub reserved_padding_63: u32,
    pub reserved_padding_64: u32,
    pub reserved_padding_65: u32,
    pub reserved_padding_66: u32,
    pub reserved_padding_67: u32,
    pub reserved_padding_68: u32,
    pub reserved_padding_69: u32,
    pub reserved_padding_70: u32,
    pub reserved_padding_71: u32,
    pub reserved_padding_72: u32,
    pub reserved_padding_73: u32,
    pub reserved_padding_74: u32,
    pub reserved_padding_75: u32,
    pub reserved_padding_76: u32,
    pub reserved_padding_77: u32,
    pub reserved_padding_78: u32,
    pub reserved_padding_79: u32,
    pub reserved_padding_80: u32,
    pub reserved_padding_81: u32,
    pub reserved_padding_82: u32,
    pub reserved_padding_83: u32,
    pub reserved_padding_84: u32,
    pub reserved_padding_85: u32,
    pub reserved_padding_86: u32,
    pub reserved_padding_87: u32,
    pub reserved_padding_88: u32,
    pub reserved_padding_89: u32,
    pub reserved_padding_90: u32,
    pub reserved_padding_91: u32,
    pub reserved_padding_92: u32,
    pub reserved_padding_93: u32,
    pub reserved_padding_94: u32,
    pub reserved_padding_95: u32,
    pub reserved_padding_96: u32,
    pub reserved_padding_97: u32,
    pub reserved_padding_98: u32,
    pub reserved_padding_99: u32,
    pub reserved_padding_100: u32,
    pub reserved_padding_101: u32,
    pub reserved_padding_102: u32,
    pub reserved_padding_103: u32,
    pub reserved_padding_104: u32,
    pub reserved_padding_105: u32,
    pub reserved_padding_106: u32,
    pub reserved_padding_107: u32,
    pub reserved_padding_108: u32,
    pub reserved_padding_109: u32,
    pub reserved_padding_110: u32,
    pub reserved_padding_111: u32,
    pub reserved_padding_112: u32,
    pub reserved_padding_113: u32,
    pub reserved_padding_114: u32,
    pub reserved_padding_115: u32,
    pub reserved_padding_116: u32,
    pub reserved_padding_117: u32,
    pub reserved_padding_118: u32,
    pub reserved_padding_119: u32,
    pub reserved_padding_120: u32,
    pub reserved_padding_121: u32,
    pub reserved_padding_122: u32,
    pub reserved_padding_123: u32,
    pub reserved_padding_124: u32,
    pub reserved_padding_125: u32,
    pub reserved_padding_126: u32,
    pub reserved_padding_127: u32,
    pub reserved_padding_128: u32,
    pub reserved_padding_129: u32,
    pub reserved_padding_130: u32,
    pub reserved_padding_131: u32,
    pub reserved_padding_132: u32,
    pub reserved_padding_133: u32,
    pub reserved_padding_134: u32,
    pub reserved_padding_135: u32,
    pub reserved_padding_136: u32,
    pub reserved_padding_137: u32,
    pub reserved_padding_138: u32,
    pub reserved_padding_139: u32,
    pub reserved_padding_140: u32,
    pub reserved_padding_141: u32,
    pub reserved_padding_142: u32,
    pub reserved_padding_143: u32,
    pub reserved_padding_144: u32,
    pub reserved_padding_145: u32,
    pub reserved_padding_146: u32,
    pub reserved_padding_147: u32,
    pub reserved_padding_148: u32,
    pub reserved_padding_149: u32,
    pub reserved_padding_150: u32,
    pub reserved_padding_151: u32,
    pub reserved_padding_152: u32,
    pub reserved_padding_153: u32,
    pub reserved_padding_154: u32,
    pub reserved_padding_155: u32,
    pub reserved_padding_156: u32,
    pub reserved_padding_157: u32,
    pub reserved_padding_158: u32,
    pub reserved_padding_159: u32,
    pub reserved_padding_160: u32,
    pub reserved_padding_161: u32,
    pub reserved_padding_162: u32,
    pub reserved_padding_163: u32,
    pub reserved_padding_164: u32,
    pub reserved_padding_165: u32,
    pub reserved_padding_166: u32,
    pub reserved_padding_167: u32,
    pub reserved_padding_168: u32,
    pub reserved_padding_169: u32,
    pub reserved_padding_170: u32,
    pub reserved_padding_171: u32,
    pub reserved_padding_172: u32,
    pub reserved_padding_173: u32,
    pub reserved_padding_174: u32,
    pub reserved_padding_175: u32,
    pub reserved_padding_176: u32,
    pub reserved_padding_177: u32,
    pub reserved_padding_178: u32,
    pub reserved_padding_179: u32,
    pub reserved_padding_180: u32,
    pub reserved_padding_181: u32,
    pub reserved_padding_182: u32,
    pub reserved_padding_183: u32,
    pub reserved_padding_184: u32,
    pub reserved_padding_185: u32,
    pub reserved_padding_186: u32,
    pub reserved_padding_187: u32,
    pub reserved_padding_188: u32,
    pub reserved_padding_189: u32,
    pub reserved_padding_190: u32,
    pub reserved_padding_191: u32,
    pub reserved_padding_192: u32,
    pub reserved_padding_193: u32,
    pub reserved_padding_194: u32,
    pub reserved_padding_195: u32,
    pub reserved_padding_196: u32,
    pub reserved_padding_197: u32,
    pub reserved_padding_198: u32,
    pub reserved_padding_199: u32,
    pub reserved_padding_200: u32,
    pub reserved_padding_201: u32,
    pub reserved_padding_202: u32,
    pub reserved_padding_203: u32,
    pub reserved_padding_204: u32,
    pub reserved_padding_205: u32,
    pub reserved_padding_206: u32,
    pub reserved_padding_207: u32,
    pub reserved_padding_208: u32,
    pub reserved_padding_209: u32,
    pub reserved_padding_210: u32,
    pub reserved_padding_211: u32,
    pub reserved_padding_212: u32,
    pub reserved_padding_213: u32,
    pub reserved_padding_214: u32,
    pub reserved_padding_215: u32,
    pub reserved_padding_216: u32,
    pub reserved_padding_217: u32,
    pub reserved_padding_218: u32,
    pub reserved_padding_219: u32,
    pub reserved_padding_220: u32,
    pub reserved_padding_221: u32,
    pub reserved_padding_222: u32,
    pub reserved_padding_223: u32,
    pub reserved_padding_224: u32,
    pub reserved_padding_225: u32,
    pub reserved_padding_226: u32,
    pub reserved_padding_227: u32,
    pub reserved_padding_228: u32,
    pub reserved_padding_229: u32,
    pub reserved_padding_230: u32,
    pub reserved_padding_231: u32,
    pub reserved_padding_232: u32,
    pub reserved_padding_233: u32,
    pub reserved_padding_234: u32,
    pub reserved_padding_235: u32,
    pub reserved_padding_236: u32,
    pub reserved_padding_237: u32,
    pub reserved_padding_238: u32,
    pub reserved_padding_239: u32,
    pub reserved_padding_240: u32,
    pub reserved_padding_241: u32,
    pub reserved_padding_242: u32,
    pub reserved_padding_243: u32,
    pub reserved_padding_244: u32,
    pub reserved_padding_245: u32,
    pub reserved_padding_246: u32,
    pub reserved_padding_247: u32,
    pub reserved_padding_248: u32,
    pub reserved_padding_249: u32,
    pub reserved_padding_250: u32,
    pub reserved_padding_251: u32,
    pub reserved_padding_252: u32,
    pub reserved_padding_253: u32,
    pub reserved_padding_254: u32,
    pub reserved_padding_255: u32,
    pub reserved_padding_256: u32,
    pub reserved_padding_257: u32,
    pub reserved_padding_258: u32,
    pub reserved_padding_259: u32,
    pub reserved_padding_260: u32,
    pub reserved_padding_261: u32,
    pub reserved_padding_262: u32,
    pub reserved_padding_263: u32,
    pub reserved_padding_264: u32,
    pub reserved_padding_265: u32,
    pub reserved_padding_266: u32,
    pub reserved_padding_267: u32,
    pub reserved_padding_268: u32,
    pub reserved_padding_269: u32,
    pub reserved_padding_270: u32,
    pub reserved_padding_271: u32,
    pub reserved_padding_272: u32,
    pub reserved_padding_273: u32,
    pub reserved_padding_274: u32,
    pub reserved_padding_275: u32,
    pub reserved_padding_276: u32,
    pub reserved_padding_277: u32,
    pub reserved_padding_278: u32,
    pub reserved_padding_279: u32,
    pub reserved_padding_280: u32,
    pub reserved_padding_281: u32,
    pub reserved_padding_282: u32,
    pub reserved_padding_283: u32,
    pub reserved_padding_284: u32,
    pub reserved_padding_285: u32,
    pub reserved_padding_286: u32,
    pub reserved_padding_287: u32,
    pub reserved_padding_288: u32,
    pub reserved_padding_289: u32,
    pub reserved_padding_290: u32,
    pub reserved_padding_291: u32,
    pub reserved_padding_292: u32,
    pub reserved_padding_293: u32,
    pub reserved_padding_294: u32,
    pub reserved_padding_295: u32,
    pub reserved_padding_296: u32,
    pub reserved_padding_297: u32,
    pub reserved_padding_298: u32,
    pub reserved_padding_299: u32,
    pub reserved_padding_300: u32,
    pub reserved_padding_301: u32,
    pub reserved_padding_302: u32,
    pub reserved_padding_303: u32,
    pub reserved_padding_304: u32,
    pub reserved_padding_305: u32,
    pub i_: u32,
    pub texture_: u32,
    pub field_pointer_: u32,
    pub array_: u32,
    pub count_: u32,
    pub field_indices_: u32,
    pub field_pointer__1: u32,
    pub array__1: u32,
    pub field_pointer__2: u32,
    pub c_: u32,
    pub field_c_: u32,
    pub field_pointer__3: u32,
    pub field_pointer__4: u32,
    pub units_: u32,
    pub field_pointer__5: u32,
    pub field_pointer__6: u32,
    pub field_residences_: u32,
    pub border_: u32,
    pub border__1: u32,
    pub width_: u32,
    pub height_: u32,
    pub field_textures_: u32,
    pub field_textures__1: u32,
    pub field_params_: usize,
    pub texture__1: u32,
    pub field_priorities_: u32,
    pub field_pixels_: u32,
    pub field_pixels__1: u32,
    pub field_void_: u32,
    pub mask_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GLCLTPROCTABLE {
    pub cEntries: u32,
    pub glDispatchTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRESENTBUFFERSCB {
    pub nVersion: u32,
    pub syncType: u32,
    pub luidAdapter: u32,
    pub pPrivData: usize,
    pub updateRect: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHECKFULLSCREENSUPPORTCB {
    pub nVersion: u32,
    pub luidAdapter: u32,
    pub hDevice: u32,
    pub hMonitor: u32,
    pub VidPnSourceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRESENTTOREDIRECTIONSURFACECB {
    pub nVersion: u32,
    pub hContext: u32,
    pub hSource: u32,
    pub hDestination: u32,
    pub hSharedHandle: usize,
    pub updateId: u64,
    pub updateRect: u32,
    pub broadcastContextCount: u32,
    pub broadcastContext: [u32; 64],
    pub broadcastSrcAllocation: [u32; 64],
    pub broadcastDstAllocation: [u32; 64],
    pub cbPrivateDriverDataSize: u32,
    pub field_pPrivateDriverData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUBMITPRESENTTOREDIRECTIONSURFACECB {
    pub nVersion: u32,
    pub hSharedHandle: usize,
    pub updateId: u64,
    pub updateRect: u32,
    pub broadcastHwQueueCount: u32,
    pub field_broadcastHwQueue: u32,
    pub field_broadcastSrcAllocation: u32,
    pub field_broadcastDstAllocation: u32,
    pub cbPrivateDriverDataSize: u32,
    pub field_pPrivateDriverData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRESENTBUFFERS {
    pub hSurface: usize,
    pub luidAdapter: u32,
    pub ullPresentToken: u64,
    pub pPrivData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPENGLCMD {
    pub ulSubEsc: u32,
    pub fl: u32,
    pub field_pwo: u32,
    pub field_pxo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPENGLCMDMULTI {
    pub ulSubEsc: u32,
    pub fl: u32,
    pub cMulti: u32,
    pub field_pxo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OPENGLGETINFO {
    pub ulSubEsc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GLDRVNAME {
    pub oglget: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GLDRVNAMERET {
    pub ulVersion: u32,
    pub ulDriverVersion: u32,
    pub awch: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GUID {
    pub Data1: u32,
    pub Data2: u32,
    pub Data3: u32,
    pub Data4: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_XFER_PACKET {
    pub reportBuffer: *mut u8,
    pub reportBufferLen: u32,
    pub reportId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_INTERFACE_NOTIFY_PNP {
    pub i: u32,
    pub StatusChangeFn: usize,
    pub CallbackContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_INTERFACE_HIDPARSE {
    pub i: u32,
    pub HidpGetCaps: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_COLLECTION_INFORMATION {
    pub DescriptorSize: u32,
    pub Polled: u8,
    pub Reserved1: [u8; 1],
    pub VendorID: u16,
    pub ProductID: u16,
    pub VersionNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HID_DRIVER_CONFIG {
    pub Size: u32,
    pub RingBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USAGE_AND_PAGE {
    pub Usage: u32,
    pub UsagePage: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_BUTTON_CAPS {
    pub UsagePage: u32,
    pub ReportID: u8,
    pub IsAlias: u8,
    pub BitField: u16,
    pub LinkCollection: u16,
    pub LinkUsage: u32,
    pub LinkUsagePage: u32,
    pub IsRange: u8,
    pub IsStringRange: u8,
    pub IsDesignatorRange: u8,
    pub IsAbsolute: u8,
    pub ReportCount: u16,
    pub Reserved2: u16,
    pub Reserved: [u32; 9],
    pub UsageMax: u32,
    pub StringMax: u32,
    pub DesignatorMax: u32,
    pub DataIndexMax: u32,
    pub Reserved1: u32,
    pub Reserved2_1: u32,
    pub Reserved3: u32,
    pub Reserved4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_VALUE_CAPS {
    pub UsagePage: u32,
    pub ReportID: u8,
    pub IsAlias: u8,
    pub BitField: u16,
    pub LinkCollection: u16,
    pub LinkUsage: u32,
    pub LinkUsagePage: u32,
    pub IsRange: u8,
    pub IsStringRange: u8,
    pub IsDesignatorRange: u8,
    pub IsAbsolute: u8,
    pub HasNull: u8,
    pub Reserved: u8,
    pub BitSize: u16,
    pub ReportCount: u16,
    pub Reserved2: [u16; 5],
    pub UnitsExp: u32,
    pub Units: u32,
    pub LogicalMax: u32,
    pub PhysicalMax: u32,
    pub UsageMax: u32,
    pub StringMax: u32,
    pub DesignatorMax: u32,
    pub DataIndexMax: u32,
    pub Reserved1: u32,
    pub Reserved2_1: u32,
    pub Reserved3: u32,
    pub Reserved4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_LINK_COLLECTION_NODE {
    pub LinkUsage: u32,
    pub LinkUsagePage: u32,
    pub Parent: u16,
    pub NumberOfChildren: u16,
    pub NextSibling: u16,
    pub FirstChild: u16,
    pub CollectionType: u32,
    pub IsAlias: u32,
    pub Reserved: u32,
    pub UserContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_CAPS {
    pub Usage: u32,
    pub UsagePage: u32,
    pub InputReportByteLength: u16,
    pub OutputReportByteLength: u16,
    pub FeatureReportByteLength: u16,
    pub Reserved: [u16; 17],
    pub NumberLinkCollectionNodes: u16,
    pub NumberInputButtonCaps: u16,
    pub NumberInputValueCaps: u16,
    pub NumberInputDataIndices: u16,
    pub NumberOutputButtonCaps: u16,
    pub NumberOutputValueCaps: u16,
    pub NumberOutputDataIndices: u16,
    pub NumberFeatureButtonCaps: u16,
    pub NumberFeatureValueCaps: u16,
    pub NumberFeatureDataIndices: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_DATA {
    pub DataIndex: u16,
    pub Reserved: u16,
    pub RawValue: u32,
    pub On: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_UNKNOWN_TOKEN {
    pub Token: u8,
    pub Reserved: [u8; 3],
    pub BitField: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_EXTENDED_ATTRIBUTES {
    pub NumGlobalUnknowns: u8,
    pub reserved_padding: [u32; 3],
    pub GlobalUnknowns: usize,
    pub reserved_padding_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_BUTTON_ARRAY_DATA {
    pub ArrayIndex: u16,
    pub On: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDP_KEYBOARD_MODIFIER_STATE {
    pub LeftControl: u32,
    pub LeftShift: u32,
    pub LeftAlt: u32,
    pub LeftGUI: u32,
    pub RightControl: u32,
    pub RightShift: u32,
    pub RightAlt: u32,
    pub RigthGUI: u32,
    pub CapsLock: u32,
    pub ScollLock: u32,
    pub NumLock: u32,
    pub Reserved: u32,
    pub ul: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDD_CONFIGURATION {
    pub cookie: usize,
    pub size: u32,
    pub RingBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HIDD_ATTRIBUTES {
    pub Size: u32,
    pub VendorID: u16,
    pub ProductID: u16,
    pub VersionNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKADDR_HV {
    pub Family: u32,
    pub Reserved: u16,
    pub VmId: u32,
    pub ServiceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HVSOCKET_ADDRESS_INFO {
    pub SystemId: u32,
    pub VirtualMachineId: u32,
    pub SiloId: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HWN_SETTINGS {
    pub HwNId: u32,
    pub HwNType: u32,
    pub HwNSettings: [u32; 1],
    pub OffOnBlink: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HWN_HEADER {
    pub HwNPayloadSize: u32,
    pub HwNPayloadVersion: u32,
    pub HwNRequests: u32,
    pub HwNSettingsInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_IF_RCV_ADDRESS_LH {
    pub ifRcvAddressType: u32,
    pub ifRcvAddressLength: u16,
    pub ifRcvAddressOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_IF_ALIAS_LH {
    pub ifAliasLength: u16,
    pub ifAliasOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PHYSICAL_LOCATION_LH {
    pub BusNumber: u32,
    pub SlotNumber: u32,
    pub FunctionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IF_COUNTED_STRING_LH {
    pub Length: u16,
    pub String: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IF_PHYSICAL_ADDRESS_LH {
    pub Length: u16,
    pub Address: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_INTERFACE_INFORMATION {
    pub ifOperStatus: u32,
    pub ifOperStatusFlags: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub ifMtu: u32,
    pub ifPromiscuousMode: u8,
    pub ifDeviceWakeUpEnable: u8,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub ifLastChange: u32,
    pub ifCounterDiscontinuityTime: u32,
    pub ifInUnknownProtos: u32,
    pub ifInDiscards: u32,
    pub ifInErrors: u32,
    pub ifHCInOctets: u32,
    pub ifHCInUcastPkts: u32,
    pub ifHCInMulticastPkts: u32,
    pub ifHCInBroadcastPkts: u32,
    pub ifHCOutOctets: u32,
    pub ifHCOutUcastPkts: u32,
    pub ifHCOutMulticastPkts: u32,
    pub ifHCOutBroadcastPkts: u32,
    pub ifOutErrors: u32,
    pub ifOutDiscards: u32,
    pub ifHCInUcastOctets: u32,
    pub ifHCInMulticastOctets: u32,
    pub ifHCInBroadcastOctets: u32,
    pub ifHCOutUcastOctets: u32,
    pub ifHCOutMulticastOctets: u32,
    pub ifHCOutBroadcastOctets: u32,
    pub CompartmentId: u32,
    pub SupportedStatistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFNUMBER {
    pub dwValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFROW {
    pub wszName: [u32; 1],
    pub dwIndex: u32,
    pub dwType: u32,
    pub dwMtu: u32,
    pub dwSpeed: u32,
    pub dwPhysAddrLen: u32,
    pub bPhysAddr: [u8; 1],
    pub dwAdminStatus: u32,
    pub dwOperStatus: u32,
    pub dwLastChange: u32,
    pub dwInOctets: u32,
    pub dwInUcastPkts: u32,
    pub dwInNUcastPkts: u32,
    pub dwInDiscards: u32,
    pub dwInErrors: u32,
    pub dwInUnknownProtos: u32,
    pub dwOutOctets: u32,
    pub dwOutUcastPkts: u32,
    pub dwOutNUcastPkts: u32,
    pub dwOutDiscards: u32,
    pub dwOutErrors: u32,
    pub dwOutQLen: u32,
    pub dwDescrLen: u32,
    pub bDescr: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_EVENT_MOUNT {
    pub Version: u32,
    pub Flags: u32,
    pub FileSystemNameLength: u32,
    pub FileSystemNameOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_EVENT_BECOMING_READY {
    pub Version: u32,
    pub Reason: u32,
    pub Estimated100msToReady: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_EVENT_EXTERNAL_REQUEST {
    pub Version: u32,
    pub DeviceClass: u32,
    pub ButtonStatus: u16,
    pub Request: u16,
    pub SystemTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_EVENT_GENERIC_DATA {
    pub EventNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_EVENT_RBC_DATA {
    pub EventNumber: u32,
    pub SenseQualifier: u8,
    pub SenseCode: u8,
    pub SenseKey: u8,
    pub Reserved: u8,
    pub Information: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GUID_IO_DISK_CLONE_ARRIVAL_INFORMATION {
    pub DiskNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_HEALTH_NOTIFICATION_DATA {
    pub DeviceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPADDRROW_XP {
    pub dwAddr: u32,
    pub dwIndex: u32,
    pub dwMask: u32,
    pub dwBCastAddr: u32,
    pub dwReasmSize: u32,
    pub unused1: u32,
    pub wType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPADDRROW_W2K {
    pub dwAddr: u32,
    pub dwIndex: u32,
    pub dwMask: u32,
    pub dwBCastAddr: u32,
    pub dwReasmSize: u32,
    pub unused1: u32,
    pub unused2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPADDRTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPFORWARDNUMBER {
    pub dwValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPFORWARDROW {
    pub dwForwardDest: u32,
    pub dwForwardMask: u32,
    pub dwForwardPolicy: u32,
    pub dwForwardNextHop: u32,
    pub dwForwardIfIndex: u32,
    pub dwForwardType: u32,
    pub ForwardType: u32,
    pub dwForwardProto: u32,
    pub ForwardProto: u32,
    pub dwForwardAge: u32,
    pub dwForwardNextHopAS: u32,
    pub dwForwardMetric1: u32,
    pub dwForwardMetric2: u32,
    pub dwForwardMetric3: u32,
    pub dwForwardMetric4: u32,
    pub dwForwardMetric5: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPFORWARDTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPNETROW_LH {
    pub dwIndex: u32,
    pub dwPhysAddrLen: u32,
    pub bPhysAddr: [u8; 1],
    pub dwAddr: u32,
    pub dwType: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPNETROW_W2K {
    pub dwIndex: u32,
    pub dwPhysAddrLen: u32,
    pub bPhysAddr: [u8; 1],
    pub dwAddr: u32,
    pub dwType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPNETTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPSTATS_LH {
    pub dwForwarding: u32,
    pub Forwarding: u32,
    pub dwDefaultTTL: u32,
    pub dwInReceives: u32,
    pub dwInHdrErrors: u32,
    pub dwInAddrErrors: u32,
    pub dwForwDatagrams: u32,
    pub dwInUnknownProtos: u32,
    pub dwInDiscards: u32,
    pub dwInDelivers: u32,
    pub dwOutRequests: u32,
    pub dwRoutingDiscards: u32,
    pub dwOutDiscards: u32,
    pub dwOutNoRoutes: u32,
    pub dwReasmTimeout: u32,
    pub dwReasmReqds: u32,
    pub dwReasmOks: u32,
    pub dwReasmFails: u32,
    pub dwFragOks: u32,
    pub dwFragFails: u32,
    pub dwFragCreates: u32,
    pub dwNumIf: u32,
    pub dwNumAddr: u32,
    pub dwNumRoutes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPSTATS_W2K {
    pub dwForwarding: u32,
    pub dwDefaultTTL: u32,
    pub dwInReceives: u32,
    pub dwInHdrErrors: u32,
    pub dwInAddrErrors: u32,
    pub dwForwDatagrams: u32,
    pub dwInUnknownProtos: u32,
    pub dwInDiscards: u32,
    pub dwInDelivers: u32,
    pub dwOutRequests: u32,
    pub dwRoutingDiscards: u32,
    pub dwOutDiscards: u32,
    pub dwOutNoRoutes: u32,
    pub dwReasmTimeout: u32,
    pub dwReasmReqds: u32,
    pub dwReasmOks: u32,
    pub dwReasmFails: u32,
    pub dwFragOks: u32,
    pub dwFragFails: u32,
    pub dwFragCreates: u32,
    pub dwNumIf: u32,
    pub dwNumAddr: u32,
    pub dwNumRoutes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIBICMPSTATS {
    pub dwMsgs: u32,
    pub dwErrors: u32,
    pub dwDestUnreachs: u32,
    pub dwTimeExcds: u32,
    pub dwParmProbs: u32,
    pub dwSrcQuenchs: u32,
    pub dwRedirects: u32,
    pub dwEchos: u32,
    pub dwEchoReps: u32,
    pub dwTimestamps: u32,
    pub dwTimestampReps: u32,
    pub dwAddrMasks: u32,
    pub dwAddrMaskReps: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIBICMPINFO {
    pub icmpInStats: u32,
    pub icmpOutStats: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_ICMP {
    pub stats: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIBICMPSTATS_EX_XPSP1 {
    pub dwMsgs: u32,
    pub dwErrors: u32,
    pub rgdwTypeCount: [u32; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_ICMP_EX_XPSP1 {
    pub icmpInStats: u32,
    pub icmpOutStats: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_OIF_XP {
    pub dwOutIfIndex: u32,
    pub dwNextHopAddr: u32,
    pub dwReserved: u32,
    pub dwReserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_OIF_W2K {
    pub dwOutIfIndex: u32,
    pub dwNextHopAddr: u32,
    pub pvReserved: usize,
    pub dwReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_MFE {
    pub dwGroup: u32,
    pub dwSource: u32,
    pub dwSrcMask: u32,
    pub dwUpStrmNgbr: u32,
    pub dwInIfIndex: u32,
    pub dwInIfProtocol: u32,
    pub dwRouteProtocol: u32,
    pub dwRouteNetwork: u32,
    pub dwRouteMask: u32,
    pub ulUpTime: u32,
    pub ulExpiryTime: u32,
    pub ulTimeOut: u32,
    pub ulNumOutIf: u32,
    pub fFlags: u32,
    pub dwReserved: u32,
    pub rgmioOutInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_MFE_TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_OIF_STATS_LH {
    pub dwOutIfIndex: u32,
    pub dwNextHopAddr: u32,
    pub dwDialContext: u32,
    pub ulTtlTooLow: u32,
    pub ulFragNeeded: u32,
    pub ulOutPackets: u32,
    pub ulOutDiscards: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_OIF_STATS_W2K {
    pub dwOutIfIndex: u32,
    pub dwNextHopAddr: u32,
    pub pvDialContext: usize,
    pub ulTtlTooLow: u32,
    pub ulFragNeeded: u32,
    pub ulOutPackets: u32,
    pub ulOutDiscards: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_MFE_STATS {
    pub dwGroup: u32,
    pub dwSource: u32,
    pub dwSrcMask: u32,
    pub dwUpStrmNgbr: u32,
    pub dwInIfIndex: u32,
    pub dwInIfProtocol: u32,
    pub dwRouteProtocol: u32,
    pub dwRouteNetwork: u32,
    pub dwRouteMask: u32,
    pub ulUpTime: u32,
    pub ulExpiryTime: u32,
    pub ulNumOutIf: u32,
    pub ulInPkts: u32,
    pub ulInOctets: u32,
    pub ulPktsDifferentIf: u32,
    pub ulQueueOverflow: u32,
    pub rgmiosOutStats: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_MFE_STATS_TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_MFE_STATS_EX_XP {
    pub dwGroup: u32,
    pub dwSource: u32,
    pub dwSrcMask: u32,
    pub dwUpStrmNgbr: u32,
    pub dwInIfIndex: u32,
    pub dwInIfProtocol: u32,
    pub dwRouteProtocol: u32,
    pub dwRouteNetwork: u32,
    pub dwRouteMask: u32,
    pub ulUpTime: u32,
    pub ulExpiryTime: u32,
    pub ulNumOutIf: u32,
    pub ulInPkts: u32,
    pub ulInOctets: u32,
    pub ulPktsDifferentIf: u32,
    pub ulQueueOverflow: u32,
    pub ulUninitMfe: u32,
    pub ulNegativeMfe: u32,
    pub ulInDiscards: u32,
    pub ulInHdrErrors: u32,
    pub ulTotalOutPackets: u32,
    pub rgmiosOutStats: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_MFE_STATS_TABLE_EX_XP {
    pub dwNumEntries: u32,
    pub table: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_GLOBAL {
    pub dwEnable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_IF_ENTRY {
    pub dwIfIndex: u32,
    pub dwTtl: u32,
    pub dwProtocol: u32,
    pub dwRateLimit: u32,
    pub ulInMcastOctets: u32,
    pub ulOutMcastOctets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_IF_TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_OPAQUE_QUERY {
    pub dwVarId: u32,
    pub rgdwVarIndex: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCPIP_OWNER_MODULE_BASIC_INFO {
    pub pModuleName: usize,
    pub pModulePath: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_BOUNDARY {
    pub dwIfIndex: u32,
    pub dwGroupAddress: u32,
    pub dwGroupMask: u32,
    pub dwStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_BOUNDARY_TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPMCAST_SCOPE {
    pub dwGroupAddress: u32,
    pub dwGroupMask: u32,
    pub snNameBuffer: u32,
    pub dwStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPDESTROW {
    pub ForwardRow: u32,
    pub dwForwardPreference: u32,
    pub dwForwardViewSet: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPDESTTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_BEST_IF {
    pub dwDestAddr: u32,
    pub dwIfIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_PROXYARP {
    pub dwAddress: u32,
    pub dwMask: u32,
    pub dwIfIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFSTATUS {
    pub dwIfIndex: u32,
    pub dwAdminStatus: u32,
    pub dwOperationalStatus: u32,
    pub bMHbeatActive: i32,
    pub bMHbeatAlive: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_ROUTESTATE {
    pub bRoutesSetToStack: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_OPAQUE_INFO {
    pub dwId: u32,
    pub ullAlign: u64,
    pub rgbyData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPSEC_KEY_MANAGER0 {
    pub keyManagerKey: u32,
    pub displayData: u32,
    pub flags: u32,
    pub keyDictationTimeoutHint: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDENTIFIER {
    pub Set: u32,
    pub Id: u32,
    pub Flags: u32,
    pub Alignment: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_BOUNDS_LONG {
    pub UnsignedMinimum: u32,
    pub UnsignedMaximum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_BOUNDS_LONGLONG {
    pub UnsignedMinimum: u64,
    pub UnsignedMaximum: u64,
    pub UnsignedMinimum_1: u32,
    pub UnsignedMaximum_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_GRAPHMANAGER_INTERFACE {
    pub GraphManager: usize,
    pub FunctionTable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSEVENT_ENTRY {
    pub ListEntry: u32,
    pub Object: usize,
    pub DpcItem: usize,
    pub BufferItem: usize,
    pub EventData: usize,
    pub NotificationType: u32,
    pub EventSet: usize,
    pub EventItem: usize,
    pub FileObject: usize,
    pub SemaphoreAdjustment: u32,
    pub Reserved: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSGATE {
    pub Count: i32,
    pub NextGate: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSDEVICE_DISPATCH {
    pub Add: usize,
    pub Start: usize,
    pub PostStart: usize,
    pub QueryStop: usize,
    pub CancelStop: usize,
    pub Stop: usize,
    pub QueryRemove: usize,
    pub CancelRemove: usize,
    pub Remove: usize,
    pub QueryCapabilities: usize,
    pub SurpriseRemoval: usize,
    pub QueryPower: usize,
    pub SetPower: usize,
    pub QueryInterface: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSFILTER_DISPATCH {
    pub Create: usize,
    pub Close: usize,
    pub Process: usize,
    pub Reset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPIN_DISPATCH {
    pub Create: usize,
    pub Close: usize,
    pub Process: usize,
    pub Reset: usize,
    pub SetDataFormat: usize,
    pub SetDeviceState: usize,
    pub Connect: usize,
    pub Disconnect: usize,
    pub Clock: usize,
    pub Allocator: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCLOCK_DISPATCH {
    pub SetTimer: usize,
    pub CancelTimer: usize,
    pub CorrelatedTime: usize,
    pub Resolution: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSALLOCATOR_DISPATCH {
    pub InitializeAllocator: usize,
    pub DeleteAllocator: usize,
    pub Allocate: usize,
    pub Free: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSDEVICE_DESCRIPTOR {
    pub Dispatch: usize,
    pub FilterDescriptorsCount: u32,
    pub FilterDescriptors: usize,
    pub Version: u32,
    pub Flags: u32,
    pub Alignment: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSFILTER_DESCRIPTOR {
    pub Dispatch: usize,
    pub AutomationTable: usize,
    pub Version: u32,
    pub Flags: u32,
    pub ReferenceGuid: usize,
    pub PinDescriptorsCount: u32,
    pub PinDescriptorSize: u32,
    pub PinDescriptors: usize,
    pub CategoriesCount: u32,
    pub Categories: usize,
    pub NodeDescriptorsCount: u32,
    pub NodeDescriptorSize: u32,
    pub NodeDescriptors: usize,
    pub ConnectionsCount: u32,
    pub Connections: usize,
    pub ComponentId: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPIN_DESCRIPTOR_EX {
    pub Dispatch: usize,
    pub AutomationTable: usize,
    pub PinDescriptor: u32,
    pub Flags: u32,
    pub InstancesPossible: u32,
    pub InstancesNecessary: u32,
    pub AllocatorFraming: usize,
    pub IntersectHandler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSNODE_DESCRIPTOR {
    pub AutomationTable: usize,
    pub Type: usize,
    pub Name: usize,
    pub Alignment: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSDEVICE {
    pub Descriptor: usize,
    pub Bag: u32,
    pub Context: usize,
    pub FunctionalDeviceObject: usize,
    pub PhysicalDeviceObject: usize,
    pub NextDeviceObject: usize,
    pub Started: u8,
    pub SystemPowerState: u32,
    pub DevicePowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSFILTERFACTORY {
    pub FilterDescriptor: usize,
    pub Bag: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSFILTER {
    pub Descriptor: usize,
    pub Bag: u32,
    pub Context: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPIN {
    pub Descriptor: usize,
    pub Bag: u32,
    pub Context: usize,
    pub Id: u32,
    pub Communication: u32,
    pub ConnectionIsExternal: u8,
    pub ConnectionInterface: u32,
    pub ConnectionMedium: u32,
    pub ConnectionPriority: u32,
    pub ConnectionFormat: usize,
    pub AttributeList: usize,
    pub StreamHeaderSize: u32,
    pub DataFlow: u32,
    pub DeviceState: u32,
    pub ResetState: u32,
    pub ClientState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSMAPPING {
    pub PhysicalAddress: u64,
    pub ByteCount: u32,
    pub Alignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSSTREAM_POINTER_OFFSET {
    pub Data: *mut u8,
    pub Mappings: usize,
    pub Data_1: *mut u8,
    pub Alignment: usize,
    pub Count: u32,
    pub Remaining: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSSTREAM_POINTER {
    pub Context: usize,
    pub Pin: usize,
    pub StreamHeader: usize,
    pub Offset: usize,
    pub OffsetIn: u32,
    pub OffsetOut: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROCESSPIN {
    pub Pin: usize,
    pub StreamPointer: usize,
    pub InPlaceCounterpart: usize,
    pub DelegateBranch: usize,
    pub CopySource: usize,
    pub Data: u32,
    pub BytesAvailable: u32,
    pub BytesUsed: u32,
    pub Flags: u32,
    pub Terminate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROCESSPIN_INDEXENTRY {
    pub field_Pins: u32,
    pub Count: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSDEVICE_THERMAL_DISPATCH {
    pub ActiveCooling: usize,
    pub PassiveCooling: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIO_PACKETSIZE_PROCESSINGMODE_CONSTRAINT {
    pub ProcessingMode: u32,
    pub SamplesPerProcessingPacket: u32,
    pub ProcessingPacketDurationInHns: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIO_PACKETSIZE_CONSTRAINTS {
    pub MinPacketPeriodInHns: u32,
    pub PacketSizeFileAlignment: u32,
    pub Reserved: u32,
    pub NumProcessingModeConstraints: u32,
    pub ProcessingModeConstraints: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIO_PACKETSIZE_CONSTRAINTS2 {
    pub MinPacketPeriodInHns: u32,
    pub PacketSizeFileAlignment: u32,
    pub MaxPacketSizeInBytes: u32,
    pub NumProcessingModeConstraints: u32,
    pub ProcessingModeConstraints: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DS3DVECTOR {
    pub x: u32,
    pub dvX: u32,
    pub y: u32,
    pub dvY: u32,
    pub z: u32,
    pub dvZ: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSTELEPHONY_CALLCONTROL {
    pub CallType: u32,
    pub CallControlOp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSTELEPHONY_PROVIDERCHANGE {
    pub CallType: u32,
    pub ProviderChangeOp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSTELEPHONY_CALLINFO {
    pub CallType: u32,
    pub CallState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSTOPOLOGY_ENDPOINTID {
    pub TopologyName: [u32; 1],
    pub PinId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSTOPOLOGY_ENDPOINTIDPAIR {
    pub RenderEndpoint: u32,
    pub CaptureEndpoint: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSMPEGVID_RECT {
    pub StartX: u32,
    pub StartY: u32,
    pub EndX: u32,
    pub EndY: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVD_YCrCb {
    pub Reserved: u8,
    pub Y: u8,
    pub Cr: u8,
    pub Cb: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVD_YUV {
    pub Reserved: u8,
    pub Y: u8,
    pub V: u8,
    pub U: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_SPPAL {
    pub sppal: [u32; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_COLCON {
    pub emph1col: u8,
    pub emph2col: u8,
    pub backcol: u8,
    pub patcol: u8,
    pub emph1con: u8,
    pub emph2con: u8,
    pub backcon: u8,
    pub patcon: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_SPHLI {
    pub HLISS: u16,
    pub Reserved: u16,
    pub StartPTM: u32,
    pub EndPTM: u32,
    pub StartX: u16,
    pub StartY: u16,
    pub StopX: u16,
    pub StopY: u16,
    pub ColCon: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_CHLGKEY {
    pub ChlgKey: [u8; 10],
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_BUSKEY {
    pub BusKey: [u8; 5],
    pub Reserved: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_DISCKEY {
    pub DiscKey: [u8; 2048],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_REGION {
    pub Reserved: u8,
    pub RegionData: u8,
    pub Reserved2: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_TITLEKEY {
    pub KeyFlags: u32,
    pub ReservedNT: [u32; 2],
    pub TitleKey: [u8; 6],
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_COPY_MACROVISION {
    pub MACROVISIONLevel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_DVDCOPY_SET_COPY_STATE {
    pub DVDCopyState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KS_VIDEO_STREAM_CONFIG_CAPS {
    pub guid: u32,
    pub VideoStandard: u32,
    pub InputSize: u32,
    pub MinCroppingSize: u32,
    pub MaxCroppingSize: u32,
    pub CropGranularityX: u32,
    pub CropGranularityY: u32,
    pub CropAlignX: u32,
    pub CropAlignY: u32,
    pub MinOutputSize: u32,
    pub MaxOutputSize: u32,
    pub OutputGranularityX: u32,
    pub OutputGranularityY: u32,
    pub StretchTapsX: u32,
    pub StretchTapsY: u32,
    pub ShrinkTapsX: u32,
    pub ShrinkTapsY: u32,
    pub MinFrameInterval: i64,
    pub MaxFrameInterval: i64,
    pub MinBitsPerSecond: i32,
    pub MaxBitsPerSecond: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICAP_PROPERTIES_PROTECTION_S {
    pub Property: u32,
    pub StreamIndex: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NABTSFEC_BUFFER {
    pub dataSize: u32,
    pub groupID: u16,
    pub Reserved: u16,
    pub data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_SCANLINES {
    pub DwordBitArray: [u32; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_NABTS_SUBSTREAMS {
    pub SubstreamMask: [u32; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_CC_SUBSTREAMS {
    pub SubstreamMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_BYTE_PAIR {
    pub Decoded: [u8; 2],
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CC_HW_FIELD {
    pub ScanlinesRequested: u32,
    pub fieldFlags: u32,
    pub PictureNumber: i64,
    pub Lines: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NABTS_BUFFER_LINE {
    pub Confidence: u8,
    pub Bytes: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NABTS_BUFFER {
    pub ScanlinesRequested: u32,
    pub PictureNumber: i64,
    pub NabtsLines: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WST_BUFFER_LINE {
    pub Confidence: u8,
    pub Bytes: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WST_BUFFER {
    pub ScanlinesRequested: u32,
    pub WstLines: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_COMMON {
    pub InputSRBsProcessed: u32,
    pub OutputSRBsProcessed: u32,
    pub SRBsIgnored: u32,
    pub InputSRBsMissing: u32,
    pub OutputSRBsMissing: u32,
    pub OutputFailures: u32,
    pub InternalErrors: u32,
    pub ExternalErrors: u32,
    pub InputDiscontinuities: u32,
    pub DSPFailures: u32,
    pub TvTunerChanges: u32,
    pub VBIHeaderChanges: u32,
    pub LineConfidenceAvg: u32,
    pub BytesOutput: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_COMMON_PIN {
    pub SRBsProcessed: u32,
    pub SRBsIgnored: u32,
    pub SRBsMissing: u32,
    pub InternalErrors: u32,
    pub ExternalErrors: u32,
    pub Discontinuities: u32,
    pub LineConfidenceAvg: u32,
    pub BytesOutput: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_NABTS {
    pub Common: u32,
    pub FECBundleBadLines: u32,
    pub FECQueueOverflows: u32,
    pub FECCorrectedLines: u32,
    pub FECUncorrectableLines: u32,
    pub BundlesProcessed: u32,
    pub BundlesSent2IP: u32,
    pub FilteredLines: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_NABTS_PIN {
    pub Common: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_CC {
    pub Common: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_CC_PIN {
    pub Common: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_TELETEXT {
    pub Common: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VBICODECFILTERING_STATISTICS_TELETEXT_PIN {
    pub Common: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEOFORMAT_DX12 {
    pub Header: u32,
    pub resourceLayout: u32,
    pub resourceFlags: u32,
    pub customLayout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCAMERA_EXTENDEDPROP_PROFILE {
    pub ProfileId: u32,
    pub Index: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCAMERA_PROFILE_MEDIAINFO {
    pub X: u32,
    pub Y: u32,
    pub Numerator: u32,
    pub Denominator: u32,
    pub Flags: u64,
    pub Data0: u32,
    pub Data1: u32,
    pub Data2: u32,
    pub Data3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCAMERA_PROFILE_PININFO {
    pub PinCategory: u32,
    pub PinIndex: u16,
    pub ProfileSensorType: u16,
    pub Reserved: u32,
    pub MediaInfoCount: u32,
    pub MediaInfos: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCAMERA_PROFILE_INFO {
    pub ProfileId: u32,
    pub Index: u32,
    pub PinCount: u32,
    pub Pins: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSCAMERA_PROFILE_CONCURRENCYINFO {
    pub ReferenceGuid: u32,
    pub Reserved: u32,
    pub ProfileCount: u32,
    pub Profiles: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSDEVICE_PROFILE_INFO {
    pub Type: u32,
    pub Size: u32,
    pub Info: u32,
    pub Reserved: u32,
    pub ConcurrencyCount: u32,
    pub Concurrency: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WNF_KSCAMERA_STREAMSTATE_INFO {
    pub ProcessId: u32,
    pub SessionId: u32,
    pub StreamState: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_NETWORKCAMERACONTROL_METADATA_INFO {
    pub MetadataItems: u32,
    pub Size: u32,
    pub PTZStatus: i32,
    pub Events: i32,
    pub Analytics: i32,
    pub Reserved: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSPROPERTY_NETWORKCAMERACONTROL_EVENT_INFO {
    pub Header: u32,
    pub EventFilter: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DDPIXELFORMAT {
    pub dwSize: u32,
    pub dwFlags: u32,
    pub dwFourCC: u32,
    pub dwRGBBitCount: u32,
    pub dwYUVBitCount: u32,
    pub dwZBufferBitDepth: u32,
    pub dwAlphaBitDepth: u32,
    pub dwRBitMask: u32,
    pub dwYBitMask: u32,
    pub dwGBitMask: u32,
    pub dwUBitMask: u32,
    pub dwBBitMask: u32,
    pub dwVBitMask: u32,
    pub dwRGBAlphaBitMask: u32,
    pub dwYUVAlphaBitMask: u32,
    pub dwRGBZBitMask: u32,
    pub dwYUVZBitMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DDVIDEOPORTCONNECT {
    pub dwSize: u32,
    pub dwPortWidth: u32,
    pub guidTypeID: u32,
    pub dwFlags: u32,
    pub dwReserved1: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSGOP_USERDATA {
    pub sc: u32,
    pub reserved1: u32,
    pub cFields: u8,
    pub l21Data: [i8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSJACK_SINK_INFORMATION {
    pub ConnType: u32,
    pub ManufacturerId: u16,
    pub ProductId: u16,
    pub AudioLatency: u16,
    pub HDCPCapable: i32,
    pub AICapable: i32,
    pub SinkDescriptionLength: u8,
    pub SinkDescription: [u32; 1],
    pub PortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSJACK_DESCRIPTION2 {
    pub DeviceStateInfo: u32,
    pub JackCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSJACK_DESCRIPTION3 {
    pub ConfigId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOENGINE_DESCRIPTOR {
    pub nHostPinId: u32,
    pub nOffloadPinId: u32,
    pub nLoopbackPinId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOENGINE_BUFFER_SIZE_RANGE {
    pub MinBufferBytes: u32,
    pub MaxBufferBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOENGINE_VOLUMELEVEL {
    pub TargetVolume: i32,
    pub CurveType: u32,
    pub CurveDuration: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOENGINE_DEVICECONTROLS {
    pub Volume: u32,
    pub Mute: u32,
    pub PeakMeter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOMODULE_DESCRIPTOR {
    pub ClassId: u32,
    pub InstanceId: u32,
    pub VersionMajor: u32,
    pub VersionMinor: u32,
    pub Name: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOMODULE_PROPERTY {
    pub Property: u32,
    pub ClassId: u32,
    pub InstanceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSAUDIOMODULE_NOTIFICATION {
    pub DeviceId: u32,
    pub ClassId: u32,
    pub InstanceId: u32,
    pub Reserved: u32,
    pub Alignment: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PIPE_DIMENSIONS {
    pub AllocatorPin: u32,
    pub MaxExpansionPin: u32,
    pub EndPin: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PIPE_TERMINATION {
    pub Flags: u32,
    pub OutsideFactors: u32,
    pub Weigth: u32,
    pub PhysicalRange: u32,
    pub OptimalRange: u32,
    pub Compression: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ALLOCATOR_PROPERTIES_EX {
    pub cBuffers: u32,
    pub cbBuffer: u32,
    pub cbAlign: u32,
    pub cbPrefix: u32,
    pub MemoryType: u32,
    pub BusType: u32,
    pub State: usize,
    pub Input: usize,
    pub Output: usize,
    pub Strategy: u32,
    pub Flags: u32,
    pub Weight: u32,
    pub LogicalMemoryType: u32,
    pub AllocatorPlace: usize,
    pub Dimensions: usize,
    pub PhysicalRange: u32,
    pub PrevSegment: usize,
    pub CountNextSegments: u32,
    pub NextSegments: usize,
    pub InsideFactors: u32,
    pub NumberPins: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KSSTREAM_SEGMENT {
    pub field_KsInterfaceHandler: u32,
    pub field_KsDataTypeHandler: u32,
    pub IoOperation: u32,
    pub CompletionEvent: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION {
    pub TransactionKey: usize,
    pub TransactionNotification: u32,
    pub TmVirtualClock: i64,
    pub ArgumentLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION_RECOVERY_ARGUMENT {
    pub EnlistmentId: u32,
    pub UOW: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION_TM_ONLINE_ARGUMENT {
    pub TmIdentity: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION_SAVEPOINT_ARGUMENT {
    pub SavepointId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION_PROPAGATE_ARGUMENT {
    pub PropagationCookie: u32,
    pub UOW: u32,
    pub TmIdentity: u32,
    pub BufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSACTION_NOTIFICATION_MARSHAL_ARGUMENT {
    pub MarshalCookie: u32,
    pub UOW: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KCRM_MARSHAL_HEADER {
    pub VersionMajor: u32,
    pub VersionMinor: u32,
    pub NumProtocols: u32,
    pub Unused: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KCRM_TRANSACTION_BLOB {
    pub UOW: u32,
    pub TmIdentity: u32,
    pub IsolationLevel: u32,
    pub IsolationFlags: u32,
    pub Timeout: u32,
    pub Description: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KCRM_PROTOCOL_BLOB {
    pub ProtocolId: u32,
    pub StaticInfoLength: u32,
    pub TransactionIdInfoLength: u32,
    pub Unused1: u32,
    pub Unused2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILETIME {
    pub dwLowDateTime: u32,
    pub dwHighDateTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REAL_TIME_NOTIFICATION_SETTING_INPUT {
    pub TransportSettingId: u32,
    pub BrokerEventGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REAL_TIME_NOTIFICATION_SETTING_INPUT_EX {
    pub TransportSettingId: u32,
    pub BrokerEventGuid: u32,
    pub Unmark: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REAL_TIME_NOTIFICATION_SETTING_OUTPUT {
    pub ChannelStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ASSOCIATE_NAMERES_CONTEXT_INPUT {
    pub TransportSettingId: u32,
    pub Handle: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TIMESTAMPING_CONFIG {
    pub Flags: u32,
    pub TxTimestampsBuffered: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRIORITY_STATUS {
    pub Sender: u32,
    pub Receiver: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_INITIAL_RTO_PARAMETERS {
    pub Rtt: u16,
    pub MaxSynRetransmissions: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ICW_PARAMETERS {
    pub Level: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ACK_FREQUENCY_PARAMETERS {
    pub TcpDelayedAckFrequency: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_INFO_v0 {
    pub State: u32,
    pub Mss: u32,
    pub ConnectionTimeMs: u32,
    pub TimestampsEnabled: u8,
    pub RttUs: u32,
    pub MinRttUs: u32,
    pub BytesInFlight: u32,
    pub Cwnd: u32,
    pub SndWnd: u32,
    pub RcvWnd: u32,
    pub RcvBuf: u32,
    pub BytesOut: u32,
    pub BytesIn: u32,
    pub BytesReordered: u32,
    pub BytesRetrans: u32,
    pub FastRetrans: u32,
    pub DupAcksIn: u32,
    pub TimeoutEpisodes: u32,
    pub SynRetrans: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_INFO_v1 {
    pub State: u32,
    pub Mss: u32,
    pub ConnectionTimeMs: u32,
    pub TimestampsEnabled: u8,
    pub RttUs: u32,
    pub MinRttUs: u32,
    pub BytesInFlight: u32,
    pub Cwnd: u32,
    pub SndWnd: u32,
    pub RcvWnd: u32,
    pub RcvBuf: u32,
    pub BytesOut: u32,
    pub BytesIn: u32,
    pub BytesReordered: u32,
    pub BytesRetrans: u32,
    pub FastRetrans: u32,
    pub DupAcksIn: u32,
    pub TimeoutEpisodes: u32,
    pub SynRetrans: u8,
    pub SndLimTransRwin: u32,
    pub SndLimTimeRwin: u32,
    pub SndLimBytesRwin: u32,
    pub SndLimTransCwnd: u32,
    pub SndLimTimeCwnd: u32,
    pub SndLimBytesCwnd: u32,
    pub SndLimTransSnd: u32,
    pub SndLimTimeSnd: u32,
    pub SndLimBytesSnd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_INFO_v2 {
    pub State: u32,
    pub Mss: u32,
    pub ConnectionTimeMs: u32,
    pub TimestampsEnabled: u8,
    pub RttUs: u32,
    pub MinRttUs: u32,
    pub BytesInFlight: u32,
    pub Cwnd: u32,
    pub SndWnd: u32,
    pub RcvWnd: u32,
    pub RcvBuf: u32,
    pub BytesOut: u32,
    pub BytesIn: u32,
    pub BytesReordered: u32,
    pub BytesRetrans: u32,
    pub FastRetrans: u32,
    pub DupAcksIn: u32,
    pub TimeoutEpisodes: u32,
    pub SynRetrans: u8,
    pub SndLimTransRwin: u32,
    pub SndLimTimeRwin: u32,
    pub SndLimBytesRwin: u32,
    pub SndLimTransCwnd: u32,
    pub SndLimTimeCwnd: u32,
    pub SndLimBytesCwnd: u32,
    pub SndLimTransSnd: u32,
    pub SndLimTimeSnd: u32,
    pub SndLimBytesSnd: u32,
    pub OutOfOrderPktsIn: u32,
    pub EcnNegotiated: u8,
    pub EceAcksIn: u32,
    pub PtoEpisodes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INET_PORT_RANGE {
    pub StartPort: u16,
    pub NumberOfPorts: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_SETTINGS {
    pub SecurityProtocol: u32,
    pub SecurityFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_SETTINGS_IPSEC {
    pub SecurityProtocol: u32,
    pub SecurityFlags: u32,
    pub IpsecFlags: u32,
    pub AuthipMMPolicyKey: u32,
    pub AuthipQMPolicyKey: u32,
    pub Reserved: u32,
    pub Reserved2: u64,
    pub UserNameStringLen: u32,
    pub DomainNameStringLen: u32,
    pub PasswordStringLen: u32,
    pub AllStrings: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_PEER_TARGET_NAME {
    pub SecurityProtocol: u32,
    pub PeerAddress: u32,
    pub PeerTargetNameStringLen: u32,
    pub AllStrings: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_QUERY_TEMPLATE {
    pub SecurityProtocol: u32,
    pub PeerAddress: u32,
    pub PeerTokenAccessMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_QUERY_TEMPLATE_IPSEC2 {
    pub SecurityProtocol: u32,
    pub PeerAddress: u32,
    pub PeerTokenAccessMask: u32,
    pub Flags: u32,
    pub FieldMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_QUERY_INFO {
    pub SecurityProtocol: u32,
    pub Flags: u32,
    pub PeerApplicationAccessTokenHandle: u64,
    pub PeerMachineAccessTokenHandle: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_SECURITY_QUERY_INFO_IPSEC2 {
    pub SecurityProtocol: u32,
    pub Flags: u32,
    pub PeerApplicationAccessTokenHandle: u64,
    pub PeerMachineAccessTokenHandle: u64,
    pub MmSaId: u64,
    pub QmSaId: u64,
    pub NegotiationWinerr: u32,
    pub SaLookupContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSS_SCALABILITY_INFO {
    pub RssEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSA_COMPATIBILITY_MODE {
    pub BehaviorId: u32,
    pub TargetOsVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RIORESULT {
    pub Status: i32,
    pub BytesTransferred: u32,
    pub SocketContext: u64,
    pub RequestContext: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RIO_BUF {
    pub BufferId: u32,
    pub Offset: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RIO_CMSG_BUFFER {
    pub TotalLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_ADAPTER_INFO {
    pub Version: u32,
    pub VendorId: u32,
    pub DeviceId: u32,
    pub MaxRegistrationSize: usize,
    pub MaxWindowSize: usize,
    pub FRMRPageCount: u32,
    pub MaxInitiatorRequestSge: u32,
    pub MaxReceiveRequestSge: u32,
    pub MaxReadRequestSge: u32,
    pub MaxTransferLength: u32,
    pub MaxInlineDataSize: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxReceiveQueueDepth: u32,
    pub MaxInitiatorQueueDepth: u32,
    pub MaxSrqDepth: u32,
    pub MaxCqDepth: u32,
    pub LargeRequestThreshold: u32,
    pub MaxCallerData: u32,
    pub MaxCalleeData: u32,
    pub AdapterFlags: u32,
    pub RdmaTechnology: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IF_ROW2 {
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub InterfaceGuid: u32,
    pub Alias: [u32; 1],
    pub Description: [u32; 1],
    pub PhysicalAddressLength: u32,
    pub PhysicalAddress: [u8; 1],
    pub PermanentPhysicalAddress: [u8; 1],
    pub Mtu: u32,
    pub Type: u32,
    pub TunnelType: u32,
    pub MediaType: u32,
    pub PhysicalMediumType: u32,
    pub AccessType: u32,
    pub DirectionType: u32,
    pub HardwareInterface: u8,
    pub FilterInterface: u8,
    pub ConnectorPresent: u8,
    pub NotAuthenticated: u8,
    pub NotMediaConnected: u8,
    pub Paused: u8,
    pub LowPower: u8,
    pub EndPointInterface: u8,
    pub OperStatus: u32,
    pub AdminStatus: u32,
    pub MediaConnectState: u32,
    pub NetworkGuid: u32,
    pub ConnectionType: u32,
    pub TransmitLinkSpeed: u32,
    pub ReceiveLinkSpeed: u32,
    pub InOctets: u32,
    pub InUcastPkts: u32,
    pub InNUcastPkts: u32,
    pub InDiscards: u32,
    pub InErrors: u32,
    pub InUnknownProtos: u32,
    pub InUcastOctets: u32,
    pub InMulticastOctets: u32,
    pub InBroadcastOctets: u32,
    pub OutOctets: u32,
    pub OutUcastPkts: u32,
    pub OutNUcastPkts: u32,
    pub OutDiscards: u32,
    pub OutErrors: u32,
    pub OutUcastOctets: u32,
    pub OutMulticastOctets: u32,
    pub OutBroadcastOctets: u32,
    pub OutQLen: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IF_TABLE2 {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPINTERFACE_ROW {
    pub Family: u32,
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub MaxReassemblySize: u32,
    pub InterfaceIdentifier: u32,
    pub MinRouterAdvertisementInterval: u32,
    pub MaxRouterAdvertisementInterval: u32,
    pub AdvertisingEnabled: u8,
    pub ForwardingEnabled: u8,
    pub WeakHostSend: u8,
    pub WeakHostReceive: u8,
    pub UseAutomaticMetric: u8,
    pub UseNeighborUnreachabilityDetection: u8,
    pub ManagedAddressConfigurationSupported: u8,
    pub OtherStatefulConfigurationSupported: u8,
    pub AdvertiseDefaultRoute: u8,
    pub RouterDiscoveryBehavior: u32,
    pub DadTransmits: u32,
    pub BaseReachableTime: u32,
    pub RetransmitTime: u32,
    pub PathMtuDiscoveryTimeout: u32,
    pub LinkLocalAddressBehavior: u32,
    pub LinkLocalAddressTimeout: u32,
    pub ZoneIndices: [u32; 1],
    pub SitePrefixLength: u32,
    pub Metric: u32,
    pub NlMtu: u32,
    pub Connected: u8,
    pub SupportsWakeUpPatterns: u8,
    pub SupportsNeighborDiscovery: u8,
    pub SupportsRouterDiscovery: u8,
    pub ReachableTime: u32,
    pub TransmitOffload: u32,
    pub ReceiveOffload: u32,
    pub DisableDefaultRoutes: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPINTERFACE_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFSTACK_ROW {
    pub HigherLayerInterfaceIndex: u32,
    pub LowerLayerInterfaceIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_INVERTEDIFSTACK_ROW {
    pub LowerLayerInterfaceIndex: u32,
    pub HigherLayerInterfaceIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IFSTACK_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_INVERTEDIFSTACK_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IP_NETWORK_CONNECTION_BANDWIDTH_ESTIMATES {
    pub InboundBandwidthInformation: u32,
    pub OutboundBandwidthInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UNICASTIPADDRESS_ROW {
    pub Address: u32,
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub PrefixOrigin: u32,
    pub SuffixOrigin: u32,
    pub ValidLifetime: u32,
    pub PreferredLifetime: u32,
    pub OnLinkPrefixLength: u32,
    pub SkipAsSource: u8,
    pub DadState: u32,
    pub ScopeId: u32,
    pub CreationTimeStamp: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UNICASTIPADDRESS_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_ANYCASTIPADDRESS_ROW {
    pub Address: u32,
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub ScopeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_ANYCASTIPADDRESS_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_MULTICASTIPADDRESS_ROW {
    pub Address: u32,
    pub InterfaceIndex: u32,
    pub InterfaceLuid: u32,
    pub ScopeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_MULTICASTIPADDRESS_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IP_ADDRESS_PREFIX {
    pub Prefix: u32,
    pub PrefixLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPFORWARD_ROW2 {
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub DestinationPrefix: u32,
    pub NextHop: u32,
    pub SitePrefixLength: u8,
    pub ValidLifetime: u32,
    pub PreferredLifetime: u32,
    pub Metric: u32,
    pub Protocol: u32,
    pub Loopback: u8,
    pub AutoconfigureAddress: u8,
    pub Publish: u8,
    pub Immortal: u8,
    pub Age: u32,
    pub Origin: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPFORWARD_TABLE2 {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPPATH_ROW {
    pub Source: u32,
    pub Destination: u32,
    pub InterfaceLuid: u32,
    pub InterfaceIndex: u32,
    pub CurrentNextHop: u32,
    pub PathMtu: u32,
    pub RttMean: u32,
    pub RttDeviation: u32,
    pub LastReachable: u32,
    pub LastUnreachable: u32,
    pub IsReachable: u8,
    pub LinkTransmitSpeed: u32,
    pub LinkReceiveSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPPATH_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPNET_ROW2 {
    pub Address: u32,
    pub InterfaceIndex: u32,
    pub InterfaceLuid: u32,
    pub PhysicalAddress: [u8; 1],
    pub PhysicalAddressLength: u32,
    pub State: u32,
    pub IsRouter: u8,
    pub IsUnreachable: u8,
    pub Flags: u8,
    pub LastReachable: u32,
    pub LastUnreachable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_IPNET_TABLE2 {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SETTINGS {
    pub Version: u32,
    pub Flags: u32,
    pub Hostname: usize,
    pub Domain: usize,
    pub SearchList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SETTINGS2 {
    pub Version: u32,
    pub Flags: u32,
    pub Hostname: usize,
    pub Domain: usize,
    pub SearchList: usize,
    pub SettingFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_DOH_SERVER_SETTINGS {
    pub Template: u32,
    pub Template_1: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_DOT_SERVER_SETTINGS {
    pub Hostname: u32,
    pub Hostname_1: usize,
    pub Flags: u32,
    pub Port: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SERVER_PROPERTY {
    pub Version: u32,
    pub ServerIndex: u32,
    pub Type: u32,
    pub Property: u32,
    pub Property_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_INTERFACE_SETTINGS {
    pub Version: u32,
    pub Flags: u32,
    pub Domain: usize,
    pub NameServer: usize,
    pub SearchList: usize,
    pub RegistrationEnabled: u32,
    pub RegisterAdapterName: u32,
    pub EnableLLMNR: u32,
    pub QueryAdapterName: u32,
    pub ProfileNameServer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_INTERFACE_SETTINGS_EX {
    pub SettingsV1: u32,
    pub DisableUnconstrainedQueries: u32,
    pub SupplementalSearchList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_INTERFACE_SETTINGS3 {
    pub Version: u32,
    pub Flags: u32,
    pub Domain: usize,
    pub NameServer: usize,
    pub SearchList: usize,
    pub RegistrationEnabled: u32,
    pub RegisterAdapterName: u32,
    pub EnableLLMNR: u32,
    pub QueryAdapterName: u32,
    pub ProfileNameServer: usize,
    pub DisableUnconstrainedQueries: u32,
    pub SupplementalSearchList: usize,
    pub cServerProperties: u32,
    pub field_ServerProperties: u32,
    pub cProfileServerProperties: u32,
    pub field_ProfileServerProperties: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_INTERFACE_SETTINGS4 {
    pub Version: u32,
    pub Flags: u32,
    pub Domain: usize,
    pub NameServer: usize,
    pub SearchList: usize,
    pub RegistrationEnabled: u32,
    pub RegisterAdapterName: u32,
    pub EnableLLMNR: u32,
    pub QueryAdapterName: u32,
    pub ProfileNameServer: usize,
    pub DisableUnconstrainedQueries: u32,
    pub SupplementalSearchList: usize,
    pub cServerProperties: u32,
    pub field_ServerProperties: u32,
    pub cProfileServerProperties: u32,
    pub field_ProfileServerProperties: u32,
    pub EncryptedDnsAdapterFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_FL_VIRTUAL_INTERFACE_ROW {
    pub Family: u32,
    pub IfLuid: u32,
    pub VirtualIfId: u32,
    pub CompartmentGuid: u32,
    pub IsolationMode: u32,
    pub Origin: u32,
    pub VirtualIfLuid: u32,
    pub VirtualIfIndex: u32,
    pub AllowLocalNd: u8,
    pub AttachedFlsnpiClients: u32,
    pub FlsnpiClientConfigErrors: u32,
    pub FlsnpiClientInjectErrors: u32,
    pub FlsnpiClientCloneErrors: u32,
    pub InFlsnpiIndicatedPackets: u32,
    pub InFlsnpiClientReturnedPackets: u32,
    pub InFlsnpiClientSilentlyDroppedPackets: u32,
    pub InFlsnpiClientDroppedPackets: u32,
    pub InFlsnpiClientInjectedPackets: u32,
    pub InFlsnpiClientClonedPackets: u32,
    pub OutFlsnpiIndicatedPackets: u32,
    pub OutFlsnpiClientReturnedPackets: u32,
    pub OutFlsnpiClientDroppedPackets: u32,
    pub OutFlsnpiClientSilentlyDroppedPackets: u32,
    pub OutFlsnpiClientInjectedPackets: u32,
    pub OutFlsnpiClientClonedPackets: u32,
    pub OutFlsnpiClientClonedPacketsForNbSplit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_FL_VIRTUAL_INTERFACE_TABLE {
    pub NumEntries: u32,
    pub Table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SNAP_HEADER {
    pub Dsap: u32,
    pub Ssap: u32,
    pub Control: u32,
    pub Oui: [u32; 3],
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ETHERNET_HEADER {
    pub Destination: u32,
    pub Source: u32,
    pub Type: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VLAN_TAG {
    pub Tag: u32,
    pub VID: u32,
    pub CFI: u32,
    pub User_Priority: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMP_HEADER {
    pub Type: u32,
    pub Code: u32,
    pub Checksum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMP_MESSAGE {
    pub Header: u32,
    pub Data32: [u32; 1],
    pub Data16: [u32; 2],
    pub Data8: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV4_HEADER {
    pub VersionAndHeaderLength: u32,
    pub HeaderLength: u32,
    pub Version: u32,
    pub TypeOfServiceAndEcnField: u32,
    pub EcnField: u32,
    pub TypeOfService: u32,
    pub TotalLength: u32,
    pub Identification: u32,
    pub FlagsAndOffset: u32,
    pub DontUse1: u32,
    pub MoreFragments: u32,
    pub DontFragment: u32,
    pub Reserved: u32,
    pub DontUse2: u32,
    pub TimeToLive: u32,
    pub Protocol: u32,
    pub HeaderChecksum: u32,
    pub SourceAddress: u32,
    pub DestinationAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV4_OPTION_HEADER {
    pub OptionType: u32,
    pub OptionNumber: u32,
    pub OptionClass: u32,
    pub CopiedFlag: u32,
    pub OptionLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV4_TIMESTAMP_OPTION {
    pub OptionHeader: u32,
    pub Pointer: u32,
    pub FlagsOverflow: u32,
    pub Flags: u32,
    pub Overflow: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV4_ROUTING_HEADER {
    pub OptionHeader: u32,
    pub Pointer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMPV4_ROUTER_SOLICIT {
    pub RsHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMPV4_ROUTER_ADVERT_HEADER {
    pub RaHeader: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMPV4_ROUTER_ADVERT_ENTRY {
    pub RouterAdvertAddr: u32,
    pub PreferenceLevel: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMPV4_TIMESTAMP_MESSAGE {
    pub Header: u32,
    pub OriginateTimestamp: u32,
    pub ReceiveTimestamp: u32,
    pub TransmitTimestamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ICMPV4_ADDRESS_MASK_MESSAGE {
    pub Header: u32,
    pub AddressMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ARP_HEADER {
    pub HardwareAddressSpace: u16,
    pub ProtocolAddressSpace: u16,
    pub HardwareAddressLength: u8,
    pub ProtocolAddressLength: u8,
    pub Opcode: u16,
    pub SenderHardwareAddress: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IGMP_HEADER {
    pub Type: u32,
    pub Version: u32,
    pub VersionType: u32,
    pub Reserved: u32,
    pub MaxRespTime: u32,
    pub Code: u32,
    pub Checksum: u32,
    pub MulticastAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IGMPV3_QUERY_HEADER {
    pub Type: u32,
    pub MaxRespCode: u32,
    pub MaxRespCodeMantissa: u32,
    pub MaxRespCodeExponent: u32,
    pub MaxRespCodeType: u32,
    pub Checksum: u32,
    pub MulticastAddress: u32,
    pub QuerierRobustnessVariable: u32,
    pub SuppressRouterSideProcessing: u32,
    pub Reserved: u32,
    pub QueriersQueryInterfaceCode: u32,
    pub QQCMantissa: u32,
    pub QQCExponent: u32,
    pub QQCType: u32,
    pub SourceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IGMPV3_REPORT_RECORD_HEADER {
    pub Type: u32,
    pub AuxillaryDataLength: u32,
    pub SourceCount: u32,
    pub MulticastAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IGMPV3_REPORT_HEADER {
    pub Type: u32,
    pub Reserved: u32,
    pub Checksum: u32,
    pub Reserved2: u32,
    pub RecordCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_HEADER {
    pub VersionClassFlow: u32,
    pub Version: u32,
    pub PayloadLength: u32,
    pub NextHeader: u32,
    pub HopLimit: u32,
    pub SourceAddress: u32,
    pub DestinationAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_FRAGMENT_HEADER {
    pub NextHeader: u32,
    pub Reserved: u32,
    pub DontUse1: u32,
    pub MoreFragments: u32,
    pub ReservedBits: u32,
    pub DontUse2: u32,
    pub OffsetAndFlags: u32,
    pub Id: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_EXTENSION_HEADER {
    pub NextHeader: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_OPTION_HEADER {
    pub Type: u32,
    pub DataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_OPTION_JUMBOGRAM {
    pub Header: u32,
    pub JumbogramLength: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_OPTION_ROUTER_ALERT {
    pub Header: u32,
    pub Value: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPV6_ROUTING_HEADER {
    pub NextHeader: u8,
    pub Length: u8,
    pub RoutingType: u8,
    pub SegmentsLeft: u8,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MLD_HEADER {
    pub IcmpHeader: u32,
    pub MaxRespTime: u32,
    pub Reserved: u32,
    pub MulticastAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MLDV2_QUERY_HEADER {
    pub IcmpHeader: u32,
    pub MaxRespCode: u32,
    pub MaxRespCodeMantissaHi: u32,
    pub MaxRespCodeExponent: u32,
    pub MaxRespCodeType: u32,
    pub MaxRespCodeMantissaLo: u32,
    pub Reserved: u32,
    pub MulticastAddress: u32,
    pub QuerierRobustnessVariable: u32,
    pub SuppressRouterSideProcessing: u32,
    pub QueryReserved: u32,
    pub QueriersQueryInterfaceCode: u32,
    pub QQCMantissa: u32,
    pub QQCExponent: u32,
    pub QQCType: u32,
    pub SourceCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MLDV2_REPORT_RECORD_HEADER {
    pub Type: u32,
    pub AuxillaryDataLength: u32,
    pub SourceCount: u32,
    pub MulticastAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MLDV2_REPORT_HEADER {
    pub IcmpHeader: u32,
    pub Reserved: u32,
    pub RecordCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPTLS_METADATA {
    pub SequenceNumber: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NPI_MODULEID {
    pub Length: u16,
    pub Type: u32,
    pub Guid: u32,
    pub IfLuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLAT_STRING {
    pub MaximumLength: i16,
    pub Length: i16,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_NAME {
    pub Name: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HARDWARE_ADDRESS {
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_RF_DISCOVERY_CONFIG {
    pub usTotalDuration: u16,
    pub ulPollConfig: u32,
    pub fDisableCardEmulation: u8,
    pub ucNfcIPMode: u8,
    pub fNfcIPTgtModeDisable: u8,
    pub ucNfcIPTgtMode: u8,
    pub ucNfcCEMode: u8,
    pub ucBailoutConfig: u8,
    pub ucSystemCode: [u8; 2],
    pub ucRequestCode: u8,
    pub ucTimeSlotNumber: u8,
    pub eRfDiscoveryMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_P2P_PARAM_CONFIG {
    pub eP2pMode: u32,
    pub cbGeneralBytes: u32,
    pub pbGeneralBytes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_REMOTE_DEV_INFO {
    pub hRemoteDev: u32,
    pub eType: u32,
    pub eRFTech: u32,
    pub eProtocol: u32,
    pub cbUid: u32,
    pub pbUid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_REMOTE_DEVICE_DISCONNET {
    pub hRemoteDev: u32,
    pub eReleaseType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_DATA_BUFFER {
    pub cbBuffer: u16,
    pub pbBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_REMOTE_DEV_SEND_INFO {
    pub hRemoteDev: u32,
    pub usTimeOut: u16,
    pub sSendBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_REMOTE_DEV_RECV_INFO {
    pub hRemoteDev: u32,
    pub sRecvBuffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_NDEF_INFO {
    pub fIsNdefFormatted: u8,
    pub fIsReadOnly: u8,
    pub dwActualMessageLength: u32,
    pub dwMaxMessageLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_OPTION {
    pub uMIUX: u16,
    pub bRW: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_CONFIG {
    pub uMIU: u16,
    pub uWKS: u16,
    pub bLTO: u8,
    pub bOptions: u8,
    pub fAutoActivate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SERVICE_NAME_ENTRY {
    pub cbServiceName: u32,
    pub pbServiceName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SERVICE_DISCOVER_REQUEST {
    pub hRemoteDev: u32,
    pub NumberOfEntries: u32,
    pub ServiceNameEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SERVICE_DISCOVER_SAP {
    pub NumberOfEntries: u32,
    pub SAPEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_INFO {
    pub eSocketType: u32,
    pub sSocketOption: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_SERVICE_INFO {
    pub hSocket: u32,
    pub bSAP: u8,
    pub sServiceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_PAYLOAD {
    pub hSocket: u32,
    pub bSAP: u8,
    pub sPayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_ACCEPT_INFO {
    pub hSocket: u32,
    pub sSocketOption: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_CONNECT_INFO {
    pub hRemoteDev: u32,
    pub hSocket: u32,
    pub eConnectType: u32,
    pub bSAP: u8,
    pub sServiceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_CL_PAYLOAD {
    pub hSocket: u32,
    pub bSAP: u8,
    pub sPayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_LLCP_SOCKET_ERROR_INFO {
    pub hSocket: u32,
    pub eSocketError: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_SERVER_INFO {
    pub eServerType: u32,
    pub sSocketOption: u32,
    pub usInboxSize: u16,
    pub bSAP: u8,
    pub sService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_SERVER_ACCEPT_INFO {
    pub hSnepServer: u32,
    pub hConnection: u32,
    pub sSocketOption: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_SERVER_REQUEST {
    pub hSnepServer: u32,
    pub hConnection: u32,
    pub eRequestType: u32,
    pub sRequestPayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_SERVER_RESPONSE_INFO {
    pub hSnepServer: u32,
    pub hConnection: u32,
    pub dwResponseStatus: u32,
    pub sResponsePayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_CLIENT_INFO {
    pub hRemoteDev: u32,
    pub eServerType: u32,
    pub sSocketOption: u32,
    pub sService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_CLIENT_PUT_INFO {
    pub hSnepClient: u32,
    pub sPutPayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SNEP_CLIENT_GET_INFO {
    pub hSnepClient: u32,
    pub sGetPayload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_INFO {
    pub hSecureElement: u32,
    pub eSecureElementType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_LIST {
    pub NumberOfEndpoints: u32,
    pub EndpointList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_EMULATION_MODE_INFO {
    pub hSecureElement: u32,
    pub eMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_TECH_ROUTING_INFO {
    pub hSecureElement: u32,
    pub bPowerState: u8,
    pub eRfTechType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_PROTO_ROUTING_INFO {
    pub hSecureElement: u32,
    pub bPowerState: u8,
    pub eRfProtocolType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_AID_ROUTING_INFO {
    pub hSecureElement: u32,
    pub bPowerState: u8,
    pub cbAid: u32,
    pub pbAid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_ROUTING_TABLE_ENTRY {
    pub eRoutingType: u32,
    pub TechRoutingInfo: u32,
    pub ProtoRoutingInfo: u32,
    pub AidRoutingInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_ROUTING_TABLE {
    pub NumberOfEntries: u32,
    pub TableEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFC_SE_EVENT_INFO {
    pub hSecureElement: u32,
    pub eEventType: u32,
    pub cbEventData: u32,
    pub pbEventData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFCRM_SET_RADIO_STATE {
    pub SystemStateUpdate: u8,
    pub MediaRadioOn: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NFCRM_RADIO_STATE {
    pub MediaRadioOn: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_ENDPOINT_INFO {
    pub guidSecureElementId: u32,
    pub eSecureElementType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_ENDPOINT_LIST {
    pub NumberOfEndpoints: u32,
    pub EndpointList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_EVENT_SUBSCRIPTION_INFO {
    pub guidSecureElementId: u32,
    pub eEventType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_EVENT_INFO {
    pub guidSecureElementId: u32,
    pub eEventType: u32,
    pub cbEventData: u32,
    pub pbEventData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_SET_CARD_EMULATION_MODE_INFO {
    pub guidSecureElementId: u32,
    pub eMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_NFCC_CAPABILITIES {
    pub cbMaxRoutingTableSize: u16,
    pub IsAidRoutingSupported: u8,
    pub IsProtocolRoutingSupported: u8,
    pub IsTechRoutingSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_TECH_ROUTING_INFO {
    pub guidSecureElementId: u32,
    pub eRfTechType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_PROTO_ROUTING_INFO {
    pub guidSecureElementId: u32,
    pub eRfProtocolType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_AID_ROUTING_INFO {
    pub guidSecureElementId: u32,
    pub cbAid: u32,
    pub pbAid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_ROUTING_TABLE_ENTRY {
    pub eRoutingType: u32,
    pub TechRoutingInfo: u32,
    pub ProtoRoutingInfo: u32,
    pub AidRoutingInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_ROUTING_TABLE {
    pub NumberOfEntries: u32,
    pub TableEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_HCE_ACTIVATION_PAYLOAD {
    pub bConnectionId: u16,
    pub eRfTechType: u8,
    pub eRfProtocolType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_HCE_DATA_PACKET {
    pub bConnectionId: u16,
    pub cbPayload: u16,
    pub pbPayload: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURE_ELEMENT_SET_POWER_MODE_INFO {
    pub guidSecureElementId: u32,
    pub powerMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUBSCRIBED_MESSAGE {
    pub cbPayloadHint: u32,
    pub payload: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NL_INTERFACE_OFFLOAD_ROD {
    pub NlChecksumSupported: u8,
    pub NlOptionsSupported: u8,
    pub TlDatagramChecksumSupported: u8,
    pub TlStreamChecksumSupported: u8,
    pub TlStreamOptionsSupported: u8,
    pub FastPathCompatible: u8,
    pub TlLargeSendOffloadSupported: u8,
    pub TlGiantSendOffloadSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NL_PATH_BANDWIDTH_ROD {
    pub Bandwidth: u32,
    pub Instability: u32,
    pub BandwidthPeaked: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NL_NETWORK_CONNECTIVITY_HINT {
    pub ConnectivityLevel: u32,
    pub ConnectivityCost: u32,
    pub ApproachingDataLimit: u8,
    pub OverDataLimit: u8,
    pub Roaming: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NL_BANDWIDTH_INFORMATION {
    pub Bandwidth: u32,
    pub Instability: u32,
    pub BandwidthPeaked: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IEEE1394_VDEV_PNP_REQUEST {
    pub fulFlags: u32,
    pub Reserved: u32,
    pub InstanceId: u64,
    pub DeviceId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IEEE1394_API_REQUEST {
    pub RequestNumber: u32,
    pub Flags: u32,
    pub AddVirtualDevice: u32,
    pub RemoveVirtualDevice: u32,
    pub fulFlags: u32,
    pub fulDesiredAccess: u32,
    pub DeviceEUI: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IEEE1394_NOTIFICATION_DATA {
    pub Version: u32,
    pub BusGeneration: u32,
    pub Type: u32,
    pub Flags: u32,
    pub fulFlags: u32,
    pub AccessFlags: u32,
    pub RemoteNodeEUI: u64,
    pub BusResetFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BEEP_SET_PARAMETERS {
    pub Frequency: u32,
    pub Duration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_READ_TOC_EX {
    pub Format: u8,
    pub Reserved1: u8,
    pub Msf: u8,
    pub SessionTrack: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRACK_DATA {
    pub Reserved: u8,
    pub Control: u8,
    pub Adr: u8,
    pub TrackNumber: u8,
    pub Reserved1: u8,
    pub Address: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC {
    pub Length: [u8; 2],
    pub FirstTrack: u8,
    pub LastTrack: u8,
    pub TrackData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_SESSION_DATA {
    pub Length: [u8; 2],
    pub FirstCompleteSession: u8,
    pub LastCompleteSession: u8,
    pub TrackData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_FULL_TOC_DATA_BLOCK {
    pub SessionNumber: u8,
    pub Control: u8,
    pub Adr: u8,
    pub Reserved1: u8,
    pub Point: u8,
    pub MsfExtra: [u8; 3],
    pub Zero: u8,
    pub Msf: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_FULL_TOC_DATA {
    pub Length: [u8; 2],
    pub FirstCompleteSession: u8,
    pub LastCompleteSession: u8,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_PMA_DATA {
    pub Length: [u8; 2],
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_ATIP_DATA_BLOCK {
    pub CdrwReferenceSpeed: u8,
    pub Reserved3: u8,
    pub WritePower: u8,
    pub True1: u8,
    pub Reserved4: u8,
    pub UnrestrictedUse: u8,
    pub Reserved5: u8,
    pub A3Valid: u8,
    pub A2Valid: u8,
    pub A1Valid: u8,
    pub DiscSubType: u8,
    pub IsCdrw: u8,
    pub True2: u8,
    pub Reserved7: u8,
    pub LeadInMsf: [u8; 3],
    pub Reserved8: u8,
    pub LeadOutMsf: [u8; 3],
    pub Reserved9: u8,
    pub A1Values: [u8; 3],
    pub Reserved10: u8,
    pub A2Values: [u8; 3],
    pub Reserved11: u8,
    pub A3Values: [u8; 3],
    pub Reserved12: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_ATIP_DATA {
    pub Length: [u8; 2],
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_CD_TEXT_DATA_BLOCK {
    pub PackType: u8,
    pub TrackNumber: u8,
    pub ExtensionFlag: u8,
    pub SequenceNumber: u8,
    pub CharacterPosition: u8,
    pub BlockNumber: u8,
    pub Unicode: u8,
    pub Text: [u8; 12],
    pub WText: [u32; 6],
    pub CRC: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_TOC_CD_TEXT_DATA {
    pub Length: [u8; 2],
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Descriptors: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_PLAY_AUDIO_MSF {
    pub StartingM: u8,
    pub StartingS: u8,
    pub StartingF: u8,
    pub EndingM: u8,
    pub EndingS: u8,
    pub EndingF: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_SEEK_AUDIO_MSF {
    pub M: u8,
    pub S: u8,
    pub F: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_DISK_DATA {
    pub DiskData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_SUB_Q_DATA_FORMAT {
    pub Format: u8,
    pub Track: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUB_Q_HEADER {
    pub Reserved: u8,
    pub AudioStatus: u8,
    pub DataLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUB_Q_CURRENT_POSITION {
    pub Header: u32,
    pub FormatCode: u8,
    pub Control: u8,
    pub ADR: u8,
    pub TrackNumber: u8,
    pub IndexNumber: u8,
    pub AbsoluteAddress: [u8; 4],
    pub TrackRelativeAddress: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUB_Q_MEDIA_CATALOG_NUMBER {
    pub Header: u32,
    pub FormatCode: u8,
    pub Reserved: [u8; 3],
    pub Reserved1: u8,
    pub Mcval: u8,
    pub MediaCatalog: [u8; 15],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SUB_Q_TRACK_ISRC {
    pub Header: u32,
    pub FormatCode: u8,
    pub Reserved0: u8,
    pub Track: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub Tcval: u8,
    pub TrackIsrc: [u8; 15],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_AUDIO_CONTROL {
    pub LbaFormat: u8,
    pub LogicalBlocksPerSecond: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_CONTROL {
    pub PortVolume: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RAW_READ_INFO {
    pub DiskOffset: i64,
    pub SectorCount: u32,
    pub TrackMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_EXCLUSIVE_ACCESS {
    pub RequestType: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_EXCLUSIVE_LOCK {
    pub Access: u32,
    pub CallerName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_EXCLUSIVE_LOCK_STATE {
    pub LockState: u8,
    pub CallerName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_SET_SPEED {
    pub RequestType: u32,
    pub ReadSpeed: u16,
    pub WriteSpeed: u16,
    pub RotationControl: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_SET_STREAMING {
    pub RequestType: u32,
    pub ReadSize: u32,
    pub ReadTime: u32,
    pub WriteSize: u32,
    pub WriteTime: u32,
    pub StartLba: u32,
    pub EndLba: u32,
    pub RotationControl: u32,
    pub RestoreDefaults: u8,
    pub SetExact: u8,
    pub RandomAccess: u8,
    pub Persistent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_STREAMING_CONTROL {
    pub RequestType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_SIMPLE_OPC_INFO {
    pub RequestType: u32,
    pub Exclude0: u8,
    pub Exclude1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_PERFORMANCE_REQUEST {
    pub RequestType: u32,
    pub PerformanceType: u32,
    pub Exceptions: u32,
    pub Tolerance: u32,
    pub StaringLba: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_WRITE_SPEED_REQUEST {
    pub RequestType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_PERFORMANCE_HEADER {
    pub DataLength: [u8; 4],
    pub Except: u8,
    pub Write: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_NOMINAL_PERFORMANCE_DESCRIPTOR {
    pub StartLba: [u8; 4],
    pub StartPerformance: [u8; 4],
    pub EndLba: [u8; 4],
    pub EndPerformance: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_EXCEPTION_PERFORMANCE_DESCRIPTOR {
    pub Lba: [u8; 4],
    pub Time: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CDROM_WRITE_SPEED_DESCRIPTOR {
    pub MixedReadWrite: u8,
    pub Exact: u8,
    pub Reserved1: u8,
    pub WriteRotationControl: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 3],
    pub EndLba: [u8; 4],
    pub ReadSpeed: [u8; 4],
    pub WriteSpeed: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_COPY_PROTECT_KEY {
    pub KeyLength: u32,
    pub SessionId: u32,
    pub KeyType: u32,
    pub KeyFlags: u32,
    pub FileHandle: usize,
    pub TitleOffset: i64,
    pub KeyData: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_SET_READ_AHEAD {
    pub TriggerAddress: i64,
    pub TargetAddress: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DESCRIPTOR_HEADER {
    pub Length: u16,
    pub Reserved: [u8; 2],
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_LAYER_DESCRIPTOR {
    pub BookVersion: u8,
    pub BookType: u8,
    pub MinimumRate: u8,
    pub DiskSize: u8,
    pub LayerType: u8,
    pub TrackPath: u8,
    pub NumberOfLayers: u8,
    pub Reserved1: u8,
    pub TrackDensity: u8,
    pub LinearDensity: u8,
    pub StartingDataSector: u32,
    pub EndDataSector: u32,
    pub EndLayerZeroSector: u32,
    pub Reserved5: u8,
    pub BCAFlag: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_FULL_LAYER_DESCRIPTOR {
    pub commonHeader: u32,
    pub MediaSpecific: [u8; 2031],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_COPYRIGHT_DESCRIPTOR {
    pub CopyrightProtectionType: u8,
    pub RegionManagementInformation: u8,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISK_KEY_DESCRIPTOR {
    pub DiskKeyData: [u8; 2048],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_BCA_DESCRIPTOR {
    pub BCAInformation: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_MANUFACTURER_DESCRIPTOR {
    pub ManufacturingInformation: [u8; 2048],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_COPYRIGHT_MANAGEMENT_DESCRIPTOR {
    pub CopyProtectionMode: u8,
    pub ContentGenerationManagementSystem: u8,
    pub CopyProtectedSector: u8,
    pub CopyProtectedMaterial: u8,
    pub Reserved0001: u8,
    pub ContentGenerationManagementSystem_1: u8,
    pub Reserved0002: u8,
    pub CopyProtectedMaterial_1: u8,
    pub Reserved0003: u8,
    pub Reserved0004: u8,
    pub ADP_TY: u8,
    pub Reserved0005: u8,
    pub CPR_MAI: u8,
    pub Reserved0: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_RAM_MEDIUM_STATUS {
    pub Reserved0: u8,
    pub PersistentWriteProtect: u8,
    pub CartridgeWriteProtect: u8,
    pub MediaSpecificWriteInhibit: u8,
    pub Reserved1: u8,
    pub CartridgeNotSealed: u8,
    pub MediaInCartridge: u8,
    pub DiscTypeIdentification: u8,
    pub Reserved2: u8,
    pub MediaSpecificWriteInhibitInformation: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_RAM_SPARE_AREA_INFORMATION {
    pub FreePrimarySpareSectors: [u8; 4],
    pub FreeSupplementalSpareSectors: [u8; 4],
    pub AllocatedSupplementalSpareSectors: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_RAM_RECORDING_TYPE {
    pub Reserved0: u8,
    pub RealTimeData: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_RECORDING_MANAGEMENT_AREA_DATA {
    pub LastRecordedRMASectorNumber: [u8; 4],
    pub RMDBytes: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_PRERECORDED_INFORMATION {
    pub FieldID_1: u8,
    pub DiscApplicationCode: u8,
    pub DiscPhysicalCode: u8,
    pub LastAddressOfDataRecordableArea: [u8; 3],
    pub ExtensionCode: u8,
    pub PartVers1on: u8,
    pub Reserved0: u8,
    pub FieldID_2: u8,
    pub OpcSuggestedCode: u8,
    pub WavelengthCode: u8,
    pub WriteStrategyCode: [u8; 4],
    pub Reserved2: u8,
    pub FieldID_3: u8,
    pub ManufacturerId_3: [u8; 6],
    pub Reserved3: u8,
    pub FieldID_4: u8,
    pub ManufacturerId_4: [u8; 6],
    pub Reserved4: u8,
    pub FieldID_5: u8,
    pub ManufacturerId_5: [u8; 6],
    pub Reserved5: u8,
    pub Reserved99: [u8; 24],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_UNIQUE_DISC_IDENTIFIER {
    pub Reserved0: [u8; 2],
    pub RandomNumber: [u8; 2],
    pub Year: [u8; 4],
    pub Month: [u8; 2],
    pub Day: [u8; 2],
    pub Hour: [u8; 2],
    pub Minute: [u8; 2],
    pub Second: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HD_DVD_R_MEDIUM_STATUS {
    pub ExtendedTestZone: u8,
    pub Reserved1: u8,
    pub NumberOfRemainingRMDsInRDZ: u8,
    pub NumberOfRemainingRMDsInCurrentRMZ: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DUAL_LAYER_RECORDING_INFORMATION {
    pub Reserved0: u8,
    pub Layer0SectorsImmutable: u8,
    pub Reserved1: [u8; 3],
    pub Layer0Sectors: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DUAL_LAYER_MIDDLE_ZONE_START_ADDRESS {
    pub Reserved0: u8,
    pub InitStatus: u8,
    pub Reserved1: [u8; 3],
    pub ShiftedMiddleAreaStartAddress: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DUAL_LAYER_JUMP_INTERVAL_SIZE {
    pub Reserved1: [u8; 4],
    pub JumpIntervalSize: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DUAL_LAYER_MANUAL_LAYER_JUMP {
    pub Reserved1: [u8; 4],
    pub ManualJumpLayerAddress: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DUAL_LAYER_REMAPPING_INFORMATION {
    pub Reserved1: [u8; 4],
    pub RemappingAddress: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_HEADER {
    pub ContentDescriptor: [u8; 4],
    pub ReservedDoNotUse_UseAsByteInstead_0: [u8; 3],
    pub RecordingWithinTheUserDataArea: u8,
    pub ReadingDiscControlBlocks: u8,
    pub FormattingTheMedium: u8,
    pub ModificationOfThisDiscControlBlock: u8,
    pub ReservedDoNotUse_UseAsByteInstead_1: u8,
    pub AsByte: [u8; 4],
    pub VendorId: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_WRITE_INHIBIT {
    pub header: u32,
    pub UpdateCount: [u8; 4],
    pub ReservedDoNotUse_UseAsByteInstead_0: [u8; 3],
    pub WriteProtectStatus: u8,
    pub ReservedDoNotUse_UseAsByteInstead_1: u8,
    pub UpdateRequiresPassword: u8,
    pub AsByte: [u8; 4],
    pub Reserved0: [u8; 16],
    pub UpdatePassword: [u8; 32],
    pub Reserved1: [u8; 32672],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_SESSION_ITEM {
    pub AsByte: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_SESSION {
    pub header: u32,
    pub SessionNumber: [u8; 2],
    pub Reserved0: [u8; 22],
    pub DiscID: [u8; 32],
    pub Reserved1: [u8; 32],
    pub SessionItem: [u32; 504],
    pub Reserved2: [u8; 24576],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_LIST_DCB {
    pub DcbIdentifier: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_DISC_CONTROL_BLOCK_LIST {
    pub header: u32,
    pub Reserved0: u8,
    pub ReadabldDCBs: u8,
    pub Reserved1: u8,
    pub WritableDCBs: u8,
    pub Dcbs: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_WRITE_PROTECTION_STATUS {
    pub SoftwareWriteProtectUntilPowerdown: u8,
    pub MediaPersistentWriteProtect: u8,
    pub CartridgeWriteProtect: u8,
    pub MediaSpecificWriteProtect: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_LIST_OF_RECOGNIZED_FORMAT_LAYERS {
    pub TypeCodeOfFormatLayer: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_LIST_OF_RECOGNIZED_FORMAT_LAYERS_TYPE_CODE {
    pub NumberOfRecognizedFormatLayers: u8,
    pub OnlineFormatlayer: u8,
    pub Reserved1: u8,
    pub DefaultFormatLayer: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_STRUCTURE_LIST_ENTRY {
    pub FormatCode: u8,
    pub Reserved0: u8,
    pub Readable: u8,
    pub Sendable: u8,
    pub FormatLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_BD_SPARE_AREA_INFORMATION {
    pub Reserved1: [u8; 4],
    pub NumberOfFreeSpareBlocks: [u8; 4],
    pub NumberOfAllocatedSpareBlocks: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BD_PAC_HEADER {
    pub PACId: [u8; 3],
    pub PACFormatNumber: u8,
    pub PACUpdateCount: [u8; 4],
    pub UnknownPACRules: [u8; 4],
    pub UnkownPACEntireDiscFlag: u8,
    pub Reserved1: [u8; 2],
    pub NumberOfSegments: u8,
    pub Segments: [u8; 8],
    pub Reserved2: [u8; 112],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BD_DISC_WRITE_PROTECT_PAC {
    pub Header: u32,
    pub KnownPACEntireDiscFlags: u8,
    pub Reserved1: [u8; 3],
    pub WriteProtectControlByte: u8,
    pub Reserved2: [u8; 7],
    pub WriteProtectPassword: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_RPC_KEY {
    pub UserResetsAvailable: u8,
    pub ManufacturerResetsAvailable: u8,
    pub TypeCode: u8,
    pub RegionMask: u8,
    pub RpcScheme: u8,
    pub Reserved02: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_SET_RPC_KEY {
    pub PreferredDriveRegionCode: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_ASF {
    pub Reserved0: [u8; 3],
    pub SuccessFlag: u8,
    pub Reserved1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DVD_REGION {
    pub CopySystem: u8,
    pub RegionData: u8,
    pub SystemRegion: u8,
    pub ResetCount: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_CERTIFICATE {
    pub Nonce: [u8; 20],
    pub Certificate: [u8; 92],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_CHALLENGE_KEY {
    pub EllipticCurvePoint: [u8; 40],
    pub Signature: [u8; 40],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_VOLUME_ID {
    pub VolumeID: [u8; 16],
    pub MAC: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_SERIAL_NUMBER {
    pub PrerecordedSerialNumber: [u8; 16],
    pub MAC: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_MEDIA_ID {
    pub MediaID: [u8; 16],
    pub MAC: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_SEND_CERTIFICATE {
    pub SessionId: u32,
    pub Certificate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_SEND_CHALLENGE_KEY {
    pub SessionId: u32,
    pub ChallengeKey: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_BINDING_NONCE {
    pub BindingNonce: [u8; 16],
    pub MAC: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AACS_READ_BINDING_NONCE {
    pub SessionId: u32,
    pub NumberOfSectors: u32,
    pub StartLba: u64,
    pub Handle: usize,
    pub ForceStructureLengthToMatch64bit: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_ELEMENT {
    pub ElementType: u32,
    pub ElementAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_ELEMENT_LIST {
    pub Element: u32,
    pub NumberOfElements: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_CHANGER_PARAMETERS {
    pub Size: u32,
    pub NumberTransportElements: u16,
    pub NumberStorageElements: u16,
    pub NumberCleanerSlots: u16,
    pub NumberIEElements: u16,
    pub NumberDataTransferElements: u16,
    pub NumberOfDoors: u16,
    pub FirstSlotNumber: u16,
    pub FirstDriveNumber: u16,
    pub FirstTransportNumber: u16,
    pub FirstIEPortNumber: u16,
    pub FirstCleanerSlotAddress: u16,
    pub MagazineSize: u16,
    pub DriveCleanTimeout: u32,
    pub Features0: u32,
    pub Features1: u32,
    pub MoveFromTransport: u8,
    pub MoveFromSlot: u8,
    pub MoveFromIePort: u8,
    pub MoveFromDrive: u8,
    pub ExchangeFromTransport: u8,
    pub ExchangeFromSlot: u8,
    pub ExchangeFromIePort: u8,
    pub ExchangeFromDrive: u8,
    pub LockUnlockCapabilities: u8,
    pub PositionCapabilities: u8,
    pub Reserved1: [u8; 2],
    pub Reserved2: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_PRODUCT_DATA {
    pub VendorId: [u8; 1],
    pub ProductId: [u8; 1],
    pub Revision: [u8; 1],
    pub SerialNumber: [u8; 1],
    pub DeviceType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_SET_ACCESS {
    pub Element: u32,
    pub Control: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_READ_ELEMENT_STATUS {
    pub ElementList: u32,
    pub VolumeTagInfo: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_ELEMENT_STATUS {
    pub Element: u32,
    pub SrcElementAddress: u32,
    pub Flags: u32,
    pub ExceptionCode: u32,
    pub TargetId: u8,
    pub Lun: u8,
    pub Reserved: u16,
    pub PrimaryVolumeID: [u8; 1],
    pub AlternateVolumeID: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_ELEMENT_STATUS_EX {
    pub Element: u32,
    pub SrcElementAddress: u32,
    pub Flags: u32,
    pub ExceptionCode: u32,
    pub TargetId: u8,
    pub Lun: u8,
    pub Reserved: u16,
    pub PrimaryVolumeID: [u8; 1],
    pub AlternateVolumeID: [u8; 1],
    pub VendorIdentification: [u8; 1],
    pub ProductIdentification: [u8; 1],
    pub SerialNumber: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_INITIALIZE_ELEMENT_STATUS {
    pub ElementList: u32,
    pub BarCodeScan: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_SET_POSITION {
    pub Transport: u32,
    pub Destination: u32,
    pub Flip: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_EXCHANGE_MEDIUM {
    pub Transport: u32,
    pub Source: u32,
    pub Destination1: u32,
    pub Destination2: u32,
    pub Flip1: u8,
    pub Flip2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_MOVE_MEDIUM {
    pub Transport: u32,
    pub Source: u32,
    pub Destination: u32,
    pub Flip: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANGER_SEND_VOLUME_TAG_INFORMATION {
    pub StartingElement: u32,
    pub ActionCode: u32,
    pub VolumeIDTemplate: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct READ_ELEMENT_ADDRESS_INFO {
    pub NumberOfElements: u32,
    pub ElementStatus: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_CACHE_SETTING {
    pub Version: u32,
    pub State: u32,
    pub IsPowerProtected: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_COPY_DATA_PARAMETERS {
    pub SourceOffset: i64,
    pub DestinationOffset: i64,
    pub CopyLength: i64,
    pub Reserved: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMAT_PARAMETERS {
    pub MediaType: u32,
    pub StartCylinderNumber: u32,
    pub EndCylinderNumber: u32,
    pub StartHeadNumber: u32,
    pub EndHeadNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FORMAT_EX_PARAMETERS {
    pub MediaType: u32,
    pub StartCylinderNumber: u32,
    pub EndCylinderNumber: u32,
    pub StartHeadNumber: u32,
    pub EndHeadNumber: u32,
    pub FormatGapLength: u16,
    pub SectorsPerTrack: u16,
    pub SectorNumber: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_GEOMETRY {
    pub Cylinders: i64,
    pub MediaType: u32,
    pub TracksPerCylinder: u32,
    pub SectorsPerTrack: u32,
    pub BytesPerSector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARTITION_INFORMATION {
    pub StartingOffset: i64,
    pub PartitionLength: i64,
    pub HiddenSectors: u32,
    pub PartitionNumber: u32,
    pub PartitionType: u8,
    pub BootIndicator: u8,
    pub RecognizedPartition: u8,
    pub RewritePartition: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_PARTITION_INFORMATION {
    pub PartitionType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVE_LAYOUT_INFORMATION {
    pub PartitionCount: u32,
    pub Signature: u32,
    pub PartitionEntry: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VERIFY_INFORMATION {
    pub StartingOffset: i64,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REASSIGN_BLOCKS {
    pub Reserved: u16,
    pub Count: u16,
    pub BlockNumber: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REASSIGN_BLOCKS_EX {
    pub Reserved: u16,
    pub Count: u16,
    pub BlockNumber: [i64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARTITION_INFORMATION_GPT {
    pub PartitionType: u32,
    pub PartitionId: u32,
    pub Attributes: u32,
    pub reserved_padding: [u32; 36],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARTITION_INFORMATION_MBR {
    pub PartitionType: u8,
    pub BootIndicator: u8,
    pub RecognizedPartition: u8,
    pub HiddenSectors: u32,
    pub PartitionId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_PARTITION_INFORMATION_EX {
    pub PartitionStyle: usize,
    pub Mbr: u32,
    pub Gpt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_DISK_GPT {
    pub DiskId: u32,
    pub MaxPartitionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_DISK_MBR {
    pub Signature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREATE_DISK {
    pub PartitionStyle: usize,
    pub Mbr: u32,
    pub Gpt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_LENGTH_INFORMATION {
    pub Length: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARTITION_INFORMATION_EX {
    pub PartitionStyle: usize,
    pub StartingOffset: i64,
    pub PartitionLength: i64,
    pub PartitionNumber: u32,
    pub RewritePartition: u8,
    pub IsServicePartition: u8,
    pub Mbr: usize,
    pub Gpt: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVE_LAYOUT_INFORMATION_GPT {
    pub DiskId: u32,
    pub StartingUsableOffset: i64,
    pub UsableLength: i64,
    pub MaxPartitionCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVE_LAYOUT_INFORMATION_MBR {
    pub Signature: u32,
    pub CheckSum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVE_LAYOUT_INFORMATION_EX {
    pub PartitionStyle: u32,
    pub PartitionCount: u32,
    pub Mbr: u32,
    pub Gpt: u32,
    pub PartitionEntry: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_INT13_INFO {
    pub DriveSelect: u16,
    pub MaxCylinders: u32,
    pub SectorsPerTrack: u16,
    pub MaxHeads: u16,
    pub NumberDrives: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_EX_INT13_INFO {
    pub ExBufferSize: u16,
    pub ExFlags: u16,
    pub ExCylinders: u32,
    pub ExHeads: u32,
    pub ExSectorsPerTrack: u32,
    pub ExSectorsPerDrive: u32,
    pub ExSectorSize: u16,
    pub ExReserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_DETECTION_INFO {
    pub SizeOfDetectInfo: u32,
    pub DetectionType: u32,
    pub Int13: u32,
    pub ExInt13: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_PARTITION_INFO {
    pub SizeOfPartitionInfo: u32,
    pub PartitionStyle: usize,
    pub Signature: u32,
    pub CheckSum: u32,
    pub DiskId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_GEOMETRY_EX {
    pub Geometry: u32,
    pub DiskSize: i64,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_CONTROLLER_NUMBER {
    pub ControllerNumber: u32,
    pub DiskNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_CACHE_INFORMATION {
    pub ParametersSavable: u8,
    pub ReadCacheEnabled: u8,
    pub WriteCacheEnabled: u8,
    pub ReadRetentionPriority: u32,
    pub WriteRetentionPriority: u32,
    pub DisablePrefetchTransferLength: u16,
    pub PrefetchScalar: u8,
    pub Minimum: u16,
    pub Maximum: u16,
    pub MaximumBlocks: u16,
    pub Minimum_1: u16,
    pub Maximum_1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_GROW_PARTITION {
    pub PartitionNumber: u32,
    pub BytesToGrow: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HISTOGRAM_BUCKET {
    pub Reads: u32,
    pub Writes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_HISTOGRAM {
    pub DiskSize: i64,
    pub Start: i64,
    pub End: i64,
    pub Average: i64,
    pub AverageRead: i64,
    pub AverageWrite: i64,
    pub Granularity: u32,
    pub Size: u32,
    pub ReadCount: u32,
    pub WriteCount: u32,
    pub Histogram: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_PERFORMANCE {
    pub BytesRead: i64,
    pub BytesWritten: i64,
    pub ReadTime: i64,
    pub WriteTime: i64,
    pub IdleTime: i64,
    pub ReadCount: u32,
    pub WriteCount: u32,
    pub QueueDepth: u32,
    pub SplitCount: u32,
    pub QueryTime: i64,
    pub StorageDeviceNumber: u32,
    pub StorageManagerName: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_RECORD {
    pub ByteOffset: i64,
    pub StartTime: i64,
    pub EndTime: i64,
    pub VirtualAddress: usize,
    pub NumberOfBytes: u32,
    pub DeviceNumber: u8,
    pub ReadRequest: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_LOGGING {
    pub Function: u8,
    pub BufferAddress: usize,
    pub BufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BIN_RANGE {
    pub StartValue: i64,
    pub Length: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERF_BIN {
    pub NumberOfBins: u32,
    pub TypeOfBin: u32,
    pub BinsRanges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BIN_COUNT {
    pub BinRange: u32,
    pub BinCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BIN_RESULTS {
    pub NumberOfBins: u32,
    pub BinCounts: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GETVERSIONINPARAMS {
    pub bVersion: u8,
    pub bRevision: u8,
    pub bReserved: u8,
    pub bIDEDeviceMap: u8,
    pub fCapabilities: u32,
    pub dwReserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IDEREGS {
    pub bFeaturesReg: u8,
    pub bSectorCountReg: u8,
    pub bSectorNumberReg: u8,
    pub bCylLowReg: u8,
    pub bCylHighReg: u8,
    pub bDriveHeadReg: u8,
    pub bCommandReg: u8,
    pub bReserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SENDCMDINPARAMS {
    pub cBufferSize: u32,
    pub irDriveRegs: u32,
    pub bDriveNumber: u8,
    pub bReserved: [u8; 3],
    pub dwReserved: [u32; 4],
    pub bBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRIVERSTATUS {
    pub bDriverError: u8,
    pub bIDEError: u8,
    pub bReserved: [u8; 2],
    pub dwReserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SENDCMDOUTPARAMS {
    pub cBufferSize: u32,
    pub DriverStatus: u32,
    pub bBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_PARTITION_ATTRIBUTES {
    pub Version: u32,
    pub Reserved1: u32,
    pub Attributes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_PARTITION_ATTRIBUTES {
    pub Version: u32,
    pub Persist: u8,
    pub Reserved1: [u8; 3],
    pub Attributes: u64,
    pub AttributesMask: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_DISK_ATTRIBUTES {
    pub Version: u32,
    pub Reserved1: u32,
    pub Attributes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_DISK_ATTRIBUTES {
    pub Version: u32,
    pub Persist: u8,
    pub RelinquishOwnership: u8,
    pub Reserved1: [u8; 2],
    pub Reserved1_1: [u8; 3],
    pub Attributes: u64,
    pub AttributesMask: u64,
    pub Owner: u32,
    pub Reserved2: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_SAN_SETTINGS {
    pub Version: u32,
    pub SanPolicy: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_SNAPSHOT_INFO {
    pub Version: u32,
    pub State: u32,
    pub SnapshotSetId: u32,
    pub SnapshotId: u32,
    pub LunId: u32,
    pub CreationTimeStamp: i64,
    pub ImportCount: u32,
    pub Flags: u32,
    pub AdditionalDataSize: u32,
    pub AdditionalData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_CLUSTER_INFO {
    pub Version: u32,
    pub Flags: u64,
    pub FlagsMask: u64,
    pub Notify: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_PERFORMANCE_PARAMETERS {
    pub Version: u32,
    pub Type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_PERFORMANCE_INFO {
    pub Version: u32,
    pub Type: u32,
    pub QueryTime: i64,
    pub BytesRead: i64,
    pub BytesWritten: i64,
    pub ReadTime: i64,
    pub WriteTime: i64,
    pub FlushTime: i64,
    pub IdleTime: i64,
    pub ReadCount: u32,
    pub WriteCount: u32,
    pub FlushCount: u32,
    pub QueueDepth: u32,
    pub SplitCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MAPPED_ADDRESS {
    pub field_NextMappedAddress: u32,
    pub MappedAddress: usize,
    pub NumberOfBytes: u32,
    pub IoAddress: i64,
    pub BusNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_INPUT_DATA {
    pub UnitId: u16,
    pub MakeCode: u16,
    pub Flags: u16,
    pub Reserved: u16,
    pub ExtraInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_TYPEMATIC_PARAMETERS {
    pub UnitId: u16,
    pub Rate: u16,
    pub Delay: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_ID {
    pub Type: u8,
    pub Subtype: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_ATTRIBUTES {
    pub KeyboardIdentifier: u32,
    pub KeyboardMode: u16,
    pub NumberOfFunctionKeys: u16,
    pub NumberOfIndicators: u16,
    pub NumberOfKeysTotal: u16,
    pub InputDataQueueLength: u32,
    pub KeyRepeatMinimum: u32,
    pub KeyRepeatMaximum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_EXTENDED_ATTRIBUTES {
    pub Version: u8,
    pub FormFactor: u8,
    pub KeyType: u8,
    pub PhysicalLayout: u8,
    pub VendorSpecificPhysicalLayout: u8,
    pub IETFLanguageTagIndex: u8,
    pub ImplementedInputAssistControls: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_INDICATOR_PARAMETERS {
    pub UnitId: u16,
    pub LedFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INDICATOR_LIST {
    pub MakeCode: u16,
    pub IndicatorFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_INDICATOR_TRANSLATION {
    pub NumberOfIndicatorKeys: u16,
    pub IndicatorList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_UNIT_ID_PARAMETER {
    pub UnitId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct KEYBOARD_IME_STATUS {
    pub UnitId: u16,
    pub ImeOpen: u32,
    pub ImeConvMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_CONFIGURATION_HEADER {
    pub DataLength: [u8; 4],
    pub Reserved: [u8; 2],
    pub CurrentProfile: [u8; 2],
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_HEADER {
    pub FeatureCode: [u8; 2],
    pub Current: u8,
    pub Persistent: u8,
    pub Version: u8,
    pub Reserved0: u8,
    pub AdditionalLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_PROFILE_LIST_EX {
    pub ProfileNumber: [u8; 2],
    pub Current: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_PROFILE_LIST {
    pub Header: u32,
    pub Profiles: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CORE {
    pub Header: u32,
    pub PhysicalInterface: [u8; 4],
    pub DeviceBusyEvent: u8,
    pub INQUIRY2: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_MORPHING {
    pub Header: u32,
    pub Asynchronous: u8,
    pub OCEvent: u8,
    pub Reserved01: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_REMOVABLE_MEDIUM {
    pub Header: u32,
    pub Lockable: u8,
    pub DBML: u8,
    pub DefaultToPrevent: u8,
    pub Eject: u8,
    pub Load: u8,
    pub LoadingMechanism: u8,
    pub Reserved3: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_WRITE_PROTECT {
    pub Header: u32,
    pub SupportsSWPPBit: u8,
    pub SupportsPersistentWriteProtect: u8,
    pub WriteInhibitDCB: u8,
    pub DiscWriteProtectPAC: u8,
    pub Reserved01: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_RANDOM_READABLE {
    pub Header: u32,
    pub LogicalBlockSize: [u8; 4],
    pub Blocking: [u8; 2],
    pub ErrorRecoveryPagePresent: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_MULTI_READ {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CD_READ {
    pub Header: u32,
    pub CDText: u8,
    pub C2ErrorData: u8,
    pub Reserved01: u8,
    pub DigitalAudioPlay: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_READ {
    pub Header: u32,
    pub Multi110: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub DualDashR: u8,
    pub Reserved3: u8,
    pub Reserved4: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_RANDOM_WRITABLE {
    pub Header: u32,
    pub LastLBA: [u8; 4],
    pub LogicalBlockSize: [u8; 4],
    pub Blocking: [u8; 2],
    pub ErrorRecoveryPagePresent: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_INCREMENTAL_STREAMING_WRITABLE {
    pub Header: u32,
    pub DataTypeSupported: [u8; 2],
    pub BufferUnderrunFree: u8,
    pub AddressModeReservation: u8,
    pub TrackRessourceInformation: u8,
    pub Reserved01: u8,
    pub NumberOfLinkSizes: u8,
    pub LinkSize: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_SECTOR_ERASABLE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_FORMATTABLE {
    pub Header: u32,
    pub FullCertification: u8,
    pub QuickCertification: u8,
    pub SpareAreaExpansion: u8,
    pub RENoSpareAllocated: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub RRandomWritable: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DEFECT_MANAGEMENT {
    pub Header: u32,
    pub Reserved1: u8,
    pub SupplimentalSpareArea: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_WRITE_ONCE {
    pub Header: u32,
    pub LogicalBlockSize: [u8; 4],
    pub Blocking: [u8; 2],
    pub ErrorRecoveryPagePresent: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_RESTRICTED_OVERWRITE {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CDRW_CAV_WRITE {
    pub Header: u32,
    pub Reserved1: [u8; 4],
    pub SupportsWritingCDRWSubType0: u8,
    pub SupportsWritingCDRWSubType1: u8,
    pub SupportsWritingCDRWSubType2: u8,
    pub SupportsWritingCDRWSubType3: u8,
    pub SupportsWritingCDRWSubType4: u8,
    pub SupportsWritingCDRWSubType5: u8,
    pub SupportsWritingCDRWSubType6: u8,
    pub SupportsWritingCDRWSubType7: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_MRW {
    pub Header: u32,
    pub Write: u8,
    pub DvdPlusRead: u8,
    pub DvdPlusWrite: u8,
    pub Reserved01: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_ENHANCED_DEFECT_REPORTING {
    pub Header: u32,
    pub DRTDMSupported: u8,
    pub Reserved0: u8,
    pub NumberOfDBICacheZones: u8,
    pub NumberOfEntries: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_PLUS_RW {
    pub Header: u32,
    pub Write: u8,
    pub Reserved1: u8,
    pub CloseOnly: u8,
    pub QuickStart: u8,
    pub Reserved02: u8,
    pub Reserved03: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_PLUS_R {
    pub Header: u32,
    pub Write: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_RW_RESTRICTED_OVERWRITE {
    pub Header: u32,
    pub Blank: u8,
    pub Intermediate: u8,
    pub DefectStatusDataRead: u8,
    pub DefectStatusDataGenerate: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CD_TRACK_AT_ONCE {
    pub Header: u32,
    pub RWSubchannelsRecordable: u8,
    pub CdRewritable: u8,
    pub TestWriteOk: u8,
    pub RWSubchannelPackedOk: u8,
    pub RWSubchannelRawOk: u8,
    pub Reserved1: u8,
    pub BufferUnderrunFree: u8,
    pub Reserved3: u8,
    pub Reserved2: u8,
    pub DataTypeSupported: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CD_MASTERING {
    pub Header: u32,
    pub RWSubchannelsRecordable: u8,
    pub CdRewritable: u8,
    pub TestWriteOk: u8,
    pub RawRecordingOk: u8,
    pub RawMultiSessionOk: u8,
    pub SessionAtOnceOk: u8,
    pub BufferUnderrunFree: u8,
    pub Reserved1: u8,
    pub MaximumCueSheetLength: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_RECORDABLE_WRITE {
    pub Header: u32,
    pub Reserved1: u8,
    pub DVD_RW: u8,
    pub TestWrite: u8,
    pub RDualLayer: u8,
    pub Reserved02: u8,
    pub BufferUnderrunFree: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DDCD_READ {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DDCD_R_WRITE {
    pub Header: u32,
    pub Reserved1: u8,
    pub TestWrite: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DDCD_RW_WRITE {
    pub Header: u32,
    pub Blank: u8,
    pub Intermediate: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_LAYER_JUMP_RECORDING {
    pub Header: u32,
    pub Reserved0: [u8; 3],
    pub NumberOfLinkSizes: u8,
    pub LinkSizes: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_CD_RW_MEDIA_WRITE_SUPPORT {
    pub Header: u32,
    pub Reserved1: u8,
    pub Subtype0: u8,
    pub Subtype1: u8,
    pub Subtype2: u8,
    pub Subtype3: u8,
    pub Subtype4: u8,
    pub Subtype5: u8,
    pub Subtype6: u8,
    pub Subtype7: u8,
    pub Reserved2: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_BD_R_PSEUDO_OVERWRITE {
    pub Header: u32,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_PLUS_RW_DUAL_LAYER {
    pub Header: u32,
    pub Write: u8,
    pub Reserved1: u8,
    pub CloseOnly: u8,
    pub QuickStart: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_PLUS_R_DUAL_LAYER {
    pub Header: u32,
    pub Write: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BD_CLASS_SUPPORT_BITMAP {
    pub Version8: u8,
    pub Version9: u8,
    pub Version10: u8,
    pub Version11: u8,
    pub Version12: u8,
    pub Version13: u8,
    pub Version14: u8,
    pub Version15: u8,
    pub Version0: u8,
    pub Version1: u8,
    pub Version2: u8,
    pub Version3: u8,
    pub Version4: u8,
    pub Version5: u8,
    pub Version6: u8,
    pub Version7: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_BD_READ {
    pub Header: u32,
    pub Reserved: [u8; 4],
    pub Class0BitmapBDREReadSupport: u32,
    pub Class1BitmapBDREReadSupport: u32,
    pub Class2BitmapBDREReadSupport: u32,
    pub Class3BitmapBDREReadSupport: u32,
    pub Class0BitmapBDRReadSupport: u32,
    pub Class1BitmapBDRReadSupport: u32,
    pub Class2BitmapBDRReadSupport: u32,
    pub Class3BitmapBDRReadSupport: u32,
    pub Class0BitmapBDROMReadSupport: u32,
    pub Class1BitmapBDROMReadSupport: u32,
    pub Class2BitmapBDROMReadSupport: u32,
    pub Class3BitmapBDROMReadSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_BD_WRITE {
    pub Header: u32,
    pub SupportsVerifyNotRequired: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
    pub Class0BitmapBDREWriteSupport: u32,
    pub Class1BitmapBDREWriteSupport: u32,
    pub Class2BitmapBDREWriteSupport: u32,
    pub Class3BitmapBDREWriteSupport: u32,
    pub Class0BitmapBDRWriteSupport: u32,
    pub Class1BitmapBDRWriteSupport: u32,
    pub Class2BitmapBDRWriteSupport: u32,
    pub Class3BitmapBDRWriteSupport: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_TSR {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_HDDVD_READ {
    pub Header: u32,
    pub Recordable: u8,
    pub Reserved0: u8,
    pub Reserved1: u8,
    pub Rewritable: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_HDDVD_WRITE {
    pub Header: u32,
    pub Recordable: u8,
    pub Reserved0: u8,
    pub Reserved1: u8,
    pub Rewritable: u8,
    pub Reserved2: u8,
    pub Reserved3: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_HYBRID_DISC {
    pub Header: u32,
    pub ResetImmunity: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_POWER_MANAGEMENT {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_SMART {
    pub Header: u32,
    pub FaultFailureReportingPagePresent: u8,
    pub Reserved1: u8,
    pub Reserved02: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_EMBEDDED_CHANGER {
    pub Header: u32,
    pub Reserved1: u8,
    pub SupportsDiscPresent: u8,
    pub Reserved2: u8,
    pub SideChangeCapable: u8,
    pub Reserved3: u8,
    pub Reserved4: [u8; 2],
    pub HighestSlotNumber: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_CD_AUDIO_ANALOG_PLAY {
    pub Header: u32,
    pub SeperateVolume: u8,
    pub SeperateChannelMute: u8,
    pub ScanSupported: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub NumerOfVolumeLevels: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_MICROCODE_UPDATE {
    pub Header: u32,
    pub M5: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_TIMEOUT {
    pub Header: u32,
    pub Group3: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub UnitLength: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_CSS {
    pub Header: u32,
    pub Reserved1: [u8; 3],
    pub CssVersion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_REAL_TIME_STREAMING {
    pub Header: u32,
    pub StreamRecording: u8,
    pub WriteSpeedInGetPerf: u8,
    pub WriteSpeedInMP2A: u8,
    pub SetCDSpeed: u8,
    pub ReadBufferCapacityBlock: u8,
    pub Reserved1: u8,
    pub Reserved2: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_LOGICAL_UNIT_SERIAL_NUMBER {
    pub Header: u32,
    pub SerialNumber: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_MEDIA_SERIAL_NUMBER {
    pub Header: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DISC_CONTROL_BLOCKS_EX {
    pub ContentDescriptor: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DISC_CONTROL_BLOCKS {
    pub Header: u32,
    pub Data: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_DVD_CPRM {
    pub Header: u32,
    pub Reserved0: [u8; 3],
    pub CPRMVersion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_FIRMWARE_DATE {
    pub Header: u32,
    pub Year: [u8; 4],
    pub Month: [u8; 2],
    pub Day: [u8; 2],
    pub Hour: [u8; 2],
    pub Minute: [u8; 2],
    pub Seconds: [u8; 2],
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_AACS {
    pub Header: u32,
    pub BindingNonceGeneration: u8,
    pub Reserved0: u8,
    pub BindingNonceBlockCount: u8,
    pub NumberOfAGIDs: u8,
    pub Reserved1: u8,
    pub AACSVersion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_VCPS {
    pub Header: u32,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_RESERVED {
    pub Header: u32,
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FEATURE_DATA_VENDOR_SPECIFIC {
    pub Header: u32,
    pub VendorSpecificData: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_CONFIGURATION_IOCTL_INPUT {
    pub Feature: u32,
    pub RequestType: u32,
    pub Reserved: [usize; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_CONFIGURATION_IOCTL_INPUT32 {
    pub Feature: u32,
    pub RequestType: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MODEM_MESSAGE {
    pub TotalSize: u32,
    pub Flags: u32,
    pub SessionId: u32,
    pub RequestId: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUSE_INPUT_DATA {
    pub UnitId: u16,
    pub Flags: u16,
    pub Buttons: u32,
    pub ButtonFlags: u16,
    pub ButtonData: u16,
    pub RawButtons: u32,
    pub LastX: i32,
    pub LastY: i32,
    pub ExtraInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUSE_ATTRIBUTES {
    pub MouseIdentifier: u16,
    pub NumberOfButtons: u16,
    pub SampleRate: u16,
    pub InputDataQueueLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MOUSE_UNIT_ID_PARAMETER {
    pub UnitId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_STATISTICS_VALUE {
    pub Oid: u32,
    pub DataLength: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_STATISTICS_VALUE_EX {
    pub Oid: u32,
    pub DataLength: u32,
    pub Length: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_VAR_DATA_DESC {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Offset: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_STATISTICS_INFO {
    pub Header: u32,
    pub SupportedStatistics: u32,
    pub ifInDiscards: u32,
    pub ifInErrors: u32,
    pub ifHCInOctets: u32,
    pub ifHCInUcastPkts: u32,
    pub ifHCInMulticastPkts: u32,
    pub ifHCInBroadcastPkts: u32,
    pub ifHCOutOctets: u32,
    pub ifHCOutUcastPkts: u32,
    pub ifHCOutMulticastPkts: u32,
    pub ifHCOutBroadcastPkts: u32,
    pub ifOutErrors: u32,
    pub ifOutDiscards: u32,
    pub ifHCInUcastOctets: u32,
    pub ifHCInMulticastOctets: u32,
    pub ifHCInBroadcastOctets: u32,
    pub ifHCOutUcastOctets: u32,
    pub ifHCOutMulticastOctets: u32,
    pub ifHCOutBroadcastOctets: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RSC_STATISTICS_INFO {
    pub Header: u32,
    pub CoalescedPkts: u32,
    pub CoalescedOctets: u32,
    pub CoalesceEvents: u32,
    pub Aborts: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_INTERRUPT_MODERATION_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub InterruptModeration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TIMEOUT_DPC_REQUEST_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub TimeoutArrayLength: u32,
    pub TimeoutArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PCI_DEVICE_CUSTOM_PROPERTIES {
    pub Header: u32,
    pub DeviceType: u32,
    pub CurrentSpeedAndMode: u32,
    pub CurrentPayloadSize: u32,
    pub MaxPayloadSize: u32,
    pub MaxReadRequestSize: u32,
    pub CurrentLinkSpeed: u32,
    pub CurrentLinkWidth: u32,
    pub MaxLinkSpeed: u32,
    pub MaxLinkWidth: u32,
    pub PciExpressVersion: u32,
    pub InterruptType: u32,
    pub MaxInterruptMessages: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_STATUS_INDICATION {
    pub StatusType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_AUTHENTICATION_REQUEST {
    pub Length: u32,
    pub Bssid: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PMKID_CANDIDATE {
    pub BSSID: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_PMKID_CANDIDATE_LIST {
    pub Version: u32,
    pub NumCandidates: u32,
    pub CandidateList: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_NETWORK_TYPE_LIST {
    pub NumberOfItems: u32,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_CONFIGURATION_FH {
    pub Length: u32,
    pub HopPattern: u32,
    pub HopSet: u32,
    pub DwellTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_CONFIGURATION {
    pub Length: u32,
    pub BeaconPeriod: u32,
    pub ATIMWindow: u32,
    pub DSConfig: u32,
    pub FHConfig: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_STATISTICS {
    pub Length: u32,
    pub TransmittedFragmentCount: i64,
    pub MulticastTransmittedFrameCount: i64,
    pub FailedCount: i64,
    pub RetryCount: i64,
    pub MultipleRetryCount: i64,
    pub RTSSuccessCount: i64,
    pub RTSFailureCount: i64,
    pub ACKFailureCount: i64,
    pub FrameDuplicateCount: i64,
    pub ReceivedFragmentCount: i64,
    pub MulticastReceivedFrameCount: i64,
    pub FCSErrorCount: i64,
    pub TKIPLocalMICFailures: i64,
    pub TKIPICVErrorCount: i64,
    pub TKIPCounterMeasuresInvoked: i64,
    pub TKIPReplays: i64,
    pub CCMPFormatErrors: i64,
    pub CCMPReplays: i64,
    pub CCMPDecryptErrors: i64,
    pub FourWayHandshakeFailures: i64,
    pub WEPUndecryptableCount: i64,
    pub WEPICVErrorCount: i64,
    pub DecryptSuccessCount: i64,
    pub DecryptFailureCount: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_KEY {
    pub Length: u32,
    pub KeyIndex: u32,
    pub KeyLength: u32,
    pub BSSID: u32,
    pub KeyRSC: u32,
    pub KeyMaterial: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_REMOVE_KEY {
    pub Length: u32,
    pub KeyIndex: u32,
    pub BSSID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_WEP {
    pub Length: u32,
    pub KeyIndex: u32,
    pub KeyLength: u32,
    pub KeyMaterial: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_SSID {
    pub SsidLength: u32,
    pub Ssid: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WLAN_BSSID {
    pub Length: u32,
    pub MacAddress: u32,
    pub Reserved: [u8; 2],
    pub Ssid: u32,
    pub Privacy: u32,
    pub Rssi: u32,
    pub NetworkTypeInUse: u32,
    pub Configuration: u32,
    pub InfrastructureMode: u32,
    pub SupportedRates: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_BSSID_LIST {
    pub NumberOfItems: u32,
    pub Bssid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WLAN_BSSID_EX {
    pub Length: u32,
    pub MacAddress: u32,
    pub Reserved: [u8; 2],
    pub Ssid: u32,
    pub Privacy: u32,
    pub Rssi: u32,
    pub NetworkTypeInUse: u32,
    pub Configuration: u32,
    pub InfrastructureMode: u32,
    pub SupportedRates: u32,
    pub IELength: u32,
    pub IEs: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_BSSID_LIST_EX {
    pub NumberOfItems: u32,
    pub Bssid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_FIXED_IEs {
    pub Timestamp: [u8; 8],
    pub BeaconInterval: u16,
    pub Capabilities: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_VARIABLE_IEs {
    pub ElementID: u8,
    pub Length: u8,
    pub data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_AI_REQFI {
    pub Capabilities: u16,
    pub ListenInterval: u16,
    pub CurrentAPAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_AI_RESFI {
    pub Capabilities: u16,
    pub StatusCode: u16,
    pub AssociationId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_ASSOCIATION_INFORMATION {
    pub Length: u32,
    pub AvailableRequestFixedIEs: u16,
    pub RequestFixedIEs: u32,
    pub RequestIELength: u32,
    pub OffsetRequestIEs: u32,
    pub AvailableResponseFixedIEs: u16,
    pub ResponseFixedIEs: u32,
    pub ResponseIELength: u32,
    pub OffsetResponseIEs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_AUTHENTICATION_EVENT {
    pub Status: u32,
    pub Request: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_TEST {
    pub Length: u32,
    pub Type: u32,
    pub AuthenticationEvent: u32,
    pub RssiTrigger: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BSSID_INFO {
    pub BSSID: u32,
    pub PMKID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_PMKID {
    pub Length: u32,
    pub BSSIDInfoCount: u32,
    pub BSSIDInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_AUTHENTICATION_ENCRYPTION {
    pub AuthModeSupported: u32,
    pub EncryptStatusSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_CAPABILITY {
    pub Length: u32,
    pub Version: u32,
    pub NoOfPMKIDs: u32,
    pub NoOfAuthEncryptPairsSupported: u32,
    pub AuthenticationEncryptionSupported: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_802_11_NON_BCAST_SSID_LIST {
    pub NumberOfItems: u32,
    pub Non_Bcast_Ssid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_ALGO_INFO {
    pub algoIdentifier: u32,
    pub algoKeylen: u32,
    pub algoRounds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_SECURITY_ASSOCIATION {
    pub Operation: u32,
    pub SPI: u32,
    pub IntegrityAlgo: u32,
    pub ConfAlgo: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_IPSEC_ADD_SA {
    pub SrcAddr: u32,
    pub SrcMask: u32,
    pub DestAddr: u32,
    pub DestMask: u32,
    pub Protocol: u32,
    pub SrcPort: u16,
    pub DestPort: u16,
    pub SrcTunnelAddr: u32,
    pub DestTunnelAddr: u32,
    pub Flags: u16,
    pub NumSAs: i16,
    pub SecAssoc: [u32; 1],
    pub OffloadHandle: usize,
    pub KeyLen: u32,
    pub KeyMat: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_IPSEC_DELETE_SA {
    pub OffloadHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_IPSEC_UDPESP_ENCAPTYPE_ENTRY {
    pub UdpEncapType: u32,
    pub DstEncapPort: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_IPSEC_ADD_UDPESP_SA {
    pub SrcAddr: u32,
    pub SrcMask: u32,
    pub DstAddr: u32,
    pub DstMask: u32,
    pub Protocol: u32,
    pub SrcPort: u16,
    pub DstPort: u16,
    pub SrcTunnelAddr: u32,
    pub DstTunnelAddr: u32,
    pub Flags: u16,
    pub NumSAs: i16,
    pub SecAssoc: [u32; 1],
    pub OffloadHandle: usize,
    pub EncapTypeEntry: u32,
    pub EncapTypeEntryOffldHandle: usize,
    pub KeyLen: u32,
    pub KeyMat: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OFFLOAD_IPSEC_DELETE_UDPESP_SA {
    pub OffloadHandle: usize,
    pub EncapTypeEntryOffldHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSPORT_HEADER_OFFSET {
    pub ProtocolType: u16,
    pub HeaderOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_ADDRESS {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_ADDRESS_LIST {
    pub AddressCount: i32,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_ADDRESS_IP {
    pub sin_port: u16,
    pub in_addr: u32,
    pub sin_zero: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_ADDRESS_IP6 {
    pub sin6_port: u16,
    pub sin6_flowinfo: u32,
    pub sin6_addr: [u16; 8],
    pub sin6_scope_id: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NETWORK_ADDRESS_IPX {
    pub NetworkAddress: u32,
    pub NodeAddress: [u8; 6],
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GEN_GET_TIME_CAPS {
    pub Flags: u32,
    pub ClockPrecision: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GEN_GET_NETCARD_TIME {
    pub ReadTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_PACKET_PATTERN {
    pub Priority: u32,
    pub Reserved: u32,
    pub MaskSize: u32,
    pub PatternOffset: u32,
    pub PatternSize: u32,
    pub PatternFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_WAKE_UP_CAPABILITIES {
    pub MinMagicPacketWakeUp: u32,
    pub MinPatternWakeUp: u32,
    pub MinLinkChangeWakeUp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PNP_CAPABILITIES {
    pub Flags: u32,
    pub WakeUpCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WAN_PROTOCOL_CAPS {
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_CO_LINK_SPEED {
    pub Outbound: u32,
    pub Inbound: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_LINK_SPEED {
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GUID {
    pub Guid: u32,
    pub Oid: u32,
    pub Status: u32,
    pub Size: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IRDA_PACKET_INFO {
    pub ExtraBOFs: u32,
    pub MinTurnAroundTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_LINK_STATE {
    pub Header: u32,
    pub MediaConnectState: u32,
    pub MediaDuplexState: u32,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub PauseFunctions: u32,
    pub AutoNegotiationFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_LINK_PARAMETERS {
    pub Header: u32,
    pub MediaDuplexState: u32,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub PauseFunctions: u32,
    pub AutoNegotiationFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OPER_STATE {
    pub Header: u32,
    pub OperationalStatus: u32,
    pub OperationalStatusFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IP_OPER_STATUS {
    pub AddressFamily: u32,
    pub OperationalStatus: u32,
    pub OperationalStatusFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IP_OPER_STATUS_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub NumberofAddressFamiliesReturned: u32,
    pub IpOperationalStatus: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IP_OPER_STATE {
    pub Header: u32,
    pub Flags: u32,
    pub IpOperationalStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD_PARAMETERS {
    pub Header: u32,
    pub IPv4Checksum: u8,
    pub TCPIPv4Checksum: u8,
    pub UDPIPv4Checksum: u8,
    pub TCPIPv6Checksum: u8,
    pub UDPIPv6Checksum: u8,
    pub LsoV1: u8,
    pub IPsecV1: u8,
    pub LsoV2IPv4: u8,
    pub LsoV2IPv6: u8,
    pub TcpConnectionIPv4: u8,
    pub TcpConnectionIPv6: u8,
    pub Flags: u32,
    pub IPsecV2: u8,
    pub IPsecV2IPv4: u8,
    pub RscIPv4: u8,
    pub RscIPv6: u8,
    pub EncapsulatedPacketTaskOffload: u8,
    pub EncapsulationTypes: u8,
    pub VxlanUDPPortNumber: u16,
    pub Value: u32,
    pub IPv4: u8,
    pub IPv6: u8,
    pub Enabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VxlanParameters {
    pub VxlanUDPPortNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_LARGE_SEND_OFFLOAD_V1 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub TcpOptions: u32,
    pub IpOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_IP_CHECKSUM_OFFLOAD {
    pub Encapsulation: u32,
    pub IpOptionsSupported: u32,
    pub TcpOptionsSupported: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
    pub IpChecksum: u32,
    pub Encapsulation_1: u32,
    pub IpOptionsSupported_1: u32,
    pub TcpOptionsSupported_1: u32,
    pub TcpChecksum_1: u32,
    pub UdpChecksum_1: u32,
    pub IpChecksum_1: u32,
    pub Encapsulation_2: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported_2: u32,
    pub TcpChecksum_2: u32,
    pub UdpChecksum_2: u32,
    pub Encapsulation_3: u32,
    pub IpExtensionHeadersSupported_1: u32,
    pub TcpOptionsSupported_3: u32,
    pub TcpChecksum_3: u32,
    pub UdpChecksum_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V1 {
    pub Encapsulation: u32,
    pub AhEspCombined: u32,
    pub TransportTunnelCombined: u32,
    pub IPv4Options: u32,
    pub Flags: u32,
    pub Md5: u32,
    pub Sha_1: u32,
    pub Transport: u32,
    pub Tunnel: u32,
    pub Send: u32,
    pub Receive: u32,
    pub Des: u32,
    pub Reserved: u32,
    pub TripleDes: u32,
    pub NullEsp: u32,
    pub Transport_1: u32,
    pub Tunnel_1: u32,
    pub Send_1: u32,
    pub Receive_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_LARGE_SEND_OFFLOAD_V2 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub Encapsulation_1: u32,
    pub MaxOffLoadSize_1: u32,
    pub MinSegmentCount_1: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_IPSEC_OFFLOAD_V2 {
    pub Encapsulation: u32,
    pub IPv6Supported: u8,
    pub IPv4Options: u8,
    pub IPv6NonIPsecExtensionHeaders: u8,
    pub Ah: u8,
    pub Esp: u8,
    pub AhEspCombined: u8,
    pub Transport: u8,
    pub Tunnel: u8,
    pub TransportTunnelCombined: u8,
    pub LsoSupported: u8,
    pub ExtendedSequenceNumbers: u8,
    pub UdpEsp: u32,
    pub AuthenticationAlgorithms: u32,
    pub EncryptionAlgorithms: u32,
    pub SaOffloadCapacity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_RECV_SEG_COALESCE_OFFLOAD {
    pub Enabled: u8,
    pub Enabled_1: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ENCAPSULATED_PACKET_TASK_OFFLOAD {
    pub TransmitChecksumOffloadSupported: u32,
    pub ReceiveChecksumOffloadSupported: u32,
    pub LsoV2Supported: u32,
    pub RssSupported: u32,
    pub VmqSupported: u32,
    pub UsoSupported: u32,
    pub Reserved: u32,
    pub MaxHeaderSizeSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ENCAPSULATED_PACKET_TASK_OFFLOAD_V2 {
    pub TransmitChecksumOffloadSupported: u32,
    pub ReceiveChecksumOffloadSupported: u32,
    pub LsoV2Supported: u32,
    pub RssSupported: u32,
    pub VmqSupported: u32,
    pub UsoSupported: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub MaxHeaderSizeSupported: u32,
    pub VxlanUDPPortNumber: u16,
    pub VxlanUDPPortNumberConfigurable: u16,
    pub Value: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VxlanInfo {
    pub VxlanUDPPortNumber: u16,
    pub VxlanUDPPortNumberConfigurable: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RFC6877_464XLAT_OFFLOAD {
    pub XlatOffload: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_UDP_SEGMENTATION_OFFLOAD {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub SubMssFinalSegmentSupported: u32,
    pub Reserved: u32,
    pub Reserved_1: u32,
    pub Encapsulation_1: u32,
    pub MaxOffLoadSize_1: u32,
    pub MinSegmentCount_1: u32,
    pub SubMssFinalSegmentSupported_1: u32,
    pub Reserved1: u32,
    pub Reserved1_1: u32,
    pub IpExtensionHeadersSupported: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OFFLOAD {
    pub Header: u32,
    pub Checksum: u32,
    pub LsoV1: u32,
    pub IPsecV1: u32,
    pub LsoV2: u32,
    pub Flags: u32,
    pub IPsecV2: u32,
    pub Rsc: u32,
    pub EncapsulatedPacketTaskOffloadGre: u32,
    pub EncapsulatedPacketTaskOffloadVxlan: u32,
    pub EncapsulationTypes: u8,
    pub Rfc6877Xlat: u32,
    pub UdpSegmentation: u32,
    pub UdpRsc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_TCP_LARGE_SEND_OFFLOAD_V1 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub TcpOptions: u32,
    pub IpOptions: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_TCP_IP_CHECKSUM_OFFLOAD {
    pub Encapsulation: u32,
    pub IpOptionsSupported: u32,
    pub TcpOptionsSupported: u32,
    pub TcpChecksum: u32,
    pub UdpChecksum: u32,
    pub IpChecksum: u32,
    pub Encapsulation_1: u32,
    pub IpOptionsSupported_1: u32,
    pub TcpOptionsSupported_1: u32,
    pub TcpChecksum_1: u32,
    pub UdpChecksum_1: u32,
    pub IpChecksum_1: u32,
    pub Encapsulation_2: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported_2: u32,
    pub TcpChecksum_2: u32,
    pub UdpChecksum_2: u32,
    pub Encapsulation_3: u32,
    pub IpExtensionHeadersSupported_1: u32,
    pub TcpOptionsSupported_3: u32,
    pub TcpChecksum_3: u32,
    pub UdpChecksum_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_IPSEC_OFFLOAD_V1 {
    pub Encapsulation: u32,
    pub AhEspCombined: u32,
    pub TransportTunnelCombined: u32,
    pub IPv4Options: u32,
    pub Flags: u32,
    pub Md5: u32,
    pub Sha_1: u32,
    pub Transport: u32,
    pub Tunnel: u32,
    pub Send: u32,
    pub Receive: u32,
    pub Des: u32,
    pub Reserved: u32,
    pub TripleDes: u32,
    pub NullEsp: u32,
    pub Transport_1: u32,
    pub Tunnel_1: u32,
    pub Send_1: u32,
    pub Receive_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_TCP_LARGE_SEND_OFFLOAD_V2 {
    pub Encapsulation: u32,
    pub MaxOffLoadSize: u32,
    pub MinSegmentCount: u32,
    pub Encapsulation_1: u32,
    pub MaxOffLoadSize_1: u32,
    pub MinSegmentCount_1: u32,
    pub IpExtensionHeadersSupported: u32,
    pub TcpOptionsSupported: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_OFFLOAD {
    pub Header: u32,
    pub Checksum: u32,
    pub LsoV1: u32,
    pub IPsecV1: u32,
    pub LsoV2: u32,
    pub Flags: u32,
    pub IPsecV2: u32,
    pub Rsc: u32,
    pub EncapsulatedPacketTaskOffloadGre: u32,
    pub UdpSegmentation: u32,
    pub UdpRsc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TCP_CONNECTION_OFFLOAD {
    pub Header: u32,
    pub Encapsulation: u32,
    pub SupportIPv4: u32,
    pub SupportIPv6: u32,
    pub SupportIPv6ExtensionHeaders: u32,
    pub SupportSack: u32,
    pub CongestionAlgorithm: u32,
    pub TcpConnectionOffloadCapacity: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_TCP_CONNECTION_OFFLOAD {
    pub Header: u32,
    pub Encapsulation: u32,
    pub SupportIPv4: u32,
    pub SupportIPv6: u32,
    pub SupportIPv6ExtensionHeaders: u32,
    pub SupportSack: u32,
    pub TcpConnectionOffloadCapacity: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PORT_AUTHENTICATION_PARAMETERS {
    pub Header: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_METHOD_HEADER {
    pub Header: u32,
    pub PortNumber: u32,
    pub NetLuid: u32,
    pub RequestId: u32,
    pub Timeout: u32,
    pub Padding: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_SET_HEADER {
    pub Header: u32,
    pub PortNumber: u32,
    pub NetLuid: u32,
    pub RequestId: u32,
    pub Timeout: u32,
    pub Padding: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_EVENT_HEADER {
    pub Header: u32,
    pub IfIndex: u32,
    pub NetLuid: u32,
    pub RequestId: u32,
    pub PortNumber: u32,
    pub DeviceNameLength: u32,
    pub DeviceNameOffset: u32,
    pub Padding: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_ENUM_ADAPTER {
    pub Header: u32,
    pub IfIndex: u32,
    pub NetLuid: u32,
    pub DeviceNameLength: u16,
    pub DeviceName: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_HD_SPLIT_PARAMETERS {
    pub Header: u32,
    pub HDSplitCombineFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_HD_SPLIT_CURRENT_CONFIG {
    pub Header: u32,
    pub HardwareCapabilities: u32,
    pub CurrentCapabilities: u32,
    pub HDSplitFlags: u32,
    pub HDSplitCombineFlags: u32,
    pub BackfillSize: u32,
    pub MaxHeaderSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_COUNTED_STRING {
    pub Length: u16,
    pub String: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub SupportedWoLPacketPatterns: u32,
    pub NumTotalWoLPatterns: u32,
    pub MaxWoLPatternSize: u32,
    pub MaxWoLPatternOffset: u32,
    pub MaxWoLPacketSaveBuffer: u32,
    pub SupportedProtocolOffloads: u32,
    pub NumArpOffloadIPv4Addresses: u32,
    pub NumNSOffloadIPv6Addresses: u32,
    pub MinMagicPacketWakeUp: u32,
    pub MinPatternWakeUp: u32,
    pub MinLinkChangeWakeUp: u32,
    pub SupportedWakeUpEvents: u32,
    pub MediaSpecificWakeUpEvents: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_PARAMETERS {
    pub Header: u32,
    pub EnabledWoLPacketPatterns: u32,
    pub EnabledProtocolOffloads: u32,
    pub WakeUpFlags: u32,
    pub MediaSpecificWakeUpEvents: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_WOL_PATTERN {
    pub Header: u32,
    pub Flags: u32,
    pub Priority: u32,
    pub WoLPacketType: u32,
    pub FriendlyName: u32,
    pub PatternId: u32,
    pub NextWoLPatternOffset: u32,
    pub Flags_1: u32,
    pub IPv4SourceAddress: [u8; 4],
    pub IPv4DestAddress: [u8; 4],
    pub TCPSourcePortNumber: u16,
    pub TCPDestPortNumber: u16,
    pub Flags_2: u32,
    pub IPv6SourceAddress: [u8; 16],
    pub IPv6DestAddress: [u8; 16],
    pub TCPSourcePortNumber_1: u16,
    pub TCPDestPortNumber_1: u16,
    pub Flags_3: u32,
    pub Flags_4: u32,
    pub MaskOffset: u32,
    pub MaskSize: u32,
    pub PatternOffset: u32,
    pub PatternSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPv4TcpSynParameters {
    pub Flags: u32,
    pub IPv4SourceAddress: [u8; 4],
    pub IPv4DestAddress: [u8; 4],
    pub TCPSourcePortNumber: u16,
    pub TCPDestPortNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPv6TcpSynParameters {
    pub Flags: u32,
    pub IPv6SourceAddress: [u8; 16],
    pub IPv6DestAddress: [u8; 16],
    pub TCPSourcePortNumber: u16,
    pub TCPDestPortNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EapolRequestIdMessageParameters {
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WoLBitMapPattern {
    pub Flags: u32,
    pub MaskOffset: u32,
    pub MaskSize: u32,
    pub PatternOffset: u32,
    pub PatternSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_PROTOCOL_OFFLOAD {
    pub Header: u32,
    pub Flags: u32,
    pub Priority: u32,
    pub ProtocolOffloadType: u32,
    pub FriendlyName: u32,
    pub ProtocolOffloadId: u32,
    pub NextProtocolOffloadOffset: u32,
    pub Flags_1: u32,
    pub RemoteIPv4Address: [u8; 4],
    pub HostIPv4Address: [u8; 4],
    pub MacAddress: [u8; 6],
    pub Flags_2: u32,
    pub RemoteIPv6Address: [u8; 16],
    pub SolicitedNodeIPv6Address: [u8; 16],
    pub MacAddress_1: [u8; 6],
    pub TargetIPv6Addresses: [u8; 2],
    pub Flags_3: u32,
    pub KCK: [u8; 1],
    pub KEK: [u8; 1],
    pub KeyReplayCounter: u64,
    pub Flags_4: u32,
    pub KeyReplayCounter_1: u64,
    pub AuthAlgo: u32,
    pub KCKLength: u32,
    pub KEKLength: u32,
    pub reserved_padding: [u32; 1],
    pub reserved_padding_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPv4ARPParameters {
    pub Flags: u32,
    pub RemoteIPv4Address: [u8; 4],
    pub HostIPv4Address: [u8; 4],
    pub MacAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPv6NSParameters {
    pub Flags: u32,
    pub RemoteIPv6Address: [u8; 16],
    pub SolicitedNodeIPv6Address: [u8; 16],
    pub MacAddress: [u8; 6],
    pub TargetIPv6Addresses: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Dot11RSNRekeyParameters {
    pub Flags: u32,
    pub KCK: [u8; 1],
    pub KEK: [u8; 1],
    pub KeyReplayCounter: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Dot11RSNRekeyParametersV2 {
    pub Flags: u32,
    pub KeyReplayCounter: u64,
    pub AuthAlgo: u32,
    pub KCKLength: u32,
    pub KEKLength: u32,
    pub reserved_padding: [u32; 1],
    pub reserved_padding_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_WAKE_REASON {
    pub Header: u32,
    pub Flags: u32,
    pub WakeReason: u32,
    pub InfoBufferOffset: u32,
    pub InfoBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PM_WAKE_PACKET {
    pub Header: u32,
    pub Flags: u32,
    pub PatternId: u32,
    pub PatternFriendlyName: u32,
    pub OriginalPacketSize: u32,
    pub SavedPacketSize: u32,
    pub SavedPacketOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_PM_ADMIN_CONFIG {
    pub Header: u32,
    pub WakeOnPattern: u32,
    pub WakeOnMagicPacket: u32,
    pub DeviceSleepOnDisconnect: u32,
    pub PMARPOffload: u32,
    pub PMNSOffload: u32,
    pub PMWiFiRekeyOffload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_PM_ACTIVE_CAPABILITIES {
    pub Header: u32,
    pub WakeOnPattern: u32,
    pub WakeOnMagicPacket: u32,
    pub DeviceSleepOnDisconnect: u32,
    pub PMARPOffload: u32,
    pub PMNSOffload: u32,
    pub PMWiFiRekeyOffload: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub EnabledFilterTypes: u32,
    pub EnabledQueueTypes: u32,
    pub NumQueues: u32,
    pub SupportedQueueProperties: u32,
    pub SupportedFilterTests: u32,
    pub SupportedHeaders: u32,
    pub SupportedMacHeaderFields: u32,
    pub MaxMacHeaderFilters: u32,
    pub MaxQueueGroups: u32,
    pub MaxQueuesPerQueueGroup: u32,
    pub MinLookaheadSplitSize: u32,
    pub MaxLookaheadSplitSize: u32,
    pub SupportedARPHeaderFields: u32,
    pub SupportedIPv4HeaderFields: u32,
    pub SupportedIPv6HeaderFields: u32,
    pub SupportedUdpHeaderFields: u32,
    pub MaxFieldTestsPerPacketCoalescingFilter: u32,
    pub MaxPacketCoalescingFilters: u32,
    pub NdisReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub NdisReserved1: u32,
    pub NumTotalMacAddresses: u32,
    pub NumMacAddressesPerPort: u32,
    pub NumVlansPerPort: u32,
    pub NdisReserved2: u32,
    pub NdisReserved3: u32,
    pub NicSwitchCapabilities: u32,
    pub MaxNumSwitches: u32,
    pub MaxNumVPorts: u32,
    pub NdisReserved4: u32,
    pub MaxNumVFs: u32,
    pub MaxNumQueuePairs: u32,
    pub NdisReserved5: u32,
    pub NdisReserved6: u32,
    pub NdisReserved7: u32,
    pub MaxNumQueuePairsPerNonDefaultVPort: u32,
    pub NdisReserved8: u32,
    pub NdisReserved9: u32,
    pub NdisReserved10: u32,
    pub NdisReserved11: u32,
    pub NdisReserved12: u32,
    pub MaxNumMacAddresses: u32,
    pub NdisReserved13: u32,
    pub NdisReserved14: u32,
    pub NdisReserved15: u32,
    pub NdisReserved16: u32,
    pub NdisReserved17: u32,
    pub MaxNumRssCapableNonDefaultPFVPorts: u32,
    pub NumberOfIndirectionTableEntriesForDefaultVPort: u32,
    pub NumberOfIndirectionTableEntriesPerNonDefaultPFVPort: u32,
    pub MaxNumQueuePairsForDefaultVPort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_GLOBAL_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub EnabledFilterTypes: u32,
    pub EnabledQueueTypes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_FIELD_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub FrameHeader: u32,
    pub ReceiveFilterTest: u32,
    pub MacHeaderField: u32,
    pub ArpHeaderField: u32,
    pub IPv4HeaderField: u32,
    pub IPv6HeaderField: u32,
    pub UdpHeaderField: u32,
    pub FieldByteValue: u8,
    pub FieldShortValue: u16,
    pub FieldLongValue: u32,
    pub FieldLong64Value: u32,
    pub FieldByteArrayValue: [u8; 16],
    pub ResultByteValue: u8,
    pub ResultShortValue: u16,
    pub ResultLongValue: u32,
    pub ResultLong64Value: u32,
    pub ResultByteArrayValue: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub QueueId: u32,
    pub FilterId: u32,
    pub FieldParametersArrayOffset: u32,
    pub FieldParametersArrayNumElements: u32,
    pub FieldParametersArrayElementSize: u32,
    pub RequestedFilterIdBitCount: u32,
    pub MaxCoalescingDelay: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_CLEAR_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
    pub FilterId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
    pub PortId: u32,
    pub InterruptCoalescingDomainId: u32,
    pub QosSqId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_FREE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub QueueState: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
    pub NumFilters: u32,
    pub InterruptCoalescingDomainId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_INFO_ARRAY {
    pub Header: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub FilterType: u32,
    pub FilterId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_INFO_ARRAY {
    pub Header: u32,
    pub QueueId: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
    pub Flags: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_ALLOCATION_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueId: u32,
    pub CompletionStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_QUEUE_ALLOCATION_COMPLETE_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_SCALE_CAPABILITIES {
    pub Header: u32,
    pub CapabilitiesFlags: u32,
    pub NumberOfInterruptMessages: u32,
    pub NumberOfReceiveQueues: u32,
    pub NumberOfIndirectionTableEntries: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_SCALE_PARAMETERS {
    pub Header: u32,
    pub Flags: u16,
    pub BaseCpuNumber: u16,
    pub HashInformation: u32,
    pub IndirectionTableSize: u16,
    pub IndirectionTableOffset: u32,
    pub HashSecretKeySize: u16,
    pub HashSecretKeyOffset: u32,
    pub ProcessorMasksOffset: u32,
    pub NumberOfProcessorMasks: u32,
    pub ProcessorMasksEntrySize: u32,
    pub DefaultProcessorNumber: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_SCALE_PARAMETERS_V2 {
    pub Header: u32,
    pub Flags: u32,
    pub HashInformation: u32,
    pub HashSecretKeySize: u32,
    pub HashSecretKeyOffset: u32,
    pub NumberOfQueues: u32,
    pub NumberOfIndirectionTableEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RSS_SET_INDIRECTION_ENTRY {
    pub SwitchId: u32,
    pub VPortId: u32,
    pub Flags: u32,
    pub IndirectionTableIndex: u16,
    pub TargetProcessorNumber: usize,
    pub EntryStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RSS_SET_INDIRECTION_ENTRIES {
    pub Header: u32,
    pub Flags: u32,
    pub RssEntrySize: u32,
    pub RssEntryTableOffset: u32,
    pub NumberOfRssEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_HASH_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub HashInformation: u32,
    pub HashSecretKeySize: u16,
    pub HashSecretKeyOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RSS_PROCESSOR {
    pub ProcNum: usize,
    pub PreferenceIndex: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RSS_PROCESSOR_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub RssBaseProcessor: usize,
    pub MaxNumRssProcessors: u32,
    pub PreferredNumaNode: u16,
    pub RssProcessorArrayOffset: u32,
    pub RssProcessorCount: u32,
    pub RssProcessorEntrySize: u32,
    pub RssMaxProcessor: usize,
    pub RssProfile: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PROCESSOR_INFO_EX {
    pub ProcNum: usize,
    pub SocketId: u32,
    pub CoreId: u32,
    pub HyperThreadId: u32,
    pub NodeId: u16,
    pub NodeDistance: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SYSTEM_PROCESSOR_INFO_EX {
    pub Header: u32,
    pub Flags: u32,
    pub ProcessorVendor: u32,
    pub NumSockets: u32,
    pub NumCores: u32,
    pub NumCoresPerSocket: u32,
    pub MaxHyperThreadingProcsPerCore: u32,
    pub ProcessorInfoOffset: u32,
    pub NumberOfProcessors: u32,
    pub ProcessorInfoEntrySize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_HYPERVISOR_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub PartitionType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_GROUP_AFFINITY {
    pub Mask: u32,
    pub Group: u16,
    pub Reserved: [u16; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_RECEIVE_QUEUE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_RECEIVE_QUEUE_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub QueueType: u32,
    pub QueueId: u32,
    pub QueueGroupId: u32,
    pub QueueState: u32,
    pub ProcessorAffinity: u32,
    pub NumSuggestedReceiveBuffers: u32,
    pub MSIXTableEntry: u32,
    pub LookaheadSize: u32,
    pub VmName: u32,
    pub QueueName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_PERFORMANCE_COUNTERS {
    pub Connect: u32,
    pub Accept: u32,
    pub ConnectFailure: u32,
    pub ConnectionError: u32,
    pub ActiveConnection: u32,
    pub Reserved01: u32,
    pub Reserved02: u32,
    pub Reserved03: u32,
    pub Reserved04: u32,
    pub Reserved05: u32,
    pub Reserved06: u32,
    pub Reserved07: u32,
    pub Reserved08: u32,
    pub Reserved09: u32,
    pub Reserved10: u32,
    pub Reserved11: u32,
    pub Reserved12: u32,
    pub Reserved13: u32,
    pub Reserved14: u32,
    pub Reserved15: u32,
    pub Reserved16: u32,
    pub Reserved17: u32,
    pub Reserved18: u32,
    pub Reserved19: u32,
    pub Reserved20: u32,
    pub CQError: u32,
    pub RDMAInOctets: u32,
    pub RDMAOutOctets: u32,
    pub RDMAInFrames: u32,
    pub RDMAOutFrames: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub MaxQpCount: u32,
    pub MaxCqCount: u32,
    pub MaxMrCount: u32,
    pub MaxPdCount: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxMwCount: u32,
    pub MaxSrqCount: u32,
    pub MissingCounterMask: u32,
    pub field_NdkInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDK_WMI_ADAPTER_INFO {
    pub Version: u32,
    pub VendorId: u32,
    pub DeviceId: u32,
    pub MaxRegistrationSize: u64,
    pub MaxWindowSize: u64,
    pub FRMRPageCount: u32,
    pub MaxInitiatorRequestSge: u32,
    pub MaxReceiveRequestSge: u32,
    pub MaxReadRequestSge: u32,
    pub MaxTransferLength: u32,
    pub MaxInlineDataSize: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxReceiveQueueDepth: u32,
    pub MaxInitiatorQueueDepth: u32,
    pub MaxSrqDepth: u32,
    pub MaxCqDepth: u32,
    pub LargeRequestThreshold: u32,
    pub MaxCallerData: u32,
    pub MaxCalleeData: u32,
    pub AdapterFlags: u32,
    pub RdmaTechnology: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_WMI_NDK_CAPABILITIES {
    pub MaxQpCount: u32,
    pub MaxCqCount: u32,
    pub MaxMrCount: u32,
    pub MaxPdCount: u32,
    pub MaxInboundReadLimit: u32,
    pub MaxOutboundReadLimit: u32,
    pub MaxMwCount: u32,
    pub MaxSrqCount: u32,
    pub MissingCounterMask: u32,
    pub NdkInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_STATISTICS_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub CounterSet: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_CONNECTION_ENTRY {
    pub Local: u32,
    pub Remote: u32,
    pub UserModeOwner: u8,
    pub OwnerPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_CONNECTIONS {
    pub Header: u32,
    pub Flags: u32,
    pub Count: u32,
    pub NDConnectionsMappedtoTCPConnections: u8,
    pub Connections: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_LOCAL_ENDPOINT_ENTRY {
    pub Local: u32,
    pub UserModeOwner: u8,
    pub Listener: u8,
    pub OwnerPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_LOCAL_ENDPOINTS {
    pub Header: u32,
    pub Flags: u32,
    pub Count: u32,
    pub NDLocalEndpointsMappedtoTCPLocalEndpoints: u8,
    pub LocalEndpoints: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub MaxNumTrafficClasses: u32,
    pub MaxNumEtsCapableTrafficClasses: u32,
    pub MaxNumPfcEnabledTrafficClasses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_CLASSIFICATION_ELEMENT {
    pub Header: u32,
    pub Flags: u32,
    pub ConditionSelector: u16,
    pub ConditionField: u16,
    pub ActionSelector: u16,
    pub ActionField: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub NumTrafficClasses: u32,
    pub PriorityAssignmentTable: [u8; 1],
    pub TcBandwidthAssignmentTable: [u8; 1],
    pub TsaAssignmentTable: [u8; 1],
    pub PfcEnable: u32,
    pub NumClassificationElements: u32,
    pub ClassificationElementSize: u32,
    pub FirstClassificationElementOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchType: u32,
    pub SwitchId: u32,
    pub SwitchFriendlyName: u32,
    pub NumVFs: u32,
    pub NdisReserved1: u32,
    pub NdisReserved2: u32,
    pub NdisReserved3: u32,
    pub NumQueuePairsForDefaultVPort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_DELETE_SWITCH_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchType: u32,
    pub SwitchId: u32,
    pub SwitchFriendlyName: u32,
    pub NumVFs: u32,
    pub NumAllocatedVFs: u32,
    pub NumVPorts: u32,
    pub NumActiveVPorts: u32,
    pub NumQueuePairsForDefaultVPort: u32,
    pub NumQueuePairsForNonDefaultVPorts: u32,
    pub NumActiveDefaultVPortMacAddresses: u32,
    pub NumActiveNonDefaultVPortMacAddresses: u32,
    pub NumActiveDefaultVPortVlanIds: u32,
    pub NumActiveNonDefaultVPortVlanIds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_INFO_ARRAY {
    pub Header: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_REQUEST_PARAMETERS {
    pub RdmaRequested: u8,
    pub SwitchPortId: u32,
    pub IfIndex: u32,
    pub MacAddress: [u8; 1],
    pub VlanId: u32,
    pub NdkReserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_RESPONSE_PARAMETERS {
    pub CapabilitiesAvailable: u8,
    pub ReceivedCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NDK_PARAMETERS {
    pub NdkRequest: u32,
    pub NdkResponse: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VPORT_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub VPortId: u32,
    pub VPortName: u32,
    pub AttachedFunctionId: u32,
    pub NumQueuePairs: u32,
    pub InterruptModeration: u32,
    pub VPortState: u32,
    pub ProcessorAffinity: u32,
    pub LookaheadSize: u32,
    pub NdkParams: u32,
    pub QosSqId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_DELETE_VPORT_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub VPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VPORT_INFO {
    pub Header: u32,
    pub VPortId: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub VPortName: u32,
    pub AttachedFunctionId: u32,
    pub NumQueuePairs: u32,
    pub InterruptModeration: u32,
    pub VPortState: u32,
    pub ProcessorAffinity: u32,
    pub LookaheadSize: u32,
    pub NumFilters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VPORT_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub AttachedFunctionId: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VF_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub VMName: u32,
    pub VMFriendlyName: u32,
    pub NicName: u32,
    pub MacAddressLength: u16,
    pub PermanentMacAddress: [u8; 1],
    pub CurrentMacAddress: [u8; 1],
    pub VFId: u32,
    pub RequestorId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_FREE_VF_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub VFId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VF_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub VMName: u32,
    pub VMFriendlyName: u32,
    pub NicName: u32,
    pub MacAddressLength: u16,
    pub PermanentMacAddress: [u8; 1],
    pub CurrentMacAddress: [u8; 1],
    pub VFId: u32,
    pub RequestorId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_NIC_SWITCH_VF_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchId: u32,
    pub FirstElementOffset: u32,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub SriovCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_READ_VF_CONFIG_SPACE_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
    pub Offset: u32,
    pub Length: u32,
    pub BufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_WRITE_VF_CONFIG_SPACE_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
    pub Offset: u32,
    pub Length: u32,
    pub BufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_READ_VF_CONFIG_BLOCK_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
    pub BlockId: u32,
    pub Length: u32,
    pub BufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_WRITE_VF_CONFIG_BLOCK_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
    pub BlockId: u32,
    pub Length: u32,
    pub BufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_RESET_VF_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_SET_VF_POWER_STATE_PARAMETERS {
    pub Header: u32,
    pub VFId: u32,
    pub PowerState: u32,
    pub WakeEnable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_CONFIG_STATE_PARAMETERS {
    pub Header: u32,
    pub BlockId: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_VF_VENDOR_DEVICE_ID_INFO {
    pub Header: u32,
    pub VFId: u32,
    pub VendorId: u16,
    pub DeviceId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_PROBED_BARS_INFO {
    pub Header: u32,
    pub BaseRegisterValuesOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_RECEIVE_FILTER_MOVE_FILTER_PARAMETERS {
    pub Header: u32,
    pub FilterId: u32,
    pub SourceQueueId: u32,
    pub SourceVPortId: u32,
    pub DestQueueId: u32,
    pub DestVPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_BAR_RESOURCES_INFO {
    pub Header: u32,
    pub VFId: u32,
    pub BarIndex: u16,
    pub BarResourcesOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_PF_LUID_INFO {
    pub Header: u32,
    pub Luid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_VF_SERIAL_NUMBER_INFO {
    pub Header: u32,
    pub SerialNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_VF_INVALIDATE_CONFIG_BLOCK_INFO {
    pub Header: u32,
    pub BlockMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SRIOV_OVERLYING_ADAPTER_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub IfIndex: u32,
    pub NdisReserved1: u32,
    pub NdisReserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ISOLATION_NAME {
    pub Length: u16,
    pub String: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ROUTING_DOMAIN_ISOLATION_ENTRY {
    pub Header: u32,
    pub Flags: u32,
    pub IsolationIdName: u32,
    pub VirtualSubnetId: u32,
    pub VlanId: u32,
    pub IsolationId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ROUTING_DOMAIN_ENTRY {
    pub Header: u32,
    pub Flags: u32,
    pub RoutingDomainId: u32,
    pub RoutingDomainName: u32,
    pub NumIsolationEntries: u32,
    pub FirstIsolationEntryOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_ISOLATION_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub IsolationMode: u32,
    pub AllowUntaggedTraffic: u8,
    pub NumRoutingDomainEntries: u32,
    pub FirstRoutingDomainEntryOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_SECURITY {
    pub Header: u32,
    pub Flags: u32,
    pub AllowMacSpoofing: u8,
    pub AllowIeeePriorityTag: u8,
    pub VirtualSubnetId: u32,
    pub AllowTeaming: u8,
    pub DynamicIPAddressLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_VLAN {
    pub Header: u32,
    pub Flags: u32,
    pub OperationMode: u32,
    pub AccessVlanId: u32,
    pub NativeVlanId: u32,
    pub PruneVlanIdArray: [u64; 64],
    pub TrunkVlanIdArray: [u64; 64],
    pub PrimaryVlanId: u32,
    pub SecondaryVlanId: u32,
    pub SecondaryVlanIdArray: [u64; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_PROFILE {
    pub Header: u32,
    pub Flags: u32,
    pub ProfileName: u32,
    pub ProfileId: u32,
    pub VendorName: u32,
    pub VendorId: u32,
    pub ProfileData: u32,
    pub NetCfgInstanceId: u32,
    pub PciSegmentNumber: u32,
    pub PciBusNumber: u32,
    pub PciDeviceNumber: u32,
    pub PciFunctionNumber: u32,
    pub CdnLabelId: u32,
    pub CdnLabel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_ISOLATION {
    pub Header: u32,
    pub Flags: u32,
    pub IsolationMode: u32,
    pub AllowUntaggedTraffic: u8,
    pub DefaultIsolationId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_ROUTING_DOMAIN {
    pub Header: u32,
    pub Flags: u32,
    pub RoutingDomainId: u32,
    pub RoutingDomainName: u32,
    pub NumIsolationEntries: u32,
    pub FirstIsolationEntryOffset: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_CUSTOM {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub PropertyVersion: u32,
    pub SerializationVersion: u32,
    pub PropertyInstanceId: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_DELETE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub PropertyInstanceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_ENUM_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub SerializationVersion: u32,
    pub FirstPropertyOffset: u32,
    pub NumProperties: u32,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PROPERTY_ENUM_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyVersion: u32,
    pub PropertyInstanceId: u32,
    pub QwordAlignedPropertyBufferLength: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_FEATURE_STATUS_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub FeatureStatusType: u32,
    pub FeatureStatusId: u32,
    pub FeatureStatusVersion: u32,
    pub SerializationVersion: u32,
    pub FeatureStatusInstanceId: u32,
    pub FeatureStatusBufferLength: u32,
    pub FeatureStatusBufferOffset: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_FEATURE_STATUS_CUSTOM {
    pub Header: u32,
    pub Flags: u32,
    pub FeatureStatusBufferLength: u32,
    pub FeatureStatusBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PROPERTY_CUSTOM {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PROPERTY_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub PropertyVersion: u32,
    pub SerializationVersion: u32,
    pub PropertyInstanceId: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PROPERTY_DELETE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub PropertyInstanceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PROPERTY_ENUM_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyInstanceId: u32,
    pub PropertyVersion: u32,
    pub QwordAlignedPropertyBufferLength: u32,
    pub PropertyBufferLength: u32,
    pub PropertyBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PROPERTY_ENUM_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PropertyType: u32,
    pub PropertyId: u32,
    pub SerializationVersion: u32,
    pub FirstPropertyOffset: u32,
    pub NumProperties: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_FEATURE_STATUS_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub FeatureStatusType: u32,
    pub FeatureStatusId: u32,
    pub FeatureStatusInstanceId: u32,
    pub FeatureStatusVersion: u32,
    pub SerializationVersion: u32,
    pub FeatureStatusBufferOffset: u32,
    pub FeatureStatusBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_FEATURE_STATUS_CUSTOM {
    pub Header: u32,
    pub Flags: u32,
    pub FeatureStatusCustomBufferLength: u32,
    pub FeatureStatusCustomBufferOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SwitchName: u32,
    pub SwitchFriendlyName: u32,
    pub NumSwitchPorts: u32,
    pub IsActive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub PortName: u32,
    pub PortFriendlyName: u32,
    pub PortType: u32,
    pub IsValidationPort: u8,
    pub PortState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_PORT_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub FirstElementOffset: u16,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NIC_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub NicName: u32,
    pub NicFriendlyName: u32,
    pub PortId: u32,
    pub NicIndex: u32,
    pub NicType: u32,
    pub NicState: u32,
    pub VmName: u32,
    pub VmFriendlyName: u32,
    pub NetCfgInstanceId: u32,
    pub MTU: u32,
    pub NumaNodeId: u16,
    pub PermanentMacAddress: [u8; 1],
    pub VMMacAddress: [u8; 1],
    pub CurrentMacAddress: [u8; 1],
    pub VFAssigned: u8,
    pub NdisReserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NIC_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub FirstElementOffset: u16,
    pub NumElements: u32,
    pub ElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NIC_OID_REQUEST {
    pub Header: u32,
    pub Flags: u32,
    pub SourcePortId: u32,
    pub SourceNicIndex: u32,
    pub DestinationPortId: u32,
    pub DestinationNicIndex: u32,
    pub OidRequest: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_SWITCH_NIC_SAVE_STATE {
    pub Header: u32,
    pub Flags: u32,
    pub PortId: u32,
    pub NicIndex: u32,
    pub ExtensionId: u32,
    pub ExtensionFriendlyName: u32,
    pub FeatureClassId: u32,
    pub SaveDataSize: u16,
    pub SaveDataOffset: u16,
    pub SaveDataSizeOverflow: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PORT_STATE {
    pub Header: u32,
    pub MediaConnectState: u32,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub Direction: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PORT_CHARACTERISTICS {
    pub Header: u32,
    pub PortNumber: u32,
    pub Flags: u32,
    pub Type: u32,
    pub MediaConnectState: u32,
    pub XmitLinkSpeed: u32,
    pub RcvLinkSpeed: u32,
    pub Direction: u32,
    pub SendControlState: u32,
    pub RcvControlState: u32,
    pub SendAuthorizationState: u32,
    pub RcvAuthorizationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PORT {
    pub Next: usize,
    pub NdisReserved: usize,
    pub MiniportReserved: usize,
    pub ProtocolReserved: usize,
    pub PortCharacteristics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_PORT_ARRAY {
    pub Header: u32,
    pub NumberOfPorts: u32,
    pub OffsetFirstPort: u32,
    pub ElementSize: u32,
    pub Ports: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_ETHERNET_HEADER_FIELDS {
    pub DestinationMacAddress: [u32; 1],
    pub SourceMacAddress: [u32; 1],
    pub EthType: u32,
    pub CustomerVlanId: u32,
    pub ProviderVlanId: u32,
    pub Priority: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_IPV4_ADDRESS_WILDCARD_MATCH {
    pub MatchType: u32,
    pub IPAddress: u32,
    pub PrefixLength: u32,
    pub RangeSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_IPV4_SRC_AND_DEST_ADDRESS_WILDCARD_MATCH {
    pub SourceIPv4Address: u32,
    pub DestinationIPv4Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_IPV6_ADDRESS_WILDCARD_MATCH {
    pub MatchType: u32,
    pub IPAddress: u32,
    pub PrefixLength: u32,
    pub RangeSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_IPV6_SRC_AND_DEST_ADDRESS_WILDCARD_MATCH {
    pub SourceIPv6Address: u32,
    pub DestinationIPv6Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_TRANSPORT_PORT_WILDCARD_MATCH {
    pub MatchType: u32,
    pub Port: u16,
    pub Range: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFP_TRANSPORT_SRC_AND_DEST_PORT_WILDCARD_MATCH {
    pub SourcePort: u32,
    pub DestinationPort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_TABLE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub TableType: u32,
    pub NextTableId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_TABLE_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub TableType: u32,
    pub NextTableId: u32,
    pub NumFlowEntries: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_TABLE_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub TableArrayOffset: u32,
    pub TableArrayNumElements: u32,
    pub TableArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_DELETE_TABLE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_PACKET_COUNTER_VALUE {
    pub Packets: u32,
    pub LastUpdate: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_BYTE_COUNTER_VALUE {
    pub Bytes: u32,
    pub LastUpdate: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_PACKET_BYTE_COUNTER_VALUE {
    pub Packets: u32,
    pub Bytes: u32,
    pub LastUpdate: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FLOW_STATE {
    pub TcpSeqNum: u32,
    pub TcpAckNum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_PACKET_BYTE_COUNTER_VALUE_AND_STATE {
    pub PacketByteCounterValue: u32,
    pub FlowState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_COUNTER_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub CounterUpdateFrequency: u32,
    pub CounterType: u32,
    pub CounterIdStart: u32,
    pub NumCounters: u32,
    pub UpdatePeriod: u32,
    pub PacketCounters: usize,
    pub ByteCounters: usize,
    pub PacketByteCounters: usize,
    pub PacketByteCountersAndState: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FREE_COUNTER_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub CounterIdStart: u32,
    pub NumCounters: u32,
    pub LastCounterValueAndStateArrayOffset: u32,
    pub LastCounterValueAndStateArrayNumElements: u32,
    pub LastCounterValueAndStateArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_COUNTER_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub CounterIdStart: u32,
    pub NumCounters: u32,
    pub CounterUpdateFrequency: u32,
    pub CounterType: u32,
    pub UpdatePeriod: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_COUNTER_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub CounterInfoArrayOffset: u32,
    pub CounterInfoArrayNumElements: u32,
    pub CounterInfoArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_COUNTER_VALUE {
    pub CounterType: u32,
    pub Packets: u32,
    pub Bytes: u32,
    pub PacketsBytes: u32,
    pub PacketsBytesAndState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_COUNTER_VALUE_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub FlowEntryId: u32,
    pub CounterId: u32,
    pub NumCounters: u32,
    pub CounterValueArrayOffset: u32,
    pub CounterValueArrayNumElements: u32,
    pub CounterValueArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_STATISTICS {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub VPortId: u32,
    pub ProviderFlowEntryId: u32,
    pub Packets: u32,
    pub Bytes: u32,
    pub L1CacheHits: u32,
    pub L2CacheHits: u32,
    pub L3CacheHits: u32,
    pub L1CacheMisses: u32,
    pub L2CacheMisses: u32,
    pub L3CacheMisses: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_HEADER_GROUP_TRANSPOSITION_PROFILE {
    pub Header: u32,
    pub Flags: u32,
    pub Action: u32,
    pub HeadersPresent: u32,
    pub HeaderFields: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_CUSTOM_ACTION_PROFILE {
    pub Header: u32,
    pub Flags: u32,
    pub TableType: u32,
    pub ProfileId: u32,
    pub Type: u32,
    pub Length: u32,
    pub Alignment: usize,
    pub ActionProfileData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_HEADER_TRANSPOSITION_PROFILE {
    pub Header: u32,
    pub Flags: u32,
    pub TableType: u32,
    pub ProfileId: u32,
    pub HeaderGroupTranspositionProfileArrayOffset: u32,
    pub HeaderGroupTranspositionProfileArrayNumElements: u32,
    pub HeaderGroupTranspositionProfileArrayElementSize: u32,
    pub CustomActionProfileOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_HEADER_GROUP_TRANSPOSITION {
    pub Header: u32,
    pub Flags: u32,
    pub Action: u32,
    pub HeadersPresent: u32,
    pub HeaderFields: u32,
    pub EthernetFields: u32,
    pub SourceIPAddress: u32,
    pub DestinationIPAddress: u32,
    pub SourceIPAddress_1: u32,
    pub DestinationIPAddress_1: u32,
    pub Ttl: u32,
    pub Dscp: u32,
    pub IPProtocol: u32,
    pub SourcePort: u16,
    pub DestinationPort: u16,
    pub SourcePort_1: u16,
    pub DestinationPort_1: u16,
    pub TenantId: u32,
    pub GreProtocol: u16,
    pub Entropy: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_CUSTOM_ACTION {
    pub Header: u32,
    pub Flags: u32,
    pub ProfileId: u32,
    pub Type: u32,
    pub Length: u32,
    pub Alignment: usize,
    pub ActionData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_EXACT_MATCH_FLOW_ENTRY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub VPortId: u32,
    pub MatchProfileId: u32,
    pub MatchRequestStatus: u32,
    pub ActionRequestStatus: u32,
    pub CounterFlags: u32,
    pub CounterUpdateFrequency: u32,
    pub CounterType: u32,
    pub UpdatePeriod: u32,
    pub PacketCounterAddress: usize,
    pub ByteCounterAddress: usize,
    pub PacketByteCounterAddress: usize,
    pub PacketByteCounterAndStateAddress: usize,
    pub HeaderTranspositionProfileId: u32,
    pub RedirectionVPortId: u32,
    pub TtlIsOneRedirectionVPortId: u32,
    pub NumCounterObjects: u32,
    pub CounterIdArray: [u32; 1],
    pub CacheHint: u32,
    pub ClientFlowEntryId: u32,
    pub ProviderFlowEntryId: u32,
    pub FlowEntryState: u32,
    pub TcpSequenceNumber: u32,
    pub HeaderGroupExactMatchArrayOffset: u32,
    pub HeaderGroupExactMatchArrayNumElements: u32,
    pub HeaderGroupExactMatchArrayElementSize: u32,
    pub HeaderGroupTranspositionArrayOffset: u32,
    pub HeaderGroupTranspositionArrayNumElements: u32,
    pub HeaderGroupTranspositionArrayElementSize: u32,
    pub CustomActionOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_WILDCARD_MATCH_FLOW_ENTRY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub VPortId: u32,
    pub MatchProfileId: u32,
    pub MatchRequestStatus: u32,
    pub ActionRequestStatus: u32,
    pub CounterFlags: u32,
    pub CounterUpdateFrequency: u32,
    pub CounterType: u32,
    pub UpdatePeriod: u32,
    pub PacketCounterAddress: usize,
    pub ByteCounterAddress: usize,
    pub PacketByteCounterAddress: usize,
    pub Priority: u32,
    pub Action: u32,
    pub RedirectionVPortId: u32,
    pub TtlIsOneRedirectionVPortId: u32,
    pub NumCounterObjects: u32,
    pub CounterIdArray: [u32; 1],
    pub CacheHint: u32,
    pub ClientFlowEntryId: u32,
    pub ProviderFlowEntryId: u32,
    pub FlowEntryState: u32,
    pub HeaderGroupWildcardMatchArrayOffset: u32,
    pub HeaderGroupWildcardMatchArrayNumElements: u32,
    pub HeaderGroupWildcardMatchArrayElementSize: u32,
    pub CustomActionOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_PROFILE_INFO {
    pub Header: u32,
    pub Flags: u32,
    pub ProfileType: u32,
    pub TableType: u32,
    pub ProfileId: u32,
    pub NumSupportedFlowEntries: u32,
    pub NumCurrentFlowEntries: u32,
    pub HeaderGroupArrayOffset: u32,
    pub HeaderGroupArrayNumElements: u32,
    pub HeaderGroupArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_PROFILE_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub ProfileInfoArrayOffset: u32,
    pub ProfileInfoArrayNumElements: u32,
    pub ProfileInfoArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_DELETE_PROFILE_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub ProfileId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FLOW_ENTRY_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub EntryType: u32,
    pub FlowEntryArrayOffset: u32,
    pub FlowEntryArrayNumElements: u32,
    pub FlowEntryArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FLOW_ENTRY_INFO_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub VPortId: u32,
    pub ProviderFlowEntryIdStart: u32,
    pub FlowEntryIdCount: u32,
    pub FlowEntryInfoArrayOffset: u32,
    pub FlowEntryInfoArrayNumElements: u32,
    pub FlowEntryInfoArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FLOW_ENTRY_ID_AND_COUNTER {
    pub FlowEntryId: u32,
    pub CounterValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_FLOW_ENTRY_ID_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub TableId: u32,
    pub VPortId: u32,
    pub ProviderFlowEntryIdArrayOffset: u32,
    pub ProviderFlowEntryIdArrayNumElements: u32,
    pub ProviderFlowEntryIdArrayElementSize: u32,
    pub ProviderFlowEntryIdAndCounterArrayOffset: u32,
    pub ProviderFlowEntryIdAndCounterArrayNumElements: u32,
    pub ProviderFlowEntryIdAndCounterArrayElementSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_OFFLOAD_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub ExceptionVPortId: u32,
    pub CopyLookaheadLength: u32,
    pub SampleLookaheadLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_OFFLOAD_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub CounterCapabilities: u32,
    pub SupportedTableTypes: u32,
    pub SupportedEncapsulationTypes: u32,
    pub SupportedIngressExactMatchTableActions: u32,
    pub SupportedEgressExactMatchTableActions: u32,
    pub SoftwareSupportedIngressExactMatchTableActions: u32,
    pub SoftwareSupportedEgressExactMatchTableActions: u32,
    pub SupportedIngressWildcardMatchTableActions: u32,
    pub SupportedEgressWildcardMatchTableActions: u32,
    pub SoftwareSupportedIngressWildcardMatchTableActions: u32,
    pub SoftwareSupportedEgressWildcardMatchTableActions: u32,
    pub NumPacketCounterObjects: u32,
    pub NumByteCounterObjects: u32,
    pub NumPacketByteCounterObjects: u32,
    pub NumPacketByteCounterAndStateObjects: u32,
    pub NumCounterObjectsPerIngressExactMatchFlowEntry: u32,
    pub NumCounterObjectsPerEgressExactMatchFlowEntry: u32,
    pub NumCounterObjectsPerIngressWildcardMatchFlowEntry: u32,
    pub NumCounterObjectsPerEgressWildcardMatchFlowEntry: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_GFT_VPORT_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub VPortId: u32,
    pub ExceptionVPortId: u32,
    pub SamplingRate: u32,
    pub DscpMask: u32,
    pub NumDscpMaskCounterObjects: u32,
    pub DscpMaskCounterIdArray: [u32; 1],
    pub ULONG64_______PriorityMask: u32,
    pub NumPriorityMaskCounterObjects: u32,
    pub PriorityMaskCounterIdArray: [u32; 1],
    pub VxLanSrcPortBase: u16,
    pub VxLanSrcPortRange: u16,
    pub DscpFlags: u32,
    pub CustomProviderReservedPointer: usize,
    pub CustomProviderReservedValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_SQ_PARAMETERS {
    pub Header: u32,
    pub Flags: u32,
    pub SqId: u32,
    pub SqType: u32,
    pub TcEnabledTable: [u8; 1],
    pub TcTransmitBandwidthCapTable: [u32; 1],
    pub TcTransmitBandwidthReservationTable: [u32; 1],
    pub TcReceiveBandwidthCapTable: [u32; 1],
    pub CrossTcTransmitBandwidthCap: u32,
    pub MaxNumSqInputs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_SQ_PARAMETERS_ENUM_ARRAY {
    pub Header: u32,
    pub Flags: u32,
    pub SqType: u32,
    pub FirstSqId: u32,
    pub MaxSqsToReturn: u32,
    pub SqArrayOffset: u32,
    pub SqParamsArrayOffset: u32,
    pub SqArrayNumElements: u32,
    pub SqParamsArrayNumElements: u32,
    pub SqArrayElementSize: u32,
    pub SqParamsArrayElementSize: u32,
    pub SqParamsArrayOffset_1: u32,
    pub SqParamsArrayNumElements_1: u32,
    pub SqParamsArrayElementSize_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_OFFLOAD_CAPABILITIES {
    pub Header: u32,
    pub Flags: u32,
    pub SupportedSqTypes: u32,
    pub TransmitCapSupported: [u8; 1],
    pub TransmitReservationSupported: [u8; 1],
    pub ReceiveCapSupported: [u8; 1],
    pub TransmitGftCapSupported: [u8; 1],
    pub ReceiveGftCapSupported: [u8; 1],
    pub TcSupportedTable: [u8; 1],
    pub NumStandardSqsSupported: u32,
    pub NumGftSqsSupported: u32,
    pub ReservationGranularitySupported: u32,
    pub MaxNumSqInputs: u32,
    pub CrossTcTransmitMaxCapSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_QOS_SQ_STATS {
    pub Header: u32,
    pub Flags: u32,
    pub SqId: u32,
    pub SqType: u32,
    pub BytesTransmitted: [u64; 1],
    pub PktsTransmitted: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TIMESTAMP_CAPABILITY_FLAGS {
    pub PtpV2OverUdpIPv4EventMsgReceiveHw: u8,
    pub PtpV2OverUdpIPv4AllMsgReceiveHw: u8,
    pub PtpV2OverUdpIPv4EventMsgTransmitHw: u8,
    pub PtpV2OverUdpIPv4AllMsgTransmitHw: u8,
    pub PtpV2OverUdpIPv6EventMsgReceiveHw: u8,
    pub PtpV2OverUdpIPv6AllMsgReceiveHw: u8,
    pub PtpV2OverUdpIPv6EventMsgTransmitHw: u8,
    pub PtpV2OverUdpIPv6AllMsgTransmitHw: u8,
    pub AllReceiveHw: u8,
    pub AllTransmitHw: u8,
    pub TaggedTransmitHw: u8,
    pub AllReceiveSw: u8,
    pub AllTransmitSw: u8,
    pub TaggedTransmitSw: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_TIMESTAMP_CAPABILITIES {
    pub Header: u32,
    pub HardwareClockFrequencyHz: u32,
    pub CrossTimestamp: u8,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub TimestampFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_HARDWARE_CROSSTIMESTAMP {
    pub Header: u32,
    pub Flags: u32,
    pub SystemTimestamp1: u32,
    pub HardwareClockTimestamp: u32,
    pub SystemTimestamp2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAR_QUERY_INFORMATION {
    pub Status: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAR_SET_INFORMATION {
    pub Init: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAR_DEVICE_ID_SIZE_INFORMATION {
    pub DeviceIdSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARCLASS_NEGOTIATION_MASK {
    pub usReadMask: u16,
    pub usWriteMask: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_HEALTH_NOTIFICATION_DATA {
    pub DeviceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_LOGICAL_DEVICE_INSTANCE {
    pub Version: u32,
    pub Size: u32,
    pub DeviceGuid: u32,
    pub SymbolicLink: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_LOGICAL_DEVICES {
    pub Version: u32,
    pub Size: u32,
    pub DeviceCount: u32,
    pub Devices: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PHYSICAL_DEVICE_INSTANCE {
    pub Version: u32,
    pub Size: u32,
    pub NfitHandle: u32,
    pub SymbolicLink: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PHYSICAL_DEVICES {
    pub Version: u32,
    pub Size: u32,
    pub DeviceCount: u32,
    pub Devices: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_REGION {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub NfitHandle: u32,
    pub LogicalDeviceGuid: u32,
    pub AddressRangeType: u32,
    pub AssociatedId: u32,
    pub Length: u32,
    pub StartingDPA: u32,
    pub BaseSPA: u32,
    pub SPAOffset: u32,
    pub RegionOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_REGIONS {
    pub Version: u32,
    pub Size: u32,
    pub RegionCount: u32,
    pub Regions: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_PROPERTY_QUERY {
    pub Version: u32,
    pub Size: u32,
    pub PropertyId: u32,
    pub QueryType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_RUNTIME_FW_ACTIVATION_INFO {
    pub Version: u32,
    pub Size: u32,
    pub RuntimeFwActivationSupported: u8,
    pub FirmwareActivationState: u32,
    pub FwManagedIoQuiesceFwActivationSupported: u32,
    pub OsManagedIoQuiesceFwActivationSupported: u32,
    pub WarmResetBasedFwActivationSupported: u32,
    pub Reserved: u32,
    pub EstimatedFirmwareActivationTimeInUSecs: u64,
    pub EstimatedProcessorAccessQuiesceTimeInUSecs: u64,
    pub EstimatedIOAccessQuiesceTimeInUSecs: u64,
    pub PlatformSupportedMaxIOAccessQuiesceTimeInUSecs: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_DEDICATED_MEMORY_DEVICE_INFO {
    pub DeviceGuid: u32,
    pub DeviceNumber: u32,
    pub ForcedByRegistry: u32,
    pub Initialized: u32,
    pub Reserved: u32,
    pub DeviceSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_DEDICATED_MEMORY_DEVICES_INFO {
    pub Version: u32,
    pub Size: u32,
    pub DeviceCount: u32,
    pub Devices: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_PROPERTY_SET {
    pub Version: u32,
    pub Size: u32,
    pub PropertyId: u32,
    pub SetType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_BUS_DEDICATED_MEMORY_STATE {
    pub ActivateState: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_INTERLEAVED_PD_INFO {
    pub DeviceHandle: u32,
    pub DeviceGuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_LD_INTERLEAVE_SET_INFO {
    pub Version: u32,
    pub Size: u32,
    pub InterleaveSetSize: u32,
    pub InterleaveSet: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PROPERTY_QUERY {
    pub Version: u32,
    pub Size: u32,
    pub PropertyId: u32,
    pub QueryType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PROPERTY_SET {
    pub Version: u32,
    pub Size: u32,
    pub PropertyId: u32,
    pub SetType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_RUNTIME_FW_ACTIVATION_ARM_STATE {
    pub ArmState: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_DESCRIPTOR_HEADER {
    pub Version: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_DEVICE_HANDLE {
    pub Version: u32,
    pub Size: u32,
    pub DeviceGuid: u32,
    pub DeviceHandle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_DEVICE_INFO {
    pub Version: u32,
    pub Size: u32,
    pub DeviceGuid: u32,
    pub UnsafeShutdownCount: u32,
    pub PersistentMemorySizeInBytes: u32,
    pub VolatileMemorySizeInBytes: u32,
    pub TotalMemorySizeInBytes: u32,
    pub SlotNumber: u32,
    pub DeviceHandle: u32,
    pub PhysicalId: u16,
    pub NumberOfFormatInterfaceCodes: u8,
    pub FormatInterfaceCodes: [u16; 1],
    pub VendorId: u32,
    pub ProductId: u32,
    pub SubsystemDeviceId: u32,
    pub SubsystemVendorId: u32,
    pub ManufacturingLocation: u8,
    pub ManufacturingWeek: u8,
    pub ManufacturingYear: u8,
    pub SerialNumber4Byte: u32,
    pub SerialNumberLengthInChars: u32,
    pub SerialNumber: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_DEVICE_SPECIFIC_PROPERTY {
    pub Name: [u32; 1],
    pub Value: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_DEVICE_SPECIFIC_INFO {
    pub Version: u32,
    pub Size: u32,
    pub NumberOfProperties: u32,
    pub DeviceSpecificProperties: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_FIRMWARE_SLOT_INFO {
    pub Version: u32,
    pub Size: u32,
    pub SlotNumber: u8,
    pub ReadOnly: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 6],
    pub Revision: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_FIRMWARE_INFO {
    pub Version: u32,
    pub Size: u32,
    pub ActiveSlot: u8,
    pub NextActiveSlot: u8,
    pub SlotCount: u8,
    pub Slots: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_MANAGEMENT_STATUS {
    pub Version: u32,
    pub Size: u32,
    pub Health: u32,
    pub NumberOfOperationalStatus: u32,
    pub NumberOfAdditionalReasons: u32,
    pub OperationalStatus: [u32; 1],
    pub AdditionalReasons: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_LOCATION_STRING {
    pub Version: u32,
    pub Size: u32,
    pub Location: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_FRU_ID_STRING {
    pub Version: u32,
    pub Size: u32,
    pub IdentifierSize: u32,
    pub Identifier: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_FIRMWARE_DOWNLOAD {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Slot: u8,
    pub Reserved: [u8; 3],
    pub Offset: u32,
    pub FirmwareImageSizeInBytes: u32,
    pub FirmwareImage: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_FIRMWARE_ACTIVATE {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Slot: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_RUNTIME_FW_ACTIVATION_INFO {
    pub Version: u32,
    pub Size: u32,
    pub LastFirmwareActivationStatus: u32,
    pub FirmwareActivationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PASSTHROUGH_INPUT {
    pub Version: u32,
    pub Size: u32,
    pub ProtocolGuid: u32,
    pub DataSize: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PASSTHROUGH_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub ProtocolGuid: u32,
    pub DataSize: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PASSTHROUGH_INVDIMM_INPUT {
    pub Opcode: u32,
    pub OpcodeParametersLength: u32,
    pub OpcodeParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_PASSTHROUGH_INVDIMM_OUTPUT {
    pub GeneralStatus: u16,
    pub ExtendedStatus: u16,
    pub OutputDataLength: u32,
    pub OutputData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_REINITIALIZE_MEDIA_INPUT {
    pub Version: u32,
    pub Size: u32,
    pub Overwrite: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCM_PD_REINITIALIZE_MEDIA_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH {
    pub Length: u16,
    pub ScsiStatus: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub CdbLength: u8,
    pub SenseInfoLength: u8,
    pub DataIn: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBufferOffset: usize,
    pub SenseInfoOffset: u32,
    pub Cdb: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_DIRECT {
    pub Length: u16,
    pub ScsiStatus: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub CdbLength: u8,
    pub SenseInfoLength: u8,
    pub DataIn: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: usize,
    pub SenseInfoOffset: u32,
    pub Cdb: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH32 {
    pub Length: u16,
    pub ScsiStatus: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub CdbLength: u8,
    pub SenseInfoLength: u8,
    pub DataIn: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBufferOffset: u32,
    pub SenseInfoOffset: u32,
    pub Cdb: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_DIRECT32 {
    pub Length: u16,
    pub ScsiStatus: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub CdbLength: u8,
    pub SenseInfoLength: u8,
    pub DataIn: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub DataBuffer: u32,
    pub SenseInfoOffset: u32,
    pub Cdb: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_EX {
    pub Version: u32,
    pub Length: u32,
    pub CdbLength: u32,
    pub StorAddressLength: u32,
    pub ScsiStatus: u8,
    pub SenseInfoLength: u8,
    pub DataDirection: u8,
    pub Reserved: u8,
    pub TimeOutValue: u32,
    pub StorAddressOffset: u32,
    pub SenseInfoOffset: u32,
    pub DataOutTransferLength: u32,
    pub DataInTransferLength: u32,
    pub DataOutBufferOffset: usize,
    pub DataInBufferOffset: usize,
    pub Cdb: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_DIRECT_EX {
    pub Version: u32,
    pub Length: u32,
    pub CdbLength: u32,
    pub StorAddressLength: u32,
    pub ScsiStatus: u8,
    pub SenseInfoLength: u8,
    pub DataDirection: u8,
    pub Reserved: u8,
    pub TimeOutValue: u32,
    pub StorAddressOffset: u32,
    pub SenseInfoOffset: u32,
    pub DataOutTransferLength: u32,
    pub DataInTransferLength: u32,
    pub DataOutBuffer: usize,
    pub DataInBuffer: usize,
    pub Cdb: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH32_EX {
    pub Version: u32,
    pub Length: u32,
    pub CdbLength: u32,
    pub StorAddressLength: u32,
    pub ScsiStatus: u8,
    pub SenseInfoLength: u8,
    pub DataDirection: u8,
    pub Reserved: u8,
    pub TimeOutValue: u32,
    pub StorAddressOffset: u32,
    pub SenseInfoOffset: u32,
    pub DataOutTransferLength: u32,
    pub DataInTransferLength: u32,
    pub DataOutBufferOffset: u32,
    pub DataInBufferOffset: u32,
    pub Cdb: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_PASS_THROUGH_DIRECT32_EX {
    pub Version: u32,
    pub Length: u32,
    pub CdbLength: u32,
    pub StorAddressLength: u32,
    pub ScsiStatus: u8,
    pub SenseInfoLength: u8,
    pub DataDirection: u8,
    pub Reserved: u8,
    pub TimeOutValue: u32,
    pub StorAddressOffset: u32,
    pub SenseInfoOffset: u32,
    pub DataOutTransferLength: u32,
    pub DataInTransferLength: u32,
    pub DataOutBuffer: u32,
    pub DataInBuffer: u32,
    pub Cdb: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASS_THROUGH_EX {
    pub Length: u16,
    pub AtaFlags: u16,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub ReservedAsUchar: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub ReservedAsUlong: u32,
    pub DataBufferOffset: usize,
    pub PreviousTaskFile: [u8; 8],
    pub CurrentTaskFile: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASS_THROUGH_DIRECT {
    pub Length: u16,
    pub AtaFlags: u16,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub ReservedAsUchar: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub ReservedAsUlong: u32,
    pub DataBuffer: usize,
    pub PreviousTaskFile: [u8; 8],
    pub CurrentTaskFile: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASS_THROUGH_EX32 {
    pub Length: u16,
    pub AtaFlags: u16,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub ReservedAsUchar: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub ReservedAsUlong: u32,
    pub DataBufferOffset: u32,
    pub PreviousTaskFile: [u8; 8],
    pub CurrentTaskFile: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ATA_PASS_THROUGH_DIRECT32 {
    pub Length: u16,
    pub AtaFlags: u16,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub ReservedAsUchar: u8,
    pub DataTransferLength: u32,
    pub TimeOutValue: u32,
    pub ReservedAsUlong: u32,
    pub DataBuffer: u32,
    pub PreviousTaskFile: [u8; 8],
    pub CurrentTaskFile: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH {
    pub PassThrough: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH_DIRECT {
    pub PassThrough: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH_EX {
    pub PassThroughOffset: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH_DIRECT_EX {
    pub PassThroughOffset: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH32 {
    pub PassThrough: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH_DIRECT32 {
    pub PassThrough: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH32_EX {
    pub PassThroughOffset: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MPIO_PASS_THROUGH_PATH_DIRECT32_EX {
    pub PassThroughOffset: u32,
    pub Version: u32,
    pub Length: u16,
    pub Flags: u8,
    pub PortNumber: u8,
    pub MpioPathId: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_BUS_DATA {
    pub NumberOfLogicalUnits: u8,
    pub InitiatorBusId: u8,
    pub InquiryDataOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_ADAPTER_BUS_INFO {
    pub NumberOfBuses: u8,
    pub BusData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_INQUIRY_DATA {
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub DeviceClaimed: u8,
    pub InquiryDataLength: u32,
    pub NextInquiryDataOffset: u32,
    pub InquiryData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SRB_IO_CONTROL {
    pub HeaderLength: u32,
    pub Signature: [u8; 8],
    pub Timeout: u32,
    pub ControlCode: u32,
    pub ReturnCode: u32,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVCACHE_REQUEST_BLOCK {
    pub NRBSize: u32,
    pub Function: u16,
    pub NRBFlags: u32,
    pub NRBStatus: u32,
    pub Count: u32,
    pub LBA: u64,
    pub DataBufSize: u32,
    pub NVCacheStatus: u32,
    pub NVCacheSubStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NV_FEATURE_PARAMETER {
    pub NVPowerModeEnabled: u16,
    pub NVParameterReserv1: u16,
    pub NVCmdEnabled: u16,
    pub NVParameterReserv2: u16,
    pub NVPowerModeVer: u16,
    pub NVCmdVer: u16,
    pub NVSize: u32,
    pub NVReadSpeed: u16,
    pub NVWrtSpeed: u16,
    pub DeviceSpinUpTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVCACHE_HINT_PAYLOAD {
    pub Command: u8,
    pub Feature7_0: u8,
    pub Feature15_8: u8,
    pub Count15_8: u8,
    pub LBA7_0: u8,
    pub LBA15_8: u8,
    pub LBA23_16: u8,
    pub LBA31_24: u8,
    pub LBA39_32: u8,
    pub LBA47_40: u8,
    pub Auxiliary7_0: u8,
    pub Auxiliary23_16: u8,
    pub Reserved: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NV_SEP_CACHE_PARAMETER {
    pub Version: u32,
    pub Size: u32,
    pub WriteCacheEnabled: u8,
    pub WriteCacheChangeable: u8,
    pub WriteThroughIOSupported: u8,
    pub FlushCacheSupported: u8,
    pub ReservedBits: u8,
    pub CacheFlagsSet: u8,
    pub WriteCacheType: u8,
    pub WriteCacheTypeEffective: u8,
    pub ParameterReserve1: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DIAGNOSTIC_MP_REQUEST {
    pub Version: u32,
    pub Size: u32,
    pub TargetType: u32,
    pub Level: u32,
    pub ProviderId: u32,
    pub BufferSize: u32,
    pub Reserved: u32,
    pub DataBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MP_DEVICE_DATA_SET_RANGE {
    pub StartingOffset: i64,
    pub LengthInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSM_NOTIFICATION_REQUEST_BLOCK {
    pub Size: u32,
    pub Version: u32,
    pub NotifyFlags: u32,
    pub DataSetProfile: u32,
    pub Reserved: [u32; 3],
    pub DataSetRangesCount: u32,
    pub DataSetRanges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HYBRID_REQUEST_BLOCK {
    pub Version: u32,
    pub Size: u32,
    pub Function: u32,
    pub Flags: u32,
    pub DataBufferOffset: u32,
    pub DataBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVCACHE_PRIORITY_LEVEL_DESCRIPTOR {
    pub PriorityLevel: u8,
    pub Reserved0: [u8; 3],
    pub ConsumedNVMSizeFraction: u32,
    pub ConsumedMappingResourcesFraction: u32,
    pub ConsumedNVMSizeForDirtyDataFraction: u32,
    pub ConsumedMappingResourcesForDirtyDataFraction: u32,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HYBRID_INFORMATION {
    pub Version: u32,
    pub Size: u32,
    pub HybridSupported: u8,
    pub Status: u32,
    pub CacheTypeEffective: u32,
    pub CacheTypeDefault: u32,
    pub FractionBase: u32,
    pub CacheSize: u64,
    pub WriteCacheChangeable: u32,
    pub WriteThroughIoSupported: u32,
    pub FlushCacheSupported: u32,
    pub Removable: u32,
    pub ReservedBits: u32,
    pub PriorityLevelCount: u8,
    pub MaxPriorityBehavior: u8,
    pub OptimalWriteGranularity: u8,
    pub Reserved: u8,
    pub DirtyThresholdLow: u32,
    pub DirtyThresholdHigh: u32,
    pub CacheDisable: u32,
    pub SetDirtyThreshold: u32,
    pub PriorityDemoteBySize: u32,
    pub PriorityChangeByLbaRange: u32,
    pub Evict: u32,
    pub ReservedBits_1: u32,
    pub MaxEvictCommands: u32,
    pub MaxLbaRangeCountForEvict: u32,
    pub MaxLbaRangeCountForChangeLba: u32,
    pub Priority: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HYBRID_DIRTY_THRESHOLDS {
    pub Version: u32,
    pub Size: u32,
    pub DirtyLowThreshold: u32,
    pub DirtyHighThreshold: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HYBRID_DEMOTE_BY_SIZE {
    pub Version: u32,
    pub Size: u32,
    pub SourcePriority: u8,
    pub TargetPriority: u8,
    pub Reserved0: u16,
    pub Reserved1: u32,
    pub LbaCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FIRMWARE_REQUEST_BLOCK {
    pub Version: u32,
    pub Size: u32,
    pub Function: u32,
    pub Flags: u32,
    pub DataBufferOffset: u32,
    pub DataBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_SLOT_INFO {
    pub SlotNumber: u8,
    pub ReadOnly: u8,
    pub Reserved: [u8; 6],
    pub Info: [u8; 8],
    pub AsUlonglong: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_SLOT_INFO_V2 {
    pub SlotNumber: u8,
    pub ReadOnly: u8,
    pub Reserved: [u8; 6],
    pub Revision: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_INFO {
    pub Version: u32,
    pub Size: u32,
    pub UpgradeSupport: u8,
    pub SlotCount: u8,
    pub ActiveSlot: u8,
    pub PendingActivateSlot: u8,
    pub Reserved: u32,
    pub Slot: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_INFO_V2 {
    pub Version: u32,
    pub Size: u32,
    pub UpgradeSupport: u8,
    pub SlotCount: u8,
    pub ActiveSlot: u8,
    pub PendingActivateSlot: u8,
    pub FirmwareShared: u8,
    pub Reserved: [u8; 3],
    pub ImagePayloadAlignment: u32,
    pub ImagePayloadMaxSize: u32,
    pub Slot: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_DOWNLOAD {
    pub Version: u32,
    pub Size: u32,
    pub Offset: u64,
    pub BufferSize: u64,
    pub ImageBuffer: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_DOWNLOAD_V2 {
    pub Version: u32,
    pub Size: u32,
    pub Offset: u64,
    pub BufferSize: u64,
    pub Slot: u8,
    pub Reserved: [u8; 3],
    pub ImageSize: u32,
    pub ImageBuffer: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FIRMWARE_ACTIVATE {
    pub Version: u32,
    pub Size: u32,
    pub SlotToActivate: u8,
    pub Reserved0: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_SCSI_CAPABILITIES {
    pub Length: u32,
    pub MaximumTransferLength: u32,
    pub MaximumPhysicalPages: u32,
    pub SupportedAsynchronousEvents: u32,
    pub AlignmentMask: u32,
    pub TaggedQueuing: u8,
    pub AdapterScansDown: u8,
    pub AdapterUsesPio: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSI_ADDRESS {
    pub Length: u32,
    pub PortNumber: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUMP_POINTERS_VERSION {
    pub Version: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUMP_POINTERS {
    pub field_AdapterObject: u32,
    pub MappedRegisterBase: usize,
    pub DumpData: usize,
    pub CommonBufferVa: usize,
    pub CommonBufferPa: i64,
    pub CommonBufferSize: u32,
    pub AllocateCommonBuffers: u8,
    pub Spare1: [u8; 3],
    pub UseDiskDump: u8,
    pub Spare1_1: [u8; 2],
    pub DeviceObject: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUMP_POINTERS_EX {
    pub Header: u32,
    pub DumpData: usize,
    pub CommonBufferVa: usize,
    pub CommonBufferSize: u32,
    pub AllocateCommonBuffers: u8,
    pub DeviceObject: usize,
    pub DriverList: usize,
    pub dwPortFlags: u32,
    pub MaxDeviceDumpSectionSize: u32,
    pub MaxDeviceDumpLevel: u32,
    pub MaxTransferSize: u32,
    pub AdapterObject: usize,
    pub MappedRegisterBase: usize,
    pub DeviceReady: *mut u8,
    pub DumpDevicePowerOn: usize,
    pub DumpDevicePowerOnContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUMP_DRIVER {
    pub DumpDriverList: usize,
    pub DriverName: [u32; 1],
    pub BaseName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NTSCSI_UNICODE_STRING {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Buffer: usize,
    pub Buffer_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DUMP_DRIVER_EX {
    pub DumpDriverList: usize,
    pub DriverName: [u32; 1],
    pub BaseName: [u32; 1],
    pub DriverFullPath: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ENDURANCE_INFO {
    pub ValidFields: u32,
    pub GroupId: u32,
    pub Shared: u32,
    pub Reserved: u32,
    pub LifePercentage: u32,
    pub BytesReadCount: [u8; 16],
    pub ByteWriteCount: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ENDURANCE_DATA_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub EnduranceInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIALPERF_STATS {
    pub ReceivedCount: u32,
    pub TransmittedCount: u32,
    pub FrameErrorCount: u32,
    pub SerialOverrunErrorCount: u32,
    pub BufferOverrunErrorCount: u32,
    pub ParityErrorCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIALCONFIG {
    pub Size: u32,
    pub Version: u16,
    pub SubType: u32,
    pub ProvOffset: u32,
    pub ProviderSize: u32,
    pub ProviderData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_LINE_CONTROL {
    pub StopBits: u8,
    pub Parity: u8,
    pub WordLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_TIMEOUTS {
    pub ReadIntervalTimeout: u32,
    pub ReadTotalTimeoutMultiplier: u32,
    pub ReadTotalTimeoutConstant: u32,
    pub WriteTotalTimeoutMultiplier: u32,
    pub WriteTotalTimeoutConstant: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_QUEUE_SIZE {
    pub InSize: u32,
    pub OutSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_BAUD_RATE {
    pub BaudRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_CHARS {
    pub EofChar: u8,
    pub ErrorChar: u8,
    pub BreakChar: u8,
    pub EventChar: u8,
    pub XonChar: u8,
    pub XoffChar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_HANDFLOW {
    pub ControlHandShake: u32,
    pub FlowReplace: u32,
    pub XonLimit: i32,
    pub XoffLimit: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_BASIC_SETTINGS {
    pub Timeouts: u32,
    pub HandFlow: u32,
    pub RxFifo: u32,
    pub TxFifo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_STATUS {
    pub Errors: u32,
    pub HoldReasons: u32,
    pub AmountInInQueue: u32,
    pub AmountInOutQueue: u32,
    pub EofReceived: u8,
    pub WaitForImmediate: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_XOFF_COUNTER {
    pub Timeout: u32,
    pub Counter: i32,
    pub XoffChar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERIAL_COMMPROP {
    pub PacketLength: u16,
    pub PacketVersion: u16,
    pub ServiceMask: u32,
    pub Reserved1: u32,
    pub MaxTxQueue: u32,
    pub MaxRxQueue: u32,
    pub MaxBaud: u32,
    pub ProvSubType: u32,
    pub ProvCapabilities: u32,
    pub SettableParams: u32,
    pub SettableBaud: u32,
    pub SettableData: u16,
    pub SettableStopParity: u16,
    pub CurrentTxQueue: u32,
    pub CurrentRxQueue: u32,
    pub ProvSpec1: u32,
    pub ProvSpec2: u32,
    pub ProvChar: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERENUM_PORT_DESC {
    pub Size: u32,
    pub PortHandle: usize,
    pub PortAddress: u64,
    pub Reserved: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SERENUM_PORT_PARAMETERS {
    pub Size: u32,
    pub ReadAccessor: usize,
    pub WriteAccessor: usize,
    pub SerPortAddress: usize,
    pub HardwareHandle: usize,
    pub Portion: u32,
    pub NumberAxis: u16,
    pub reserved_padding: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HOTPLUG_INFO {
    pub Size: u32,
    pub MediaRemovable: u8,
    pub MediaHotplug: u8,
    pub DeviceHotplug: u8,
    pub WriteCacheEnableOverride: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FEATURE_SUPPORT {
    pub Size: u32,
    pub Version: u32,
    pub StorMQMiniportsSupported: u64,
    pub Reserved: u64,
    pub AsUlonglong: u64,
    pub Reserved_1: [u64; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_NUMBER {
    pub DeviceType: u32,
    pub DeviceNumber: u32,
    pub PartitionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_NUMBERS {
    pub Version: u32,
    pub Size: u32,
    pub NumberOfDevices: u32,
    pub Devices: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_NUMBER_EX {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub DeviceType: u32,
    pub DeviceNumber: u32,
    pub DeviceGuid: u32,
    pub PartitionNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_BUS_RESET_REQUEST {
    pub PathId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PREVENT_MEDIA_REMOVAL {
    pub PreventMediaRemoval: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CLASS_MEDIA_CHANGE_CONTEXT {
    pub MediaChangeCount: u32,
    pub NewState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROPERTY_QUERY {
    pub PropertyId: u32,
    pub QueryType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROPERTY_SET {
    pub PropertyId: u32,
    pub SetType: u32,
    pub AdditionalParameters: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DESCRIPTOR_HEADER {
    pub Version: u32,
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub DeviceType: u8,
    pub DeviceTypeModifier: u8,
    pub RemovableMedia: u8,
    pub CommandQueueing: u8,
    pub VendorIdOffset: u32,
    pub ProductIdOffset: u32,
    pub ProductRevisionOffset: u32,
    pub SerialNumberOffset: u32,
    pub BusType: u32,
    pub RawPropertiesLength: u32,
    pub RawDeviceProperties: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ADAPTER_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub MaximumTransferLength: u32,
    pub MaximumPhysicalPages: u32,
    pub AlignmentMask: u32,
    pub AdapterUsesPio: u8,
    pub AdapterScansDown: u8,
    pub CommandQueueing: u8,
    pub AcceleratedTransfer: u8,
    pub BusType: u8,
    pub BusType_1: u8,
    pub BusMajorVersion: u16,
    pub BusMinorVersion: u16,
    pub SrbType: u8,
    pub AddressType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ACCESS_ALIGNMENT_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub BytesPerCacheLine: u32,
    pub BytesOffsetForCacheAlignment: u32,
    pub BytesPerLogicalSector: u32,
    pub BytesPerPhysicalSector: u32,
    pub BytesOffsetForSectorAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_MEDIUM_PRODUCT_TYPE_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub MediumProductType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_MINIPORT_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub Portdriver: u32,
    pub LUNResetSupported: u8,
    pub TargetResetSupported: u8,
    pub IoTimeoutValue: u16,
    pub ExtraIoInfoSupported: u8,
    pub LogicalPoFxForDisk: u8,
    pub ForwardIo: u8,
    pub Reserved: u8,
    pub AsUCHAR: u8,
    pub Reserved0: [u8; 2],
    pub Reserved0_1: [u8; 3],
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_IDENTIFIER {
    pub CodeSet: u32,
    pub Type: u32,
    pub IdentifierSize: u16,
    pub NextOffset: u16,
    pub Association: u32,
    pub Identifier: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_ID_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub NumberOfIdentifiers: u32,
    pub Identifiers: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_SEEK_PENALTY_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub IncursSeekPenalty: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_WRITE_AGGREGATION_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub BenefitsFromWriteAggregation: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_TRIM_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub TrimEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_LB_PROVISIONING_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub ThinProvisioningEnabled: u8,
    pub ThinProvisioningReadZeros: u8,
    pub AnchorSupported: u8,
    pub UnmapGranularityAlignmentValid: u8,
    pub GetFreeSpaceSupported: u8,
    pub MapSupported: u8,
    pub Reserved1: [u8; 7],
    pub OptimalUnmapGranularity: u64,
    pub UnmapGranularityAlignment: u64,
    pub MaxUnmapLbaCount: u32,
    pub MaxUnmapBlockDescriptorCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_LB_PROVISIONING_MAP_RESOURCES {
    pub Size: u32,
    pub Version: u32,
    pub AvailableMappingResourcesValid: u8,
    pub UsedMappingResourcesValid: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 3],
    pub AvailableMappingResourcesScope: u8,
    pub UsedMappingResourcesScope: u8,
    pub Reserved2: u8,
    pub Reserved3: [u8; 3],
    pub AvailableMappingResources: u64,
    pub UsedMappingResources: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_POWER_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub DeviceAttentionSupported: u8,
    pub AsynchronousNotificationSupported: u8,
    pub IdlePowerManagementEnabled: u8,
    pub D3ColdEnabled: u8,
    pub D3ColdSupported: u8,
    pub NoVerifyDuringIdlePower: u8,
    pub Reserved: [u8; 2],
    pub IdleTimeoutInMS: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_COPY_OFFLOAD_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub MaximumTokenLifetime: u32,
    pub DefaultTokenLifetime: u32,
    pub MaximumTransferSize: u64,
    pub OptimalTransferCount: u64,
    pub MaximumDataDescriptors: u32,
    pub MaximumTransferLengthPerDescriptor: u32,
    pub OptimalTransferLengthPerDescriptor: u32,
    pub OptimalTransferLengthGranularity: u16,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_RESILIENCY_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub NameOffset: u32,
    pub NumberOfLogicalCopies: u32,
    pub NumberOfPhysicalCopies: u32,
    pub PhysicalDiskRedundancy: u32,
    pub NumberOfColumns: u32,
    pub Interleave: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_RPMB_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub SizeInBytes: u32,
    pub MaxReliableWriteSizeInBytes: u32,
    pub FrameFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_CRYPTO_CAPABILITY {
    pub Version: u32,
    pub Size: u32,
    pub CryptoCapabilityIndex: u32,
    pub AlgorithmId: u32,
    pub KeySize: u32,
    pub DataUnitSizeBitmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_CRYPTO_CAPABILITY_V2 {
    pub Version: u32,
    pub Size: u32,
    pub CryptoCapabilityIndex: u32,
    pub AlgorithmId: u32,
    pub KeySize: u32,
    pub DataUnitSizeBitmask: u32,
    pub MaxIVBitSize: u16,
    pub Reserved: u16,
    pub SecurityComplianceBitmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_CRYPTO_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub NumKeysSupported: u32,
    pub NumCryptoCapabilities: u32,
    pub CryptoCapabilities: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_CRYPTO_DESCRIPTOR_V2 {
    pub Version: u32,
    pub Size: u32,
    pub NumKeysSupported: u32,
    pub NumCryptoCapabilities: u32,
    pub IceType: u32,
    pub SecurityComplianceBitmask: u32,
    pub KeyTypeBitmask: u32,
    pub CryptoCapabilities: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_CRYPTO_CAPABILITY {
    pub Version: u32,
    pub Size: u32,
    pub CryptoCapabilityIndex: u32,
    pub AlgorithmId: u32,
    pub KeySize: u32,
    pub DataUnitSizeBitmask: u32,
    pub MaxIVBitSize: u16,
    pub Reserved: u16,
    pub SecurityComplianceBitmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_CRYPTO_DESCRIPTOR {
    pub Header: u32,
    pub NumKeysSupported: u32,
    pub NumCryptoCapabilities: u32,
    pub OffsetToCryptoCapabilities: u32,
    pub SizeOfCryptoCapability: u32,
    pub IceType: u32,
    pub SecurityComplianceBitmask: u32,
    pub KeyTypeBitmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TIER {
    pub Id: u32,
    pub Name: [u32; 1],
    pub Description: [u32; 1],
    pub Flags: u64,
    pub ProvisionedCapacity: u64,
    pub MediaType: u32,
    pub Class: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_TIERING_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub TotalNumberOfTiers: u32,
    pub NumberOfTiersReturned: u32,
    pub Tiers: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_FAULT_DOMAIN_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub NumberOfFaultDomains: u32,
    pub FaultDomainIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROTOCOL_SPECIFIC_DATA {
    pub ProtocolType: u32,
    pub DataType: u32,
    pub ProtocolDataRequestValue: u32,
    pub ProtocolDataRequestSubValue: u32,
    pub ProtocolDataOffset: u32,
    pub ProtocolDataLength: u32,
    pub FixedProtocolReturnData: u32,
    pub ProtocolDataRequestSubValue2: u32,
    pub ProtocolDataRequestSubValue3: u32,
    pub ProtocolDataRequestSubValue4: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROTOCOL_SPECIFIC_DATA_EXT {
    pub ProtocolType: u32,
    pub DataType: u32,
    pub ProtocolDataValue: u32,
    pub ProtocolDataSubValue: u32,
    pub ProtocolDataOffset: u32,
    pub ProtocolDataLength: u32,
    pub FixedProtocolReturnData: u32,
    pub ProtocolDataSubValue2: u32,
    pub ProtocolDataSubValue3: u32,
    pub ProtocolDataSubValue4: u32,
    pub ProtocolDataSubValue5: u32,
    pub ProtocolDataSubValue6: u32,
    pub Reserved: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROTOCOL_DATA_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub ProtocolSpecificData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROTOCOL_DATA_DESCRIPTOR_EXT {
    pub Version: u32,
    pub Size: u32,
    pub ProtocolSpecificData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TEMPERATURE_INFO {
    pub Index: u16,
    pub Temperature: i16,
    pub OverThreshold: i16,
    pub UnderThreshold: i16,
    pub OverThresholdChangable: u8,
    pub UnderThresholdChangable: u8,
    pub EventGenerated: u8,
    pub Reserved0: u8,
    pub Reserved1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TEMPERATURE_DATA_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub CriticalTemperature: i16,
    pub WarningTemperature: i16,
    pub InfoCount: u16,
    pub Reserved0: [u8; 2],
    pub Reserved1: [u32; 2],
    pub TemperatureInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TEMPERATURE_THRESHOLD {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u16,
    pub Index: u16,
    pub Threshold: i16,
    pub OverThreshold: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PHYSICAL_DEVICE_DATA {
    pub DeviceId: u32,
    pub Role: u32,
    pub HealthStatus: u32,
    pub CommandProtocol: u32,
    pub SpecVersion: u32,
    pub FormFactor: u32,
    pub Vendor: [u8; 8],
    pub Model: [u8; 40],
    pub FirmwareRevision: [u8; 16],
    pub Capacity: u64,
    pub PhysicalLocation: [u8; 32],
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PHYSICAL_ADAPTER_DATA {
    pub AdapterId: u32,
    pub HealthStatus: u32,
    pub CommandProtocol: u32,
    pub SpecVersion: u32,
    pub Vendor: [u8; 8],
    pub Model: [u8; 40],
    pub FirmwareRevision: [u8; 16],
    pub PhysicalLocation: [u8; 32],
    pub ExpanderConnected: u8,
    pub Reserved0: [u8; 3],
    pub Reserved1: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PHYSICAL_NODE_DATA {
    pub NodeId: u32,
    pub AdapterCount: u32,
    pub AdapterDataLength: u32,
    pub AdapterDataOffset: u32,
    pub DeviceCount: u32,
    pub DeviceDataLength: u32,
    pub DeviceDataOffset: u32,
    pub Reserved: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PHYSICAL_TOPOLOGY_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub NodeCount: u32,
    pub Reserved: u32,
    pub Node: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_IO_CAPABILITY_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub LunMaxIoCount: u32,
    pub AdapterMaxIoCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_ATTRIBUTES_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub Attributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_OPERATIONAL_REASON {
    pub Version: u32,
    pub Size: u32,
    pub Reason: u32,
    pub SenseKey: u8,
    pub ASC: u8,
    pub ASCQ: u8,
    pub Reserved: u8,
    pub CriticalHealth: u8,
    pub ModuleHealth: [u8; 2],
    pub ErrorThresholdStatus: u8,
    pub AsUlong: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_MANAGEMENT_STATUS {
    pub Version: u32,
    pub Size: u32,
    pub Health: u32,
    pub NumberOfOperationalStatus: u32,
    pub NumberOfAdditionalReasons: u32,
    pub OperationalStatus: [u32; 1],
    pub AdditionalReasons: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ADAPTER_SERIAL_NUMBER {
    pub Version: u32,
    pub Size: u32,
    pub SerialNumber: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ZONE_GROUP {
    pub ZoneCount: u32,
    pub ZoneType: u32,
    pub ZoneSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ZONED_DEVICE_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub DeviceType: u32,
    pub ZoneCount: u32,
    pub MaxOpenZoneCount: u32,
    pub UnrestrictedRead: u8,
    pub Reserved: [u8; 3],
    pub OptimalOpenZoneCount: u32,
    pub Reserved_1: u32,
    pub ZoneGroupCount: u32,
    pub ZoneGroup: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_LOCATION {
    pub Socket: u32,
    pub Slot: u32,
    pub Adapter: u32,
    pub Port: u32,
    pub Channel: u32,
    pub Device: u32,
    pub Target: u32,
    pub Lun: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_LOCATION_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub Location: u32,
    pub StringOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_NUMA_PROPERTY {
    pub Version: u32,
    pub Size: u32,
    pub NumaNode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_UNSAFE_SHUTDOWN_COUNT {
    pub Version: u32,
    pub Size: u32,
    pub UnsafeShutdownCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_ENDURANCE_INFO {
    pub ValidFields: u32,
    pub GroupId: u32,
    pub Shared: u32,
    pub Reserved: u32,
    pub LifePercentage: u32,
    pub BytesReadCount: [u8; 16],
    pub ByteWriteCount: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_ENDURANCE_DATA_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub EnduranceInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_STACK_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub StorageStackType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_LED_STATE_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub State: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_SELF_ENCRYPTION_PROPERTY {
    pub Version: u32,
    pub Size: u32,
    pub SupportsSelfEncryption: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_SELF_ENCRYPTION_PROPERTY_V2 {
    pub Version: u32,
    pub Size: u32,
    pub SupportsSelfEncryption: u8,
    pub EncryptionType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_FRU_ID_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub IdentifierSize: u32,
    pub Identifier: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_RANGE {
    pub StartingOffset: i64,
    pub LengthInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_MANAGE_DATA_SET_ATTRIBUTES {
    pub Size: u32,
    pub Action: u32,
    pub Flags: u32,
    pub ParameterBlockOffset: u32,
    pub ParameterBlockLength: u32,
    pub DataSetRangesOffset: u32,
    pub DataSetRangesLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_MANAGE_DATA_SET_ATTRIBUTES_OUTPUT {
    pub Size: u32,
    pub Action: u32,
    pub Flags: u32,
    pub OperationStatus: u32,
    pub ExtendedError: u32,
    pub TargetDetailedError: u32,
    pub ReservedStatus: u32,
    pub OutputBlockOffset: u32,
    pub OutputBlockLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_DEFINITION {
    pub Action: u32,
    pub SingleRange: u8,
    pub ParameterBlockAlignment: u32,
    pub ParameterBlockLength: u32,
    pub HasOutput: u8,
    pub OutputBlockAlignment: u32,
    pub OutputBlockLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_NOTIFICATION_PARAMETERS {
    pub Size: u32,
    pub Flags: u32,
    pub NumFileTypeIDs: u32,
    pub FileTypeID: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_OFFLOAD_TOKEN {
    pub TokenType: [u8; 4],
    pub Reserved: [u8; 2],
    pub TokenIdLength: [u8; 2],
    pub Reserved2: [u8; 1],
    pub Token: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_OFFLOAD_READ_PARAMETERS {
    pub Flags: u32,
    pub TimeToLive: u32,
    pub Reserved: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_OFFLOAD_READ_OUTPUT {
    pub OffloadReadFlags: u32,
    pub Reserved: u32,
    pub LengthProtected: u64,
    pub TokenLength: u32,
    pub Token: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_OFFLOAD_WRITE_PARAMETERS {
    pub Flags: u32,
    pub Reserved: u32,
    pub TokenOffset: u64,
    pub Token: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_OFFLOAD_WRITE_OUTPUT {
    pub OffloadWriteFlags: u32,
    pub Reserved: u32,
    pub LengthCopied: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_LBP_STATE_PARAMETERS {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub OutputVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_LB_PROVISIONING_STATE {
    pub Size: u32,
    pub Version: u32,
    pub SlabSizeInBytes: u64,
    pub SlabOffsetDeltaInBytes: u32,
    pub SlabAllocationBitMapBitCount: u32,
    pub SlabAllocationBitMapLength: u32,
    pub SlabAllocationBitMap: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_LB_PROVISIONING_STATE_V2 {
    pub Size: u32,
    pub Version: u32,
    pub SlabSizeInBytes: u64,
    pub SlabOffsetDeltaInBytes: u64,
    pub SlabAllocationBitMapBitCount: u32,
    pub SlabAllocationBitMapLength: u32,
    pub SlabAllocationBitMap: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_REPAIR_PARAMETERS {
    pub NumberOfRepairCopies: u32,
    pub SourceCopy: u32,
    pub RepairCopies: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_REPAIR_OUTPUT {
    pub ParityExtent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_QUERY_PREFER_LOCAL_REPAIR_OUTPUT {
    pub Version: u32,
    pub PreferLocalRepair: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_SCRUB_OUTPUT {
    pub BytesProcessed: u64,
    pub BytesRepaired: u64,
    pub BytesFailed: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_SCRUB_EX_OUTPUT {
    pub BytesProcessed: u64,
    pub BytesRepaired: u64,
    pub BytesFailed: u64,
    pub ParityExtent: u32,
    pub BytesScrubbed: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_TIERING_QUERY_INPUT {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub NumberOfTierIds: u32,
    pub TierIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_TIER_REGION {
    pub TierId: u32,
    pub Offset: u64,
    pub Length: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_TIERING_QUERY_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Reserved: u32,
    pub Alignment: u64,
    pub TotalNumberOfRegions: u32,
    pub NumberOfRegionsReturned: u32,
    pub Regions: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_NVCACHE_CHANGE_PRIORITY_PARAMETERS {
    pub Size: u32,
    pub TargetPriority: u8,
    pub Reserved: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DATA_SET_TOPOLOGY_ID_QUERY_OUTPUT {
    pub TopologyRangeBytes: u64,
    pub TopologyId: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_STORAGE_ADDRESS_RANGE {
    pub StartAddress: i64,
    pub LengthInBytes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_PHYSICAL_ADDRESSES_OUTPUT {
    pub Version: u32,
    pub Flags: u32,
    pub TotalNumberOfRanges: u32,
    pub NumberOfRangesReturned: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_REPORT_ZONES_PARAMETERS {
    pub Size: u32,
    pub ReportOption: u8,
    pub Partial: u8,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ZONE_DESCRIPTOR {
    pub Size: u32,
    pub ZoneType: u32,
    pub ZoneCondition: u32,
    pub ResetWritePointerRecommend: u8,
    pub Reserved0: [u8; 3],
    pub ZoneSize: u64,
    pub WritePointerOffset: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_REPORT_ZONES_DATA {
    pub Size: u32,
    pub ZoneCount: u32,
    pub Attributes: u32,
    pub Reserved0: u32,
    pub ZoneDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_STORAGE_RANGE_ATTRIBUTES {
    pub LengthInBytes: u64,
    pub AllFlags: u32,
    pub IsRangeBad: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_RANGE_ERROR_INFO {
    pub Version: u32,
    pub Flags: u32,
    pub TotalNumberOfRanges: u32,
    pub NumberOfRangesReturned: u32,
    pub Ranges: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_LOST_QUERY_PARAMETERS {
    pub Version: u32,
    pub Granularity: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_LOST_QUERY_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Alignment: u64,
    pub NumberOfBits: u32,
    pub BitMap: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_FREE_SPACE_OUTPUT {
    pub Version: u32,
    pub FreeSpace: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DSM_CONVERSION_OUTPUT {
    pub Version: u32,
    pub Source: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_GET_BC_PROPERTIES_OUTPUT {
    pub MaximumRequestsPerPeriod: u32,
    pub MinimumPeriod: u32,
    pub MaximumRequestSize: u64,
    pub EstimatedTimePerRequest: u32,
    pub NumOutStandingRequests: u32,
    pub RequestSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ALLOCATE_BC_STREAM_INPUT {
    pub Version: u32,
    pub RequestsPerPeriod: u32,
    pub Period: u32,
    pub RetryFailures: u8,
    pub Discardable: u8,
    pub Reserved1: [u8; 2],
    pub AccessType: u32,
    pub AccessMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ALLOCATE_BC_STREAM_OUTPUT {
    pub RequestSize: u64,
    pub NumOutStandingRequests: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PRIORITY_HINT_SUPPORT {
    pub SupportFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DIAGNOSTIC_REQUEST {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub TargetType: u32,
    pub Level: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DIAGNOSTIC_DATA {
    pub Version: u32,
    pub Size: u32,
    pub ProviderId: u32,
    pub BufferSize: u32,
    pub Reserved: u32,
    pub DiagnosticDataBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_ELEMENT_STATUS_REQUEST {
    pub Version: u32,
    pub Size: u32,
    pub StartingElement: u32,
    pub Filter: u8,
    pub ReportType: u8,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_ELEMENT_STATUS_DESCRIPTOR {
    pub Version: u32,
    pub Size: u32,
    pub ElementIdentifier: u32,
    pub PhysicalElementType: u8,
    pub PhysicalElementHealth: u8,
    pub Reserved1: [u8; 2],
    pub AssociatedCapacity: u64,
    pub Reserved2: [u32; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PHYSICAL_ELEMENT_STATUS {
    pub Version: u32,
    pub Size: u32,
    pub DescriptorCount: u32,
    pub ReturnedDescriptorCount: u32,
    pub ElementIdentifierBeingDepoped: u32,
    pub Reserved: u32,
    pub Descriptors: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct REMOVE_ELEMENT_AND_TRUNCATE_REQUEST {
    pub Version: u32,
    pub Size: u32,
    pub RequestCapacity: u64,
    pub ElementIdentifier: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GET_DEVICE_INTERNAL_STATUS_DATA_REQUEST {
    pub Version: u32,
    pub Size: u32,
    pub RequestDataType: u32,
    pub RequestDataSet: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_INTERNAL_STATUS_DATA {
    pub Version: u32,
    pub Size: u32,
    pub T10VendorId: u64,
    pub DataSet1Length: u32,
    pub DataSet2Length: u32,
    pub DataSet3Length: u32,
    pub DataSet4Length: u32,
    pub StatusDataVersion: u8,
    pub Reserved: [u8; 3],
    pub ReasonIdentifier: [u8; 128],
    pub StatusDataLength: u32,
    pub StatusData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_REINITIALIZE_MEDIA {
    pub Version: u32,
    pub Size: u32,
    pub TimeoutInSeconds: u32,
    pub SanitizeMethod: u32,
    pub DisallowUnrestrictedSanitizeExit: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_MEDIA_SERIAL_NUMBER_DATA {
    pub Reserved: u16,
    pub SerialNumberLength: u16,
    pub SerialNumber: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_READ_CAPACITY {
    pub Version: u32,
    pub Size: u32,
    pub BlockLength: u32,
    pub NumberOfBlocks: i64,
    pub DiskLength: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_WRITE_CACHE_PROPERTY {
    pub Version: u32,
    pub Size: u32,
    pub WriteCacheType: u32,
    pub WriteCacheEnabled: u32,
    pub WriteCacheChangeable: u32,
    pub WriteThroughSupported: u32,
    pub FlushCacheSupported: u8,
    pub UserDefinedPowerProtection: u8,
    pub NVCacheEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PERSISTENT_RESERVE_COMMAND {
    pub Version: u32,
    pub Size: u32,
    pub ServiceAction: u8,
    pub Reserved1: u8,
    pub AllocationLength: u16,
    pub ServiceAction_1: u8,
    pub Reserved1_1: u8,
    pub Type: u8,
    pub Scope: u8,
    pub ParameterList: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_SUBSECTION_POINTER {
    pub dwSize: u32,
    pub dwFlags: u32,
    pub dwOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_STRUCTURE_VERSION {
    pub dwSignature: u32,
    pub dwVersion: u32,
    pub dwSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_SECTION_HEADER {
    pub guidDeviceDataId: u32,
    pub sOrganizationID: [u8; 16],
    pub dwFirmwareRevision: u32,
    pub sModelNumber: [u8; 1],
    pub szDeviceManufacturingID: [u8; 1],
    pub dwFlags: u32,
    pub bRestrictedPrivateDataVersion: u32,
    pub dwFirmwareIssueId: u32,
    pub szIssueDescriptionString: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GP_LOG_PAGE_DESCRIPTOR {
    pub LogAddress: u16,
    pub LogSectors: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_PUBLIC_SUBSECTION {
    pub dwFlags: u32,
    pub GPLogTable: [u32; 1],
    pub szDescription: [i8; 1],
    pub bData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_RESTRICTED_SUBSECTION {
    pub bData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_PRIVATE_SUBSECTION {
    pub dwFlags: u32,
    pub GPLogId: u32,
    pub bData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_STORAGEDEVICE_DATA {
    pub Descriptor: u32,
    pub SectionHeader: u32,
    pub dwBufferSize: u32,
    pub dwReasonForCollection: u32,
    pub PublicData: u32,
    pub RestrictedData: u32,
    pub PrivateData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_STORAGESTACK_PUBLIC_STATE_RECORD {
    pub Cdb: [u8; 1],
    pub Command: [u8; 1],
    pub StartTime: u64,
    pub EndTime: u64,
    pub OperationStatus: u32,
    pub OperationError: u32,
    pub dwReserved: u32,
    pub dwAtaPortSpecific: u32,
    pub SrbTag: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICEDUMP_STORAGESTACK_PUBLIC_DUMP {
    pub Descriptor: u32,
    pub dwReasonForCollection: u32,
    pub cDriverName: [u8; 16],
    pub uiNumRecords: u32,
    pub RecordArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_IDLE_POWER {
    pub Version: u32,
    pub Size: u32,
    pub WakeCapableHint: u32,
    pub D3ColdSupported: u32,
    pub Reserved: u32,
    pub D3IdleTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_IDLE_POWERUP_REASON {
    pub Version: u32,
    pub Size: u32,
    pub PowerupReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_DEVICE_POWER_CAP {
    pub Version: u32,
    pub Size: u32,
    pub Units: u32,
    pub MaxPower: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_RPMB_DATA_FRAME {
    pub Stuff: [u8; 196],
    pub KeyOrMAC: [u8; 32],
    pub Data: [u8; 256],
    pub Nonce: [u8; 16],
    pub WriteCounter: [u8; 4],
    pub Address: [u8; 2],
    pub BlockCount: [u8; 2],
    pub OperationResult: [u8; 2],
    pub RequestOrResponseType: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_EVENT_NOTIFICATION {
    pub Version: u32,
    pub Size: u32,
    pub Events: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_COUNTER {
    pub Type: u32,
    pub Week: u32,
    pub Year: u32,
    pub AsUlonglong: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_COUNTERS {
    pub Version: u32,
    pub Size: u32,
    pub NumberOfCounters: u32,
    pub Counters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_INFO_QUERY {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_SLOT_INFO {
    pub Version: u32,
    pub Size: u32,
    pub SlotNumber: u8,
    pub ReadOnly: u8,
    pub Reserved0: u8,
    pub Reserved1: [u8; 6],
    pub Revision: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_INFO {
    pub Version: u32,
    pub Size: u32,
    pub SupportUpgrade: u8,
    pub Reserved0: u8,
    pub SlotCount: u8,
    pub ActiveSlot: u8,
    pub PendingActivateSlot: u8,
    pub FirmwareShared: u8,
    pub Reserved: [u8; 3],
    pub ImagePayloadAlignment: u32,
    pub ImagePayloadMaxSize: u32,
    pub Slot: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_DOWNLOAD {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Slot: u8,
    pub Reserved: [u8; 3],
    pub Offset: u64,
    pub BufferSize: u64,
    pub ImageBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_DOWNLOAD_V2 {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Slot: u8,
    pub Reserved: [u8; 3],
    pub Offset: u64,
    pub BufferSize: u64,
    pub ImageSize: u32,
    pub Reserved2: u32,
    pub ImageBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_HW_FIRMWARE_ACTIVATE {
    pub Version: u32,
    pub Size: u32,
    pub Flags: u32,
    pub Slot: u8,
    pub Reserved0: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_PROTOCOL_COMMAND {
    pub Version: u32,
    pub Length: u32,
    pub ProtocolType: u32,
    pub Flags: u32,
    pub ReturnStatus: u32,
    pub ErrorCode: u32,
    pub CommandLength: u32,
    pub ErrorInfoLength: u32,
    pub DataToDeviceTransferLength: u32,
    pub DataFromDeviceTransferLength: u32,
    pub TimeOutValue: u32,
    pub ErrorInfoOffset: u32,
    pub DataToDeviceBufferOffset: u32,
    pub DataFromDeviceBufferOffset: u32,
    pub CommandSpecific: u32,
    pub Reserved0: u32,
    pub FixedProtocolReturnData: u32,
    pub FixedProtocolReturnData2: u32,
    pub Reserved1: [u32; 2],
    pub Command: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STORAGE_ATTRIBUTE_MGMT {
    pub Version: u32,
    pub Size: u32,
    pub Action: u32,
    pub Attribute: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BPIO_INPUT {
    pub Version: u32,
    pub Size: u32,
    pub Operation: u32,
    pub InFlags: u32,
    pub Reserved2: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BPIO_RESULTS {
    pub OpStatus: i32,
    pub FailingDriverNameLen: u16,
    pub FailingDriverName: [u32; 32],
    pub FailureReasonLen: u16,
    pub FailureReason: [u32; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BPIO_OUTPUT {
    pub Version: u32,
    pub Size: u32,
    pub Operation: u32,
    pub OutFlags: u32,
    pub Reserved2: u64,
    pub Enable: u32,
    pub Query: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_VDM {
    pub ProcessHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_REGISTER_VDM {
    pub MinimumStateSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DXGK_WIN32K_PARAM_DATA {
    pub PathsArray: usize,
    pub ModesArray: usize,
    pub NumPathArrayElements: u32,
    pub NumModeArrayElements: u32,
    pub SDCFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_WIN32K_CALLBACKS_PARAMS {
    pub CalloutType: usize,
    pub PhysDisp: usize,
    pub Param: usize,
    pub Status: i32,
    pub LockUserSession: u8,
    pub IsPostDevice: u8,
    pub SurpriseRemoval: u8,
    pub WaitForQueueReady: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_WIN32K_CALLBACKS {
    pub PhysDisp: usize,
    pub Callout: usize,
    pub bACPI: u32,
    pub pPhysDeviceObject: usize,
    pub DualviewFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_DEVICE_SESSION_STATUS {
    pub bEnable: u32,
    pub bSuccess: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_HARDWARE_STATE_HEADER {
    pub Length: u32,
    pub PortValue: [u8; 1],
    pub AttribIndexDataState: u32,
    pub BasicSequencerOffset: u32,
    pub BasicCrtContOffset: u32,
    pub BasicGraphContOffset: u32,
    pub BasicAttribContOffset: u32,
    pub BasicDacOffset: u32,
    pub BasicLatchesOffset: u32,
    pub ExtendedSequencerOffset: u32,
    pub ExtendedCrtContOffset: u32,
    pub ExtendedGraphContOffset: u32,
    pub ExtendedAttribContOffset: u32,
    pub ExtendedDacOffset: u32,
    pub ExtendedValidatorStateOffset: u32,
    pub ExtendedMiscDataOffset: u32,
    pub PlaneLength: u32,
    pub Plane1Offset: u32,
    pub Plane2Offset: u32,
    pub Plane3Offset: u32,
    pub Plane4Offset: u32,
    pub VGAStateFlags: u32,
    pub DIBOffset: u32,
    pub DIBBitsPerPixel: u32,
    pub DIBXResolution: u32,
    pub DIBYResolution: u32,
    pub DIBXlatOffset: u32,
    pub DIBXlatLength: u32,
    pub VesaInfoOffset: u32,
    pub FrameBufferData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_HARDWARE_STATE {
    pub StateHeader: usize,
    pub StateLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_NUM_MODES {
    pub NumModes: u32,
    pub ModeInformationLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_MODE {
    pub RequestedMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_MODE_INFORMATION {
    pub Length: u32,
    pub ModeIndex: u32,
    pub VisScreenWidth: u32,
    pub VisScreenHeight: u32,
    pub ScreenStride: u32,
    pub NumberOfPlanes: u32,
    pub BitsPerPlane: u32,
    pub Frequency: u32,
    pub XMillimeter: u32,
    pub YMillimeter: u32,
    pub NumberRedBits: u32,
    pub NumberGreenBits: u32,
    pub NumberBlueBits: u32,
    pub RedMask: u32,
    pub GreenMask: u32,
    pub BlueMask: u32,
    pub AttributeFlags: u32,
    pub VideoMemoryBitmapWidth: u32,
    pub VideoMemoryBitmapHeight: u32,
    pub DriverSpecificAttributeFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_LOAD_FONT_INFORMATION {
    pub WidthInPixels: u16,
    pub HeightInPixels: u16,
    pub FontSize: u32,
    pub Font: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PALETTE_DATA {
    pub NumEntries: u16,
    pub FirstEntry: u16,
    pub Colors: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CLUTDATA {
    pub Red: u8,
    pub Green: u8,
    pub Blue: u8,
    pub Unused: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CURSOR_POSITION {
    pub Column: i16,
    pub Row: i16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_CURSOR_ATTRIBUTES {
    pub Width: u16,
    pub Height: u16,
    pub Column: i16,
    pub Row: i16,
    pub Rate: u8,
    pub Enable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_POINTER_POSITION {
    pub Column: i16,
    pub Row: i16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_POINTER_ATTRIBUTES {
    pub Flags: u32,
    pub Width: u32,
    pub Height: u32,
    pub WidthInBytes: u32,
    pub Enable: u32,
    pub Column: i16,
    pub Row: i16,
    pub Pixels: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_POINTER_CAPABILITIES {
    pub Flags: u32,
    pub MaxWidth: u32,
    pub MaxHeight: u32,
    pub HWPtrBitmapStart: u32,
    pub HWPtrBitmapEnd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_BANK_SELECT {
    pub Length: u32,
    pub Size: u32,
    pub BankingFlags: u32,
    pub BankingType: u32,
    pub PlanarHCBankingType: u32,
    pub BitmapWidthInBytes: u32,
    pub BitmapSize: u32,
    pub Granularity: u32,
    pub PlanarHCGranularity: u32,
    pub CodeOffset: u32,
    pub PlanarHCBankCodeOffset: u32,
    pub PlanarHCEnableCodeOffset: u32,
    pub PlanarHCDisableCodeOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_MEMORY {
    pub RequestedVirtualAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_SHARE_MEMORY {
    pub ProcessHandle: usize,
    pub ViewOffset: u32,
    pub ViewSize: u32,
    pub RequestedVirtualAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_SHARE_MEMORY_INFORMATION {
    pub SharedViewOffset: u32,
    pub SharedViewSize: u32,
    pub VirtualAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_MEMORY_INFORMATION {
    pub VideoRamBase: usize,
    pub VideoRamLength: u32,
    pub FrameBufferBase: usize,
    pub FrameBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PUBLIC_ACCESS_RANGES {
    pub InIoSpace: u32,
    pub MappedInIoSpace: u32,
    pub VirtualAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_COLOR_CAPABILITIES {
    pub Length: u32,
    pub AttributeFlags: u32,
    pub RedPhosphoreDecay: i32,
    pub GreenPhosphoreDecay: i32,
    pub BluePhosphoreDecay: i32,
    pub WhiteChromaticity_x: i32,
    pub WhiteChromaticity_y: i32,
    pub WhiteChromaticity_Y: i32,
    pub RedChromaticity_x: i32,
    pub RedChromaticity_y: i32,
    pub GreenChromaticity_x: i32,
    pub GreenChromaticity_y: i32,
    pub BlueChromaticity_x: i32,
    pub BlueChromaticity_y: i32,
    pub WhiteGamma: i32,
    pub RedGamma: i32,
    pub GreenGamma: i32,
    pub BlueGamma: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_POWER_MANAGEMENT {
    pub Length: u32,
    pub DPMSVersion: u32,
    pub PowerState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_COLOR_LUT_DATA {
    pub Length: u32,
    pub LutDataFormat: u32,
    pub LutData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_LUT_RGB256WORDS {
    pub Red: [u16; 256],
    pub Green: [u16; 256],
    pub Blue: [u16; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BANK_POSITION {
    pub ReadBankPosition: u32,
    pub WriteBankPosition: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISPLAY_BRIGHTNESS {
    pub ucDisplayPolicy: u8,
    pub ucACBrightness: u8,
    pub ucDCBrightness: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COORD {
    pub X: i16,
    pub Y: i16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHAR_INFO {
    pub UnicodeChar: u32,
    pub AsciiChar: i8,
    pub Attributes: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSCNTL_SCREEN_INFO {
    pub Position: u32,
    pub ScreenSize: u32,
    pub nNumberOfChars: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FONT_IMAGE_INFO {
    pub FontSize: u32,
    pub ImageBits: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHAR_IMAGE_INFO {
    pub CharInfo: u32,
    pub FontImageInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VGA_CHAR {
    pub Char: i8,
    pub Attributes: i8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_COPY_FRAME_BUFFER {
    pub SrcScreen: u32,
    pub DestScreen: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_WRITE_TO_FRAME_BUFFER {
    pub SrcBuffer: usize,
    pub DestScreen: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_REVERSE_MOUSE_POINTER {
    pub Screen: u32,
    pub dwType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_MODE_INFORMATION {
    pub VideoMode: u32,
    pub VideoMemory: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_SCREEN_INFORMATION {
    pub ScreenSize: u32,
    pub FontSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FSVIDEO_CURSOR_POSITION {
    pub Coord: u32,
    pub dwType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENG_EVENT {
    pub pKEvent: usize,
    pub fFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_PERFORMANCE_COUNTER {
    pub NbOfAllocationEvicted: [u64; 1],
    pub NbOfAllocationMarked: [u64; 1],
    pub NbOfAllocationRestored: [u64; 1],
    pub KBytesEvicted: [u64; 1],
    pub KBytesMarked: [u64; 1],
    pub KBytesRestored: [u64; 1],
    pub NbProcessCommited: u64,
    pub NbAllocationCommited: u64,
    pub NbAllocationMarked: u64,
    pub KBytesAllocated: u64,
    pub KBytesAvailable: u64,
    pub KBytesCurMarked: u64,
    pub Reference: u64,
    pub Unreference: u64,
    pub TrueReference: u64,
    pub NbOfPageIn: u64,
    pub KBytesPageIn: u64,
    pub NbOfPageOut: u64,
    pub KBytesPageOut: u64,
    pub NbOfRotateOut: u64,
    pub KBytesRotateOut: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEO_QUERY_PERFORMANCE_COUNTER {
    pub BufferSize: u32,
    pub Buffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_QUERY_BRIGHTNESS_CAPS {
    pub Version: u32,
    pub Smooth: u32,
    pub Adaptive: u32,
    pub NitsCalibrated: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRIGHTNESS_LEVEL {
    pub Count: u8,
    pub Level: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BRIGHTNESS_NIT_RANGE {
    pub MinLevelInMillinit: u32,
    pub MaxLevelInMillinit: u32,
    pub StepSizeInMillinit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_QUERY_BRIGHTNESS_RANGES {
    pub Version: u32,
    pub BrightnessLevel: u32,
    pub NitRanges: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_GET_BRIGHTNESS {
    pub Version: u32,
    pub Level: u8,
    pub CurrentInMillinits: u32,
    pub TargetInMillinits: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHROMATICITY_COORDINATE {
    pub x: u32,
    pub y: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_BRIGHTNESS_SENSOR_DATA {
    pub AlsReadingValid: u32,
    pub ChromaticityCoordinateValid: u32,
    pub ColorTemperatureValid: u32,
    pub Reserved: u32,
    pub Value: u32,
    pub AlsReading: u32,
    pub ChromaticityCoordinate: u32,
    pub ColorTemperature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_SET_BRIGHTNESS {
    pub Version: u32,
    pub Level: u8,
    pub Millinits: u32,
    pub TransitionTimeInMs: u32,
    pub SensorData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_SET_BRIGHTNESS_STATE {
    pub Smooth: u32,
    pub Reserved: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_SET_BACKLIGHT_OPTIMIZATION {
    pub Level: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BACKLIGHT_REDUCTION_GAMMA_RAMP {
    pub R: [u16; 256],
    pub G: [u16; 256],
    pub B: [u16; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PANEL_GET_BACKLIGHT_REDUCTION {
    pub BacklightUsersetting: u16,
    pub BacklightEffective: u16,
    pub GammaRamp: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_DATA_CAP {
    pub DataType: u32,
    pub BitCountOfInteger: u32,
    pub BitCountOfFraction: u32,
    pub BitCountOfExponent: u32,
    pub BitCountOfMantissa: u32,
    pub Value: u32,
    pub NumericRangeMin: u32,
    pub NumericRangeMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_1DLUT_CAP {
    pub NumberOfLUTEntries: u32,
    pub DataCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_MATRIX_CAP {
    pub MatrixSizeX: u32,
    pub MatrixSizeY: u32,
    pub Value: u32,
    pub DataCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_SCALAR_MULTIPLIER_CAPS {
    pub Valid: u8,
    pub NumericRangeMin: u32,
    pub NumericRangeMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_TARGET_CAPS {
    pub Version: u32,
    pub LookupTable1DDegammaCap: u32,
    pub ColorMatrix3x3Cap: u32,
    pub LookupTable1DRegammaCap: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GAMMA_RAMP_RGB256x3x16 {
    pub Red: [u16; 256],
    pub Green: [u16; 256],
    pub Blue: [u16; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GAMMA_RAMP_RGB {
    pub Red: u32,
    pub Green: u32,
    pub Blue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GAMMA_RAMP_DXGI_1 {
    pub Scale: u32,
    pub Offset: u32,
    pub GammaCurve: [u32; 1025],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_3x4 {
    pub ColorMatrix3x4: [u32; 3],
    pub ScalarMultiplier: u32,
    pub LookupTable1D: [u32; 4096],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OUTPUT_WIRE_FORMAT {
    pub ColorEncoding: u32,
    pub BitsPerPixel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_MATRIX_V2 {
    pub StageControlLookupTable1DDegamma: u32,
    pub LookupTable1DDegamma: [u32; 4096],
    pub StageControlColorMatrix3x3: u32,
    pub ColorMatrix3x3: [u32; 3],
    pub StageControlLookupTable1DRegamma: u32,
    pub LookupTable1DRegamma: [u32; 4096],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM {
    pub Type: u32,
    pub Rgb256x3x16: u32,
    pub Dxgi1: u32,
    pub T3x4: u32,
    pub MatrixV2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COLORSPACE_TRANSFORM_SET_INPUT {
    pub OutputWireColorSpaceExpected: u32,
    pub OutputWireFormatExpected: u32,
    pub ColorSpaceTransform: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SET_ACTIVE_COLOR_PROFILE_NAME {
    pub ColorProfileName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIPI_DSI_CAPS {
    pub DSITypeMajor: u8,
    pub DSITypeMinor: u8,
    pub SpecVersionMajor: u8,
    pub SpecVersionMinor: u8,
    pub SpecVersionPatch: u8,
    pub TargetMaximumReturnPacketSize: u16,
    pub ResultCodeFlags: u8,
    pub ResultCodeStatus: u8,
    pub Revision: u8,
    pub Level: u8,
    pub DeviceClassHi: u8,
    pub DeviceClassLo: u8,
    pub ManufacturerHi: u8,
    pub ManufacturerLo: u8,
    pub ProductHi: u8,
    pub ProductLo: u8,
    pub LengthHi: u8,
    pub LengthLo: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIPI_DSI_PACKET {
    pub DataId: u8,
    pub DataType: u8,
    pub VirtualChannel: u8,
    pub Data0: u8,
    pub Data1: u8,
    pub LongWriteWordCount: u16,
    pub EccFiller: u8,
    pub Payload: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIPI_DSI_TRANSMISSION {
    pub TotalBufferSize: u32,
    pub PacketCount: u8,
    pub FailedPacket: u8,
    pub TransmissionMode: u16,
    pub ReportMipiErrors: u16,
    pub ClearMipiErrors: u16,
    pub SecondaryPort: u16,
    pub ManufacturingMode: u16,
    pub Reserved: u16,
    pub ReadWordCount: u16,
    pub FinalCommandExtraPayload: u16,
    pub MipiErrors: u16,
    pub HostErrors: u16,
    pub Packets: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIPI_DSI_RESET {
    pub Flags: u32,
    pub MipiErrors: u32,
    pub ResetFailed: u32,
    pub NeedModeSet: u32,
    pub Results: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_EXTENT {
    pub DiskNumber: u32,
    pub StartingOffset: i64,
    pub ExtentLength: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_DISK_EXTENTS {
    pub NumberOfDiskExtents: u32,
    pub Extents: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_GET_GPT_ATTRIBUTES_INFORMATION {
    pub GptAttributes: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_FAILOVER_SET {
    pub NumberOfDisks: u32,
    pub DiskNumbers: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_NUMBER {
    pub VolumeNumber: u32,
    pub VolumeManagerName: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_LOGICAL_OFFSET {
    pub LogicalOffset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_PHYSICAL_OFFSET {
    pub DiskNumber: u32,
    pub Offset: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_PHYSICAL_OFFSETS {
    pub NumberOfPhysicalOffsets: u32,
    pub PhysicalOffset: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_READ_PLEX_INPUT {
    pub ByteOffset: i64,
    pub Length: u32,
    pub PlexNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_SET_GPT_ATTRIBUTES_INFORMATION {
    pub GptAttributes: u64,
    pub RevertOnClose: u8,
    pub ApplyToAllConnectedVolumes: u8,
    pub Reserved1: u16,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_GET_BC_PROPERTIES_INPUT {
    pub Version: u32,
    pub Reserved1: u32,
    pub LowestByteOffset: u64,
    pub HighestByteOffset: u64,
    pub AccessType: u32,
    pub AccessMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_GET_BC_PROPERTIES_OUTPUT {
    pub MaximumRequestsPerPeriod: u32,
    pub MinimumPeriod: u32,
    pub MaximumRequestSize: u64,
    pub EstimatedTimePerRequest: u32,
    pub NumOutStandingRequests: u32,
    pub RequestSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_ALLOCATE_BC_STREAM_INPUT {
    pub Version: u32,
    pub RequestsPerPeriod: u32,
    pub Period: u32,
    pub RetryFailures: u8,
    pub Discardable: u8,
    pub Reserved1: [u8; 2],
    pub LowestByteOffset: u64,
    pub HighestByteOffset: u64,
    pub AccessType: u32,
    pub AccessMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_ALLOCATE_BC_STREAM_OUTPUT {
    pub RequestSize: u64,
    pub NumOutStandingRequests: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FILE_EXTENT {
    pub VolumeOffset: u64,
    pub ExtentLength: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_CRITICAL_IO {
    pub AccessType: u32,
    pub ExtentsCount: u32,
    pub Extents: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_ALLOCATION_HINT_INPUT {
    pub ClusterSize: u32,
    pub NumberOfClusters: u32,
    pub StartingClusterNumber: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_ALLOCATION_HINT_OUTPUT {
    pub Bitmap: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VOLUME_SHRINK_INFO {
    pub VolumeSize: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_FILECACHE_CONTEXT {
    pub IoStatus: u32,
    pub IrpFlags: u32,
    pub MajorFunction: u8,
    pub MinorFunction: u8,
    pub OperationFlags: u8,
    pub Length: u32,
    pub Key: u32,
    pub ByteOffset: i64,
    pub MdlAddress: usize,
    pub Length_1: u64,
    pub ByteOffset_1: i64,
    pub StreamId: u32,
    pub CsvIoContext: usize,
    pub DriverContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_BLOCKCACHE_CALLBACK {
    pub Version: u32,
    pub PurgeCache: u32,
    pub CacheEnable: u32,
    pub CacheDisable: u32,
    pub CallbackContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_CACHE_CALLBACK_V2_OUTPUT {
    pub HandleCacheIo: u32,
    pub CompleteCacheMiss: u32,
    pub PurgeFileCache: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_CACHE_CALLBACK_V2_INPUT {
    pub HandleCacheMiss: u32,
    pub CompleteCacheIo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_FILECACHE_IO_CONTEXT {
    pub Type: u32,
    pub field_m_pIrp: u32,
    pub field_m_pStackLocation: u32,
    pub StartOffset: i64,
    pub EndOffset: i64,
    pub fHard: u8,
    pub field_m_pFileContext: u32,
    pub m_ExtraContext: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_FILECACHE_IO_RANGE {
    pub Offset: u64,
    pub Length: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_CACHE_CALLBACK_V3_INPUT {
    pub HandleCacheMiss: u32,
    pub CompleteCacheIo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSV_CACHE_CALLBACK_V3_OUTPUT {
    pub FileCacheCallbackContext: usize,
    pub IoContextBytesRequired: u32,
    pub CacheCleanup: u32,
    pub FileCreate: u32,
    pub FileClose: u32,
    pub HandleCacheIo: u32,
    pub CompleteCacheMiss: u32,
    pub FileSetExtension: u32,
    pub FileGetExtension: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLOAT128 {
    pub LowPart: i64,
    pub HighPart: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STRING {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSTRING {
    pub Length: u16,
    pub MaximumLength: u16,
    pub field_Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNICODE_STRING {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Buffer: usize,
    pub Buffer_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LIST_ENTRY {
    pub field_Flink: u32,
    pub field_Blink: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SINGLE_LIST_ENTRY {
    pub field_Next: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RTL_BALANCED_NODE {
    pub field_Children: [u32; 2],
    pub field_Left: u32,
    pub field_Right: u32,
    pub Red: u8,
    pub Balance: u8,
    pub ParentValue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SINGLE_LIST_ENTRY32 {
    pub Next: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WNF_STATE_NAME {
    pub Data: [u32; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STRING32 {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STRING64 {
    pub Length: u16,
    pub MaximumLength: u16,
    pub Buffer: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_ATTRIBUTES64 {
    pub Length: u32,
    pub RootDirectory: u32,
    pub ObjectName: u32,
    pub Attributes: u32,
    pub SecurityDescriptor: u32,
    pub SecurityQualityOfService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_ATTRIBUTES32 {
    pub Length: u32,
    pub RootDirectory: u32,
    pub ObjectName: u32,
    pub Attributes: u32,
    pub SecurityDescriptor: u32,
    pub SecurityQualityOfService: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECT_ATTRIBUTES {
    pub Length: u32,
    pub RootDirectory: usize,
    pub ObjectName: usize,
    pub Attributes: u32,
    pub SecurityDescriptor: usize,
    pub SecurityQualityOfService: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OBJECTID {
    pub Lineage: u32,
    pub Uniquifier: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_PARTITION {
    pub FtType: u32,
    pub FtState: u32,
    pub StartingOffset: i64,
    pub Length: i64,
    pub FtLength: i64,
    pub ReservedTwoLongs: [u32; 2],
    pub DriveLetter: u8,
    pub AssignDriveLetter: u8,
    pub LogicalNumber: u16,
    pub FtGroup: u16,
    pub FtMember: u16,
    pub Modified: u8,
    pub ReservedChars: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_DESCRIPTION {
    pub NumberOfPartitions: u16,
    pub ReservedShort: u16,
    pub Signature: u32,
    pub Partitions: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DISK_REGISTRY {
    pub NumberOfDisks: u16,
    pub ReservedShort: u16,
    pub Disks: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_NVM_SUBSYSTEM_RESET {
    pub NSSRC: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_NVM_SUBSYSTEM_SHUTDOWN {
    pub NSSC: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_WCS_DEVICE_RESET_ACTION {
    pub ControllerReset: u8,
    pub NVMeSubsystemReset: u8,
    pub PCIeFLR: u8,
    pub PERST: u8,
    pub PowerCycle: u8,
    pub PCIeConventionalHotReset: u8,
    pub Reserved: u8,
    pub AsUCHAR: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_WCS_DEVICE_CAPABILITIES {
    pub PanicAEN: u32,
    pub PanicCFS: u32,
    pub Reserved: u32,
    pub AsULONG: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_WCS_DEVICE_SMART_ATTRIBUTES_LOG {
    pub VersionSpecificData: [u8; 494],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_WCS_DEVICE_SMART_ATTRIBUTES_LOG_V2 {
    pub MediaUnitsWritten: [u8; 16],
    pub MediaUnitsRead: [u8; 16],
    pub RawCount: [u8; 6],
    pub Normalized: [u8; 2],
    pub RawCount_1: [u8; 6],
    pub Normalized_1: [u8; 2],
    pub XORRecoveryCount: u64,
    pub UnrecoverableReadErrorCount: u64,
    pub SoftECCErrorCount: u64,
    pub DetectedCounts: u32,
    pub CorrectedCounts: u32,
    pub PercentageSystemDataUsed: u8,
    pub RefreshCount: [u8; 7],
    pub MaximumCount: u32,
    pub MinimumCount: u32,
    pub EventCount: u8,
    pub Status: u8,
    pub Reserved0: [u8; 6],
    pub PCIeCorrectableErrorCount: u64,
    pub IncompleteShutdownCount: u32,
    pub Reserved1: u32,
    pub PercentageFreeBlocks: u8,
    pub Reserved2: [u8; 7],
    pub CapacitorHealth: u16,
    pub Reserved3: [u8; 6],
    pub UnalignedIOCount: u64,
    pub SecurityVersionNumber: u64,
    pub NUSE: u64,
    pub PLPStartCount: [u8; 16],
    pub EnduranceEstimate: [u8; 16],
    pub Reserved4: [u8; 302],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_SMART_INFORMATION_LOG_V3 {
    pub MediaUnitsWritten: [u8; 16],
    pub MediaUnitsRead: [u8; 16],
    pub RawCount: [u8; 6],
    pub Normalized: [u8; 2],
    pub RawCount_1: [u8; 6],
    pub Normalized_1: [u8; 2],
    pub XORRecoveryCount: u64,
    pub UnrecoverableReadErrorCount: u64,
    pub SoftECCErrorCount: u64,
    pub DetectedCounts: u32,
    pub CorrectedCounts: u32,
    pub PercentageSystemDataUsed: u8,
    pub RefreshCount: [u8; 7],
    pub MaximumCount: u32,
    pub MinimumCount: u32,
    pub EventCount: u8,
    pub Status: u8,
    pub Errata: u8,
    pub PointVersion: u16,
    pub MinorVersion: u16,
    pub MajorVersion: u8,
    pub PCIeCorrectableErrorCount: u64,
    pub IncompleteShutdownCount: u32,
    pub Reserved1: u32,
    pub PercentageFreeBlocks: u8,
    pub Reserved2: [u8; 7],
    pub CapacitorHealth: u16,
    pub NvmeErrata: u8,
    pub Reserved3: [u8; 5],
    pub UnalignedIOCount: u64,
    pub SecurityVersionNumber: u64,
    pub NUSE: u64,
    pub PLPStartCount: [u8; 16],
    pub EnduranceEstimate: [u8; 16],
    pub PCIeLinkRetrainingCount: u64,
    pub PowerStateChangeCount: u64,
    pub Reserved4: [u8; 286],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_WCS_DEVICE_ERROR_RECOVERY_LOG {
    pub PanicResetWaitTime: u16,
    pub PanicResetAction: u32,
    pub DriveRecoveryAction: u8,
    pub PanicId: u64,
    pub DeviceCapabilities: u32,
    pub VendorSpecificRecoveryCode: u8,
    pub Reserved0: [u8; 3],
    pub VendorSpecificCommandCDW12: u32,
    pub VendorSpecificCommandCDW13: u32,
    pub Reserved1: [u8; 466],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_ERROR_RECOVERY_LOG_V2 {
    pub PanicResetWaitTime: u16,
    pub PanicResetAction: u32,
    pub DeviceRecoveryAction1: u8,
    pub PanicId: u64,
    pub DeviceCapabilities: u32,
    pub VendorSpecificRecoveryCode: u8,
    pub Reserved0: [u8; 3],
    pub VendorSpecificCommandCDW12: u32,
    pub VendorSpecificCommandCDW13: u32,
    pub VendorSpecificCommandTimeout: u8,
    pub DeviceRecoveryAction2: u8,
    pub DeviceRecoveryAction2Timeout: u8,
    pub Reserved1: [u8; 463],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FIRMWARE_ACTIVATION_HISTORY_ENTRY {
    pub VersionNumber: u8,
    pub Length: u8,
    pub Reserved0: u16,
    pub ActivationCount: u16,
    pub Timestamp: u64,
    pub Reserved1: u64,
    pub PowerCycleCount: u64,
    pub PreviousFirmware: u64,
    pub NewFirmware: u64,
    pub SlotNumber: u8,
    pub CommitActionType: u8,
    pub Result: u16,
    pub Reserved2: [u8; 14],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_FIRMWARE_ACTIVATION_HISTORY_LOG {
    pub LID: u8,
    pub Reserved0: [u8; 3],
    pub ValidNumberOfEntries: u32,
    pub Entries: [u32; 20],
    pub Reserved1: [u8; 2790],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LATENCY_MONITOR_FEATURE_STATUS {
    pub FeatureEnabled: u8,
    pub ActiveLatencyMode: u8,
    pub ActiveMeasuredLatency: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACTIVE_LATENCY_CONFIGURATION {
    pub Read0: u16,
    pub Write0: u16,
    pub Trim0: u16,
    pub Read1: u16,
    pub Write1: u16,
    pub Trim1: u16,
    pub Read2: u16,
    pub Write2: u16,
    pub Trim2: u16,
    pub Read3: u16,
    pub Write3: u16,
    pub Trim3: u16,
    pub Reserved: u16,
    pub AsUshort: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BUCKET_COUNTER {
    pub Reserved: u32,
    pub Trim: u32,
    pub Write: u32,
    pub Read: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LATENCY_STAMP {
    pub Trim3: u64,
    pub Write3: u64,
    pub Read3: u64,
    pub Trim2: u64,
    pub Write2: u64,
    pub Read2: u64,
    pub Trim1: u64,
    pub Write1: u64,
    pub Read1: u64,
    pub Trim0: u64,
    pub Write0: u64,
    pub Read0: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MEASURED_LATENCY {
    pub Trim3: u16,
    pub Write3: u16,
    pub Read3: u16,
    pub Trim2: u16,
    pub Write2: u16,
    pub Read2: u16,
    pub Trim1: u16,
    pub Write1: u16,
    pub Read1: u16,
    pub Trim0: u16,
    pub Write0: u16,
    pub Read0: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct LATENCY_STAMP_UNITS {
    pub Read0: u16,
    pub Write0: u16,
    pub Trim0: u16,
    pub Read1: u16,
    pub Write1: u16,
    pub Trim1: u16,
    pub Read2: u16,
    pub Write2: u16,
    pub Trim2: u16,
    pub Read3: u16,
    pub Write3: u16,
    pub Trim3: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEBUG_BIT_FIELD {
    pub Read0: u16,
    pub Write0: u16,
    pub Trim0: u16,
    pub Read1: u16,
    pub Write1: u16,
    pub Trim1: u16,
    pub Read2: u16,
    pub Write2: u16,
    pub Trim2: u16,
    pub Read3: u16,
    pub Write3: u16,
    pub Trim3: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_LATENCY_MONITOR_LOG {
    pub FeatureStatus: u32,
    pub Reserved0: u8,
    pub ActiveBucketTimer: u16,
    pub ActiveBucketTimerThreshold: u16,
    pub ActiveThresholdA: u8,
    pub ActiveThresholdB: u8,
    pub ActiveThresholdC: u8,
    pub ActiveThresholdD: u8,
    pub ActiveLatencyConfig: u32,
    pub ActiveLatencyMinimumWindow: u8,
    pub Reserved1: [u8; 19],
    pub ActiveBucketCounter0: u32,
    pub ActiveBucketCounter1: u32,
    pub ActiveBucketCounter2: u32,
    pub ActiveBucketCounter3: u32,
    pub ActiveLatencyStamp: u32,
    pub ActiveMeasuredLatency: u32,
    pub ActiveLatencyStampUnits: u32,
    pub Reserved2: [u8; 22],
    pub StaticBucketCounter0: u32,
    pub StaticBucketCounter1: u32,
    pub StaticBucketCounter2: u32,
    pub StaticBucketCounter3: u32,
    pub StaticLatencyStamp: u32,
    pub StaticMeasuredLatency: u32,
    pub StaticLatencyStampUnits: u32,
    pub Reserved3: [u8; 22],
    pub DebugLogTriggerEnable: u32,
    pub DebugLogMeasuredLatency: u16,
    pub DebugLogLatencyStamp: u64,
    pub DebugLogPointer: u16,
    pub DebugCounterTriggerSource: u32,
    pub BasedOnTimestamp: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub Reserved4: [u8; 29],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DSSD_POWER_STATE_DESCRIPTOR {
    pub NvmePowerState: u8,
    pub Reserved: u8,
    pub ValidDSSDPowerState: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_CAPABILITIES_LOG {
    pub PciePorts: u16,
    pub MctpOverSMBusSupported: u16,
    pub MctpOverPcieVDMSupported: u16,
    pub BasicMgmtCommandSupported: u16,
    pub Reserved: u16,
    pub CompliesWithSpec: u16,
    pub AsUshort: u16,
    pub Supported: u16,
    pub DEACBitSupported: u16,
    pub FUABitSupported: u16,
    pub NvmeIo5Met: u16,
    pub NvmeIo6Met: u16,
    pub Reserved_1: u16,
    pub CompliesWithSpec_1: u16,
    pub AsUshort_1: u16,
    pub Supported_1: u16,
    pub CryptoEraseSupported: u16,
    pub BlockEraseSupported: u16,
    pub OverwriteSupported: u16,
    pub DeallocateLbaSupported: u16,
    pub Reserved_2: u16,
    pub CompliesWithSpec_2: u16,
    pub AsUshort_2: u16,
    pub Supported_2: u16,
    pub AttribDeallocateSupported: u16,
    pub Reserved_3: u16,
    pub CompliesWithSpec_3: u16,
    pub AsUshort_3: u16,
    pub Supported_3: u16,
    pub SingleLBASupported: u16,
    pub MaxLBASupported: u16,
    pub NvmeIo14Met: u16,
    pub Reserved_4: u16,
    pub CompliesWithSpec_4: u16,
    pub AsUshort_4: u16,
    pub CWFusedSupported: u16,
    pub Reserved_5: u16,
    pub CompliesWithSpec_5: u16,
    pub AsUshort_5: u16,
    pub MinimumValidDSSDPowerState: u16,
    pub Reserved0: u8,
    pub DssdDescriptors: [u32; 127],
    pub Reserved1: [u8; 3934],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UNSUPPORTED_REQUIREMENT {
    pub ReqId: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_UNSUPPORTED_REQUIREMENTS_LOG {
    pub UnsupportedCount: u16,
    pub Reserved0: [u8; 14],
    pub UnsupportedReqList: [u32; 253],
    pub Reserved1: [u8; 14],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_TCG_CONFIGURATION_LOG {
    pub CPINSIDValue: u8,
    pub CPINSIDBlocked: u8,
    pub LockingEnabled: u8,
    pub SUMOwner: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub Reserved0: [u8; 3],
    pub LSPActivationCount: u8,
    pub TPRevertCount: u8,
    pub LSPRevertCount: u8,
    pub LOCount: u8,
    pub SUMLOCount: u8,
    pub RPLOCount: u8,
    pub NPLOCount: u8,
    pub RLLOCount: u8,
    pub WLLOCount: u8,
    pub RULOCount: u8,
    pub WULOCount: u8,
    pub Reserved1: u8,
    pub SIDAuthTryCount: u32,
    pub SIDAuthTryLimit: u32,
    pub ResetCount: u32,
    pub ResetLockCount: u32,
    pub Reserved2: [u8; 462],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_HISTORY_ENTRY {
    pub VersionNumber: u8,
    pub EntryLength: u8,
    pub PowerCycleCount: u16,
    pub TcgCommandCount: u32,
    pub TcgCommandCompletionTS: u64,
    pub InvokingId: u64,
    pub MethodId: u64,
    pub ComId: u16,
    pub ProtocolId: u8,
    pub TcgStatus: u8,
    pub ProcessTime: u16,
    pub CommandSpecific: [u8; 10],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_AUTH_METHOD_SPECIFIC {
    pub AuthorityId: u64,
    pub TriesCount: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_ACTIVATE_METHOD_SPECIFIC {
    pub RangeStartLengthPolicy: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_REACTIVATE_METHOD_SPECIFIC {
    pub RangeStartLengthPolicy: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_ASSIGN_METHOD_SPECIFIC {
    pub NamespaceId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCG_BLOCKSID_METHOD_SPECIFIC {
    pub ClearEvents: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_OCP_DEVICE_TCG_HISTORY_LOG {
    pub LID: u8,
    pub Reserved0: [u8; 3],
    pub HistoryEntryCount: u32,
    pub HistoryEntries: [u32; 84],
    pub Reserved1: [u8; 38],
    pub LogPageVersionNumber: u16,
    pub LogPageGUID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_TELEMETRY_HOST_INITIATED_LOG {
    pub LogIdentifier: u8,
    pub Reserved0: [u8; 4],
    pub OrganizationID: [u8; 3],
    pub Area1LastBlock: u16,
    pub Area2LastBlock: u16,
    pub Area3LastBlock: u16,
    pub Reserved1: [u8; 2],
    pub Area4LastBlock: u32,
    pub Reserved2: [u8; 361],
    pub HostInitiatedDataGenerationNumber: u8,
    pub ControllerInitiatedDataAvailable: u8,
    pub ControllerInitiatedDataGenerationNumber: u8,
    pub ReasonIdentifier: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_TELEMETRY_CONTROLLER_INITIATED_LOG {
    pub LogIdentifier: u8,
    pub Reserved0: [u8; 4],
    pub OrganizationID: [u8; 3],
    pub Area1LastBlock: u16,
    pub Area2LastBlock: u16,
    pub Area3LastBlock: u16,
    pub Reserved1: [u8; 2],
    pub Area4LastBlock: u32,
    pub Reserved2: [u8; 362],
    pub ControllerInitiatedDataAvailable: u8,
    pub ControllerInitiatedDataGenerationNumber: u8,
    pub ReasonIdentifier: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_DESC {
    pub Reserved0: [u8; 15],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_DATABLOCK_DESC {
    pub Address: u64,
    pub Length: u32,
    pub Reserved0: [u8; 3],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_BITBUCKET_DESC {
    pub Reserved0: u64,
    pub Length: u32,
    pub Reserved1: [u8; 3],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_SEGMENT_DESC {
    pub Address: u64,
    pub Length: u32,
    pub Reserved0: [u8; 3],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_LASTSEG_DESC {
    pub Address: u64,
    pub Length: u32,
    pub Reserved0: [u8; 3],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_KEYDATABLOCK_DESC {
    pub Address: u64,
    pub Length: [u8; 3],
    pub Key: [u8; 4],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_SGL_TRANSPORTDATA_DESC {
    pub Reserved0: u64,
    pub Length: u32,
    pub Reserved1: [u8; 3],
    pub SubType: u8,
    pub Type: u8,
    pub AsUchar: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_FABRICS_COMMAND {
    pub OPC: u8,
    pub PSDT: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved: [u8; 35],
    pub Specific: [u8; 24],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_FABRICS_RESPONSE {
    pub Specific: [u8; 8],
    pub SQHD: u16,
    pub Reserved: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_CONNECT_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 19],
    pub SGL1: u32,
    pub RECFMT: u16,
    pub QID: u16,
    pub SQSIZE: u16,
    pub PriorityClass: u8,
    pub SqFlowControlDisable: u8,
    pub IoQueueDeletion: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub Reserved2: u8,
    pub KATO: u32,
    pub Reserved3: [u8; 12],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_CONNECT_DATA {
    pub HOSTID: [u8; 1],
    pub CNTLID: u16,
    pub Reserved0: [u8; 238],
    pub SUBNQN: [u8; 1],
    pub HOSTNQN: [u8; 1],
    pub Reserved1: [u8; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_CONNECT_RESPONSE {
    pub CNTLID: u16,
    pub Obsolete: u16,
    pub ATR: u16,
    pub ASCR: u16,
    pub Reserved: u16,
    pub AsUshort: u16,
    pub AsUlong: u32,
    pub Reserved0: u32,
    pub SQHD: u16,
    pub Reserved1: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_DISCONNECT_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 19],
    pub SGL1: u32,
    pub RECFMT: u16,
    pub Reserved2: [u8; 22],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_DISCONNECT_RESPONSE {
    pub Reserved0: u64,
    pub SQHD: u16,
    pub Reserved1: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_PROPERTY_GET_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 35],
    pub PropertySize: u8,
    pub Reserved: u8,
    pub Reserved2: [u8; 3],
    pub OFST: u32,
    pub Reserved3: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_PROPERTY_GET_RESPONSE {
    pub Value: u32,
    pub Reserved: u32,
    pub EightBytes: u64,
    pub SQHD: u16,
    pub Reserved0: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_PROPERTY_SET_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 35],
    pub PropertySize: u8,
    pub Reserved: u8,
    pub Reserved2: [u8; 3],
    pub OFST: u32,
    pub Value: u32,
    pub Reserved_1: u32,
    pub EightBytes: u64,
    pub Reserved3: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_PROPERTY_SET_RESPONSE {
    pub Reserved0: u64,
    pub SQHD: u16,
    pub Reserved1: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_RECEIVE_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 19],
    pub SGL1: u32,
    pub Reserved2: u8,
    pub SPSP0: u8,
    pub SPSP1: u8,
    pub SECP: u8,
    pub AL: u32,
    pub Reserved3: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_RECEIVE_RESPONSE {
    pub Reserved0: u64,
    pub SQHD: u16,
    pub Reserved1: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_SEND_COMMAND {
    pub OPC: u8,
    pub Reserved0: u8,
    pub CID: u16,
    pub FCTYPE: u8,
    pub Reserved1: [u8; 19],
    pub SGL1: u32,
    pub Reserved2: u8,
    pub SPSP0: u8,
    pub SPSP1: u8,
    pub SECP: u8,
    pub TL: u32,
    pub Reserved3: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_SEND_RESPONSE {
    pub Reserved0: u64,
    pub SQHD: u16,
    pub Reserved1: u16,
    pub CID: u16,
    pub STS: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_DISCOVERY_ENTRY {
    pub TRTYPE: u8,
    pub ADRFAM: u8,
    pub SUBTYPE: u8,
    pub SecureChannel: u8,
    pub SqFlowControlDisable: u8,
    pub ZeroHostIdSupport: u8,
    pub AuthAndSecureChannel: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub PORTID: u16,
    pub CNTLID: u16,
    pub ASQSZ: u16,
    pub DuplicateReturnedInfo: u16,
    pub ExplicitPersistentConnectionSupport: u16,
    pub NoCDCConnectivity: u16,
    pub Reserved_1: u16,
    pub AsUshort: u16,
    pub Reserved0: [u8; 20],
    pub TRSVCID: [u8; 1],
    pub Reserved1: [u8; 192],
    pub NQN: [u8; 1],
    pub TRADDR: [u8; 1],
    pub TSAS: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_EXTENDED_DISCOVERY_ENTRY {
    pub TRTYPE: u8,
    pub ADRFAM: u8,
    pub SUBTYPE: u8,
    pub SecureChannel: u8,
    pub SqFlowControlDisable: u8,
    pub ZeroHostIdSupport: u8,
    pub AuthAndSecureChannel: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub PORTID: u16,
    pub CNTLID: u16,
    pub ASQSZ: u16,
    pub DuplicateReturnedInfo: u16,
    pub ExplicitPersistentConnectionSupport: u16,
    pub NoCDCConnectivity: u16,
    pub Reserved_1: u16,
    pub AsUshort: u16,
    pub Reserved0: [u8; 20],
    pub TRSVCID: [u8; 1],
    pub Reserved1: [u8; 192],
    pub NQN: [u8; 1],
    pub TRADDR: [u8; 1],
    pub TSAS: [u8; 1],
    pub TEL: u32,
    pub NUMEXAT: u16,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_EXTENDED_ATTR {
    pub EXATTYPE: u16,
    pub EXATLEN: u16,
    pub EXATVAL: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_DISCOVERY_HEADER {
    pub GENCTR: u64,
    pub NUMREC: u64,
    pub RECFMT: u16,
    pub Extended: u8,
    pub PortLocal: u8,
    pub AllSubsystems: u8,
    pub Reserved: u8,
    pub AsUchar: u8,
    pub Reserved0: u8,
    pub TDLPL: u32,
    pub Reserved1: [u8; 1000],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVME_DISCOVERY_INFO_MGMT_HEADER {
    pub TDL: u32,
    pub Reserved0: u32,
    pub NUMENT: u64,
    pub ENTFMT: u16,
    pub ETYPE: u16,
    pub PORTLCL: u8,
    pub Reserved1: u8,
    pub NQN: u16,
    pub TSAS: u16,
    pub TRSVCID: u16,
    pub ADRFAM: u16,
    pub TRTYPE: u16,
    pub PORTID: u16,
    pub TRADDR: u16,
    pub Reserved: u16,
    pub AsUshort: u16,
    pub EID: [u8; 256],
    pub ENAME: [u8; 256],
    pub EVER: [u8; 64],
    pub Reserved2: [u8; 424],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_NEGOTIATE {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub SC_C: u8,
    pub NAPD: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_FAILURE {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub ReasonCode: u8,
    pub ReasonExplanation: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_DHCHAP_DESCRIPTOR {
    pub AuthId: u8,
    pub Reserved0: u8,
    pub HALEN: u8,
    pub DHLEN: u8,
    pub IdList: [u8; 60],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_DHCHAP_CHALLENGE {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub HL: u8,
    pub Reserved1: u8,
    pub HashID: u8,
    pub DHgID: u8,
    pub DHVLEN: u16,
    pub SEQNUM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_DHCHAP_REPLY {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub HL: u8,
    pub Reserved1: u8,
    pub CVALID: u8,
    pub Reserved2: u8,
    pub DHVLEN: u16,
    pub SEQNUM: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_DHCHAP_SUCCESS1 {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub HL: u8,
    pub Reserved1: u8,
    pub RVALID: u8,
    pub Reserved2: [u8; 7],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NVMEOF_AUTH_DHCHAP_SUCCESS2 {
    pub AUTH_TYPE: u8,
    pub AUTH_ID: u8,
    pub Reserved0: u16,
    pub T_ID: u16,
    pub Reserved1: [u8; 10],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_QUERY_INFORMATION {
    pub BatteryTag: u32,
    pub InformationLevel: u32,
    pub AtRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_INFORMATION {
    pub Capabilities: u32,
    pub Technology: u8,
    pub Reserved: [u8; 3],
    pub Chemistry: [u8; 4],
    pub DesignedCapacity: u32,
    pub FullChargedCapacity: u32,
    pub DefaultAlert1: u32,
    pub DefaultAlert2: u32,
    pub CriticalBias: u32,
    pub CycleCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_CHARGING_SOURCE {
    pub Type: u32,
    pub MaxCurrent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_CHARGING_SOURCE_INFORMATION {
    pub Type: u32,
    pub SourceOnline: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_SET_INFORMATION {
    pub BatteryTag: u32,
    pub InformationLevel: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_CHARGER_STATUS {
    pub Type: u32,
    pub VaData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_USB_CHARGER_STATUS {
    pub Type: u32,
    pub Reserved: u32,
    pub Flags: u32,
    pub MaxCurrent: u32,
    pub Voltage: u32,
    pub PortType: u32,
    pub PortId: u64,
    pub PowerSourceInformation: usize,
    pub OemCharger: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_WAIT_STATUS {
    pub BatteryTag: u32,
    pub Timeout: u32,
    pub PowerState: u32,
    pub LowCapacity: u32,
    pub HighCapacity: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_STATUS {
    pub PowerState: u32,
    pub Capacity: u32,
    pub Voltage: u32,
    pub Rate: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_ADAPTER_STATUS {
    pub Version: u8,
    pub Reserved: [u8; 3],
    pub PowerState: usize,
    pub PeakPower: u32,
    pub MaxOutputPower: u32,
    pub MaxInputPower: u32,
    pub RecStartTime: u32,
    pub RecEndTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_ADAPTER_SET_STATUS_BUFFER {
    pub Version: u8,
    pub RecOverride: u8,
    pub Reserved: [u8; 2],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_ADAPTER_CHARGE_REQUIREMENT {
    pub AcAdapterType: u32,
    pub MinimumPower: u32,
    pub NominalPower: u32,
    pub MaximumPower: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BATTERY_MANUFACTURE_DATE {
    pub Day: u8,
    pub Month: u8,
    pub Year: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUSTOMIZED_IO_CAPABILITIES {
    pub SupportedInputs: u32,
    pub SupportedOutputs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUSTOMIZED_IO_QUERY_INPUT_RETURN {
    pub FunctionId: u32,
    pub ErrorCode: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUSTOMIZED_IO_SEND_OUTPUT_BUFFER {
    pub FunctionId: u32,
    pub Value: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THERMAL_INFORMATION {
    pub ThermalStamp: u32,
    pub ThermalConstant1: u32,
    pub ThermalConstant2: u32,
    pub Processors: u32,
    pub SamplingPeriod: u32,
    pub CurrentTemperature: u32,
    pub PassiveTripPoint: u32,
    pub CriticalTripPoint: u32,
    pub ActiveTripPointCount: u8,
    pub ActiveTripPoint: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THERMAL_WAIT_READ {
    pub Timeout: u32,
    pub LowTemperature: u32,
    pub HighTemperature: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THERMAL_POLICY {
    pub Version: u32,
    pub WaitForUpdate: u8,
    pub Hibernate: u8,
    pub Critical: u8,
    pub ThermalStandby: u8,
    pub ActivationReasons: u32,
    pub PassiveLimit: u32,
    pub ActiveLevel: u32,
    pub OverThrottled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct THERMAL_COOLING_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub Flags: u32,
    pub ActiveCooling: usize,
    pub PassiveCooling: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POWER_LIMIT_INTERFACE {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub DomainCount: u32,
    pub QueryAttributes: usize,
    pub SetPowerLimit: usize,
    pub QueryPowerLimit: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WAKE_ALARM_INFORMATION {
    pub TimerIdentifier: u32,
    pub Timeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_REAL_TIME {
    pub Year: u32,
    pub Month: u32,
    pub Day: u32,
    pub Hour: u32,
    pub Minute: u32,
    pub Second: u32,
    pub Valid: u32,
    pub Milliseconds: u32,
    pub TimeZone: u32,
    pub DayLight: u32,
    pub Reserved1: [u32; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ACPI_TIME_AND_ALARM_CAPABILITIES {
    pub AcWakeSupported: u8,
    pub DcWakeSupported: u8,
    pub S4AcWakeSupported: u8,
    pub S4DcWakeSupported: u8,
    pub S5AcWakeSupported: u8,
    pub S5DcWakeSupported: u8,
    pub S4S5WakeStatusSupported: u8,
    pub DeepestWakeSystemState: u32,
    pub RealTimeFeaturesSupported: u8,
    pub RealTimeResolution: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BarcodeSymbologyAttributesData {
    pub Symbology: u32,
    pub IsCheckDigitValidationSupported: u32,
    pub IsCheckDigitValidationEnabled: u32,
    pub IsCheckDigitTransmissionSupported: u32,
    pub IsCheckDigitTransmissionEnabled: u32,
    pub IsDecodeLengthSupported: u32,
    pub DecodeLengthType: u32,
    pub DecodeLength1: u32,
    pub DecodeLength2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosDeviceInformation {
    pub UnifiedPOSVersion: [u32; 1],
    pub DeviceCategory: [u32; 1],
    pub ManufacturerName: [u32; 1],
    pub ModelName: [u32; 1],
    pub SerialNumber: [u32; 1],
    pub ManufactureDate: [u32; 1],
    pub MechanicalRevision: [u32; 1],
    pub FirmwareRevision: [u32; 1],
    pub Interface: [u32; 1],
    pub InstallationDate: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosEventDataHeader {
    pub EventType: usize,
    pub DataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosStatusUpdatedEventData {
    pub Header: usize,
    pub Status: u32,
    pub ExtendedStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosStringType {
    pub DataLengthInBytes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosProfileType {
    pub BufferSize: u32,
    pub ProfileCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosStatisticsHeader {
    pub EntryCount: u32,
    pub DeviceInformation: usize,
    pub DataLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosValueStatisticsEntry {
    pub EntryName: [u32; 1],
    pub Value: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosBarcodeScannerDataReceivedEventData {
    pub Header: usize,
    pub DataType: u32,
    pub ScanDataLength: u32,
    pub ScanDataLabelLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosBarcodeScannerErrorOccurredEventData {
    pub Header: usize,
    pub IsRetriable: i32,
    pub Severity: u32,
    pub VendorErrorCode: u32,
    pub Reason: u32,
    pub ExtendedReason: u32,
    pub MessageLength: u32,
    pub PartialData: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosBarcodeScanDataTypeData {
    pub dataLengthInBytes: u32,
    pub scanDataTypes_0: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosDeviceBasicsType {
    pub Version: u32,
    pub DeviceType: usize,
    pub RecommendedBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosBarcodeScannerCapabilitiesType {
    pub PowerReportingType: u32,
    pub IsStatisticsReportingSupported: i32,
    pub IsStatisticsUpdatingSupported: i32,
    pub IsImagePreviewSupported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosBarcodeScannerCapabilitiesType2 {
    pub PosBarcodeScannerCapabilities: usize,
    pub IsSoftwareTriggerSupported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PosMagneticStripeReaderCapabilitiesType {
    pub PowerReportingType: u32,
    pub IsStatisticsReportingSupported: i32,
    pub IsStatisticsUpdatingSupported: i32,
    pub CardAuthenticationLength: u32,
    pub SupportedEncryptionAlgorithms: u32,
    pub AuthenticationLevel: u32,
    pub IsIsoSupported: i32,
    pub IsJisOneSupported: i32,
    pub IsJisTwoSupported: i32,
    pub IsTrackDataMaskingSupported: i32,
    pub IsTransmitSentinelsSupported: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_DATA_RECEIVED {
    pub CardType: u32,
    pub Track1EncryptedDataLength: u32,
    pub Track2EncryptedDataLength: u32,
    pub Track3EncryptedDataLength: u32,
    pub Track4EncryptedDataLength: u32,
    pub Track1EncryptedData: [u32; 1],
    pub Track2EncryptedData: [u32; 1],
    pub Track3EncryptedData: [u32; 1],
    pub Track4EncryptedData: [u32; 1],
    pub Track1MaskedDataLength: u32,
    pub Track2MaskedDataLength: u32,
    pub Track3MaskedDataLength: u32,
    pub Track4MaskedDataLength: u32,
    pub Track1MaskedData: [u32; 1],
    pub Track2MaskedData: [u32; 1],
    pub Track3MaskedData: [u32; 1],
    pub Track4MaskedData: [u32; 1],
    pub Track1DiscretionaryDataLength: u32,
    pub Track2DiscretionaryDataLength: u32,
    pub Track1DiscretionaryData: [u32; 1],
    pub Track2DiscretionaryData: [u32; 1],
    pub CardAuthenicationDataLength: u32,
    pub CardAuthenticationDataAbsoluteLength: u32,
    pub CardAuthenicationData: [u32; 1],
    pub AdditionalSecurityInformationLength: u32,
    pub AdditionalSecurityInformation: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_SUPPORTED_CARD_TYPES {
    pub Count: u32,
    pub CardTypes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_RETRIEVE_DEVICE_AUTHENTICATION_DATA {
    pub KeySerialNumber: [u32; 1],
    pub Challenge1: [u32; 1],
    pub Challenge2: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_AUTHENTICATE_DEVICE {
    pub Size: u32,
    pub Challenge1: [u32; 1],
    pub SessionId: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_DEAUTHENTICATE_DEVICE {
    pub Challenge2: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_UPDATE_KEY {
    pub KeyLength: u32,
    pub KeyNameLength: u32,
    pub Key: [u32; 1],
    pub KeyName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MSR_ERROR_EVENT {
    pub Header: usize,
    pub Track1Status: u32,
    pub Track2Status: u32,
    pub Track3Status: u32,
    pub Track4Status: u32,
    pub Severity: u32,
    pub Reason: u32,
    pub ExtendedReason: u32,
    pub CardData: u32,
    pub Message: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRM_DRIVER_INTERFACE_STANDARD {
    pub Size: u16,
    pub Version: u16,
    pub Context: usize,
    pub InterfaceReference: usize,
    pub InterfaceDereference: usize,
    pub InvokeHandler: usize,
    pub QueryHandler: usize,
    pub LockModule: usize,
    pub UnlockModule: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PRM_INTERFACE {
    pub Version: u32,
    pub UnlockModule: usize,
    pub LockModule: usize,
    pub InvokeHandler: usize,
    pub QueryHandler: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_CONTROLLER_INFO {
    pub Size: usize,
    pub PinCount: u32,
    pub MinimumPeriod: usize,
    pub MaximumPeriod: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_CONTROLLER_GET_ACTUAL_PERIOD_OUTPUT {
    pub ActualPeriod: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_CONTROLLER_SET_DESIRED_PERIOD_INPUT {
    pub DesiredPeriod: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_CONTROLLER_SET_DESIRED_PERIOD_OUTPUT {
    pub ActualPeriod: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_PIN_GET_ACTIVE_DUTY_CYCLE_PERCENTAGE_OUTPUT {
    pub Percentage: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_PIN_SET_ACTIVE_DUTY_CYCLE_PERCENTAGE_INPUT {
    pub Percentage: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_PIN_GET_POLARITY_OUTPUT {
    pub Polarity: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_PIN_SET_POLARITY_INPUT {
    pub Polarity: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PWM_PIN_IS_STARTED_OUTPUT {
    pub IsStarted: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLOWSPEC {
    pub TokenRate: u32,
    pub TokenBucketSize: u32,
    pub PeakBandwidth: u32,
    pub Latency: u32,
    pub DelayVariation: u32,
    pub ServiceType: u32,
    pub MaxSduSize: u32,
    pub MinimumPolicedSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_SD_MODE {
    pub ObjectHdr: u32,
    pub ShapeDiscardMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_SHAPING_RATE {
    pub ObjectHdr: u32,
    pub ShapingRate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_FRIENDLY_NAME {
    pub ObjectHdr: u32,
    pub FriendlyName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_TRAFFIC_CLASS {
    pub ObjectHdr: u32,
    pub TrafficClass: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_DS_CLASS {
    pub ObjectHdr: u32,
    pub DSField: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_DIFFSERV {
    pub ObjectHdr: u32,
    pub DSFieldCount: u32,
    pub DiffservRule: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_DIFFSERV_RULE {
    pub InboundDSField: u8,
    pub ConformingOutboundDSField: u8,
    pub NonConformingOutboundDSField: u8,
    pub ConformingUserPriority: u8,
    pub NonConformingUserPriority: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_TCP_TRAFFIC {
    pub ObjectHdr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IN_ADDR_IPV6 {
    pub Addr: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC_V4 {
    pub Address: u32,
    pub Unused: u16,
    pub Port: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC_V6 {
    pub Address: u32,
    pub UnUsed: u16,
    pub Port: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC_V6_FLOW {
    pub Address: u32,
    pub UnUsed: u8,
    pub FlowLabel: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC_V4_GPI {
    pub Address: u32,
    pub GeneralPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC_V6_GPI {
    pub Address: u32,
    pub GeneralPortId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_FILTERSPEC {
    pub Type: u32,
    pub FilterSpecV4: u32,
    pub FilterSpecV6: u32,
    pub FilterSpecV6Flow: u32,
    pub FilterSpecV4Gpi: u32,
    pub FilterSpecV6Gpi: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLOWDESCRIPTOR {
    pub FlowSpec: u32,
    pub NumFilters: u32,
    pub FilterList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_POLICY {
    pub Len: u16,
    pub Type: u16,
    pub Info: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_POLICY_INFO {
    pub ObjectHdr: u32,
    pub NumPolicyElement: u32,
    pub PolicyElement: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_RESERVE_INFO {
    pub ObjectHdr: u32,
    pub Style: u32,
    pub ConfirmRequest: u32,
    pub PolicyElementList: usize,
    pub NumFlowDesc: u32,
    pub FlowDescList: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_STATUS_INFO {
    pub ObjectHdr: u32,
    pub StatusCode: u32,
    pub ExtendedStatus1: u32,
    pub ExtendedStatus2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct QOS_DESTADDR {
    pub ObjectHdr: u32,
    pub SocketAddress: usize,
    pub SocketAddressLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AD_GENERAL_PARAMS {
    pub IntServAwareHopCount: u32,
    pub PathBandwidthEstimate: u32,
    pub MinimumLatency: u32,
    pub PathMTU: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AD_GUARANTEED {
    pub CTotal: u32,
    pub DTotal: u32,
    pub CSum: u32,
    pub DSum: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PARAM_BUFFER {
    pub ParameterId: u32,
    pub Length: u32,
    pub Buffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CONTROL_SERVICE {
    pub Length: u32,
    pub Service: u32,
    pub Overrides: u32,
    pub Guaranteed: u32,
    pub ParamBuffer: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RSVP_ADSPEC {
    pub ObjectHdr: u32,
    pub GeneralParams: u32,
    pub NumberOfServices: u32,
    pub Services: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_QUERY_CONNECTION_PROPERTIES_INPUT_BUFFER {
    pub Version: u32,
    pub QueryType: u32,
    pub ConnectionId: i64,
    pub InterruptVector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_QUERY_CONNECTION_PROPERTIES_OUTPUT_BUFFER {
    pub Version: u32,
    pub PropertiesLength: u32,
    pub ConnectionProperties: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_GPIO_IO_CONNECTION_PARAMETERS {
    pub ConnectionId: i64,
    pub UpdateMask: u16,
    pub PinConfiguration: u8,
    pub DebounceTimeout: u16,
    pub DriveStrength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_GPIO_INTERRUPT_CONNECTION_PARAMETERS {
    pub InterruptVector: u32,
    pub UpdateMask: u16,
    pub InterruptMode: u8,
    pub InterruptPolarity: u8,
    pub PinConfiguration: u8,
    pub DebounceTimeout: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_SPI_CONNECTION_PARAMETERS {
    pub ConnectionId: i64,
    pub UpdateMask: u16,
    pub DeviceSelection: u16,
    pub ConnectionSpeed: u32,
    pub ClockPolarity: u8,
    pub ClockPhase: u8,
    pub DevicePolarity: u8,
    pub DataBitLength: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_I2C_CONNECTION_PARAMETERS {
    pub ConnectionId: i64,
    pub UpdateMask: u16,
    pub TypeSpecificFlags: u16,
    pub ConnectionSpeed: u32,
    pub SlaveAddress: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_UART_CONNECTION_PARAMETERS {
    pub ConnectionId: i64,
    pub UpdateMask: u16,
    pub BaudRate: u32,
    pub TypeSpecificFlags: u16,
    pub RxBufferSize: u16,
    pub TxBufferSize: u16,
    pub Parity: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_ALLOCATE_UPDATE_CONNECTION_INPUT_BUFFER {
    pub Version: u32,
    pub ConnectionType: u32,
    pub IoConnection: u32,
    pub InterruptConnection: u32,
    pub SpiConnection: u32,
    pub I2cConnection: u32,
    pub UartConnection: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_ALLOCATE_CONNECTION_OUTPUT_BUFFER {
    pub Version: u32,
    pub ConnectionId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_UPDATE_CONNECTION_PROPERTIES_OUTPUT_BUFFER {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_FREE_CONNECTION_INPUT_BUFFER {
    pub Version: u32,
    pub ConnectionId: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_FREE_CONNECTION_OUTPUT_BUFFER {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_QUERY_ACTIVE_BOTH_INITIAL_POLARITY_INPUT_BUFFER {
    pub Version: u32,
    pub InterruptVector: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_QUERY_ACTIVE_BOTH_INITIAL_POLARITY_OUTPUT_BUFFER {
    pub Version: u32,
    pub InitialPolarity: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_UPDATE_ACTIVE_BOTH_INITIAL_POLARITY_INPUT_BUFFER {
    pub Version: u32,
    pub InterruptVector: u32,
    pub InitialPolarity: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RH_UPDATE_ACTIVE_BOTH_INITIAL_POLARITY_OUTPUT_BUFFER {
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_GPIO_INTERRUPT_IO_DESCRIPTOR {
    pub Tag: u8,
    pub Length: u16,
    pub Revision: u8,
    pub DescriptorType: u8,
    pub GeneralFlags: u16,
    pub InterruptIoFlags: u16,
    pub PinConfiguration: u8,
    pub DriveStrength: u16,
    pub DebounceTimeout: u16,
    pub PinTableOffset: u16,
    pub ResourceSourceIndex: u8,
    pub ResourceSourceOffset: u16,
    pub VendorDataOffset: u16,
    pub VendorDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_LARGE_VENDOR_DESCRIPTOR {
    pub Tag: u8,
    pub Length: u16,
    pub SubType: u8,
    pub UUID: u32,
    pub VendorDescriptor: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_FUNCTION_CONFIG_DESCRIPTOR {
    pub Tag: u8,
    pub Length: u16,
    pub Revision: u8,
    pub Flags: u16,
    pub PinConfiguration: u8,
    pub FunctionNumber: u16,
    pub PinTableOffset: u16,
    pub ResourceSourceIndex: u8,
    pub ResourceSourceOffset: u16,
    pub VendorDataOffset: u16,
    pub VendorDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PNP_SERIAL_BUS_DESCRIPTOR {
    pub Tag: u8,
    pub Length: u16,
    pub RevisionId: u8,
    pub ResourceSourceIndex: u8,
    pub SerialBusType: u8,
    pub GeneralFlags: u8,
    pub TypeSpecificFlags: u16,
    pub TypeSpecificRevisionId: u8,
    pub TypeDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_ASYNC_STATE {
    pub Size: u32,
    pub Signature: u32,
    pub Lock: u32,
    pub Flags: u32,
    pub field_StubInfo: u32,
    pub field_UserInfo: u32,
    pub field_RuntimeInfo: u32,
    pub Event: u32,
    pub NotificationType: u32,
    pub u: u32,
    pub Reserved: [isize; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_CALL_LOCAL_ADDRESS_V1 {
    pub Version: u32,
    pub field_Buffer: u32,
    pub BufferSize: u32,
    pub AddressFormat: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_VECTOR {
    pub Count: u32,
    pub BindingH: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UUID_VECTOR {
    pub Count: u32,
    pub field_Uuid: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_IF_ID {
    pub Uuid: u32,
    pub VersMajor: u32,
    pub VersMinor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_PROTSEQ_VECTORA {
    pub Count: u32,
    pub Protseq: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_PROTSEQ_VECTORW {
    pub Count: u32,
    pub Protseq: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_PROTSEQ_VECTOR {
    pub Count: u32,
    pub Protseq: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_POLICY {
    pub Length: u32,
    pub EndpointFlags: u32,
    pub NICFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_IDENTITY_A {
    pub field_User: u32,
    pub UserLength: u32,
    pub field_Domain: u32,
    pub DomainLength: u32,
    pub field_Password: u32,
    pub PasswordLength: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_W {
    pub field_TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_A {
    pub field_TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_V2_W {
    pub field_TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
    pub field_ProxyCredentials: u32,
    pub NumberOfProxyAuthnSchemes: u32,
    pub field_ProxyAuthnSchemes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_V2_A {
    pub field_TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
    pub field_ProxyCredentials: u32,
    pub NumberOfProxyAuthnSchemes: u32,
    pub field_ProxyAuthnSchemes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_V3_W {
    pub TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
    pub ProxyCredentials: u32,
    pub NumberOfProxyAuthnSchemes: u32,
    pub field_ProxyAuthnSchemes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_HTTP_TRANSPORT_CREDENTIALS_V3_A {
    pub TransportCredentials: u32,
    pub Flags: u32,
    pub AuthenticationTarget: u32,
    pub NumberOfAuthnSchemes: u32,
    pub field_AuthnSchemes: u32,
    pub field_ServerCertificateSubject: u32,
    pub ProxyCredentials: u32,
    pub NumberOfProxyAuthnSchemes: u32,
    pub field_ProxyAuthnSchemes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V2_W {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V2_A {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V3_W {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V3_A {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V4_W {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
    pub EffectiveOnly: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V4_A {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
    pub EffectiveOnly: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V5_W {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
    pub EffectiveOnly: u32,
    pub field_ServerSecurityDescriptor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SECURITY_QOS_V5_A {
    pub Version: u32,
    pub Capabilities: u32,
    pub IdentityTracking: u32,
    pub ImpersonationType: u32,
    pub AdditionalSecurityInfoType: u32,
    pub field_HttpCredentials: u32,
    pub field_Sid: u32,
    pub EffectiveOnly: u32,
    pub field_ServerSecurityDescriptor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_HANDLE_TEMPLATE_V1_W {
    pub Version: u32,
    pub Flags: u32,
    pub ProtocolSequence: u32,
    pub field_NetworkAddress: u32,
    pub field_StringEndpoint: u32,
    pub field_Reserved: u32,
    pub ObjectUuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_HANDLE_TEMPLATE_V1_A {
    pub Version: u32,
    pub Flags: u32,
    pub ProtocolSequence: u32,
    pub field_NetworkAddress: u32,
    pub field_StringEndpoint: u32,
    pub field_Reserved: u32,
    pub ObjectUuid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_HANDLE_SECURITY_V1_W {
    pub Version: u32,
    pub field_ServerPrincName: u32,
    pub AuthnLevel: u32,
    pub AuthnSvc: u32,
    pub field_AuthIdentity: u32,
    pub field_SecurityQos: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_HANDLE_SECURITY_V1_A {
    pub Version: u32,
    pub field_ServerPrincName: u32,
    pub AuthnLevel: u32,
    pub AuthnSvc: u32,
    pub field_AuthIdentity: u32,
    pub field_SecurityQos: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_BINDING_HANDLE_OPTIONS_V1 {
    pub Version: u32,
    pub Flags: u32,
    pub ComTimeout: u32,
    pub CallTimeout: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_VERSION {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SYNTAX_IDENTIFIER {
    pub SyntaxGUID: u32,
    pub SyntaxVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_MESSAGE {
    pub Handle: u32,
    pub DataRepresentation: u32,
    pub Buffer: usize,
    pub BufferLength: u32,
    pub ProcNum: u32,
    pub TransferSyntax: usize,
    pub RpcInterfaceInformation: usize,
    pub ReservedForRuntime: usize,
    pub ManagerEpv: usize,
    pub ImportContext: usize,
    pub RpcFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_PROTSEQ_ENDPOINT {
    pub RpcProtocolSequence: usize,
    pub Endpoint: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SERVER_INTERFACE {
    pub Length: u32,
    pub InterfaceId: u32,
    pub TransferSyntax: u32,
    pub DispatchTable: usize,
    pub RpcProtseqEndpointCount: u32,
    pub RpcProtseqEndpoint: usize,
    pub field_DefaultManagerEpv: u32,
    pub field_InterpreterInfo: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_CLIENT_INTERFACE {
    pub Length: u32,
    pub InterfaceId: u32,
    pub TransferSyntax: u32,
    pub DispatchTable: usize,
    pub RpcProtseqEndpointCount: u32,
    pub RpcProtseqEndpoint: usize,
    pub Reserved: usize,
    pub InterpreterInfo: usize,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_SEC_CONTEXT_KEY_INFO {
    pub EncryptAlgorithm: u32,
    pub KeySize: u32,
    pub SignatureAlgorithm: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_TRANSFER_SYNTAX {
    pub Uuid: u32,
    pub VersMajor: u32,
    pub VersMinor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RPC_C_OPT_COOKIE_AUTH_DESCRIPTOR {
    pub BufferSize: u32,
    pub field_Buffer: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RDR_CALLOUT_STATE {
    pub LastError: u32,
    pub field_LastEEInfo: u32,
    pub LastCalledStage: u32,
    pub field_ServerName: u32,
    pub field_ServerPort: u32,
    pub field_RemoteUser: u32,
    pub field_AuthType: u32,
    pub ResourceTypePresent: u32,
    pub SessionIdPresent: u32,
    pub InterfacePresent: u32,
    pub ResourceType: u32,
    pub SessionId: u32,
    pub Interface: u32,
    pub field_CertContext: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_SCONTEXT {
    pub pad: [usize; 2],
    pub userContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCONTEXT_QUEUE {
    pub NumberOfObjects: u32,
    pub ArrayOfObjects: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_STUB_MESSAGE {
    pub RpcMsg: usize,
    pub Buffer: usize,
    pub BufferStart: usize,
    pub BufferEnd: usize,
    pub BufferMark: usize,
    pub BufferLength: u32,
    pub MemorySize: u32,
    pub Memory: usize,
    pub IsClient: u32,
    pub Pad: u32,
    pub uFlags2: u32,
    pub ReuseBuffer: u32,
    pub field_pAllocAllNodesContext: u32,
    pub field_pPointerQueueState: u32,
    pub IgnoreEmbeddedPointers: u32,
    pub PointerBufferMark: usize,
    pub CorrDespIncrement: u32,
    pub uFlags: u32,
    pub UniquePtrCount: u32,
    pub MaxCount: usize,
    pub Offset: u32,
    pub ActualCount: u32,
    pub reserved_padding: u32,
    pub field_: u32,
    pub StackTop: usize,
    pub pPresentedType: usize,
    pub pTransmitType: usize,
    pub SavedHandle: u32,
    pub StubDesc: usize,
    pub FullPtrXlatTables: usize,
    pub FullPtrRefId: u32,
    pub PointerLength: u32,
    pub fInDontFree: u32,
    pub fDontCallFreeInst: u32,
    pub fUnused1: u32,
    pub fHasReturn: u32,
    pub fHasExtensions: u32,
    pub fHasNewCorrDesc: u32,
    pub fIsIn: u32,
    pub fIsOut: u32,
    pub fIsOicf: u32,
    pub fBufferValid: u32,
    pub fHasMemoryValidateCallback: u32,
    pub fInFree: u32,
    pub fNeedMCCP: u32,
    pub fUnused2: u32,
    pub fUnused3: u32,
    pub dwDestContext: u32,
    pub pvDestContext: usize,
    pub SavedContextHandles: usize,
    pub ParamNumber: u32,
    pub pRpcChannelBuffer: usize,
    pub pArrayInfo: usize,
    pub SizePtrCountArray: usize,
    pub SizePtrOffsetArray: usize,
    pub SizePtrLengthArray: usize,
    pub pArgQueue: usize,
    pub dwStubPhase: u32,
    pub LowStackMark: usize,
    pub pAsyncMsg: usize,
    pub pCorrInfo: usize,
    pub pCorrMemory: usize,
    pub pMemoryList: usize,
    pub pCSInfo: u32,
    pub ConformanceMark: usize,
    pub VarianceMark: usize,
    pub Unused: u32,
    pub pContext: usize,
    pub ContextHandleHash: usize,
    pub pUserMarshalList: usize,
    pub pFullPtrFormat: usize,
    pub Reserved51_4: u32,
    pub Reserved51_5: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERIC_BINDING_ROUTINE_PAIR {
    pub pfnBind: u32,
    pub pfnUnbind: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GENERIC_BINDING_INFO {
    pub pObj: usize,
    pub Size: u32,
    pub pfnBind: u32,
    pub pfnUnbind: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct XMIT_ROUTINE_QUINTUPLE {
    pub pfnTranslateToXmit: u32,
    pub pfnTranslateFromXmit: u32,
    pub pfnFreeXmit: u32,
    pub pfnFreeInst: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USER_MARSHAL_ROUTINE_QUADRUPLE {
    pub pfnBufferSize: u32,
    pub pfnMarshall: u32,
    pub pfnUnmarshall: u32,
    pub pfnFree: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USER_MARSHAL_CB {
    pub Flags: u32,
    pub pStubMsg: usize,
    pub pReserve: usize,
    pub Signature: u32,
    pub CBType: u32,
    pub pFormat: usize,
    pub pTypeFormat: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MALLOC_FREE_STRUCT {
    pub pfnAllocate__size_t_: usize,
    pub field_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COMM_FAULT_OFFSETS {
    pub CommOffset: u32,
    pub FaultOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_CS_SIZE_CONVERT_ROUTINES {
    pub pfnNetSize: u32,
    pub pfnToNetCs: u32,
    pub pfnLocalSize: u32,
    pub pfnFromNetCs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_CS_ROUTINES {
    pub field_pSizeConvertRoutines: u32,
    pub field_pTagGettingRoutines: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_EXPR_DESC {
    pub pOffset: usize,
    pub pFormatExpr: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_STUB_DESC {
    pub RpcInterfaceInformation: usize,
    pub pfnAllocate__size_t_: usize,
    pub field_: u32,
    pub pAutoHandle: usize,
    pub pPrimitiveHandle: usize,
    pub pGenericBindingInfo: usize,
    pub apfnNdrRundownRoutines: usize,
    pub aGenericBindingRoutinePairs: usize,
    pub apfnExprEval: usize,
    pub aXmitQuintuple: usize,
    pub pFormatTypes: usize,
    pub fCheckBounds: u32,
    pub Version: u32,
    pub pMallocFreeStruct: usize,
    pub MIDLVersion: u32,
    pub CommFaultOffsets: usize,
    pub aUserMarshalQuadruple: usize,
    pub NotifyRoutineTable: usize,
    pub mFlags: usize,
    pub CsRoutineTables: usize,
    pub ProxyServerInfo: usize,
    pub pExprInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_FORMAT_STRING {
    pub Pad: u32,
    pub Format: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_METHOD_PROPERTY {
    pub Id: u32,
    pub Value: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_METHOD_PROPERTY_MAP {
    pub Count: u32,
    pub field_Properties: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_INTERFACE_METHOD_PROPERTIES {
    pub MethodCount: u32,
    pub field_MethodProperties: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_SERVER_INFO {
    pub pStubDesc: usize,
    pub DispatchTable: usize,
    pub ProcString: usize,
    pub FmtStringOffset: usize,
    pub ThunkTable: usize,
    pub pTransferSyntax: usize,
    pub nCount: usize,
    pub pSyntaxInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_STUBLESS_PROXY_INFO {
    pub pStubDesc: usize,
    pub ProcFormatString: usize,
    pub FormatStringOffset: usize,
    pub pTransferSyntax: usize,
    pub nCount: usize,
    pub pSyntaxInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_SYNTAX_INFO {
    pub TransferSyntax: u32,
    pub DispatchTable: usize,
    pub ProcString: usize,
    pub FmtStringOffset: usize,
    pub TypeString: usize,
    pub aUserMarshalQuadruple: usize,
    pub field_pMethodProperties: u32,
    pub pReserved2: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FULL_PTR_XLAT_TABLES {
    pub RefIdToPointer: usize,
    pub PointerToRefId: usize,
    pub NextRefId: u32,
    pub XlatSide: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_INTERCEPTION_INFO {
    pub Version: u32,
    pub ProcString: usize,
    pub field_ProcFormatOffsetTable: u32,
    pub ProcCount: u32,
    pub TypeString: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIDL_WINRT_TYPE_SERIALIZATION_INFO {
    pub Version: u32,
    pub TypeFormatString: usize,
    pub FormatStringSize: u32,
    pub TypeOffset: u32,
    pub StubDesc: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_USER_MARSHAL_INFO_LEVEL1 {
    pub Buffer: usize,
    pub BufferSize: u32,
    pub pfnAllocate__size_t_: usize,
    pub field_: u32,
    pub pRpcChannelBuffer: usize,
    pub Reserved: [usize; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDR_USER_MARSHAL_INFO {
    pub InformationLevel: u32,
    pub Level1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct P_impl {
    pub field_d_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct R_impl {
    pub field_d_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct M_ {
    pub field_d_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct A_ {
    pub field_d_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct F_ {
    pub field_d_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSISCAN_CMD {
    pub Reserved1: u32,
    pub Size: u32,
    pub SrbFlags: u32,
    pub CdbLength: u8,
    pub SenseLength: u8,
    pub TransferLength: u32,
    pub pSrbStatus: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCSISCAN_INFO {
    pub Size: u32,
    pub Flags: u32,
    pub PortNumber: u8,
    pub PathId: u8,
    pub TargetId: u8,
    pub Lun: u8,
    pub AdapterName: [u8; 1],
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_INTEGER {
    pub LowPart: u32,
    pub HighPart: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_STRING {
    pub Length: u32,
    pub MaximumLength: u32,
    pub Buffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgInfoA {
    pub fCapabilities: u32,
    pub wVersion: u32,
    pub wRPCID: u32,
    pub cbMaxToken: u32,
    pub Name: usize,
    pub Comment: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_NamesA {
    pub sUserName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgCredentials_SSIProviderA {
    pub sProviderName: usize,
    pub ProviderInfoLength: u32,
    pub ProviderInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_LastClientTokenStatus {
    pub LastClientTokenStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NamesA {
    pub sUserName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_KeyInfoA {
    pub sSignatureAlgorithmName: usize,
    pub sEncryptAlgorithmName: usize,
    pub KeySize: u32,
    pub SignatureAlgorithm: u32,
    pub EncryptAlgorithm: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_AuthorityA {
    pub sAuthorityName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_ProtoInfoA {
    pub sProtocolName: usize,
    pub majorVersion: u32,
    pub minorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_PackageInfoA {
    pub PackageInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegotiationInfoA {
    pub PackageInfo: usize,
    pub NegotiationState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NativeNamesW {
    pub sClientName: usize,
    pub sServerName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NativeNamesA {
    pub sClientName: usize,
    pub sServerName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_CredentialNameA {
    pub CredentialType: u32,
    pub field_sCredentialName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_AccessToken {
    pub AccessToken: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_TargetInformation {
    pub MarshalledTargetInfoLength: u32,
    pub MarshalledTargetInfo: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_AuthzID {
    pub AuthzIDLength: u32,
    pub AuthzID: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_Target {
    pub TargetLength: u32,
    pub Target: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_ClientSpecifiedTarget {
    pub sTargetName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_Bindings {
    pub BindingsLength: u32,
    pub Bindings: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_ApplicationProtocol {
    pub ProtoNegoStatus: u32,
    pub ProtoNegoExt: u32,
    pub ProtocolIdSize: u32,
    pub ProtocolId: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecPkgContext_NegotiatedTlsExtensions {
    pub ExtensionsCount: u32,
    pub Extensions: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECPKG_APP_MODE_INFO {
    pub UserFunction: u32,
    pub Argument1: usize,
    pub Argument2: usize,
    pub UserData: u32,
    pub ReturnToLsa: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SecurityFunctionTableA {
    pub dwVersion: u32,
    pub EnumerateSecurityPackagesA: u32,
    pub QueryCredentialsAttributesA: u32,
    pub AcquireCredentialsHandleA: u32,
    pub FreeCredentialHandle: u32,
    pub Reserved2: usize,
    pub InitializeSecurityContextA: u32,
    pub AcceptSecurityContext: u32,
    pub CompleteAuthToken: u32,
    pub DeleteSecurityContext: u32,
    pub ApplyControlToken: u32,
    pub QueryContextAttributesA: u32,
    pub ImpersonateSecurityContext: u32,
    pub RevertSecurityContext: u32,
    pub MakeSignature: u32,
    pub VerifySignature: u32,
    pub FreeContextBuffer: u32,
    pub QuerySecurityPackageInfoA: u32,
    pub Reserved3: usize,
    pub Reserved4: usize,
    pub ExportSecurityContext: u32,
    pub ImportSecurityContextA: u32,
    pub AddCredentialsA: u32,
    pub Reserved8: usize,
    pub QuerySecurityContextToken: u32,
    pub EncryptMessage: u32,
    pub DecryptMessage: u32,
    pub SetContextAttributesA: u32,
    pub SetCredentialsAttributesA: u32,
    pub ChangeAccountPasswordA: u32,
    pub Reserved9: usize,
    pub QueryContextAttributesExA: u32,
    pub QueryCredentialsAttributesExA: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_IDENTITY_EXA {
    pub Version: u32,
    pub Length: u32,
    pub field_User: u32,
    pub UserLength: u32,
    pub field_Domain: u32,
    pub DomainLength: u32,
    pub field_Password: u32,
    pub PasswordLength: u32,
    pub Flags: u32,
    pub PackageList: usize,
    pub PackageListLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_BYTE_VECTOR {
    pub ByteArrayOffset: u32,
    pub ByteArrayLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_DATA {
    pub CredType: u32,
    pub CredData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_PACKED_CREDENTIALS {
    pub cbHeaderLength: u32,
    pub cbStructureLength: u32,
    pub AuthData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_DATA_PASSWORD {
    pub UnicodePassword: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_CERTIFICATE_DATA {
    pub cbHeaderLength: u32,
    pub cbStructureLength: u32,
    pub Certificate: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_NGC_DATA {
    pub LogonId: u32,
    pub Flags: u32,
    pub CspInfo: u32,
    pub UserIdKeyAuthTicket: u32,
    pub DecryptionKeyName: u32,
    pub DecryptionKeyAuthTicket: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_DATA_TYPE_SMARTCARD_CONTEXTS_DATA {
    pub pcc: usize,
    pub hProv: usize,
    pub pwszECDHKeyName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_FIDO_DATA {
    pub cbHeaderLength: u32,
    pub cbStructureLength: u32,
    pub Secret: u32,
    pub NewSecret: u32,
    pub EncryptedNewSecret: u32,
    pub NetworkLogonBuffer: u32,
    pub ulSignatureCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_CREDUI_CONTEXT_VECTOR {
    pub CredUIContextArrayOffset: u32,
    pub CredUIContextCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_SHORT_VECTOR {
    pub ShortArrayOffset: u32,
    pub ShortArrayCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CREDUIWIN_MARSHALED_CONTEXT {
    pub StructureType: u32,
    pub cbHeaderLength: u16,
    pub LogonId: u32,
    pub MarshaledDataType: u32,
    pub MarshaledDataOffset: u32,
    pub MarshaledDataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_CREDUI_CONTEXT {
    pub cbHeaderLength: u16,
    pub CredUIContextHandle: usize,
    pub UIInfo: usize,
    pub UIInfo_1: usize,
    pub dwAuthError: u32,
    pub pInputAuthIdentity: usize,
    pub TargetName: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SEC_WINNT_AUTH_PACKED_CREDENTIALS_EX {
    pub cbHeaderLength: u32,
    pub Flags: u32,
    pub PackedCredentials: u32,
    pub PackageList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_PACKAGE_OPTIONS {
    pub Size: u32,
    pub Type: u32,
    pub Flags: u32,
    pub SignatureSize: u32,
    pub Signature: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TPM_DEVICE_INFO {
    pub structVersion: u32,
    pub tpmVersion: u32,
    pub tpmInterfaceType: u32,
    pub tpmImpRevision: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_GET_UUID_OUTPUT {
    pub Uuid: u32,
    pub ProductId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_NVM_WRITE_INPUT {
    pub NVMOffset: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_NVM_WRITE_OUTPUT {
    pub NVMStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_NVM_WRITE_AUTHENTICATE_OUTPUT {
    pub NVMStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_NVM_READ_INPUT {
    pub NVMOffset: u32,
    pub DataLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TBT3_NVM_READ_OUTPUT {
    pub NVMStatus: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SYN_OPTS_ROS_v0 {
    pub ActiveOpen: u8,
    pub MssRcvd: u32,
    pub MssSent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_DATA_ROD_v0 {
    pub DataBytesOut: u32,
    pub DataSegsOut: u32,
    pub DataBytesIn: u32,
    pub DataSegsIn: u32,
    pub SegsOut: u32,
    pub SegsIn: u32,
    pub SoftErrors: u32,
    pub SoftErrorReason: u32,
    pub SndUna: u32,
    pub SndNxt: u32,
    pub SndMax: u32,
    pub ThruBytesAcked: u32,
    pub RcvNxt: u32,
    pub ThruBytesReceived: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_DATA_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SND_CONG_ROD_v0 {
    pub SndLimTransRwin: u32,
    pub SndLimTimeRwin: u32,
    pub SndLimBytesRwin: usize,
    pub SndLimTransCwnd: u32,
    pub SndLimTimeCwnd: u32,
    pub SndLimBytesCwnd: usize,
    pub SndLimTransSnd: u32,
    pub SndLimTimeSnd: u32,
    pub SndLimBytesSnd: usize,
    pub SlowStart: u32,
    pub CongAvoid: u32,
    pub OtherReductions: u32,
    pub CurCwnd: u32,
    pub MaxSsCwnd: u32,
    pub MaxCaCwnd: u32,
    pub CurSsthresh: u32,
    pub MaxSsthresh: u32,
    pub MinSsthresh: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SND_CONG_ROS_v0 {
    pub LimCwnd: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SND_CONG_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_PATH_ROD_v0 {
    pub FastRetran: u32,
    pub Timeouts: u32,
    pub SubsequentTimeouts: u32,
    pub CurTimeoutCount: u32,
    pub AbruptTimeouts: u32,
    pub PktsRetrans: u32,
    pub BytesRetrans: u32,
    pub DupAcksIn: u32,
    pub SacksRcvd: u32,
    pub SackBlocksRcvd: u32,
    pub CongSignals: u32,
    pub PreCongSumCwnd: u32,
    pub PreCongSumRtt: u32,
    pub PostCongSumRtt: u32,
    pub PostCongCountRtt: u32,
    pub EcnSignals: u32,
    pub EceRcvd: u32,
    pub SendStall: u32,
    pub QuenchRcvd: u32,
    pub RetranThresh: u32,
    pub SndDupAckEpisodes: u32,
    pub SumBytesReordered: u32,
    pub NonRecovDa: u32,
    pub NonRecovDaEpisodes: u32,
    pub AckAfterFr: u32,
    pub DsackDups: u32,
    pub SampleRtt: u32,
    pub SmoothedRtt: u32,
    pub RttVar: u32,
    pub MaxRtt: u32,
    pub MinRtt: u32,
    pub SumRtt: u32,
    pub CountRtt: u32,
    pub CurRto: u32,
    pub MaxRto: u32,
    pub MinRto: u32,
    pub CurMss: u32,
    pub MaxMss: u32,
    pub MinMss: u32,
    pub SpuriousRtoDetections: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_PATH_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SEND_BUFF_ROD_v0 {
    pub CurRetxQueue: usize,
    pub MaxRetxQueue: usize,
    pub CurAppWQueue: usize,
    pub MaxAppWQueue: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_SEND_BUFF_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_REC_ROD_v0 {
    pub CurRwinSent: u32,
    pub MaxRwinSent: u32,
    pub MinRwinSent: u32,
    pub LimRwin: u32,
    pub DupAckEpisodes: u32,
    pub DupAcksOut: u32,
    pub CeRcvd: u32,
    pub EcnSent: u32,
    pub EcnNoncesRcvd: u32,
    pub CurReasmQueue: u32,
    pub MaxReasmQueue: u32,
    pub CurAppRQueue: usize,
    pub MaxAppRQueue: usize,
    pub WinScaleSent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_REC_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_OBS_REC_ROD_v0 {
    pub CurRwinRcvd: u32,
    pub MaxRwinRcvd: u32,
    pub MinRwinRcvd: u32,
    pub WinScaleRcvd: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_OBS_REC_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_BANDWIDTH_RW_v0 {
    pub EnableCollectionOutbound: u32,
    pub EnableCollectionInbound: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_BANDWIDTH_ROD_v0 {
    pub OutboundBandwidth: u32,
    pub InboundBandwidth: u32,
    pub OutboundInstability: u32,
    pub InboundInstability: u32,
    pub OutboundBandwidthPeaked: u8,
    pub InboundBandwidthPeaked: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_FINE_RTT_RW_v0 {
    pub EnableCollection: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCP_ESTATS_FINE_RTT_ROD_v0 {
    pub RttVar: u32,
    pub MaxRtt: u32,
    pub MinRtt: u32,
    pub SumRtt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPROW_LH {
    pub dwState: u32,
    pub State: u32,
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPROW_W2K {
    pub dwState: u32,
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPROW2 {
    pub dwState: u32,
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
    pub dwOwningPid: u32,
    pub dwOffloadState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPTABLE2 {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPROW_OWNER_PID {
    pub dwState: u32,
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
    pub dwOwningPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPTABLE_OWNER_PID {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPROW_OWNER_MODULE {
    pub dwState: u32,
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPTABLE_OWNER_MODULE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6ROW {
    pub State: u32,
    pub LocalAddr: u32,
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub RemoteAddr: u32,
    pub dwRemoteScopeId: u32,
    pub dwRemotePort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6ROW2 {
    pub LocalAddr: u32,
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub RemoteAddr: u32,
    pub dwRemoteScopeId: u32,
    pub dwRemotePort: u32,
    pub State: u32,
    pub dwOwningPid: u32,
    pub dwOffloadState: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6TABLE2 {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6ROW_OWNER_PID {
    pub ucLocalAddr: [u8; 16],
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub ucRemoteAddr: [u8; 16],
    pub dwRemoteScopeId: u32,
    pub dwRemotePort: u32,
    pub dwState: u32,
    pub dwOwningPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6TABLE_OWNER_PID {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6ROW_OWNER_MODULE {
    pub ucLocalAddr: [u8; 16],
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub ucRemoteAddr: [u8; 16],
    pub dwRemoteScopeId: u32,
    pub dwRemotePort: u32,
    pub dwState: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub OwningModuleInfo: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCP6TABLE_OWNER_MODULE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPSTATS_LH {
    pub dwRtoAlgorithm: u32,
    pub RtoAlgorithm: u32,
    pub dwRtoMin: u32,
    pub dwRtoMax: u32,
    pub dwMaxConn: u32,
    pub dwActiveOpens: u32,
    pub dwPassiveOpens: u32,
    pub dwAttemptFails: u32,
    pub dwEstabResets: u32,
    pub dwCurrEstab: u32,
    pub dwInSegs: u32,
    pub dwOutSegs: u32,
    pub dwRetransSegs: u32,
    pub dwInErrs: u32,
    pub dwOutRsts: u32,
    pub dwNumConns: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPSTATS_W2K {
    pub dwRtoAlgorithm: u32,
    pub dwRtoMin: u32,
    pub dwRtoMax: u32,
    pub dwMaxConn: u32,
    pub dwActiveOpens: u32,
    pub dwPassiveOpens: u32,
    pub dwAttemptFails: u32,
    pub dwEstabResets: u32,
    pub dwCurrEstab: u32,
    pub dwInSegs: u32,
    pub dwOutSegs: u32,
    pub dwRetransSegs: u32,
    pub dwInErrs: u32,
    pub dwOutRsts: u32,
    pub dwNumConns: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_TCPSTATS2 {
    pub RtoAlgorithm: u32,
    pub dwRtoMin: u32,
    pub dwRtoMax: u32,
    pub dwMaxConn: u32,
    pub dwActiveOpens: u32,
    pub dwPassiveOpens: u32,
    pub dwAttemptFails: u32,
    pub dwEstabResets: u32,
    pub dwCurrEstab: u32,
    pub dw64InSegs: u64,
    pub dw64OutSegs: u64,
    pub dwRetransSegs: u32,
    pub dwInErrs: u32,
    pub dwOutRsts: u32,
    pub dwNumConns: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_ADDRESS {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TRANSPORT_ADDRESS {
    pub TAAddressCount: i32,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_NETBIOS_UNICODE_EX_XP {
    pub NetbiosNameType: u16,
    pub NameBufferType: u32,
    pub EndpointName: u32,
    pub RemoteName: u32,
    pub EndpointBuffer: [u32; 17],
    pub RemoteNameBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_NETBIOS_UNICODE_EX_ADDRESS_XP {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrNetbiosWCharEx {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_NETBIOS {
    pub NetbiosNameType: u16,
    pub NetbiosName: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_NETBIOS_EX {
    pub EndpointName: [u8; 16],
    pub NetbiosAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_NETONE {
    pub NetoneNameType: u16,
    pub NetoneName: [u8; 20],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_APPLETALK {
    pub Network: u16,
    pub Node: u8,
    pub Socket: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_8022 {
    pub MACAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_IP {
    pub sin_port: u16,
    pub in_addr: u32,
    pub sin_zero: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_IP6_WIN2K {
    pub sin6_port: u16,
    pub sin6_flowinfo: u32,
    pub sin6_addr: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_IP6_XP {
    pub sin6_port: u16,
    pub sin6_flowinfo: u32,
    pub sin6_addr: [u16; 8],
    pub sin6_scope_id: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_IPX {
    pub NetworkAddress: u32,
    pub NodeAddress: [u8; 6],
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_NS {
    pub NetworkAddress: u32,
    pub NodeAddress: [u8; 6],
    pub Socket: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_VNS {
    pub net_address: [u8; 4],
    pub subnet_addr: [u8; 2],
    pub port: [u8; 2],
    pub hops: u8,
    pub filler: [u8; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_OSI_TSAP {
    pub tp_addr_type: u16,
    pub tp_taddr_len: u16,
    pub tp_tsel_len: u16,
    pub tp_addr: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_NETBIOS_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct Addr {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_NETBIOS_EX_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrNetbiosEx {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_APPLETALK_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub Address_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrAtalk {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_IP_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrIp {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_IP6_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrIp6 {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_IPX_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrIpx {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_NS_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrNs {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TA_VNS_ADDRESS {
    pub TAAddressCount: i32,
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
    pub reserved_padding: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct AddrVns {
    pub AddressLength: u16,
    pub AddressType: u16,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST {
    pub AddressHandle: usize,
    pub ConnectionContext: u32,
    pub ControlChannel: usize,
    pub RequestNotifyObject: usize,
    pub RequestContext: usize,
    pub TdiStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_STATUS {
    pub Status: u32,
    pub RequestContext: usize,
    pub BytesTransferred: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_CONNECTION_INFORMATION {
    pub UserDataLength: i32,
    pub UserData: u32,
    pub OptionsLength: i32,
    pub Options: u32,
    pub RemoteAddressLength: i32,
    pub RemoteAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_ASSOCIATE_ADDRESS {
    pub Request: u32,
    pub AddressHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_CONNECT {
    pub Request: u32,
    pub RequestConnectionInformation: usize,
    pub ReturnConnectionInformation: usize,
    pub Timeout: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_ACCEPT {
    pub Request: u32,
    pub RequestConnectionInformation: usize,
    pub ReturnConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_LISTEN {
    pub Request: u32,
    pub RequestConnectionInformation: usize,
    pub ReturnConnectionInformation: usize,
    pub ListenFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_DISCONNECT {
    pub Request: u32,
    pub Timeout: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_SEND {
    pub Request: u32,
    pub SendFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_RECEIVE {
    pub Request: u32,
    pub ReceiveFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_SEND_DATAGRAM {
    pub Request: u32,
    pub SendDatagramInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_RECEIVE_DATAGRAM {
    pub Request: u32,
    pub ReceiveDatagramInformation: usize,
    pub ReturnInformation: usize,
    pub ReceiveFlags: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_SET_EVENT_HANDLER {
    pub Request: u32,
    pub EventType: i32,
    pub EventHandler: usize,
    pub EventContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_QUERY_INFORMATION {
    pub Request: u32,
    pub QueryType: u32,
    pub RequestConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_REQUEST_SET_INFORMATION {
    pub Request: u32,
    pub SetType: u32,
    pub RequestConnectionInformation: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ENDPOINT_INFO {
    pub State: u32,
    pub Event: u32,
    pub TransmittedTsdus: u32,
    pub ReceivedTsdus: u32,
    pub TransmissionErrors: u32,
    pub ReceiveErrors: u32,
    pub MinimumLookaheadData: u32,
    pub MaximumLookaheadData: u32,
    pub PriorityLevel: u32,
    pub SecurityLevel: u32,
    pub SecurityCompartment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_CONNECTION_INFO {
    pub State: u32,
    pub Event: u32,
    pub TransmittedTsdus: u32,
    pub ReceivedTsdus: u32,
    pub TransmissionErrors: u32,
    pub ReceiveErrors: u32,
    pub Throughput: i64,
    pub Delay: i64,
    pub SendBufferSize: u32,
    pub ReceiveBufferSize: u32,
    pub Unreliable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ADDRESS_INFO {
    pub ActivityCount: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_DATAGRAM_INFO {
    pub MaximumDatagramBytes: u32,
    pub MaximumDatagramCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_MAX_DATAGRAM_INFO {
    pub MaxDatagramSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_PROVIDER_INFO {
    pub Version: u32,
    pub MaxSendSize: u32,
    pub MaxConnectionUserData: u32,
    pub MaxDatagramSize: u32,
    pub ServiceFlags: u32,
    pub MinimumLookaheadData: u32,
    pub MaximumLookaheadData: u32,
    pub NumberOfResources: u32,
    pub StartTime: i64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ROUTING_INFO_WS03 {
    pub Protocol: u32,
    pub InterfaceId: u32,
    pub LinkId: u32,
    pub Address: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_PROVIDER_RESOURCE_STATS {
    pub ResourceId: u32,
    pub MaximumResourceUsed: u32,
    pub AverageResourceUsed: u32,
    pub ResourceExhausted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_PROVIDER_STATISTICS {
    pub Version: u32,
    pub OpenConnections: u32,
    pub ConnectionsAfterNoRetry: u32,
    pub ConnectionsAfterRetry: u32,
    pub LocalDisconnects: u32,
    pub RemoteDisconnects: u32,
    pub LinkFailures: u32,
    pub AdapterFailures: u32,
    pub SessionTimeouts: u32,
    pub CancelledConnections: u32,
    pub RemoteResourceFailures: u32,
    pub LocalResourceFailures: u32,
    pub NotFoundFailures: u32,
    pub NoListenFailures: u32,
    pub DatagramsSent: u32,
    pub DatagramBytesSent: i64,
    pub DatagramsReceived: u32,
    pub DatagramBytesReceived: i64,
    pub PacketsSent: u32,
    pub PacketsReceived: u32,
    pub DataFramesSent: u32,
    pub DataFrameBytesSent: i64,
    pub DataFramesReceived: u32,
    pub DataFrameBytesReceived: i64,
    pub DataFramesResent: u32,
    pub DataFrameBytesResent: i64,
    pub DataFramesRejected: u32,
    pub DataFrameBytesRejected: i64,
    pub ResponseTimerExpirations: u32,
    pub AckTimerExpirations: u32,
    pub MaximumSendWindow: u32,
    pub AverageSendWindow: u32,
    pub PiggybackAckQueued: u32,
    pub PiggybackAckTimeouts: u32,
    pub WastedPacketSpace: i64,
    pub WastedSpacePackets: u32,
    pub NumberOfResources: u32,
    pub ResourceStats: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_ACTION_HEADER {
    pub TransportId: u32,
    pub ActionCode: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct STREAMS_TDI_ACTION {
    pub Header: u32,
    pub DatagramOption: u8,
    pub BufferLength: u32,
    pub Buffer: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TDI_TL_IO_CONTROL_ENDPOINT {
    pub Type: u32,
    pub Level: u32,
    pub IoControlCode: u32,
    pub OptionName: u32,
    pub InputBuffer: u32,
    pub InputBufferLength: u32,
    pub OutputBuffer: u32,
    pub OutputBufferLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TraceLoggingMetadata_t {
    pub Signature: u32,
    pub Size: u32,
    pub Version: u32,
    pub Flags: u32,
    pub Magic: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgProviderMetadata_t {
    pub Type: u32,
    pub ProviderId: u32,
    pub RemainingSize: u32,
    pub ProviderName: [u32; 1],
    pub ChunkSize: u32,
    pub ChunkType: u32,
    pub ChunkData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgEventMetadata_t {
    pub Type: u32,
    pub Channel: u8,
    pub Level: u8,
    pub Opcode: u8,
    pub Keyword: u64,
    pub RemainingSize: u32,
    pub Tags: [u32; 1],
    pub EventName: [u32; 1],
    pub FieldName: [u32; 1],
    pub InType: u32,
    pub OutType: u32,
    pub Tags_1: [u32; 1],
    pub ValueCount: u32,
    pub TypeInfoSize: u32,
    pub TypeInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgProvider_t {
    pub LevelPlus1: u32,
    pub ProviderMetadataPtr: usize,
    pub KeywordAny: u64,
    pub KeywordAll: u64,
    pub RegHandle: u32,
    pub EnableCallback: usize,
    pub CallbackContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgIndexSequence {
    pub Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWriteTemplate {
    pub WrappedTys______type: u32,
    pub field_2: u32,
    pub values___: u32,
    pub data: [u32; 1],
    pub wrappedArgs____: u32,
    pub data_: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperByVal {
    pub field_1: u32,
    pub StorageType: u32,
    pub Value: u32,
    pub Size_: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperByRef {
    pub field_1: u32,
    pub Ptr: usize,
    pub Size_: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperTypeForScalar {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapSid {
    pub field_1: u32,
    pub Sid: usize,
    pub field_tlgGetLengthSid_Sid__: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapSz {
    pub field_1: u32,
    pub Psz: usize,
    pub Psz_: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperPtrSize {
    pub field_1: u32,
    pub Ptr: usize,
    pub Size: u32,
    pub Size_: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperPtrSizeType {
    pub field_1: u32,
    pub Ptr: usize,
    pub Size: u32,
    pub Reserved: u32,
    pub field_UINT_PTR_Ptr: u32,
    pub Size_1: u32,
    pub Reserved_1: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperBinary {
    pub field_2: u32,
    pub Ptr: usize,
    pub Size: u32,
    pub field_2_: u32,
    pub Size_: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapBuffer {
    pub field_2: u32,
    pub Ptr: usize,
    pub field_2_: u32,
    pub static_cast_UINT16__Ptr__Length__: u32,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgWrapperArray {
    pub field_2: u32,
    pub Ptr: usize,
    pub Length: u32,
    pub field_2_: u32,
    pub Size_: usize,
    pub pDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgCheckStructFieldCount {
    pub field_128___: u32,
    pub field_0: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgRemoveReference {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgDecay_cv {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgDecay_noref {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgDecay {
    pub field_type: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgTypeMapBase {
    pub TraceLoggingValue___: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct traceLoggingKeyword {
    pub n: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct tlgTagEnc {
    pub integer___: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TCI_CLIENT_FUNC_LIST {
    pub ClNotifyHandler: u32,
    pub ClAddFlowCompleteHandler: u32,
    pub ClModifyFlowCompleteHandler: u32,
    pub ClDeleteFlowCompleteHandler: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ADDRESS_LIST_DESCRIPTOR {
    pub MediaType: u32,
    pub AddressList: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TC_IFC_DESCRIPTOR {
    pub Length: u32,
    pub pInterfaceName: usize,
    pub pInterfaceID: usize,
    pub AddressListDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TC_SUPPORTED_INFO_BUFFER {
    pub InstanceIDLength: u16,
    pub InstanceID: [u32; 1],
    pub InterfaceLuid: u32,
    pub AddrListDesc: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TC_GEN_FILTER {
    pub AddressType: u16,
    pub PatternSize: u32,
    pub Pattern: u32,
    pub Mask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TC_GEN_FLOW {
    pub SendingFlowspec: u32,
    pub ReceivingFlowspec: u32,
    pub TcObjectsLength: u32,
    pub TcObjects: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IP_PATTERN {
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub SrcAddr: u32,
    pub DstAddr: u32,
    pub s_srcport: u16,
    pub s_dstport: u16,
    pub s_type: u8,
    pub s_code: u8,
    pub S_Spi: u32,
    pub ProtocolId: u8,
    pub Reserved3: [u8; 3],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IPX_PATTERN {
    pub NetworkAddress: u32,
    pub NodeAddress: [u8; 6],
    pub Socket: u16,
    pub Dest: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ENUMERATION_BUFFER {
    pub Length: u32,
    pub OwnerProcessId: u32,
    pub FlowNameLength: u16,
    pub FlowName: [u32; 1],
    pub pFlow: usize,
    pub NumberOfFilters: u32,
    pub GenericFilter: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAYMENT_SERVICE_IN {
    pub CmdId: u32,
    pub ValidMatchDuration: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PAYMENT_SERVICE_OUT {
    pub Status: i32,
    pub BioMatchTime: u64,
    pub CurrentTime: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SERVICE_INFORMATION_V1 {
    pub InterfaceVersion: u32,
    pub ServiceMajorVersion: u32,
    pub ServiceMinorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SERVICE_REQUEST_V1 {
    pub InterfaceVersion: u32,
    pub ServiceMajorVersion: u32,
    pub ServiceMinorVersion: u32,
    pub FunctionCode: u32,
    pub InputBuffer: u32,
    pub InputBufferSize: u32,
    pub OutputBuffer: u32,
    pub OutputBufferSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct TR_SERVICE_REQUEST_RESPONSE_V1 {
    pub InterfaceVersion: u32,
    pub BytesWritten: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VIDEOPARAMETERS {
    pub Guid: u32,
    pub dwOffset: u32,
    pub dwCommand: u32,
    pub dwFlags: u32,
    pub dwMode: u32,
    pub dwTVStandard: u32,
    pub dwAvailableModes: u32,
    pub dwAvailableTVStandard: u32,
    pub dwFlickerFilter: u32,
    pub dwOverScanX: u32,
    pub dwOverScanY: u32,
    pub dwMaxUnscaledX: u32,
    pub dwMaxUnscaledY: u32,
    pub dwPositionX: u32,
    pub dwPositionY: u32,
    pub dwBrightness: u32,
    pub dwContrast: u32,
    pub dwCPType: u32,
    pub dwCPCommand: u32,
    pub dwCPStandard: u32,
    pub dwCPKey: u32,
    pub bCP_APSTriggerBits: u32,
    pub bOEMCopyProtection: [u8; 256],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPROW {
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPTABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPROW_OWNER_PID {
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPTABLE_OWNER_PID {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPROW_OWNER_MODULE {
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub SpecificPortBind: u32,
    pub dwFlags: u32,
    pub OwningModuleInfo: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPTABLE_OWNER_MODULE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPROW2 {
    pub dwLocalAddr: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub SpecificPortBind: u32,
    pub dwFlags: u32,
    pub OwningModuleInfo: [u64; 1],
    pub dwRemoteAddr: u32,
    pub dwRemotePort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPTABLE2 {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6ROW {
    pub dwLocalAddr: u32,
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6TABLE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6ROW_OWNER_PID {
    pub ucLocalAddr: [u8; 16],
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6TABLE_OWNER_PID {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6ROW_OWNER_MODULE {
    pub ucLocalAddr: [u8; 16],
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub SpecificPortBind: u32,
    pub dwFlags: u32,
    pub OwningModuleInfo: [u64; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6TABLE_OWNER_MODULE {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6ROW2 {
    pub ucLocalAddr: [u8; 16],
    pub dwLocalScopeId: u32,
    pub dwLocalPort: u32,
    pub dwOwningPid: u32,
    pub liCreateTimestamp: i64,
    pub SpecificPortBind: u32,
    pub dwFlags: u32,
    pub OwningModuleInfo: [u64; 1],
    pub ucRemoteAddr: [u8; 16],
    pub dwRemoteScopeId: u32,
    pub dwRemotePort: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDP6TABLE2 {
    pub dwNumEntries: u32,
    pub table: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPSTATS {
    pub dwInDatagrams: u32,
    pub dwNoPorts: u32,
    pub dwInErrors: u32,
    pub dwOutDatagrams: u32,
    pub dwNumAddrs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MIB_UDPSTATS2 {
    pub dw64InDatagrams: u64,
    pub dwNoPorts: u32,
    pub dwInErrors: u32,
    pub dw64OutDatagrams: u64,
    pub dwNumAddrs: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct UFS_STRING_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorIDN: u8,
    pub String: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_VERSION_INFORMATION {
    pub USBDI_Version: u32,
    pub Supported_USB_Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_DEVICE_INFORMATION {
    pub OffsetNext: u32,
    pub UsbdDeviceHandle: usize,
    pub DeviceDescriptor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_PIPE_INFORMATION {
    pub MaximumPacketSize: u16,
    pub EndpointAddress: u8,
    pub Interval: u8,
    pub PipeType: u32,
    pub PipeHandle: u32,
    pub MaximumTransferSize: u32,
    pub PipeFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_ENDPOINT_OFFLOAD_INFORMATION_V1 {
    pub Size: u32,
    pub EndpointAddress: u16,
    pub ResourceId: u32,
    pub Mode: u32,
    pub RootHubPortNumber: u32,
    pub RouteString: u32,
    pub Speed: u32,
    pub UsbDeviceAddress: u32,
    pub SlotId: u32,
    pub MultiTT: u32,
    pub LSOrFSDeviceConnectedToTTHub: u32,
    pub Reserved0: u32,
    pub TransferSegmentLA: u64,
    pub TransferSegmentVA: usize,
    pub TransferRingSize: u32,
    pub TransferRingInitialCycleBit: u32,
    pub MessageNumber: u32,
    pub EventRingSegmentLA: u64,
    pub EventRingSegmentVA: usize,
    pub EventRingSize: u32,
    pub EventRingInitialCycleBit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_ENDPOINT_OFFLOAD_INFORMATION {
    pub Size: u32,
    pub EndpointAddress: u16,
    pub ResourceId: u32,
    pub Mode: u32,
    pub RootHubPortNumber: u32,
    pub RouteString: u32,
    pub Speed: u32,
    pub UsbDeviceAddress: u32,
    pub SlotId: u32,
    pub MultiTT: u32,
    pub LSOrFSDeviceConnectedToTTHub: u32,
    pub Reserved0: u32,
    pub TransferSegmentLA: u64,
    pub TransferSegmentVA: usize,
    pub TransferRingSize: u32,
    pub TransferRingInitialCycleBit: u32,
    pub MessageNumber: u32,
    pub EventRingSegmentLA: u64,
    pub EventRingSegmentVA: usize,
    pub EventRingSize: u32,
    pub EventRingInitialCycleBit: u32,
    pub ClientTransferRingSegmentPAIn: u64,
    pub ClientTransferRingSizeIn: u32,
    pub ClientDataBufferPAIn: u64,
    pub ClientDataBufferSizeIn: u32,
    pub ClientDataBufferLAOut: u64,
    pub ClientDataBufferVAOut: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_INTERFACE_INFORMATION {
    pub Length: u16,
    pub InterfaceNumber: u8,
    pub AlternateSetting: u8,
    pub Class: u8,
    pub SubClass: u8,
    pub Protocol: u8,
    pub Reserved: u8,
    pub InterfaceHandle: u32,
    pub NumberOfPipes: u32,
    pub Pipes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_HCD_AREA {
    pub Reserved8: [usize; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_HEADER {
    pub Length: u16,
    pub Function: u16,
    pub Status: u32,
    pub UsbdDeviceHandle: usize,
    pub UsbdFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_SELECT_INTERFACE {
    pub Hdr: u32,
    pub ConfigurationHandle: u32,
    pub Interface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_SELECT_CONFIGURATION {
    pub Hdr: u32,
    pub ConfigurationDescriptor: usize,
    pub ConfigurationHandle: u32,
    pub Interface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_PIPE_REQUEST {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_FRAME_LENGTH_CONTROL {
    pub Hdr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_GET_FRAME_LENGTH {
    pub Hdr: u32,
    pub FrameLength: u32,
    pub FrameNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_SET_FRAME_LENGTH {
    pub Hdr: u32,
    pub FrameLengthDelta: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_GET_CURRENT_FRAME_NUMBER {
    pub Hdr: u32,
    pub FrameNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_DESCRIPTOR_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved0: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Reserved1: u16,
    pub Index: u8,
    pub DescriptorType: u8,
    pub LanguageId: u16,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_GET_STATUS_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved0: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Reserved1: [u8; 4],
    pub Index: u16,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_FEATURE_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved2: u32,
    pub Reserved3: u32,
    pub Reserved4: usize,
    pub Reserved5: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Reserved0: u16,
    pub FeatureSelector: u16,
    pub Index: u16,
    pub Reserved1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_VENDOR_OR_CLASS_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub TransferFlags: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub RequestTypeReservedBits: u8,
    pub Request: u8,
    pub Value: u16,
    pub Index: u16,
    pub Reserved1: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_GET_INTERFACE_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved0: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Reserved1: [u8; 4],
    pub Interface: u16,
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_GET_CONFIGURATION_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved0: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Reserved1: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct OS_STRING {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub MicrosoftString: [u32; 7],
    pub bVendorCode: u8,
    pub bPad: u8,
    pub bFlags: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_OS_FEATURE_DESCRIPTOR_REQUEST {
    pub Hdr: u32,
    pub Reserved: usize,
    pub Reserved0: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub Recipient: u8,
    pub Reserved1: u8,
    pub Reserved2: u8,
    pub InterfaceNumber: u8,
    pub MS_PageIndex: u8,
    pub MS_FeatureDescriptorIndex: u16,
    pub Reserved3: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_TRANSFER {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub TransferFlags: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub SetupPacket: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_CONTROL_TRANSFER_EX {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub TransferFlags: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub Timeout: u32,
    pub Pad: u32,
    pub hca: u32,
    pub SetupPacket: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_BULK_OR_INTERRUPT_TRANSFER {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub TransferFlags: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_ISO_PACKET_DESCRIPTOR {
    pub Offset: u32,
    pub Length: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_ISOCH_TRANSFER {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub TransferFlags: u32,
    pub TransferBufferLength: u32,
    pub TransferBuffer: usize,
    pub TransferBufferMDL: usize,
    pub field_UrbLink: u32,
    pub hca: u32,
    pub StartFrame: u32,
    pub NumberOfPackets: u32,
    pub ErrorCount: u32,
    pub IsoPacket: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBD_STREAM_INFORMATION {
    pub PipeHandle: u32,
    pub StreamID: u32,
    pub MaximumTransferSize: u32,
    pub PipeFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_OPEN_STATIC_STREAMS {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub NumberOfStreams: u32,
    pub StreamInfoVersion: u16,
    pub StreamInfoSize: u16,
    pub Streams: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS {
    pub Hdr: u32,
    pub PipeHandle: u32,
    pub MaximumSendPathDelayInMilliSeconds: u32,
    pub MaximumCompletionPathDelayInMilliSeconds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct URB {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB4_HRD_DEBUG_ROUTE_STRING {
    pub Depth: u32,
    pub Route: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB4_HRD_DEBUG_READ_CONFIGURATION_SPACE_INPUT {
    pub Route: u32,
    pub AdapterNumber: u32,
    pub ConfigurationSpaceType: u32,
    pub DwOffset: u32,
    pub DwLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB4_HRD_DEBUG_READ_CONFIGURATION_SPACE_OUTPUT {
    pub Usb4Status: u32,
    pub Data: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct ALTERNATE_INTERFACE {
    pub InterfaceNumber: u16,
    pub AlternateInterfaceNumber: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_NOTIFICATION {
    pub Event: u32,
    pub BusSpeed: u32,
    pub SetupPacket: u32,
    pub ConfigurationValue: u16,
    pub PortType: u32,
    pub AlternateInterface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_PIPE_INFORMATION {
    pub EpDesc: u32,
    pub PipeId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_CLASS_INTERFACE {
    pub InterfaceNumber: u32,
    pub PipeCount: u32,
    pub PipeArr: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_CLASS_INFORMATION_PACKET {
    pub FullSpeedClassInterface: u32,
    pub HighSpeedClassInterface: u32,
    pub InterfaceName: [u32; 1],
    pub InterfaceGuid: [u32; 1],
    pub HasInterfaceGuid: u8,
    pub SuperSpeedClassInterface: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_CLASS_INTERFACE_EX {
    pub BaseInterfaceNumber: u32,
    pub InterfaceCount: u32,
    pub PipeCount: u32,
    pub PipeArr: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_CLASS_INFORMATION_PACKET_EX {
    pub FullSpeedClassInterfaceEx: u32,
    pub HighSpeedClassInterfaceEx: u32,
    pub SuperSpeedClassInterfaceEx: u32,
    pub InterfaceName: [u32; 1],
    pub InterfaceGuid: [u32; 1],
    pub HasInterfaceGuid: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_INTERFACE_INFO {
    pub InterfaceNumber: u32,
    pub Speed: u32,
    pub Size: u16,
    pub InterfaceDescriptorSet: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_USB_STRING {
    pub StringIndex: u32,
    pub UsbString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBFN_BUS_CONFIGURATION_INFO {
    pub ConfigurationName: [u32; 1],
    pub IsCurrent: u8,
    pub IsActive: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_IDLE_CALLBACK_INFO {
    pub IdleCallback: usize,
    pub IdleContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_START_FAILDATA {
    pub LengthInBytes: u32,
    pub NtStatus: u32,
    pub UsbdStatus: u32,
    pub ConnectStatus: u32,
    pub DriverData: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_TOPOLOGY_ADDRESS {
    pub PciBusNumber: u32,
    pub PciDeviceNumber: u32,
    pub PciFunctionNumber: u32,
    pub Reserved: u32,
    pub RootHubPortNumber: u16,
    pub HubPortNumber: [u16; 5],
    pub Reserved2: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_INFORMATION {
    pub HubDescriptor: u32,
    pub HubIsBusPowered: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_MI_PARENT_INFORMATION {
    pub NumberOfInterfaces: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_INFORMATION {
    pub NodeType: u32,
    pub HubInformation: u32,
    pub MiParentInformation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_PIPE_INFO {
    pub EndpointDescriptor: u32,
    pub ScheduleOffset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_INFORMATION {
    pub ConnectionIndex: u32,
    pub DeviceDescriptor: u32,
    pub CurrentConfigurationValue: u8,
    pub LowSpeed: u8,
    pub DeviceIsHub: u8,
    pub DeviceAddress: u16,
    pub NumberOfOpenPipes: u32,
    pub ConnectionStatus: u32,
    pub PipeList: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_DRIVERKEY_NAME {
    pub ConnectionIndex: u32,
    pub ActualLength: u32,
    pub DriverKeyName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_NAME {
    pub ConnectionIndex: u32,
    pub ActualLength: u32,
    pub NodeName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_NAME {
    pub ActualLength: u32,
    pub HubName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_ROOT_HUB_NAME {
    pub ActualLength: u32,
    pub RootHubName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HCD_DRIVERKEY_NAME {
    pub ActualLength: u32,
    pub DriverKeyName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DESCRIPTOR_REQUEST {
    pub ConnectionIndex: u32,
    pub bmRequest: u8,
    pub bRequest: u8,
    pub wValue: u16,
    pub wIndex: u16,
    pub wLength: u16,
    pub Data: [u8; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_CAPABILITIES {
    pub HubIs2xCapable: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_ATTRIBUTES {
    pub ConnectionIndex: u32,
    pub ConnectionStatus: u32,
    pub PortAttributes: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_INFORMATION_EX {
    pub ConnectionIndex: u32,
    pub DeviceDescriptor: u32,
    pub CurrentConfigurationValue: u8,
    pub Speed: u8,
    pub DeviceIsHub: u8,
    pub DeviceAddress: u16,
    pub NumberOfOpenPipes: u32,
    pub ConnectionStatus: u32,
    pub PipeList: [u32; 0],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_CAPABILITIES_EX {
    pub CapabilityFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CYCLE_PORT_PARAMS {
    pub ConnectionIndex: u32,
    pub StatusReturned: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_ID_STRING {
    pub LanguageId: u16,
    pub Pad: u16,
    pub LengthInBytes: u32,
    pub Buffer: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_DEVICE_UXD_SETTINGS {
    pub Version: u32,
    pub PnpGuid: u32,
    pub OwnerGuid: u32,
    pub DeleteOnShutdown: u32,
    pub DeleteOnReload: u32,
    pub DeleteOnDisconnect: u32,
    pub Reserved: [u32; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HUB_DEVICE_CONFIG_INFO {
    pub Version: u32,
    pub Length: u32,
    pub HubFlags: u32,
    pub HardwareIds: u32,
    pub CompatibleIds: u32,
    pub DeviceDescription: u32,
    pub Reserved: [u32; 19],
    pub UxdSettings: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HCD_STAT_COUNTERS {
    pub BytesTransferred: u32,
    pub IsoMissedCount: u16,
    pub DataOverrunErrorCount: u16,
    pub CrcErrorCount: u16,
    pub ScheduleOverrunCount: u16,
    pub TimeoutErrorCount: u16,
    pub InternalHcErrorCount: u16,
    pub BufferOverrunErrorCount: u16,
    pub SWErrorCount: u16,
    pub StallPidCount: u16,
    pub PortDisableCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HCD_ISO_STAT_COUNTERS {
    pub LateUrbs: u16,
    pub DoubleBufferedPackets: u16,
    pub TransfersCF_5ms: u16,
    pub TransfersCF_2ms: u16,
    pub TransfersCF_1ms: u16,
    pub MaxInterruptLatency: u16,
    pub BadStartFrame: u16,
    pub StaleUrbs: u16,
    pub IsoPacketNotAccesed: u16,
    pub IsoPacketHWError: u16,
    pub SmallestUrbPacketCount: u16,
    pub LargestUrbPacketCount: u16,
    pub IsoCRC_Error: u16,
    pub IsoOVERRUN_Error: u16,
    pub IsoINTERNAL_Error: u16,
    pub IsoUNKNOWN_Error: u16,
    pub IsoBytesTransferred: u32,
    pub LateMissedCount: u16,
    pub HWIsoMissedCount: u16,
    pub Reserved7: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HCD_STAT_INFORMATION_1 {
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub ResetCounters: u32,
    pub TimeRead: i64,
    pub Counters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HCD_STAT_INFORMATION_2 {
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub ResetCounters: u32,
    pub TimeRead: i64,
    pub LockedMemoryUsed: i32,
    pub Counters: u32,
    pub IsoCounters: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NOTIFICATION {
    pub NotificationType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONNECTION_NOTIFICATION {
    pub NotificationType: u32,
    pub ConnectionNumber: u32,
    pub RequestedBandwidth: u32,
    pub EnumerationFailReason: u32,
    pub PowerRequested: u32,
    pub HubNameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BUS_NOTIFICATION {
    pub NotificationType: u32,
    pub TotalBandwidth: u32,
    pub ConsumedBandwidth: u32,
    pub ControllerNameLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_ACQUIRE_INFO {
    pub NotificationType: u32,
    pub TotalSize: u32,
    pub Buffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_STATE {
    pub DeviceConnected: u32,
    pub DeviceStarted: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_PORT_INFORMATION {
    pub DeviceState: u32,
    pub PortNumber: u16,
    pub DeviceAddress: u16,
    pub ConnectionIndex: u32,
    pub ConnectionStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_DEVICE_INFO {
    pub HubDescriptor: u32,
    pub HubNumber: u32,
    pub DeviceAddress: u16,
    pub HubIsSelfPowered: u8,
    pub HubIsRootHub: u8,
    pub HubCapabilities: u32,
    pub NumberOfHubPorts: u32,
    pub PortInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_COMPOSITE_FUNCTION_INFO {
    pub FunctionNumber: u8,
    pub BaseInterfaceNumber: u8,
    pub NumberOfInterfaces: u8,
    pub FunctionIsIdle: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_COMPOSITE_DEVICE_INFO {
    pub DeviceDescriptor: u32,
    pub CurrentConfigDescriptor: u32,
    pub CurrentConfigurationValue: u8,
    pub NumberOfFunctions: u8,
    pub FunctionInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONTROLLER_DEVICE_INFO {
    pub PciVendorId: u32,
    pub PciDeviceId: u32,
    pub PciRevision: u32,
    pub NumberOfRootPorts: u32,
    pub HcFeatureFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_INFO {
    pub DeviceState: u32,
    pub PortNumber: u16,
    pub DeviceDescriptor: u32,
    pub CurrentConfigurationValue: u8,
    pub Speed: u32,
    pub DeviceAddress: u16,
    pub ConnectionIndex: u32,
    pub ConnectionStatus: u32,
    pub PnpHardwareId: [u32; 128],
    pub PnpCompatibleId: [u32; 128],
    pub SerialNumberId: [u32; 128],
    pub PnpDeviceDescription: [u32; 128],
    pub NumberOfOpenPipes: u32,
    pub PipeList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_NODE_INFO {
    pub Sig: u32,
    pub LengthInBytes: u32,
    pub DeviceDescription: [u32; 40],
    pub NodeType: u32,
    pub BusAddress: u32,
    pub UsbDeviceInfo: u32,
    pub HubDeviceInfo: u32,
    pub CompositeDeviceInfo: u32,
    pub ControllerDeviceInfo: u32,
    pub DeviceInformation: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_PERFORMANCE_INFO {
    pub BulkBytes: u32,
    pub ControlDataBytes: u32,
    pub IsoBytes: u32,
    pub InterruptBytes: u32,
    pub BulkUrbCount: u32,
    pub ControlUrbCount: u32,
    pub IsoUrbCount: u32,
    pub InterruptUrbCount: u32,
    pub AllocedInterrupt: [u32; 6],
    pub AllocedIso: u32,
    pub Total32secBandwidth: u32,
    pub TotalTtBandwidth: u32,
    pub DeviceDescription: [u32; 60],
    pub DeviceSpeed: u32,
    pub TotalIsoLatency: u32,
    pub DroppedIsoPackets: u32,
    pub TransferErrors: u32,
    pub PciInterruptCount: u32,
    pub HcIdleState: u32,
    pub HcAsyncIdleState: u32,
    pub HcAsyncCacheFlushCount: u32,
    pub HcPeriodicIdleState: u32,
    pub HcPeriodicCacheFlushCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_INFORMATION_EX {
    pub HubType: u32,
    pub HighestPortNumber: u16,
    pub UsbHubDescriptor: u32,
    pub Usb30HubDescriptor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_PORT_CONNECTOR_PROPERTIES {
    pub ConnectionIndex: u32,
    pub ActualLength: u32,
    pub UsbPortProperties: u32,
    pub CompanionIndex: u16,
    pub CompanionPortNumber: u16,
    pub CompanionHubSymbolicLinkName: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_INFORMATION_EX_V2 {
    pub ConnectionIndex: u32,
    pub Length: u32,
    pub SupportedUsbProtocols: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_TRANSPORT_CHARACTERISTICS {
    pub Version: u32,
    pub TransportCharacteristicsFlags: u32,
    pub CurrentRoundtripLatencyInMilliSeconds: u32,
    pub MaxPotentialBandwidth: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_REGISTRATION {
    pub ChangeNotificationInputFlags: u32,
    pub Handle: u32,
    pub UsbTransportCharacteristics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_NOTIFICATION {
    pub Handle: u32,
    pub UsbTransportCharacteristics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_UNREGISTRATION {
    pub Handle: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CHARACTERISTICS {
    pub Version: u32,
    pub Reserved: [u32; 2],
    pub UsbDeviceCharacteristicsFlags: u32,
    pub MaximumSendPathDelayInMilliSeconds: u32,
    pub MaximumCompletionPathDelayInMilliSeconds: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_START_TRACKING_FOR_TIME_SYNC_INFORMATION {
    pub TimeTrackingHandle: usize,
    pub IsStartupDelayTolerable: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_STOP_TRACKING_FOR_TIME_SYNC_INFORMATION {
    pub TimeTrackingHandle: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_FRAME_NUMBER_AND_QPC_FOR_TIME_SYNC_INFORMATION {
    pub TimeTrackingHandle: usize,
    pub InputFrameNumber: u32,
    pub InputMicroFrameNumber: u32,
    pub QueryPerformanceCounterAtInputFrameOrMicroFrame: i64,
    pub QueryPerformanceCounterFrequency: i64,
    pub PredictedAccuracyInMicroSeconds: u32,
    pub CurrentGenerationID: u32,
    pub CurrentQueryPerformanceCounter: i64,
    pub CurrentHardwareFrameNumber: u32,
    pub CurrentHardwareMicroFrameNumber: u32,
    pub CurrentUSBFrameNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_NODE_CONNECTION_SUPERSPEEDPLUS_INFORMATION {
    pub ConnectionIndex: u32,
    pub Length: u32,
    pub RxSuperSpeedPlus: u32,
    pub RxLaneCount: u32,
    pub TxSuperSpeedPlus: u32,
    pub TxLaneCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DRV_VERSION {
    pub major: u32,
    pub minor: u32,
    pub internal: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_BLOCK {
    pub uOffset: u32,
    pub uLength: u32,
    pub pbyData: u32,
    pub uIndex: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct IO_BLOCK_EX {
    pub uOffset: u32,
    pub uLength: u32,
    pub pbyData: u32,
    pub uIndex: u32,
    pub bRequest: u8,
    pub bmRequestType: u8,
    pub fTransferDirectionIn: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CHANNEL_INFO {
    pub EventChannelSize: u32,
    pub uReadDataAlignment: u32,
    pub uWriteDataAlignment: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBSCAN_GET_DESCRIPTOR {
    pub DescriptorType: u8,
    pub Index: u8,
    pub LanguageId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DEVICE_DESCRIPTOR {
    pub usVendorId: u16,
    pub usProductId: u16,
    pub usBcdDevice: u16,
    pub usLanguageId: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBSCAN_PIPE_INFORMATION {
    pub MaximumPacketSize: u16,
    pub EndpointAddress: u8,
    pub Interval: u8,
    pub PipeType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBSCAN_PIPE_CONFIGURATION {
    pub NumberOfPipes: u32,
    pub PipeInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USBSCAN_TIMEOUT {
    pub TimeoutRead: u32,
    pub TimeoutWrite: u32,
    pub TimeoutEvent: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct s {
    pub Recipient: u8,
    pub Reserved: u8,
    pub Type: u8,
    pub Dir: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEFAULT_PIPE_SETUP_PACKET {
    pub bmRequestType: u32,
    pub bRequest: u8,
    pub LowByte: u8,
    pub HiByte: u8,
    pub W: u16,
    pub LowByte_1: u8,
    pub HiByte_1: u8,
    pub W_1: u16,
    pub wLength: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_COMMON_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bcdUSB: u16,
    pub bDeviceClass: u8,
    pub bDeviceSubClass: u8,
    pub bDeviceProtocol: u8,
    pub bMaxPacketSize0: u8,
    pub idVendor: u16,
    pub idProduct: u16,
    pub bcdDevice: u16,
    pub iManufacturer: u8,
    pub iProduct: u8,
    pub iSerialNumber: u8,
    pub bNumConfigurations: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_QUALIFIER_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bcdUSB: u16,
    pub bDeviceClass: u8,
    pub bDeviceSubClass: u8,
    pub bDeviceProtocol: u8,
    pub bMaxPacketSize0: u8,
    pub bNumConfigurations: u8,
    pub bReserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_BOS_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub wTotalLength: u16,
    pub bNumDeviceCaps: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub AsUlong: u32,
    pub Reserved: u32,
    pub LPMCapable: u32,
    pub BESLAndAlternateHIRDSupported: u32,
    pub BaselineBESLValid: u32,
    pub DeepBESLValid: u32,
    pub Reserved1: u32,
    pub BaselineBESL: u32,
    pub DeepBESL: u32,
    pub Reserved2: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bReserved: u8,
    pub AsUlong: u32,
    pub Reserved1: u32,
    pub BatteryCharging: u32,
    pub USBPowerDelivery: u32,
    pub Provider: u32,
    pub Consumer: u32,
    pub ChargingPolicy: u32,
    pub TypeCCurrent: u32,
    pub Reserved2: u32,
    pub ACSupply: u32,
    pub Battery: u32,
    pub Other: u32,
    pub NumBatteries: u32,
    pub UsesVbus: u32,
    pub Reserved3: u32,
    pub bmProviderPorts: u16,
    pub bmConsumerPorts: u16,
    pub bcdBCVersion: u16,
    pub bcdPDVersion: u16,
    pub bcdUSBTypeCVersion: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bReserved: u8,
    pub AsUshort: u16,
    pub BatteryCharging: u16,
    pub USBPowerDelivery: u16,
    pub USBTypeCCurrent: u16,
    pub Reserved: u16,
    pub wMinVoltage: u16,
    pub wMaxVoltage: u16,
    pub wReserved: u16,
    pub dwMaxOperatingPower: u32,
    pub dwMaxPeakPower: u32,
    pub dwMaxPeakPowerTime: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_SUPERSPEED_USB_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bmAttributes: u8,
    pub wSpeedsSupported: u16,
    pub bFunctionalitySupport: u8,
    pub bU1DevExitLat: u8,
    pub wU2DevExitLat: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bReserved: u8,
    pub AsUlong: u32,
    pub SublinkSpeedAttrCount: u32,
    pub SublinkSpeedIDCount: u32,
    pub Reserved: u32,
    pub AsUshort: u16,
    pub SublinkSpeedAttrID: u16,
    pub Reserved_1: u16,
    pub MinRxLaneCount: u16,
    pub MinTxLaneCount: u16,
    pub wReserved: u16,
    pub bmSublinkSpeedAttr: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_CONTAINER_ID_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bReserved: u8,
    pub ContainerID: [u8; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_PLATFORM_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bReserved: u8,
    pub PlatformCapabilityUuid: u32,
    pub CapabililityData: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub iAddtionalInfoURL: u8,
    pub bNumberOfAlternateModes: u8,
    pub bPreferredAlternateMode: u8,
    pub AsUshort: u16,
    pub VConnPowerNeededForFullFunctionality: u16,
    pub Reserved: u16,
    pub NoVconnPowerRequired: u16,
    pub bmConfigured: [u8; 32],
    pub bReserved: u32,
    pub wSVID: u16,
    pub bAlternateMode: u8,
    pub iAlternateModeSetting: u8,
    pub AlternateMode: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
    pub bcdDescriptorVersion: u8,
    pub AsUlong: u32,
    pub GetFirmwareImageHashSupport: u32,
    pub DisallowFirmwareUpdateSupport: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_DEVICE_CAPABILITY_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bDevCapabilityType: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONFIGURATION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub wTotalLength: u16,
    pub bNumInterfaces: u8,
    pub bConfigurationValue: u8,
    pub iConfiguration: u8,
    pub bmAttributes: u8,
    pub MaxPower: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_INTERFACE_ASSOCIATION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bFirstInterface: u8,
    pub bInterfaceCount: u8,
    pub bFunctionClass: u8,
    pub bFunctionSubClass: u8,
    pub bFunctionProtocol: u8,
    pub iFunction: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_INTERFACE_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bInterfaceNumber: u8,
    pub bAlternateSetting: u8,
    pub bNumEndpoints: u8,
    pub bInterfaceClass: u8,
    pub bInterfaceSubClass: u8,
    pub bInterfaceProtocol: u8,
    pub iInterface: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_ENDPOINT_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bEndpointAddress: u8,
    pub bmAttributes: u8,
    pub wMaxPacketSize: u16,
    pub bInterval: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct MP {
    pub MaxPacket: u16,
    pub HSmux: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_STRING_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bString: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bMaxBurst: u8,
    pub AsUchar: u8,
    pub MaxStreams: u8,
    pub Reserved1: u8,
    pub Mult: u8,
    pub Reserved2: u8,
    pub SspCompanion: u8,
    pub wBytesPerInterval: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_SUPERSPEEDPLUS_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub wReserved: u16,
    pub dwBytesPerInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct EUSB2_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub wMaxPacketSize: u16,
    pub dwBytesPerInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_HUB_DESCRIPTOR {
    pub bDescriptorLength: u8,
    pub bDescriptorType: u8,
    pub bNumberOfPorts: u8,
    pub wHubCharacteristics: u16,
    pub bPowerOnToPowerGood: u8,
    pub bHubControlCurrent: u8,
    pub bRemoveAndPowerMask: [u8; 64],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_30_HUB_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bNumberOfPorts: u8,
    pub wHubCharacteristics: u16,
    pub bPowerOnToPowerGood: u8,
    pub bHubControlCurrent: u8,
    pub bHubHdrDecLat: u8,
    pub wHubDelay: u16,
    pub DeviceRemovable: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_CONFIGURATION_POWER_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub SelfPowerConsumedD0: [u8; 3],
    pub bPowerSummaryId: u8,
    pub bBusPowerSavingD1: u8,
    pub bSelfPowerSavingD1: u8,
    pub bBusPowerSavingD2: u8,
    pub bSelfPowerSavingD2: u8,
    pub bBusPowerSavingD3: u8,
    pub bSelfPowerSavingD3: u8,
    pub TransitionTimeFromD1: u16,
    pub TransitionTimeFromD2: u16,
    pub TransitionTimeFromD3: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct USB_INTERFACE_POWER_DESCRIPTOR {
    pub bLength: u8,
    pub bDescriptorType: u8,
    pub bmCapabilitiesFlags: u8,
    pub bBusPowerSavingD1: u8,
    pub bSelfPowerSavingD1: u8,
    pub bBusPowerSavingD2: u8,
    pub bSelfPowerSavingD2: u8,
    pub bBusPowerSavingD3: u8,
    pub bSelfPowerSavingD3: u8,
    pub TransitionTimeFromD1: u16,
    pub TransitionTimeFromD2: u16,
    pub TransitionTimeFromD3: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VHF_CONFIG {
    pub Size: u32,
    pub VhfClientContext: usize,
    pub OperationContextSize: u32,
    pub DeviceObject: usize,
    pub FileHandle: usize,
    pub VendorID: u16,
    pub ProductID: u16,
    pub VersionNumber: u16,
    pub ContainerID: u32,
    pub InstanceIDLength: u16,
    pub InstanceID: usize,
    pub ReportDescriptorLength: u16,
    pub ReportDescriptor: *mut u8,
    pub EvtVhfReadyForNextReadReport: usize,
    pub EvtVhfAsyncOperationGetFeature: usize,
    pub EvtVhfAsyncOperationSetFeature: usize,
    pub EvtVhfAsyncOperationWriteReport: usize,
    pub EvtVhfAsyncOperationGetInputReport: usize,
    pub EvtVhfCleanup: usize,
    pub HardwareIDsLength: u16,
    pub HardwareIDs: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct VM_GENCOUNTER {
    pub GenerationCount: u64,
    pub GenerationCountHigh: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SENSOR_ATTRIBUTES {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub WinBioVersion: u32,
    pub SensorType: u32,
    pub SensorSubType: u32,
    pub Capabilities: u32,
    pub ManufacturerName: u32,
    pub ModelName: u32,
    pub SerialNumber: u32,
    pub FirmwareVersion: u32,
    pub SupportedFormatEntries: u32,
    pub SupportedFormat: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_DATA {
    pub Size: u32,
    pub Data: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_UPDATE_FIRMWARE {
    pub PayloadSize: u32,
    pub FirmwareData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_CALIBRATION_INFO {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub CalibrationData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_DIAGNOSTICS {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub SensorStatus: u32,
    pub VendorDiagnostics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BLANK_PAYLOAD {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_CAPTURE_PARAMETERS {
    pub PayloadSize: u32,
    pub Purpose: u32,
    pub Format: u32,
    pub VendorFormat: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_CAPTURE_DATA {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub SensorStatus: u32,
    pub RejectDetail: u32,
    pub CaptureData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SUPPORTED_ALGORITHMS {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub NumberOfAlgorithms: u32,
    pub AlgorithmData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_GET_INDICATOR {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub IndicatorStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SET_INDICATOR {
    pub PayloadSize: u32,
    pub IndicatorStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_PRIVATE_SENSOR_TYPE_INFO {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub PrivateSensorTypeInfo: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_ENCRYPTED_CAPTURE_PARAMS {
    pub PayloadSize: u32,
    pub Purpose: u32,
    pub Format: u32,
    pub VendorFormat: u32,
    pub Flags: u32,
    pub NonceSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_NOTIFY_WAKE {
    pub PayloadSize: u32,
    pub WinBioHresult: u32,
    pub Reason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_VERSION {
    pub MajorVersion: u32,
    pub MinorVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_IDENTITY {
    pub Type: u32,
    pub Null: u32,
    pub Wildcard: u32,
    pub TemplateGuid: u32,
    pub Size: u32,
    pub Data: [u8; 1],
    pub SecureId: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SECURE_CONNECTION_PARAMS {
    pub PayloadSize: u32,
    pub Version: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SECURE_CONNECTION_DATA {
    pub Size: u32,
    pub Version: u32,
    pub Flags: u32,
    pub ModelCertificateSize: u32,
    pub IntermediateCA1Size: u32,
    pub IntermediateCA2Size: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BIR_DATA {
    pub Size: u32,
    pub Offset: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BIR {
    pub HeaderBlock: u32,
    pub StandardDataBlock: u32,
    pub VendorDataBlock: u32,
    pub SignatureBlock: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_REGISTERED_FORMAT {
    pub Owner: u16,
    pub Type: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BIR_HEADER {
    pub ValidFields: u16,
    pub HeaderVersion: u32,
    pub PatronHeaderVersion: u32,
    pub DataFlags: u32,
    pub Type: u32,
    pub Subtype: u32,
    pub Purpose: u32,
    pub DataQuality: u32,
    pub CreationDate: i64,
    pub BeginDate: i64,
    pub EndDate: i64,
    pub BiometricDataFormat: u32,
    pub ProductId: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BDB_ANSI_381_HEADER {
    pub RecordLength: u32,
    pub FormatIdentifier: u32,
    pub VersionNumber: u32,
    pub ProductId: u32,
    pub CaptureDeviceId: u16,
    pub ImageAcquisitionLevel: u16,
    pub HorizontalScanResolution: u16,
    pub VerticalScanResolution: u16,
    pub HorizontalImageResolution: u16,
    pub VerticalImageResolution: u16,
    pub ElementCount: u8,
    pub ScaleUnits: u8,
    pub PixelDepth: u8,
    pub ImageCompressionAlg: u8,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BDB_ANSI_381_RECORD {
    pub BlockLength: u32,
    pub HorizontalLineLength: u16,
    pub VerticalLineLength: u16,
    pub Position: u32,
    pub CountOfViews: u8,
    pub ViewNumber: u8,
    pub ImageQuality: u8,
    pub ImpressionType: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_SECURE_BUFFER_HEADER_V1 {
    pub Type: u32,
    pub Size: u32,
    pub Flags: u32,
    pub ValidationTag: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EVENT {
    pub Type: u32,
    pub UnitId: u32,
    pub RejectDetail: u32,
    pub UnitId_1: u32,
    pub Identity: u32,
    pub SubFactor: u32,
    pub RejectDetail_1: u32,
    pub ErrorCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_PRESENCE {
    pub Factor: u32,
    pub SubFactor: u32,
    pub Status: u32,
    pub RejectDetail: u32,
    pub Identity: u32,
    pub TrackingId: u64,
    pub Ticket: u32,
    pub Properties: u32,
    pub Size: u32,
    pub Data: [u8; 32],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_BSP_SCHEMA {
    pub BiometricFactor: u32,
    pub BspId: u32,
    pub Description: u32,
    pub Vendor: u32,
    pub Version: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_UNIT_SCHEMA {
    pub UnitId: u32,
    pub PoolType: u32,
    pub BiometricFactor: u32,
    pub SensorSubType: u32,
    pub Capabilities: u32,
    pub DeviceInstanceId: u32,
    pub Description: u32,
    pub Manufacturer: u32,
    pub Model: u32,
    pub SerialNumber: u32,
    pub FirmwareVersion: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_STORAGE_SCHEMA {
    pub BiometricFactor: u32,
    pub DatabaseId: u32,
    pub DataFormat: u32,
    pub Attributes: u32,
    pub FilePath: u32,
    pub ConnectionString: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_SENSOR_INFO {
    pub GenericSensorCapabilities: u32,
    pub Factor: u32,
    pub Null: u32,
    pub FrameSize: u32,
    pub FrameOffset: usize,
    pub MandatoryOrientation: u32,
    pub ColorSensorId: [u32; 260],
    pub InfraredSensorId: [u32; 260],
    pub InfraredSensorRotationAngle: u32,
    pub Reserved: u32,
    pub FrameSize_1: u32,
    pub FrameOffset_1: usize,
    pub MandatoryOrientation_1: u32,
    pub Reserved_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_ENGINE_INFO {
    pub GenericEngineCapabilities: u32,
    pub Factor: u32,
    pub Null: u32,
    pub Capabilities: u32,
    pub Null_1: u32,
    pub Capabilities_1: u32,
    pub GeneralSamples: u32,
    pub Center: u32,
    pub TopEdge: u32,
    pub BottomEdge: u32,
    pub LeftEdge: u32,
    pub RightEdge: u32,
    pub Capabilities_2: u32,
    pub Null_2: u32,
    pub Capabilities_3: u32,
    pub Null_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_STORAGE_INFO {
    pub GenericStorageCapabilities: u32,
    pub Factor: u32,
    pub Null: u32,
    pub Capabilities: u32,
    pub Capabilities_1: u32,
    pub Capabilities_2: u32,
    pub Capabilities_3: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_ENROLLMENT_STATUS {
    pub TemplateStatus: u32,
    pub RejectDetail: u32,
    pub PercentComplete: u32,
    pub Factor: u32,
    pub SubFactor: u32,
    pub Null: u32,
    pub BoundingBox: u32,
    pub Distance: i32,
    pub AdapterId: u32,
    pub Data: [u32; 1],
    pub GeneralSamples: u32,
    pub Center: u32,
    pub TopEdge: u32,
    pub BottomEdge: u32,
    pub LeftEdge: u32,
    pub RightEdge: u32,
    pub EyeBoundingBox_1: u32,
    pub EyeBoundingBox_2: u32,
    pub PupilCenter_1: usize,
    pub PupilCenter_2: usize,
    pub Distance_1: i32,
    pub GridPointCompletionPercent: u32,
    pub GridPointIndex: u32,
    pub X: u32,
    pub Y: u32,
    pub Z: u32,
    pub StopCaptureAndShowCriticalFeedback: i32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_UNIT_STATUS {
    pub Availability: u32,
    pub ReasonCode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_FP_BU_STATE {
    pub SensorAttached: i32,
    pub CreationResult: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_ANTI_SPOOF_POLICY {
    pub Action: u32,
    pub Source: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_EXTENDED_ENROLLMENT_PARAMETERS {
    pub Size: usize,
    pub SubFactor: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_ACCOUNT_POLICY {
    pub Identity: u32,
    pub AntiSpoofBehavior: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_PROTECTION_POLICY {
    pub Version: u32,
    pub Identity: u32,
    pub DatabaseId: u32,
    pub UserState: u64,
    pub PolicySize: usize,
    pub Policy: [u8; 128],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_GESTURE_METADATA {
    pub Size: usize,
    pub BiometricType: u32,
    pub MatchType: u32,
    pub ProtectionType: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINBIO_CONNECTED_SENSOR {
    pub biometricType: u32,
    pub isEnhancedSignInSecurityCapable: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_HEADER_EXT {
    pub Reserved: u16,
    pub DnssecOk: u16,
    pub chRcode: u8,
    pub chVersion: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_MANDATORY {
    pub cMandatoryKeys: u16,
    pub rgwMandatoryKeys: [u16; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_ALPN_ID {
    pub cBytes: u8,
    pub field_pbId: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_ALPN {
    pub cIds: u16,
    pub rgIds: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_IPV4 {
    pub cIps: u16,
    pub rgIps: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_IPV6 {
    pub cIps: u16,
    pub rgIps: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM_UNKNOWN {
    pub cBytes: u16,
    pub pbSvcParamValue: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_PARAM {
    pub wSvcParamKey: u16,
    pub field_pIpv4Hints: u32,
    pub field_pIpv6Hints: u32,
    pub field_pMandatory: u32,
    pub field_pAlpn: u32,
    pub wPort: u16,
    pub field_pUnknown: u32,
    pub pszDohPath: usize,
    pub pReserved: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_SVCB_DATA {
    pub wSvcPriority: u16,
    pub pszTargetName: usize,
    pub cSvcParams: u16,
    pub field_pSvcParams: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_RECORD_FLAGS {
    pub Section: u32,
    pub Delete: u32,
    pub CharSet: u32,
    pub Unused: u32,
    pub Reserved: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_RECORDW {
    pub pNext: usize,
    pub pName: usize,
    pub wType: u16,
    pub wDataLength: u16,
    pub DW: u32,
    pub S: u32,
    pub dwTtl: u32,
    pub dwReserved: u32,
    pub A: u32,
    pub Soa: u32,
    pub Mr: u32,
    pub Rp: u32,
    pub Rt: u32,
    pub Null: u32,
    pub Wks: u32,
    pub AAAA: u32,
    pub Key: u32,
    pub Sig: u32,
    pub Atma: u32,
    pub Nxt: u32,
    pub Srv: u32,
    pub Naptr: u32,
    pub Opt: u32,
    pub Ds: u32,
    pub Rrsig: u32,
    pub Nsec: u32,
    pub Dnskey: u32,
    pub Tkey: u32,
    pub Tsig: u32,
    pub Wins: u32,
    pub Nbstat: u32,
    pub DHCID: u32,
    pub Nsec3: u32,
    pub Nsec3Param: u32,
    pub Tlsa: u32,
    pub Svcb: u32,
    pub Unknown: u32,
    pub pDataPtr: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_RECORD_OPTW {
    pub pNext: usize,
    pub pName: usize,
    pub wType: u16,
    pub wDataLength: u16,
    pub DW: u32,
    pub S: u32,
    pub ExtHeader: u32,
    pub wPayloadSize: u16,
    pub wReserved: u16,
    pub Opt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_RECORDA {
    pub pNext: usize,
    pub pName: usize,
    pub wType: u16,
    pub wDataLength: u16,
    pub DW: u32,
    pub S: u32,
    pub dwTtl: u32,
    pub dwReserved: u32,
    pub A: u32,
    pub Soa: u32,
    pub Mr: u32,
    pub Rp: u32,
    pub Rt: u32,
    pub Null: u32,
    pub Wks: u32,
    pub AAAA: u32,
    pub Key: u32,
    pub Sig: u32,
    pub Atma: u32,
    pub Nxt: u32,
    pub Srv: u32,
    pub Naptr: u32,
    pub Opt: u32,
    pub Ds: u32,
    pub Rrsig: u32,
    pub Nsec: u32,
    pub Dnskey: u32,
    pub Tkey: u32,
    pub Tsig: u32,
    pub Wins: u32,
    pub Nbstat: u32,
    pub DHCID: u32,
    pub Nsec3: u32,
    pub Nsec3Param: u32,
    pub Tlsa: u32,
    pub Svcb: u32,
    pub Unknown: u32,
    pub pDataPtr: *mut u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_RECORD_OPTA {
    pub pNext: usize,
    pub pName: usize,
    pub wType: u16,
    pub wDataLength: u16,
    pub DW: u32,
    pub S: u32,
    pub ExtHeader: u32,
    pub wPayloadSize: u16,
    pub wReserved: u16,
    pub Opt: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_ADDR {
    pub MaxSa: [i8; 1],
    pub DnsAddrUserDword: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_ADDR_ARRAY {
    pub MaxCount: u32,
    pub AddrCount: u32,
    pub Tag: u32,
    pub Family: u16,
    pub WordReserved: u16,
    pub Flags: u32,
    pub MatchFlag: u32,
    pub Reserved1: u32,
    pub Reserved2: u32,
    pub AddrArray: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_HEADER {
    pub Xid: u16,
    pub Flags: u16,
    pub RecursionDesired: u8,
    pub Truncation: u8,
    pub Authoritative: u8,
    pub Opcode: u8,
    pub IsResponse: u8,
    pub ResponseCode: u8,
    pub CheckingDisabled: u8,
    pub AuthenticatedData: u8,
    pub Reserved: u8,
    pub RecursionAvailable: u8,
    pub QuestionCount: u16,
    pub AnswerCount: u16,
    pub NameServerCount: u16,
    pub AdditionalCount: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_MESSAGE_BUFFER {
    pub MessageHead: u32,
    pub MessageBody: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DNS_CUSTOM_SERVER {
    pub dwServerType: u32,
    pub ullFlags: u32,
    pub pwszTemplate: usize,
    pub pwszHostname: usize,
    pub ServerAddr: u32,
    pub MaxSa: [i8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MAC_ADDRESS {
    pub ucDot11MacAddress: [u8; 6],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RATE_SET {
    pub uRateSetLength: u32,
    pub ucRateSet: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_SESSION_INFO {
    pub uSessionInfoLength: u32,
    pub ucSessionInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OFFLOAD_CAPABILITY {
    pub uReserved: u32,
    pub uFlags: u32,
    pub uSupportedWEPAlgorithms: u32,
    pub uNumOfReplayWindows: u32,
    pub uMaxWEPKeyMappingLength: u32,
    pub uSupportedAuthAlgorithms: u32,
    pub uMaxAuthKeyMappingLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_CURRENT_OFFLOAD_CAPABILITY {
    pub uReserved: u32,
    pub uFlags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_IV48_COUNTER {
    pub uIV32Counter: u32,
    pub usIV16Counter: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WEP_OFFLOAD {
    pub uReserved: u32,
    pub hOffloadContext: usize,
    pub hOffload: usize,
    pub dot11OffloadType: u32,
    pub dwAlgorithm: u32,
    pub bRowIsOutbound: u8,
    pub bUseDefault: u8,
    pub uFlags: u32,
    pub ucMacAddress: [u8; 6],
    pub uNumOfRWsOnPeer: u32,
    pub uNumOfRWsOnMe: u32,
    pub dot11IV48Counters: [u32; 16],
    pub usDot11RWBitMaps: [u16; 16],
    pub usKeyLength: u16,
    pub ucKey: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WEP_UPLOAD {
    pub uReserved: u32,
    pub dot11OffloadType: u32,
    pub hOffload: usize,
    pub uNumOfRWsUsed: u32,
    pub dot11IV48Counters: [u32; 16],
    pub usDot11RWBitMaps: [u16; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DEFAULT_WEP_OFFLOAD {
    pub uReserved: u32,
    pub hOffloadContext: usize,
    pub hOffload: usize,
    pub dwIndex: u32,
    pub dot11OffloadType: u32,
    pub dwAlgorithm: u32,
    pub uFlags: u32,
    pub dot11KeyDirection: u32,
    pub ucMacAddress: [u8; 6],
    pub uNumOfRWsOnMe: u32,
    pub dot11IV48Counters: [u32; 16],
    pub usDot11RWBitMaps: [u16; 16],
    pub usKeyLength: u16,
    pub ucKey: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DEFAULT_WEP_UPLOAD {
    pub uReserved: u32,
    pub dot11OffloadType: u32,
    pub hOffload: usize,
    pub uNumOfRWsUsed: u32,
    pub dot11IV48Counters: [u32; 16],
    pub usDot11RWBitMaps: [u16; 16],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OPERATION_MODE_CAPABILITY {
    pub uReserved: u32,
    pub uMajorVersion: u32,
    pub uMinorVersion: u32,
    pub uNumOfTXBuffers: u32,
    pub uNumOfRXBuffers: u32,
    pub uOpModeCapability: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_CURRENT_OPERATION_MODE {
    pub uReserved: u32,
    pub uCurrentOpMode: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SCAN_REQUEST {
    pub dot11BSSType: u32,
    pub dot11BSSID: u32,
    pub dot11SSID: u32,
    pub dot11ScanType: u32,
    pub bRestrictedScan: u8,
    pub bUseRequestIE: u8,
    pub uRequestIDsOffset: u32,
    pub uNumOfRequestIDs: u32,
    pub uPhyTypesOffset: u32,
    pub uNumOfPhyTypes: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PHY_TYPE_INFO {
    pub dot11PhyType: u32,
    pub bUseParameters: u8,
    pub uProbeDelay: u32,
    pub uMinChannelTime: u32,
    pub uMaxChannelTime: u32,
    pub ChDescriptionType: u32,
    pub uChannelListSize: u32,
    pub ucChannelListBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SCAN_REQUEST_V2 {
    pub dot11BSSType: u32,
    pub dot11BSSID: u32,
    pub dot11ScanType: u32,
    pub bRestrictedScan: u8,
    pub udot11SSIDsOffset: u32,
    pub uNumOfdot11SSIDs: u32,
    pub bUseRequestIE: u8,
    pub uRequestIDsOffset: u32,
    pub uNumOfRequestIDs: u32,
    pub uPhyTypeInfosOffset: u32,
    pub uNumOfPhyTypeInfos: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_BSS_DESCRIPTION {
    pub uReserved: u32,
    pub dot11BSSID: u32,
    pub dot11BSSType: u32,
    pub usBeaconPeriod: u16,
    pub ullTimestamp: u64,
    pub usCapabilityInformation: u16,
    pub uBufferLength: u32,
    pub ucBuffer: [u32; 1],
    pub ucBuffer_1: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_JOIN_REQUEST {
    pub uJoinFailureTimeout: u32,
    pub OperationalRateSet: u32,
    pub uChCenterFrequency: u32,
    pub dot11BSSDescription: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_START_REQUEST {
    pub uStartFailureTimeout: u32,
    pub OperationalRateSet: u32,
    pub uChCenterFrequency: u32,
    pub dot11BSSDescription: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_UPDATE_IE {
    pub dot11UpdateIEOp: u32,
    pub uBufferLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RESET_REQUEST {
    pub dot11ResetType: u32,
    pub dot11MacAddress: u32,
    pub bSetDefaultMIB: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OPTIONAL_CAPABILITY {
    pub uReserved: u32,
    pub bDot11PCF: u8,
    pub bDot11PCFMPDUTransferToPC: u8,
    pub bStrictlyOrderedServiceClass: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_CURRENT_OPTIONAL_CAPABILITY {
    pub uReserved: u32,
    pub bDot11CFPollable: u8,
    pub bDot11PCF: u8,
    pub bDot11PCFMPDUTransferToPC: u8,
    pub bStrictlyOrderedServiceClass: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_POWER_MGMT_MODE {
    pub dot11PowerMode: u32,
    pub uPowerSaveLevel: u32,
    pub usListenInterval: u16,
    pub usAID: u16,
    pub bReceiveDTIMs: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_COUNTERS_ENTRY {
    pub uTransmittedFragmentCount: u32,
    pub uMulticastTransmittedFrameCount: u32,
    pub uFailedCount: u32,
    pub uRetryCount: u32,
    pub uMultipleRetryCount: u32,
    pub uFrameDuplicateCount: u32,
    pub uRTSSuccessCount: u32,
    pub uRTSFailureCount: u32,
    pub uACKFailureCount: u32,
    pub uReceivedFragmentCount: u32,
    pub uMulticastReceivedFrameCount: u32,
    pub uFCSErrorCount: u32,
    pub uTransmittedFrameCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_PHY_TYPES {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11PHYType: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_POWER_LEVELS {
    pub uNumOfSupportedPowerLevels: u32,
    pub uTxPowerLevelValues: [u32; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_REG_DOMAIN_VALUE {
    pub uRegDomainsSupportIndex: u32,
    pub uRegDomainsSupportValue: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_REG_DOMAINS_SUPPORT_VALUE {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11RegDomainValue: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_ANTENNA {
    pub uAntennaListIndex: u32,
    pub bSupportedAntenna: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_ANTENNA_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11SupportedAntenna: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DIVERSITY_SELECTION_RX {
    pub uAntennaListIndex: u32,
    pub bDiversitySelectionRX: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DIVERSITY_SELECTION_RX_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11DiversitySelectionRx: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_DATA_RATES_VALUE {
    pub ucSupportedTxDataRatesValue: [u8; 1],
    pub ucSupportedRxDataRatesValue: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_DATA_RATES_VALUE_V2 {
    pub ucSupportedTxDataRatesValue: [u8; 1],
    pub ucSupportedRxDataRatesValue: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MULTI_DOMAIN_CAPABILITY_ENTRY {
    pub uMultiDomainCapabilityIndex: u32,
    pub uFirstChannelNumber: u32,
    pub uNumberOfChannels: u32,
    pub lMaximumTransmitPowerLevel: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MD_CAPABILITY_ENTRY_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11MDCapabilityEntry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_HOPPING_PATTERN_ENTRY {
    pub uHoppingPatternIndex: u32,
    pub uRandomTableFieldNumber: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_HOPPING_PATTERN_ENTRY_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11HoppingPatternEntry: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WPA_TSC {
    pub uReserved: u32,
    pub dot11OffloadType: u32,
    pub hOffload: usize,
    pub dot11IV48Counter: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RSSI_RANGE {
    pub dot11PhyType: u32,
    pub uRSSIMin: u32,
    pub uRSSIMax: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_NIC_SPECIFIC_EXTENSION {
    pub uBufferLength: u32,
    pub uTotalBufferLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_AP_JOIN_REQUEST {
    pub uJoinFailureTimeout: u32,
    pub OperationalRateSet: u32,
    pub uChCenterFrequency: u32,
    pub dot11BSSDescription: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECV_SENSITIVITY {
    pub ucDataRate: u8,
    pub lRSSIMin: i32,
    pub lRSSIMax: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECV_SENSITIVITY_LIST {
    pub dot11PhyType: u32,
    pub uPhyId: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11RecvSensitivity: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WME_AC_PARAMETERS {
    pub ucAccessCategoryIndex: u8,
    pub ucAIFSN: u8,
    pub ucECWmin: u8,
    pub ucECWmax: u8,
    pub usTXOPLimit: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WME_AC_PARAMETERS_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11WMEACParameters: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WME_UPDATE_IE {
    pub uParamElemMinBeaconIntervals: u32,
    pub uWMEInfoElemOffset: u32,
    pub uWMEInfoElemLength: u32,
    pub uWMEParamElemOffset: u32,
    pub uWMEParamElemLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_QOS_TX_DURATION {
    pub uNominalMSDUSize: u32,
    pub uMinPHYRate: u32,
    pub uDuration: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_QOS_TX_MEDIUM_TIME {
    pub dot11PeerAddress: u32,
    pub ucQoSPriority: u8,
    pub uMediumTimeAdmited: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_OFDM_FREQUENCY {
    pub uCenterFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_OFDM_FREQUENCY_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11SupportedOFDMFrequency: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_DSSS_CHANNEL {
    pub uChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SUPPORTED_DSSS_CHANNEL_LIST {
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub dot11SupportedDSSSChannel: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_ASSOCIATION_INFO_EX {
    pub PeerMacAddress: u32,
    pub BSSID: u32,
    pub usCapabilityInformation: u16,
    pub usListenInterval: u16,
    pub ucPeerSupportedRates: [u8; 1],
    pub usAssociationID: u16,
    pub dot11AssociationState: u32,
    pub dot11PowerMode: u32,
    pub liAssociationUpTime: i64,
    pub ullNumOfTxPacketSuccesses: u64,
    pub ullNumOfTxPacketFailures: u64,
    pub ullNumOfRxPacketSuccesses: u64,
    pub ullNumOfRxPacketFailures: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DATA_RATE_MAPPING_TABLE {
    pub Header: u32,
    pub uDataRateMappingLength: u32,
    pub DataRateMappingEntries: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_FRAGMENT_DESCRIPTOR {
    pub uOffset: u32,
    pub uLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PER_MSDU_COUNTERS {
    pub uTransmittedFragmentCount: u32,
    pub uRetryCount: u32,
    pub uRTSSuccessCount: u32,
    pub uRTSFailureCount: u32,
    pub uACKFailureCount: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_CONTEXT {
    pub Header: u32,
    pub pvReserved: usize,
    pub uFlags: u32,
    pub uPSLifetime: u32,
    pub uDelayedSleepValue: u32,
    pub ucTXDataRates: [u8; 8],
    pub bIndicateAssociatedACKs: u8,
    pub bIndicateTXStatus: u8,
    pub ucPriority: u8,
    pub bDontFragment: u8,
    pub dwExtendedStatus: u32,
    pub hIntegrityOffload: usize,
    pub hWEPOffload: usize,
    pub ucWPAMSDUPriority: u8,
    pub ucNumOfRWsOnPeer: u8,
    pub usAID: u16,
    pub pDot11PerMSDUCounters: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_EXTENSION_INFO {
    pub uVersion: u32,
    pub Header: u32,
    pub pvReserved: usize,
    pub uFlags: u32,
    pub uPSLifetime: u32,
    pub uDelayedSleepValue: u32,
    pub ucTXDataRates: [u8; 8],
    pub bIndicateAssociatedACKs: u8,
    pub bIndicateTXStatus: u8,
    pub ucPriority: u8,
    pub bDontFragment: u8,
    pub dwExtendedStatus: u32,
    pub hIntegrityOffload: usize,
    pub hWEPOffload: usize,
    pub ucWPAMSDUPriority: u8,
    pub ucNumOfRWsOnPeer: u8,
    pub usAID: u16,
    pub pDot11PerMSDUCounters: usize,
    pub usNumberOfFragments: u16,
    pub Dot11FragmentDescriptors: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_EXTENSION_INFO_V2 {
    pub ucQoSPriority: u8,
    pub ucAckPolicy: u8,
    pub usExtendedTXDataRatesOffset: u16,
    pub ucNumOfExtendedTXDataRates: u8,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECV_CONTEXT {
    pub Header: u32,
    pub pvReserved: usize,
    pub dot11PhyType: u32,
    pub uChCenterFrequency: u32,
    pub lRSSI: i32,
    pub uRSSI: u32,
    pub ucPriority: u8,
    pub ucDataRate: u8,
    pub ucPeerMacAddress: [u8; 6],
    pub dwExtendedStatus: u32,
    pub hWEPOffloadContext: usize,
    pub hAuthOffloadContext: usize,
    pub usWEPAppliedMask: u16,
    pub usWPAMSDUPriority: u16,
    pub dot11LowestIV48Counter: u32,
    pub usDot11LeftRWBitMap: u16,
    pub dot11HighestIV48Counter: u32,
    pub usDot11RightRWBitMap: u16,
    pub usNumberOfMPDUsReceived: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECV_EXTENSION_INFO {
    pub uVersion: u32,
    pub pvReserved: usize,
    pub dot11PhyType: u32,
    pub uChCenterFrequency: u32,
    pub lRSSI: i32,
    pub lRSSIMin: i32,
    pub lRSSIMax: i32,
    pub uRSSI: u32,
    pub ucPriority: u8,
    pub ucDataRate: u8,
    pub ucPeerMacAddress: [u8; 6],
    pub dwExtendedStatus: u32,
    pub hWEPOffloadContext: usize,
    pub hAuthOffloadContext: usize,
    pub usWEPAppliedMask: u16,
    pub usWPAMSDUPriority: u16,
    pub dot11LowestIV48Counter: u32,
    pub usDot11LeftRWBitMap: u16,
    pub dot11HighestIV48Counter: u32,
    pub usDot11RightRWBitMap: u16,
    pub usNumberOfMPDUsReceived: u16,
    pub usNumberOfFragments: u16,
    pub pNdisPackets: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECV_EXTENSION_INFO_V2 {
    pub uVersion: u32,
    pub pvReserved: usize,
    pub dot11PhyType: u32,
    pub uChCenterFrequency: u32,
    pub lRSSI: i32,
    pub uRSSI: u32,
    pub ucPriority: u8,
    pub ucDataRate: u8,
    pub ucPeerMacAddress: [u8; 6],
    pub dwExtendedStatus: u32,
    pub hWEPOffloadContext: usize,
    pub hAuthOffloadContext: usize,
    pub usWEPAppliedMask: u16,
    pub usWPAMSDUPriority: u16,
    pub dot11LowestIV48Counter: u32,
    pub usDot11LeftRWBitMap: u16,
    pub dot11HighestIV48Counter: u32,
    pub usDot11RightRWBitMap: u16,
    pub usNumberOfMPDUsReceived: u16,
    pub usNumberOfFragments: u16,
    pub pNdisPackets: [usize; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_STATUS_INDICATION {
    pub uStatusType: u32,
    pub ndisStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_EXTAP_ATTRIBUTES {
    pub Header: u32,
    pub uScanSSIDListSize: u32,
    pub uDesiredSSIDListSize: u32,
    pub uPrivacyExemptionListSize: u32,
    pub uAssociationTableSize: u32,
    pub uDefaultKeyTableSize: u32,
    pub uWEPKeyValueMaxLength: u32,
    pub bStrictlyOrderedServiceClassImplemented: u8,
    pub uNumSupportedCountryOrRegionStrings: u32,
    pub pSupportedCountryOrRegionStrings: usize,
    pub uInfraNumSupportedUcastAlgoPairs: u32,
    pub pInfraSupportedUcastAlgoPairs: usize,
    pub uInfraNumSupportedMcastAlgoPairs: u32,
    pub pInfraSupportedMcastAlgoPairs: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INCOMING_ASSOC_STARTED_PARAMETERS {
    pub Header: u32,
    pub PeerMacAddr: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INCOMING_ASSOC_REQUEST_RECEIVED_PARAMETERS {
    pub Header: u32,
    pub PeerMacAddr: u32,
    pub bReAssocReq: u8,
    pub uAssocReqOffset: u32,
    pub uAssocReqSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INCOMING_ASSOC_COMPLETION_PARAMETERS {
    pub Header: u32,
    pub PeerMacAddr: u32,
    pub uStatus: u32,
    pub ucErrorSource: u8,
    pub bReAssocReq: u8,
    pub bReAssocResp: u8,
    pub uAssocReqOffset: u32,
    pub uAssocReqSize: u32,
    pub uAssocRespOffset: u32,
    pub uAssocRespSize: u32,
    pub AuthAlgo: u32,
    pub UnicastCipher: u32,
    pub MulticastCipher: u32,
    pub uActivePhyListOffset: u32,
    pub uActivePhyListSize: u32,
    pub uBeaconOffset: u32,
    pub uBeaconSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_STOP_AP_PARAMETERS {
    pub Header: u32,
    pub ulReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PHY_FREQUENCY_ADOPTED_PARAMETERS {
    pub Header: u32,
    pub ulPhyId: u32,
    pub ulChannel: u32,
    pub ulFrequency: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_CAN_SUSTAIN_AP_PARAMETERS {
    pub Header: u32,
    pub ulReason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_AVAILABLE_CHANNEL_LIST {
    pub Header: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub uChannelNumber: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_AVAILABLE_FREQUENCY_LIST {
    pub Header: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub uFrequencyValue: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_DISASSOCIATE_PEER_REQUEST {
    pub Header: u32,
    pub PeerMacAddr: u32,
    pub usReason: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INCOMING_ASSOC_DECISION {
    pub Header: u32,
    pub PeerMacAddr: u32,
    pub bAccept: u8,
    pub usReasonCode: u16,
    pub uAssocResponseIEsOffset: u32,
    pub uAssocResponseIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INCOMING_ASSOC_DECISION_V2 {
    pub Header: u32,
    pub PeerMacAddr: u32,
    pub bAccept: u8,
    pub usReasonCode: u16,
    pub uAssocResponseIEsOffset: u32,
    pub uAssocResponseIEsLength: u32,
    pub WFDStatus: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_ADDITIONAL_IE {
    pub Header: u32,
    pub uBeaconIEsOffset: u32,
    pub uBeaconIEsLength: u32,
    pub uResponseIEsOffset: u32,
    pub uResponseIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PEER_STATISTICS {
    pub ullDecryptSuccessCount: u64,
    pub ullDecryptFailureCount: u64,
    pub ullTxPacketSuccessCount: u64,
    pub ullTxPacketFailureCount: u64,
    pub ullRxPacketSuccessCount: u64,
    pub ullRxPacketFailureCount: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PEER_INFO {
    pub MacAddress: u32,
    pub usCapabilityInformation: u16,
    pub AuthAlgo: u32,
    pub UnicastCipherAlgo: u32,
    pub MulticastCipherAlgo: u32,
    pub bWpsEnabled: u8,
    pub usListenInterval: u16,
    pub ucSupportedRates: [u8; 1],
    pub usAssociationID: u16,
    pub AssociationState: u32,
    pub PowerMode: u32,
    pub liAssociationUpTime: i64,
    pub Statistics: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PEER_INFO_LIST {
    pub Header: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub PeerInfo: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_VWIFI_COMBINATION {
    pub Header: u32,
    pub uNumInfrastructure: u32,
    pub uNumAdhoc: u32,
    pub uNumSoftAP: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_VWIFI_COMBINATION_V2 {
    pub Header: u32,
    pub uNumInfrastructure: u32,
    pub uNumAdhoc: u32,
    pub uNumSoftAP: u32,
    pub uNumVirtualStation: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_VWIFI_COMBINATION_V3 {
    pub Header: u32,
    pub uNumInfrastructure: u32,
    pub uNumAdhoc: u32,
    pub uNumSoftAP: u32,
    pub uNumVirtualStation: u32,
    pub uNumWFDGroup: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MAC_PARAMETERS {
    pub Header: u32,
    pub uOpmodeMask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_ATTRIBUTES {
    pub Header: u32,
    pub uNumConcurrentGORole: u32,
    pub uNumConcurrentClientRole: u32,
    pub WPSVersionsSupported: u32,
    pub bServiceDiscoverySupported: u8,
    pub bClientDiscoverabilitySupported: u8,
    pub bInfrastructureManagementSupported: u8,
    pub uMaxSecondaryDeviceTypeListSize: u32,
    pub DeviceAddress: u32,
    pub uInterfaceAddressListCount: u32,
    pub pInterfaceAddressList: usize,
    pub uNumSupportedCountryOrRegionStrings: u32,
    pub pSupportedCountryOrRegionStrings: usize,
    pub uDiscoveryFilterListSize: u32,
    pub uGORoleClientTableSize: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DEVICE_TYPE {
    pub CategoryID: u16,
    pub SubCategoryID: u16,
    pub OUI: [u8; 4],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WPS_DEVICE_NAME {
    pub uDeviceNameLength: u32,
    pub ucDeviceName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_CONFIGURATION_TIMEOUT {
    pub GOTimeout: u8,
    pub ClientTimeout: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GROUP_ID {
    pub DeviceAddress: u32,
    pub SSID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GO_INTENT {
    pub TieBreaker: u8,
    pub Intent: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_CHANNEL {
    pub CountryRegionString: u32,
    pub OperatingClass: u8,
    pub ChannelNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WFDSVC_CONNECTION_CAPABILITY {
    pub bNew: u8,
    pub bClient: u8,
    pub bGO: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_SERVICE_HASH_LIST {
    pub ServiceHashCount: u16,
    pub ServiceHash: [u32; 1],
    pub ServiceHash_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_ADVERTISEMENT_ID {
    pub AdvertisementID: u32,
    pub ServiceAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_SESSION_ID {
    pub SessionID: u32,
    pub SessionAddress: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_ADVERTISED_SERVICE_DESCRIPTOR {
    pub AdvertisementID: u32,
    pub ConfigMethods: u16,
    pub ServiceNameLength: u8,
    pub ServiceName: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_ADVERTISED_SERVICE_LIST {
    pub ServiceCount: u16,
    pub AdvertisedService: [u32; 1],
    pub AdvertisedService_1: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DISCOVER_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub Status: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub uListOffset: u32,
    pub uListLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_GO_NEGOTIATION_REQUEST_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_GO_NEGOTIATION_REQUEST_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_GO_NEGOTIATION_RESPONSE_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_GO_NEGOTIATION_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub ResponseContext: usize,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_GO_NEGOTIATION_CONFIRMATION_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_GO_NEGOTIATION_CONFIRMATION_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INVITATION_REQUEST_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub ReceiverAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_INVITATION_REQUEST_PARAMETERS {
    pub Header: u32,
    pub TransmitterDeviceAddress: u32,
    pub BSSID: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_INVITATION_RESPONSE_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub ReceiverDeviceAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_INVITATION_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub TransmitterDeviceAddress: u32,
    pub BSSID: u32,
    pub DialogToken: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PROVISION_DISCOVERY_REQUEST_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub ReceiverAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_PROVISION_DISCOVERY_REQUEST_PARAMETERS {
    pub Header: u32,
    pub TransmitterDeviceAddress: u32,
    pub BSSID: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_PROVISION_DISCOVERY_RESPONSE_SEND_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub ReceiverDeviceAddress: u32,
    pub DialogToken: u32,
    pub Status: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_RECEIVED_PROVISION_DISCOVERY_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub TransmitterDeviceAddress: u32,
    pub BSSID: u32,
    pub DialogToken: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_ANQP_QUERY_COMPLETE_PARAMETERS {
    pub Header: u32,
    pub Status: u32,
    pub hContext: usize,
    pub uResponseLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DEVICE_CAPABILITY_CONFIG {
    pub Header: u32,
    pub bServiceDiscoveryEnabled: u8,
    pub bClientDiscoverabilityEnabled: u8,
    pub bConcurrentOperationSupported: u8,
    pub bInfrastructureManagementEnabled: u8,
    pub bDeviceLimitReached: u8,
    pub bInvitationProcedureEnabled: u8,
    pub WPSVersionsEnabled: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GROUP_OWNER_CAPABILITY_CONFIG {
    pub Header: u32,
    pub bPersistentGroupEnabled: u8,
    pub bIntraBSSDistributionSupported: u8,
    pub bCrossConnectionSupported: u8,
    pub bPersistentReconnectSupported: u8,
    pub bGroupFormationEnabled: u8,
    pub uMaximumGroupLimit: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GROUP_OWNER_CAPABILITY_CONFIG_V2 {
    pub Header: u32,
    pub bPersistentGroupEnabled: u8,
    pub bIntraBSSDistributionSupported: u8,
    pub bCrossConnectionSupported: u8,
    pub bPersistentReconnectSupported: u8,
    pub bGroupFormationEnabled: u8,
    pub uMaximumGroupLimit: u32,
    pub bEapolKeyIpAddressAllocationSupported: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DEVICE_INFO {
    pub Header: u32,
    pub DeviceAddress: u32,
    pub ConfigMethods: u16,
    pub PrimaryDeviceType: u32,
    pub DeviceName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_SECONDARY_DEVICE_TYPE_LIST {
    pub Header: u32,
    pub uNumOfEntries: u32,
    pub uTotalNumOfEntries: u32,
    pub SecondaryDeviceTypes: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DISCOVER_DEVICE_FILTER {
    pub DeviceID: u32,
    pub ucBitmask: u8,
    pub GroupSSID: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DISCOVER_REQUEST {
    pub Header: u32,
    pub DiscoverType: u32,
    pub ScanType: u32,
    pub uDiscoverTimeout: u32,
    pub uDeviceFilterListOffset: u32,
    pub uNumDeviceFilters: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
    pub bForceScanLegacyNetworks: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DEVICE_ENTRY {
    pub uPhyId: u32,
    pub PhySpecificInfo: u32,
    pub dot11BSSID: u32,
    pub dot11BSSType: u32,
    pub TransmitterAddress: u32,
    pub lRSSI: i32,
    pub uLinkQuality: u32,
    pub usBeaconPeriod: u16,
    pub ullTimestamp: u64,
    pub ullBeaconHostTimestamp: u64,
    pub ullProbeResponseHostTimestamp: u64,
    pub usCapabilityInformation: u16,
    pub uBeaconIEsOffset: u32,
    pub uBeaconIEsLength: u32,
    pub uProbeResponseIEsOffset: u32,
    pub uProbeResponseIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_ADDITIONAL_IE {
    pub Header: u32,
    pub uBeaconIEsOffset: u32,
    pub uBeaconIEsLength: u32,
    pub uProbeResponseIEsOffset: u32,
    pub uProbeResponseIEsLength: u32,
    pub uDefaultRequestIEsOffset: u32,
    pub uDefaultRequestIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_GO_NEGOTIATION_REQUEST_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub uSendTimeout: u32,
    pub GroupOwnerIntent: u32,
    pub MinimumConfigTimeout: u32,
    pub IntendedInterfaceAddress: u32,
    pub GroupCapability: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_GO_NEGOTIATION_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uSendTimeout: u32,
    pub Status: u32,
    pub GroupOwnerIntent: u32,
    pub MinimumConfigTimeout: u32,
    pub IntendedInterfaceAddress: u32,
    pub GroupCapability: u32,
    pub GroupID: u32,
    pub bUseGroupID: u8,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_GO_NEGOTIATION_CONFIRMATION_PARAMETERS {
    pub Header: u32,
    pub PeerDeviceAddress: u32,
    pub DialogToken: u32,
    pub ResponseContext: usize,
    pub uSendTimeout: u32,
    pub Status: u32,
    pub GroupCapability: u32,
    pub GroupID: u32,
    pub bUseGroupID: u8,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_INVITATION_FLAGS {
    pub InvitationType: u8,
    pub Reserved: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_INVITATION_REQUEST_PARAMETERS {
    pub Header: u32,
    pub DialogToken: u32,
    pub PeerDeviceAddress: u32,
    pub uSendTimeout: u32,
    pub MinimumConfigTimeout: u32,
    pub InvitationFlags: u32,
    pub GroupBSSID: u32,
    pub bUseGroupBSSID: u8,
    pub OperatingChannel: u32,
    pub bUseSpecifiedOperatingChannel: u8,
    pub GroupID: u32,
    pub bLocalGO: u8,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_INVITATION_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub ReceiverDeviceAddress: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uSendTimeout: u32,
    pub Status: u32,
    pub MinimumConfigTimeout: u32,
    pub GroupBSSID: u32,
    pub bUseGroupBSSID: u8,
    pub OperatingChannel: u32,
    pub bUseSpecifiedOperatingChannel: u8,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_PROVISION_DISCOVERY_REQUEST_PARAMETERS {
    pub Header: u32,
    pub DialogToken: u32,
    pub PeerDeviceAddress: u32,
    pub uSendTimeout: u32,
    pub GroupCapability: u32,
    pub GroupID: u32,
    pub bUseGroupID: u8,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SEND_PROVISION_DISCOVERY_RESPONSE_PARAMETERS {
    pub Header: u32,
    pub ReceiverDeviceAddress: u32,
    pub DialogToken: u32,
    pub RequestContext: usize,
    pub uSendTimeout: u32,
    pub uIEsOffset: u32,
    pub uIEsLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_DEVICE_LISTEN_CHANNEL {
    pub Header: u32,
    pub ChannelNumber: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GROUP_START_PARAMETERS {
    pub Header: u32,
    pub AdvertisedOperatingChannel: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_WFD_GROUP_JOIN_PARAMETERS {
    pub Header: u32,
    pub GOOperatingChannel: u32,
    pub GOConfigTime: u32,
    pub bInGroupFormation: u8,
    pub bWaitForWPSReady: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_POWER_MGMT_AUTO_MODE_ENABLED_INFO {
    pub Header: u32,
    pub bEnabled: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_POWER_MGMT_MODE_STATUS_INFO {
    pub Header: u32,
    pub PowerSaveMode: u32,
    pub uPowerSaveLevel: u32,
    pub Reason: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OFFLOAD_NETWORK_LIST_INFO {
    pub Header: u32,
    pub ulFlags: u32,
    pub FastScanPeriod: u32,
    pub FastScanIterations: u32,
    pub SlowScanPeriod: u32,
    pub uNumOfEntries: u32,
    pub offloadNetworkList: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OFFLOAD_NETWORK_STATUS_PARAMETERS {
    pub Header: u32,
    pub Status: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_TEST {
    pub dot11ManufacturingTestType: u32,
    pub uBufferLength: u32,
    pub ucBuffer: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_SELF_TEST_SET_PARAMS {
    pub SelfTestType: u32,
    pub uTestID: u32,
    pub uPinBitMask: u32,
    pub pvContext: usize,
    pub uBufferLength: u32,
    pub ucBufferIn: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_SELF_TEST_QUERY_RESULTS {
    pub SelfTestType: u32,
    pub uTestID: u32,
    pub bResult: u8,
    pub uPinFailedBitMask: u32,
    pub pvContext: usize,
    pub uBytesWrittenOut: u32,
    pub ucBufferOut: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_FUNCTIONAL_TEST_RX {
    pub bEnabled: u8,
    pub Dot11Band: u32,
    pub uChannel: u32,
    pub PowerLevel: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_FUNCTIONAL_TEST_TX {
    pub bEnable: u8,
    pub bOpenLoop: u8,
    pub Dot11Band: u32,
    pub uChannel: u32,
    pub uSetPowerLevel: u32,
    pub ADCPowerLevel: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_FUNCTIONAL_TEST_QUERY_ADC {
    pub Dot11Band: u32,
    pub uChannel: u32,
    pub ADCPowerLevel: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_TEST_SET_DATA {
    pub uKey: u32,
    pub uOffset: u32,
    pub uBufferLength: u32,
    pub ucBufferIn: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_TEST_QUERY_DATA {
    pub uKey: u32,
    pub uOffset: u32,
    pub uBufferLength: u32,
    pub uBytesRead: u32,
    pub ucBufferOut: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_MANUFACTURING_TEST_SLEEP {
    pub uSleepTime: u32,
    pub pvContext: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_IO_REQUEST {
    pub dwProtocol: u32,
    pub cbPciLength: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_T0_COMMAND {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_T0_REQUEST {
    pub ioRequest: u32,
    pub CmdBytes: u32,
    pub rgbHeader: [u8; 5],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SCARD_T1_REQUEST {
    pub ioRequest: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINUSB_PIPE_INFORMATION {
    pub PipeType: u32,
    pub PipeId: u8,
    pub MaximumPacketSize: u16,
    pub Interval: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WINUSB_PIPE_INFORMATION_EX {
    pub PipeType: u32,
    pub PipeId: u8,
    pub MaximumPacketSize: u16,
    pub Interval: u8,
    pub MaximumBytesPerInterval: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_SSID {
    pub uSSIDLength: u32,
    pub ucSSID: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DOT11_OI {
    pub OILength: u16,
    pub OI: [u8; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WNODE_HEADER {
    pub BufferSize: u32,
    pub ProviderId: u32,
    pub HistoricalContext: u32,
    pub Version: u32,
    pub Linkage: u32,
    pub CountLost: u32,
    pub KernelHandle: usize,
    pub TimeStamp: i64,
    pub Guid: u32,
    pub ClientContext: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_ADDRESS {
    pub lpSockaddr: u32,
    pub iSockaddrLength: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_ADDRESS_LIST {
    pub iAddressCount: i32,
    pub Address: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct CSADDR_INFO {
    pub LocalAddr: u32,
    pub RemoteAddr: u32,
    pub iSocketType: i32,
    pub iProtocol: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKET_PROCESSOR_AFFINITY {
    pub Processor: usize,
    pub NumaNodeId: u16,
    pub Reserved: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSABUF {
    pub len: u32,
    pub field_buf: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSAMSG {
    pub name: u32,
    pub namelen: i32,
    pub lpBuffers: usize,
    pub dwBufferCount: u32,
    pub dwBufferCount_1: u32,
    pub Control: u32,
    pub dwFlags: u32,
    pub dwFlags_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WSACMSGHDR {
    pub cmsg_len: usize,
    pub cmsg_level: i32,
    pub cmsg_type: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERFACE_INFO {
    pub iiFlags: u32,
    pub iiAddress: u32,
    pub iiBroadcastAddress: u32,
    pub iiNetmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct INTERFACE_INFO_EX {
    pub iiFlags: u32,
    pub iiAddress: u32,
    pub iiBroadcastAddress: u32,
    pub iiNetmask: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SOCKADDR_IN6_PAIR {
    pub SourceAddress: usize,
    pub DestinationAddress: usize,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RECTL {
    pub left: i32,
    pub top: i32,
    pub right: i32,
    pub bottom: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct POINTL {
    pub x: i32,
    pub y: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userCLIPFORMAT {
    pub fContext: i32,
    pub dwValue: u32,
    pub field_pwszName: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct GDI_NONREMOTE {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHGLOBAL {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHMETAFILE {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct remoteMETAFILEPICT {
    pub mm: i32,
    pub xExt: i32,
    pub yExt: i32,
    pub field_hMF: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHMETAFILEPICT {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHENHMETAFILE {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userBITMAP {
    pub bmType: i32,
    pub bmWidth: i32,
    pub bmHeight: i32,
    pub bmWidthBytes: i32,
    pub bmPlanes: u16,
    pub bmBitsPixel: u16,
    pub cbSize: u32,
    pub pBuffer: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHBITMAP {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct userHPALETTE {
    pub fContext: i32,
    pub hInproc: u32,
    pub field_hRemote: u32,
    pub hInproc64: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct RemotableHandle {
    pub fContext: i32,
    pub hInproc: u32,
    pub hRemote: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct PROPERTYKEY {
    pub fmtid: u32,
    pub pid: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct uCLSSPEC {
    pub tyspec: u32,
    pub clsid: u32,
    pub pFileExt: u32,
    pub pMimeType: u32,
    pub pProgId: u32,
    pub pFileName: u32,
    pub pPackageName: usize,
    pub PolicyId: u32,
    pub ObjectId: u32,
    pub PolicyId_1: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SYSTEMTIME {
    pub wYear: u16,
    pub wMonth: u16,
    pub wDayOfWeek: u16,
    pub wDay: u16,
    pub wHour: u16,
    pub wMinute: u16,
    pub wSecond: u16,
    pub wMilliseconds: u16,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct SECURITY_ATTRIBUTES {
    pub nLength: u32,
    pub lpSecurityDescriptor: usize,
    pub bInheritHandle: i32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COAUTHIDENTITY {
    pub field_User: u32,
    pub UserLength: u32,
    pub field_Domain: u32,
    pub DomainLength: u32,
    pub field_Password: u32,
    pub PasswordLength: u32,
    pub Flags: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct COAUTHINFO {
    pub dwAuthnSvc: u32,
    pub dwAuthzSvc: u32,
    pub pwszServerPrincName: usize,
    pub dwAuthnLevel: u32,
    pub dwImpersonationLevel: u32,
    pub field_pAuthIdentityData: u32,
    pub dwCapabilities: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BYTE_BLOB {
    pub clSize: u32,
    pub abData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WORD_BLOB {
    pub clSize: u32,
    pub asData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DWORD_BLOB {
    pub clSize: u32,
    pub alData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLAGGED_BYTE_BLOB {
    pub fFlags: u32,
    pub clSize: u32,
    pub abData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct FLAGGED_WORD_BLOB {
    pub fFlags: u32,
    pub clSize: u32,
    pub asData: [u32; 1],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct BYTE_SIZEDARR {
    pub clSize: u32,
    pub field_pData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct WORD_SIZEDARR {
    pub clSize: u32,
    pub field_pData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct DWORD_SIZEDARR {
    pub clSize: u32,
    pub field_pData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct HYPER_SIZEDARR {
    pub clSize: u32,
    pub field_pData: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NDIS_OBJECT_HEADER {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_CHECKSUM {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_NET_MEMORY {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_EXTENSION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_GSO {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_IEEE8021Q {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_LOGICAL_ADDRESS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_MDL {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_LAYOUT {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_HASH {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_RETURN_CONTEXT {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_RING_COLLECTION {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_RSC {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_RSC_TIMESTAMP {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_VIRTUAL_ADDRESS {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_FRAGMENT_WDFMEMORY {
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct NET_PACKET_WIFI_EXEMPTION_ACTION {
    pub ExemptionAction: u32,
}

