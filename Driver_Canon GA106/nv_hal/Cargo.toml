[package]
name = "nv_hal"
version.workspace = true
edition.workspace = true
description = "Hardware Abstraction Layer — MMIO, PCI config space, DMA, timers"

[dependencies]
nv_error = { path = "../nv_error" }
nv_regs = { path = "../nv_regs" }

[features]
default = []
std = []
