[package]
name = "nv_cmd"
version.workspace = true
edition.workspace = true
description = "FIFO command submission — channels, pushbuffers, fences"

[dependencies]
nv_error = { path = "../nv_error" }
nv_regs = { path = "../nv_regs" }
nv_hal = { path = "../nv_hal" }

[features]
default = []
std = []
