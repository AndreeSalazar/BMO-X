[package]
name = "bmo-abi"
version = "1.0.0"
edition = "2021"
authors = ["BMO Project"]
description = "BMO ABI — types, syscall definitions, interfaces (foundation layer)"

[dependencies]
bitflags = "2.13.0"
