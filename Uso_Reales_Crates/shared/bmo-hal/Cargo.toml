[package]
name = "bmo-hal"
version = "1.0.0"
edition = "2021"
authors = ["BMO Project"]
description = "BMO HAL — hardware abstraction layer types shared across rings"

[dependencies]
bmo-boot-protocol = { path = "../../../kernel/Boot_Protocol" }
