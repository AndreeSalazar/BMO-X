[package]
name = "bmo-ahci"
version = "0.1.0"
edition = "2021"
description = "BMO AHCI/SATA Storage Driver — HAL-based controller driver"

[lib]

[dependencies]
bmo-abi = { path = "../../../abi/bmo-abi" }
