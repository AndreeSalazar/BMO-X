# BMO Symbol Registry — auto-generated by bmo-linker
# Maps module:category -> symbol -> virtual address
# v2: capability-based discovery for provides/requires resolution

[timeback._module_start]
addr = 31000000
provides = "versioning.checkpoint, versioning.rollback, versioning.snapshot"
requires = "storage.write"

[cabina._module_start]
addr = 31100000
provides = "telemetry.log, telemetry.metrics, telemetry.persist"
requires = "serial.write"

[bmo_core._module_start]
addr = 30400000
provides = "framebuffer.write, input.poll, desktop.render"
requires = ""

[linux_devour._module_start]
addr = 31200000
provides = "elf.devour, elf.translate, elf.execute"
requires = "heap.alloc, serial.write"
