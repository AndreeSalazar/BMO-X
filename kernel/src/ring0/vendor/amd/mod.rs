//! `vendor/amd/` — AMD hardware profiles.
//!
//! v1.8.8: only the CPU side (`cpu/`) is populated. The GPU side
//! (`gpu/`) is a skeleton — RDNA4 (RX 9060 XT) will land here.

pub mod cpu;
// pub mod gpu; // v1.8.8: not yet — RDNA4 driver is a Phase 4 task
