[package]
name = "bmo-kernel"
version = "2.0.0"
edition = "2021"
authors = ["BMO Project"]
description = "BMO kernel — pure Ring 0 (arch, mm, dev, cpu, proc)"
license = "Techne"

[dependencies]
bmo-boot-protocol = { path = "../crates_Personal/boot_protocol" }
cpu-vendor-profile = { path = "../crates_Personal/cpu_vendor_profile" }
nvram-log = { path = "../crates_Personal/nvram-log" }
bmo-hal-defs = { path = "../crates_Personal/bmo_hal_defs" }
llfree = { path = "../crates_Personal/llfree-rs/core", optional = true }
volatile = "0.4"
bitflags = "2"
spin = "0.9"

[features]
default = ["alloc-buddy", "syscalls-base"]
alloc-buddy = []
alloc-llfree = ["llfree"]
syscalls-base = []
syscalls-net = []
syscalls-gpu = []
syscalls-ipc = []
syscalls-audio = []
    
[[bin]]
name =     "bmo-kernel"
path = "src/main.rs"
