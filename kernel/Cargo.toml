[package]
name = "fastos-kernel"
version = "0.1.0"
edition = "2021"
authors = ["FastOS Project"]
description = "FastOS kernel — Ryzen 5 5600X + RTX 3060 12G"
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
