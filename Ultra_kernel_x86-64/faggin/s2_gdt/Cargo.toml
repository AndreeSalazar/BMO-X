[workspace]
[package]
name = "s2-gdt"
version = "2.0.0"
edition = "2021"
description = "Faggin stage 2 ? see Ultra_kernel/fagging/README.md"

[dependencies]
serial-shared = { path = "../serial_shared" }
boot-context = { path = "../../boot_context" }


[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = 'symbols'
