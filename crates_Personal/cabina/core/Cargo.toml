[package]
name = "cabina-core"
version = "1.0.0"
edition = "2021"
description = "FastOS CABINA Core — diagnostic types shared across Ring 0 and Ring 3"
license = "MIT"

[features]
default = []
std = []

[dependencies]
