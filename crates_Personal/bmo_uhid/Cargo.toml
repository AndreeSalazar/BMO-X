[package]
name = "bmo-uhid"
version = "0.1.0"
edition = "2021"
description = "BMO USB HID Driver — boot protocol keyboard/mouse via xHCI"

[lib]

[dependencies]
bmo-xhci = { path = "../bmo_xhci" }
bmo-input = { path = "../bmo_input" }
