[package]
name = "mod-timeback"
version = "1.0.0"
edition = "2021"
authors = ["BMO Project"]
description = "TimeBack module — standalone Ring 3 versioning system loaded by bootloader"

[dependencies]
timeback = { path = "../timeback" }
bmo-hal-defs = { path = "../bmo_hal_defs" }
