[workspace]

[package]
name = "mod-linux-devour"
version = "1.0.0"
edition = "2021"
description = "BMO Linux Devourer — ELF to BEF runtime converter (Ring 3 module)"

[dependencies]
bmo-hal-defs = { path = "../../shared/hal_defs" }
bmo-abi = { path = "../../../Uso_Reales_Crates/abi/bmo-abi" }
goblin = { path = "../../plugins/goblin", default-features = false, features = ["alloc", "elf32", "elf64", "endian_fd"] }
