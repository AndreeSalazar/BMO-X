[package]
name = "mod-cabina"
version = "1.0.0"
edition = "2021"
authors = ["BMO Project"]
description = "Cabina module — standalone Ring 3 telemetry/logging system loaded by bootloader"

[dependencies]
cabina-daemon = { path = "../../services/cabina/daemon" }
bmo-hal-defs = { path = "../../shared/hal_defs" }
