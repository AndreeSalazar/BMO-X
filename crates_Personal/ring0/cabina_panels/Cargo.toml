[package]
name = "cabina-panels"
version = "1.0.0"
edition = "2021"
description = "BMO CABINA Panels â€” HUD rendering, query DSL, overlay (Ring 0)"

[features]
default = []

[dependencies]
cabina-core = { path = "../../../Uso_Reales_Crates/services/cabina-core" }
