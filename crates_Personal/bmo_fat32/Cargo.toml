[package]
name = "bmo-fat32"
version = "0.1.0"
edition = "2021"
description = "BMO FAT32 filesystem reader — cluster chain traversal, 8.3 name lookup"

[lib]

[dependencies]
bmo-ahci = { path = "../bmo_ahci" }
log = "0.4"
